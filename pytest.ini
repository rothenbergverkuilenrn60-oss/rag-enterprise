[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
timeout = 60
log_cli = true
log_cli_level = INFO
markers =
    integration: integration tests requiring real external dependencies (e.g., paddleocr models, network)
addopts = -m "not integration"
