# Milestone v1.1: Retrieval Depth & Frontend

**Status:** ✅ SHIPPED 2026-05-08
**Phases:** 7–10
**Total Plans:** 9
**Total Commits:** 70
**Timeline:** 2026-04-27 → 2026-05-08 (12 days)

## Overview

Closed v1.0's image-PDF retrieval gap (`data/raw/GB4785-2019.pdf` returned wrong sources for "第N页" / section-scoped queries because captions did not carry page or section context). Added PP-StructureV3 layout-aware OCR with bounded concurrency and Docker-baked models, GB-style section walker producing `section_id`/`section_title` metadata, pgvector JSONB filter retrieval with HNSW iterative scan + GUCs, regex-based Chinese query filter extractor (`第63页` / `3.10节`), inline `_UI_HTML` extraction to `static/ui.html` served via FastAPI StaticFiles, and a CI diff-coverage gate (`diff-cover` ≥ 80%) protecting newly-touched v1.1 files while leaving the legacy 46% floor as a separate informational metric.

## Phases

### Phase 7: OCR Engine Integration

**Goal:** Pure-image PDFs ingest with real per-page text and tables extracted by PP-StructureV3, running async-safe under bounded concurrency with models baked into the Docker image.
**Depends on:** v1.0 Phase 4 (image extraction pipeline must exist; OCR text path runs alongside it)
**Requirements:** OCR-01, OCR-02
**Success Criteria:**
1. Ingesting `data/raw/GB4785-2019.pdf` (an image-only PDF) produces non-empty `chars > 0` and at least one chunk per page span — verified end-to-end through the existing `/ingest` endpoint.
2. PP-StructureV3 is invoked exactly once per process (singleton); concurrent ingest requests do not exceed `settings.ocr_concurrency` parallel OCR calls.
3. Building the production Docker image produces a self-contained container that performs its first OCR request within 5 seconds of worker startup (no runtime model download).
4. An OCR call exceeding `settings.ocr_timeout_sec` retries once and, on second failure, surfaces in `IngestionResponse.extraction_errors` rather than crashing the worker.
5. Environments without PaddleOCR installed continue to use the documented Tesseract fallback with no behavioural regression.

**Plans:** 2 plans
- [x] 07-01-PLAN.md — OcrEngine module + PP-StructureV3 singleton + Tesseract fallback adapter + settings/deps (OCR-01/02 partial)
- [x] 07-02-PLAN.md — Docker model bake + ARQ worker pre-warm + tenacity timeout retry + garbled-CJK heuristic + GB4785 e2e integration test (OCR-01/02 complete)

---

### Phase 8: Multimodal Metadata + Query Filter

**Goal:** A user typing "第63页…" or "3.10节中的…" gets the page/section lifted into a metadata filter and returns the matching chunk in the top-3, with section-heading context aiding semantic recall but page numbers never polluting the embedding space.
**Depends on:** Phase 7 (chunker needs OCR text + reading-order blocks before it can attach `section_id` / `section_title`)
**Requirements:** META-01, META-02, QUERY-01
**Success Criteria:**
1. A chunk from a known section reads `"3.10 定义的透光面\n\n<body>"` in `content_with_header`, while `metadata.section_id` is `"3.10"` and `metadata.section_title` is `"定义的透光面"` — page numbers and section numeric IDs do not appear in the embedded text.
2. A filtered query for a known `(page_number=63, query)` pair returns the matching chunk in the top-3 against `PgVectorStore`; the same query without filter still works at unchanged recall.
3. A user query "第63页灯具的发光面" reaches `vector_store.search()` with `filters={"page_number": 63}` and the literal "第63页" stripped from the embedded query text.
4. Image-caption chunks produced by Phase 4's pipeline carry the host page's `page_number` and `section_id` and are retrievable by page-scoped queries.
5. Existing legacy chunks ingested before v1.1 (without section context) load and search without errors.

**Plans:** 5 plans
- [x] 08-01-PLAN.md — Wave 0 schema + settings + pgvector ≥ 0.8.0 gate + RED test scaffolds (META-01, META-02, QUERY-01)
- [x] 08-02-PLAN.md — services/nlu/filter_extractor.py + unit tests (QUERY-01)
- [x] 08-03-PLAN.md — Chunker GB section walker + D-02 content_with_header + D-04 image-caption injection (META-01)
- [x] 08-04-PLAN.md — PgVectorStore B-tree expression indexes + filtered HNSW search + SET LOCAL hnsw GUCs (META-02)
- [x] 08-05-PLAN.md — Pipeline wiring (extract_filters → tf merge → effective_query) + e2e propagation integration test (QUERY-01, META-02)

---

### Phase 9: Frontend Extraction

**Goal:** The inline UI is a real `static/ui.html` file editable with normal frontend tooling, served by FastAPI StaticFiles at `/ui/`, with no behavioural change versus the v1.0 inline page.
**Depends on:** Nothing (parallel with Phase 7/8 — independent track)
**Requirements:** UI-01
**Success Criteria:**
1. `static/ui.html` exists as a standalone file; `main.py` no longer contains a `_UI_HTML` triple-quoted string and the `/ui` route is replaced by `app.mount("/ui", StaticFiles(directory="static", html=True))`.
2. Visiting `http://localhost:8000/ui/` renders the UI, calls `/api/v1/query` with `include_images: true`, and renders answer + sources + images identically to the v1.0 inline version.
3. The production Docker image includes `static/` via `COPY` and serves the page without any bind-mount or extra volume.
4. The page renders without horizontal scroll on a 1280×800 viewport; source images cap at the container width.

**Plans:** 1 plan
- [x] 09-01-PLAN.md — Extract `_UI_HTML` to `static/ui.html`, mount StaticFiles in main.py, verify Dockerfile COPY, integration test + human visual verify (UI-01)

---

### Phase 10: Coverage Gate on New Code

**Goal:** Any v1.1 PR touching a file leaves that file at ≥ 80% line coverage on the changed lines; legacy modules continue to track the v1.0 46% baseline as a separate metric.
**Depends on:** Nothing (tooling layer — runs against whatever code has shipped). Landed last so it gates Phase 7–9 deliverables on the way through CI.
**Requirements:** TEST-03
**Success Criteria:**
1. A PR that modifies a v1.1 file with < 80% diff coverage fails the CI `coverage-diff` step; a PR meeting the threshold passes.
2. `make coverage-diff` run locally against `git diff origin/master...HEAD` produces the same pass/fail verdict as CI.
3. The CI run attaches an HTML diff-coverage report as a downloadable GitHub Actions artifact.
4. The legacy 46% global floor remains a separate informational CI step and continues to pass for unchanged files.

**Plans:** 1 plan
- [x] 10-01-PLAN.md — diff-cover dependency pin + CI step against v1.0 + Makefile coverage-diff target + README docs (TEST-03 complete in single wave)

---

## Milestone Summary

### Key Decisions
- **Phase 7 D-01:** PP-StructureV3 singleton per process — concurrency capped via `settings.ocr_concurrency` semaphore.
- **Phase 7 D-02:** Tesseract is documented fallback only; production runs PP-StructureV3 baked into image (no runtime model download).
- **Phase 8 D-02:** `content_with_header` form locked to `f"{section_id} {section_title}\n\n{body}"` — page numbers never enter embedded text (preserves embedding space cleanliness).
- **Phase 8 D-04:** Image-caption chunks inherit host page's `section_id` AND inject `图片位于第N页` prefix into `chat_with_vision` query.
- **Phase 8 T-08-01 mitigation:** Filter values pass through asyncpg `$N` parameters; the only safe f-string surface in `vector_store.search` is `int(settings.pgvector_ef_search_filtered)` for the `SET LOCAL hnsw.ef_search` GUC.
- **Phase 8 T-08-09:** `page_number=0` is the "unknown" sentinel — stripped from effective filter before WHERE clause is built.
- **Phase 8 RLS precedence:** `set_config('app.current_tenant', $1, true)` runs BEFORE GUCs and SELECT inside the same transaction.
- **Phase 9 D-01..D-04:** Pure refactor; inline `<script>` and `<style>` stay in `ui.html`; accept FastAPI default 307 redirect for `/ui` → `/ui/`; byte-for-byte content (no variable renames, no DOM API rewrites).
- **Phase 9 deviation surfaced at checkpoint:** `static/index.html → ui.html` symlink added because `StaticFiles(html=True)` looks for `index.html`. Preserves SC #1 file-name AND makes SC #2 work.
- **Phase 10 D-01+D-02 split:** CI compares vs `v1.0` git tag (REQ C-1 milestone-baseline ref); local `make coverage-diff` compares vs `origin/master...HEAD` (SC #2 dev-loop ref). Each ref serves the use case its source spec was written for.
- **Phase 10 D-03..D-05:** Diff-coverage scope unit-only; append step to existing `unit-tests` job (no new workflow); hard block, `--fail-under=80`, no `continue-on-error`, no override comments.
- **branching_strategy `none`:** Whole milestone shipped as PR #1 from `gsd/phase-8-multimodal-metadata-query-filter` since branching_strategy=none.

### Issues Encountered
- Phase 7 — pre-existing test-ordering flake in `test_worker_startup` surfaced (passes in isolation, fails after siblings); logged to `deferred-items.md`.
- Phase 8 — pre-existing collection error in `test_ragas_eval.py` (PermissionError on `/app` in WSL); pre-v1.1.
- Phase 8 — 6 pre-existing ruff F401/E741 entries surfaced; all pre-Phase-8.
- Phase 9 — `StaticFiles(html=True)` looks for `index.html`, not `ui.html`. Surfaced at executor checkpoint; resolved with symlink.
- Phase 10 — local `make coverage-diff` deliberately drops `--cov-fail-under=46` (focuses on diff verdict only); divergence from strict ROADMAP SC #2 reading but aligns with REQ TEST-03 AC #4.

### Tech Debt Carried Forward
- Visual diff vs v1.0 in browser (Phase 9 SC #2) — deferred to first deploy
- Real 1280×800 viewport render check (Phase 9 SC #4) — deferred to first deploy
- Production Docker image build with the symlink (Phase 9 SC #3) — deferred to first deploy
- Live PR through CI confirms `coverage-diff` step + HTML artifact (Phase 10 SC #1, #3) — natural confirmation on first PR through merged v1.1 CI
- Production DSN pgvector ≥ 0.8.0 server gate verification (Phase 8) — manual check on deploy
- Full GB4785-2019.pdf live OCR + ingest e2e (Phase 7) — pending image rebuild

### Deferred to v1.2+ (captured during v1.1)
- LLM-based filter extractor (regex-only in v1.1; LLM fallback)
- React / Vue / Streamlit frontend (single static HTML is the v1.1 ceiling)
- Reaching 80% coverage on legacy modules
- MinerU / PP-OCRv5 raw replacement of PP-StructureV3
- OCR container isolation as a separate microservice
- Query-time pgvector `ef_search` auto-tuning
- JS / CSS extraction from `static/ui.html` (Phase 9 D-01/D-02 deferred)
- Inline JS modernization (DOM API / template literals / esc audit)
- Explicit `/ui` no-slash route (only if reverse-proxy needs it)
- JS test framework for `static/ui.html`
- Integration-test coverage merging via `coverage combine`
- Per-file `# coverage:ignore-diff` overrides
- Provider-agnostic agentic layer + parallel tool-call burst + true swarm (office-hours design APPROVED, deferred to v1.2 milestone)
