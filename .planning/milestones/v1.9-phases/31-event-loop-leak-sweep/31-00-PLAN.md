---
phase: 31-event-loop-leak-sweep
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/factories/app.py
  - tests/integration/conftest.py
  - tests/unit/test_singleton_inventory_complete.py
  - .planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md
autonomous: false
requirements:
  - EVT-02

must_haves:
  truths:
    - "PG-host integration run reports zero matches against the D-01 broader-regex (3 loop-error shapes)."
    - "_SINGLETON_INVENTORY grows from 34 to 34 + N where N = surfaced count (no padding entries)."
    - "tests/unit/test_singleton_inventory_complete.py passes with the new count."
    - "Integration-suite green count does NOT regress vs v1.8 close baseline (9 failed / 32 passed / 1 skipped / 3 errors)."
    - "tests/integration/conftest.py autouse embedder/reranker mock (Phase 30-02 carry-forward) remains active and unregressed."
    - "No new mypy --strict silences introduced (would otherwise count against Phase 32 MYPY-02 drain)."
    - "Pre-existing 9 failed + 3 errors from v1.8 close stay pre-existing (D-04 filter — Phase 31 does not own those)."
  artifacts:
    - path: "tests/factories/app.py"
      provides: "_SINGLETON_INVENTORY grown by N verified (module_path, attr) tuples; each entry resolvable via importlib.import_module + hasattr."
      contains: "_SINGLETON_INVENTORY"
    - path: "tests/integration/conftest.py OR per-test conftest"
      provides: "Per-test event_loop fixture(s) for factory-unfit leak sites (Wave B outliers)."
    - path: "tests/unit/test_singleton_inventory_complete.py"
      provides: "Lint pass at new count (data-driven scan; no hard-coded count expected, but verify and patch if needed)."
    - path: ".planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md"
      provides: "Per-site triage (factory-fit vs factory-unfit decision per surfaced site) + acceptance evidence."
  key_links:
    - from: "tests/factories/app.py::_SINGLETON_INVENTORY"
      to: "tests/factories/app.py::_reset_singletons"
      via: "tuple iteration + importlib.import_module + setattr(mod, attr, None)"
      pattern: "_reset_singletons"
    - from: "tests/integration/conftest.py autouse mock"
      to: "services.vectorizer.embedder.HuggingFaceEmbedder.__init__ + services.retriever.retriever.CrossEncoderReranker.__init__"
      via: "patch.object inside autouse fixture"
      pattern: "patch\\.object\\(.*HuggingFaceEmbedder"
    - from: "D-01 enumeration regex"
      to: "PG-host pytest -m integration --uses-redis output"
      via: "grep -E '(no current event loop|attached to a different loop|got Future.*attached)'"
      pattern: "attached to a different loop"
---

<objective>
Eliminate residual module-level singleton-bound-to-import-time-loop failures so the PG-host integration suite reports zero asyncio "different loop" errors and `_SINGLETON_INVENTORY` reaches authoritative coverage.

Purpose: Close out Phase 30 Plan 30-01 supersession. v1.8 left ~10 leak sites deferred to v1.9 because enumeration requires a PG-enabled host (now available — `docker rag-postgres` healthy as of session start). EVT-02 is the closeout requirement.

Output: Extended `_SINGLETON_INVENTORY` (34 → 34+N entries — N is execute-time-truth, not padded), per-test `event_loop` fixtures for factory-unfit outliers, lint pass, zero-error grep, and a per-site triage SUMMARY.

Scope discipline: ZERO production-code change. Test infra refactor only. Pre-existing 9+3 failures from v1.8 close (Phase 33+34 scope) are filtered by D-01 regex at parse time — Phase 31 does NOT fix them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-event-loop-leak-sweep/31-CONTEXT.md
@.planning/milestones/v1.8-phases/30-test-infra-mypy-hardening/30-CONTEXT.md
@.planning/milestones/v1.8-phases/30-test-infra-mypy-hardening/30-VERIFICATION.md
@.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-02-SUMMARY.md
@tests/factories/app.py
@tests/integration/conftest.py
@tests/unit/test_singleton_inventory_complete.py

<interfaces>
<!-- Key contracts the executor MUST use directly — no codebase exploration needed. -->

From tests/factories/app.py (current baseline, 34 entries):
```python
_SINGLETON_INVENTORY: tuple[tuple[str, str], ...] = (
    ("services.nlu.nlu_service",              "_nlu_service"),
    ...  # 33 more entries — see file
)

def _reset_singletons() -> None:
    for module_path, attr in _SINGLETON_INVENTORY:
        mod = importlib.import_module(module_path)
        if hasattr(mod, attr):
            setattr(mod, attr, None)

def create_app(*, dependency_overrides=None) -> FastAPI: ...
```

From tests/unit/test_singleton_inventory_complete.py (lint — DATA-DRIVEN, not count-hardcoded):
```python
# Scans services/ for ^_X = None lines; asserts each is in _SINGLETON_INVENTORY
# OR in _SKIP. Adding entries does NOT require lint patch unless _SKIP semantics change.
_SKIP: set[tuple[str, str]] = {
    ("services/generator/generator.py",  "_tiktoken_enc"),
    ("services/generator/llm_client.py", "_anthropic_rate_limit_cls"),
    ("services/generator/llm_client.py", "_anthropic_overload_cls"),
    ("services/extractor/ocr_engine.py", "_sem"),
}
```
**Implication:** Adding new entries to `_SINGLETON_INVENTORY` will NOT break the lint. Lint patch only needed IF a new leak site is in `services/` and module-level `_X = None`, in which case adding it to inventory is the fix (not a lint code change).

From tests/integration/conftest.py (Phase 30-02 carry-forward — DO NOT REGRESS):
```python
@pytest.fixture(autouse=True)
def _mock_local_model_inits() -> object:
    # Patches HuggingFaceEmbedder.__init__ + CrossEncoderReranker.__init__
    # autouse=True at tests/integration/ scope
    ...
```

D-01 enumeration command (VERBATIM — do NOT paraphrase the regex):
```bash
uv run pytest -m integration --uses-redis -v 2>&1 \
  | grep -E "(no current event loop|attached to a different loop|got Future.*attached)" \
  | sort -u > /tmp/31-00-leak-sites.txt
wc -l /tmp/31-00-leak-sites.txt
```

v1.8 close baseline (D-04 reference — these stay pre-existing, NOT fixed by Phase 31):
```
9 failed / 32 passed / 1 skipped / 3 errors (125s)
```
Categories: real-LLM, perf-bench, UI sentinel drift (Phase 34 TEST-11), schema drift (Phase 34 TEST-10), real-embedder requirements (Phase 33 TEST-08).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: PG-host availability + baseline state capture</name>
  <files>/tmp/31-00-baseline.txt (scratch)</files>
  <read_first>
    - tests/factories/app.py (confirm _SINGLETON_INVENTORY tuple count = 34 pre-fix)
    - .planning/phases/31-event-loop-leak-sweep/31-CONTEXT.md §Acceptance + §Open Risks
    - .planning/milestones/v1.8-phases/30-test-infra-mypy-hardening/30-VERIFICATION.md §re_verification (v1.8 baseline numbers)
  </read_first>
  <action>
    Verify pre-conditions before enumeration:
    1. Confirm `docker rag-postgres` is healthy: `docker ps --filter "name=rag-postgres" --format "{{.Names}} {{.Status}}"` — must show `Up ... (healthy)`. Halt if not healthy.
    2. Confirm `_SINGLETON_INVENTORY` baseline count: `uv run python -c "from tests.factories.app import _SINGLETON_INVENTORY; print(len(_SINGLETON_INVENTORY))"` — MUST print `34`. If not 34, halt and surface to user (inventory may have drifted since CONTEXT.md was written).
    3. Run baseline lint pass to confirm current state is green: `uv run pytest tests/unit/test_singleton_inventory_complete.py -v` — MUST pass. If fails, fix lint first (likely a new services/ singleton landed since v1.8 close) before proceeding.
    4. Write `/tmp/31-00-baseline.txt` documenting: PG-host status, inventory count, lint result, current git HEAD. This is the reference state for Task 6 regression check.

    Do NOT run enumeration yet — Task 1 owns that.
  </action>
  <verify>
    <automated>docker ps --filter "name=rag-postgres" --format "{{.Status}}" | grep -q "healthy" && uv run python -c "from tests.factories.app import _SINGLETON_INVENTORY; assert len(_SINGLETON_INVENTORY) == 34, len(_SINGLETON_INVENTORY)" && uv run pytest tests/unit/test_singleton_inventory_complete.py -v --tb=short</automated>
  </verify>
  <done>PG-host healthy; _SINGLETON_INVENTORY count = 34 confirmed; lint green; baseline snapshot written to /tmp/31-00-baseline.txt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run D-01 enumeration command + checkpoint on N</name>
  <files>/tmp/31-00-leak-sites.txt</files>
  <read_first>
    - .planning/phases/31-event-loop-leak-sweep/31-CONTEXT.md §D-01 (verbatim regex) + §Open Risks (range 5-25+)
  </read_first>
  <action>
    Execute the D-01 enumeration command VERBATIM (do NOT paraphrase the grep regex — the three error shapes are deliberate):

    ```bash
    uv run pytest -m integration --uses-redis -v 2>&1 \
      | grep -E "(no current event loop|attached to a different loop|got Future.*attached)" \
      | sort -u > /tmp/31-00-leak-sites.txt
    wc -l /tmp/31-00-leak-sites.txt
    ```

    Capture three artifacts:
    1. `/tmp/31-00-leak-sites.txt` — unique loop-error lines (N matches; site count derived from these)
    2. `/tmp/31-00-full-pytest.log` — full pytest output (run the same pytest command separately if needed; needed for Task 6 baseline compare). Save via: `uv run pytest -m integration --uses-redis -v 2>&1 | tee /tmp/31-00-full-pytest.log`
    3. Site-count line + first 5 unique error lines printed inline for user review.

    **CHECKPOINT — DO NOT PROCEED TO TASK 2 UNTIL USER APPROVES:**

    Report to user (concise):
    - N (line count from `/tmp/31-00-leak-sites.txt`)
    - First 5 sample error lines (so user can sanity-check shapes)
    - Test files implicated (extract via `grep -oE "tests/[^:]*\.py" /tmp/31-00-leak-sites.txt | sort -u`)
    - Comparison to estimate: "~10 estimated per CONTEXT D-EVT-01; actual = N. D-02 makes count descriptive, but flagging because [matches / under-shoots / over-shoots]."

    Per CONTEXT.md §Open Risks: "executor must surface the actual N to the user before bulk remediation." Pause for explicit approval before Task 2.
  </action>
  <what-built>D-01 enumeration command executed; /tmp/31-00-leak-sites.txt written with N unique loop-error lines from PG-host integration run.</what-built>
  <how-to-verify>
    1. Read `/tmp/31-00-leak-sites.txt` — confirm contents look like real asyncio loop errors (not pytest noise).
    2. Confirm `wc -l /tmp/31-00-leak-sites.txt` matches the N reported above.
    3. Decide: proceed (approve N as the surfaced count), or halt (request re-run with different invocation, or surface a Phase 31 scope concern).
  </how-to-verify>
  <resume-signal>Type "approved N=<count>" to proceed to Task 2, or describe issues (e.g., "re-run with --no-header", "N looks wrong, investigate before remediation", "shapes look unrelated to event-loop").</resume-signal>
  <verify>
    <automated>test -f /tmp/31-00-leak-sites.txt && test -f /tmp/31-00-full-pytest.log && [ "$(wc -l < /tmp/31-00-leak-sites.txt)" -ge 0 ] && docker ps --filter "name=rag-postgres" --format "{{.Status}}" | grep -q "healthy"</automated>
  </verify>
  <done>/tmp/31-00-leak-sites.txt + /tmp/31-00-full-pytest.log written; PG container still healthy post-run (per ARCH-02 review — guards against false-positive zero-matches if container died mid-run); N + sample shapes + implicated test files surfaced to user; user-approved N is the binding count for Tasks 2-7.</done>
</task>

<task type="auto">
  <name>Task 2: Per-site triage — factory-fit vs factory-unfit</name>
  <files>/tmp/31-00-triage.md (scratch)</files>
  <read_first>
    - /tmp/31-00-leak-sites.txt (output of Task 1)
    - /tmp/31-00-full-pytest.log (for traceback context per site)
    - tests/factories/app.py (factory pattern + _SINGLETON_INVENTORY conventions)
    - .planning/milestones/v1.8-phases/30-test-infra-mypy-hardening/30-CONTEXT.md §EVT-01 (factory-default + per-test fixture for outliers convention)
    - .planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-02-SUMMARY.md (original factory rationale)
  </read_first>
  <action>
    For each unique surfaced site (line in `/tmp/31-00-leak-sites.txt`):
    1. Trace back to the offending module:attr in `/tmp/31-00-full-pytest.log` — find the FAIL/ERROR line + traceback that emitted that loop-error shape. Identify the module-level singleton being bound to a stale loop.
    2. Classify per CONTEXT §D-EVT-01 convention:
       - **factory-fit (Wave A — Task 3):** Singleton lives in `services/<area>/<module>.py` as a module-level `_X = None` cache. Fix = add `(module_path, attr_name)` tuple to `_SINGLETON_INVENTORY`. `_reset_singletons()` will then zero it per test via the existing `create_app()` factory.
       - **factory-unfit (Wave B — Task 4):** Singleton lives outside `services/` (e.g., in `tests/`, `controllers/`, third-party module-state) OR is imported outside the FastAPI lifecycle OR has init side-effects that the brute reset breaks. Fix = per-test `event_loop` fixture in the appropriate conftest (default: `tests/integration/conftest.py`; per-test-file conftest if scope is narrower).
    3. For each factory-fit candidate: VERIFY the (module_path, attr_name) tuple resolves via `uv run python -c "import importlib; m = importlib.import_module('<module_path>'); assert hasattr(m, '<attr>'), '<attr> missing'"`. If resolution fails, downgrade to factory-unfit.
    4. Write `/tmp/31-00-triage.md` (markdown table — feeds 31-00-SUMMARY at Task 7):

       | Site # | Error shape | Implicated module:attr | Classification | Target file |
       |---|---|---|---|---|
       | 1 | "attached to a different loop" | services.foo.bar._baz | factory-fit | tests/factories/app.py |
       | 2 | "no current event loop" | tests.helpers._cached_x | factory-unfit | tests/integration/conftest.py |

    5. Surface to user (concise — no checkpoint required, just visibility):
       - Wave A count (factory-fit) → predicts N_A new _SINGLETON_INVENTORY entries
       - Wave B count (factory-unfit) → predicts N_B new event_loop fixtures
       - Any sites that resisted classification (escalate to user before Tasks 3+4 if ≥ 3 such cases)

    Discretion (per CONTEXT §Claude's Discretion): order of remediation across sites is executor's call (alphabetical, by service area, or by error shape — choose one and document).
  </action>
  <verify>
    <automated>test -f /tmp/31-00-triage.md && grep -qE "factory-(fit|unfit)" /tmp/31-00-triage.md && python3 -c "import re,pathlib; t=pathlib.Path('/tmp/31-00-triage.md').read_text(); n=len(re.findall(r'\\| factory-fit \\|', t))+len(re.findall(r'\\| factory-unfit \\|', t)); print(f'classified={n}'); assert n >= 1 or 'N=0' in t, 'triage missing classifications'"</automated>
  </verify>
  <done>Every surfaced site classified factory-fit or factory-unfit; each factory-fit tuple resolved via importlib pre-check; triage table written to /tmp/31-00-triage.md; Wave A + Wave B counts reported to user.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wave A — extend _SINGLETON_INVENTORY for factory-fit sites</name>
  <files>tests/factories/app.py</files>
  <read_first>
    - /tmp/31-00-triage.md (Task 2 output — Wave A rows)
    - tests/factories/app.py (current 34-entry tuple + _reset_singletons)
  </read_first>
  <action>
    For each factory-fit row in `/tmp/31-00-triage.md`:
    1. Append a `(module_path, attr_name)` tuple to `_SINGLETON_INVENTORY` in `tests/factories/app.py`, preserving the existing whitespace alignment (column-aligned attr names — match existing style).
    2. Maintain logical grouping: place new entries near existing tuples from the same `services.<area>.<module>` namespace where possible. Pure-append at the end is also acceptable if no obvious grouping exists.
    3. Do NOT modify `_reset_singletons()`, `create_app()`, or any other function in the file — tuple grow only.
    4. After all Wave A entries added, run `uv run python -c "from tests.factories.app import _SINGLETON_INVENTORY; print(len(_SINGLETON_INVENTORY))"` — count MUST equal `34 + N_A` (where N_A is the Wave A row count from triage).
    5. Run a smoke check on each new entry: `uv run python -c "from tests.factories.app import _reset_singletons; _reset_singletons()"` — MUST exit 0 (importlib.import_module succeeds for every entry; `hasattr` guard handles renamed attrs gracefully).
    6. Run inventory lint: `uv run pytest tests/unit/test_singleton_inventory_complete.py -v` — MUST pass. (Lint is data-driven; new entries are detected as covered.)

    **DO NOT add padding entries.** Per D-02, every new tuple MUST trace to a real surfaced leak in `/tmp/31-00-leak-sites.txt`. If `N_A == 0`, skip Task 3 entirely and document in SUMMARY.

    Commit (atomic): `chore(31-00): grow _SINGLETON_INVENTORY +<N_A> for EVT-02 factory-fit sites`
  </action>
  <verify>
    <automated>uv run python -c "from tests.factories.app import _SINGLETON_INVENTORY, _reset_singletons; _reset_singletons(); print(f'len={len(_SINGLETON_INVENTORY)}')" && uv run pytest tests/unit/test_singleton_inventory_complete.py -v --tb=short</automated>
  </verify>
  <done>_SINGLETON_INVENTORY grown by exactly N_A entries (no padding); _reset_singletons() smoke-runs clean; inventory lint passes; atomic commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wave B — per-test event_loop fixtures for factory-unfit outliers</name>
  <files>tests/integration/conftest.py (default target; per-test-file conftest if scope narrower per triage)</files>
  <read_first>
    - /tmp/31-00-triage.md (Task 2 output — Wave B rows + target file column)
    - tests/integration/conftest.py (Phase 30-02 carry-forward — DO NOT REGRESS the autouse embedder/reranker mock)
    - .planning/milestones/v1.8-phases/30-test-infra-mypy-hardening/30-CONTEXT.md §EVT-01 (per-test fixture is the documented fallback convention)
  </read_first>
  <action>
    For each factory-unfit row in `/tmp/31-00-triage.md`:
    1. Add a per-test `event_loop` fixture scoped narrowly to the offending test(s). Convention (per Phase 30 EVT-01 carry-forward):
       - Fixture name: `event_loop` (overrides pytest-asyncio default, per-function scope unless triage row notes otherwise)
       - Body: create a fresh `asyncio.new_event_loop()`, yield, close. Standard shape.
       - Placement: default to `tests/integration/conftest.py`; only use a per-test-file conftest if triage row indicates the leak is isolated to one file AND a broader fixture would over-scope.
    2. **Critical non-regression:** Existing `_mock_local_model_inits` autouse fixture in `tests/integration/conftest.py` MUST remain intact and `autouse=True`. New fixtures co-exist; do NOT merge or replace.
    3. **Critical non-regression:** Do NOT add `autouse=True` to new `event_loop` fixtures unless the triage row explicitly justifies it. Per-test opt-in (via fixture name match) is the default.
    4. If Wave B count ≥ 3 AND all share a common pattern (per CONTEXT §Claude's Discretion): consider extracting a shared helper in `tests/factories/app.py` (e.g., `make_event_loop_fixture()`). Otherwise, inline per fixture.
    5. After fixtures added, run the integration suite locally (subset only — full PG run is Task 5): `uv run pytest tests/integration/<implicated_test_file>.py -v` for each Wave B test file. Each MUST stop emitting its previous loop-error.
    6. If a new fixture would require a mypy --strict silence (e.g., `# type: ignore[misc]  # why: pytest-asyncio fixture override`), STOP and surface to user — per CONTEXT §Constraints, no new mypy silences without explicit justification. Use a typed fixture signature instead if possible.

    Commit (atomic): `test(31-00): add per-test event_loop fixtures for <N_B> factory-unfit leak sites`

    If `N_B == 0`, skip Task 4 entirely and document in SUMMARY.
  </action>
  <verify>
    <automated>uv run python -c "import ast,pathlib; tree=ast.parse(pathlib.Path('tests/integration/conftest.py').read_text()); has_autouse_mock=any(isinstance(n, ast.FunctionDef) and n.name=='_mock_local_model_inits' for n in ast.walk(tree)); assert has_autouse_mock, 'Phase 30-02 autouse fixture missing — REGRESSION'" && uv run python -c "import ast,pathlib

def _autouse_kw(decorator):
    # pytest.fixture(autouse=True) — handles both Call and Attribute decorator forms
    if not isinstance(decorator, ast.Call):
        return False
    for kw in decorator.keywords:
        if kw.arg == 'autouse' and isinstance(kw.value, ast.Constant) and kw.value.value is True:
            return True
    return False

tree = ast.parse(pathlib.Path('tests/integration/conftest.py').read_text())
offenders = [
    n.name for n in ast.walk(tree)
    if isinstance(n, ast.FunctionDef)
    and n.name == 'event_loop'
    and any(_autouse_kw(d) for d in n.decorator_list)
]
assert not offenders, f'New event_loop fixture(s) are autouse=True — REGRESSION risk per ARCH-01: {offenders}. Phase 31 Wave B requires opt-in (autouse=False / unset).'" && uv run ruff check tests/integration/conftest.py</automated>
  </verify>
  <done>Wave B fixtures added (or task skipped if N_B=0); Phase 30-02 autouse embedder/reranker mock preserved verbatim; new event_loop fixtures verified autouse=False (ARCH-01 AST guard); ruff clean on touched conftest files; no new mypy silences introduced; atomic commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Re-run D-01 enumeration — zero-error gate verification</name>
  <files>/tmp/31-00-leak-sites-post.txt</files>
  <read_first>
    - /tmp/31-00-leak-sites.txt (pre-fix baseline for diff)
    - .planning/phases/31-event-loop-leak-sweep/31-CONTEXT.md §D-02 (zero-error gate dominates)
  </read_first>
  <action>
    Re-run the D-01 enumeration command VERBATIM post-remediation:

    ```bash
    uv run pytest -m integration --uses-redis -v 2>&1 \
      | grep -E "(no current event loop|attached to a different loop|got Future.*attached)" \
      | sort -u > /tmp/31-00-leak-sites-post.txt
    wc -l /tmp/31-00-leak-sites-post.txt
    ```

    Also save the full pytest output for Task 6 baseline compare:
    ```bash
    uv run pytest -m integration --uses-redis -v 2>&1 | tee /tmp/31-00-full-pytest-post.log
    ```

    **Acceptance gate (D-02 HARD):** `wc -l /tmp/31-00-leak-sites-post.txt` MUST output `0`.

    If non-zero:
    1. Diff against `/tmp/31-00-leak-sites.txt`: `diff /tmp/31-00-leak-sites.txt /tmp/31-00-leak-sites-post.txt`
    2. For each remaining line: identify whether it's a Wave A miss (tuple resolution failed silently?), a Wave B miss (fixture scope too narrow?), or a newly-surfaced site (run revealed a leak masked by an earlier-failing test).
    3. STOP and surface remaining sites to user — do NOT silently absorb or pad inventory. Options: (a) iterate Tasks 3-4 on the residual set, (b) escalate scope concern.

    Only proceed to Task 6 when `/tmp/31-00-leak-sites-post.txt` is empty (zero matches).
  </action>
  <verify>
    <automated>test -f /tmp/31-00-leak-sites-post.txt && [ "$(wc -l < /tmp/31-00-leak-sites-post.txt)" -eq 0 ] && docker ps --filter "name=rag-postgres" --format "{{.Status}}" | grep -q "healthy"</automated>
  </verify>
  <done>/tmp/31-00-leak-sites-post.txt is empty (zero matches against D-01 broader regex); PG container still healthy post-run (ARCH-02 — guards against false-positive zero-matches if container died mid-run); /tmp/31-00-full-pytest-post.log captured for Task 6.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Regression check vs v1.8 close baseline (D-04 filter applied)</name>
  <files>/tmp/31-00-regression-diff.md (scratch)</files>
  <read_first>
    - /tmp/31-00-full-pytest-post.log (Task 5 output)
    - .planning/milestones/v1.8-phases/30-test-infra-mypy-hardening/30-VERIFICATION.md §re_verification (v1.8 baseline: 9 failed / 32 passed / 1 skipped / 3 errors)
    - .planning/phases/31-event-loop-leak-sweep/31-CONTEXT.md §D-04 (pre-existing failure categories — DO NOT fix)
  </read_first>
  <action>
    1. Extract green/red/skip/error counts from `/tmp/31-00-full-pytest-post.log` — grep the final pytest summary line (e.g., `=== N passed, M failed, K skipped, L errors in T.Ts ===`).
    2. Compare vs v1.8 close baseline:
       - v1.8: 9 failed / 32 passed / 1 skipped / 3 errors
       - v1.9 Phase 31 post: <captured>
    3. **Acceptance (must_have #4):** passed count MUST NOT decrease (i.e., post-passed ≥ 32). Skipped/errors can shift if leak sites that previously errored now skip or pass (improvement).
    4. For any **new** failures (test that was passing in v1.8 close but is now failing):
       - Classify: (a) pre-existing category mis-attributed by v1.8 close, or (b) NEW regression introduced by Phase 31 remediation
       - Category (a): document in 31-00-SUMMARY and accept (Phase 33/34 owns)
       - Category (b): STOP and surface — Phase 31 must not introduce green→red transitions. Rollback the offending Wave A entry or Wave B fixture and re-run.
    5. For the pre-existing 9+3 (D-04 filter): confirm they map to documented categories (real-LLM, perf-bench, UI sentinel drift, schema drift, real-embedder). If a previously-failing test is now passing unexpectedly, that's fine (improvement); document in SUMMARY but do not block.
    6. Write `/tmp/31-00-regression-diff.md` with the side-by-side count table + new-failure triage. Feeds 31-00-SUMMARY at Task 7.

    No commit at this task — diff is a scratch artifact consumed by Task 7's SUMMARY.
  </action>
  <verify>
    <automated>test -f /tmp/31-00-regression-diff.md && grep -qE "(passed|PASSED)" /tmp/31-00-regression-diff.md && python3 -c "import re,pathlib; d=pathlib.Path('/tmp/31-00-regression-diff.md').read_text(); m=re.search(r'post[- ]passed[: ]*(\\d+)', d, re.I) or re.search(r'(\\d+)\\s+passed', d); n=int(m.group(1)) if m else -1; assert n >= 32, f'green count regressed: {n} < 32 baseline'"</automated>
  </verify>
  <done>Green count ≥ 32 (v1.8 baseline preserved); any new failures classified as pre-existing or escalated for rollback; diff written to /tmp/31-00-regression-diff.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Lint patch (if needed) + 31-00-SUMMARY.md</name>
  <files>tests/unit/test_singleton_inventory_complete.py (only if patch needed); .planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md</files>
  <read_first>
    - tests/unit/test_singleton_inventory_complete.py (confirm data-driven design — does it hardcode a count?)
    - /tmp/31-00-triage.md (per-site classification)
    - /tmp/31-00-regression-diff.md (baseline compare)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
  </read_first>
  <action>
    **Step 1 — Lint patch decision (likely NO patch needed):**
    Re-read `tests/unit/test_singleton_inventory_complete.py`. The lint scans `services/` for `^_X = None` patterns and asserts each is in `_SINGLETON_INVENTORY` OR `_SKIP`. It does NOT hardcode a count. Therefore:
    - If all Wave A entries are in `services/`: lint already detects them as covered (data-driven). NO patch.
    - If any Wave A entry is outside `services/` (rare — would have been factory-unfit per Task 2 triage): also NO patch (lint only checks services/).
    - Only patch the lint if Task 3 revealed a NEW services/ singleton not in inventory AND not in `_SKIP` — in which case the Wave A entry IS the fix. (If unsure, run `uv run pytest tests/unit/test_singleton_inventory_complete.py -v` once more — green = no patch needed.)

    Do NOT pad `_SKIP` to silence the lint. Per D-02, only legitimate non-service primitives belong in `_SKIP`.

    **Step 2 — Write 31-00-SUMMARY.md** at `.planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md` using `$HOME/.claude/get-shit-done/templates/summary.md` as the template:

    Required sections:
    - **Frontmatter:** `phase: 31-event-loop-leak-sweep`, `plan: 00`, `status: complete`, `requirement: EVT-02`
    - **Objective:** copy from PLAN.md `<objective>` summary line
    - **Per-site triage:** include the table from `/tmp/31-00-triage.md` verbatim (factory-fit vs factory-unfit per site, with module:attr + target file)
    - **_SINGLETON_INVENTORY growth:** baseline 34 → new count `34 + N_A` (cite the actual N_A); enumerate the new tuples added in Task 3
    - **Wave B fixtures:** enumerate per-test event_loop fixtures added in Task 4 (or "Skipped — N_B = 0")
    - **D-01 zero-error gate:** evidence (Task 5 output: `wc -l /tmp/31-00-leak-sites-post.txt` = 0)
    - **D-04 baseline compare:** table from `/tmp/31-00-regression-diff.md` (v1.8 baseline vs v1.9 post-fix); any new failures classified
    - **Lint pass evidence:** `uv run pytest tests/unit/test_singleton_inventory_complete.py -v` output (pass)
    - **Phase 30-02 non-regression evidence:** confirmation that autouse embedder/reranker mock in `tests/integration/conftest.py` is unchanged + still autouse
    - **Mypy silence audit:** "No new mypy --strict silences introduced" (or list any that were unavoidable with justification — per CONTEXT §Constraints)
    - **Acceptance bullets (1-4 from ROADMAP §Phase 31):** each marked VERIFIED with evidence pointer
    - **Commits landed:** list atomic commits from Tasks 3, 4, 7 (this task)
    - **Deferred:** explicitly carry-forward "Pre-existing 9+3 v1.8 failures owned by Phase 33+34 — NOT closed in Phase 31"

    **Step 3 — Final regression sanity:**
    Run `uv run pytest tests/unit/test_singleton_inventory_complete.py -v` one more time. Confirm green.

    Commit (atomic): `docs(31-00): SUMMARY — EVT-02 zero-error gate + inventory grew 34→<new>`
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_singleton_inventory_complete.py -v --tb=short && test -f .planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md && grep -qE "factory-(fit|unfit)" .planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md && grep -qE "EVT-02" .planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md && grep -qE "zero-error|0 matches|wc -l.*0" .planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md</automated>
  </verify>
  <done>Lint green (no patch needed in typical case; patched only if Task 3 added a non-services/ entry); SUMMARY.md written covering all 4 ROADMAP acceptance bullets + per-site triage + non-regression evidence + carry-forward documentation; atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest process ↔ docker rag-postgres | asyncpg connection pool crosses process boundary; loop-binding errors surface here when stale loop references leak across test fixtures |
| test fixture ↔ module-level singleton cache | Module-import-time `_X = None` cache binds to whichever event loop happens to exist at import; subsequent tests creating fresh loops trigger "different loop" assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | Test infra masking real failures (similar to v1.8 Phase 30-00 OAI-01 stale-data discovery) | mitigate | D-04 filter-then-enumerate: D-01 regex only matches loop-error shapes — pre-existing non-loop failures stay visible in baseline compare (Task 6) and cannot silently absorb event-loop regressions |
| T-31-02 | Tampering | _SINGLETON_INVENTORY padding to hit a target count | mitigate | D-02 zero-error gate dominates; every new tuple MUST trace to a surfaced site in /tmp/31-00-leak-sites.txt; Task 3 explicitly forbids padding |
| T-31-03 | Repudiation | Per-site triage decisions undocumented | mitigate | Task 2 writes /tmp/31-00-triage.md (factory-fit vs factory-unfit per site with rationale); Task 7 ports verbatim into 31-00-SUMMARY for permanent audit trail |
| T-31-04 | DoS | Phase 30-02 autouse embedder/reranker mock regressed by Phase 31 fixture changes | mitigate | Task 4 explicitly preserves _mock_local_model_inits; verify step uses AST scan to confirm fixture still present + autouse |
| T-31-05 | Tampering | Pre-existing 9+3 failures from v1.8 close get re-triaged into Phase 31 scope | accept | Documented carry-forward to Phase 33+34; D-04 regex filters them at parse time so they cannot mask event-loop signal; Task 7 SUMMARY documents deferred ownership |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in Phase 31 (test-infra-only refactor; uses existing uv-managed deps). RESEARCH.md Package Legitimacy Audit not required (no install tasks). |
</threat_model>

<verification>
**Acceptance bullets (from ROADMAP §Phase 31 + CONTEXT §Acceptance):**

1. **HARD — Zero-error gate (D-01 + D-02):**
   ```bash
   uv run pytest -m integration --uses-redis -v 2>&1 \
     | grep -E "(no current event loop|attached to a different loop|got Future.*attached)" \
     | sort -u | wc -l
   ```
   MUST output `0`. (Verified at Task 5.)

2. **DESCRIPTIVE — Inventory grew authentically:**
   ```bash
   uv run python -c "from tests.factories.app import _SINGLETON_INVENTORY; print(len(_SINGLETON_INVENTORY))"
   ```
   MUST output `34 + N_A` where N_A = Wave A factory-fit count from Task 2 triage. Each new entry traces to a real surfaced leak (Task 7 SUMMARY documents per-tuple provenance).

3. **HARD — Lint passes:**
   ```bash
   uv run pytest tests/unit/test_singleton_inventory_complete.py -v
   ```
   MUST pass with new count. (Verified at Tasks 3 and 7.)

4. **HARD — No regression vs v1.8 baseline:**
   Integration-suite passed count ≥ 32 (Task 6 acceptance). Any newly-failing test must be either (a) a pre-existing category newly surfaced (documented, not fixed — Phase 33+34) or (b) a Phase 31 regression (rolled back).

5. **HARD — Phase 30-02 autouse mock not regressed:**
   AST scan in Task 4 verify step confirms `_mock_local_model_inits` fixture still present and `autouse=True`.

6. **HARD — No new mypy --strict silences:**
   ```bash
   uv run mypy --strict tests/factories/app.py tests/integration/conftest.py tests/unit/test_singleton_inventory_complete.py
   ```
   MUST exit 0 without new `# type: ignore` introductions vs git HEAD pre-fix. (Spot-check during Tasks 3-4; document in Task 7 SUMMARY.)
</verification>

<success_criteria>
- D-01 broader-regex enumeration on PG host returns ZERO matches post-fix.
- `_SINGLETON_INVENTORY` grew from 34 to `34 + N_A` where N_A is the user-approved Wave A factory-fit count from Task 1 + Task 2 (no padding entries).
- `tests/unit/test_singleton_inventory_complete.py` passes.
- Integration-suite green count ≥ 32 (v1.8 close baseline); any new failures triaged + documented in 31-00-SUMMARY.
- `tests/integration/conftest.py` autouse embedder/reranker mock (Phase 30-02) preserved verbatim.
- No new mypy --strict silences introduced.
- 31-00-SUMMARY.md documents per-site triage (factory-fit vs factory-unfit) + Wave A growth + Wave B fixtures + baseline compare + acceptance bullets each marked VERIFIED.
- Pre-existing 9+3 failures from v1.8 close remain pre-existing (filtered at parse time by D-01 regex; not fixed in Phase 31; ownership carried to Phase 33+34 in SUMMARY).
- Atomic commits landed (one per task that mutates tracked files): Task 3, Task 4 (if N_B > 0), Task 7.
</success_criteria>

<output>
Create `.planning/phases/31-event-loop-leak-sweep/31-00-SUMMARY.md` at Task 7 documenting all sections enumerated in Task 7 action.
</output>

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR (PLAN) | 2 issues, 0 critical gaps — both applied inline (ARCH-01 AST guard + ARCH-02 PG health re-check) |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |
| Adversarial | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Outside Voice | `/plan-eng-review` outside-voice | Cross-model challenge | 0 | SKIPPED | Well-scoped closeout — no architectural inflection |

**UNRESOLVED:** 0
**VERDICT:** ENG CLEARED — ready to execute. Run `/gsd-execute-phase 31` when ready.

**Findings applied inline:**
- **ARCH-01 (P2):** Task 4 verify gained AST guard asserting new `event_loop` fixtures are NOT `autouse=True` (mirrors existing `_mock_local_model_inits` autouse-preservation check).
- **ARCH-02 (P3):** Task 1 + Task 5 verify gained `docker ps ... | grep healthy` post-pytest check. Closes false-positive silent-success window if PG container dies mid-125s-run.

**Skipped sections:** Outside voice (ROI judgment — closeout of established pattern), TODOS.md (project uses `.planning/STATE.md` carry-forward), distribution check (test infra, no new artifact).

**Lake score:** 2/2 — all review-surfaced recommendations chose the complete option.

*Reviewed 2026-05-18 against commit `857b39c` via `/plan-eng-review 31` (gstack v1.27).*
