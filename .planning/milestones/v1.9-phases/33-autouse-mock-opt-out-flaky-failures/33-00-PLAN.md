---
phase: 33-autouse-mock-opt-out-flaky-failures
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - tests/integration/conftest.py
  - tests/integration/test_real_embedder_canary.py
  - docs/RUNBOOK.md
autonomous: true
requirements: [TEST-08]
nyquist_compliant: true

must_haves:
  truths:
    - "Marker `real_embedder` is registered in pytest.ini and discoverable via `pytest --markers`."
    - "Autouse fixture `_mock_local_model_inits` in tests/integration/conftest.py early-returns (yields without patching) when the executing test carries `@pytest.mark.real_embedder`."
    - "A canary test file exists that opts out via the marker, asserts real bge-m3 embed shape (1024-d) + rerank scalar score, and skips cleanly with a documented reason when bge-m3 files are absent at $APP_MODEL_DIR."
    - "Default integration baseline (Phase 32 close: 31 passed / 9 failed / 1 skipped / 3 errors) is unchanged under `-m 'integration and not real_llm and not real_embedder and not benchmark'`."
    - "docs/RUNBOOK.md documents the autouse mock + real-embedder opt-out path in a `## Test Infrastructure` section."
  artifacts:
    - path: "pytest.ini"
      provides: "real_embedder marker registration"
      contains: "real_embedder:"
    - path: "tests/integration/conftest.py"
      provides: "Marker-aware autouse fixture (opt-out branch)"
      contains: 'get_closest_marker("real_embedder")'
    - path: "tests/integration/test_real_embedder_canary.py"
      provides: "Minimal canary exercising real HuggingFaceEmbedder + CrossEncoderReranker"
      min_lines: 25
    - path: "docs/RUNBOOK.md"
      provides: "Test Infrastructure section documenting autouse mock + opt-out"
      contains: "## Test Infrastructure"
  key_links:
    - from: "tests/integration/test_real_embedder_canary.py"
      to: "tests/integration/conftest.py::_mock_local_model_inits"
      via: "@pytest.mark.real_embedder triggers early-return"
      pattern: 'pytest\.mark\.real_embedder'
    - from: "tests/integration/conftest.py"
      to: "pytest.ini markers block"
      via: "request.node.get_closest_marker reads registered marker"
      pattern: 'get_closest_marker\("real_embedder"\)'
---

<objective>
Add the `real_embedder` opt-out marker to Phase 30-02's autouse mock fixture so a marked test observes the real `HuggingFaceEmbedder` + `CrossEncoderReranker` constructors. Ship a minimal canary test (skipped by default) that proves the opt-out path works end-to-end on a PG host with bge-m3 + bge-m3-rerank files present. Document the contract in `docs/RUNBOOK.md`.

Purpose: TEST-08 requirement (v1.9 Hardening Round 3). The Phase 30-02 autouse mock currently has no opt-out; any integration test that wants the real model path is blocked. This phase opens a single, surgical opt-out without disturbing default integration suite behaviour.

Output: 1 new file, 3 edits, zero behaviour change for unmarked tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md
@.planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md
@.planning/phases/33-autouse-mock-opt-out-flaky-failures/33-VALIDATION.md
@tests/integration/conftest.py
@pytest.ini

<interfaces>
<!-- Extracted from current code; executor uses these directly — no codebase exploration needed. -->

From tests/integration/conftest.py (current state — pre-edit):
- Line 25: `@pytest.fixture(autouse=True)`
- Line 26: `def _mock_local_model_inits() -> object:`  (NO `request` parameter — must be added)
- Lines 40-41: module aliases `_embedder_mod` = `services.vectorizer.embedder`, `_retriever_mod` = `services.retriever.retriever`
- Lines 55-58: `with (patch.object(_embedder_mod.HuggingFaceEmbedder, "__init__", _noop_embedder_init), patch.object(_retriever_mod.CrossEncoderReranker, "__init__", _noop_reranker_init)): yield`
- Insertion point per RESEARCH §Q4: between current line 54 (blank after `_noop_reranker_init` body) and line 55 (start of `with` block). The early-return branch MUST be `yield; return` — bare `return` before any `yield` errors at collection because the fixture is a generator.

From services/vectorizer/embedder.py:106:
- `class HuggingFaceEmbedder: def __init__(self) -> None:` (sync)
- `async def embed_batch(self, texts: list[str]) -> list[list[float]]:` returns 1024-d vectors per bge-m3

From services/retriever/retriever.py:138:
- `class CrossEncoderReranker: def __init__(self) -> None:` (sync)
- Real `_model` is `sentence_transformers.CrossEncoder`; `.predict(pairs)` is sync, returns scalar floats

From config/settings.py:30-65:
- `resolve_embedding_model_path(name: str) -> Path` — already used by tests/conftest.py:155 `embedder_or_mock` fixture. The canary uses this same resolver for the `skipif` precondition.

From pytest.ini lines 10-14 (markers block, current):
- `markers =` with 4 indented entries (integration, pgvector, real_llm, uses_postgres). New `real_embedder:` entry appends after `real_llm:` for grep adjacency (D-MARKER-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 33-00-01: Register real_embedder marker in pytest.ini</name>
  <read_first>
    - pytest.ini (lines 10-14 markers block — current state shows 4 entries)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-MARKER-01 (exact marker description text — must be verbatim)
  </read_first>
  <action>
    Open pytest.ini. Within the `markers =` block (lines 10-14), insert a new line immediately after the `real_llm:` entry, indented to match the existing 4-space indent. The marker name MUST be exactly `real_embedder` and the description MUST read verbatim: `integration tests requiring real local model files (bge-m3 / bge-m3-rerank); skipped in default CI without the files present` (per D-MARKER-01). Do NOT modify `addopts`, `markers =` header, or any other line — the marker is opt-in via explicit `-m real_embedder`, NOT a default exclusion. Per D-PLAN-01 and Q8 guardrails, do not touch `.github/`, `Makefile`, or any CI config.
  </action>
  <verify>
    <automated>grep -q '^    real_embedder: integration tests requiring real local model files' pytest.ini &amp;&amp; uv run pytest --markers 2&gt;&amp;1 | grep -q '@pytest.mark.real_embedder'</automated>
  </verify>
  <acceptance_criteria>
    - TEST-08a: `grep -q 'real_embedder:' pytest.ini` exits 0.
    - `uv run pytest --markers` lists `@pytest.mark.real_embedder` with the D-MARKER-01 description string.
    - Diff on pytest.ini is +1 line, 0 deletions; `addopts` line unchanged.
  </acceptance_criteria>
  <done>
    Marker registered. `pytest --markers` discovers `real_embedder`. No other lines in pytest.ini modified.
  </done>
</task>

<task type="auto">
  <name>Task 33-00-02: Add real_embedder opt-out branch to integration autouse fixture</name>
  <read_first>
    - tests/integration/conftest.py (full file — 60 lines; current fixture body at lines 25-59)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md §Q4 (exact insertion point line 54 -> 55; signature change rationale; fixture is a generator so opt-out must `yield` then `return`)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-OPTOUT-01 (acceptance contract — marked test sees real `HuggingFaceEmbedder.__init__`)
  </read_first>
  <action>
    Edit tests/integration/conftest.py in two places:
    (1) Change the fixture signature on line 26 from `def _mock_local_model_inits() -> object:` to `def _mock_local_model_inits(request: pytest.FixtureRequest) -> object:`. The `request` parameter is required for marker lookup; pytest auto-injects it for autouse fixtures that declare it. `pytest` is already imported at line 22 — no new import needed.
    (2) Insert an opt-out branch between the `_noop_reranker_init` body close (current line 54 blank) and the `with` block (current line 55). Branch condition: `if request.node.get_closest_marker("real_embedder") is not None:` then `yield` then `return` on the next line. A bare `return` before any `yield` would error at collection — the fixture is a generator-based autouse. Add a single-line comment above the branch that names TEST-08 and D-OPTOUT-01 for grep traceability.
    Do NOT modify `_noop_embedder_init`, `_noop_reranker_init`, or the existing `with patch.object(...)` block. Do NOT touch tests/conftest.py (that surface belongs to 33-01). Mock-at-consumer convention (v1.3 D-mock) is preserved — the existing `patch.object` targets are kept verbatim.
    Phase 32 hygiene carry-forward: no new bare `# type: ignore`. The `request: pytest.FixtureRequest` annotation is fully typed; no `[code]  # why:` silence should be needed.
  </action>
  <verify>
    <automated>grep -q 'def _mock_local_model_inits(request: pytest.FixtureRequest)' tests/integration/conftest.py &amp;&amp; grep -q 'get_closest_marker("real_embedder")' tests/integration/conftest.py &amp;&amp; uv run mypy --strict tests/integration/conftest.py 2&gt;&amp;1 | grep -qE 'Success|no issues found' &amp;&amp; uv run pytest -m 'integration and not real_llm and not real_embedder and not benchmark' --tb=no -q 2&gt;&amp;1 | tee /tmp/baseline.txt &amp;&amp; grep -qE '31 passed.*9 failed' /tmp/baseline.txt</automated>
  </verify>
  <acceptance_criteria>
    - TEST-08b: `grep -q 'get_closest_marker("real_embedder")' tests/integration/conftest.py` exits 0.
    - Signature line exact-matches `def _mock_local_model_inits(request: pytest.FixtureRequest) -> object:`.
    - TEST-08f regression gate: `uv run pytest -m 'integration and not real_llm and not real_embedder and not benchmark' --tb=no -q` matches Phase 32 close = 31 passed / 9 failed / 1 skipped / 3 errors. Failure count must NOT increase.
    - `mypy --strict tests/integration/conftest.py` is clean (Phase 32 D1 typing hygiene gate).
    - No new `# type: ignore` lines introduced (`git diff tests/integration/conftest.py | grep -c '^+.*type: ignore'` is 0).
  </acceptance_criteria>
  <done>
    Fixture accepts `request` and early-returns when the marker is set. Integration baseline unchanged. mypy --strict clean on the edited file.
  </done>
</task>

<task type="auto">
  <name>Task 33-00-03: Create real-embedder canary test</name>
  <read_first>
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md §Q4 (full canary structure ~30 LOC; module-level pytestmark + skipif precondition; async test body)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-CANARY-01 (must instantiate both classes; call encode-equivalent + predict; assert 1024-d vector + scalar float)
    - config/settings.py:30-65 (`resolve_embedding_model_path` signature — same call used by tests/conftest.py:155)
    - services/vectorizer/embedder.py:106-127 (HuggingFaceEmbedder.__init__ sync; embed_batch async)
    - services/retriever/retriever.py:138-144 (CrossEncoderReranker.__init__ sync; expose `_model.predict` sync)
  </read_first>
  <action>
    Create new file `tests/integration/test_real_embedder_canary.py` (~30 LOC). Structure exactly per RESEARCH §Q4 Option A recommendation:
    (1) Module docstring naming TEST-08, D-OPTOUT-01, and the run invocation `uv run pytest tests/integration/test_real_embedder_canary.py -m real_embedder`.
    (2) Top-line imports: `from __future__ import annotations`, then `pytest`, then `from config.settings import resolve_embedding_model_path`.
    (3) Private module-level helper `_models_present() -> bool` returning True iff `resolve_embedding_model_path("bge-m3").exists() and resolve_embedding_model_path("bge-m3-rerank").exists()`.
    (4) Module-level `pytestmark = [pytest.mark.integration, pytest.mark.real_embedder, pytest.mark.skipif(not _models_present(), reason="bge-m3 / bge-m3-rerank not present at $APP_MODEL_DIR; see docs/RUNBOOK.md test infra section")]`. Per Assumption A6 + D-VERIFY-02: `skipif` produces `skipped` status (clean baseline), NOT `error` from raised FileNotFoundError. The skip status is mandatory — without it the canary would dirty the integration error count.
    (5) Single async test `async def test_real_embedder_models_load_and_encode() -> None:`. Body does lazy imports inside the function — `from services.vectorizer.embedder import HuggingFaceEmbedder` and `from services.retriever.retriever import CrossEncoderReranker` — so import-time side-effects are deferred past the skip check. Instantiate each. `vectors = await embedder.embed_batch(["hello"])`; assert `len(vectors) == 1` and `len(vectors[0]) == 1024`. `scores = reranker._model.predict([("q", "d")])`; assert `len(scores) == 1` and `isinstance(float(scores[0]), float)`.
    Constraints (all explicit guardrails from CONTEXT §Q8 and CLAUDE.md):
      - Do NOT mark any existing test with `real_embedder` (D-CANARY-01 — minimal canary only, no promotion).
      - No bare `except` (ERR-01).
      - Type annotations on function signatures and the helper (mypy --strict clean).
      - No `# type: ignore` introduced.
  </action>
  <verify>
    <automated>test -f tests/integration/test_real_embedder_canary.py &amp;&amp; uv run mypy --strict tests/integration/test_real_embedder_canary.py 2&gt;&amp;1 | grep -qE 'Success|no issues found' &amp;&amp; uv run pytest tests/integration/test_real_embedder_canary.py -m real_embedder --tb=short -q 2&gt;&amp;1 | tee /tmp/canary.txt &amp;&amp; grep -qE '(1 skipped|1 passed)' /tmp/canary.txt</automated>
  </verify>
  <acceptance_criteria>
    - TEST-08c: `test -f tests/integration/test_real_embedder_canary.py` exits 0.
    - TEST-08d: `uv run pytest tests/integration/test_real_embedder_canary.py -m real_embedder -q` exits 0 with status either `1 skipped` (no models) OR `1 passed` (models present).
    - `mypy --strict` on the new file is clean.
    - File contains `pytestmark = ` with all three markers (`integration`, `real_embedder`, `skipif`) per D-VERIFY-01c.
    - No bare `except` (`grep -nE '^\s*except\s*:' tests/integration/test_real_embedder_canary.py` returns nothing).
  </acceptance_criteria>
  <done>
    Canary file exists. mypy --strict clean. pytest reports `1 skipped` or `1 passed`. No new error rows in integration baseline.
  </done>
</task>

<task type="auto">
  <name>Task 33-00-04: Document Test Infrastructure section in docs/RUNBOOK.md</name>
  <read_first>
    - docs/RUNBOOK.md (full file — verify current top-level structure: Local dev, Ops, Troubleshooting; insertion location is between Ops and Troubleshooting per RESEARCH §Q6 Task 4)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-DOCS-01 (section sub-headings: "Default integration suite behavior" + "Real-embedder opt-out")
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md §Q6 (insertion line ~185, between Ops and Troubleshooting)
  </read_first>
  <action>
    Edit `docs/RUNBOOK.md`. Insert a new top-level section `## Test Infrastructure` between the existing `## Ops` and `## Troubleshooting` sections (file currently has 3 sections: Local dev, Ops, Troubleshooting per RESEARCH §Q4). The section MUST contain at minimum these two subsections (per D-DOCS-01):
    (1) `### Default integration suite behavior` — explain that `tests/integration/conftest.py::_mock_local_model_inits` is autouse and patches `HuggingFaceEmbedder.__init__` + `CrossEncoderReranker.__init__` to no-op stubs so the suite runs without bge-m3 / bge-m3-rerank on disk. Reference Phase 30-02 origin.
    (2) `### Real-embedder opt-out` — explain (a) when to use (functional test that needs real embedding shape/quality, not mock), (b) how to mark (`@pytest.mark.real_embedder` plus the standard `@pytest.mark.integration`), (c) environment requirements (`$APP_MODEL_DIR` must contain `bge-m3/` and `bge-m3-rerank/` directories — point at `config.settings.resolve_embedding_model_path` as the resolver), (d) how to run (`uv run pytest tests/integration/test_real_embedder_canary.py -m real_embedder`), (e) skip behavior on hosts without the files.
    Do NOT add CI integration notes (Q8 guardrail — TEST-09 plan is also forbidden from touching `.github/`; TEST-08 inherits the same scope). Do NOT promote any other test to `real_embedder`. Keep section under ~80 lines of markdown prose.
  </action>
  <verify>
    <automated>grep -q '^## Test Infrastructure' docs/RUNBOOK.md &amp;&amp; grep -q '^### Default integration suite behavior' docs/RUNBOOK.md &amp;&amp; grep -q '^### Real-embedder opt-out' docs/RUNBOOK.md &amp;&amp; grep -q 'pytest.mark.real_embedder' docs/RUNBOOK.md &amp;&amp; grep -q 'APP_MODEL_DIR' docs/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - TEST-08e: `grep -q '^## Test Infrastructure' docs/RUNBOOK.md` exits 0.
    - Both subsection headings present (`### Default integration suite behavior`, `### Real-embedder opt-out`).
    - Section mentions `pytest.mark.real_embedder`, `$APP_MODEL_DIR`, `bge-m3`, `bge-m3-rerank`, and the canary file path.
    - Section is between `## Ops` and `## Troubleshooting` in source order.
    - No `.github/` or `Makefile` modification (verify `git diff --name-only` shows only the 4 files in this plan's `files_modified`).
  </acceptance_criteria>
  <done>
    `## Test Infrastructure` section exists with both required subsections; references all 5 keywords above; no out-of-scope file touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture <-> production module init | autouse mock currently patches `__init__` for all integration tests; opt-out branch must NOT widen this boundary (cannot grant marker-less tests access to real init) |
| test runtime <-> filesystem | `resolve_embedding_model_path` reads `$APP_MODEL_DIR`; canary depends on env var being set, not on test-controlled path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-00-01 | Tampering | autouse fixture opt-out gate | mitigate | Marker check uses pytest's built-in `request.node.get_closest_marker` (no string parsing of user input); marker registration is name-locked via pytest.ini |
| T-33-00-02 | Information Disclosure | real-embedder canary on dev host | accept | Canary only embeds the literal string "hello" + rerank pair `("q","d")` — no PII, no user data |
| T-33-00-03 | Denial of Service | canary errors when models absent | mitigate | `pytest.mark.skipif` precondition (Option A from RESEARCH §Q4 + Assumption A6) ensures `skipped` status, not `error` — baseline error count cannot increase |
| T-33-00-SC | Tampering | npm/pip/cargo installs | accept | Plan installs NO packages (TEST-08 is pure source edits); package legitimacy audit not applicable to this plan |
</threat_model>

<verification>
After every task: run the task's `<automated>` block.

End-of-plan gates (all must be green before sign-off):
- TEST-08a: `grep -q 'real_embedder:' pytest.ini`
- TEST-08b: `grep -q 'get_closest_marker("real_embedder")' tests/integration/conftest.py`
- TEST-08c: `test -f tests/integration/test_real_embedder_canary.py`
- TEST-08d: `uv run pytest tests/integration/test_real_embedder_canary.py -m real_embedder -q` → 1 skipped OR 1 passed
- TEST-08e: `grep -q '^## Test Infrastructure' docs/RUNBOOK.md`
- TEST-08f: `uv run pytest -m 'integration and not real_llm and not real_embedder and not benchmark' --tb=no -q` matches 31p/9f/1s/3e Phase 32 baseline (regression non-regression gate)

Plus typing-hygiene gate (Phase 32 carry-forward): `uv run python scripts/check_typing_hygiene.py` exits 0.
</verification>

<success_criteria>
1. `real_embedder` marker registered in pytest.ini with verbatim D-MARKER-01 description.
2. `_mock_local_model_inits` honors the marker (early-return `yield`/`return`); unmarked tests see the unchanged autouse mock.
3. Canary file exists, mypy --strict clean, exits as `1 skipped` (no models) or `1 passed` (PG host with bge-m3 + bge-m3-rerank).
4. `docs/RUNBOOK.md` has the new `## Test Infrastructure` section with both required subsections.
5. Integration baseline unchanged (31p/9f/1s/3e under default filter).
6. No file touched outside this plan's `files_modified` (4 files).
</success_criteria>

<output>
On completion, create `.planning/phases/33-autouse-mock-opt-out-flaky-failures/33-00-SUMMARY.md` documenting:
- Files changed (4 files) with diff size per file
- Marker registration verified via `pytest --markers`
- Canary pass/skip outcome on this host (note `$APP_MODEL_DIR` state)
- Integration baseline before/after (must match)
- Any deviations from D-MARKER-01 / D-OPTOUT-01 / D-CANARY-01 / D-DOCS-01 with rationale (expected: none)
</output>
</content>
</invoke>