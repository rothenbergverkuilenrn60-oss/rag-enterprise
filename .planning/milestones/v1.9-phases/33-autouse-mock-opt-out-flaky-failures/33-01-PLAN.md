---
phase: 33-autouse-mock-opt-out-flaky-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements-dev.txt
  - tests/conftest.py
  - tests/unit/test_memory_service_extra.py
autonomous: true
requirements: [TEST-09]
nyquist_compliant: true

must_haves:
  truths:
    - "`pytest-randomly>=3.16.0` is installed via uv (auto-discovers on test run; surfaces seed in pytest header)."
    - "An autouse function-scope fixture in tests/conftest.py resets `services.agent.tools.registry._registry` between unit tests, then enumerates concrete `BaseTool` subclasses via `pkgutil.iter_modules` walk over `services.agent.tools` (D2: self-healing against future @register additions), and re-registers each via an idempotent guard `if cls.name not in reg.list()` (D1: removes import-order coupling with `ToolRegistry.register()`'s ValueError-on-duplicate). Post-condition sentinel: `len(tool_classes) >= 4`."
    - "The 7 named failing tests (verified RESEARCH §Q1 inventory — supersedes CONTEXT.md draft) all pass under the default-order run."
    - "Under each of seeds 12345 / 67890 / 99999, the unit suite reports 0 failures excluding the deferred OCR Cluster C (4 tests, deselected)."
    - "Integration baseline is unchanged: 31 passed / 9 failed / 1 skipped / 3 errors under `-m 'integration and not real_llm and not real_embedder and not benchmark'`."
    - "Unit-suite runtime stays within 1.05x of Phase 32 baseline (~21s ceiling)."
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest-randomly in [dependency-groups].dev"
      contains: "pytest-randomly"
    - path: "requirements-dev.txt"
      provides: "Mirror of pytest-randomly for non-uv consumers"
      contains: "pytest-randomly"
    - path: "tests/conftest.py"
      provides: "_reset_tool_registry autouse function-scope fixture"
      contains: "_reset_tool_registry"
    - path: "tests/unit/test_memory_service_extra.py"
      provides: "embed_batch mock-shape parity fix at line ~235"
      contains: "embed_batch=AsyncMock"
  key_links:
    - from: "tests/conftest.py::_reset_tool_registry"
      to: "services.agent.tools.registry._registry"
      via: "set to None then re-populate via explicit register(cls) calls"
      pattern: '_registry\s*=\s*None'
    - from: "tests/conftest.py::_reset_tool_registry"
      to: "services.agent.tools.* submodules (concrete BaseTool subclasses)"
      via: "pkgutil.iter_modules walk + importlib.import_module + isinstance(obj, type) and issubclass(obj, BaseTool); each cls re-registered via guarded `if cls.name not in reg.list(): reg.register(cls)` (D1+D2 from eng review)"
      pattern: 'pkgutil\.iter_modules'
    - from: "tests/unit/test_memory_service_extra.py:235"
      to: "services.memory.memory_service.save_facts (line 640)"
      via: "fake_embedder provides embed_batch returning [[1024-d]]"
      pattern: 'embed_batch=AsyncMock'
---

<objective>
Eliminate the order-dependent unit-test failures from (a) `_registry` singleton pollution between tests and (b) the `embed_batch` mock-shape stale at `tests/unit/test_memory_service_extra.py:235`. Install `pytest-randomly` and seed-verify against three fixed seeds (12345, 67890, 99999). Deselect the OCR Cluster C (Phase 31 EVT-02 residue) per RESEARCH §Q2 — documented as v1.10 carry-forward, NOT silently absorbed.

Purpose: TEST-09 requirement (v1.9 Hardening Round 3). The Phase 32 close confirms 7 named failures + RESEARCH §Q2 surfaces 4 free-win agent_pipeline_refactor failures that share the same root cause (registry empty after `_reset_singletons()`). One narrow reset fixture clears 10 failures; one mock-shape line fix clears the 11th. The 4 OCR Cluster C failures stay out of scope.

Output: 1 new fixture, 1 one-line mock fix, 1 dual-write dependency add (pyproject + requirements-dev).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md
@.planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md
@.planning/phases/33-autouse-mock-opt-out-flaky-failures/33-VALIDATION.md
@tests/conftest.py
@tests/unit/test_memory_service_extra.py
@pyproject.toml
@requirements-dev.txt

<interfaces>
<!-- Extracted directly; executor uses these — no codebase exploration needed. -->

From services/agent/tools/registry.py:100-118:
- Module attribute: `_registry: ToolRegistry | None = None` (line 106)
- Factory: `def get_tool_registry() -> ToolRegistry:` (lines 109-118) — lazy-inits a fresh `ToolRegistry()` whenever `_registry is None`
- `ToolRegistry.register(cls)` registers a tool class on the instance; `ToolRegistry.list()` returns registered tool names
- Setting `_registry = None` does NOT re-fire module-level `@register` decorators — reset fixture MUST explicitly re-register

The 4 canonical tool classes to re-register (RESEARCH §Q1 verified locations):
- `services.agent.tools.retrieve.RetrieveTool` (@register at services/agent/tools/retrieve.py:161)
- `services.agent.tools.retrieve.RefinedRetrieveTool` (@register at services/agent/tools/retrieve.py:209)
- `services.agent.tools.web_search.WebSearchTool` (@register at services/agent/tools/web_search.py:208)
- `services.agent.tools.recall.RecallTool` (@register at services/agent/tools/recall.py:39)

From services/vectorizer/embedder.py canonical signatures (DO NOT modify; this is read-only parity reference):
- `async def embed_batch(self, texts: list[str]) -> list[list[float]]:` — bulk, returns LIST of vectors
- `async def embed_one(self, text: str) -> list[float]:` — single, returns one vector
- Default `BaseEmbedder.embed_one` (line 32-34) delegates to `embed_batch([text])[0]`

From services/memory/memory_service.py:640:
- `LongTermMemory.save_facts` calls `embeddings = list(await embedder.embed_batch(texts))` — this is the failing callsite for `test_long_term_save_fact_calls_insert`. The stale mock at tests/unit/test_memory_service_extra.py:235 only supplies `embed_one`, causing `TypeError: object MagicMock can't be used in 'await' expression`.

From tests/unit/test_memory_service_extra.py:233-242 (current state, pre-fix):
- Line 231: `async def test_long_term_save_fact_calls_insert(monkeypatch):`
- Line 235: `fake_embedder = MagicMock(embed_one=AsyncMock(return_value=[0.1] * 1024))`  ← ONLY embed_one
- Line 236-237: `monkeypatch.setattr("services.vectorizer.embedder.get_embedder", lambda: fake_embedder)`
- Imports at top of file already include `from unittest.mock import AsyncMock, MagicMock`

From pyproject.toml:78-93 (current state):
- `[dependency-groups]` then `dev = [` array, alphabetical order
- Insertion position for `"pytest-randomly>=3.16.0",`: between `"pytest-cov==6.0.0",` (line 88) and `"pytest-timeout==2.3.1",` (line 89). Alphabetical: pytest-cov < pytest-randomly < pytest-timeout.

From requirements-dev.txt (full 13-line file, current state):
- Loose grouping (lint/type first, test second, type-stubs last). Latest additions (asyncpg-stubs, pandas-stubs at lines 12-13) appended at EOF with inline `# mirrors pyproject.toml [dependency-groups].dev` comments.
- Append target: `pytest-randomly>=3.16.0  # Random-order test plugin (TEST-09 — mirrors pyproject.toml [dependency-groups].dev)`

From scripts/check_typing_hygiene.py `_STUB_NAME_RE`:
- Matches `[\w.-]+-stubs` and `types-[\w.-]+`. `pytest-randomly` matches NEITHER. The hygiene gate will not flag this addition. RESEARCH §Q5 verified.

The 7 verified default-order failing tests (RESEARCH §Q1 — supersedes CONTEXT.md draft list):
- tests/unit/test_memory_service_extra.py::test_long_term_save_fact_calls_insert  (Cluster B mock-shape; ALSO Cluster A via registry)
- tests/unit/test_pipeline_tool_schema_regression.py::test_registry_anthropic_shape_satisfies_call_agentic_turn  (Cluster A)
- tests/unit/test_recall_tool.py::test_recall_tool_registered_once  (Cluster A)
- tests/unit/test_retrieve_tool.py::TestRetrieveToolRegistration::test_retrieve_tool_registered  (Cluster A)
- tests/unit/test_retrieve_tool.py::TestRetrieveToolRegistration::test_refine_tool_registered  (Cluster A)
- tests/unit/test_retrieve_tool.py::TestSchemasForParity::test_retrieve_tool_xml_format_parity  (Cluster A)
- tests/unit/test_web_search_tool.py::TestWebSearchToolRegistration::test_web_search_tool_registered  (Cluster A)

The 4 free-win random-order-only tests (also Cluster A — fixed by same reset fixture):
- tests/unit/test_agent_pipeline_refactor.py::test_single_tool_call_uses_gather
- tests/unit/test_agent_pipeline_refactor.py::test_chunk_dedup_runs_after_gather_not_inside
- tests/unit/test_agent_pipeline_refactor.py::test_two_tool_calls_run_concurrently
- tests/unit/test_agent_pipeline_refactor.py::test_tool_exception_becomes_is_error_tool_result

The 4 OUT-OF-SCOPE OCR Cluster C tests (deferred to v1.10 / TEST-12 candidate — Q8 guardrail):
- tests/unit/test_ocr_engine.py::test_semaphore_serialises_concurrent_extract_pdf_calls
- tests/unit/test_ocr_failure_modes.py::test_extract_pdf_still_uses_semaphore
- tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_retries_once_then_surfaces_error
- tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_then_success_on_retry
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 33-01-01: Dual-write pytest-randomly in pyproject.toml + requirements-dev.txt</name>
  <read_first>
    - pyproject.toml lines 78-93 (verify current alphabetical order of `dev = [...]`)
    - requirements-dev.txt (full 13-line file — note inline `# mirrors pyproject.toml` comment convention from lines 12-13)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-PLUGIN-01 (pin spec `pytest-randomly>=3.16.0`)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md §Q5 (alphabetical insertion position; hygiene gate non-match)
    - scripts/check_typing_hygiene.py `_STUB_NAME_RE` (RESEARCH §Q5 confirmed pytest-randomly does NOT match)
  </read_first>
  <action>
    (1) Edit `pyproject.toml`. Inside `[dependency-groups]` `dev = [...]` array, insert `"pytest-randomly>=3.16.0",` between line 88 (`"pytest-cov==6.0.0",`) and line 89 (`"pytest-timeout==2.3.1",`). Maintain alphabetical sort (pytest-cov < pytest-randomly < pytest-timeout). Use exact spec `pytest-randomly>=3.16.0` (open upper bound per D-PLUGIN-01).
    (2) Edit `requirements-dev.txt`. Append a new line at EOF: `pytest-randomly>=3.16.0  # Random-order test plugin (TEST-09 — mirrors pyproject.toml [dependency-groups].dev)`. Match the inline-comment style used by lines 12-13 (asyncpg-stubs, pandas-stubs). File is loosely grouped (not alphabetical) — EOF append per convention.
    (3) Run `uv sync` to install + update lockfile. Verify `uv pip show pytest-randomly` reports version ≥ 3.16. Do NOT pin to a specific version (D-PLUGIN-01 says `>=3.16.0`, open upper).
    Out-of-scope guardrails (Q8): do NOT touch `.github/workflows/*.yml`, do NOT add to any CI `Makefile` target, do NOT add a `[tool.pytest.ini_options]` plugin entry (auto-discovery is sufficient per RESEARCH §Q5).
  </action>
  <verify>
    <automated>grep -q '"pytest-randomly&gt;=3.16.0"' pyproject.toml &amp;&amp; grep -q '^pytest-randomly&gt;=3.16.0' requirements-dev.txt &amp;&amp; uv sync 2&gt;&amp;1 | tail -3 &amp;&amp; uv pip show pytest-randomly 2&gt;&amp;1 | grep -qE 'Version: (3\.(1[6-9]|[2-9][0-9])|[4-9]\.[0-9]+|[1-9][0-9]+\.)' &amp;&amp; uv run python scripts/check_typing_hygiene.py</automated>
  </verify>
  <acceptance_criteria>
    - TEST-09a: `uv pip show pytest-randomly` reports Version ≥ 3.16 (3.16.0, 3.17.x, 4.x.y, etc).
    - TEST-09b: `grep -q '^pytest-randomly' requirements-dev.txt` exits 0.
    - pyproject.toml `dev` array remains alphabetical when read top-to-bottom.
    - `scripts/check_typing_hygiene.py` exits 0 (Phase 32 D1+D3 hygiene gate — pytest-randomly is not a stub so `_STUB_NAME_RE` does not match).
    - `uv.lock` updated (file is dirty after `uv sync`); commit the lockfile alongside the pyproject change.
    - No `.github/`, `Makefile`, or `pytest.ini` plugins= modification (RESEARCH §Q5 + Q8 guardrails).
  </acceptance_criteria>
  <done>
    Plugin installed via uv; both files updated; lockfile committed; hygiene gate green; auto-discovery works (next `pytest` run will print `Using --randomly-seed=...` in header).
  </done>
</task>

<task type="auto">
  <name>Task 33-01-02: Fix embed_batch mock-shape at tests/unit/test_memory_service_extra.py:235</name>
  <read_first>
    - tests/unit/test_memory_service_extra.py (full file — focus on test_long_term_save_fact_calls_insert lines 229-243; verify `AsyncMock` is already imported at the top of the file)
    - services/memory/memory_service.py:635-655 (verify embed_batch is the production callsite for save_facts → save_fact path)
    - services/vectorizer/embedder.py:115 (embed_batch signature: `async def embed_batch(self, texts: list[str]) -> list[list[float]]:`)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md §Q3 (exhaustive audit — ONLY this one bug site; no other mock files need touching; no compat shim per D-MOCK-01)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-MOCK-01 (mock-at-consumer convention preserved; no compat shim)
  </read_first>
  <action>
    Edit `tests/unit/test_memory_service_extra.py` at line 235 ONLY. Change the `fake_embedder = MagicMock(...)` constructor to provide BOTH `embed_one` and `embed_batch` kwargs:
      - Current (line 235): `fake_embedder = MagicMock(embed_one=AsyncMock(return_value=[0.1] * 1024))`
      - New: add a second kwarg `embed_batch=AsyncMock(return_value=[[0.1] * 1024])` — note the OUTER list (batch return = list of vectors per `embed_batch` signature). Add a brief inline `# why:` comment naming TEST-09 / D-MOCK-01 and v1.7 batch API.
    Mock-at-consumer convention (v1.3 D-mock) is preserved by leaving the existing `monkeypatch.setattr("services.vectorizer.embedder.get_embedder", lambda: fake_embedder)` line untouched at line 236-237. The patch target is at the consumer import path inside `services.memory.memory_service`, not at the source.
    Constraints (Q8 guardrails):
    - Do NOT modify any other test in this file or any other test file. RESEARCH §Q3 audited all `embed_one=` / `embed_batch=` mock sites and confirmed this is the ONLY bug.
    - Do NOT add a compat shim for `embed_one` in production (D-MOCK-01 explicit rejection). RESEARCH §Q3 confirmed `embed_one` has 5 legitimate production consumers and is not a straggler.
    - Phase 32 hygiene: no new `# type: ignore`. `AsyncMock` is already imported at top of file.
  </action>
  <verify>
    <automated>grep -n 'embed_batch=AsyncMock' tests/unit/test_memory_service_extra.py &amp;&amp; uv run pytest tests/unit/test_memory_service_extra.py::test_long_term_save_fact_calls_insert -q 2&gt;&amp;1 | grep -qE '1 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'embed_batch=AsyncMock' tests/unit/test_memory_service_extra.py` returns ≥ 1.
    - `uv run pytest tests/unit/test_memory_service_extra.py::test_long_term_save_fact_calls_insert -q` exits 0 with `1 passed`.
    - Diff on `tests/unit/test_memory_service_extra.py` is +1 or +2 lines (mock-shape line + optional comment), 0 deletions. No other test in the file modified.
    - No new `# type: ignore` introduced.
  </acceptance_criteria>
  <done>
    Cluster B bug fixed. The named test passes in isolation. No other file touched.
  </done>
</task>

<task type="auto">
  <name>Task 33-01-03: Add _reset_tool_registry autouse fixture in tests/conftest.py</name>
  <read_first>
    - tests/conftest.py (full file — identify a location near existing fixtures; the file already hosts the unit-scope pytest_collection_modifyitems hook per RESEARCH §Q6, so adding an autouse fixture here is the canonical landing pad)
    - services/agent/tools/registry.py:100-118 (verify `_registry` attribute name + `get_tool_registry()` factory signature)
    - tests/factories/app.py:42, 69-79 (verify `_SINGLETON_INVENTORY` contains `("services.agent.tools.registry", "_registry")` and `_reset_singletons` sets it to None — this is the leak source)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md §Q1 (Option B fixture pattern — explicit register(cls), no importlib.reload — recommended over Option A per Open Questions #2)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-RESET-01 (single-entry reset list discipline — audit-mode-before-enforce)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-SEEDS-01 (3 seeds 12345/67890/99999 documented in fixture docstring)
  </read_first>
  <action>
    Add a new autouse function-scope fixture to `tests/conftest.py` named exactly `_reset_tool_registry`. Per RESEARCH §Q1 Option B (recommended; avoids `importlib.reload` interaction with monkeypatch in TestWebSearchToolRun):

    Fixture behavior contract:
    (1) Decorator: `@pytest.fixture(autouse=True)` (function-scope is the default — do NOT add `scope=` to avoid module/session bleed).
    (2) Imports inside the fixture body (NOT at module top — keeps tests/conftest.py import-time light):
        - `import importlib`, `import pkgutil`
        - `import services.agent.tools as _tools_pkg`
        - `import services.agent.tools.registry as _reg`
        - `from services.agent.tools.registry import get_tool_registry`
        - `from services.agent.tools.base import BaseTool`
    (3) Pre-yield body — enumerate tool classes via package introspection (D2 fix: self-healing against future @register additions; replaces hardcoded 4-class list):
        - Walk `services.agent.tools` package via `pkgutil.iter_modules(_tools_pkg.__path__, prefix="services.agent.tools.")`. Skip the `registry` and `base` submodules (they expose `BaseTool` / `ToolRegistry`, not concrete tools). For each remaining submodule, `importlib.import_module(name)` and collect attributes whose value is a concrete `BaseTool` subclass (`isinstance(obj, type) and issubclass(obj, BaseTool) and obj is not BaseTool and getattr(obj, "name", None)`). Store the unique set in a local `tool_classes: set[type[BaseTool]]`.
        - Set `_reg._registry = None` (zero the module-level singleton — same surface `tests/factories/app.py:_reset_singletons` targets, but bounded; D-RESET-01 single-entry list).
        - Call `reg = get_tool_registry()` (constructs a fresh empty `ToolRegistry()`).
        - For each `cls in tool_classes`: D1 fix — guard with `if cls.name not in reg.list():` before `reg.register(cls)`. `ToolRegistry.register()` (services/agent/tools/registry.py:39-40) RAISES `ValueError` on duplicate; the guard removes the import-order coupling. If the tool module was first-imported during this fixture invocation, its `@register` decorator already populated the new registry — guard prevents the duplicate. If modules were pre-imported during pytest collection, the new registry is empty and the explicit register fires once. Either path is safe.
        - Assert post-condition: `assert len(tool_classes) >= 4`. Sentinel against a future refactor that breaks the package layout (e.g., someone moves all tools into a sibling package). 4 = current canonical count (RetrieveTool, RefinedRetrieveTool, WebSearchTool, RecallTool). Anything fewer = the introspection broke and we should fail loud at fixture setup, not silently re-introduce TEST-09's root cause.
    (4) `yield` (let the test run with a populated registry).
    (5) Post-yield (teardown): `_reg._registry = None` (leave clean for next test's fixture setup — defensive; the pre-yield body will re-init anyway, but explicit teardown is safer for fixture-ordering edge cases).

    Docstring requirements (per D-SEEDS-01 + Q8 acceptance trail):
    - Reference TEST-09 + D-RESET-01 + RESEARCH §Q1 Option B by name (grep-discoverable for future contributors)
    - Document the 3 acceptance seeds (12345, 67890, 99999) and the exact `--deselect` flags for the OCR Cluster C
    - Note that this fixture is bounded to unit-scope: `tests/integration/conftest.py` has its own autouse mock (`_mock_local_model_inits` from Phase 30-02) operating on a disjoint module surface (`HuggingFaceEmbedder.__init__` / `CrossEncoderReranker.__init__`), so the two compose cleanly (RESEARCH §Q6 R2)
    - Document the audit-mode-before-enforce decision: single-entry reset list = `_registry` only; do NOT broaden to `_SINGLETON_INVENTORY`

    Constraints (Q8 guardrails):
    - Do NOT use `importlib.reload` (Option A — rejected per RESEARCH §Q1 + Open Q#2 due to monkeypatch interaction risk). `importlib.import_module` (for first-time package discovery) is fine and required by the introspection step (D2).
    - Do NOT add any other singleton to the reset list (D-RESET-01 single-entry discipline). The pkgutil walk does NOT change the singleton inventory — it only enumerates `BaseTool` subclasses to feed `register()`.
    - Do NOT modify `tests/factories/app.py` (out-of-scope — that's where the leak originates but it's owned by other test infra)
    - Do NOT use bare `except` (ERR-01)
    - Type annotations on the fixture function; mypy --strict clean (Phase 32 D1). `pkgutil.iter_modules` yields `ModuleInfo`; `importlib.import_module` returns `ModuleType`; both typed in stdlib stubs.
    - No new `# type: ignore` — `_reg._registry = None` is well-typed (`_registry: ToolRegistry | None` is declared at services/agent/tools/registry.py:106). If mypy still complains at edit time, fall back to a typed accessor; bare `# type: ignore[attr-defined]  # why: test-scope singleton reset` is permitted ONLY if no typed alternative works (Phase 32 form: `[code]  # why:`).
  </action>
  <verify>
    <automated>grep -q '_reset_tool_registry' tests/conftest.py &amp;&amp; grep -q 'pkgutil.iter_modules' tests/conftest.py &amp;&amp; grep -q 'cls.name not in reg.list()' tests/conftest.py &amp;&amp; uv run mypy --strict tests/conftest.py 2&gt;&amp;1 | grep -qE 'Success|no issues found' &amp;&amp; uv run pytest tests/unit/test_memory_service_extra.py::test_long_term_save_fact_calls_insert tests/unit/test_pipeline_tool_schema_regression.py::test_registry_anthropic_shape_satisfies_call_agentic_turn tests/unit/test_recall_tool.py::test_recall_tool_registered_once tests/unit/test_retrieve_tool.py::TestRetrieveToolRegistration tests/unit/test_retrieve_tool.py::TestSchemasForParity tests/unit/test_web_search_tool.py::TestWebSearchToolRegistration -q 2&gt;&amp;1 | tee /tmp/named.txt &amp;&amp; ! grep -qE '[1-9][0-9]* failed' /tmp/named.txt</automated>
  </verify>
  <acceptance_criteria>
    - TEST-09c: `grep -q '_reset_tool_registry' tests/conftest.py` exits 0.
    - Fixture enumerates tool classes via `pkgutil.iter_modules` (D2 fix) — grep for `pkgutil.iter_modules` in tests/conftest.py exits 0.
    - Fixture uses idempotent register guard (D1 fix) — grep for `cls.name not in reg.list()` in tests/conftest.py exits 0.
    - Post-condition sentinel `assert len(tool_classes) >= 4` present (grep for the literal `>= 4`).
    - TEST-09g: All 7 named failures pass under default order (run the 7 explicit node IDs above; expect `7 passed` or `7 passed, 1 deselected` — no failures).
    - `mypy --strict tests/conftest.py` is clean.
    - Default-order full unit run: `uv run pytest tests/unit/ -m 'not integration' -q` shows 0 failed in the 7 previously-failing tests; runtime within 1.05x of Phase 32 baseline (~21s ceiling).
    - No regression in `tests/unit/test_web_search_tool.py::TestWebSearchToolRun` (Assumption A2 probe — these tests use `monkeypatch.setattr` on `services.agent.tools.web_search.<x>`; introspection step doesn't call `importlib.reload` so no monkeypatch conflict).
  </acceptance_criteria>
  <done>
    Reset fixture lands in tests/conftest.py with single-entry inventory + 4 re-registrations + 3-seed docstring. mypy --strict clean. All 7 named failures green. No web_search.TestWebSearchToolRun regression.
  </done>
</task>

<task type="auto">
  <name>Task 33-01-04: Seed verification + OCR Cluster C deselection</name>
  <read_first>
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-SEEDS-01 (3 acceptance seeds: 12345, 67890, 99999)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-RESEARCH.md §Q2 (per-seed pre-flight results table; expected post-fix: 0 failures excluding OCR Cluster C)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-VALIDATION.md (TEST-09d/e/f gate definitions; OCR cluster member list)
    - .planning/phases/33-autouse-mock-opt-out-flaky-failures/33-CONTEXT.md §D-VERIFY-02 (integration baseline unchanged contract)
  </read_first>
  <action>
    Run the three seed-verification commands listed below. Each command MUST exit with 0 failed; the 4 OCR Cluster C tests are deselected via explicit `--deselect` flags (per RESEARCH §Q2 + VALIDATION.md TEST-09d/e/f — the OCR Cluster C is Phase 31 EVT-02 residue and explicitly out of scope per Q8 guardrail; deselection makes the gate machine-checkable without silently hiding the failures).

    Commands (run all three; record per-seed pass count + runtime in the SUMMARY):
    (1) `uv run pytest tests/unit/ -m 'not integration' -p randomly --randomly-seed=12345 --deselect tests/unit/test_ocr_engine.py::test_semaphore_serialises_concurrent_extract_pdf_calls --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_still_uses_semaphore --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_retries_once_then_surfaces_error --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_then_success_on_retry -q`
    (2) Same with `--randomly-seed=67890`
    (3) Same with `--randomly-seed=99999`

    Also run the integration non-regression gate (TEST-09h, same as TEST-08f from plan 33-00 — note 33-00 may run in parallel; this gate should still pass independently since 33-01 touches only unit-scope files):
    (4) `uv run pytest -m 'integration and not real_llm and not real_embedder and not benchmark' --tb=no -q` MUST match Phase 32 close = 31 passed / 9 failed / 1 skipped / 3 errors. NO change.

    Also run the unit-suite perf gate (TEST-09i):
    (5) Time the full unit suite: `time uv run pytest tests/unit/ -m 'not integration' -q`. Runtime ceiling is 1.05x Phase 32 baseline (~21 seconds).

    If any seed reports > 0 failures (excluding the 4 deselected OCR Cluster C tests), stop and surface the failure in the SUMMARY. Do NOT widen the deselection list — that violates Q8 / D-RESET-01 audit-mode discipline.

    Constraints:
    - Do NOT add more seeds (D-SEEDS-01 + Q8 — broader random-order hardening is deferred to future test-infra phase).
    - Do NOT modify `services/extractor/ocr_engine.py` or `services/extractor/ocr_*.py` (OCR Cluster C is Phase 31 EVT-02 territory, deferred to v1.10 / TEST-12 candidate).
    - Do NOT add the deselection list to a config file or Makefile — keep it in the verify command for explicit documentation per RESEARCH §Q2.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ -m 'not integration' -p randomly --randomly-seed=12345 --deselect tests/unit/test_ocr_engine.py::test_semaphore_serialises_concurrent_extract_pdf_calls --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_still_uses_semaphore --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_retries_once_then_surfaces_error --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_then_success_on_retry -q 2&gt;&amp;1 | tee /tmp/s12345.txt &amp;&amp; ! grep -qE '[1-9][0-9]* failed' /tmp/s12345.txt &amp;&amp; uv run pytest tests/unit/ -m 'not integration' -p randomly --randomly-seed=67890 --deselect tests/unit/test_ocr_engine.py::test_semaphore_serialises_concurrent_extract_pdf_calls --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_still_uses_semaphore --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_retries_once_then_surfaces_error --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_then_success_on_retry -q 2&gt;&amp;1 | tee /tmp/s67890.txt &amp;&amp; ! grep -qE '[1-9][0-9]* failed' /tmp/s67890.txt &amp;&amp; uv run pytest tests/unit/ -m 'not integration' -p randomly --randomly-seed=99999 --deselect tests/unit/test_ocr_engine.py::test_semaphore_serialises_concurrent_extract_pdf_calls --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_still_uses_semaphore --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_retries_once_then_surfaces_error --deselect tests/unit/test_ocr_failure_modes.py::test_extract_pdf_timeout_then_success_on_retry -q 2&gt;&amp;1 | tee /tmp/s99999.txt &amp;&amp; ! grep -qE '[1-9][0-9]* failed' /tmp/s99999.txt &amp;&amp; uv run pytest -m 'integration and not real_llm and not real_embedder and not benchmark' --tb=no -q 2&gt;&amp;1 | tee /tmp/intg.txt &amp;&amp; grep -qE '31 passed.*9 failed' /tmp/intg.txt</automated>
  </verify>
  <acceptance_criteria>
    - TEST-09d: seed 12345 run reports 0 failed (cluster C deselected via 4 `--deselect` flags).
    - TEST-09e: seed 67890 run reports 0 failed (same deselection).
    - TEST-09f: seed 99999 run reports 0 failed (same deselection).
    - TEST-09g: explicit verification — the original named-7 + 4 free-win agent_pipeline tests all pass under each seed (covered by the full-suite invocation since they are part of `tests/unit/`).
    - TEST-09h (integration regression): default integration filter matches 31p/9f/1s/3e Phase 32 close baseline.
    - TEST-09i (perf): full unit suite runtime within 1.05x of Phase 32 baseline (note in SUMMARY).
    - Only the 4 OCR Cluster C node-ids are deselected; no other tests deselected (Q8 — no scope expansion).
  </acceptance_criteria>
  <done>
    All three seeds green (excluding documented OCR Cluster C); integration baseline unchanged; unit-suite runtime non-regressed; deferral to v1.10 / TEST-12 noted in SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unit-test runtime <-> production module state | reset fixture mutates module-level `_registry` attribute on `services.agent.tools.registry`; the boundary must not bleed into production code at runtime (autouse fixture is scoped to test session only) |
| test runtime <-> dev-deps install | `pytest-randomly` is a test-time plugin; must not be installed into the production runtime image (mitigated by `[dependency-groups].dev` placement — non-production group per uv convention) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | Tampering | `_reset_tool_registry` autouse fixture | mitigate | Fixture is bounded to `tests/conftest.py` (unit scope); does not import or affect anything under `services/` at runtime; integration/conftest.py owns its own autouse mock on disjoint surfaces (RESEARCH §Q6 R2) |
| T-33-01-02 | Tampering | pytest-randomly auto-discovery | mitigate | Plugin auto-registers when installed; surfaces seed in CLI header for triage; pinned `>=3.16.0` open upper to allow security patches; not added to CI default (Q8 guardrail) |
| T-33-01-03 | DoS | unit-suite runtime regression | mitigate | TEST-09i perf gate enforces 1.05x ceiling (~21s) vs Phase 32 baseline; per-test fixture cost target is sub-ms (4 imports + 4 register() calls); fail-fast if exceeded |
| T-33-01-04 | Information Disclosure | reset fixture exposes internal `_registry` attribute | accept | `_registry` is a module-private attribute on a test-shared module; no PII or secret in scope; test-only mutation |
| T-33-01-05 | Tampering | embed_batch mock-shape change | mitigate | Single-line additive fix to existing mock — no production code modified; mock-at-consumer (v1.3 D-mock) preserved |
| T-33-01-SC | Tampering | pytest-randomly package install (npm/pip/cargo gate) | mitigate | pytest-randomly is the dominant random-order pytest plugin on PyPI (~1M downloads/month per CONTEXT D-PLUGIN-01; pre-flight install during research succeeded with no signature/install warnings); audit table see plan-level `## Package Legitimacy Audit` below |
</threat_model>

## Package Legitimacy Audit

| Package | Spec | Status | Source | Evidence |
|---------|------|--------|--------|----------|
| `pytest-randomly` | `>=3.16.0` | VERIFIED | https://pypi.org/project/pytest-randomly/ | Author: pytest-dev affiliated maintainer Adam Johnson (~1M downloads/month per CONTEXT.md D-PLUGIN-01); RESEARCH §Q2 pre-flight install of 4.1.0 succeeded with no plugin-conflict errors against pytest 9.0.3 + pytest-asyncio 1.3.0 already in lockfile; `scripts/check_typing_hygiene.py:_STUB_NAME_RE` confirmed non-match (RESEARCH §Q5) |

VERIFIED disposition — no `[ASSUMED]` / `[SUS]` packages in this plan, so no blocking-human checkpoint is required per `planner-source-audit.md`.

<verification>
After every task: run that task's `<automated>` gate.

End-of-plan gates (all must be green):
- TEST-09a: `uv pip show pytest-randomly` Version ≥ 3.16
- TEST-09b: `grep -q '^pytest-randomly' requirements-dev.txt`
- TEST-09c: `grep -q '_reset_tool_registry' tests/conftest.py`
- TEST-09d/e/f: three seeds (12345, 67890, 99999) report 0 failed with OCR Cluster C deselected
- TEST-09g: 7 named failures from RESEARCH §Q1 all green (including the 4 agent_pipeline free wins)
- TEST-09h: integration baseline unchanged (31p/9f/1s/3e)
- TEST-09i: unit-suite runtime within 1.05x Phase 32 baseline (~21s ceiling)

Plus Phase 32 carry-forward hygiene gate: `uv run python scripts/check_typing_hygiene.py` exits 0.
</verification>

<success_criteria>
1. `pytest-randomly>=3.16.0` installed via uv; dual-written in pyproject.toml + requirements-dev.txt; auto-discovers on `pytest` invocation.
2. `_reset_tool_registry` autouse fixture in tests/conftest.py uses RESEARCH §Q1 Option B (single-entry reset list, no importlib.reload), with two eng-review hardenings: D1 idempotent register guard (`if cls.name not in reg.list()`) prevents ValueError-on-duplicate when modules are first-imported by the fixture; D2 pkgutil-based package introspection auto-discovers future `@register`'d tools instead of hardcoding the current 4.
3. `tests/unit/test_memory_service_extra.py:235` mock-shape parity fix landed (one line + comment).
4. All 7 verified RESEARCH §Q1 default-order failures green; the 4 free-win `test_agent_pipeline_refactor.py` tests also green (RESEARCH §Q2 Cluster A absorption).
5. Three acceptance seeds (12345/67890/99999) green excluding the 4 deselected OCR Cluster C tests.
6. Integration baseline unchanged (31p/9f/1s/3e under D-VERIFY-02 filter).
7. Unit-suite runtime within 1.05x Phase 32 baseline.
8. OCR Cluster C documented as deferred (v1.10 / TEST-12 candidate); NO `services/extractor/` files modified.
9. No file touched outside this plan's `files_modified` (4 files + uv.lock side-effect).
</success_criteria>

<output>
On completion, create `.planning/phases/33-autouse-mock-opt-out-flaky-failures/33-01-SUMMARY.md` documenting:
- Files changed (4 files + uv.lock); diff sizes per file
- pytest-randomly installed version + auto-discovery verified
- Reset fixture pattern (Option B per RESEARCH §Q1)
- Mock-shape fix (single-line) verified
- Per-seed pass count + runtime for 12345 / 67890 / 99999
- Unit-suite runtime delta vs Phase 32 baseline (must be ≤ 1.05x)
- Integration baseline before/after (must match 31p/9f/1s/3e)
- OCR Cluster C deferral note + recommendation: append `TEST-12` candidate to `.planning/REQUIREMENTS.md` v1.10 backlog (do NOT add the requirement in this phase — surface only)
- Any deviation from D-PLUGIN-01 / D-RESET-01 / D-MOCK-01 / D-SEEDS-01 / D-VERIFY-01 / D-VERIFY-02 with rationale (expected: none)
</output>
</content>
</invoke>