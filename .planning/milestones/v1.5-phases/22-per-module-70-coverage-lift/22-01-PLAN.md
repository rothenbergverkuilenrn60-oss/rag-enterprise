---
phase: 22-per-module-70-coverage-lift
plan: 01
type: execute
wave: 2
depends_on: [22-00]
files_modified:
  - tests/unit/test_pipeline_coverage.py
autonomous: true
requirements: [TEST-08]
tags: [coverage, pipeline, agent, swarm]

must_haves:
  truths:
    - "NO production code changes to services/pipeline.py (CF-01) — `git diff --stat services/pipeline.py` shows 0 lines changed"
    - "Mock at consumer path `services.pipeline.<dep>` only (CF-02) — NOT at SDK source"
    - "`coverage report --include=services/pipeline.py --fail-under=70` exits 0 on combined `.coverage` data"
    - "Module docstring at top cites TEST-08 + SC1 (D-12)"
    - "All ROADMAP SC1-listed branches have ≥1 test (CF-07): AgentQueryPipeline.run/run_streaming error branches; SwarmQueryPipeline synthesis (debate=False); _dedup_chunks; _build_initial_messages"
    - "diff-cover --fail-under=80 passes on the new test file (CF-06)"
  artifacts:
    - path: tests/unit/test_pipeline_coverage.py
      provides: "Coverage tests for services/pipeline.py per TEST-08 / Phase 22 SC1"
      contains: '"""Coverage tests for services/pipeline.py per TEST-08'
      min_lines: 250
  key_links:
    - from: tests/unit/test_pipeline_coverage.py
      to: services.pipeline (consumer paths only)
      via: monkeypatch.setattr at consumer paths
      pattern: 'monkeypatch\.setattr\("services\.pipeline\.'
---

<objective>
Lift `services/pipeline.py` per-module line coverage to ≥70% via new unit tests in `tests/unit/test_pipeline_coverage.py`. Cover ROADMAP SC1-prescribed branches (`AgentQueryPipeline.run`/`run_streaming` error branches, `SwarmQueryPipeline` synthesis path with `debate=False`, `_dedup_chunks`, `_build_initial_messages`); if still <70% after SC1 branches, backfill against highest-impact Missing line ranges per 22-BASELINE.md (D-06 measure-then-add).

Purpose: TEST-08; pipeline.py is the largest module (606 stmts, ~165 to add per baseline) — ships FIRST in Wave 2 to surface gate-mechanism integration issues early.
Output: One new test file `tests/unit/test_pipeline_coverage.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md
@.planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md
@services/pipeline.py
@tests/unit/test_pipeline_helpers.py
@tests/unit/test_pipeline_pii_block.py
@tests/unit/test_pipeline_tool_schema_regression.py

<interfaces>
<!-- services/pipeline.py top-level surface (extracted via grep -n) -->

services/pipeline.py top-level structure:
- L33-46: imports of consumer-path dependencies
  - `from services.agent import get_executor, get_planner`
  - `from services.agent.tools import get_tool_registry`
  - `from services.agent.tools.retrieve import retrieve_impl as _shared_execute_tool_call`
  - `from services.audit.audit_service import AuditAction, AuditEvent, AuditResult, get_audit_service`
  - `from services.events.event_bus import get_event_bus`
  - `from services.generator.generator import get_generator`
  - `from services.generator.llm_client import get_llm_client`
  - `from services.knowledge.knowledge_service import get_knowledge_service`
  - `from services.memory.memory_service import ...`
- L117: class `IngestionPipeline`
- L286: class `QueryPipeline`
- L556: class `_SubAgentResult` (frozen dataclass)
- L622: class `AgentQueryPipeline` (run + run_streaming live here; ~lines 658-735 region for run_streaming per CONTEXT.md)
- L994: `def get_ingest_pipeline()`
- L1000: `def get_query_pipeline()`
- L1006: `def get_agent_pipeline()`
- L1016: class `SwarmQueryPipeline` (synthesis path ~lines 819-878 region for debate=False)
- L1574: `def get_swarm_pipeline()`

Helper functions inside the file (per CONTEXT.md SC1):
- `_dedup_chunks` (line ~710 region)
- `_build_initial_messages` (lines ~939-957 region)

Mock-at-consumer-path pattern (CF-02):
- `monkeypatch.setattr("services.pipeline.get_planner", lambda: stub_planner)`
- `monkeypatch.setattr("services.pipeline.get_executor", lambda: stub_executor)`
- `monkeypatch.setattr("services.pipeline.get_llm_client", lambda: stub_llm_client)`
- `monkeypatch.setattr("services.pipeline.get_audit_service", lambda: stub_audit)`
- NOT `monkeypatch.setattr("services.agent.planner.create_planner", ...)` — wrong layer

Existing test patterns to reuse style from:
- tests/unit/test_pipeline_helpers.py (94 L) — pipeline module-level helper test conventions
- tests/unit/test_pipeline_pii_block.py (160 L) — fixture conventions for AgentQueryPipeline-adjacent
- tests/unit/test_pipeline_tool_schema_regression.py (207 L) — full pipeline integration mock conventions

D-10: inline per-file fixtures only — NO conftest.py extension, NO new tests/unit/coverage/ subdir.
D-11: function-style tests + grouping comments + parametrize for tables.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-1 SC1-prescribed coverage tests for AgentQueryPipeline + helpers</name>
  <files>tests/unit/test_pipeline_coverage.py</files>
  <read_first>
    - services/pipeline.py (full file; pay attention to AgentQueryPipeline class L622 and run/run_streaming methods, _dedup_chunks ~L710, _build_initial_messages ~L939-957, SwarmQueryPipeline class L1016 and synthesis path ~L819-878)
    - tests/unit/test_pipeline_helpers.py (94 L; existing module-level helper test style)
    - tests/unit/test_pipeline_tool_schema_regression.py (207 L; full pipeline mock conventions — closest analog)
    - tests/unit/test_pipeline_pii_block.py (160 L; AgentQueryPipeline fixture conventions)
    - .planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md (D-09 strict file path; D-10 inline fixtures; D-11 function style + parametrize; D-12 module docstring)
    - .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md (current pipeline.py Missing line ranges — informs Wave-2 backfill in Task 2)
  </read_first>
  <behavior>
    SC1-prescribed branches (CF-07 — these are the canonical Wave-1 targets, ROADMAP-locked):
    - `AgentQueryPipeline.run` error branches:
      - planner raises → audit logs failure + raises typed error (or yields error event in run path; verify per actual signature)
      - executor raises mid-loop → audit logs failure + raises typed error
      - synthesizer raises → audit logs failure + raises typed error
      - tool returns ToolResult with `error=True` → run continues to synthesizer with error chunk
    - `AgentQueryPipeline.run_streaming` error branches:
      - planner raises → emits error AgentEvent + closes stream cleanly
      - executor raises → emits error AgentEvent
      - synthesizer raises → emits error AgentEvent + synthesizer.final still terminal per Phase 21 invariant
    - `SwarmQueryPipeline` synthesis path with `debate=False`:
      - peer fan-out completes → synthesizer composes → returns GenerationResponse
      - assert NO verifier hop runs when debate=False (Phase 21 invariant carry-forward)
    - `_dedup_chunks`:
      - duplicate chunk_id collapses
      - distinct chunk_ids preserve order
      - empty input returns empty list
    - `_build_initial_messages`:
      - system prompt + user query produces 2-message list
      - retrieved_context (if accepted as param) injects into system or user message per actual signature

    All tests use mock-at-consumer-path (CF-02) — `monkeypatch.setattr("services.pipeline.<dep>", stub)`, NEVER at SDK source.
  </behavior>
  <action>
1. Create `tests/unit/test_pipeline_coverage.py` with module docstring (D-12 verbatim form):
   ```python
   """Coverage tests for services/pipeline.py per TEST-08 (Phase 22 SC1).

   Targets:
   - AgentQueryPipeline.run/run_streaming error branches
   - SwarmQueryPipeline synthesis path (debate=False)
   - _dedup_chunks
   - _build_initial_messages

   Mock at consumer path (services.pipeline.<dep>) only — CF-02 v1.3 D-04 lock.
   No production-code changes (CF-01 v1.3 D-04 lock).
   """
   ```

2. Standard `os.environ.setdefault` setup at top (match `test_vector_store_filter_where.py` lines 11-13 pattern):
   ```python
   import os
   os.environ.setdefault("APP_MODEL_DIR", "/tmp")
   os.environ.setdefault("SECRET_KEY", "a-very-secure-key-for-testing-that-is-long-32c")
   ```

3. Define inline fixtures at top of file (D-10) — `stub_planner`, `stub_executor`, `stub_llm_client`, `stub_audit_service`, `stub_event_bus`, `stub_tool_registry`. Each as `@pytest.fixture` returning a `unittest.mock.AsyncMock`-wrapped object whose methods return Pydantic V2 frozen models matching the actual contracts (ToolPlan, ToolCall, AgentEvent subclasses, GenerationResponse). Read `services/pipeline.py` for the exact return shapes; do not invent fields.

4. Group sections with `# --- AgentQueryPipeline.run error branches ---` style comments per D-11.

5. Test functions to write (function-style, not class-based, per D-11):
   - `test_agent_query_pipeline_run_planner_raises_audits_and_raises(monkeypatch, stub_audit_service)`
     - Arrange: monkeypatch `services.pipeline.get_planner` → planner whose `plan()` raises `RuntimeError("planner-down")`
     - Act + Assert: `with pytest.raises(<TypedError>)` + assert `stub_audit_service.log.call_count >= 1` + assert audit kwargs include `result=AuditResult.FAILURE`
   - `test_agent_query_pipeline_run_executor_raises_audits_and_raises(monkeypatch, stub_audit_service, stub_planner)`
   - `test_agent_query_pipeline_run_synthesizer_raises_audits_and_raises(monkeypatch, ...)`
   - `test_agent_query_pipeline_run_tool_error_result_continues_to_synth(monkeypatch, ...)` (assert no exception; assert error chunk reaches synthesizer in mock call args)
   - `test_agent_query_pipeline_run_streaming_planner_raises_emits_error_event(monkeypatch, ...)` — collect events from `async for e in pipeline.run_streaming(req)`; assert at least one event is the typed error event class; assert stream closes cleanly (no unhandled exception in test)
   - `test_agent_query_pipeline_run_streaming_executor_raises_emits_error_event(...)`
   - `test_agent_query_pipeline_run_streaming_synthesizer_raises_emits_error_event(...)`
   - `test_swarm_query_pipeline_run_debate_false_no_verifier(monkeypatch, ...)` — assert verifier consumer is NOT called; assert synthesizer composes from peer answers
   - `test_swarm_query_pipeline_synthesis_composes_peer_answers(monkeypatch, ...)`
   - `test_dedup_chunks_collapses_duplicate_chunk_ids()` — pure function, no monkeypatch needed
   - `test_dedup_chunks_preserves_order_for_distinct_ids()`
   - `test_dedup_chunks_empty_input_returns_empty()`
   - `test_build_initial_messages_returns_two_message_list()`
   - `test_build_initial_messages_with_retrieved_context_injects()` (only if `_build_initial_messages` accepts a context param — verify by reading the function signature first)

6. Use `pytest.mark.parametrize` for table-driven cases where natural (e.g., parametrize the 3 error-source variants `[planner, executor, synthesizer]` if signatures align).

7. CF-02 self-check: every `monkeypatch.setattr` call MUST start with `"services.pipeline."` as the path prefix. NO `"services.agent.planner..."` or `"anthropic..."` etc.

8. CF-04 self-check: do NOT add `# pragma: no cover` markers ANYWHERE in the test file or in services/pipeline.py.
  </action>
  <verify>
    <automated>test -f tests/unit/test_pipeline_coverage.py && head -1 tests/unit/test_pipeline_coverage.py | grep -q '"""Coverage tests for services/pipeline.py per TEST-08' && grep -c 'monkeypatch.setattr("services.pipeline\.' tests/unit/test_pipeline_coverage.py | awk '{exit ($1 < 5)}' && grep -cE '^def test_|^async def test_' tests/unit/test_pipeline_coverage.py | awk '{exit ($1 < 12)}' && pytest tests/unit/test_pipeline_coverage.py -v --timeout=30 -x</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_pipeline_coverage.py` exists
    - First line is the D-12 module docstring starting with `"""Coverage tests for services/pipeline.py per TEST-08 (Phase 22 SC1).`
    - At least 12 test functions defined (`grep -cE '^def test_|^async def test_' ≥ 12`)
    - At least 5 `monkeypatch.setattr("services.pipeline.` calls (CF-02 consumer-path mocking)
    - Zero `monkeypatch.setattr("anthropic.` or `monkeypatch.setattr("openai.` or `monkeypatch.setattr("services.agent.` calls (CF-02 SDK-source-mock prohibition; check via `grep -c` returning 0)
    - Zero `# pragma: no cover` additions to services/pipeline.py (CF-01: `git diff services/pipeline.py | grep -c 'pragma: no cover'` returns 0)
    - `pytest tests/unit/test_pipeline_coverage.py -v --timeout=30 -x` exits 0 (all tests pass)
    - `[ -z "$(git diff --name-only services/ | grep '\.py$')" ]` succeeds (CF-01: zero production .py files changed — W2-fix unambiguous form)
  </acceptance_criteria>
  <done>SC1-prescribed branches all tested; ready to measure pipeline.py per-module coverage and decide if Task 2 backfill is needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-2 backfill (only if Wave 1 didn't reach ≥70%) + per-module gate verification</name>
  <files>tests/unit/test_pipeline_coverage.py</files>
  <read_first>
    - tests/unit/test_pipeline_coverage.py (file just created in Task 1; current state)
    - .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md (Missing line ranges from 22-00 baseline)
    - services/pipeline.py (line ranges flagged by post-Task-1 `coverage report --include=services/pipeline.py --show-missing`)
  </read_first>
  <action>
1. Run combined-coverage measurement after Task 1 lands:
   ```bash
   coverage erase
   COVERAGE_FILE=.coverage.unit pytest tests/unit/ --asyncio-mode=auto --timeout=30 --cov=services --cov=utils --cov-report= -q
   COVERAGE_FILE=.coverage.integration pytest tests/integration/ --asyncio-mode=auto --timeout=60 --cov=services --cov=utils --cov-append --cov-report= -q || true
   coverage combine --keep .coverage.unit .coverage.integration
   coverage report --include=services/pipeline.py --show-missing
   ```

2. Compute current Cover% for `services/pipeline.py`:
   - If `Cover ≥ 70%`: Task 2 is a NO-OP. Verify the gate, run diff-cover, and finalize.
   - If `Cover < 70%`: Identify the highest-impact Missing line ranges (longest contiguous blocks in the Missing column). For each, write 1-3 targeted tests appended to `tests/unit/test_pipeline_coverage.py` under a new section comment `# --- Wave-2 backfill: <branch description> ---`. Use the same fixture set + consumer-path mocking style from Task 1.

3. D-06 measure-then-add backfill priority order (largest blast radius first):
   - IngestionPipeline.run path branches (line range 117-285 region per Wave-1 measurement)
   - QueryPipeline.run path branches (line range 286-555 region)
   - AgentQueryPipeline branches not covered by SC1 (e.g., `_persist_turn` audit gate, `_run_tool_loop` exhaustion branch — per actual Missing list)
   - SwarmQueryPipeline branches not covered by SC1

4. Re-run the per-module gate after each backfill batch; stop as soon as `coverage report --include=services/pipeline.py --fail-under=70` exits 0.

5. Final verification: run `make coverage-per-module` (from 22-00) — only the `services/pipeline.py` line should show PASS at minimum (other 4 modules are addressed in 22-02..22-05 in parallel; their FAIL is expected and acceptable until those plans land).

6. CF-06 diff-cover: run locally
   ```bash
   coverage xml -o coverage.xml
   diff-cover coverage.xml --compare-branch=origin/master --fail-under=80
   ```
   Diff-cover should pass since the new test file is 100% new code.

7. Self-check: re-run CF-01 (`[ -z "$(git diff --name-only services/ | grep '\.py$')" ]` — W2-fix unambiguous form), CF-02 (no SDK-source mocks), CF-04 (no `# pragma: no cover` additions).
  </action>
  <verify>
    <automated>coverage report --include=services/pipeline.py --fail-under=70 && pytest tests/unit/test_pipeline_coverage.py -v --timeout=30 && [ -z "$(git diff --name-only services/ | grep '\.py$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `coverage report --include=services/pipeline.py --fail-under=70` exits 0 (TEST-08 SC1 satisfied)
    - `pytest tests/unit/test_pipeline_coverage.py -v --timeout=30` exits 0
    - `[ -z "$(git diff --name-only services/ | grep '\.py$')" ]` succeeds (CF-01: zero production .py files changed — W2-fix unambiguous form)
    - If diff-cover available: `diff-cover coverage.xml --compare-branch=origin/master --fail-under=80` exits 0 on the touched test file (CF-06)
    - All Task-1 tests still pass (no regressions from backfill additions)
  </acceptance_criteria>
  <done>services/pipeline.py per-module ≥70% on combined data; TEST-08 testing requirement satisfied (gate hard-fail flip happens in 22-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → services/pipeline.py | tests import + monkeypatch consumer-path attributes; no network, no SDK calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Information Disclosure | new test file | accept | Test file imports services.pipeline only; uses inline stub fixtures; does not load secrets, does not call external APIs |
| T-22-01-02 | Tampering | services/pipeline.py | accept | CF-01 lock: zero production code changes asserted in acceptance criteria via `[ -z "$(git diff --name-only services/ | grep '\.py$')" ]` (W2-fix unambiguous form) |
</threat_model>

<verification>
- `pytest tests/unit/test_pipeline_coverage.py -v --timeout=30` exits 0
- `coverage report --include=services/pipeline.py --fail-under=70` exits 0 on combined data
- `[ -z "$(git diff --name-only services/ | grep '\.py$')" ]` succeeds (CF-01 — W2-fix)
- All `monkeypatch.setattr` paths begin with `services.pipeline.` (CF-02)
- No `# pragma: no cover` additions anywhere (CF-04)
- diff-cover ≥80% on touched files (CF-06)
</verification>

<success_criteria>
1. SC1 (TEST-08): services/pipeline.py per-module coverage ≥70% on combined `.coverage` data
2. All 4 SC1 branch families covered with ≥1 test each (AgentQueryPipeline.run/run_streaming error branches; SwarmQueryPipeline synthesis debate=False; _dedup_chunks; _build_initial_messages)
3. CF-01 lock honored: zero production code changes
4. CF-02 lock honored: all mocks at consumer path `services.pipeline.<dep>`
</success_criteria>

<output>
After completion, create `.planning/phases/22-per-module-70-coverage-lift/22-01-SUMMARY.md` summarizing: (a) final pipeline.py Cover%, (b) test count + parametrize-expansion count, (c) Wave-2 backfill scope (or "no backfill needed"), (d) confirmation of CF-01/CF-02/CF-04/CF-06 locks.
</output>
</output>
