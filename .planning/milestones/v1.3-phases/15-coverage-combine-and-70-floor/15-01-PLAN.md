---
phase: 15-coverage-combine-and-70-floor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
  - README.md
  - Makefile
autonomous: true
requirements: [TEST-04, TEST-06]
must_haves:
  truths:
    - "pyproject.toml contains [tool.coverage.run] with source = [\"services\", \"utils\"] and parallel = false (D-01, D-08 revised)"
    - "pyproject.toml contains [tool.coverage.report] with fail_under = 70, show_missing = true, precision = 1 (D-08; TEST-06 AC#1, AC#5)"
    - "ci.yml unit-tests job no longer passes --cov-fail-under=46 to pytest (D-06)"
    - "ci.yml unit-tests job sets COVERAGE_FILE=.coverage.unit env var and uploads .coverage.unit as artifact named coverage-unit (D-02, D-10)"
    - "ci.yml unit-tests job no longer runs the diff-cover step (D-05; the step migrates to coverage-combine)"
    - "ci.yml integration-tests job sets COVERAGE_FILE=.coverage.integration, gains --cov=services --cov=utils --cov-append --cov-report= flags, uploads .coverage.integration as artifact named coverage-integration (D-10)"
    - "ci.yml integration-tests job retains continue-on-error: true (D-03)"
    - "ci.yml has a new coverage-combine job with needs: [unit-tests, integration-tests] and if: always() (D-02, D-03)"
    - "coverage-combine job uses actions/checkout@v4 with fetch-depth: 0 (Pitfall 4 — required for diff-cover merge-base with v1.0)"
    - "coverage-combine job downloads coverage-unit and coverage-integration artifacts (latter with continue-on-error: true) (D-03)"
    - "coverage-combine job runs `coverage combine --keep .coverage.unit .coverage.integration` (Pitfall 2 — preserves inputs for debug artifact)"
    - "coverage-combine job runs `coverage report --fail-under=70` as TEST-06 hard gate after combine (TEST-06 AC#2)"
    - "coverage-combine job runs `coverage xml -o coverage.xml` then `git fetch origin tag v1.0 --no-tags` then `diff-cover coverage.xml --compare-branch=v1.0 --fail-under=80 --format html:diff-cover.html` (D-05, D-09; TEST-04 AC#3, AC#5)"
    - "coverage-combine job uploads coverage-report artifact containing .coverage, .coverage.unit, .coverage.integration, coverage.xml, diff-cover.html (TEST-04 AC#4)"
    - "README.md §Coverage section (lines 241-273) updated to document combined unit + integration flow with 70% floor; explicitly cites Phase 15 D-05 supersession of Phase 10 D-03 (D-12)"
    - "Makefile coverage-diff target preserved unchanged; new coverage-combined target added that mirrors CI (run unit + integration + combine + report --fail-under=70 locally)"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.coverage.run] and [tool.coverage.report] config blocks (D-01, D-08)"
      contains: "[tool.coverage.report]"
    - path: ".github/workflows/ci.yml"
      provides: "3-job coverage topology: unit-tests (data only), integration-tests (data only --cov-append), coverage-combine (combine + floor + diff-cover + artifact)"
      contains: "coverage-combine:"
    - path: "README.md"
      provides: "Coverage section documenting combined report + 70% floor + Phase 10 D-03 supersession note"
      contains: "70%"
    - path: "Makefile"
      provides: "coverage-combined target mirroring CI for local DX"
      contains: "coverage-combined:"
  key_links:
    - from: "ci.yml unit-tests pytest step"
      to: ".coverage.unit data file"
      via: "COVERAGE_FILE=.coverage.unit env var (D-10)"
      pattern: "COVERAGE_FILE:[[:space:]]*\\.coverage\\.unit"
    - from: "ci.yml integration-tests pytest step"
      to: ".coverage.integration data file"
      via: "COVERAGE_FILE=.coverage.integration env var + --cov-append (D-10)"
      pattern: "COVERAGE_FILE:[[:space:]]*\\.coverage\\.integration"
    - from: "coverage-combine job"
      to: "coverage-unit + coverage-integration artifacts"
      via: "actions/download-artifact@v4 with name: coverage-{unit,integration}"
      pattern: "name:[[:space:]]*coverage-(unit|integration)"
    - from: "coverage-combine job"
      to: "coverage report --fail-under=70 hard gate"
      via: "step runs after `coverage combine` on combined .coverage SQLite"
      pattern: "coverage report --fail-under=70"
    - from: "coverage-combine job"
      to: "diff-cover --fail-under=80 hard gate"
      via: "step consumes coverage.xml emitted from combined .coverage (D-05)"
      pattern: "diff-cover coverage\\.xml"
---

<objective>
Plumb combined unit + integration coverage in CI and raise the global floor from 46% to 70%. Add `[tool.coverage.run]` and `[tool.coverage.report]` blocks to `pyproject.toml` (`fail_under = 70`, `parallel = false` per researcher-revised D-08, `source = ["services", "utils"]`). Refactor `.github/workflows/ci.yml`: drop `--cov-fail-under=46` from `unit-tests` (D-06); split coverage data via `COVERAGE_FILE` env var per job (D-10); add `--cov-append` flags to `integration-tests` (which keeps `continue-on-error: true` per D-03); add a NEW `coverage-combine` job with `needs: [unit-tests, integration-tests]` and `if: always()` (D-03) that downloads both artifacts, runs `coverage combine --keep`, enforces TEST-06 floor (`coverage report --fail-under=70`), generates combined `coverage.xml`, fetches the `v1.0` tag, runs the migrated TEST-03 diff-cover gate (`--fail-under=80`), and uploads the final `coverage-report` artifact (combined SQLite + per-job inputs + xml + diff-cover.html). The diff-cover step MOVES from `unit-tests` to `coverage-combine` per D-05 (combined report = source of truth for both gates per TEST-04 AC#3). Update `README.md` lines 241-273 to document the new flow and explicitly note Phase 15 D-05 supersedes Phase 10 D-03. Add a `make coverage-combined` target that mirrors CI behavior locally for developer DX.

Purpose: Wave 1 is pure plumbing — no test files added, no service code touched. It establishes the data-collection topology, the named artifacts, and the floor gate so Wave 2 (15-02) can run `coverage report` against combined data, identify modules under 70%, and backfill targeted unit tests against a stable measurement substrate. Phase 15's TEST-04 ACs #1, #2, #3, #4, #5 and TEST-06 ACs #1, #2, #3, #5 are all satisfied by this plan; only TEST-06 AC#4 (backfill of services/ modules below 70%) is deferred to Wave 2 per D-04 (measure-then-plan workflow).

Output: 4 files modified. `pyproject.toml` gains ~25 lines of `[tool.coverage.*]` config. `.github/workflows/ci.yml` refactors the existing `unit-tests` and `integration-tests` jobs (drop floor flag, swap artifact name, add `COVERAGE_FILE` env, remove diff-cover step from unit) and appends a new `coverage-combine` job (~70 lines). `README.md` rewrites lines 241-273. `Makefile` gains a `coverage-combined` target (~12 lines). No test files added; existing `tests/unit/` and `tests/integration/` are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-coverage-combine-and-70-floor/15-CONTEXT.md
@.planning/phases/15-coverage-combine-and-70-floor/15-RESEARCH.md

@pyproject.toml
@.github/workflows/ci.yml
@README.md
@Makefile
@pytest.ini

<interfaces>
<!-- Existing pyproject.toml — Phase 15 APPENDS [tool.coverage.*] blocks. -->
<!-- Existing dev dependencies (pyproject.toml lines 76-87) already include pytest-cov==6.0.0, diff-cover>=9.7.2, coverage 7.x is transitive. NO new packages required. -->

<!-- Existing ci.yml shape (286 lines as of Phase 14 close). Key line landmarks: -->

```yaml
# .github/workflows/ci.yml — current 4-job structure
# - lint-and-type-check  (lines ~25-50)
# - unit-tests           (lines ~50-95)
#   • line 64: --cov-fail-under=46 (REMOVE per D-06)
#   • line 67-68: git fetch origin tag v1.0 --no-tags (KEEP — also needed in combine job)
#   • line 79: diff-cover coverage.xml ... (REMOVE — migrates to coverage-combine per D-05)
#   • line 84-92: actions/upload-artifact@v4 name: coverage-report (RENAME → coverage-unit, narrow path)
# - integration-tests    (lines ~97-145)
#   • line 130: continue-on-error: true (KEEP per D-03)
#   • pytest call: ADD --cov=services --cov=utils --cov-append --cov-report=
#   • ADD env: COVERAGE_FILE: .coverage.integration
#   • ADD upload-artifact step → coverage-integration (.coverage.integration)
# - security-scan        (UNCHANGED in Phase 15)
# - docker-build         (UNCHANGED in Phase 15)
# After Phase 15: NEW coverage-combine job inserted between integration-tests and security-scan
```

<!-- Existing Makefile shape (112 lines). coverage-diff target at line 88 — KEEP UNCHANGED. -->

```makefile
# Makefile:7   help build up down logs shell ingest eval clean coverage-diff   (target list)
# Makefile:88  coverage-diff:  ## Run diff-cover locally against origin/master...HEAD (TEST-03)
# Makefile:91  conda run -n torch_env pytest ... --cov-report=xml:coverage.xml ...
# Makefile:100 conda run -n torch_env diff-cover coverage.xml --compare-branch=origin/master ...
# After Phase 15: APPEND new target `coverage-combined:` (no edit to existing coverage-diff body).
```

<!-- README.md §Coverage section spans lines 241-273 (verified by RESEARCH §Pattern 5). -->
<!-- Replace per the canonical text in 15-RESEARCH.md §Code Examples §"README.md replacement copy". -->

<!-- Researcher-verified coverage.py 7.x semantics (15-RESEARCH.md): -->
<!-- 1. `coverage combine` with one missing input → stderr warning, exit 0 (continues). -->
<!-- 2. `coverage combine` with ALL missing inputs → "No data to combine", exit 1 (correct fail signal per D-03). -->
<!-- 3. `coverage report --fail-under=N` exits status 2 on miss (NOT 1 — Pitfall 5). GitHub Actions treats any non-zero as failure → step fails → job fails → PR blocked. -->
<!-- 4. `coverage combine` deletes inputs by default; `--keep` preserves them for the combined artifact (Pitfall 2). -->
<!-- 5. `parallel = true` + `COVERAGE_FILE=.coverage.unit` produces `.coverage.unit.HOST.PID.RAND` — breaks `path: .coverage.unit` artifact match (Pitfall 1). MUST set `parallel = false`. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append [tool.coverage.run] and [tool.coverage.report] blocks to pyproject.toml (D-01, D-08)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (entire file, 93 lines — confirm no existing `[tool.coverage.*]` block; identify the bottom-of-file insertion point after the last existing `[tool.*]` block)
    - .planning/phases/15-coverage-combine-and-70-floor/15-CONTEXT.md §"Specifics" lines 102-125 (canonical pyproject.toml additions)
    - .planning/phases/15-coverage-combine-and-70-floor/15-RESEARCH.md §"Pattern 1" + §"Pitfall 1" (parallel=false rationale, empirically verified 2026-05-09)
    - .planning/phases/15-coverage-combine-and-70-floor/15-RESEARCH.md §"Code Examples — Full pyproject.toml additions"
  </read_first>
  <behavior>
    - Test: `grep -c '^\[tool.coverage.run\]' pyproject.toml` returns exactly 1
    - Test: `grep -c '^\[tool.coverage.report\]' pyproject.toml` returns exactly 1
    - Test: `grep -c '^fail_under = 70' pyproject.toml` returns exactly 1
    - Test: `grep -c '^parallel = false' pyproject.toml` returns exactly 1
    - Test: `grep -c '^source = \["services", "utils"\]' pyproject.toml` returns exactly 1
    - Test: `grep -c '^show_missing = true' pyproject.toml` returns exactly 1
    - Test: `grep -c '^precision = 1' pyproject.toml` returns exactly 1
    - Test: `python -c "import tomllib; d = tomllib.load(open('pyproject.toml','rb')); assert d['tool']['coverage']['report']['fail_under'] == 70; assert d['tool']['coverage']['run']['parallel'] is False; assert d['tool']['coverage']['run']['source'] == ['services','utils']"` exits 0
    - Test: `coverage report --help` succeeds (sanity — coverage.py recognizes pyproject config)
  </behavior>
  <action>
    Append the following block to the END of `pyproject.toml`. Do NOT modify any existing line. Insert one blank line before the new content if the file does not already end with one.

    ```toml

    [tool.coverage.run]
    source = ["services", "utils"]
    parallel = false   # NOTE: deviates from CONTEXT.md D-08 draft (parallel = true) per RESEARCH.md Pitfall 1.
                        # Empirically verified 2026-05-09 (coverage.py 7.13.5): parallel = true + COVERAGE_FILE
                        # appends .HOST.PID.RAND suffix and breaks `path: .coverage.unit` artifact upload.
                        # Explicit COVERAGE_FILE per-job (D-10) already provides per-job uniqueness.
    branch = false   # line coverage only; branch coverage deferred to v1.4+
    omit = [
        "*/__init__.py",
        "*/migrations/*",
        "*/tests/*",
    ]

    [tool.coverage.report]
    fail_under = 70   # TEST-06 AC#1: raises floor from 46% (v1.0 bootstrap) to 70%
    show_missing = true   # TEST-06 AC#5: per-module breakdown in CI logs
    precision = 1
    skip_covered = false   # show all modules (covered + uncovered) for visibility
    exclude_lines = [
        "pragma: no cover",
        "raise NotImplementedError",
        "if __name__ == .__main__.:",
        "if TYPE_CHECKING:",
    ]

    [tool.coverage.xml]
    output = "coverage.xml"
    ```

    Do NOT:
    - Set `parallel = true` (would break artifact upload — Pitfall 1, empirically verified)
    - Set `branch = true` (deferred per CONTEXT deferred-ideas list — line-only in v1.3)
    - Add a `[tool.coverage.html]` block (no HTML report consumed in v1.3)
    - Add `[tool.coverage.paths]` (no path-mapping needed — single repo, single CI runner)
    - Create a separate `.coveragerc` file (D-01 explicitly forbids; would conflict with pyproject precedence)
    - Reorder or modify any pre-existing `[tool.*]` block (e.g., `[tool.uv.workspace]`, `[tool.ruff]`, `[tool.mypy]` — all UNCHANGED)
    - Increase `precision` above 1 (default 0; precision=1 sufficient for AC#5 readability)
    - Add `fail_under_module` keys (per-module floors are a v1.4+ candidate; phase scope is global floor only)
    - Remove `*/__init__.py` from `omit` (project convention; init files have minimal coverable code, would skew %)
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/project_pytorch/project/rag_enterprise && python -c "
import tomllib
with open('pyproject.toml', 'rb') as f:
    d = tomllib.load(f)
cov = d.get('tool', {}).get('coverage', {})
run = cov.get('run', {})
report = cov.get('report', {})
xml = cov.get('xml', {})
assert run.get('source') == ['services', 'utils'], run
assert run.get('parallel') is False, run
assert run.get('branch') is False, run
assert '*/__init__.py' in run.get('omit', []), run
assert report.get('fail_under') == 70, report
assert report.get('show_missing') is True, report
assert report.get('precision') == 1, report
assert report.get('skip_covered') is False, report
assert 'pragma: no cover' in report.get('exclude_lines', []), report
assert xml.get('output') == 'coverage.xml', xml
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[tool.coverage.run\]' pyproject.toml` returns exactly 1
    - `grep -c '^\[tool.coverage.report\]' pyproject.toml` returns exactly 1
    - `grep -c '^\[tool.coverage.xml\]' pyproject.toml` returns exactly 1
    - `grep -c '^fail_under = 70' pyproject.toml` returns exactly 1 (TEST-06 AC#1)
    - `grep -c '^parallel = false' pyproject.toml` returns exactly 1 (D-08 revised, Pitfall 1)
    - `grep -c '^parallel = true' pyproject.toml` returns 0 (must NOT use parallel mode)
    - `grep -c 'source = \["services", "utils"\]' pyproject.toml` returns exactly 1
    - `grep -c '^show_missing = true' pyproject.toml` returns exactly 1 (TEST-06 AC#5)
    - `grep -c '^precision = 1' pyproject.toml` returns exactly 1
    - `grep -c '\.coveragerc' pyproject.toml` returns 0 (D-01 — single config file)
    - `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0 (TOML is parseable)
    - `coverage debug config 2>&1 | grep -E 'fail_under|source|parallel'` shows fail_under=70, source=['services','utils'], parallel=False (coverage.py auto-discovers pyproject.toml)
    - No pre-existing block modified: `git diff pyproject.toml -- | grep -E '^-\[tool\.' | wc -l` returns 0
    - `ruff check pyproject.toml` is not applicable (ruff doesn't lint TOML); skip — TOML validity covered by tomllib.load above
  </acceptance_criteria>
  <done>
    `pyproject.toml` contains 3 new top-level blocks (`[tool.coverage.run]`, `[tool.coverage.report]`, `[tool.coverage.xml]`) with `fail_under = 70`, `parallel = false`, `source = ["services", "utils"]`, `show_missing = true`, `precision = 1`. Pre-existing `[tool.*]` blocks unchanged. TOML parses; coverage.py auto-discovers the config.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor ci.yml unit-tests + integration-tests jobs and add coverage-combine job (D-02, D-03, D-05, D-06, D-10)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file, 286 lines — identify exact line ranges of `unit-tests` and `integration-tests` jobs, including the existing diff-cover step and artifact upload steps)
    - .planning/phases/15-coverage-combine-and-70-floor/15-CONTEXT.md §"Specifics" lines 127-200 (canonical job sketches)
    - .planning/phases/15-coverage-combine-and-70-floor/15-RESEARCH.md §"Code Examples" (full per-job YAML — `unit-tests` edits, `integration-tests` edits, NEW `coverage-combine` job)
    - .planning/phases/15-coverage-combine-and-70-floor/15-RESEARCH.md §"Common Pitfalls" #1, #2, #3, #4, #5, #6 (parallel=false, --keep, missing-input semantics, fetch-depth, exit-2, --cov-report=)
  </read_first>
  <behavior>
    - Test: `unit-tests` job pytest invocation does NOT include `--cov-fail-under=46` (D-06)
    - Test: `unit-tests` job sets `env: COVERAGE_FILE: .coverage.unit` on the pytest step (D-10)
    - Test: `unit-tests` job upload-artifact step uses `name: coverage-unit` and `path: .coverage.unit`
    - Test: `unit-tests` job no longer contains a `diff-cover` step (D-05 — migrates to combine)
    - Test: `unit-tests` job no longer contains a `git fetch origin tag v1.0` step (D-05 — migrates to combine; if the existing one was solely for diff-cover, remove)
    - Test: `integration-tests` job pytest call includes `--cov=services`, `--cov=utils`, `--cov-append`, `--cov-report=` (D-10; Pitfall 6 empty `--cov-report=` suppresses misleading partial terminal output)
    - Test: `integration-tests` job sets `env: COVERAGE_FILE: .coverage.integration` (D-10)
    - Test: `integration-tests` job retains `continue-on-error: true` on the pytest step (D-03)
    - Test: `integration-tests` job has a NEW upload-artifact step uploading `name: coverage-integration` `path: .coverage.integration` with `if: always()` and `if-no-files-found: warn`
    - Test: NEW `coverage-combine` job exists with `needs: [unit-tests, integration-tests]` and `if: always()` (D-02, D-03)
    - Test: combine job's checkout step sets `fetch-depth: 0` (Pitfall 4 — required for diff-cover merge-base)
    - Test: combine job downloads both `coverage-unit` and `coverage-integration` artifacts; integration download has `continue-on-error: true` (D-03 + Pitfall 3)
    - Test: combine job runs `coverage combine --keep .coverage.unit .coverage.integration` (Pitfall 2 — preserves inputs for debug)
    - Test: combine job runs `coverage report --fail-under=70` AFTER combine as TEST-06 hard gate (TEST-06 AC#2)
    - Test: combine job runs `coverage xml -o coverage.xml` then `git fetch origin tag v1.0 --no-tags` then `diff-cover coverage.xml --compare-branch=v1.0 --fail-under=80 --format html:diff-cover.html` (D-05, D-09; TEST-04 AC#5)
    - Test: combine job's final upload-artifact step uses `name: coverage-report` and includes `.coverage`, `.coverage.unit`, `.coverage.integration`, `coverage.xml`, `diff-cover.html` (TEST-04 AC#4)
    - Test: `yamllint -d "{extends: default, rules: {line-length: disable, document-start: disable, truthy: disable, indentation: {indent-sequences: consistent}}}" .github/workflows/ci.yml` parses cleanly (no critical errors)
    - Test: ci.yml has exactly one `coverage-combine:` top-level job key (no duplicate, no nested misplacement)
  </behavior>
  <action>
    Modify `.github/workflows/ci.yml` in three regions. Use the canonical YAML from 15-RESEARCH.md §"Code Examples". Do NOT alter `lint-and-type-check`, `security-scan`, `docker-build`, or any other unrelated job.

    **Edit 1 — `unit-tests` job (currently lines ~50-95)**

    Locate the existing `Run unit tests with coverage` step. Apply 4 sub-edits:

    1. **REMOVE `--cov-fail-under=46`** from the pytest invocation (current line 64). The pytest call becomes:
       ```yaml
       - name: Run unit tests with coverage (data collection only)
         env:
           COVERAGE_FILE: .coverage.unit
         run: |
           pytest tests/unit/ \
             --asyncio-mode=auto \
             --timeout=30 \
             --cov=services \
             --cov=utils \
             --cov-report= \
             -v
       ```
       Drop any `--cov-report=xml:coverage.xml` and `--cov-report=term-missing` flags from this step — combine job emits the canonical xml. Add the `env: COVERAGE_FILE: .coverage.unit` block above `run:`.

    2. **REMOVE the diff-cover step** (currently around line 79: `Run diff-cover against v1.0`) and the preceding `Fetch v1.0 tag for diff-cover baseline` step (~line 67-68 if it lives only inside the `unit-tests` job and is not used by anything else there). Both move to the new `coverage-combine` job per D-05.

       If the `git fetch origin tag v1.0 --no-tags` step is co-located with the diff-cover step in `unit-tests`, remove both. The combine job has its own checkout + fetch.

    3. **REPLACE the `Upload coverage report` step** (currently ~line 84-92) with:
       ```yaml
       - name: Upload unit coverage data
         uses: actions/upload-artifact@v4
         if: always()
         with:
           name: coverage-unit
           path: .coverage.unit
           if-no-files-found: error
           retention-days: 7
       ```
       The artifact name changes from the previous `coverage-report` (or whatever) to **`coverage-unit`**, and `path:` is the exact filename `.coverage.unit` (NOT a glob — `parallel = false` guarantees exact name per Pitfall 1).

    4. **Preserve** the existing `actions/checkout@v4` with `fetch-depth: 0` line (existing line ~42-44 if present) — it's still useful for any local diff-cover invocation by developers, and removing it is out of scope.

    **Edit 2 — `integration-tests` job (currently lines ~97-145)**

    Apply 3 sub-edits:

    1. **EXTEND the pytest invocation** to add `--cov` flags and the empty `--cov-report=` suppressor (Pitfall 6). The full step becomes:
       ```yaml
       - name: Run integration tests with coverage (data collection only)
         env:
           REDIS_URL: redis://localhost:6379/0
           ENVIRONMENT: development
           COVERAGE_FILE: .coverage.integration
         run: |
           pytest tests/integration/ \
             --asyncio-mode=auto \
             --timeout=60 \
             --cov=services \
             --cov=utils \
             --cov-append \
             --cov-report= \
             -v
         continue-on-error: true   # KEEP per D-03 — integration is gated by continue-on-error; combine job uses whatever data exists
       ```
       Preserve any other env vars currently set on this step (e.g., `REDIS_URL`, `ENVIRONMENT`). Add `COVERAGE_FILE: .coverage.integration` to the `env:` block. **KEEP `continue-on-error: true`** on the step (D-03).

    2. **ADD a new artifact upload step** immediately after the pytest step:
       ```yaml
       - name: Upload integration coverage data
         uses: actions/upload-artifact@v4
         if: always()   # always upload; combine handles missing/empty per D-03
         with:
           name: coverage-integration
           path: .coverage.integration
           if-no-files-found: warn   # integration may have produced no data (per D-03 acceptable)
           retention-days: 7
       ```
       `if-no-files-found: warn` (NOT `error`) is intentional — D-03 explicitly allows the integration job to crash without producing an artifact; combine job handles the absence (Pitfall 3).

    3. **Preserve** the existing `services:` block (Redis service definition) and any `needs: unit-tests` directive at job top.

    **Edit 3 — INSERT new `coverage-combine` job**

    Insert AFTER the entire `integration-tests:` job block and BEFORE the next existing job (e.g., `security-scan:` or `docker-build:`). Use this exact YAML, copied from 15-RESEARCH.md §"Code Examples — NEW coverage-combine job":

    ```yaml
      coverage-combine:
        name: Coverage Combine and Floor (TEST-04 + TEST-06 + TEST-03)
        runs-on: ubuntu-latest
        needs: [unit-tests, integration-tests]
        if: always()   # run even if integration-tests failed (D-03)
        steps:
          - uses: actions/checkout@v4
            with:
              fetch-depth: 0   # Pitfall 4: diff-cover needs full history to find merge-base with v1.0

          - name: Set up Python 3.11
            uses: actions/setup-python@v5
            with:
              python-version: "3.11"
              cache: pip

          - name: Install dependencies
            run: pip install -r requirements.txt -r requirements-dev.txt

          - name: Download unit coverage
            uses: actions/download-artifact@v4
            with:
              name: coverage-unit
              path: .

          - name: Download integration coverage
            uses: actions/download-artifact@v4
            with:
              name: coverage-integration
              path: .
            continue-on-error: true   # D-03: integration may have produced no artifact

          - name: List coverage data files (debug)
            run: ls -la .coverage* || true

          - name: Combine coverage data
            run: |
              # --keep preserves .coverage.unit / .coverage.integration in artifact for debugging (Pitfall 2).
              # Missing one input → stderr warning + exit 0 (correct per D-03).
              # ALL inputs missing → exit 1 (correct fail signal — combine job fails before report).
              coverage combine --keep .coverage.unit .coverage.integration

          - name: TEST-04 — Coverage report (combined, source of truth)
            run: coverage report

          - name: TEST-06 — Floor gate (combined coverage >= 70%)
            run: coverage report --fail-under=70   # exit 2 on miss → step fails (Pitfall 5)

          - name: Generate coverage.xml for diff-cover
            run: coverage xml -o coverage.xml

          - name: Fetch v1.0 tag for diff-cover baseline
            run: git fetch origin tag v1.0 --no-tags

          - name: TEST-03 — diff-cover gate (>= 80% on changed lines vs v1.0)
            run: |
              diff-cover coverage.xml \
                --compare-branch=v1.0 \
                --fail-under=80 \
                --format html:diff-cover.html

          - name: Upload combined coverage artifacts
            uses: actions/upload-artifact@v4
            if: always()   # always publish even if a gate failed — reviewers need the report
            with:
              name: coverage-report
              path: |
                .coverage
                .coverage.unit
                .coverage.integration
                coverage.xml
                diff-cover.html
              retention-days: 30
    ```

    Indentation: match the surrounding job blocks (top-level jobs are indented under `jobs:` with 2-space indent at the YAML root level — verify by inspecting the `unit-tests:` indent in the existing file).

    Do NOT:
    - Add `--cov-fail-under` to either the unit OR integration pytest call (D-06: floor enforced ONCE in combine job; AntiPattern from RESEARCH §"Anti-Patterns to Avoid")
    - Remove `continue-on-error: true` from `integration-tests` pytest step (D-03 hard rule)
    - Use `path: .coverage.unit*` (glob) for artifact upload — `parallel = false` guarantees exact filename; glob is messier and matches stale parallel-mode files (Pitfall 1)
    - Use `if-no-files-found: error` on the integration upload step — D-03 explicitly allows missing integration artifact
    - Combine in the `integration-tests` job (would couple combine to integration's `continue-on-error` semantics — wrong; combine is a separate gate)
    - Set `if-no-files-found: ignore` on the unit-tests upload — unit MUST produce data (no `continue-on-error` on unit job); use `error`
    - Add `--data-file=.coverage` to the combine command — coverage's default output is `.coverage`, redundant flag
    - Use `actions/download-artifact@v3` — v4 is the standard at the project (existing v4 elsewhere); v3 has different default merge-multiple semantics
    - Add a `if: success()` to the combine job — must be `if: always()` per D-03 (combine runs even if integration failed)
    - Skip `--keep` on combine — Pitfall 2 says combine deletes inputs by default; without `--keep`, `path: .coverage.unit` in the final upload step would silently drop those entries
    - Hard-code Python version to anything other than `"3.11"` (matches existing CI; would diverge from the rest of the workflow)
    - Add `--cov-report=html` anywhere — no HTML coverage report consumed in v1.3
    - Run `coverage combine` WITHOUT explicit filenames (just `coverage combine`) — would combine any stray `.coverage.*` file in the workspace; explicit filenames are deterministic
    - Set `--fail-under=70` on `coverage xml` step — `coverage xml` doesn't have that flag; floor check is the prior step
    - Move the `lint-and-type-check` job dependency on integration-tests (`needs: unit-tests`) — keep existing topology; combine is the new aggregation point
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/project_pytorch/project/rag_enterprise && python -c "
import yaml, pathlib, sys
src = pathlib.Path('.github/workflows/ci.yml').read_text()
doc = yaml.safe_load(src)
jobs = doc.get('jobs', {})

# 1. unit-tests job — no --cov-fail-under, has COVERAGE_FILE, no diff-cover step
ut = jobs['unit-tests']
ut_text = yaml.safe_dump(ut)
assert '--cov-fail-under' not in ut_text, 'unit-tests still has --cov-fail-under (D-06 violation)'
assert 'COVERAGE_FILE' in ut_text and '.coverage.unit' in ut_text, 'unit-tests missing COVERAGE_FILE=.coverage.unit'
assert 'diff-cover' not in ut_text, 'unit-tests still has diff-cover step (D-05 violation)'
ut_artifacts = [s for s in ut['steps'] if s.get('uses', '').startswith('actions/upload-artifact')]
assert any(s.get('with', {}).get('name') == 'coverage-unit' for s in ut_artifacts), 'unit-tests artifact name != coverage-unit'

# 2. integration-tests job — has --cov-append, COVERAGE_FILE=.coverage.integration, continue-on-error, upload step
it = jobs['integration-tests']
it_text = yaml.safe_dump(it)
assert '--cov=services' in it_text and '--cov=utils' in it_text, 'integration-tests missing --cov flags'
assert '--cov-append' in it_text, 'integration-tests missing --cov-append'
assert '--cov-report=' in it_text, 'integration-tests missing empty --cov-report= (Pitfall 6)'
assert 'COVERAGE_FILE' in it_text and '.coverage.integration' in it_text, 'integration-tests missing COVERAGE_FILE'
pytest_step = next(s for s in it['steps'] if s.get('name', '').lower().startswith('run integration'))
assert pytest_step.get('continue-on-error') is True, 'integration pytest step lost continue-on-error (D-03)'
it_artifacts = [s for s in it['steps'] if s.get('uses', '').startswith('actions/upload-artifact')]
assert any(s.get('with', {}).get('name') == 'coverage-integration' for s in it_artifacts), 'integration-tests missing coverage-integration upload'

# 3. coverage-combine job exists with required topology
assert 'coverage-combine' in jobs, 'coverage-combine job missing'
cc = jobs['coverage-combine']
assert set(cc['needs']) == {'unit-tests', 'integration-tests'}, cc['needs']
assert cc.get('if') == 'always()', cc.get('if')
cc_text = yaml.safe_dump(cc)
# Checkout with fetch-depth: 0 (Pitfall 4)
checkout = next(s for s in cc['steps'] if s.get('uses', '').startswith('actions/checkout'))
assert checkout['with']['fetch-depth'] == 0, 'combine job checkout missing fetch-depth: 0'
# Both downloads
download_names = [s.get('with', {}).get('name') for s in cc['steps'] if s.get('uses', '').startswith('actions/download-artifact')]
assert 'coverage-unit' in download_names and 'coverage-integration' in download_names, download_names
# Integration download has continue-on-error (D-03)
intg_dl = next(s for s in cc['steps'] if s.get('uses', '').startswith('actions/download-artifact') and s.get('with', {}).get('name') == 'coverage-integration')
assert intg_dl.get('continue-on-error') is True, 'combine: integration download missing continue-on-error'
# Combine command
assert 'coverage combine --keep .coverage.unit .coverage.integration' in cc_text, 'combine: --keep flag or filenames missing'
# Floor gate
assert 'coverage report --fail-under=70' in cc_text, 'combine: floor gate missing (TEST-06 AC#2)'
# diff-cover migration (D-05)
assert 'diff-cover coverage.xml' in cc_text and '--fail-under=80' in cc_text, 'combine: diff-cover migration incomplete'
assert 'git fetch origin tag v1.0' in cc_text, 'combine: missing v1.0 tag fetch'
# Final artifact
final_upload = [s for s in cc['steps'] if s.get('uses', '').startswith('actions/upload-artifact')][-1]
final_path = final_upload['with']['path']
for required in ['.coverage', '.coverage.unit', '.coverage.integration', 'coverage.xml', 'diff-cover.html']:
    assert required in final_path, f'combine: final artifact missing {required}'
assert final_upload['with']['name'] == 'coverage-report', final_upload['with']['name']

print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - YAML parses: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
    - Exactly one `coverage-combine:` job key: `grep -c '^  coverage-combine:' .github/workflows/ci.yml` returns exactly 1
    - `unit-tests` no longer enforces 46% floor: `grep -c -- '--cov-fail-under=46' .github/workflows/ci.yml` returns 0 (D-06)
    - `unit-tests` no longer enforces ANY --cov-fail-under: `grep -c -- '--cov-fail-under' .github/workflows/ci.yml` returns 0 (D-06; floor enforced in combine job only)
    - `unit-tests` sets COVERAGE_FILE=.coverage.unit: `grep -c 'COVERAGE_FILE:[[:space:]]*\.coverage\.unit' .github/workflows/ci.yml` returns ≥ 1 (D-10)
    - `integration-tests` sets COVERAGE_FILE=.coverage.integration: `grep -c 'COVERAGE_FILE:[[:space:]]*\.coverage\.integration' .github/workflows/ci.yml` returns ≥ 1 (D-10)
    - `integration-tests` retains continue-on-error: true (D-03): `awk '/integration-tests:/,/^  [a-z]/ {print}' .github/workflows/ci.yml | grep -c 'continue-on-error:[[:space:]]*true'` returns ≥ 1
    - `integration-tests` pytest call has --cov-append: `grep -c -- '--cov-append' .github/workflows/ci.yml` returns ≥ 1 (D-10)
    - Empty --cov-report= suppression (Pitfall 6): `grep -cE -- '--cov-report=$' .github/workflows/ci.yml` returns ≥ 2 (one in unit, one in integration)
    - Artifact `coverage-unit` exists: `grep -c 'name:[[:space:]]*coverage-unit' .github/workflows/ci.yml` returns ≥ 2 (1 upload in unit-tests, 1 download in combine)
    - Artifact `coverage-integration` exists: `grep -c 'name:[[:space:]]*coverage-integration' .github/workflows/ci.yml` returns ≥ 2 (1 upload, 1 download)
    - Artifact `coverage-report` exists: `grep -c 'name:[[:space:]]*coverage-report' .github/workflows/ci.yml` returns ≥ 1 (final combine upload — TEST-04 AC#4)
    - combine job has needs on both: `awk '/coverage-combine:/,/^  [a-z]/ {print}' .github/workflows/ci.yml | grep -c 'unit-tests\|integration-tests'` returns ≥ 2 in the combine job's needs/region
    - combine job has if: always() (D-03): `awk '/coverage-combine:/,/^  [a-z]/ {print}' .github/workflows/ci.yml | grep -c "if:[[:space:]]*always"` returns ≥ 1
    - combine job has fetch-depth: 0 (Pitfall 4): `awk '/coverage-combine:/,/^  [a-z]/ {print}' .github/workflows/ci.yml | grep -c 'fetch-depth:[[:space:]]*0'` returns ≥ 1
    - combine command has --keep (Pitfall 2): `grep -c 'coverage combine --keep' .github/workflows/ci.yml` returns ≥ 1
    - combine command has explicit filenames: `grep -c 'coverage combine --keep \.coverage\.unit \.coverage\.integration' .github/workflows/ci.yml` returns exactly 1
    - TEST-06 floor gate present: `grep -c 'coverage report --fail-under=70' .github/workflows/ci.yml` returns ≥ 1 (TEST-06 AC#2)
    - diff-cover migrated (D-05): combine job contains diff-cover, unit-tests does NOT: `awk '/^  unit-tests:/,/^  integration-tests:/ {print}' .github/workflows/ci.yml | grep -c 'diff-cover'` returns 0; `awk '/^  coverage-combine:/,/^  [a-z]/ {print}' .github/workflows/ci.yml | grep -c 'diff-cover coverage.xml'` returns ≥ 1
    - diff-cover gate threshold preserved (D-09): `grep -c -- '--fail-under=80' .github/workflows/ci.yml` returns exactly 1
    - v1.0 tag fetch in combine: `awk '/^  coverage-combine:/,/^  [a-z]/ {print}' .github/workflows/ci.yml | grep -c 'git fetch origin tag v1.0'` returns ≥ 1
    - Combined artifact path includes all 5 required files: `grep -A8 'name:[[:space:]]*coverage-report' .github/workflows/ci.yml | grep -c -E '(\.coverage|\.coverage\.unit|\.coverage\.integration|coverage\.xml|diff-cover\.html)'` returns ≥ 5 (TEST-04 AC#4)
    - No `parallel = true` reintroduced via env: `grep -c 'COVERAGE_PARALLEL' .github/workflows/ci.yml` returns 0
    - Per-comment-stripped grep gate (avoiding self-invalidating comment matches per planner hygiene): `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c -- '--cov-fail-under=46'` returns 0
  </acceptance_criteria>
  <done>
    `unit-tests` job: --cov-fail-under=46 removed, COVERAGE_FILE=.coverage.unit set, diff-cover step removed, artifact renamed to coverage-unit. `integration-tests` job: --cov-append + --cov-report= added, COVERAGE_FILE=.coverage.integration set, continue-on-error preserved, coverage-integration artifact uploaded. NEW `coverage-combine` job: needs both, runs always, downloads both artifacts, runs `coverage combine --keep`, enforces 70% floor, generates xml, fetches v1.0, runs diff-cover --fail-under=80, uploads coverage-report artifact with all 5 files. YAML parses; ci.yml structure intact (other jobs unchanged).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README.md §Coverage section (lines 241-273) and add Makefile coverage-combined target (D-12)</name>
  <files>README.md, Makefile</files>
  <read_first>
    - README.md lines 241-273 (current Coverage section to be replaced)
    - .planning/phases/15-coverage-combine-and-70-floor/15-RESEARCH.md §"Code Examples — README.md replacement copy" (canonical replacement text)
    - .planning/phases/15-coverage-combine-and-70-floor/15-RESEARCH.md §"Code Examples — Optional: make coverage-combined" (canonical Makefile target)
    - Makefile lines 1-15 (target list at top — confirm `coverage-diff` is in `.PHONY` / target-list line; add `coverage-combined` alongside)
    - Makefile lines 85-100 (existing `coverage-diff` target — KEEP UNCHANGED; new target appended after)
    - .planning/phases/10-coverage-gate-on-new-code/ (verify Phase 10 D-03 is the supersession target — referenced in D-12)
  </read_first>
  <behavior>
    - Test: README.md no longer contains the literal text "46.6%" or "46%" in the Coverage section (replaced with "70%" or higher)
    - Test: README.md contains the literal text "Phase 15 D-05" (or equivalent supersession citation referencing this plan's phase) inside the Coverage section
    - Test: README.md Coverage section documents the 3-job CI flow (unit-tests, integration-tests, coverage-combine) with the combined-report-as-source-of-truth note
    - Test: Makefile has a NEW `coverage-combined:` target alongside the existing `coverage-diff:` target (existing target body unchanged)
    - Test: `coverage-combined` target in Makefile runs `coverage erase` then unit pytest with `COVERAGE_FILE=.coverage.unit` then integration pytest with `COVERAGE_FILE=.coverage.integration --cov-append` then `coverage combine --keep` then `coverage report` then `coverage report --fail-under=70`
    - Test: Makefile `.PHONY` / first-line target list includes `coverage-combined` alongside `coverage-diff`
    - Test: `make help` (if a help target exists) lists the new target with a description
    - Test: `make -n coverage-combined` (dry-run) prints the planned commands without executing them
  </behavior>
  <action>
    **Edit 1 — README.md lines 241-273**

    Replace the existing Coverage section (lines 241-273) with the canonical text from 15-RESEARCH.md §"Code Examples — README.md replacement copy". Use the markdown rendering literally:

    ```markdown
    ## Coverage

    Current floor: **70%** (raised from 46% in v1.3 — Phase 15). CI enforces this on the **combined** unit + integration coverage report. The diff-coverage gate (>= 80% on changed lines vs the v1.0 baseline tag) also runs on the combined report.

    ### Combined Coverage in CI (TEST-04 + TEST-06)

    CI uses three jobs:
    1. `unit-tests` collects unit coverage → uploads `coverage-unit` artifact (`.coverage.unit`).
    2. `integration-tests` collects integration coverage with `--cov-append` → uploads `coverage-integration` artifact (`.coverage.integration`). Keeps `continue-on-error: true` so flaky infra doesn't block PRs.
    3. `coverage-combine` (new in v1.3) downloads both, runs `coverage combine`, then `coverage report --fail-under=70` (TEST-06) and `diff-cover coverage.xml --fail-under=80` (TEST-03) on the combined artifact.

    > **Supersession note:** Phase 10 decision D-03 ("only unit-test coverage counts") is superseded by Phase 15 D-05 — the combined coverage report is now the single source of truth for both the floor gate and the diff-cover gate. See `.planning/phases/15-coverage-combine-and-70-floor/15-CONTEXT.md`.

    ### Diff-Coverage Gate on PRs (TEST-03)

    Any file modified in a PR must ship with **>= 80% line coverage on the changed lines**, measured against the v1.0 baseline. Both unit AND integration test execution count toward this gate (since Phase 15).

    **How to run locally before pushing:**
    ```bash
    make coverage-diff           # unit-only diff-cover (existing target — fast, dev DX)
    make coverage-combined       # NEW: full unit+integration mirror of CI (slower)
    ```

    The `coverage-combined` target writes `diff-cover.html` and prints the per-module breakdown.

    **CI behaviour:** the `Coverage Combine and Floor` job runs both gates. A floor below 70% OR diff-coverage below 80% **blocks the merge** — there are no override comments and no soft-warn mode (decisions D-05, D-06, D-09 in `.planning/phases/15-coverage-combine-and-70-floor/15-CONTEXT.md`).

    **How to fix a failure:**
    - *Floor below 70%:* identify modules with low coverage (`coverage report --skip-covered=false | sort -k4 -n`), add unit tests in `tests/unit/test_<module>.py`.
    - *Diff-coverage below 80%:* add unit tests covering the changed lines in your PR.

    **Scope notes:**
    - Combined coverage (unit + integration) counts. Integration paths exercised end-to-end count toward both gates.
    - The `coverage-report` GitHub Actions artifact contains the final `.coverage` SQLite, the per-job inputs (`.coverage.unit`, `.coverage.integration` — preserved via `--keep`), `coverage.xml`, and `diff-cover.html` for debugging.
    ```

    Insert this as the replacement for lines 241-273. Preserve any heading hierarchy from the surrounding sections (the `## Coverage` H2 heading line MUST stay; replace ONLY the body content beneath it through the section's end). If the existing line 241 is exactly `## Coverage`, the replacement starts at line 242.

    **Edit 2 — Makefile**

    Apply 2 sub-edits:

    1. **EXTEND the target list at line 7** to include `coverage-combined`. The current line is approximately:
       ```makefile
       .PHONY: help build up down logs shell ingest eval clean coverage-diff
       ```
       (or equivalent — confirm by reading line 7 of the existing Makefile). Add `coverage-combined` to the end:
       ```makefile
       .PHONY: help build up down logs shell ingest eval clean coverage-diff coverage-combined
       ```

    2. **APPEND a new target** to the END of the Makefile (after the existing `coverage-diff:` target body). Use a tab character (NOT spaces) for command lines — Makefile syntax requires it:
       ```makefile

       coverage-combined:  ## Mirror CI: combined unit+integration coverage report (TEST-04 + TEST-06)
       	@echo ">> Erasing prior coverage data..."
       	conda run -n torch_env coverage erase
       	@echo ">> Running unit tests with coverage (separate data file)..."
       	COVERAGE_FILE=.coverage.unit conda run -n torch_env pytest tests/unit/ \
       		--asyncio-mode=auto --timeout=30 \
       		--cov=services --cov=utils --cov-report= -q
       	@echo ">> Running integration tests with --cov-append..."
       	COVERAGE_FILE=.coverage.integration conda run -n torch_env pytest tests/integration/ \
       		--asyncio-mode=auto --timeout=60 \
       		--cov=services --cov=utils --cov-append --cov-report= -q || true
       	@echo ">> Combining and reporting..."
       	conda run -n torch_env coverage combine --keep .coverage.unit .coverage.integration
       	conda run -n torch_env coverage report
       	conda run -n torch_env coverage report --fail-under=70
       ```

       The `|| true` after the integration pytest call mirrors CI's `continue-on-error: true` (D-03) so a developer can debug coverage even when integration tests are flaky locally. The final `coverage report --fail-under=70` is the same gate CI enforces.

    Do NOT:
    - Modify the body of the existing `coverage-diff:` target (line 88-100) — strict scope; it's unaffected by Phase 15
    - Replace `conda run -n torch_env` with bare commands — project convention uses the `torch_env` conda env consistently (matches existing target)
    - Use `pip` or `uv` invocations inside the Makefile target — `conda run` is the established pattern
    - Add a `branch = true` flag anywhere — branch coverage deferred per CONTEXT
    - Add a `--cov-report=html` flag — no HTML report consumed in v1.3
    - Hard-code `70` as a Make variable — keep inline to mirror CI YAML literally for grep-ability
    - Touch any other Makefile target (`build`, `up`, `down`, `logs`, etc. — strict scope)
    - Add a coverage badge image (e.g., `![Coverage](...)`) to README — D-11 explicitly defers badges to v1.4+
    - Reference Phase 10 D-03 without explicit "superseded by" wording — D-12 requires explicit supersession citation
    - Strip the existing `## Coverage` H2 heading from README — only replace the body
    - Move the Coverage section to a different location in README — keep at lines 241-273 (the H2 heading anchor stays)
    - Add references to `pytest-cov` 7.x or other unreleased versions — pin to actual installed `==6.0.0`
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/project_pytorch/project/rag_enterprise && python -c "
import pathlib, re

# README.md checks
readme = pathlib.Path('README.md').read_text()
# Old 46% reference removed (within Coverage section context)
assert '46.6%' not in readme, 'README still contains 46.6%'
# 70% present
assert '70%' in readme, 'README missing 70% reference'
# Supersession note present
assert 'Phase 15 D-05' in readme or 'Phase 10 decision D-03' in readme, 'README missing supersession citation'
# 3-job topology mentioned
for token in ['unit-tests', 'integration-tests', 'coverage-combine']:
    assert token in readme, f'README missing {token} reference'
# H2 Coverage heading preserved
assert '## Coverage' in readme, 'README missing ## Coverage H2'
# Both make targets cited
assert 'make coverage-diff' in readme and 'make coverage-combined' in readme, 'README missing make target references'

# Makefile checks
mf = pathlib.Path('Makefile').read_text()
# .PHONY extended
phony_lines = [l for l in mf.split('\n') if l.startswith('.PHONY')]
assert any('coverage-combined' in l for l in phony_lines), '.PHONY missing coverage-combined'
assert any('coverage-diff' in l for l in phony_lines), '.PHONY lost coverage-diff (should be preserved)'
# New target body present
assert 'coverage-combined:' in mf, 'Makefile missing coverage-combined target'
# Required pieces of the body
required = [
    'COVERAGE_FILE=.coverage.unit',
    'COVERAGE_FILE=.coverage.integration',
    '--cov-append',
    'coverage combine --keep',
    'coverage report --fail-under=70',
    'conda run -n torch_env',
]
for piece in required:
    assert piece in mf, f'Makefile coverage-combined missing: {piece}'
# Existing coverage-diff body unchanged (heuristic: still references diff-cover)
assert 'coverage-diff:' in mf, 'Makefile lost coverage-diff target'
assert 'diff-cover coverage.xml' in mf, 'Makefile coverage-diff body damaged'

print('OK')
"

# Make dry-run check (does not execute, just validates target syntax)
make -n coverage-combined > /dev/null 2>&1 && echo "make -n coverage-combined: OK" || echo "make -n coverage-combined: FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md no longer contains "46.6%" anywhere: `grep -c '46\.6%' README.md` returns 0
    - README.md no longer contains "46%" in the Coverage section context (allow it elsewhere only if pre-existing): `awk '/^## Coverage/,/^## [^C]/ {print}' README.md | grep -c '46%'` returns 0
    - README.md contains "70%" in Coverage section: `awk '/^## Coverage/,/^## [^C]/ {print}' README.md | grep -c '70%'` returns ≥ 2
    - Supersession citation present: `grep -c -E '(Phase 15 D-05|Phase 10 (decision )?D-03)' README.md` returns ≥ 1
    - 3-job topology documented: `awk '/^## Coverage/,/^## [^C]/ {print}' README.md | grep -cE '(unit-tests|integration-tests|coverage-combine)'` returns ≥ 3
    - H2 Coverage heading preserved: `grep -c '^## Coverage$' README.md` returns exactly 1
    - Make targets referenced: `grep -c 'make coverage-combined' README.md` returns ≥ 1; `grep -c 'make coverage-diff' README.md` returns ≥ 1
    - Makefile .PHONY extended: `grep -c '^\.PHONY:.*coverage-combined' Makefile` returns ≥ 1
    - Makefile coverage-diff preserved: `grep -c '^\.PHONY:.*coverage-diff' Makefile` returns ≥ 1; `grep -c '^coverage-diff:' Makefile` returns exactly 1
    - Makefile coverage-combined target present: `grep -c '^coverage-combined:' Makefile` returns exactly 1
    - Makefile coverage-combined contains required pieces (avoiding self-invalidating comment matches): `grep -v '^[[:space:]]*#' Makefile | grep -c 'COVERAGE_FILE=\.coverage\.unit'` returns ≥ 1; `grep -v '^[[:space:]]*#' Makefile | grep -c 'COVERAGE_FILE=\.coverage\.integration'` returns ≥ 1; `grep -v '^[[:space:]]*#' Makefile | grep -c 'coverage combine --keep'` returns ≥ 1; `grep -v '^[[:space:]]*#' Makefile | grep -c 'coverage report --fail-under=70'` returns ≥ 1
    - Makefile coverage-diff body unchanged (heuristic — still has diff-cover invocation): `grep -c 'diff-cover coverage\.xml' Makefile` returns ≥ 1
    - Makefile dry-run succeeds: `make -n coverage-combined` exits 0 (validates target syntax + dependencies)
    - No coverage badge added to README (D-11 reaffirmation): `grep -c -E '(shields\.io|codecov\.io)' README.md` returns 0
    - No Makefile tab-vs-space corruption: `awk 'BEGIN {p=0} /^coverage-combined:/ {p=1; next} /^[a-zA-Z]/ {p=0} p && /^[^\t]/ && /[^[:space:]]/ {print "BAD: " NR ": " $0; exit 1}' Makefile` exits 0 (every body line under coverage-combined begins with a tab)
  </acceptance_criteria>
  <done>
    README.md §Coverage section rewritten to document combined unit + integration coverage with 70% floor, 3-job CI topology, and explicit Phase 15 D-05 supersession of Phase 10 D-03. Makefile gains `coverage-combined` target that mirrors CI locally (erase → unit pytest with COVERAGE_FILE → integration pytest with --cov-append + COVERAGE_FILE → combine --keep → report → report --fail-under=70). Existing `coverage-diff` target unchanged. `.PHONY` extended. `make -n coverage-combined` parses cleanly. No coverage badge added (D-11).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer commit → CI workflow → coverage gate decision | CI is the gatekeeper; misconfigured floor allows untested code to merge |
| `.coverage.*` SQLite artifacts → cross-job download → combine job | Artifacts are scoped to the workflow run; GitHub-managed access |
| `git fetch origin tag v1.0` in combine job → diff-cover baseline | Trust boundary on the `v1.0` tag remaining stable / un-rewritten |
| pyproject.toml `[tool.coverage.report] fail_under = 70` → enforcement at CI | Config drift between local and CI is a risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | A PR author edits `pyproject.toml` to lower `fail_under` from 70 to a passing value, sneaking unmaintained coverage past the floor gate | mitigate | The combine-job step `coverage report --fail-under=70` hard-codes the threshold inline (acceptance criterion verified by `grep -c 'coverage report --fail-under=70' .github/workflows/ci.yml` returns ≥ 1). Even if pyproject.toml is tampered, CI enforces 70 literally. PR review on `pyproject.toml` changes catches the second vector |
| T-15-01-02 | Spoofing | Adversary uploads a malicious workflow run that produces `.coverage.unit` SQLite with fabricated 100% coverage data; combine job consumes it | accept | Workflow runs are scoped to the GitHub repo + branch; only authorized contributors trigger CI; Anthropic-style enterprise repos already restrict workflow_dispatch / pull_request_target. No new exposure beyond existing GitHub Actions trust model |
| T-15-01-03 | Tampering | Adversary force-pushes the `v1.0` tag to point at a commit with worse coverage, defeating the diff-cover comparison baseline | mitigate | GitHub branch protection rules can mark tags immutable; `v1.0` tag was created at v1.0 ship and is referenced in the existing CI for unit-tests already. This phase doesn't introduce new tag-trust surface; existing model preserved |
| T-15-01-04 | Denial of Service | A pathological PR with thousands of changed files makes diff-cover slow enough to time out the combine job (default 360-min ubuntu-latest timeout) | accept | diff-cover is fast (~seconds) on diffs of any realistic PR size; even 10K-file PRs complete in < 60s per published benchmarks. No timeout configured on combine job in this plan; if encountered, can add `timeout-minutes: 30` in a follow-up |
| T-15-01-05 | Information Disclosure | The `coverage-report` artifact persisted for 30 days exposes the project's `.coverage` SQLite to anyone with repo read access; .coverage may contain absolute filesystem paths from the CI runner | accept | Coverage SQLite path data is `services/...` and `utils/...` (relative to checkout root) — no absolute paths beyond `/home/runner/work/...` which is well-known public infrastructure. No secrets in coverage data; project is open-source-class. Retention 30 days matches existing artifact policy |
| T-15-01-06 | Repudiation | After a coverage drop, no audit trail records WHICH job's data was missing or empty — combine job runs `if: always()` and may pass with degenerate data | mitigate | Combine job's `List coverage data files (debug)` step (`ls -la .coverage*`) provides a per-run record in CI logs. Pitfall 3 documents combine semantics: missing one input → exit 0, all missing → exit 1. Reviewers can cross-reference combine-job logs with the `unit-tests` and `integration-tests` job statuses on the same workflow run |
| T-15-01-07 | Elevation of Privilege | The combine job runs `pip install -r requirements.txt -r requirements-dev.txt` — a malicious dependency in requirements.txt could exfiltrate the workflow `${{ secrets.GITHUB_TOKEN }}` | mitigate | Same risk as existing `unit-tests` and `integration-tests` jobs (which already run `pip install -r requirements.txt -r requirements-dev.txt`). No new dependency added by Phase 15 (transitive coverage 7.x already present via pytest-cov 6.0.0). Combine job inherits existing dependency-pinning posture; no new attack surface |
</threat_model>

<verification>
1. TOML parses + coverage.py auto-discovers config: `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0; `coverage debug config 2>&1 | grep -E 'fail_under|source|parallel'` shows `fail_under=70`, `source=['services','utils']`, `parallel=False`.
2. YAML parses: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
3. ci.yml has exactly 5 top-level jobs after Phase 15 (was 4): `python -c "import yaml; print(len(yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']))"` returns 5 (or matches expected count: lint-and-type-check, unit-tests, integration-tests, coverage-combine, security-scan, docker-build = 6 if all preserved).
4. Floor enforcement single-source-of-truth (D-06): `grep -c -- '--cov-fail-under' .github/workflows/ci.yml` returns 0; `grep -c 'coverage report --fail-under=70' .github/workflows/ci.yml` returns ≥ 1.
5. Diff-cover migrated (D-05): `awk '/^  unit-tests:/,/^  integration-tests:/ {print}' .github/workflows/ci.yml | grep -c 'diff-cover'` returns 0; `awk '/^  coverage-combine:/,/^  [a-z]/ {print}' .github/workflows/ci.yml | grep -c 'diff-cover'` returns ≥ 1.
6. Locally-verified combine on dummy data (sanity, not full repo): the combine command `coverage combine --keep .coverage.unit .coverage.integration` is syntactically correct via `coverage combine --help | grep -- '--keep'` returns ≥ 1.
7. Makefile target dry-runs cleanly: `make -n coverage-combined` exits 0.
8. README §Coverage rewritten: `awk '/^## Coverage/,/^## [^C]/ {print}' README.md | grep -c '70%'` returns ≥ 2; `grep -c -E '(Phase 15 D-05|Phase 10 (decision )?D-03)' README.md` returns ≥ 1.
9. No coverage badge added (D-11): `grep -c -E '(shields\.io|codecov\.io)' README.md` returns 0.
10. Existing CI jobs not affected: `git diff .github/workflows/ci.yml -- | grep -E '^[+-].*lint-and-type-check|security-scan|docker-build'` shows zero substantive changes (only context lines if any).
</verification>

<success_criteria>
- `pyproject.toml` has `[tool.coverage.run]` with `source = ["services", "utils"]`, `parallel = false`, `branch = false`; `[tool.coverage.report]` with `fail_under = 70`, `show_missing = true`, `precision = 1`; `[tool.coverage.xml]` with `output = "coverage.xml"`. (TEST-06 AC#1, AC#5; D-01, D-08 revised)
- `.github/workflows/ci.yml`: `unit-tests` job no longer enforces 46% floor, sets `COVERAGE_FILE=.coverage.unit`, uploads `coverage-unit` artifact, no longer runs diff-cover. (D-05, D-06, D-10)
- `.github/workflows/ci.yml`: `integration-tests` job adds `--cov=services --cov=utils --cov-append --cov-report=`, sets `COVERAGE_FILE=.coverage.integration`, retains `continue-on-error: true`, uploads `coverage-integration` artifact. (D-03, D-10)
- `.github/workflows/ci.yml`: NEW `coverage-combine` job exists with `needs: [unit-tests, integration-tests]` + `if: always()` + `fetch-depth: 0`, downloads both artifacts, runs `coverage combine --keep`, enforces `coverage report --fail-under=70` (TEST-06 AC#2), generates `coverage.xml`, fetches v1.0, runs `diff-cover --fail-under=80` (TEST-04 AC#5; D-05, D-09), uploads `coverage-report` artifact with 5 files (TEST-04 AC#3, AC#4). (D-02, D-03, D-05)
- `README.md` §Coverage section documents combined flow with 70% floor, cites Phase 15 D-05 supersession of Phase 10 D-03 (D-12), references both `make coverage-diff` and `make coverage-combined`. No coverage badge added (D-11).
- `Makefile` gains `coverage-combined` target mirroring CI locally; `coverage-diff` target unchanged.
- All YAML / TOML parses cleanly; `make -n coverage-combined` exits 0.
- Pre-existing `lint-and-type-check`, `security-scan`, `docker-build` jobs untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/15-coverage-combine-and-70-floor/15-01-SUMMARY.md` capturing:
- Exact line spans of the 3 new pyproject.toml `[tool.coverage.*]` blocks (cite line numbers post-edit).
- Confirmation that `parallel = false` is set (deviates from CONTEXT D-08 draft per RESEARCH Pitfall 1; verify decision is reflected in committed file).
- ci.yml diff summary: lines removed from `unit-tests` (count: --cov-fail-under flag, diff-cover step, fetch-v1.0 step if pre-existing only here), lines added to `integration-tests` (--cov flags + COVERAGE_FILE + new upload step), lines added for the NEW `coverage-combine` job (full block, ~70 lines).
- README.md line count delta (lines 241-273 replaced; new section may be longer or shorter — note the post-edit line range of the Coverage section).
- Makefile delta: `.PHONY` line edit + new target append (line range of new target).
- Confirmation that Wave 2 (15-02) can now run `coverage report --fail-under=70` against combined data and identify modules below the floor (D-04 measure-then-plan precondition).
- Any unexpected adjacent edits (must be zero — strictly bound to the 4 listed files).
- Result of dry-run: `make -n coverage-combined` exit code, plus any local sanity-test output (e.g., `python -c "import tomllib; ..."` and `python -c "import yaml; ..."` results).
- Confirmation that combine-job step ordering is correct: download → combine --keep → report → report --fail-under=70 → xml → fetch v1.0 → diff-cover → upload.
</output>
