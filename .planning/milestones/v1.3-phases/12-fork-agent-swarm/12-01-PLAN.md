---
phase: 12-fork-agent-swarm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/models.py
  - config/settings.py
autonomous: true
requirements: [AGENT-03]
must_haves:
  truths:
    - "GenerationRequest accepts swarm_mode=True without validation error"
    - "settings.max_swarm_agents is readable and defaults to 5"
    - "settings.max_swarm_turns_per_agent is readable and defaults to 5"
    - "Both settings are env-var-overridable (Pydantic BaseSettings pattern)"
  artifacts:
    - path: "utils/models.py"
      provides: "GenerationRequest.swarm_mode bool field, defaulted False"
      contains: "swarm_mode"
    - path: "config/settings.py"
      provides: "max_swarm_agents and max_swarm_turns_per_agent int fields with defaults"
      contains: "max_swarm_agents"
  key_links:
    - from: "GenerationRequest"
      to: "API layer routing"
      via: "swarm_mode bool gate"
      pattern: "req\\.swarm_mode"
    - from: "SwarmQueryPipeline class constants"
      to: "settings"
      via: "getattr(settings, 'max_swarm_agents', 5)"
      pattern: "settings\\.max_swarm_(agents|turns_per_agent)"
---

<objective>
Add the data-model and configuration foundations Phase 12 needs: a `swarm_mode` opt-in flag on `GenerationRequest` (D-04) and two env-var-backed integer caps in `settings` (D-09: `MAX_SWARM_AGENTS` default 5, `MAX_SWARM_TURNS_PER_AGENT` default 5). No pipeline logic yet — this plan only delivers the typed surface area later plans depend on.

Purpose: Wave 1 must finish before SwarmQueryPipeline is written so the implementation can reference both `req.swarm_mode` and `settings.max_swarm_*` directly without scaffolding workarounds.
Output: Two field additions in `utils/models.py`, two field additions in `config/settings.py`. Pydantic V2 strict-typed, OPS-01 compliant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fork-agent-swarm/12-CONTEXT.md
@.planning/phases/12-fork-agent-swarm/12-RESEARCH.md
@.planning/phases/12-fork-agent-swarm/12-PATTERNS.md

@utils/models.py
@config/settings.py

<interfaces>
<!-- Existing GenerationRequest shape (utils/models.py lines 205-223). -->
<!-- The swarm_mode field is added immediately after agent_mode (line 214). -->

```python
class GenerationRequest(BaseModel):
    query:        str                           = Field(..., min_length=1, max_length=2000)
    top_k:        int                           = Field(default=6, ge=1, le=20)
    filters:      dict[str, Any] | None         = None
    session_id:   str                           = Field(default_factory=lambda: uuid.uuid4().hex[:16])
    chat_history: list[dict[str, str]]          = Field(default_factory=list)
    temperature:  float                         = Field(default=0.1, ge=0.0, le=2.0)
    stream:       bool                          = False
    agent_mode:   bool                          = False   # True 时使用 Agentic 工具循环
    include_images: bool                        = False
    tenant_id:    str                           = ""
    user_id:      str                           = ""
```

<!-- Existing settings field shape (config/settings.py around line 281). -->
<!-- New ints sit alongside llm_max_tokens / llm_context_window. -->

```python
llm_temperature:    float = 0.1
llm_max_tokens:     int   = 2048
llm_context_window: int   = 8192
llm_stream:         bool  = True
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add swarm_mode field to GenerationRequest</name>
  <files>utils/models.py</files>
  <read_first>
    - utils/models.py (the file being modified — confirm current `GenerationRequest` definition at lines 205–223 and the `agent_mode` line at 214 to copy alignment style exactly)
    - .planning/phases/12-fork-agent-swarm/12-PATTERNS.md (lines 199–212 — the exact alignment template for `swarm_mode`)
    - .planning/phases/12-fork-agent-swarm/12-CONTEXT.md (D-04 — locked decision wording)
  </read_first>
  <behavior>
    - Test: `GenerationRequest(query="q", swarm_mode=True)` constructs without error and `.swarm_mode is True`
    - Test: `GenerationRequest(query="q")` defaults `.swarm_mode is False`
    - Test: `GenerationRequest(query="q", swarm_mode="not-a-bool")` either coerces (Pydantic V2 default) or raises `ValidationError` consistent with sibling `agent_mode` behavior — match whichever the existing field exhibits
  </behavior>
  <action>
    Modify `utils/models.py`. Implement D-04: add a new boolean field `swarm_mode: bool = False` to the `GenerationRequest` Pydantic model.

    Concrete edit (insert AFTER the existing `agent_mode` line at line 214, BEFORE the `include_images` line at line 215):

    ```python
        swarm_mode:   bool                          = False   # True 时使用 Fork-Agent Swarm（AGENT-03）
    ```

    Match the column alignment of adjacent fields exactly (the `bool` token aligns under the same column as `agent_mode`'s `bool`, the `=` aligns under the same column as `agent_mode`'s `=`, the trailing comment uses the same `#` style and Chinese annotation pattern).

    Do NOT:
    - Add a `@field_validator` for `swarm_mode` (plain bool — no validator needed; matches `agent_mode`)
    - Modify `agent_mode` or any other field
    - Touch the `strip_query` validator
    - Reorder existing fields
    - Add new imports (no new types referenced)
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/project_pytorch/project/rag_enterprise && python -c "from utils.models import GenerationRequest; r = GenerationRequest(query='x', swarm_mode=True); assert r.swarm_mode is True; r2 = GenerationRequest(query='y'); assert r2.swarm_mode is False; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^[[:space:]]*swarm_mode:[[:space:]]*bool[[:space:]]*=[[:space:]]*False' utils/models.py` returns ≥ 1
    - `grep -n 'agent_mode\|swarm_mode' utils/models.py` shows `agent_mode` immediately followed by `swarm_mode` (line numbers consecutive or differing by 1)
    - `python -c "from utils.models import GenerationRequest; assert 'swarm_mode' in GenerationRequest.model_fields"` exits 0
    - `python -c "from utils.models import GenerationRequest; assert GenerationRequest.model_fields['swarm_mode'].default is False"` exits 0
    - `python -c "from utils.models import GenerationRequest; r=GenerationRequest(query='x', swarm_mode=True); assert r.swarm_mode is True"` exits 0
    - mypy --strict on utils/models.py passes (no new errors introduced): `mypy --strict utils/models.py` exit code 0
  </acceptance_criteria>
  <done>
    `swarm_mode: bool = False` field exists on `GenerationRequest`, defaulted False, immediately following `agent_mode`. No other fields modified. mypy strict clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add max_swarm_agents and max_swarm_turns_per_agent to Settings</name>
  <files>config/settings.py</files>
  <read_first>
    - config/settings.py (the file being modified — read the entire `Settings` class to find the most appropriate location: near `llm_max_tokens` at line 281 OR in a dedicated agent section if one exists)
    - .planning/phases/12-fork-agent-swarm/12-CONTEXT.md (D-09 — locked names and defaults)
    - .planning/phases/12-fork-agent-swarm/12-RESEARCH.md (Standard Stack table — env-var pattern reference)
  </read_first>
  <behavior>
    - Test: `from config.settings import settings; assert settings.max_swarm_agents == 5` (default)
    - Test: `from config.settings import settings; assert settings.max_swarm_turns_per_agent == 5` (default)
    - Test: setting env var `MAX_SWARM_AGENTS=3` before re-importing causes `settings.max_swarm_agents == 3` (Pydantic BaseSettings env-var binding works automatically — no extra Config required if existing settings already read env vars)
  </behavior>
  <action>
    Modify `config/settings.py`. Implement D-09: add two int fields to the `Settings` class, both env-var-backed via Pydantic BaseSettings (the same mechanism that already binds `llm_max_tokens` to `LLM_MAX_TOKENS`).

    Concrete edit (insert AFTER the existing `llm_stream:         bool  = True` line at line 283, in a clearly commented sub-section):

    ```python

        # ══════════════════════════════════════════════════════════════════════════
        # Swarm（Fork-Agent — AGENT-03）
        # ══════════════════════════════════════════════════════════════════════════
        max_swarm_agents:           int   = 5    # 协调器分解后最多并发的子代理数（D-09）
        max_swarm_turns_per_agent:  int   = 5    # 每个子代理 call_agentic_turn 循环上限（D-09）
    ```

    Field naming MUST be exactly `max_swarm_agents` and `max_swarm_turns_per_agent` (snake_case Python attribute names; Pydantic BaseSettings auto-binds env vars `MAX_SWARM_AGENTS` and `MAX_SWARM_TURNS_PER_AGENT` via the project's existing case-insensitive env loader — no manual `env=` annotation is required if `llm_max_tokens` lacks one).

    Match column alignment of the surrounding `llm_*` fields (the `int` token under `int`, `=` under `=`).

    Do NOT:
    - Touch `llm_max_tokens`, `llm_context_window`, or any unrelated field
    - Add a new `model_config` / `Config` class
    - Add a `@field_validator` (plain int with default — no validation needed beyond Pydantic's default)
    - Cross-reference `MAX_ITERATIONS` from `AgentQueryPipeline` — that constant stays in pipeline.py
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/project_pytorch/project/rag_enterprise && python -c "from config.settings import settings; assert settings.max_swarm_agents == 5; assert settings.max_swarm_turns_per_agent == 5; assert isinstance(settings.max_swarm_agents, int); assert isinstance(settings.max_swarm_turns_per_agent, int); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^[[:space:]]*max_swarm_agents:[[:space:]]*int[[:space:]]*=[[:space:]]*5' config/settings.py` returns ≥ 1
    - `grep -c '^[[:space:]]*max_swarm_turns_per_agent:[[:space:]]*int[[:space:]]*=[[:space:]]*5' config/settings.py` returns ≥ 1
    - `python -c "from config.settings import settings; assert settings.max_swarm_agents == 5 and settings.max_swarm_turns_per_agent == 5"` exits 0
    - `python -c "from config.settings import Settings; assert 'max_swarm_agents' in Settings.model_fields and 'max_swarm_turns_per_agent' in Settings.model_fields"` exits 0
    - Env override works: `MAX_SWARM_AGENTS=3 python -c "from config.settings import Settings; s=Settings(); assert s.max_swarm_agents == 3"` exits 0
    - mypy --strict passes: `mypy --strict config/settings.py` exit code 0
  </acceptance_criteria>
  <done>
    Two int fields (`max_swarm_agents=5`, `max_swarm_turns_per_agent=5`) added to `Settings` in a labelled "Swarm" sub-section. Defaults match D-09. Env-var override verified. No other fields touched. mypy strict clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env vars → Settings (`max_swarm_agents`, `max_swarm_turns_per_agent`) | Operator-controlled (deployment/secret manager); not user-facing |
| HTTP body → `GenerationRequest.swarm_mode` | Untrusted input crosses here (tenant API caller chooses true/false) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | `swarm_mode` field abused to force expensive swarm path on every request | mitigate | Pydantic V2 typed bool field — only `True`/`False`/coercible values accepted; rate limit (`rate_limit_query_rpm`) applies at controller; `MAX_SWARM_AGENTS` cap (Plan 02) bounds blast radius even if `swarm_mode=True` always |
| T-12-01-02 | Denial of Service | Operator misconfiguration sets `MAX_SWARM_AGENTS` extremely high | accept | Operator-controlled env var; ASVS L1 V14.1.1 — rely on deployment hygiene; default of 5 is a safe ceiling; cap usage is enforced at runtime in Plan 02 (`sub_questions[: self.MAX_SWARM_AGENTS]`) |
| T-12-01-03 | Information Disclosure | `swarm_mode` field leaks via API schema | accept | Public API schema already exposes `agent_mode`; `swarm_mode` follows the same exposure model — no new sensitive surface |
| T-12-01-04 | Input Validation | Negative or zero `max_swarm_agents` env var | accept | Defaults are positive; if operator sets 0, swarm degenerates to N=0 → empty list → graceful handling required in Plan 02 (synthesis fallback). No validator added here to keep this plan minimal; downstream plan handles edge case |
</threat_model>

<verification>
1. `pytest tests/unit/test_models.py -x` (existing tests still pass — no regression)
2. `python -c "from utils.models import GenerationRequest; print(GenerationRequest.model_fields['swarm_mode'])"`
3. `python -c "from config.settings import settings; print(settings.max_swarm_agents, settings.max_swarm_turns_per_agent)"`
4. `mypy --strict utils/models.py config/settings.py`
5. `ruff check utils/models.py config/settings.py`
</verification>

<success_criteria>
- `GenerationRequest.swarm_mode: bool = False` declared, accepts True/False at construction
- `Settings.max_swarm_agents: int = 5` and `Settings.max_swarm_turns_per_agent: int = 5` declared, env-var override functional
- mypy --strict + ruff pass with zero new diagnostics
- No regression in existing test suite for `utils/models.py` or `config/settings.py`
</success_criteria>

<output>
After completion, create `.planning/phases/12-fork-agent-swarm/12-01-SUMMARY.md` capturing:
- Exact line numbers of the two new field additions
- Confirmation that env-var override works (or note if Pydantic BaseSettings prefix differs)
- Any unexpected adjacent-line changes (should be zero)
</output>
