---
phase: 04-image-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/models.py
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-04

must_haves:
  truths:
    - "ExtractedContent carries a typed list of ExtractedImage objects out of Stage 2"
    - "ChunkMetadata has a chunk_type discriminator field distinguishing text from image chunks"
    - "DocType.IMAGE exists for standalone image file ingestion"
    - "IngestionResponse carries extraction_errors so callers see skipped images"
  artifacts:
    - path: "utils/models.py"
      provides: "ExtractedImage model, images field on ExtractedContent, chunk_type on ChunkMetadata, DocType.IMAGE"
      contains: "class ExtractedImage"
  key_links:
    - from: "utils/models.py ExtractedImage"
      to: "services/extractor/image_extractor.py"
      via: "import"
      pattern: "from utils\\.models import.*ExtractedImage"
    - from: "utils/models.py ChunkMetadata.chunk_type"
      to: "services/doc_processor/chunker.py"
      via: "DocumentChunk.metadata.chunk_type"
      pattern: "chunk_type.*=.*['\"]image['\"]"
---

<objective>
Extend the shared Pydantic V2 data models that form the handoff contracts between every pipeline stage.

Purpose: Every downstream task (image extractor, chunker, pipeline orchestrator) depends on these type contracts. Defining them first eliminates forward-reference ambiguity and ensures mypy --strict passes before any implementation begins.

Output: `utils/models.py` updated with `ExtractedImage`, `images` list on `ExtractedContent`, `chunk_type` field on `ChunkMetadata`, and `DocType.IMAGE`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@utils/models.py

<interfaces>
<!-- Current models.py key facts the executor must know before editing. -->

DocType (line 19): str Enum with PDF, DOCX, XLSX, CSV, HTML, JSON, TXT, MD, UNKNOWN.
  → Add: IMAGE = "image"

ExtractedContent (line 73): Pydantic V2 BaseModel with fields:
  raw_id, doc_type, title, author, created_date, pages, body_text, tables,
  images_count: int = 0, language, metadata, extraction_engine, extraction_errors: list[str]
  → Add: images: list[ExtractedImage] = Field(default_factory=list)
  → images_count remains; it will be set to len(images) in model_post_init.

ChunkMetadata (line 109): Pydantic V2 BaseModel with fields:
  source, doc_id, title, author, section, sub_section, page_number,
  chunk_index, total_chunks, doc_type, language, tags, parent_chunk_id,
  chunk_level, node_type
  → Add: chunk_type: str = "text"   # "text" | "image"
  → Add: image_b64: str = ""        # base64-encoded image bytes (only populated for chunk_type="image")

IngestionResponse (line 230): already has doc_id, total_chunks, success, elapsed_ms, error.
  → Add: extraction_errors: list[str] = Field(default_factory=list)
    (callers use this to surface skipped images per D-04)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ExtractedImage model and update ExtractedContent, ChunkMetadata, IngestionResponse</name>
  <files>utils/models.py</files>
  <read_first>
    utils/models.py — read current state before editing; every class and import must be preserved
  </read_first>
  <behavior>
    - ExtractedImage(raw_bytes: bytes, width: int, height: int, page_number: int, image_index: int, ext: str = "png") — Pydantic V2 BaseModel
    - ExtractedContent gets images: list[ExtractedImage] = Field(default_factory=list); model_post_init sets images_count = len(self.images) when images is non-empty and images_count is still 0
    - ChunkMetadata gets chunk_type: str = "text" and image_b64: str = ""
    - IngestionResponse gets extraction_errors: list[str] = Field(default_factory=list)
    - DocType gets IMAGE = "image"
    - All existing fields remain unchanged
  </behavior>
  <action>
Read utils/models.py first. Then make these ADDITIVE changes only — do not alter any existing field:

1. In DocType enum, add after MD = "md":
   IMAGE = "image"

2. After the existing imports block, add a new model in the STAGE 2 section (after ExtractedContent):

   class ExtractedImage(BaseModel):
       """An image extracted from a PDF page or standalone image file."""
       raw_bytes:   bytes      = Field(..., description="Raw image bytes before base64 encoding")
       width:       int        = 0
       height:      int        = 0
       page_number: int        = 0    # 0 for standalone image files
       image_index: int        = 0    # position within the page (0-based)
       ext:         str        = "png"

3. In ExtractedContent, add after the existing images_count field:
   images: list["ExtractedImage"] = Field(default_factory=list)

   Add model_post_init (if not already present):
   def model_post_init(self, __context: Any) -> None:
       if self.images and self.images_count == 0:
           self.images_count = len(self.images)

4. In ChunkMetadata, add two fields after the existing node_type field:
   chunk_type:  str = "text"   # "text" | "image"
   image_b64:   str = ""       # base64 str; non-empty only for chunk_type="image" chunks

5. In IngestionResponse, add after the existing error field:
   extraction_errors: list[str] = Field(default_factory=list)

IMPORTANT: ExtractedImage must be defined BEFORE ExtractedContent in the file so the forward reference resolves. Place it immediately before ExtractedContent class definition. Update the `list["ExtractedImage"]` annotation to `list[ExtractedImage]` (no quotes) since the class precedes the reference.

Verify mypy --strict compatibility: raw_bytes: bytes is correct (Pydantic V2 accepts bytes natively). No `Any` used in new fields.
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/project_pytorch/project/rag_enterprise && python -c "from utils.models import ExtractedImage, ExtractedContent, ChunkMetadata, IngestionResponse, DocType; img = ExtractedImage(raw_bytes=b'x', width=100, height=100, page_number=1, image_index=0); ec = ExtractedContent(raw_id='test', images=[img]); assert ec.images_count == 1; cm = ChunkMetadata(); assert cm.chunk_type == 'text'; assert cm.image_b64 == ''; ir = IngestionResponse(doc_id='d'); assert ir.extraction_errors == []; assert DocType.IMAGE == 'image'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ExtractedImage" utils/models.py` returns a line before ExtractedContent
    - `grep -n "IMAGE = " utils/models.py` returns a line inside the DocType enum
    - `grep -n "images:" utils/models.py` returns a field on ExtractedContent with list[ExtractedImage]
    - `grep -n "chunk_type" utils/models.py` returns a field on ChunkMetadata with default "text"
    - `grep -n "image_b64" utils/models.py` returns a field on ChunkMetadata with default ""
    - `grep -n "extraction_errors" utils/models.py` returns a field on IngestionResponse
    - Python import verification command above prints "OK" with exit code 0
    - `python -m mypy utils/models.py --strict --ignore-missing-imports` exits 0 or only emits pre-existing errors (no new type errors introduced by this task)
  </acceptance_criteria>
  <done>All new model fields exist, import correctly, pass the inline assertion test, and introduce no mypy --strict errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models.py → all pipeline stages | Shared contract; malformed types here cascade to every consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | ExtractedImage.raw_bytes | mitigate | Pydantic V2 validates bytes type; size cap enforced in image_extractor.py (Plan 02), not here |
| T-04-01-02 | Information Disclosure | ChunkMetadata.image_b64 | accept | base64 field is opaque bytes; no PII beyond what was already in the source PDF; existing PII detection runs before image extraction |
| T-04-01-03 | Denial of Service | ExtractedContent.images list | mitigate | max 50 images per doc enforced in extractor (D-02, Plan 02); model itself imposes no list cap by design |
</threat_model>

<verification>
```bash
cd /home/ubuntu/workspace/project_pytorch/project/rag_enterprise
python -c "
from utils.models import ExtractedImage, ExtractedContent, ChunkMetadata, IngestionResponse, DocType
# Verify ExtractedImage
img = ExtractedImage(raw_bytes=b'abc', width=200, height=150, page_number=2, image_index=0)
assert img.ext == 'png'
# Verify ExtractedContent auto-count
ec = ExtractedContent(raw_id='r1', images=[img])
assert ec.images_count == 1
assert ec.images[0].page_number == 2
# Verify ChunkMetadata defaults
cm = ChunkMetadata()
assert cm.chunk_type == 'text'
assert cm.image_b64 == ''
# Verify IngestionResponse
ir = IngestionResponse(doc_id='d1')
assert ir.extraction_errors == []
# Verify DocType
assert DocType.IMAGE == 'image'
print('ALL CHECKS PASSED')
"
```
</verification>

<success_criteria>
- `ExtractedImage` model exists in utils/models.py with raw_bytes, width, height, page_number, image_index, ext fields
- `ExtractedContent.images` field is `list[ExtractedImage]` with empty-list default; `images_count` auto-set in post_init
- `ChunkMetadata.chunk_type` field defaults to `"text"`; `ChunkMetadata.image_b64` field defaults to `""`
- `IngestionResponse.extraction_errors` field is `list[str]` with empty-list default
- `DocType.IMAGE = "image"` member exists
- No existing field altered; zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-image-extraction/04-01-SUMMARY.md` following the summary template.
</output>
