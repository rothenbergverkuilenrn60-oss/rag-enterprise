---
phase: 03-error-handling-sweep
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - services/pipeline.py
  - services/retriever/retriever.py
  - services/auth/oidc_auth.py
  - services/vectorizer/indexer.py
autonomous: true
requirements:
  - ERR-01
requirements_addressed:
  - ERR-01
tags:
  - error-handling
  - exception-narrowing
  - external-dependencies
must_haves:
  truths:
    - "No `except Exception` or bare `except:` remains in services/pipeline.py, services/retriever/retriever.py, services/auth/oidc_auth.py, or services/vectorizer/indexer.py"
    - "asyncpg call sites catch asyncpg.PostgresError (per D-01)"
    - "OpenAI call sites catch openai.APIError, httpx call sites catch httpx.HTTPError, Redis call sites catch redis.RedisError (per D-02)"
    - "JWT verification sites catch python_jose.JWTError (per D-02)"
    - "In-request pipeline errors return IngestionResponse/QueryResponse(success=False, error=...) and do NOT re-raise (per D-05)"
    - "Every caught exception is logged via logger.error (no silent swallows) (per D-11)"
  artifacts:
    - path: "services/pipeline.py"
      provides: "Narrowed in-request pipeline exception handling with structured error responses"
      contains: "except asyncpg.PostgresError"
    - path: "services/retriever/retriever.py"
      provides: "Narrowed asyncpg exception handling"
      contains: "except asyncpg.PostgresError"
    - path: "services/auth/oidc_auth.py"
      provides: "Narrowed JWT exception handling"
      contains: "JWTError"
    - path: "services/vectorizer/indexer.py"
      provides: "Narrowed vector upsert exception handling"
      contains: "except asyncpg.PostgresError"
  key_links:
    - from: "services/pipeline.py"
      to: "logger.error"
      via: "structured error response after specific exception catch"
      pattern: "except \\w+Error.*\\n.*logger\\.error"
    - from: "services/auth/oidc_auth.py"
      to: "HTTP 401 response"
      via: "JWTError caught, structured 401 returned (never raw exception message to client)"
      pattern: "except.*JWTError"
---

<objective>
Narrow all `except Exception` (and any bare `except:`) sites in four external-dependency-heavy files to library-specific exception types per CONTEXT.md D-01 through D-05. Pipeline in-request errors must return structured error responses (never re-raise); auth JWT errors must surface as 401 with generic messages (never leak exception text).

Purpose: Close ERR-01 for the four highest-risk files — these sites touch asyncpg, Redis, OpenAI, httpx, and python-jose and must distinguish library errors from genuinely unexpected faults. Broad catches here mask DB/auth/external failures behind generic logs.
Output: Four source files with narrowed exception handling, each exception logged, structured error responses returned from pipeline paths, 401 returned from auth paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-error-handling-sweep/03-CONTEXT.md

@services/pipeline.py
@services/retriever/retriever.py
@services/auth/oidc_auth.py
@services/vectorizer/indexer.py

<interfaces>
<!-- Library exception hierarchies the executor must import and use. Do NOT invent new types. -->
<!-- If any library is not currently imported in a file, add the import with the catch narrowing. -->

Exception types per decision (CONTEXT.md D-01, D-02):
```python
# asyncpg (all DB call sites — query, execute, fetch, copy, transaction):
import asyncpg
except asyncpg.PostgresError as exc:  # base class covers connection, query, constraint errors

# Redis (aioredis/redis.asyncio):
import redis
except redis.RedisError as exc:  # base class covers connection, timeout, command errors
# NOTE: if code uses `redis.asyncio` import path, the base exception is still `redis.RedisError`

# OpenAI (async client):
import openai
except openai.APIError as exc:  # base class covers rate limit, auth, connection, 5xx

# httpx:
import httpx
except httpx.HTTPError as exc:  # base class covers timeout, connection, HTTP status errors

# python-jose (JWT verification):
from jose import JWTError  # package is python-jose, module is `jose`, exception is JWTError
except JWTError as exc:  # base class covers ExpiredSignatureError, JWTClaimsError, etc.
```

Structured error response types (from services/pipeline.py — use existing, do NOT redefine):
```python
# Already exist in services/pipeline.py — use as-is:
IngestionResponse(success=False, error="<generic message>", ...)
QueryResponse(success=False, error="<generic message>", ...)
```

Per D-05 (in-request pipeline errors):
- Catch specific exception
- Log with logger.error including exc_info=exc
- Return IngestionResponse(success=False, error=<generic_message>) or QueryResponse(success=False, error=<generic_message>)
- DO NOT re-raise
- DO NOT pass exception text to the response's `error` field — use a generic, user-safe string

Per D-11 (infrastructure errors → logger only, NEVER audit trail):
- Do NOT call audit.log_ingest / audit.log_query for DB/Redis/OpenAI/httpx/JWT failures
- Those are reserved for business security events (PII blocks, authz failures, tenant isolation violations)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow exception catches in services/pipeline.py (IngestionPipeline + QueryPipeline stages)</name>
  <files>services/pipeline.py</files>
  <read_first>
    - services/pipeline.py (ALL `except Exception` sites — use `grep -n 'except Exception' services/pipeline.py` and read each ±5 lines of surrounding context to determine which library is in play)
    - asyncpg exception hierarchy: https://magicstack.github.io/asyncpg/current/api/index.html#exceptions — confirm `asyncpg.PostgresError` is the correct base class
    - openai Python SDK exceptions: `openai.APIError` is the v1+ base exception class
    - httpx exceptions: `httpx.HTTPError` is the correct base class for transport + HTTP status errors
    - services/pipeline.py — locate `IngestionResponse` and `QueryResponse` dataclass/model definitions at top of file; confirm `success: bool` and `error: str | None` fields exist
  </read_first>
  <action>
    Replace every `except Exception as e:` (or `as exc:`) site in `services/pipeline.py` with a library-specific catch per D-01, D-02, D-05, D-11.

    **Procedure (per site):**

    1. For each `except Exception` site, identify which external library the `try:` block is calling into:
       - Direct asyncpg query (`.execute`, `.fetch`, `.fetchrow`, `.fetchval`, `.copy_records_to_table`) → `except asyncpg.PostgresError as exc:`
       - Vector store call that wraps asyncpg (`vector_store.upsert_parent_chunks`, `vector_store.fetch_parent_chunks`, `vector_store.search`) → `except asyncpg.PostgresError as exc:` (the underlying exception propagates)
       - OpenAI embedding/completion (`client.embeddings.create`, `client.chat.completions.create`) → `except openai.APIError as exc:`
       - httpx call (`httpx.AsyncClient.post`, `httpx.AsyncClient.get`) → `except httpx.HTTPError as exc:`
       - Redis call (`redis_client.get`, `.set`, `.delete`, `.hset`) → `except redis.RedisError as exc:`
       - Mixed block (block calls MORE THAN ONE external library) → split into sequential `except` clauses, one per library:
         ```python
         except asyncpg.PostgresError as exc:
             logger.error("pipeline stage DB failure", stage=<stage>, exc_info=exc)
             return IngestionResponse(success=False, error="Storage backend unavailable")
         except openai.APIError as exc:
             logger.error("pipeline stage embedding failure", stage=<stage>, exc_info=exc)
             return IngestionResponse(success=False, error="Embedding service unavailable")
         ```
       - Pure Python logic failure (no external library in the try block — e.g., data transformation, validation) → narrow to the most specific applicable type: `ValueError`, `KeyError`, `TypeError`, `RuntimeError`. Per D-03, NO site may remain as `except Exception`.

    2. Replace the log line with `logger.error("<descriptive message including stage name>", <context_fields>, exc_info=exc)`.
       - Use keyword args for context: `stage="retrieval"`, `tenant_id=tenant_id`, `doc_id=doc_id` etc. where the variable is in scope.
       - NEVER include the raw `str(exc)` in the log message — use `exc_info=exc` so the structured logger attaches the traceback in its own field.

    3. Replace `raise` / `return None` / silent swallow with a structured response per D-05:
       - IngestionPipeline sites: `return IngestionResponse(success=False, error="<generic message>", ...<existing required fields populated with safe defaults>)`
       - QueryPipeline sites: `return QueryResponse(success=False, error="<generic message>", ...<existing required fields populated with safe defaults>)`
       - Generic error messages (user-safe, no internal detail):
         * DB errors → `"Storage backend temporarily unavailable"`
         * OpenAI errors → `"Embedding service temporarily unavailable"`
         * httpx errors → `"Upstream service request failed"`
         * Redis errors → `"Cache backend temporarily unavailable"`
         * Generic fallback → `"Request could not be completed"`

    4. Add any missing imports at the top of the file:
       ```python
       import asyncpg
       import openai
       import httpx
       import redis
       ```
       Only add imports for libraries actually caught in this file. Do not speculatively import.

    5. **D-04 / D-06 EXEMPTIONS — DO NOT TOUCH:**
       - If you encounter a `try/except Exception as exc: logger.warning(f"... (non-fatal): {exc}")` pattern related to startup/warmup (e.g., `VectorStore.ensure_collection`), KEEP log+continue semantics. Narrow the exception type per D-03, but keep `logger.warning` (NOT `logger.error`) and do NOT add a structured error return. These are non-fatal.
       - If you encounter a `try: ... except Exception: pass` inside a shutdown/lifespan flush path (should not be in this file, but check), leave it entirely unchanged per D-06.

    6. **Forbidden:**
       - Do NOT call `audit.log_ingest` or `audit.log_query` from any narrowed site in this file. These are infrastructure errors per D-11 and must stay in `logger.error` only. Existing audit calls for business events (PII blocks, authz failures) stay as-is — do not remove them.
       - Do NOT re-raise from a narrowed pipeline stage catch (per D-05 — structured response instead).
       - Do NOT include raw `str(exc)` or `repr(exc)` in the `error` field of the structured response.
  </action>
  <verify>
    <automated>[ "$(grep -c 'except Exception' services/pipeline.py)" = "0" ] && [ "$(grep -cE '^\s*except\s*:' services/pipeline.py)" = "0" ] && python -c "import ast; ast.parse(open('services/pipeline.py').read())" && python -c "from services import pipeline"</automated>
  </verify>
  <acceptance_criteria>
    - Zero broad catches remain: `grep -c 'except Exception' services/pipeline.py` returns 0
    - Zero bare except clauses: `grep -cE '^\s*except\s*:' services/pipeline.py` returns 0
    - asyncpg narrowing present: `grep -c 'except asyncpg.PostgresError' services/pipeline.py` returns at least 1
    - All caught exceptions are logged: `grep -c 'logger.error' services/pipeline.py` is greater than or equal to the count of `except ` clauses (excluding warmup `logger.warning` sites)
    - Structured error return present: `grep -cE 'success=False, error=' services/pipeline.py` returns at least 1
    - No audit calls in narrowed infrastructure catches: `grep -B2 'audit.log_' services/pipeline.py` shows audit only under business security paths, not under `except asyncpg.PostgresError` / `except openai.APIError` / etc.
    - No raw exception text leaked to response: `grep -nE 'error=.*str\(exc' services/pipeline.py` returns nothing
    - File parses as valid Python: `python -c "import ast; ast.parse(open('services/pipeline.py').read())"` exits 0
    - Module imports cleanly: `python -c "from services import pipeline"` exits 0
    - Required imports present: `grep -c '^import asyncpg' services/pipeline.py` returns 1 (and similarly for each library actually caught)
  </acceptance_criteria>
  <done>All `except Exception` sites in services/pipeline.py narrowed to library-specific types; every catch logs via logger.error with exc_info; in-request pipeline errors return structured IngestionResponse/QueryResponse with generic error messages; D-04 warmup sites preserved at logger.warning; file parses and imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow exception catches in retriever.py, oidc_auth.py, and vectorizer/indexer.py</name>
  <files>services/retriever/retriever.py, services/auth/oidc_auth.py, services/vectorizer/indexer.py</files>
  <read_first>
    - services/retriever/retriever.py (run `grep -n 'except Exception' services/retriever/retriever.py` — read each site ±5 lines)
    - services/auth/oidc_auth.py (run `grep -n 'except Exception' services/auth/oidc_auth.py` — read each site ±5 lines; note current JWT decode calls — likely `jose.jwt.decode(...)`)
    - services/vectorizer/indexer.py (run `grep -n 'except Exception' services/vectorizer/indexer.py` — read each site ±5 lines)
    - python-jose exception: `from jose import JWTError` — base class for `ExpiredSignatureError`, `JWTClaimsError`, `JWSError`
  </read_first>
  <action>
    Apply the same narrowing procedure as Task 1 to these three files. Each file has its own concerns — follow the mapping below.

    ---
    **File A: services/retriever/retriever.py**

    All retrieval sites touch asyncpg (via vector store) or Redis (if a cache layer is present). For each `except Exception`:

    1. Vector store / asyncpg query sites → `except asyncpg.PostgresError as exc:`
       - Log: `logger.error("retrieval failed", tenant_id=tenant_id, query=<safe_field>, exc_info=exc)` (do NOT log the full query string if it may contain user PII — log query_hash or length)
       - Raise-or-return behavior: retriever is called INSIDE pipeline, so returning an empty result list is acceptable IF the caller (pipeline.py) is the one producing the structured response. Choose based on existing return type:
         * If the existing `except Exception` returned `[]` → keep `return []` but ensure `logger.error` runs first
         * If the existing `except Exception` re-raised → re-raise as `raise` (bare re-raise) so `services/pipeline.py` catches the narrowed `asyncpg.PostgresError` and returns the structured response
         * Do NOT introduce new re-raises that weren't there before
    2. Redis cache sites (if present) → `except redis.RedisError as exc:` with `logger.warning` (cache miss is non-fatal — degrade to DB fetch) and `return None` / `pass` as the existing control flow dictates.
    3. Pure-Python sites (score sorting, dedup logic) → narrow to `ValueError` / `KeyError` as applicable.

    Add imports at top:
    ```python
    import asyncpg
    # import redis  # only if redis is actually caught
    ```

    ---
    **File B: services/auth/oidc_auth.py**

    All JWT verification sites use python-jose. For each `except Exception`:

    1. JWT decode / verification sites (`jwt.decode`, `jwt.get_unverified_header`, `jwks.fetch`) → `except JWTError as exc:`
       - Log: `logger.error("JWT verification failed", reason=type(exc).__name__, exc_info=exc)` — do NOT log the token value
       - Return/raise: PRESERVE existing HTTP 401 control flow. Typical pattern:
         ```python
         from jose import JWTError
         try:
             payload = jwt.decode(token, key, algorithms=[...])
         except JWTError as exc:
             logger.error("JWT verification failed", reason=type(exc).__name__, exc_info=exc)
             raise HTTPException(status_code=401, detail="Invalid authentication token")
         ```
         - The HTTPException `detail` must be a generic message — NEVER `str(exc)` (would leak algorithm mismatch, clock skew, etc. to clients per threat T-03-02).
    2. JWKS fetch sites (uses httpx) → if there is a separate try block around `httpx.AsyncClient.get(jwks_url)`, narrow to `except httpx.HTTPError as exc:` and raise `HTTPException(status_code=401, detail="Authentication service unavailable")`.
    3. Any remaining pure-Python sites → narrow to `ValueError` / `KeyError`.

    Add imports at top:
    ```python
    from jose import JWTError
    # import httpx  # only if httpx is actually caught
    ```

    ---
    **File C: services/vectorizer/indexer.py**

    All indexer sites touch asyncpg (writing vectors) or the embedder (OpenAI / sentence-transformers). For each `except Exception`:

    1. Vector upsert / DB write sites → `except asyncpg.PostgresError as exc:`
       - Log: `logger.error("vector upsert failed", tenant_id=tenant_id, chunk_count=len(chunks), exc_info=exc)`
       - Preserve existing control flow (raise or return False). Do NOT introduce new exception swallowing.
    2. Embedder calls (if OpenAI) → `except openai.APIError as exc:`
       - Log: `logger.error("embedding generation failed", exc_info=exc)`
    3. Embedder calls (if sentence-transformers — no official exception class) → Claude's discretion per CONTEXT.md: narrow to `RuntimeError` (sentence-transformers raises RuntimeError for CUDA OOM, model load failures) or `ValueError`.
    4. Pure-Python sites (batch splitting, shape validation) → narrow to `ValueError`.

    Add imports at top:
    ```python
    import asyncpg
    # import openai  # only if openai is actually caught
    ```

    ---
    **Common rules (apply to all three files):**

    - Every caught exception MUST be logged via `logger.error` (infrastructure failure) or `logger.warning` (degraded-mode, per D-04 if applicable) with `exc_info=exc`. No silent swallows.
    - Never include raw `str(exc)` / `repr(exc)` in response bodies or HTTPException detail (threat T-03-02).
    - Never call `audit.log_ingest` or `audit.log_query` from a narrowed infrastructure catch (per D-11). Existing audit calls for business security events (authz failures in `oidc_auth.py` — e.g., tenant mismatch after successful JWT decode) stay as-is.
    - **D-04 exemptions:** if any file contains a startup warmup pattern (`logger.warning(f"... (non-fatal): {exc}")`), narrow its exception type per D-03 but keep `logger.warning` and the continue-in-degraded-mode semantics.
    - **D-06 exemptions:** if any file contains a shutdown-path `except Exception: pass`, leave it entirely unchanged.
    - **D-03 invariant:** ZERO `except Exception` may remain in any of the three files after this task.
  </action>
  <verify>
    <automated>for f in services/retriever/retriever.py services/auth/oidc_auth.py services/vectorizer/indexer.py; do [ "$(grep -c 'except Exception' $f)" = "0" ] && [ "$(grep -cE '^\s*except\s*:' $f)" = "0" ] && python -c "import ast; ast.parse(open('$f').read())" || exit 1; done && python -c "from services.retriever import retriever; from services.auth import oidc_auth; from services.vectorizer import indexer"</automated>
  </verify>
  <acceptance_criteria>
    - services/retriever/retriever.py: `grep -c 'except Exception' services/retriever/retriever.py` returns 0
    - services/retriever/retriever.py: `grep -cE '^\s*except\s*:' services/retriever/retriever.py` returns 0
    - services/retriever/retriever.py: `grep -c 'except asyncpg.PostgresError' services/retriever/retriever.py` returns at least 1
    - services/retriever/retriever.py: `grep -c 'logger.error\|logger.warning' services/retriever/retriever.py` is at least equal to the count of `except ` clauses
    - services/auth/oidc_auth.py: `grep -c 'except Exception' services/auth/oidc_auth.py` returns 0
    - services/auth/oidc_auth.py: `grep -cE '^\s*except\s*:' services/auth/oidc_auth.py` returns 0
    - services/auth/oidc_auth.py: `grep -c 'JWTError' services/auth/oidc_auth.py` returns at least 2 (one import, one catch)
    - services/auth/oidc_auth.py: `grep -nE 'detail=.*str\(exc' services/auth/oidc_auth.py` returns nothing (no raw exception text in HTTP responses)
    - services/vectorizer/indexer.py: `grep -c 'except Exception' services/vectorizer/indexer.py` returns 0
    - services/vectorizer/indexer.py: `grep -cE '^\s*except\s*:' services/vectorizer/indexer.py` returns 0
    - services/vectorizer/indexer.py: `grep -c 'except asyncpg.PostgresError' services/vectorizer/indexer.py` returns at least 1
    - All three files parse: `python -c "import ast; [ast.parse(open(f).read()) for f in ['services/retriever/retriever.py', 'services/auth/oidc_auth.py', 'services/vectorizer/indexer.py']]"` exits 0
    - All three modules import cleanly: `python -c "from services.retriever import retriever; from services.auth import oidc_auth; from services.vectorizer import indexer"` exits 0
    - No audit calls introduced in infrastructure catches: grep around each new `except asyncpg.PostgresError` / `except openai.APIError` / `except httpx.HTTPError` / `except redis.RedisError` / `except JWTError` in all three files shows no `audit.log_` call within 3 lines after the catch
  </acceptance_criteria>
  <done>All `except Exception` sites in retriever.py, oidc_auth.py, and vectorizer/indexer.py narrowed to library-specific types per D-01/D-02; every catch logs via logger.error or logger.warning with exc_info; JWT errors return HTTP 401 with generic detail (no raw exception text leaked); D-04 warmup and D-06 shutdown sites preserved; all three files parse and import cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client ↔ API (oidc_auth.py) | Untrusted JWT token crosses this boundary; verification errors must not leak internal detail (key type, clock skew, algorithm) to client |
| Pipeline stage ↔ external service (asyncpg/OpenAI/httpx/Redis) | Library exceptions crossing this boundary must be logged with full context server-side but surfaced to caller as generic messages |
| Background logger ↔ stdout / log aggregator | Exception tracebacks via `exc_info=` may contain PII if SQL parameters or request bodies appear in frames — rely on structured logger's redaction; never log raw `str(exc)` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | services/auth/oidc_auth.py JWT error response | mitigate | HTTPException detail is a hard-coded generic string ("Invalid authentication token" / "Authentication service unavailable"). Full JWTError context routes to `logger.error(..., exc_info=exc)` only. Grep check in acceptance_criteria confirms no `str(exc)` in `detail=` |
| T-03-02-02 | Information Disclosure | services/pipeline.py structured error responses | mitigate | `IngestionResponse.error` / `QueryResponse.error` is a hard-coded generic string per library category ("Storage backend temporarily unavailable", etc.). Full exception routes to `logger.error(..., exc_info=exc)` only. Grep check confirms no `str(exc)` in `error=` |
| T-03-02-03 | Repudiation | All four files — silent exception swallow | mitigate | Acceptance criteria enforce `logger.error` (or `logger.warning` for D-04 warmup) count ≥ `except` count. No catch path is permitted to `pass` or silently return without logging, except the D-06 shutdown-flush exemption |
| T-03-02-04 | Tampering | services/auth/oidc_auth.py JWT validation downgrade | accept | Narrowing `except Exception` to `except JWTError` reduces surface for accidental swallowing of NON-jose errors (e.g., a programming bug becomes a 500, not a silent 401 bypass). This is a security improvement — disposition records the intent |
| T-03-02-05 | Denial of Service | services/retriever/retriever.py empty-result-on-error | accept | Retriever returning `[]` on `asyncpg.PostgresError` (if that's the pre-existing behavior) can mask DB outages as "no results found" to end users. Acceptance: we preserve existing control flow rather than expand scope to fix behavioral bugs — that's a separate concern tracked as a possible follow-up |
| T-03-02-06 | Information Disclosure | services/pipeline.py log fields (tenant_id, doc_id, query) | mitigate | Log keyword args use IDs and bounded metadata — NEVER the full query string, NEVER the full document text. Action instructs "log query_hash or length" for retriever, keeps tenant_id/doc_id (which are already in logs elsewhere) |
</threat_model>

<verification>
- `grep -rn 'except Exception' services/pipeline.py services/retriever/retriever.py services/auth/oidc_auth.py services/vectorizer/indexer.py` — returns nothing (0 matches across all 4 files)
- `grep -rnE '^\s*except\s*:' services/pipeline.py services/retriever/retriever.py services/auth/oidc_auth.py services/vectorizer/indexer.py` — returns nothing (no bare except clauses)
- `python -c "import ast; [ast.parse(open(f).read()) for f in ['services/pipeline.py', 'services/retriever/retriever.py', 'services/auth/oidc_auth.py', 'services/vectorizer/indexer.py']]"` — exits 0
- `python -c "from services import pipeline; from services.retriever import retriever; from services.auth import oidc_auth; from services.vectorizer import indexer"` — exits 0 (all four modules import cleanly)
- `grep -rnE 'error=.*str\(exc' services/pipeline.py` — returns nothing (no raw exception text leaked to pipeline responses)
- `grep -rnE 'detail=.*str\(exc' services/auth/oidc_auth.py` — returns nothing (no raw exception text leaked to HTTP 401)
</verification>

<success_criteria>
- Zero `except Exception` sites remain in services/pipeline.py, services/retriever/retriever.py, services/auth/oidc_auth.py, services/vectorizer/indexer.py
- Zero bare `except:` clauses remain in the same four files
- Every narrowed catch uses the library-specific type per D-01/D-02 (asyncpg.PostgresError, openai.APIError, httpx.HTTPError, redis.RedisError, JWTError)
- Every narrowed catch logs via logger.error or logger.warning with exc_info=exc (no silent swallows)
- In-request pipeline errors return IngestionResponse/QueryResponse with generic error messages (no raw exception text, no re-raise per D-05)
- JWT verification errors return HTTP 401 with generic detail (no raw exception text)
- D-04 warmup sites preserved (logger.warning, continue) if any exist in these files
- D-06 shutdown-flush sites (none expected in these files) remain unchanged
- No audit.log_ingest / audit.log_query calls introduced into infrastructure error paths (D-11)
- All four files parse and import cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-handling-sweep/03-02-SUMMARY.md` describing:
- Per-file count of `except Exception` sites before and after (should be N → 0)
- Per-file mapping table: line_number → old_catch → new_catch → library → decision_id (D-01/D-02/D-03/D-04)
- Confirmed grep counts matching acceptance criteria
- Any Claude's-discretion type choices made for less common libraries (e.g., sentence-transformers → RuntimeError)
- Whether a `_handle_pipeline_error` DRY helper was introduced (and if so, where it lives and how many sites call it)
</output>
