---
plan_id: 28-00
phase: 28
plan: 0
type: execute
wave: 1
depends_on: []
requirements: [DOC-01]
files_modified:
  - docs/RUNBOOK.md
autonomous: true
notes: |
  Implements D-01 (new docs/RUNBOOK.md) + D-02 (Local dev setup section) + D-03 (Ops procedures) + D-04 (Troubleshooting).
  On-call playbook deferred to v1.8 per D-01 explicit scope ruling.
  Audience = mixed (dev onboarding + ops day-2) per Claude's-discretion default in CONTEXT.md.
  Carry-forward: audit-mode-before-enforce wording when describing TD-04 near-dup audit metric (CONTEXT carry-forward row 1).
  Carry-forward: docs MUST NOT suggest UPDATE/DELETE on audit_log (INSERT-ONLY invariant, CONTEXT carry-forward row 4).

must_haves:
  truths:
    - "A new operator can follow Local dev section from a clean clone and end with `uv run pytest -m 'not benchmark'` green."
    - "An operator on a fresh PG cluster knows audit_log auto-creates on first audit_service call (TD-01) — no manual DDL step documented."
    - "An operator knows bge-m3 loads from vanilla HF cache `{MODEL_DIR}/BAAI/bge-m3/` (TD-07) — no symlink workaround documented; legacy `{MODEL_DIR}/embedding_models/bge-m3/` fallback noted."
    - "An operator knows asyncpg `?ssl=disable` strip is handled in utils/asyncpg_helper.prepare_dsn (TD-03) — not per-module."
    - "An operator hitting the 4 documented troubleshooting symptoms (Redis-CE / openai-SDK / asyncpg SSL literal / extractor_e2e bge-m3) finds a workaround or a v1.8 backlog ID."
  artifacts:
    - path: docs/RUNBOOK.md
      provides: "Three-section RUNBOOK (Local dev / Ops procedures / Troubleshooting)"
      min_lines: 120
      contains:
        - "## Local dev setup"
        - "## Ops procedures"
        - "## Troubleshooting"
        - "uv venv"
        - "uv sync"
        - "MODEL_DIR"
        - "audit_log auto-create"
        - "utils/asyncpg_helper"
        - "BAAI/bge-m3"
        - "MEMORY_NEAR_DUPLICATE_SKIPPED"
        - "@pytest.mark.uses_redis"
        - "OAI-01"
        - "EVT-01"
        - "TEST-INFRA-01"
  key_links:
    - from: docs/RUNBOOK.md
      to: docs/memory-eviction.md
      via: relative markdown link
      pattern: "memory-eviction\\.md"
    - from: docs/RUNBOOK.md
      to: docs/DOCKER_DEPLOY.md
      via: relative markdown link
      pattern: "DOCKER_DEPLOY\\.md"
    - from: docs/RUNBOOK.md
      to: README.md
      via: relative markdown link
      pattern: "README\\.md"
---

<objective>
Create `docs/RUNBOOK.md` — a new mixed-audience (dev onboarding + ops day-2) operational reference for the post-v1.7 codebase. Three sections per D-01..D-04: Local dev setup, Ops procedures, Troubleshooting.

Purpose: Establish the runbook surface this milestone (v1.7) and seed it with the v1.7 ops-relevant deltas (TD-01 audit_log auto-create, TD-03 asyncpg URL handling, TD-07 bge-m3 HF cache layout, TD-06 redis-mock test pattern). Future milestones append; on-call playbook deferred to v1.8.

Output: `docs/RUNBOOK.md` (single new file, ~150–250 lines). Zero production code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-doc-sweep-v1.7-release/28-CONTEXT.md
@.planning/phases/26-memory-infra-hygiene/26-01-SUMMARY.md
@.planning/phases/26-memory-infra-hygiene/26-04-SUMMARY.md
@.planning/phases/26-memory-infra-hygiene/26-05-SUMMARY.md
@.planning/phases/27-test-isolation-memory-reliability/deferred-items.md
@README.md
@docs/DOCKER_DEPLOY.md
@docs/memory-eviction.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft docs/RUNBOOK.md (single-shot, three sections per D-01..D-04)</name>
  <files>docs/RUNBOOK.md</files>
  <action>
Create `docs/RUNBOOK.md` from scratch. Use a top-of-file H1 `# EnterpriseRAG Runbook` and a 2-line preamble stating audience = mixed dev/ops, scope = v1.7+ ops deltas plus baseline local-dev steps, on-call playbook deferred to v1.8.

Three H2 sections in order: `## Local dev setup`, `## Ops procedures`, `## Troubleshooting`. Per Claude's-discretion default in CONTEXT (RUNBOOK audience framing): one file, sections clearly labelled.

**`## Local dev setup` (per D-02):** From-zero local environment. Cover in order — `uv venv`; `uv add` / `uv sync` (project standard per CLAUDE.md); Postgres + pgvector docker bring-up (link to existing `docs/DOCKER_DEPLOY.md` for Docker-stack mode, only fill the gap for "I just want unit + integration tests locally"); Redis docker bring-up (one-liner — `docker run -d -p 6379:6379 redis:7-alpine`); `MODEL_DIR` env-var (note that per OPS-01 / CLAUDE.md it must be env-driven, not hardcoded); `.env` template (point to `.env.docker` as starting template per existing README Quick start §Configuration); first invocation `uv run pytest -m 'not benchmark'` (matches Phase 27 CI gate per 27-BENCHMARK.md). Do NOT duplicate the README Quick start verbatim — link back and only fill gaps.

**`## Ops procedures` (per D-03):** Five subsections (H3), in this order:
1. **Verify audit_log auto-create on fresh PG (TD-01 / Phase 26)** — describe expected behavior: first call into `services/audit/audit_service.py` triggers `_get_pool` → `_create_tables` → DDL with `REVOKE UPDATE, DELETE ON audit_log FROM PUBLIC` (INSERT-ONLY invariant preserved per CONTEXT carry-forward row 4). Verification SQL: `SELECT to_regclass('public.audit_log')` returns non-null after first audit-write. NO manual DDL step. Cite 26-04-SUMMARY.md.
2. **Run the eviction job (v1.6 Phase 25 carry-over)** — one-line summary; link to `docs/memory-eviction.md` for the full k8s CronJob YAML + audit-then-enforce workflow. Do NOT duplicate.
3. **GDPR forget API (v1.6 Phase 25 carry-over)** — one-line summary + curl example (`DELETE /api/v1/memory/forget?user_id=<id>` with admin-claim JWT); link to `docs/memory-eviction.md` for full operational usage.
4. **bge-m3 model dir layout (TD-07 / Phase 26)** — vanilla HF cache path is `{MODEL_DIR}/BAAI/bge-m3/`. Legacy `{MODEL_DIR}/embedding_models/bge-m3/` is still resolved by the loader (backwards-compat per TD-07 acceptance criterion). State: pre-download via `huggingface-cli download BAAI/bge-m3 --local-dir $MODEL_DIR/BAAI/bge-m3` (or rely on first-use auto-download if MODEL_DIR is writable and HF_HUB_OFFLINE is unset). **Authoring constraint — the word `symlink` MUST NOT appear anywhere in `docs/RUNBOOK.md` (verify gate forbids it). Frame the two-path resolution as "vanilla HF cache (primary) + legacy directory (backwards-compat)"; do not mention or negate symlink-based workarounds.**
5. **asyncpg URL `?ssl=disable` handling (TD-03 / Phase 26)** — note that `services/memory/memory_service.py` and `services/audit/audit_service.py` both connect via `utils/asyncpg_helper.prepare_dsn(dsn)` which strips the literal `ssl=disable` URL param before passing to asyncpg (asyncpg URL parser misreads the literal otherwise — that's why this helper exists). Operators do NOT need to per-module-handle this anymore. Cite 26-01-SUMMARY.md.

**`## Troubleshooting` (per D-04):** Five subsections (H3), table-style symptom → diagnosis → fix where it fits, prose where it doesn't:
1. **Redis-ConnectionError on unit suite** — symptom: `redis.exceptions.ConnectionError` during `pytest tests/unit/...`. Diagnosis: unit test hits production `get_redis()` codepath. Fix: apply `@pytest.mark.uses_redis` marker on the test (the `tests/conftest.py` `pytest_collection_modifyitems` hook auto-applies the `redis_mock` fixture to marked tests — TD-06 pattern from Phase 27). Reference: 27-02-SUMMARY.md (full marker rollout).
2. **openai SDK signature drift — 32 pre-existing unit failures** — symptom: `APIError.__init__() missing 1 required positional argument: 'request'` from `test_agent_pipeline_refactor.py` / `test_agent_sse.py` / `test_pipeline_coverage.py` / `test_feedback_ab_forward.py` / `test_memory_controller.py` / `test_recall_tool.py`. Diagnosis: openai SDK ≥ v1.x changed `APIError.__init__` signature; tests construct it with old shape. Workaround: skip-mark or fix-locally; **tracked as v1.8 backlog OAI-01** — link `.planning/REQUIREMENTS-v1.8.md` (will exist after 28-03 ships).
3. **asyncpg DSN `ssl=disable` literal misread** — symptom: asyncpg connect failure with cryptic SSL error when `PG_DSN` contains `?ssl=disable`. Fix: do NOT bypass `utils/asyncpg_helper.prepare_dsn` — call sites already use it per TD-03. If you wrote a NEW module that opens its own asyncpg pool, import `from utils.asyncpg_helper import prepare_dsn` and apply it to the DSN before `asyncpg.create_pool`.
4. **+14 event-loop singleton leaks newly exposed by Phase 27 marker rollout** — symptom: integration test fails with "There is no current event loop in thread 'MainThread'" after applying `@pytest.mark.uses_redis`. Diagnosis: module-level singleton was constructed under a stale loop (TD-02-style pre-existing leak surfaced by the marker tightening test isolation). Workaround pattern: rebuild via `create_app()` factory from `tests/factories/app.py` (Phase 27 SC-1). **Tracked as v1.8 backlog EVT-01**.
5. **`tests/integration/test_extractor_e2e.py` FileNotFoundError on bge-m3** — symptom: `FileNotFoundError: Path /tmp/embedding_models/bge-m3 not found` when running `tests/integration/test_extractor_e2e.py`. Diagnosis: the `embedder_or_mock` fixture monkeypatch reaches the consumer AFTER `AgentQueryPipeline.__init__` has already called `get_embedder()` → `HuggingFaceEmbedder.__init__` raises. Per `.planning/phases/27-test-isolation-memory-reliability/deferred-items.md`, this is a pre-existing fixture-ordering bug not caused by v1.7. **Tracked as v1.8 backlog TEST-INFRA-01** — see deferred-items.md for the three suggested fix paths (move patch earlier / pre-download in CI / mock embedder __init__ directly). For now: run with mock-at-consumer pattern (CLAUDE.md §"Mock at consumer path (services.<mod>.<dep>)") at the embedder import site, OR pre-download bge-m3 to `$APP_MODEL_DIR/BAAI/bge-m3/` per Ops §4.

Cross-cutting writing constraints:
- INSERT-ONLY audit_log invariant: never suggest UPDATE/DELETE on `audit_log` anywhere in this doc (CONTEXT carry-forward row 4).
- Audit-mode-before-enforce wording when mentioning TD-04 in Ops §1 or Troubleshooting: v1.7 emits `MEMORY_NEAR_DUPLICATE_SKIPPED` audit row AND the INSERT still runs (D-09; ROADMAP 27-SC-3 wording 'save is skipped' was explicitly overridden per 27-VERIFICATION.md SC-3 row). Use this exact phrasing if referenced.
- `OAI-01`, `EVT-01`, `TEST-INFRA-01` are forward references — link `.planning/REQUIREMENTS-v1.8.md` (created in plan 28-03; this RUNBOOK ships in Wave 1 parallel, file may not yet exist at write-time — that is acceptable per dependency graph, 28-04 archive runs after both).
- Use relative markdown links throughout (`docs/memory-eviction.md`, `docs/DOCKER_DEPLOY.md`, `README.md#configuration`).
- No code blocks executing production-code; only shell snippets (uv, docker, curl, pytest, huggingface-cli) and SQL verification one-liners.
- File must be reviewer-walkable: every TD-01..TD-07 referenced in this doc must allow the reviewer to trace from RUNBOOK → SUMMARY.md → code without `grep`.

Length target: 150–250 lines. Surgical, not exhaustive.
  </action>
  <verify>
    <automated>test -f docs/RUNBOOK.md && grep -c '^## ' docs/RUNBOOK.md | awk '$1 >= 3 { exit 0 } { exit 1 }' && grep -q '## Local dev setup' docs/RUNBOOK.md && grep -q '## Ops procedures' docs/RUNBOOK.md && grep -q '## Troubleshooting' docs/RUNBOOK.md && grep -q 'audit_log' docs/RUNBOOK.md && grep -q 'utils/asyncpg_helper' docs/RUNBOOK.md && grep -q 'BAAI/bge-m3' docs/RUNBOOK.md && grep -q 'MEMORY_NEAR_DUPLICATE_SKIPPED' docs/RUNBOOK.md && grep -q 'uses_redis' docs/RUNBOOK.md && grep -q 'OAI-01' docs/RUNBOOK.md && grep -q 'EVT-01' docs/RUNBOOK.md && grep -q 'TEST-INFRA-01' docs/RUNBOOK.md && ! grep -E 'UPDATE\s+audit_log|DELETE\s+FROM\s+audit_log' docs/RUNBOOK.md && ! grep -i 'symlink' docs/RUNBOOK.md</automated>
  </verify>
  <done>docs/RUNBOOK.md exists with three required H2 sections; all 5 ops subsections + 5 troubleshooting subsections present; cross-references to TD-* SUMMARYs and v1.8 backlog IDs present; INSERT-ONLY invariant preserved (no UPDATE/DELETE on audit_log); no bge-m3 symlink workaround documented.</done>
</task>

</tasks>

<verification>
- File created at correct path; section count and content gates above pass.
- Spot-check (not automated): a reviewer following Local dev setup from a clean clone ends at `uv run pytest -m 'not benchmark'` green (validated against Phase 27 CI gate per 27-BENCHMARK.md §CI gating).
- No production code touched: `git diff --name-only HEAD` must show only `docs/RUNBOOK.md` (verifier check in 28-CONTEXT.md §"Notes for downstream agents").
</verification>

<success_criteria>
- `docs/RUNBOOK.md` exists with the 3 required H2 sections and all referenced anchors.
- All Ops + Troubleshooting subsections reference at least one TD-* requirement or v1.8 backlog ID.
- Zero changes to `services/`, `controllers/`, `utils/`, `config/`, or `tests/` paths.
</success_criteria>

<output>
Create `.planning/phases/28-doc-sweep-v1.7-release/28-00-SUMMARY.md` when done, recording final line count, the 5 Ops subsections + 5 Troubleshooting subsections shipped, and any deviations (e.g. content added beyond D-02..D-04 scope).
</output>
