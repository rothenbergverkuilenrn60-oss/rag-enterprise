---
plan_id: 27-03-save-fact-cosine-precheck
phase: 27
plan: 03
type: execute
wave: 2
depends_on: [27-00-test-infra-prep, 27-02-redis-mock-rollout]
requirements: [TD-04]
files_modified:
  - config/settings.py
  - services/audit/audit_service.py
  - services/memory/memory_service.py
  - tests/unit/memory/__init__.py
  - tests/unit/memory/test_save_fact_precheck.py
  - tests/unit/memory/test_save_fact_precheck_failure.py
autonomous: true
notes: |
  Wave 2 SC-3 — D-09 audit-mode-only correction WINS over ROADMAP SC-3 wording.
  C3 OVERRIDE: ROADMAP SC-3 reads "save is skipped" — WRONG for v1.7. Per CONTEXT D-09 (audit-mode-before-enforce per v1.6 EVICT-02 carry-forward):
    v1.7 emits `MEMORY_NEAR_DUPLICATE_SKIPPED` audit metric BUT THE ROW IS STILL INSERTED.
    Silent-skip enforcement deferred to v1.8 (v1.8+ follow-up).
  Tests assert post-call row count IS incremented even when near-dup detected.
  No silent skip logic in this plan. The audit row carries semantic "would-skip" intent (AuditResult.SKIPPED) but the INSERT runs.
  - Settings field name: `memory_near_duplicate_threshold: float = Field(default=0.05, ge=0.0, le=1.0)` (D-07).
  - AuditAction enum value: `MEMORY_NEAR_DUPLICATE_SKIPPED` appended AFTER `MEMORY_EVICT` (D-08).
  - Precheck SQL uses ORDER BY ... LIMIT 1 (NOT WHERE <threshold LIMIT 1) so audit row records actual nearest distance per D-08 detail.
  - Failure mode is fail-OPEN: `asyncpg.PostgresError` on precheck → log warning, skip precheck, INSERT proceeds (RESEARCH §Theme 3 + analog `get_relevant_facts:353-357`).
  - 27-04 builds save_facts(list) on top of this — D-12 makes save_fact a thin wrapper after 27-04 ships; THIS plan keeps save_fact as the primary path.
  - v1.8 follow-up: promote audit-mode to silent-skip enforcement (tracked in STATE.md "v1.8+ follow-up: silent-skip rollout for MEMORY_NEAR_DUPLICATE_SKIPPED" — add to STATE in summary).
  - **v1.8 TOCTOU follow-up (A2 from /plan-eng-review):** when silent-skip is enforced, the SELECT-then-INSERT pattern has a TOCTOU race window — two parallel save_fact calls with near-identical embeddings can both see "no dup" before either commits, then both INSERT. v1.7 is unaffected because INSERT runs unconditionally (D-09), so the race only mis-fires the audit metric (acceptable in audit-mode). v1.8 silent-skip work MUST address this: options are (a) advisory lock per (user, tenant), (b) `INSERT ... ON CONFLICT` with a cosine-distance unique-ish index (hard with pgvector), or (c) accept the race and rely on the precheck only being a "good-faith" guard. Pick during v1.8 planning, not here.

must_haves:
  truths:
    - "config/settings.py exports memory_near_duplicate_threshold: float (default 0.05, ge=0.0, le=1.0)"
    - "services/audit/audit_service.py::AuditAction has MEMORY_NEAR_DUPLICATE_SKIPPED appended after MEMORY_EVICT"
    - "LongTermMemory.save_fact runs cosine <=> precheck on every call with per-(user,tenant) filter + HNSW iterative_scan + ef_search GUC"
    - "When precheck hits (dist < threshold), AuditService.log fires MEMORY_NEAR_DUPLICATE_SKIPPED row AND the INSERT still runs (D-09 audit-mode-only)"
    - "When precheck SELECT fails (asyncpg.PostgresError), the warning is logged and the INSERT proceeds (fail-open)"
    - "save_fact adds <=+1 PG round-trip on the hot path (1 SELECT for precheck + 1 INSERT — same INSERT as before)"
  artifacts:
    - path: config/settings.py
      provides: "memory_near_duplicate_threshold setting"
      contains: "memory_near_duplicate_threshold"
    - path: services/audit/audit_service.py
      provides: "AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED enum value (appended after MEMORY_EVICT)"
      contains: "MEMORY_NEAR_DUPLICATE_SKIPPED"
    - path: services/memory/memory_service.py
      provides: "LongTermMemory.save_fact with cosine precheck + audit-mode-only metric emit"
      contains: "_is_near_duplicate"
    - path: tests/unit/memory/test_save_fact_precheck.py
      provides: "Asserts precheck fires + audit row emitted + INSERT STILL runs (D-09) + <=+1 PG RTT"
    - path: tests/unit/memory/test_save_fact_precheck_failure.py
      provides: "Asserts fail-open semantics — precheck PostgresError causes warning + INSERT still runs"
  key_links:
    - from: services/memory/memory_service.py::LongTermMemory.save_fact
      to: services/memory/memory_service.py::_is_near_duplicate (new private helper)
      via: direct call inside acquire+transaction
      pattern: "await self._is_near_duplicate"
    - from: services/memory/memory_service.py::_fire_near_duplicate_audit (new module-level helper)
      to: services.audit.audit_service.get_audit_service.log
      via: await get_audit_service().log(AuditEvent(...))
      pattern: "MEMORY_NEAR_DUPLICATE_SKIPPED"
    - from: tests/unit/memory/test_save_fact_precheck.py
      to: LongTermMemory.save_fact behavior
      via: pytest fixtures (mocked pool + spied audit service + mocked embedder)
      pattern: "audit.*MEMORY_NEAR_DUPLICATE_SKIPPED"
---

<objective>
Land SC-3 of Phase 27 (per the D-09 CORRECTION over ROADMAP wording): `LongTermMemory.save_fact` runs `<embedding> <=> $vec < 0.05` cosine precheck on every call. On precheck hit, the `MEMORY_NEAR_DUPLICATE_SKIPPED` audit row is emitted with actor + truncated fact + nearest distance — **but the INSERT still runs** (audit-mode-only per v1.6 EVICT-02 audit-mode-before-enforce discipline). Precheck adds at most one extra PG round-trip on the hot path. Existing save unit tests still green.

Purpose: ROADMAP SC-3 reads "save is skipped" which is wrong for v1.7. CONTEXT D-09 is canonical — v1.7 ships metric-only; v1.8 promotes to actual skip. This plan implements the metric-only behavior with the assertion bar set to "audit row emitted AND duplicate row inserted" (NOT "save skipped").

Output: 1 settings field + 1 AuditAction enum value + 1 helper method `_is_near_duplicate` + 1 module-level helper `_fire_near_duplicate_audit` + save_fact body edit + 2 new unit test files. No batch API yet — 27-04 wraps this into save_facts(list).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-isolation-memory-reliability/27-CONTEXT.md
@.planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md
@.planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md
@.planning/phases/27-test-isolation-memory-reliability/27-VALIDATION.md
@.planning/phases/27-test-isolation-memory-reliability/27-00-PLAN.md
@services/memory/memory_service.py
@services/audit/audit_service.py
@config/settings.py
@tests/conftest.py
@CLAUDE.md
</context>

<interfaces>
Key existing interfaces:

From services/memory/memory_service.py (current — to modify):
- `LongTermMemory.get_relevant_facts` at lines 290-357 — the canonical HNSW cosine query pattern. Mirror for precheck SQL: `SET LOCAL hnsw.iterative_scan='strict_order'` + `SET LOCAL hnsw.ef_search = settings.pgvector_ef_search_filtered` then SELECT with `embedding <=> $::vector` ordering.
- `LongTermMemory.save_fact` at lines 359-396 — current shape: embed (try/except httpx.HTTPError, RuntimeError, OSError -> MemoryFactWriteError), then `pool.acquire() -> conn.execute(INSERT)` (try/except asyncpg.PostgresError -> MemoryFactWriteError).
- `LongTermMemory._get_pool` lazy-init lifecycle.
- Lazy imports inside method bodies pattern: `import httpx; from config.settings import settings; from services.vectorizer.embedder import get_embedder` (lines 316-320).
- Narrow exception tuple for embed: `(httpx.HTTPError, RuntimeError, OSError)` (line 325-329).

From services/audit/audit_service.py:
- `AuditAction` enum (line 25-42) — last entry `MEMORY_EVICT` at line 42. Append-only convention per Phase 25 EVICT-02.
- `AuditEvent` dataclass (line 52-64) — fields: event_id, timestamp, user_id, tenant_id, action: str, resource_id, ip_address, result, detail: dict, trace_id.
- `AuditResult.SKIPPED` (line 49) — semantic value already exists.
- `AuditService.log(event: AuditEvent)` async method (line 183-204) — best-effort audit-write.
- `get_audit_service()` accessor (line 386-393).

From config/settings.py:
- `memory_facts_cap_per_user: int = Field(default=500, ge=1)` at line 498 — analog placement target.
- `pgvector_ef_search_filtered: int` — the GUC for ef_search (already exists, reused).

From utils/models.py:
- `MemoryFactWriteError` exception class — preserved verbatim.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add memory_near_duplicate_threshold setting + AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED enum value</name>
  <files>config/settings.py, services/audit/audit_service.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §Theme 2 D-07/D-08 (lines 33-35) + §8 lines 240-264 (AuditService.log signature + AuditEvent shape)
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"AuditAction enum — append MEMORY_NEAR_DUPLICATE_SKIPPED" (lines 156-172) + §"config/settings.py — add memory_near_duplicate_threshold" (lines 174-186) + §"Audit-Mode Metric Naming" (lines 449-457)
    - config/settings.py lines 480-520 (memory_facts_cap_per_user placement region — the analog for D-07)
    - services/audit/audit_service.py lines 25-45 (AuditAction enum tail; MEMORY_EVICT is the current last entry at line 42)
  </read_first>
  <behavior>
    - Test 1: `from config.settings import settings; assert settings.memory_near_duplicate_threshold == 0.05` (default).
    - Test 2: `settings.memory_near_duplicate_threshold` constrained `[0.0, 1.0]` — invalid env value raises ValidationError on settings re-load (Pydantic V2 le=1.0 enforcement).
    - Test 3: `from services.audit.audit_service import AuditAction; assert AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED.value == "MEMORY_NEAR_DUPLICATE_SKIPPED"`.
    - Test 4: enum ordering — `MEMORY_NEAR_DUPLICATE_SKIPPED` appears AFTER `MEMORY_EVICT` in `list(AuditAction)`.
  </behavior>
  <action>
    Edit `config/settings.py`:
    - Locate `memory_facts_cap_per_user: int = Field(default=500, ge=1)` (around line 498).
    - Add IMMEDIATELY AFTER, with section-locality comment per PATTERNS.md §"config/settings.py — add memory_near_duplicate_threshold":
      - Comment: `# Phase 27 / TD-04 — cosine near-duplicate guard threshold. v1.7 audit-mode-only (D-09): save still happens; row emitted for ops visibility. Override via env APP_MEMORY_NEAR_DUPLICATE_THRESHOLD=<float>.`
      - Field: `memory_near_duplicate_threshold: float = Field(default=0.05, ge=0.0, le=1.0)`
    - Preserve all surrounding settings + existing Pydantic V2 BaseSettings shape. No env-prefix changes.

    Edit `services/audit/audit_service.py`:
    - Locate `AuditAction` enum (lines 25-42). Last entry is `MEMORY_EVICT = "MEMORY_EVICT"` at line 42.
    - Append AFTER `MEMORY_EVICT`, with comment per PATTERNS.md §"AuditAction enum — append":
      - Comment: `# Phase 27 — TD-04 — near-duplicate audit-mode-only metric (D-09: save NOT skipped in v1.7)`
      - Enum value: `MEMORY_NEAR_DUPLICATE_SKIPPED = "MEMORY_NEAR_DUPLICATE_SKIPPED"`
    - DDL impact: zero (RESEARCH §"DDL impact" line 172). `action VARCHAR(64)` in audit_log accepts any string; no Postgres enum type.

    No tests added in this task — behavior assertions surface in Task 3's full unit tests + the inline smoke at acceptance time.

    Per CLAUDE.md: Pydantic V2 (already used), mypy --strict (no new type annotations beyond Field), ruff clean.
  </action>
  <verify>
    <automated>uv run python -c "from config.settings import settings; assert settings.memory_near_duplicate_threshold == 0.05; from services.audit.audit_service import AuditAction; assert AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED.value == 'MEMORY_NEAR_DUPLICATE_SKIPPED'; values = [a.value for a in AuditAction]; assert values.index('MEMORY_NEAR_DUPLICATE_SKIPPED') > values.index('MEMORY_EVICT'); print('settings + enum ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' config/settings.py | grep -c "memory_near_duplicate_threshold"` returns >= 1.
    - `grep -v '^#' config/settings.py | grep -c "Field(default=0.05, ge=0.0, le=1.0)"` returns >= 1.
    - `grep -v '^#' services/audit/audit_service.py | grep -c "MEMORY_NEAR_DUPLICATE_SKIPPED"` returns >= 1.
    - `uv run python -c "from services.audit.audit_service import AuditAction; vals = [a.value for a in AuditAction]; assert vals.index('MEMORY_NEAR_DUPLICATE_SKIPPED') > vals.index('MEMORY_EVICT')"` exits 0.
    - `uv run ruff check config/settings.py services/audit/audit_service.py` passes.
    - `uv run mypy --strict config/settings.py services/audit/audit_service.py` — preserves baseline pass/fail status.
    - Regression: `uv run pytest tests/unit/test_audit_service.py tests/unit/test_audit_service_pool.py -x --timeout 30` exit 0 (if these files exist; otherwise skip this check).
  </acceptance_criteria>
  <done>memory_near_duplicate_threshold setting present with default 0.05 + bounds; AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED appended after MEMORY_EVICT preserving append-only ordering; ruff + mypy --strict clean; existing audit tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LongTermMemory._is_near_duplicate helper + _fire_near_duplicate_audit + wire into save_fact body (audit-mode-only per D-09)</name>
  <files>services/memory/memory_service.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §Theme 3 "Exact SQL (singular path)" (lines 614-643) + "Semantics clarification" (lines 651-680, D-09 audit-mode-only canonical implementation sketch) + "Audit-write helper" (lines 682-699) + "Failure mode policy" (lines 701-703)
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"LongTermMemory.save_fact — TD-04 precheck" (lines 53-80) + §"Pattern D — Audit-write non-fatal boundary" (lines 376-379)
    - services/memory/memory_service.py lines 80-180 (ShortTermMemory pre-context, _get_pool, _create_tables for LongTermMemory) + lines 290-357 (get_relevant_facts — exact SQL pattern to mirror) + lines 359-396 (current save_fact body to modify)
    - services/audit/audit_service.py lines 25-204 (AuditAction enum after Task 1 update, AuditEvent dataclass, AuditResult enum, AuditService.log signature)
  </read_first>
  <behavior>
    - Behavior 1: `LongTermMemory._is_near_duplicate(conn, *, user_id, tenant_id, embedding, threshold) -> tuple[bool, float | None]` executes a SET LOCAL GUC + SELECT cosine query; returns `(False, None)` when table empty, `(True, dist)` when nearest distance < threshold, `(False, dist)` otherwise.
    - Behavior 2: `_fire_near_duplicate_audit(user_id, tenant_id, fact, dist)` is module-level (or static method); wraps `await get_audit_service().log(AuditEvent(action=MEMORY_NEAR_DUPLICATE_SKIPPED, ...))` in narrow-exception catch (Pattern D — audit-write non-fatal boundary); logs warning on failure but does NOT raise.
    - Behavior 3: `LongTermMemory.save_fact` flow becomes: (a) embed (existing narrow-exception tuple unchanged); (b) acquire pool conn; (c) call `_is_near_duplicate` inside try/except `asyncpg.PostgresError` — on PostgresError, fail-open: log warning, treat `is_dup=False`; (d) if `is_dup`, fire audit (best-effort) — INSERT STILL RUNS (D-09); (e) execute INSERT (existing INSERT SQL preserved verbatim, asyncpg.PostgresError -> MemoryFactWriteError preserved).
    - Behavior 4: save_fact still raises MemoryFactWriteError on embed failure (unchanged); save_fact still raises MemoryFactWriteError on INSERT PostgresError (unchanged).
    - Behavior 5: Hot-path RTT count: 1 SELECT (precheck) + 1 INSERT = 2 PG calls per save (was 1 INSERT = 1 call). Net +1 RTT per save_fact, matching D-10.
  </behavior>
  <action>
    Edit `services/memory/memory_service.py` LongTermMemory class:

    1. Add private method `_is_near_duplicate` per RESEARCH §Theme 3 lines 614-643. Method signature:
       - `async def _is_near_duplicate(self, conn, *, user_id: str, tenant_id: str, embedding: list[float], threshold: float) -> tuple[bool, float | None]`
       - Body:
         - `async with conn.transaction():`
         - Lazy `from config.settings import settings` (mirror save_fact pattern).
         - `ef = int(getattr(settings, "pgvector_ef_search_filtered", 200))`.
         - `await conn.execute("SET LOCAL hnsw.iterative_scan = 'strict_order'")`.
         - `await conn.execute(f"SET LOCAL hnsw.ef_search = {ef}")` — same GUC discipline as `get_relevant_facts:336-342`.
         - `row = await conn.fetchrow("""SELECT embedding <=> $3::vector AS dist FROM long_term_facts WHERE user_id=$1 AND tenant_id=$2 ORDER BY embedding <=> $3::vector LIMIT 1""", user_id, tenant_id, embedding)`.
         - If `row is None`: return `(False, None)`.
         - `dist = float(row["dist"])`.
         - Return `(dist < threshold, dist)`.
       - Note: ORDER BY...LIMIT 1 form (NOT `WHERE <threshold LIMIT 1`) so D-08 audit row can include actual nearest distance.

    2. Add `_fire_near_duplicate_audit` as a **staticmethod on the `LongTermMemory` class** (CQ2 from /plan-eng-review — locks the placement; tightly coupled to LongTermMemory.save_fact's audit emission so it lives next to its only caller). Signature: `@staticmethod\n    async def _fire_near_duplicate_audit(user_id: str, tenant_id: str, fact: str, dist: float | None) -> None`. Body per RESEARCH §Theme 3 lines 682-699:
       - Lazy imports: `from services.audit.audit_service import AUDIT_DETAIL_TRUNCATE_LEN, AuditAction, AuditEvent, AuditResult, get_audit_service`. **CQ3 from /plan-eng-review:** before authoring this method, add `AUDIT_DETAIL_TRUNCATE_LEN = 200` as a module-level constant in `services/audit/audit_service.py` (near `AuditAction` enum). Replace the hardcoded `200` everywhere it appears in the audit-emit code path with this constant. This becomes the project-wide convention for audit detail truncation.
       - `try: await get_audit_service().log(AuditEvent(user_id=user_id, tenant_id=tenant_id, action=AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED, resource_id="", result=AuditResult.SKIPPED, detail={"fact_truncated": fact[:AUDIT_DETAIL_TRUNCATE_LEN], "nearest_distance": dist}))`
       - `except Exception as exc:  # noqa: BLE001 — audit-write failure must NOT block (v1.6 GDPR T1 Pattern D)`
       - `    logger.warning("audit write failed (non-fatal): {}", exc)` — using loguru.logger (already imported at module top of memory_service.py).
       - PATTERNS.md §"Pattern D" lifts the bare `except Exception` ban for this specific audit-write boundary (v1.6 GDPR T1 carry-forward). Add the `# noqa: BLE001` comment so ruff knows it's intentional.

    3. Modify `LongTermMemory.save_fact` body (lines 359-396):
       - Keep Step 1 (embed) verbatim including try/except `(httpx.HTTPError, RuntimeError, OSError)` -> `MemoryFactWriteError("embedding failed")`.
       - Restructure Step 2 INSERT:
         - `pool = await self._get_pool()`.
         - `async with pool.acquire() as conn:`
         - Insert precheck BEFORE the INSERT:
           - `try: is_dup, dist = await self._is_near_duplicate(conn, user_id=user_id, tenant_id=tenant_id, embedding=embedding, threshold=settings.memory_near_duplicate_threshold)`
           - `except asyncpg.PostgresError as exc: logger.warning("near-duplicate precheck failed (fail-open): {}", exc); is_dup, dist = False, None` — fail-OPEN per RESEARCH §"Failure mode policy" lines 701-703.
           - If `is_dup`: `await _fire_near_duplicate_audit(user_id, tenant_id, fact, dist)` — best-effort; INSERT STILL RUNS (D-09).
         - Execute existing INSERT verbatim (preserve the SQL string and the outer try/except `asyncpg.PostgresError` -> `MemoryFactWriteError("persistence failed")`).
       - Note: the INSERT remains inside the `pool.acquire()` block but OUTSIDE any conn.transaction() (matching v1.6 baseline — save is non-transactional per RESEARCH §3 line 178). The precheck's `async with conn.transaction()` is internal to `_is_near_duplicate` and uses SET LOCAL semantics that auto-revert on transaction exit — correct.

    Per CLAUDE.md: no bare except outside the audit-write Pattern D exception, narrow exception types elsewhere preserved, no blocking I/O in async (pgvector + asyncpg async-compatible per RESEARCH §"register_vector(conn) is async-compatible"), structured logging via loguru, mypy --strict on the new helper signatures.

    DO NOT write tests in this task — Task 3 owns the test files for testability separation.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from services.memory.memory_service import LongTermMemory; assert hasattr(LongTermMemory, '_is_near_duplicate'), 'helper missing'; sig = inspect.signature(LongTermMemory._is_near_duplicate); params = list(sig.parameters); assert 'embedding' in params and 'threshold' in params, f'unexpected params: {params}'; print('save_fact precheck wiring: ok')" && uv run ruff check services/memory/memory_service.py && uv run pytest tests/unit/test_memory_save_fact.py -x --timeout 30 -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' services/memory/memory_service.py | grep -c "_is_near_duplicate"` returns >= 2 (definition + call site).
    - `grep -v '^#' services/memory/memory_service.py | grep -c "_fire_near_duplicate_audit\|MEMORY_NEAR_DUPLICATE_SKIPPED"` returns >= 2 (helper definition + AuditAction usage).
    - `grep -v '^#' services/memory/memory_service.py | grep -c "SET LOCAL hnsw.iterative_scan"` returns >= 2 (existing get_relevant_facts + new precheck).
    - `grep -v '^#' services/memory/memory_service.py | grep -c "memory_near_duplicate_threshold"` returns >= 1.
    - `uv run pytest tests/unit/test_memory_save_fact.py -x --timeout 30` exit 0 — existing save_fact unit tests still green (SC-3 acceptance "existing save unit tests still green").
    - `uv run ruff check services/memory/memory_service.py` passes.
    - `uv run mypy --strict services/memory/memory_service.py` — preserves baseline pass/fail.
    - Sanity grep: `grep -c "try:.*await.*_is_near_duplicate\|except asyncpg.PostgresError" services/memory/memory_service.py` shows the fail-open try/except exists around precheck.
  </acceptance_criteria>
  <done>LongTermMemory._is_near_duplicate + _fire_near_duplicate_audit defined; save_fact body invokes precheck inside acquire(); precheck fail-open on PostgresError; audit row emitted when near-dup detected; INSERT runs unconditionally (audit-mode-only D-09); pre-existing save tests still pass; ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for save_fact precheck behavior + fail-open + audit-mode-only semantics (D-09)</name>
  <files>tests/unit/memory/__init__.py, tests/unit/memory/test_save_fact_precheck.py, tests/unit/memory/test_save_fact_precheck_failure.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §Theme 3 (lines 612-703) + §Pitfall 5 "SC-3 vs D-09 wording trap" (lines 1079-1083) — the "audit row written AND duplicate row inserted" assertion bar
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"tests/unit/memory/test_save_fact_precheck.py — SC-3" (lines 258-286, mock helpers verbatim + dual-path embedder patch + D-09 audit-mode-only assertion list) + §"tests/unit/memory/test_save_fact_precheck_failure.py" (lines 288-290, fail-open parametrize)
    - tests/unit/test_memory_save_fact.py lines 25-188 (helper class `_AcquireCtx`, `_make_fake_pool`, `_make_long` — verbatim reuse; dual-path embedder patch pattern at lines 89-96)
    - tests/conftest.py — confirm pg_pool + Pattern A env-var setup conventions
  </read_first>
  <behavior>
    Test file 1 (`test_save_fact_precheck.py`):
    - Test 1 `test_precheck_emits_audit_when_near_duplicate_and_still_inserts` (CRITICAL — D-09 audit-mode-only assertion):
      - Mock embedder via dual-path patch returning a fixed vector.
      - Mock pool/conn where `conn.fetchrow` returns `{"dist": 0.02}` (below 0.05 threshold).
      - Mock `get_audit_service().log` as AsyncMock.
      - Spy on `conn.execute` for INSERT.
      - Call `save_fact`.
      - Assert: `audit_log_mock.call_count == 1` AND `audit_log_mock.call_args.args[0].action == AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED` AND `conn.execute` was called with the INSERT statement (INSERT STILL RUNS — D-09).
      - Specifically NOT asserting "save was skipped" — the v1.7 contract is the OPPOSITE.
    - Test 2 `test_precheck_no_audit_when_not_near_duplicate`:
      - `conn.fetchrow` returns `{"dist": 0.5}` (above threshold).
      - Audit NOT called; INSERT called.
    - Test 3 `test_precheck_no_audit_when_table_empty`:
      - `conn.fetchrow` returns `None`.
      - Audit NOT called; INSERT called.
    - Test 4 `test_precheck_adds_exactly_one_pg_rtt`:
      - Spy on `conn.fetchrow` (precheck SELECT) AND `conn.execute` calls.
      - Call save_fact once.
      - Assert: `conn.fetchrow.await_count == 1` (one precheck call). `conn.execute` call_count includes the SET LOCAL pair + INSERT — the SET LOCALs happen inside the precheck transaction, the INSERT happens outside. Pattern: precheck does 2 SET LOCAL `execute` + 1 `fetchrow` + commit; INSERT does 1 `execute`. The +1 RTT contract is about fetchrow (the data query), not about counting individual `execute` calls. Document this in the test docstring.
    - Test 5 `test_precheck_uses_per_user_tenant_filter`:
      - Inspect `conn.fetchrow` call args; assert the SQL string contains `user_id=$1 AND tenant_id=$2` and the params include the test user_id + tenant_id (verifies RLS contract from D-06).

    Test file 2 (`test_save_fact_precheck_failure.py`):
    - Parametrize over `[asyncpg.PostgresError, asyncpg.ConnectionDoesNotExistError, asyncpg.InterfaceError]` (analog `test_memory_save_fact.py:128-188` parametrize shape per PATTERNS.md):
      - Test `test_precheck_postgres_error_is_fail_open[<exc>]`:
        - `conn.fetchrow.side_effect = exc(...)`.
        - Spy on `conn.execute` (INSERT) and on loguru `logger.warning`.
        - Call save_fact.
        - Assert: function did NOT raise MemoryFactWriteError; logger.warning was called; `conn.execute` for the INSERT was called (INSERT proceeds — fail-open semantics).
    - Test `test_audit_log_failure_is_non_fatal`:
      - Mock `get_audit_service().log` to raise RuntimeError.
      - `conn.fetchrow` returns `{"dist": 0.02}` (near-dup hit).
      - Call save_fact.
      - Assert: function did NOT raise; INSERT still called; logger.warning logged the audit failure.
  </behavior>
  <action>
    Create `tests/unit/memory/__init__.py` (empty marker if dir doesn't exist).

    Create `tests/unit/memory/test_save_fact_precheck.py`:
    - Pattern A env-var setdefault at module top (APP_MODEL_DIR, SECRET_KEY).
    - Reuse helper classes verbatim from `test_memory_save_fact.py:50-80` (per PATTERNS.md lines 261-279):
      - `_AcquireCtx` async context manager.
      - `_make_fake_pool(execute_mock)` factory returning `(pool, conn)` — EXTEND `conn` to also have `fetchrow=AsyncMock(...)` and `transaction` returning an async context manager (per PATTERNS.md "conn needs fetchrow + transaction(). Mirror test_memory_recall_semantic.py:82-90").
      - `_make_long(pool)` factory.
    - Pattern B autouse `reset_memory_singleton` fixture (verbatim from `test_memory_save_fact.py:43-47`).
    - Pattern C dual-path embedder patch (verbatim from `test_memory_save_fact.py:89-96`):
      - `monkeypatch.setattr("services.vectorizer.embedder.get_embedder", lambda: fake)`
      - `monkeypatch.setattr("services.memory.memory_service.get_embedder", lambda: fake, raising=False)`
    - Audit service mock per PATTERNS.md line 300-302:
      - `mock_audit = MagicMock(log=AsyncMock())`
      - `monkeypatch.setattr("services.audit.audit_service.get_audit_service", lambda: mock_audit)`
    - Add the 5 behavior tests above. Each test uses `@pytest.mark.asyncio`.

    Create `tests/unit/memory/test_save_fact_precheck_failure.py`:
    - Same module-top setup (Pattern A + B + C).
    - Parametrize per PATTERNS.md line 290:
      - `@pytest.mark.parametrize("exc_cls", [asyncpg.PostgresError, asyncpg.ConnectionDoesNotExistError, asyncpg.InterfaceError])`
    - Add the 2 fail-open tests above.

    Per CLAUDE.md: mypy --strict on new test files (they import services internals — mypy may want type ignores for the MagicMock spies; permitted), ruff clean, no bare except.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/memory/test_save_fact_precheck.py tests/unit/memory/test_save_fact_precheck_failure.py -x -v --timeout 30 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/memory/test_save_fact_precheck.py -x` — all 5 tests pass.
    - `uv run pytest tests/unit/memory/test_save_fact_precheck_failure.py -x` — all parametrized tests pass (3 from the parametrize + 1 audit-failure test = 4 cases minimum).
    - CRITICAL D-09 assertion grep: `grep -A 3 -B 1 "test_precheck_emits_audit_when_near_duplicate_and_still_inserts" tests/unit/memory/test_save_fact_precheck.py | grep -c "execute.*INSERT\|conn.execute.*await_count\|conn.execute.*called"` returns >= 1 — the test explicitly asserts INSERT runs even on near-dup hit (NOT that save was skipped).
    - `grep -v '^#' tests/unit/memory/test_save_fact_precheck.py | grep -c "MEMORY_NEAR_DUPLICATE_SKIPPED"` returns >= 1.
    - `grep -v '^#' tests/unit/memory/test_save_fact_precheck.py | grep -c "user_id=\\$1 AND tenant_id=\\$2"` returns >= 1 (per-(user,tenant) filter assertion).
    - SC-3 RTT bound assertion present: `grep -c "fetchrow.*await_count.*==.*1\|fetchrow.call_count.*==.*1" tests/unit/memory/test_save_fact_precheck.py` returns >= 1.
    - `uv run pytest tests/unit/test_memory_save_fact.py -x --timeout 30` exit 0 (existing tests untouched and still green — SC-3 acceptance).
    - `uv run ruff check tests/unit/memory/` passes.
  </acceptance_criteria>
  <done>5 SC-3 precheck unit tests + 4+ fail-open parametrized tests; all assert D-09 audit-mode-only semantics (audit row emitted AND INSERT still runs — never "save skipped"); per-(user,tenant) filter verified; +1 RTT bound verified; existing test_memory_save_fact.py still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save_fact caller -> LongTermMemory PG pool | LLM-extracted fact content crosses into PG via parameterized INSERT (preserved); precheck adds a SELECT crossing the same boundary. |
| LongTermMemory -> AuditService | Audit-write is best-effort; failure here MUST NOT block save (v1.6 GDPR T1 carry-forward Pattern D). |
| Cross-tenant precheck | precheck SQL MUST filter on `(user_id, tenant_id)` before applying HNSW (D-06; RESEARCH §Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Information Disclosure | Cross-tenant leakage via precheck | mitigate | Every precheck query MUST filter on `user_id=$1 AND tenant_id=$2` BEFORE applying HNSW; SET LOCAL `hnsw.iterative_scan='strict_order'` ensures exact top-K under filter (v1.1 Phase 8 / v1.6 Phase 24 carry-forward). Test 5 in test_save_fact_precheck.py verifies SQL string + params. |
| T-27-03-02 | Repudiation | Audit log not written on near-dup | mitigate | Audit-write helper wraps the call in narrow exception catch (Pattern D); on failure, loguru warning logged with context. The save still proceeds (audit-write failure non-fatal per v1.6 GDPR T1). |
| T-27-03-03 | Tampering | Audit log poisoning via fact content | mitigate | `fact[:200]` truncation per Phase 25 RULE_BLOCKED convention; attacker-controlled fact field cannot bloat audit_log.detail. |
| T-27-03-04 | Denial of Service | Precheck SELECT slow path under high churn | accept | ~1-2ms per query on local pgvector with HNSW + GUC (RESEARCH §"Cost estimate"); SC-3 acceptance allows <=+1 RTT. 27-04 batch path collapses N saves into 1 RTT regardless. |
| T-27-03-05 | Information Disclosure | nearest_distance leaked via audit_log | accept | Distance is a float in [0, 2]; reveals semantic similarity but not the source fact's content. Stored only in audit_log.detail JSONB which is admin-readable. |
</threat_model>

<verification>
- `uv run pytest tests/unit/memory/test_save_fact_precheck.py tests/unit/memory/test_save_fact_precheck_failure.py -x -v --timeout 30` — all green.
- `uv run pytest tests/unit/test_memory_save_fact.py -x --timeout 30` — pre-existing save unit tests still green (SC-3 acceptance).
- `uv run python -c "from config.settings import settings; print(settings.memory_near_duplicate_threshold)"` prints `0.05`.
- `uv run python -c "from services.audit.audit_service import AuditAction; print(AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED.value)"` prints `MEMORY_NEAR_DUPLICATE_SKIPPED`.
- `grep -c "_is_near_duplicate" services/memory/memory_service.py` returns >= 2.
- `grep -c "MEMORY_NEAR_DUPLICATE_SKIPPED" services/memory/memory_service.py` returns >= 1.
- `uv run ruff check config/settings.py services/audit/audit_service.py services/memory/memory_service.py tests/unit/memory/` passes.
- `uv run mypy --strict config/settings.py services/audit/audit_service.py services/memory/memory_service.py` preserves baseline pass/fail.
</verification>

<success_criteria>
SC-3 of Phase 27 (per D-09 correction):
- `memory_near_duplicate_threshold` settings field (default 0.05, bounds [0.0, 1.0]) exists.
- `AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED` enum value appended after `MEMORY_EVICT`.
- `LongTermMemory.save_fact` runs cosine `<=>` precheck per-(user,tenant) with HNSW GUC discipline.
- When precheck distance < threshold: audit row emitted (best-effort) AND INSERT still runs (D-09 audit-mode-only — NOT silent skip).
- When precheck SELECT fails (PostgresError): warning logged, INSERT proceeds (fail-open).
- Audit-write failure is non-fatal (Pattern D); save still succeeds.
- +1 PG RTT cost per save_fact (1 SELECT + 1 INSERT = 2 PG operations on hot path).
- Existing `tests/unit/test_memory_save_fact.py` still passes — no behavioral regression on the happy path.
- v1.8 follow-up: silent-skip promotion tracked in SUMMARY.md and propagated to STATE.md.
</success_criteria>

<output>
Create `.planning/phases/27-test-isolation-memory-reliability/27-03-SUMMARY.md` capturing: LOC delta on memory_service.py, exact AuditAction enum tail, precheck SQL shape (ORDER BY ... LIMIT 1 confirmation), D-09 override callout for VERIFICATION.md author, the audit-row vs INSERT-still-runs assertion list, and "v1.8 follow-up: silent-skip rollout for MEMORY_NEAR_DUPLICATE_SKIPPED" added to STATE.md.
</output>
