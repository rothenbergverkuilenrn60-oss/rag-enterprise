# Milestone v1.8: Production Hardening Round 2

**Status:** ✅ SHIPPED 2026-05-17
**Phases:** 29–30
**Total Plans:** 6 shipped + 1 superseded (orchestrator-accepted override on Plan 30-01)

## Overview

Close v1.7-deferred hardening items — promote near-duplicate audit-mode to silent-skip (after closing the TOCTOU race), clean up 32 pre-existing openai SDK drift test failures, fix +14 event-loop singleton leaks exposed by the Phase 27 `uses_redis` marker rollout, resolve mypy `--strict` accumulation, rewrite `save_facts` precheck tests against the bulk-SELECT shape. Zero new user-facing capabilities — pure reliability + test infra polish.

**Carry-forward gates** (inherited from v1.7): `diff-cover ≥ 80%` on touched files; combined coverage `--fail-under=70`; INSERT-ONLY `audit_log` invariant; audit-mode-before-enforce (SK-01 promotes audit-mode → enforce per this discipline, post-TOC-01); audit-write failure must NOT block destructive action.

## Phases

### Phase 29: TOCTOU + Silent-Skip Enforcement

**Goal:** Close the precheck/INSERT race on `LongTermMemory.save_facts`, then promote v1.7 near-duplicate audit-mode (D-09) to silent-skip enforcement. Rewrite precheck unit tests against the bulk-SELECT shape (same code paths).

**Requirements:** TOC-01, SK-01, TEST-INFRA-02
**Depends on:** v1.7 audit-mode (D-09)
**Plans:** 3

**Success criteria:**
1. Two parallel `save_facts` writers with the same `(user_id, tenant_id)` + fact text produce exactly 1 row in `long_term_facts` (TOC-01 acceptance). TOCTOU mitigation choice (ON CONFLICT vs advisory-lock vs WITH ... RETURNING) locked at v1.8 discussion + documented in plan.
2. When `_is_near_duplicate` returns `True` for a candidate, the candidate is NOT included in `rows_to_insert`; `executemany` inserts only non-duplicate rows; `MEMORY_NEAR_DUPLICATE_SKIPPED` audit row still emitted. v1.7 pin test `test_dedupe_in_batch_fires_audit_AND_executemany_inserts_all_rows` flipped to `..._inserts_non_dup_rows_only` form. `save_fact` wrapper (D-12) inherits via delegation.
3. `save_facts` precheck unit tests rewritten against bulk-SELECT mock shape; `nearest_distance=None` branch covered explicitly; assertions match C1 SQL shape (`unnest($1::text[]) WITH ORDINALITY` + `vec_txt::vector` cast). Per-file LOC delta ≤ +150; no production-code changes from TEST-INFRA-02 alone.

**Plans:**
- [x] **29-00-PLAN.md** — TOC-01: advisory-lock wraps precheck+INSERT in `save_facts` (TDD, Wave 1) ✓ shipped 2026-05-17 (commits bc9c523, 23b0d18, 9892b72, bb45835)
- [x] **29-01-PLAN.md** — SK-01: silent-skip filter excludes dups from `rows_to_insert` (TDD, Wave 2, depends on 29-00) ✓ shipped 2026-05-17 (commits 44278ab, cf916e2, 5bbae8f, c1d7bfe). Note: integration test rewrite for the SK-01 contract finalized in commit e940280 (chore(29-01) — discovered during v1.8 close).
- [x] **29-02-PLAN.md** — TEST-INFRA-02: rewrite precheck unit tests to C1 bulk-SELECT shape (execute, Wave 3, depends on 29-00, 29-01) ✓ shipped 2026-05-17 (commits 12d6ed9, 0122b1e, ce14dea)

**Verification:** `.planning/phases/29-toctou-silent-skip-enforcement/29-VERIFICATION.md` — status `passed` (re-verified on docker rag-postgres pgvector/pgvector:pg16, PG 16.13, vector 0.8.2 at 2026-05-17T22:05). TOC-01 + GUC + SK-01 integration tests all PASSED.

---

### Phase 30: Test Infra + mypy Hardening

**Goal:** Clean up the test surface that's been masking real failures + finish the mypy --strict sweep. Fixes 32 openai-SDK-drift failures + 14 event-loop singleton leaks + 1 known-flaky `extractor_e2e` test + parametric-type annotations.

**Requirements:** OAI-01, EVT-01, TEST-INFRA-01, MYPY-01
**Depends on:** v1.7 marker rollout (Phase 27-02), v1.4 `create_app()` factory (TD-02 pattern)
**Plans:** 4 (Wave 1 → Wave 2 [overlap halt gate] → Wave 3)

**Success criteria:**
1. All 32 enumerated openai-SDK-drift unit tests pass with the new `APIError(request=...)` construction shape. `pytest tests/unit/ -m 'not benchmark'` on master post-fix shows green. No production-code changes (test-only) unless the test mirrors a production codepath (OAI-01).
2. Each of the +14 event-loop leak sites either migrates to the `create_app()` factory pattern or adds an explicit per-test loop fixture. Marker rollout (`@pytest.mark.uses_redis`) introduces zero regressions in integration suite. Curated `_SINGLETON_INVENTORY` in `tests/factories/app.py` grows from 34 to cover the +14 (EVT-01).
3. `uv run pytest tests/integration/test_extractor_e2e.py -v` passes on a clean checkout. Fix path documented in plan SUMMARY: either (a) earlier `embedder_or_mock` patching, (b) CI pre-download of bge-m3, or (c) direct mock of `HuggingFaceEmbedder.__init__` (TEST-INFRA-01).
4. `uv run mypy --strict config/settings.py` returns "Success: no issues found in 1 source file". Full-repo `uv run mypy --strict` scan: surfaced violations either fixed or explicitly silenced with `# type: ignore[error-code]` + comment justifying the silence (MYPY-01).

**Plans:**
- [x] **30-00-PLAN.md** — OAI-01: `make_api_error()` helper in `tests/factories/openai_errors.py` (DEVIATION: 32 callsites stale — no APIError construction failures on current master; executor pivoted to fix event-loop / Redis fixture leaks. Helper landed for future SDK drift. 16 RED → 1200 passed.) ✓ shipped 2026-05-17 (commits 030d774, 0c28ae9, f0a2d33, 8e681b2)
- [~] **30-01-PLAN.md** — EVT-01: +14 event-loop leak sites — **SUPERSEDED** by Plan 30-00 deviation. Plan 30-01 skipped by orchestrator decision (2026-05-17T21:11:00Z). 30-00 fixed ~4 sites as scope pivot. Remaining ~10 sites deferred to v1.9 hardening (require PG-enabled host for enumeration). `_SINGLETON_INVENTORY` remains at 34 (not grown to 48). **Accepted override** documented in `30-VERIFICATION.md` + `v1.8-MILESTONE-AUDIT.md`.
- [x] **30-02-PLAN.md** — TEST-INFRA-01: mock `HuggingFaceEmbedder` + `CrossEncoderReranker` init at `tests/integration/conftest.py` (Rule-2 deviation: reranker also raises FileNotFoundError on bge-m3-rerank — both mocked in same autouse fixture) ✓ shipped 2026-05-17 (commits 4cbb4e0, 7b5c6e5)
- [x] **30-03-PLAN.md** — MYPY-01: fix `config/settings.py:154` + bounded sweep cap=25 — 32→7 mypy errors (NET -25); 1 fix + 25 silenced with `# type: ignore[error-code]` + `# why:`; 7 overflow → `deferred-items.md` ✓ shipped 2026-05-17 (commits 3736b62, 2f67cd7, a9db41d, ee3273c)

**Verification:** `.planning/phases/30-test-infra-mypy-hardening/30-VERIFICATION.md` — status `passed` (re-verified on docker rag-postgres at 2026-05-17T22:10). `extractor_e2e` 2/2 PASSED; full integration suite 9 failed / 32 passed / 1 skipped / 3 errors — all failures triaged as pre-existing real-LLM / perf / UI / schema-drift, none caused by Phase 30 mock fixture.

---

## Milestone Summary

**Audit:** `.planning/milestones/v1.8-MILESTONE-AUDIT.md` — status `passed`, 7/7 requirements satisfied, 1 accepted override, 6/6 cross-phase wiring connections.

### Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| TOC-01 mitigation: advisory lock (option b) | Schema-migration-free; `pg_advisory_xact_lock` available out-of-the-box on PG 16; lock hash uses `hashtext($1 \|\| '\|' \|\| $2)` with explicit separator to prevent prefix collision | ✓ Good — COUNT(*)==1 confirmed live on PG host |
| SK-01 implementation: filter `rows_to_insert` before executemany | Single-point change at line ~745-748 of `memory_service.py`; preserves audit emission; minimal surface area | ✓ Good — integration test confirmed silent-skip post-rewrite |
| OAI-01 scope pivot to event-loop fixture cleanup | 32 callsite count was stale (Phase 26 CI snapshot); fix moved to where pain actually lived; helper still landed for future drift | ✓ Good — 1200 unit tests green |
| EVT-01 plan 30-01 skipped | After 30-00 pivot fixed ~4 sites; remaining sites need PG-enabled enumeration; risk of churn without payoff in v1.8 | — Pending — re-evaluate in v1.9 with PG host |
| MYPY-01 bounded sweep cap=25 | Cap forces discipline on silence rationales (each requires `# why:` per the convention); overflow captured in `deferred-items.md` | ✓ Good — 25 disciplined silences applied; 7 deferred |

### Issues Resolved

- **TOCTOU race window** between precheck SELECT and `executemany` INSERT — closed via per-(user_id, tenant_id) advisory lock; confirmed under live concurrent writers
- **Near-duplicate silent-skip enforcement** — SK-01 filter excludes duplicates from `rows_to_insert`; audit row still fires for observability
- **`extractor_e2e` flakiness** — autouse `tests/integration/conftest.py` mocks both `HuggingFaceEmbedder.__init__` and `CrossEncoderReranker.__init__`; no `bge-m3` download required
- **`config/settings.py:154` mypy violation** — `list[dict] → list[dict[str, Any]]`
- **mypy --strict baseline** — 32 errors reduced to 7 deferred (NET -25 via disciplined silence convention)
- **Stale D-09 integration test** (`test_save_facts_with_near_duplicate_emits_audit_and_still_inserts_real_pg`) — discovered during v1.8 close; rewritten to SK-01 contract (`..._and_skips_silently_real_pg`) in commit e940280

### Issues Deferred to v1.9

- **EVT-01 residual** — ~10 event-loop singleton leak sites; `_SINGLETON_INVENTORY` to grow from 34 toward 48. Enumeration requires PG-enabled host. Plan 30-01 superseded; accepted orchestrator override.
- **MYPY-01 overflow** — 7 violations captured in `.planning/phases/30-test-infra-mypy-hardening/deferred-items.md` (cap=25 exhausted in v1.8).
- **Bare `# type: ignore` at `services/nlu/nlu_service.py:538`** — pre-existing since v1.3/v1.6; add `[attr-or-return-code]  # why:` per convention.
- **`tests/integration/memory/test_save_facts_toctou.py:32, 57`** — asyncpg + pgvector.asyncpg imports need `# type: ignore[import-untyped]  # why:` (Phase 30 boundary missed these because the test file lives in Phase 29 surface).
- **`tests/integration/conftest.py` autouse mock has no opt-out** — future real-embedder tests will be silently neutered. Add `@pytest.mark.real_embedder` marker + conditional skip/unfixture in v1.9.

### Technical Debt Acknowledged (predate v1.8)

- 7 pre-existing order-dependent unit-test failures (registry-singleton pollution + `embed_one` vs `embed_batch` mock mismatch) — all pass in isolation.
- `test_pipeline_load_context_audit::test_no_v1_5_regression` — uses `q=` parameter but `GenerationRequest` requires `query=` (schema-drift since the rename).
- `test_ui_static::test_ui_static_serves_html` — asserts `<title>RAG 查询</title>` sentinel but the current page renders `<title>Agent 查询</title>` (drift since v1.4 UI title change).

### Nyquist VALIDATION.md

Both phases shipped without `*-VALIDATION.md` artifacts. Flagged in audit as `nyquist: missing` for v1.9 process polish — not a v1.8 blocker.

---

_For current project status, see .planning/ROADMAP.md_
