---
phase: 19-agent-first-docs-demo-release
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - docs/v1.4-design.md
autonomous: true
requirements: [AGENT-08]
must_haves:
  truths:
    - "CHANGELOG.md exists at repo root in keep-a-changelog 1.1.0 format"
    - "CHANGELOG entries reverse-chronological: v1.4.0 → v1.3.0 → v1.2.0 → v1.1.0 → v1.0.0"
    - "v1.4.0 entry uses formal Added/Changed categories (D-14 Claude's Discretion default)"
    - "v1.0..v1.3 entries use free-form bullets, each linking the archived `.planning/milestones/vX.Y-ROADMAP.md`"
    - "docs/v1.4-design.md exists; verbatim copy of `~/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md` (D-16)"
    - "docs/v1.4-design.md security review pass: no real customer names, no internal infrastructure paths, no real tenant identifiers"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "keep-a-changelog 1.1.0 history; entry per milestone"
      contains: "## [1.4.0]"
    - path: "docs/v1.4-design.md"
      provides: "v1.4 milestone design doc (Approach A — incremental refactor, no framework lock-in)"
      contains: "Agent-First Architecture Inversion"
  key_links:
    - from: "CHANGELOG.md::## [1.4.0]"
      to: "docs/v1.4-design.md"
      via: "markdown link"
      pattern: "\\[v1.4 design\\]\\(docs/v1.4-design.md\\)"
    - from: "CHANGELOG.md::## [1.4.0]"
      to: ".planning/phases/{16,17,18,19}-*/SUMMARY"
      via: "phase summary links"
      pattern: ".planning/phases/.*-SUMMARY\\.md"
---

<objective>
Two independent artifacts; both required for plan 19-06 (README) and plan 19-08 (release tag).

(1) `CHANGELOG.md` — fresh keep-a-changelog 1.1.0 file at repo root with v1.0..v1.4
    entries in reverse-chronological order. v1.4 uses formal categories;
    v1.0..v1.3 use free-form bullets each linking the archived milestone roadmap.

(2) `docs/v1.4-design.md` — verbatim copy of the gstack milestone design doc
    so external readers can follow the architectural-inversion thesis without
    needing the gstack project tree.

Purpose: D-13 (release-notes link target), D-14 (CHANGELOG creation), D-16
(in-repo design doc copy).

Output: 2 new files at repo root + docs/. No code; no tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md
@.planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md
@.planning/milestones/v1.0-ROADMAP.md
@.planning/milestones/v1.1-ROADMAP.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.3-ROADMAP.md

<interfaces>
<!-- keep-a-changelog 1.1.0 format reference. -->

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [X.Y.Z] - YYYY-MM-DD

### Added
- Item.

### Changed
- Item.
```

<!-- gstack design doc source path (D-16). -->

`/home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md` (13.3K, verified to exist at planning time).

<!-- Existing milestone roadmap link targets (verified to exist at planning time). -->

- `.planning/milestones/v1.0-ROADMAP.md` (9.9K)
- `.planning/milestones/v1.1-ROADMAP.md` (10.7K)
- `.planning/milestones/v1.2-ROADMAP.md` (4.6K)
- `.planning/milestones/v1.3-ROADMAP.md` (9.3K)

<!-- Existing v1.0..v1.3 phase descriptions for CHANGELOG bullets — extracted from .planning/ROADMAP.md lines 13-58. -->

v1.0 (Phases 1-6): pgvector Foundation, Security Hardening + Operational Fixes, Error Handling Sweep, Image Extraction, Async Ingest Tracking, Test Coverage and Eval. Shipped 2026-04-27.

v1.1 (Phases 7-10): OCR Engine Integration, Multimodal Metadata + Query Filter, Frontend Extraction, Coverage Gate on New Code. Shipped 2026-05-08.

v1.2 (Phase 11): Provider-Agnostic Agentic Layer + Parallel Tool-Call Burst. Shipped 2026-05-08.

v1.3 (Phases 12-15): Fork-Agent Swarm, LLM Filter Fallback, Frontend Split and DOM Modernization, Coverage Combine and 70% Floor. Shipped 2026-05-09.

v1.4 (Phases 16-19): Planner + Executor Extraction (AGENT-06, AGENT-09, NLU-03), Tool Abstraction + RetrieveTool (AGENT-07), SSE Planner Trace Event Stream (AGENT-04), Agent-First Docs + Demo + Release (AGENT-08). Shipping 2026-05-09.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md (keep-a-changelog 1.1.0; v1.0..v1.4 reverse-chron)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 5-58 — milestone summary table)
    - .planning/milestones/v1.0-ROADMAP.md (top heading + 1-paragraph summary — surface for v1.0 bullets)
    - .planning/milestones/v1.1-ROADMAP.md (top heading + summary)
    - .planning/milestones/v1.2-ROADMAP.md (top heading + summary)
    - .planning/milestones/v1.3-ROADMAP.md (top heading + summary)
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-14 (CHANGELOG entry shape; default = free-form bullets per version, formal categories only for v1.4)
    - .planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md §6 (CHANGELOG.md adaptation notes; format pattern at lines 244-272)
    - .planning/phases/16-planner-executor-extraction/16-03-SUMMARY.md (verify path exists for v1.4 link)
    - .planning/phases/17-tool-abstraction-retrievetool/17-03-SUMMARY.md (verify path exists)
    - .planning/phases/18-sse-planner-trace-event-stream/18-03-SUMMARY.md (verify path exists)
  </read_first>
  <action>
    Create `CHANGELOG.md` at the repo root. Verbatim content (substitute the actual repo URL placeholder `<repo-url>` per the comment at the bottom — leave as `<owner>/<repo>` if the canonical URL is unknown; plan 19-08 will finalize the compare-link footer at release time):

    ````markdown
    # Changelog

    All notable changes to this project will be documented in this file.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    ## [1.4.0] - 2026-05-09

    Agent-First Architecture Inversion. The agent runtime (Planner / Executor /
    Synthesizer) is now the project's core; agentic RAG is one tool the agent
    calls. See [v1.4 design](docs/v1.4-design.md) for the architectural thesis.

    ### Added
    - Phase 16 — Planner + Executor extraction. `services/agent/{planner,executor}.py`; `ToolPlan` / `ToolCall` Pydantic V2 frozen models; query intent encoded as `ToolPlan` shape (no separate `IntentRouter`); shared `execute_tool_call` helper used by `SwarmQueryPipeline` and `Executor`. Closes AGENT-06, AGENT-09, NLU-03. See [Phase 16 SUMMARY](.planning/phases/16-planner-executor-extraction/16-03-SUMMARY.md).
    - Phase 17 — Tool abstraction. `BaseTool` ABC + static `ToolRegistry` + `RetrieveTool` (wraps `QueryPipeline.run`) + `RefinedRetrieveTool` + `WebSearchTool` placeholder. `Executor` dispatches strictly through the registry. Closes AGENT-07. See [Phase 17 SUMMARY](.planning/phases/17-tool-abstraction-retrievetool/17-03-SUMMARY.md).
    - Phase 18 — SSE event stream on `POST /api/v1/agent/v1/run/stream`. Six event types: `planner.plan`, `tool.span.start`, `tool.span.end`, `tool.span.error`, `executor.parallel`, `synthesizer.final`. Latency assertion: parallel groups bounded by `max(tool_latency)` not sum. Closes AGENT-04. See [Phase 18 SUMMARY](.planning/phases/18-sse-planner-trace-event-stream/18-03-SUMMARY.md).
    - Phase 19 — Agent-first docs + demo + release. `docs/agent-architecture.md` Concept → Tool authoring → Wire format trilogy; `make demo-agent` (4-tool parallel fan-out, fixture-driven, no API keys); asciinema cast embedded in README. Closes AGENT-08. See [Phase 19 SUMMARY](.planning/phases/19-agent-first-docs-demo-release/19-08-SUMMARY.md).

    ### Changed
    - Architectural inversion: agent is the core, RAG is one tool. README rewritten with agent-first framing; v1.3 technical content preserved under `## Platform features`.
    - `AgentQueryPipeline.run` body delegates to `Planner` → `Executor` → `Synthesizer`; thin orchestrator (43 lines, ≤ 50 line gate).
    - `_execute_tool_call` consolidated to a single shared helper; both `SwarmQueryPipeline` and `Executor` call it (no copy duplicates).

    ## [1.3.0] - 2026-05-09

    Fork Swarm, NLU & Quality. See [v1.3 milestone roadmap](.planning/milestones/v1.3-ROADMAP.md).

    - Phase 12 — Fork-Agent Swarm: `SwarmQueryPipeline` with sub-agent fork via `call_agentic_turn`; sub-agents do NOT inherit chat history (true context isolation).
    - Phase 13 — LLM Filter Fallback: regex-first filter extractor with LLM fallback when regex fails or yields ambiguous filters.
    - Phase 14 — Frontend Split and DOM Modernization: `static/index.html` decomposed; FastAPI StaticFiles mount on `/ui/`.
    - Phase 15 — Coverage Combine and 70% Floor: combined unit+integration coverage report; `--fail-under=70` global floor enforced in CI.

    ## [1.2.0] - 2026-05-08

    Agentic Layer + Swarm. See [v1.2 milestone roadmap](.planning/milestones/v1.2-ROADMAP.md).

    - Phase 11 — Provider-Agnostic Agentic Layer + Parallel Tool-Call Burst: `BaseLLMClient.call_agentic_turn` non-abstract default-raise; `parallel_tool_calls=True` (OpenAI) / `disable_parallel_tool_use=False` (Anthropic) explicit; `asyncio.gather` for concurrent tool execution.

    ## [1.1.0] - 2026-05-08

    Retrieval Depth & Frontend. See [v1.1 milestone roadmap](.planning/milestones/v1.1-ROADMAP.md).

    - Phase 7 — OCR Engine Integration: PaddleOCR fallback for image-only PDFs.
    - Phase 8 — Multimodal Metadata + Query Filter: section heading text in embedded content; numeric IDs in metadata only; regex-first filter extractor; HNSW `iterative_scan = strict_order` + `ef_search` GUC pattern when filter active.
    - Phase 9 — Frontend Extraction: FastAPI StaticFiles mount at `/ui/`; `static/index.html → ui.html` symlink.
    - Phase 10 — Coverage Gate on New Code: `diff-cover ≥ 80%` gate on touched files (TEST-03) on every PR.

    ## [1.0.0] - 2026-04-27

    Initial release — production-grade RAG platform. See [v1.0 milestone roadmap](.planning/milestones/v1.0-ROADMAP.md).

    - Phase 1 — pgvector Foundation: PostgreSQL + pgvector backend with HNSW + Row-Level Security (multi-tenant by construction).
    - Phase 2 — Security Hardening + Operational Fixes: JWT startup validation, per-route rate limiting, PII blocking, CORS lockdown.
    - Phase 3 — Error Handling Sweep: no bare `except`; narrow exception types; structured logging on every error path.
    - Phase 4 — Image Extraction: PDF-embedded images extracted via PyMuPDF, captioned by LLM, stored as retrievable `chunk_type="image"` chunks. PyMuPDF AGPL-3.0 license note.
    - Phase 5 — Async Ingest Tracking: `POST /ingest/async` returns `task_id`; ARQ/Redis worker; poll status via `GET /ingest/status/{task_id}`.
    - Phase 6 — Test Coverage and Eval: 200 stratified RAGAS QA pairs; `faithfulness > 0.85`, `answer_relevancy > 0.80` gates.

    [Unreleased]: https://github.com/<owner>/<repo>/compare/v1.4.0...HEAD
    [1.4.0]:      https://github.com/<owner>/<repo>/compare/v1.3.0...v1.4.0
    [1.3.0]:      https://github.com/<owner>/<repo>/compare/v1.2.0...v1.3.0
    [1.2.0]:      https://github.com/<owner>/<repo>/compare/v1.1.0...v1.2.0
    [1.1.0]:      https://github.com/<owner>/<repo>/compare/v1.0.0...v1.1.0
    [1.0.0]:      https://github.com/<owner>/<repo>/releases/tag/v1.0.0
    ````

    Notes:
    - Heading dates use ISO 8601 `YYYY-MM-DD` per spec.
    - v1.4 entry uses formal `### Added` / `### Changed` categories (D-14 default for v1.4 entry).
    - v1.0..v1.3 entries use free-form bullets per version (D-14 default).
    - Each version after v1.0 references the prior milestone via the milestone roadmap link.
    - Phase 19 SUMMARY link points to the not-yet-existent `19-08-SUMMARY.md` — plan 19-08 creates it. The link will resolve once plan 19-08 ships. Acceptable: forward-reference within a release-coupled commit set.
    - The compare-link footer uses `<owner>/<repo>` placeholders. Plan 19-08 (release tag) replaces these with the canonical URL once known. The placeholder warning is documented inline.
  </action>
  <verify>
    <automated>test -s CHANGELOG.md && grep -E "^## \\[(Unreleased|1\\.[0-4]\\.0)\\]" CHANGELOG.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `CHANGELOG.md` exists at repo root, ≥ 50 lines.
    - Format header present: `grep -c "Keep a Changelog" CHANGELOG.md` returns 1; `grep -c "Semantic Versioning" CHANGELOG.md` returns 1.
    - 6 version headings: `grep -cE "^## \\[(Unreleased|1\\.[0-4]\\.0)\\]" CHANGELOG.md` returns 6 ([Unreleased], [1.4.0], [1.3.0], [1.2.0], [1.1.0], [1.0.0]).
    - Reverse-chronological order verified: `grep -nE "^## \\[1\\.[0-4]\\.0\\]" CHANGELOG.md | awk -F: '{print $2}' | head -5` shows `[1.4.0]`, `[1.3.0]`, `[1.2.0]`, `[1.1.0]`, `[1.0.0]` in this order.
    - v1.4 formal categories: `grep -c "^### Added$" CHANGELOG.md` returns 1; `grep -c "^### Changed$" CHANGELOG.md` returns 1.
    - All four phase SUMMARY links present in v1.4 entry: `grep -cE "\\.planning/phases/(16|17|18|19).*SUMMARY\\.md" CHANGELOG.md` returns 4.
    - All four milestone roadmap links present (v1.0..v1.3): `grep -cE "\\.planning/milestones/v1\\.[0-3]-ROADMAP\\.md" CHANGELOG.md` returns 4.
    - v1.4 design doc link: `grep -c "docs/v1.4-design.md" CHANGELOG.md` returns 1.
    - Compare-link footer present: `grep -cE "^\\[1\\.[0-4]\\.0\\]:\\s+https://github.com" CHANGELOG.md` returns 5; `grep -cE "^\\[Unreleased\\]:\\s+https" CHANGELOG.md` returns 1.
    - All 4 v1.4 phase requirement IDs cited (AGENT-04, AGENT-06, AGENT-07, AGENT-08, AGENT-09, NLU-03): `grep -cE "AGENT-0[4-9]|NLU-03" CHANGELOG.md` returns ≥ 6.
    - Commit message: `docs(19-07-T1): add CHANGELOG.md (keep-a-changelog v1.0..v1.4)`.
  </acceptance_criteria>
  <done>CHANGELOG.md committed at repo root with all 5 release entries + Unreleased section + compare-link footer.</done>
</task>

<task type="auto">
  <name>Task 2: Copy gstack milestone design doc to docs/v1.4-design.md (verbatim, with status banner update)</name>
  <files>docs/v1.4-design.md</files>
  <read_first>
    - /home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md (the source — read full file; security review for embedded credentials, real customer names, internal infrastructure paths)
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-13, D-16 (link target + canonical path + status banner update)
    - .planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md §5 (file 5 — adaptation notes; naming convention; transformations needed)
  </read_first>
  <action>
    Three-step copy:

    **Step 1 — Read the source doc.**

    Read `/home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md` in full. Inspect for:
    - Embedded credentials (API keys, JWT samples, internal SSH keys) — must be 0 occurrences.
    - Real customer / tenant names — must be 0 occurrences (project is open-source).
    - Internal infrastructure paths beyond the repo working tree (e.g., `/srv/internal-prod/`, `/var/lib/customers/`) — must be 0 occurrences.

    Run grep redaction gates on the source:
    ```bash
    grep -cE "sk-[a-zA-Z0-9]{20,}|Bearer [A-Za-z0-9._\\-]{20,}|ANTHROPIC_API_KEY=[^[:space:]\"]+|OPENAI_API_KEY=[^[:space:]\"]+" /home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md
    grep -cE "/srv/internal|/var/lib/customers|/etc/private" /home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md
    ```

    Each MUST return 0. If non-zero, STOP and surface the offending content for redaction before copying.

    **Step 2 — Write to docs/v1.4-design.md.**

    Copy the entire source file content to `docs/v1.4-design.md`. Preserve every paragraph, every code block, every section heading, every link target verbatim. Two minor transforms allowed:

    1. **Status banner update** — if the source's top-of-file status block says "Status: DRAFT" or "Status: PROPOSAL" or similar, change to:
       ```
       *Status: SHIPPED in v1.4 (2026-05-09). Approach A — incremental refactor, no framework lock-in. Phases 16–19 implementation: see [CHANGELOG.md](../CHANGELOG.md).*
       ```
       If the source already has a "Status" line, REPLACE just that line — leave all other content unchanged.

    2. **Internal-link adjustment** — the source uses bare `services/pipeline.py`-style references that resolve relative to the repo root. Since `docs/v1.4-design.md` lives one level deep, these resolve correctly via GitHub's repo-root rendering. NO change needed unless the source uses absolute paths beginning with `/home/` — in that case, replace with repo-relative paths. Run:
       ```bash
       grep -E "^/home/|/home/.*\\.md" docs/v1.4-design.md
       ```
       Must return 0. If non-zero, fix manually.

    DO NOT add new content. DO NOT reorganize. DO NOT condense. The doc IS the milestone artifact verbatim.

    **Step 3 — Sanity-check the copy.**

    Compare line count to source (should be ≈ equal, ±1 for the status banner change):
    ```bash
    wc -l /home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md docs/v1.4-design.md
    ```

    Diff the two for unexpected differences:
    ```bash
    diff /home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md docs/v1.4-design.md
    ```

    Expected diff: only the Status line (1-2 lines changed) and possibly absolute-path replacements.
  </action>
  <verify>
    <automated>test -s docs/v1.4-design.md && grep -c "Agent-First\\|Approach A\\|Planner\\|Executor\\|Synthesizer" docs/v1.4-design.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/v1.4-design.md` exists, ≥ 100 lines (source is 13.3K — substantial doc).
    - Line count within ±5 lines of the source: `python3 -c "src=sum(1 for _ in open('/home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md')); dst=sum(1 for _ in open('docs/v1.4-design.md')); assert abs(src-dst) <= 5, (src, dst)"` exits 0.
    - Content keywords preserved: `grep -cE "Agent-First|Planner|Executor|Synthesizer|Approach A" docs/v1.4-design.md` returns ≥ 5.
    - Redaction gates on the COPY (must match source — 0 matches expected): `grep -cE "sk-[a-zA-Z0-9]{20,}|Bearer [A-Za-z0-9._\\-]{20,}|ANTHROPIC_API_KEY=|OPENAI_API_KEY=" docs/v1.4-design.md` returns 0; `grep -cE "/srv/internal|/var/lib/customers|/etc/private" docs/v1.4-design.md` returns 0; `grep -cE "^/home/" docs/v1.4-design.md` returns 0.
    - Status banner updated: `grep -c "Status: SHIPPED in v1.4" docs/v1.4-design.md` returns 1 (or matches the agreed banner phrasing).
    - Diff against source shows ≤ 5 line changes (status banner + any absolute-path fixes): `diff /home/ubuntu/.gstack/projects/rothenbergverkuilenrn60-oss-rag-enterprise/ubuntu-gsd-v1.3-milestone-design-20260509-163809.md docs/v1.4-design.md | grep -cE "^[<>]" | awk '{print $1; exit ($1 > 10 ? 1 : 0)}'` exits 0 (≤ 10 changed lines).
    - Commit message: `docs(19-07-T2): add docs/v1.4-design.md (verbatim copy of gstack milestone design)`.
  </acceptance_criteria>
  <done>docs/v1.4-design.md committed; verbatim copy of source with status banner updated to SHIPPED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source design doc → public repo | Once committed, docs/v1.4-design.md is publicly accessible; any embedded secrets / customer names / internal paths leak. |
| CHANGELOG.md → public repo | CHANGELOG references all milestone summaries; broken-link risk if archived `vX.Y-ROADMAP.md` paths are wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07-01 | Information Disclosure | Source design doc embedding API keys / JWT samples | mitigate | Step 1 redaction gate runs grep against the source BEFORE copying; if any match, STOP. Step 3 diff confirms the copy contains no NEW credentials. |
| T-19-07-02 | Information Disclosure | Source design doc referencing real customer / tenant names | mitigate | Step 1 manual security review (read full doc); explicit grep gate for known-real prefixes (`/srv/internal`, `/var/lib/customers`). User-actionable surface if matches found. |
| T-19-07-03 | Tampering | CHANGELOG.md compare-link footer pointing to wrong repo | mitigate | Footer uses `<owner>/<repo>` placeholder; plan 19-08 substitutes the canonical URL at release-tag time. Documented in plan 19-08's release-tag drafting task. |
| T-19-07-04 | Tampering | Phase 19 SUMMARY link in CHANGELOG points to a not-yet-created file | accept | Forward-reference within a release-coupled commit set. Plan 19-08 creates the SUMMARY before the v1.4 tag is cut; users following the link before plan 19-08 lands see a 404. Acceptable since CHANGELOG ships in the same milestone PR. |
| T-19-07-05 | Information Disclosure | Verbatim doc copy preserving internal terminology | accept | The source doc was authored as a public-facing design artifact (Approach A, no framework lock-in). Manual security review in Step 1 catches any oversights. If borderline content is found, surface for user redaction before commit. |
</threat_model>

<verification>
- `CHANGELOG.md` exists at repo root with all 5 release entries (v1.0..v1.4) + [Unreleased] section + compare-link footer.
- `docs/v1.4-design.md` exists; verbatim copy of source minus the status banner; line count ±5 of source.
- Redaction gates pass on both files: 0 API keys, 0 real tenant IDs, 0 internal infrastructure paths.
- All cross-link targets resolve: phase SUMMARYs (16/17/18 — 19's is forward-reference), milestone roadmaps (v1.0..v1.3), docs/v1.4-design.md ↔ CHANGELOG.md.
</verification>

<success_criteria>
- 2 new files at repo root + docs/.
- Both pass redaction gates.
- CHANGELOG follows keep-a-changelog 1.1.0; v1.4 uses formal categories per D-14.
- docs/v1.4-design.md is a faithful copy of the gstack source per D-16.
- 2 atomic commits.
</success_criteria>

<output>
After completion, create `.planning/phases/19-agent-first-docs-demo-release/19-07-SUMMARY.md`
</output>
