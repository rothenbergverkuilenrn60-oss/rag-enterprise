---
phase: 19-agent-first-docs-demo-release
plan: 02
type: tdd
wave: 2
depends_on: [19-01]
files_modified:
  - tests/integration/test_demo_agent.py
  - services/agent/_demo_runner.py
autonomous: true
requirements: [AGENT-08]
must_haves:
  truths:
    - "AgentQueryPipeline.run_streaming over the demo stubs emits exactly 11 events: 1 PlannerPlanEvent + 4 ToolSpanStartEvent + 4 ToolSpanEndEvent + 1 ExecutorParallelEvent(fan_out=4) + 1 SynthesizerFinalEvent. Terminal-plan iteration emits no PlannerPlanEvent (gated by `len(plan.steps) > 0` per services/pipeline.py:887). Single canonical assertion — no ceiling/floor split."
    - "Total elapsed wall-time bounded by max(tool_latency)+overhead, NOT sum: 450 < elapsed_ms < 700 for 4×0.5s parallel"
    - "ExecutorParallelEvent.fan_out == 4 (single 4-way parallel group)"
    - "services/agent/_demo_runner.py exists, is invocable as `python -m services.agent._demo_runner`, prints SSE-formatted event lines, exits 0"
  artifacts:
    - path: "tests/integration/test_demo_agent.py"
      provides: "End-to-end demo correctness gate (in-process variant + subprocess variant)"
      contains: "def test_demo_runner_emits_expected_event_sequence"
    - path: "services/agent/_demo_runner.py"
      provides: "CLI entrypoint for `make demo-agent` — wires demo stubs + iterates AgentQueryPipeline.run_streaming + prints SSE frames"
      contains: "def main"
  key_links:
    - from: "services/agent/_demo_runner.py"
      to: "services.agent._demo_stubs"
      via: "imports DemoStubPlanner, make_fake_retrieve_tool, build_demo_registry, DEMO_QUERY"
      pattern: "from services.agent._demo_stubs import"
    - from: "services/agent/_demo_runner.py"
      to: "services.pipeline.AgentQueryPipeline"
      via: "instantiates pipeline + iterates run_streaming"
      pattern: "AgentQueryPipeline\\(\\)"
    - from: "tests/integration/test_demo_agent.py"
      to: "services.agent._demo_runner"
      via: "imports run_demo or invokes via subprocess"
      pattern: "from services.agent._demo_runner"
---

<objective>
Build the demo correctness gate. The integration test IS the demo's contract: invoke
`AgentQueryPipeline.run_streaming` over the Phase 18 stub fixtures (now in plan 19-01's
`_demo_stubs.py`) and assert the exact event sequence + latency bound. Then build the
runner module that executes that same code path and prints SSE-formatted lines to stdout
for `make demo-agent` (plan 19-03) to invoke.

Purpose: lock SC3 (`make demo-agent` exits 0 + SSE log to stdout) and SC4 (parallel
fan-out visible) at the test level. The Make target in plan 19-03 simply invokes this
runner — no new logic.

Output: 1 integration test file + 1 runner module. TDD: tests first (RED), runner
second (GREEN). REFACTOR optional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md
@.planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md
@.planning/phases/19-agent-first-docs-demo-release/19-01-SUMMARY.md
@.planning/phases/18-sse-planner-trace-event-stream/18-03-SUMMARY.md
@tests/unit/test_agent_sse.py
@services/agent/_demo_stubs.py
@controllers/api.py

<interfaces>
<!-- Pipeline + event types the runner + test consume. -->

From services/pipeline.py (existing):
```python
class AgentQueryPipeline:
    async def run_streaming(self, req: GenerationRequest) -> AsyncIterator[AgentEvent]: ...
```

From utils/models.py (Phase 18 events):
```python
class PlannerPlanEvent(AgentEvent):
    event_type: ClassVar[str] = "planner.plan"
    plan: ToolPlan

class ToolSpanStartEvent(AgentEvent):
    event_type: ClassVar[str] = "tool.span.start"
    span_id: str
    name: str
    args: dict[str, Any]

class ToolSpanEndEvent(AgentEvent):
    event_type: ClassVar[str] = "tool.span.end"
    span_id: str
    latency_ms: int
    chunk_count: int
    is_error: bool
    content_preview: str

class ExecutorParallelEvent(AgentEvent):
    event_type: ClassVar[str] = "executor.parallel"
    fan_out: int
    group_latency_ms: int

class SynthesizerFinalEvent(AgentEvent):
    event_type: ClassVar[str] = "synthesizer.final"
    answer: str
    sources_count: int
```

From controllers/api.py (existing SSE wire-format pattern, ~line 282):
```python
yield f"event: {evt.event_type}\ndata: {evt.model_dump_json()}\n\n"
```

From plan 19-01 (services/agent/_demo_stubs.py):
```python
DEMO_QUERY: Final[str]
DEMO_KB_SHARDS: Final[tuple[str, ...]]
class DemoStubPlanner: ...
def make_fake_retrieve_tool(name: str, sleep_s: float, content: str) -> type[BaseTool]: ...
def build_demo_registry(*tool_classes: type[BaseTool]) -> ToolRegistry: ...
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing integration test for demo runner event sequence + latency</name>
  <files>tests/integration/test_demo_agent.py</files>
  <read_first>
    - tests/unit/test_agent_sse.py lines 109-256 (the patch_pipeline_singletons fixture + smoke + latency tests — structural twins)
    - services/agent/_demo_stubs.py (just created in plan 19-01 — imports the runner needs)
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-05, D-06 (event sequence shape + exit-code contract)
    - .planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md §8 (file 8 — adaptation notes for tests/integration/test_demo_agent.py)
    - controllers/api.py lines ~280-294 (SSE wire format `event: <type>\ndata: <json>\n\n`)
  </read_first>
  <behavior>
    - Test 1: `test_demo_runner_emits_expected_event_sequence_in_process` — invoke `services.agent._demo_runner.run_demo()` (an async function returning the list of emitted events) using the same monkeypatch consumer-path discipline as `test_agent_sse.py::patch_pipeline_singletons`. Assert event-type counts: `PlannerPlanEvent == 1`, `ToolSpanStartEvent == 4`, `ToolSpanEndEvent == 4`, `ExecutorParallelEvent == 1`, `SynthesizerFinalEvent == 1`, total events == 11.
    - Test 2: `test_demo_runner_latency_bounded_by_max_not_sum` — same as Test 1 but time the iteration; assert `450 < elapsed_ms < 700` (4×0.5s parallel — D-05 + Phase 18 SC4 reference test bound).
    - Test 3: `test_demo_runner_executor_parallel_fan_out_is_four` — assert the single ExecutorParallelEvent has `fan_out == 4` and `group_latency_ms < 700`.
    - Test 4: `test_demo_runner_synthesizer_final_answer_non_empty` — assert the terminal SynthesizerFinalEvent has non-empty `answer`.
    - Test 5: `test_demo_runner_main_writes_sse_frames_to_stdout` — invoke `services.agent._demo_runner.main()` with `capsys` capture; assert stdout contains at least 11 occurrences of `event: ` lines and at least 11 occurrences of `data: ` lines (SSE wire format per controllers/api.py); assert stdout contains `event: planner.plan`, `event: tool.span.start`, `event: tool.span.end`, `event: executor.parallel`, `event: synthesizer.final` (5 distinct event_type values verified by grep).
    - Test 6: `test_demo_runner_exit_code_zero_on_success` — invoke `subprocess.run([sys.executable, "-m", "services.agent._demo_runner"], capture_output=True, text=True, timeout=10)` (true subprocess via -m); assert `proc.returncode == 0`; assert `b"event: synthesizer.final" in proc.stdout.encode()`. Skip with `pytest.skip` if conda env unavailable (CI vs local-dev portability).
  </behavior>
  <action>
    Create `tests/integration/test_demo_agent.py`. Module docstring: `"""Integration test for `make demo-agent` (Phase 19, AGENT-08, ROADMAP SC3+SC4).` followed by 1 paragraph: "Asserts the in-process demo runner emits the exact 11-event sequence + 4-way parallel fan-out + max-not-sum latency bound. Also exercises the subprocess invocation for the Make target."

    Imports: `from __future__ import annotations`; `import asyncio, subprocess, sys, time`; `from typing import Any`; `import pytest`; `from services.agent._demo_stubs import DemoStubPlanner, build_demo_registry, make_fake_retrieve_tool`; `from utils.models import (AgentEvent, ExecutorParallelEvent, PlannerPlanEvent, SynthesizerFinalEvent, ToolSpanEndEvent, ToolSpanStartEvent)`. Import `services.agent._demo_runner` lazily inside tests OR import at top — at RED stage all `_demo_runner` imports MUST fail (ModuleNotFoundError).

    Reuse the `patch_pipeline_singletons` shape from `tests/unit/test_agent_sse.py` lines 109-173 — promote it to a fixture in this file (DO NOT import from the unit test file — copy the fixture). The fixture takes `_StubPlanner` and tool classes. Use `DemoStubPlanner()` and `make_fake_retrieve_tool("search_knowledge_base", sleep_s=0.5)` for Tests 1-4.

    For Test 6 (subprocess): use `["conda", "run", "-n", "torch_env", "python", "-m", "services.agent._demo_runner"]` invocation pattern (matches Makefile convention). Add 10s timeout. Use `pytest.importorskip` not raw skip — skip only if conda binary not present on PATH.

    Run `conda run -n torch_env pytest tests/integration/test_demo_agent.py -v` — ALL tests MUST fail (RED gate). Tests 1-5 fail with `ModuleNotFoundError: services.agent._demo_runner`. Test 6 fails with subprocess returncode != 0 (because `-m services.agent._demo_runner` doesn't exist yet).
  </action>
  <verify>
    <automated>conda run -n torch_env pytest tests/integration/test_demo_agent.py -v --asyncio-mode=auto 2>&1 | grep -E "ModuleNotFoundError|FAILED|ERRORS|PASSED" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/test_demo_agent.py` exists.
    - File contains exactly 6 `def test_` functions matching the names in `<behavior>`.
    - All 6 tests FAIL when run (RED gate). Tests 1-5: `ModuleNotFoundError: No module named 'services.agent._demo_runner'`. Test 6: `subprocess.CalledProcessError` or returncode != 0.
    - `grep -c "from services.agent._demo_stubs import" tests/integration/test_demo_agent.py` returns ≥ 1 (consumes plan 19-01 outputs).
    - `grep -c "from services.agent._demo_runner" tests/integration/test_demo_agent.py` returns ≥ 1 (the to-be-created runner).
    - `grep -c "patch_pipeline_singletons" tests/integration/test_demo_agent.py` returns ≥ 2 (fixture defined + used).
    - Commit message: `test(19-02-T1): RED — failing integration test for services.agent._demo_runner`.
  </acceptance_criteria>
  <done>RED state confirmed: 6 tests fail with ModuleNotFoundError on services.agent._demo_runner. Test file committed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement services/agent/_demo_runner.py to make tests pass</name>
  <files>services/agent/_demo_runner.py</files>
  <read_first>
    - tests/integration/test_demo_agent.py (the RED tests from Task 1)
    - services/agent/_demo_stubs.py (the imports the runner needs)
    - tests/unit/test_agent_sse.py lines 109-173 (the consumer-path patch pattern — runtime equivalent uses `unittest.mock.patch` context managers)
    - controllers/api.py lines ~280-294 (SSE wire format `event: <type>\ndata: <json>\n\n` — runner mirrors this format on stdout)
    - .planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md §7 (companion runner module section, last 4 bullets)
    - services/pipeline.py lines 822+ (AgentQueryPipeline.run_streaming signature + sync constructor)
  </read_first>
  <action>
    Create `services/agent/_demo_runner.py` (~80-100 lines target). Module docstring: `"""CLI runner for `make demo-agent` (Phase 19, AGENT-08).` followed by: "Wires the Phase 19 demo stubs (`_demo_stubs.py`) into `AgentQueryPipeline.run_streaming` via runtime monkey-patching of singleton accessors at consumer paths (v1.3 D-16 discipline). Iterates the event stream and prints each event as an SSE frame (`event: <type>\\ndata: <json>\\n\\n`) to stdout. Exit 0 on success; non-zero on shape mismatch."

    Required exports + structure:

    1. `async def run_demo() -> list[AgentEvent]:` — returns the full list of emitted events. Internal flow:
       a. Build the stub planner: `planner = DemoStubPlanner()`.
       b. Build the fake tool: `Tool = make_fake_retrieve_tool("search_knowledge_base", sleep_s=0.5)`.
       c. Build the demo registry: `registry = build_demo_registry(Tool)`.
       d. Inside an `ExitStack` (or chained `try`/`finally`), patch the singleton accessors at consumer paths via `unittest.mock.patch` context managers — patches identical to `tests/unit/test_agent_sse.py::patch_pipeline_singletons` lines 141-169:
          - `services.pipeline.get_memory_service` → no-op stub (load_context returns object with empty short_term; save_turn no-ops)
          - `services.pipeline.get_audit_service` → no-op stub (log_query no-ops)
          - `services.pipeline.get_tenant_service` → no-op stub (get_tenant_filter returns `{}`)
          - `services.pipeline.get_filter_extractor` → no-op stub (extract returns object with `filters={}`, `semantic_query=q`)
          - `services.pipeline.get_planner` → `lambda: planner`
          - `services.agent.executor.get_tool_registry` → `lambda: registry`
          - `services.pipeline.get_executor` → `lambda: Executor(retriever=object(), llm=type("LLM", (), {"provider_name": "openai"})())`
          - `services.pipeline.get_tool_registry` → `lambda: registry` (belt-and-braces per Phase 18)
          - `services.pipeline.get_llm_client` → stub with `provider_name = "openai"`
          - `services.pipeline.get_retriever` → `lambda: object()`
       e. Define the no-op stub classes locally (inside `run_demo`) — copy verbatim from `test_agent_sse.py::patch_pipeline_singletons` `_NoMem`/`_NoAudit`/`_NoTenant`/`_NoFilter`/`_LLM` shapes. CRITICAL: do NOT depend on pytest.MonkeyPatch — use `unittest.mock.patch` context managers (PATTERNS.md §7 last bullet).
       f. Build `req = GenerationRequest(query=DEMO_QUERY, session_id="demo-session", tenant_id="demo-tenant", user_id="demo-user", top_k=5)` (placeholder identifiers per security_gate — NOT real tenant IDs).
       g. Instantiate `pipeline = AgentQueryPipeline()` inside the patches.
       h. Collect events: `events = [e async for e in pipeline.run_streaming(req)]`.
       i. Return `events`.

    2. `def emit_sse_frame(evt: AgentEvent) -> str:` — returns `f"event: {evt.event_type}\ndata: {evt.model_dump_json()}\n\n"` (verbatim format from controllers/api.py).

    3. `def validate_event_shape(events: list[AgentEvent]) -> None:` — asserts the expected counts (1 PlannerPlan, 4 ToolSpanStart, 4 ToolSpanEnd, 1 ExecutorParallel, 1 SynthesizerFinal). Raise `RuntimeError(f"Unexpected event sequence: {types}")` on mismatch. This is the exit-code-non-zero gate per D-06.

    4. `def main() -> int:` — synchronous entrypoint. Body:
       ```python
       events = asyncio.run(run_demo())
       for evt in events:
           print(emit_sse_frame(evt), end="")
       validate_event_shape(events)
       return 0
       ```
       Wrap in `try/except RuntimeError as e: print(f"DEMO FAILED: {e}", file=sys.stderr); return 1`.

    5. Module bottom: `if __name__ == "__main__":\n    sys.exit(main())` — enables `python -m services.agent._demo_runner`.

    Imports: `from __future__ import annotations`; `import asyncio, sys`; `from contextlib import ExitStack`; `from typing import Any, ClassVar`; `from unittest.mock import patch`; `from services.agent._demo_stubs import DEMO_QUERY, DemoStubPlanner, build_demo_registry, make_fake_retrieve_tool`; `from services.agent.executor import Executor`; `from services.pipeline import AgentQueryPipeline`; `from utils.models import AgentEvent, GenerationRequest`.

    Then run `conda run -n torch_env pytest tests/integration/test_demo_agent.py -v --asyncio-mode=auto` — all 6 tests MUST pass (GREEN gate).
  </action>
  <verify>
    <automated>conda run -n torch_env pytest tests/integration/test_demo_agent.py -v --asyncio-mode=auto --timeout=60 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `services/agent/_demo_runner.py` exists, ≤ 130 lines.
    - `pytest tests/integration/test_demo_agent.py -v --asyncio-mode=auto --timeout=60` exits 0 with `6 passed`.
    - Direct invocation works: `conda run -n torch_env python -m services.agent._demo_runner` exits 0 within 10s, stdout contains `event: synthesizer.final` (`echo $?` returns 0).
    - `grep -E "^def main\\b|^async def run_demo\\b|^def emit_sse_frame\\b|^def validate_event_shape\\b" services/agent/_demo_runner.py` returns exactly 4 matches.
    - `grep -c "from services.agent._demo_stubs import" services/agent/_demo_runner.py` returns 1.
    - `grep -c "demo-tenant\\|demo-user\\|demo-session" services/agent/_demo_runner.py | grep -v '^#'` returns ≥ 3 (placeholder IDs only — security gate).
    - `grep -E "tenant.*acme|tenant.*production" services/agent/_demo_runner.py` returns 0 (no real tenant IDs).
    - `conda run -n torch_env ruff check services/agent/_demo_runner.py tests/integration/test_demo_agent.py` clean.
    - `conda run -n torch_env mypy --strict services/agent/_demo_runner.py 2>&1 | grep -E "error:" | wc -l` returns 0 new errors.
    - Commit message: `feat(19-02-T2): GREEN — services/agent/_demo_runner.py + integration test (Phase 19, AGENT-08, SC3+SC4)`.
  </acceptance_criteria>
  <done>All 6 integration tests pass. `python -m services.agent._demo_runner` exits 0. ruff + mypy clean. Files committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo runner → orchestrator | Runtime monkey-patching scoped via `unittest.mock.patch` context managers — patches active ONLY during `run_demo()` call. No global state mutation. |
| demo runner → stdout | SSE frames printed verbatim — same wire format as the real `/agent/v1/run/stream` route (controllers/api.py). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Elevation of Privilege | Auth-bypass leakage from demo runner into production code paths | mitigate | All `unittest.mock.patch` calls scoped via `ExitStack` / `with` blocks — patches reverted on context exit. The runner is invoked only from `make demo-agent` (plan 19-03); the patches never touch FastAPI dependency_overrides or any HTTP route. Verified via grep: `grep -E "dependency_overrides|app\\." services/agent/_demo_runner.py` returns 0. |
| T-19-02-02 | Information Disclosure | Real tenant IDs / JWT samples in test fixture | mitigate | GenerationRequest in run_demo uses placeholders `tenant_id="demo-tenant"`, `user_id="demo-user"`, `session_id="demo-session"`. No JWT — bypassed via stubbed auth services. Verified by grep gate in acceptance_criteria. |
| T-19-02-03 | Tampering | Subprocess invocation in Test 6 leaking env vars | accept | subprocess.run inherits parent env; tests run in a controlled CI/dev environment. Test 6 captures stdout/stderr, no secrets leak. Documented in Test 6 docstring. |
| T-19-02-04 | Denial of Service | run_demo hanging due to event loop or asyncio.sleep stuck | mitigate | pytest --timeout=60 ceiling; subprocess.run timeout=10. Real elapsed bounded by 0.5s parallel sleep + ~200ms overhead = ~700ms. |
</threat_model>

<verification>
- All 6 integration tests pass under pytest --asyncio-mode=auto --timeout=60.
- `python -m services.agent._demo_runner` direct invocation exits 0 within 10s and prints valid SSE frames.
- ruff check on services/agent/_demo_runner.py + tests/integration/test_demo_agent.py: clean.
- mypy --strict on services/agent/_demo_runner.py: 0 new errors vs baseline.
- Latency bound (450 < elapsed_ms < 700) verified by Test 2.
- Event sequence (11 events, 4-way fan-out) verified by Tests 1, 3, 5.
</verification>

<success_criteria>
- `services/agent/_demo_runner.py` exists with `run_demo`, `emit_sse_frame`, `validate_event_shape`, `main` exports + `__main__` guard.
- `tests/integration/test_demo_agent.py` exists with 6 tests, all passing.
- Direct invocation `python -m services.agent._demo_runner` exits 0 + prints SSE frames including `event: synthesizer.final`.
- No real auth / tenant IDs leaked — placeholders only.
- 2 atomic commits: RED (test file only), GREEN (runner + makes tests pass).
</success_criteria>

<output>
After completion, create `.planning/phases/19-agent-first-docs-demo-release/19-02-SUMMARY.md`
</output>
