---
phase: 19-agent-first-docs-demo-release
plan: 04
type: execute
wave: 3
depends_on: [19-01]
files_modified:
  - docs/agent-architecture.md
autonomous: true
requirements: [AGENT-08]
must_haves:
  truths:
    - "docs/agent-architecture.md has a `## Planner / Executor Model` section BEFORE `## Authoring Tools`"
    - "Section contains 5 subsections: Concept, Flow, Pydantic V2 Signatures, Method Signatures, Runnable Example"
    - "Section includes one runnable Python snippet (~25-30 lines) that imports real shipped types and iterates AgentQueryPipeline.run_streaming"
    - "Existing `## Authoring Tools` (Phase 17) and `## Event Schema Reference` (Phase 18) sections UNCHANGED"
    - "ROADMAP SC2 fully closed: each of the 3 docs sections has its runnable snippet"
  artifacts:
    - path: "docs/agent-architecture.md"
      provides: "Concept → Tool authoring → Wire format trilogy with runnable snippets"
      contains: "## Planner / Executor Model"
  key_links:
    - from: "docs/agent-architecture.md::## Planner / Executor Model"
      to: "## Authoring Tools"
      via: "Cross-reference link from Concept subsection"
      pattern: "\\[Authoring Tools\\]\\(#authoring-tools\\)"
    - from: "docs/agent-architecture.md::## Planner / Executor Model"
      to: "## Event Schema Reference"
      via: "Cross-reference link from Runnable Example subsection"
      pattern: "\\[Event Schema Reference\\]\\(#event-schema-reference\\)"
---

<objective>
Insert a new `## Planner / Executor Model` section into `docs/agent-architecture.md`
BEFORE the existing `## Authoring Tools` section (D-09). Final docs section order
becomes: Planner / Executor Model → Authoring Tools (Phase 17) → Event Schema
Reference (Phase 18). Each section has its runnable snippet — closing ROADMAP SC2.

Purpose: complete the Concept → Tool authoring → Wire format trilogy. Reader who
arrives at the docs starting from "what is this thing?" gets the architectural
mental model first, then learns to add tools, then sees the wire format.

Output: ~120-150 line section inserted at the top of the docs file. Existing sections
verbatim-preserved. Status banner updated to reflect Phase 19 closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md
@.planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md
@.planning/phases/19-agent-first-docs-demo-release/19-01-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-02-SUMMARY.md
@docs/agent-architecture.md
@utils/models.py
@services/agent/planner.py
@services/agent/executor.py
@services/pipeline.py

<interfaces>
<!-- Existing sections in docs/agent-architecture.md (DO NOT MODIFY). -->

Lines 1-5 (header):
```markdown
# Agent Architecture

*Status: Phase 18 (v1.4) — Tool abstraction + SSE event schemas shipped. This
document is the tool-author quick-start AND the SSE event-schema reference.
Historical intent mapping (Phase 19) extends this file later.*
```

Line 7 (`## Authoring Tools` Phase 17 section): UNCHANGED — new section inserts BEFORE this.

Line 99 (`## Event Schema Reference` Phase 18 section): UNCHANGED.

<!-- Pydantic V2 type definitions to copy verbatim into the new section. -->

From utils/models.py lines 244-258 (ToolCall):
```python
class ToolCall(BaseModel):
    """A single tool invocation requested by the LLM in one assistant turn."""
    model_config = ConfigDict(frozen=True)

    id:        str
    name:      str
    arguments: dict[str, Any] = Field(default_factory=dict)
```

From utils/models.py lines 291-315 (ToolPlan, header through fields only — DO NOT include the validator bodies):
```python
class ToolPlan(BaseModel):
    """Planner output: an ordered list of ToolCalls plus their parallel-group assignment."""
    model_config = ConfigDict(frozen=True)

    steps:             list[ToolCall]  = Field(default_factory=list)
    parallel_groups:   list[list[int]] = Field(default_factory=list)
    rationale:         str             = ""
    raw_assistant_msg: dict[str, Any]  = Field(default_factory=dict)
    stop_reason:       str             = "text_only"
```

<!-- Method signatures to copy verbatim. -->

From services/agent/planner.py lines 43-48 (Planner.plan_from_messages):
```python
async def plan_from_messages(
    self,
    messages: list[dict[str, Any]],
    tools: list[dict[str, Any]] | None = None,
    system: str | None = None,
) -> ToolPlan: ...
```

From services/agent/executor.py lines 105-113 (Executor.execute_plan_streaming):
```python
async def execute_plan_streaming(
    self,
    plan: ToolPlan,
    tf: dict[str, Any],
    req: GenerationRequest,
    *,
    trace_id: str,
    seq_counter: Iterator[int],
) -> AsyncIterator[AgentEvent | ToolResult | BaseException]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert `## Planner / Executor Model` section into docs/agent-architecture.md (additive; existing sections preserved)</name>
  <files>docs/agent-architecture.md</files>
  <read_first>
    - docs/agent-architecture.md (full file — pay attention to the existing 3-paragraph status banner lines 1-5, the `## Authoring Tools` section opening lines 7-13 to mirror tone, and the runnable-example pattern lines 60-89)
    - utils/models.py lines 244-315 (verbatim source for ToolCall + ToolPlan field tables)
    - services/agent/planner.py lines 1-90 (Planner class + plan_from_messages signature + module-level _PLANNER_SYSTEM constant)
    - services/agent/executor.py lines 1-130 (Executor class + execute_plan_streaming signature + as_completed loop pattern)
    - services/pipeline.py lines 800-870 (AgentQueryPipeline.run_streaming signature for the runnable-snippet section)
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-09, D-10, D-11 (section content + ASCII diagram + snippet shape)
    - .planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md §2 (file 2 — full adaptation notes for the docs section)
  </read_first>
  <action>
    Edit `docs/agent-architecture.md`. Two precise changes.

    **Change 1** — Update the status banner (lines 1-5). Replace lines 3-5:

    Before:
    ```
    *Status: Phase 18 (v1.4) — Tool abstraction + SSE event schemas shipped. This
    document is the tool-author quick-start AND the SSE event-schema reference.
    Historical intent mapping (Phase 19) extends this file later.*
    ```

    After:
    ```
    *Status: Phase 19 (v1.4) — Concept + tool-authoring + SSE event-schema trilogy
    complete. Read top-to-bottom: the Planner / Executor mental model, then how to
    add a tool the planner can pick, then the wire format the executor emits.*
    ```

    **Change 2** — Insert a new `## Planner / Executor Model` section between line 5 (banner) and line 7 (existing `## Authoring Tools` heading). The new section consists of FIVE subsections (~120-150 lines total). Insert exactly the following block (replace `<NEW SECTION>` placeholder with the content below):

    Insert AFTER the blank line on line 6, BEFORE the `## Authoring Tools` heading on line 7. Do NOT modify line 7 or any line after it.

    The new section content (verbatim, including the `## Planner / Executor Model` heading):

    ````markdown
    ## Planner / Executor Model

    The agent runtime is three explicit collaborators: a **Planner** that turns
    a conversation into a `ToolPlan`, an **Executor** that dispatches the plan's
    parallel groups concurrently, and a **Synthesizer** that composes the final
    answer from the accumulated tool results. Each collaborator is a single-purpose
    object behind a stable Pydantic V2 contract. The orchestrator (`AgentQueryPipeline`)
    is a thin loop over these three.

    The Planner is stateless: `(messages, tools) → ToolPlan`. It issues one LLM
    call via the provider-neutral `BaseLLMClient.call_agentic_turn` (Phase 11) and
    normalizes the assistant turn into a `ToolPlan`. The Executor consumes that
    plan and walks `parallel_groups` — each inner list is a wave of step indices
    dispatched concurrently via `asyncio.as_completed`, with v1.3 D-01
    `BaseException` isolation so a failing sibling does not cancel the rest.
    The Synthesizer is the LLM's terminal `call_agentic_turn` after results
    return — it sees the tool-result messages and emits the final text-only
    plan whose `rationale` IS the answer.

    ### Flow

    ```
    Request ──▶ AgentQueryPipeline ──▶ Planner.plan_from_messages
                                          │
                                          ▼
                                       ToolPlan ──▶ Executor.execute_plan_streaming
                                                        │
                                                        ▼
                                                  parallel groups
                                                  (asyncio.as_completed,
                                                   BaseException isolation)
                                                        │
                                                        ▼
                                                   ToolResult[]
                                                        │
                                                        ▼
                                              Synthesizer (call_agentic_turn) ──▶ Response
    ```

    Each arrow is a single function call. The pipeline issues at most
    `MAX_ITERATIONS = 5` planner turns (Phase 16 D-12); each turn either yields
    a non-terminal `ToolPlan` (with steps to dispatch) or a terminal one (no
    steps, `rationale` is the answer).

    ### Pydantic V2 Signatures

    Planner output is a frozen `ToolPlan`:

    ```python
    class ToolCall(BaseModel):
        """A single tool invocation requested by the LLM in one assistant turn."""
        model_config = ConfigDict(frozen=True)

        id:        str
        name:      str
        arguments: dict[str, Any] = Field(default_factory=dict)


    class ToolPlan(BaseModel):
        """Planner output: an ordered list of ToolCalls plus their parallel-group assignment."""
        model_config = ConfigDict(frozen=True)

        steps:             list[ToolCall]  = Field(default_factory=list)
        parallel_groups:   list[list[int]] = Field(default_factory=list)
        rationale:         str             = ""
        raw_assistant_msg: dict[str, Any]  = Field(default_factory=dict)
        stop_reason:       str             = "text_only"
    ```

    `parallel_groups` is the canonical execution shape: every step index in
    `range(len(steps))` MUST appear in exactly one group. A non-empty plan
    always has a non-empty `parallel_groups`. `stop_reason` mirrors the upstream
    LLM stop reason (`text_only` / `tool_use` / `max_tokens` / `error`).

    ### Method Signatures

    The Planner exposes one async method:

    ```python
    async def plan_from_messages(
        self,
        messages: list[dict[str, Any]],
        tools: list[dict[str, Any]] | None = None,
        system: str | None = None,
    ) -> ToolPlan: ...
    ```

    The Executor exposes the streaming dispatcher consumed by `AgentQueryPipeline.run_streaming`:

    ```python
    async def execute_plan_streaming(
        self,
        plan: ToolPlan,
        tf: dict[str, Any],
        req: GenerationRequest,
        *,
        trace_id: str,
        seq_counter: Iterator[int],
    ) -> AsyncIterator[AgentEvent | ToolResult | BaseException]: ...
    ```

    The streaming variant yields events at lifecycle boundaries (`tool.span.start`,
    `tool.span.end` / `tool.span.error`, `executor.parallel`) interleaved with
    the bare results — see [Event Schema Reference](#event-schema-reference)
    for the wire format.

    ### Runnable Example

    Drive the full Planner → Executor → Synthesizer loop and print every event:

    ```python
    import asyncio
    from services.pipeline import AgentQueryPipeline
    from utils.models import GenerationRequest

    async def main() -> None:
        pipeline = AgentQueryPipeline()
        req = GenerationRequest(
            query=(
                "Across our compliance, finance, engineering, and HR knowledge "
                "bases, where do we mention 'data retention'?"
            ),
            session_id="demo-session",
            tenant_id="demo-tenant",
            user_id="demo-user",
            top_k=5,
        )
        async for evt in pipeline.run_streaming(req):
            print(
                f"{evt.event_type:>22}  seq={evt.seq:>3}  "
                f"ts={evt.ts_ms}  payload={evt.model_dump_json()}"
            )

    asyncio.run(main())
    ```

    This is the same query `make demo-agent` runs; the only difference is that
    `make demo-agent` (Phase 19) wires stub Planner + stub `RetrieveTool` so the
    snippet is offline-runnable from a clean checkout. To run it against real
    LLM + real retrieval, point `LLM_PROVIDER` and `PG_DSN` at live services
    and remove the stubs (see `services/agent/_demo_runner.py` for the patch list).

    Add tools the planner can pick: see [Authoring Tools](#authoring-tools).
    Decode the events on the wire: see [Event Schema Reference](#event-schema-reference).

    ````

    Notes:
    - Verify the inserted block ends with the `[Event Schema Reference](#event-schema-reference).` line followed by a blank line, AND the next line in the file is `## Authoring Tools` (the existing line 7 — now shifted down to ~line 145).
    - The ASCII flow diagram uses `──▶` (U+2500 / U+25B6) and `▼` (U+25BC) box-drawing characters. Render in monospace fenced code block.
    - Cross-reference anchors `#authoring-tools` and `#event-schema-reference` are GitHub auto-generated from the existing `## Authoring Tools` and `## Event Schema Reference` headings — verify by clicking after commit.
    - The runnable snippet uses placeholder identifiers (`demo-tenant`, `demo-user`, `demo-session`) per security gate — NO real tenant IDs.
    - Section length target: 120-150 lines (D-10). Count after insertion: `awk '/^## Planner \\/ Executor Model$/,/^## Authoring Tools$/' docs/agent-architecture.md | wc -l` — expect 130-155 lines (includes both heading lines).
  </action>
  <verify>
    <automated>grep -n "^## Planner / Executor Model" docs/agent-architecture.md && grep -n "^## Authoring Tools" docs/agent-architecture.md && grep -n "^## Event Schema Reference" docs/agent-architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Planner / Executor Model$" docs/agent-architecture.md` returns 1.
    - Section order verified: `grep -n "^## " docs/agent-architecture.md` returns three lines, in this order — `## Planner / Executor Model` (low line number), `## Authoring Tools` (mid), `## Event Schema Reference` (high).
    - `grep -c "^### Flow$" docs/agent-architecture.md` returns 1.
    - `grep -c "^### Pydantic V2 Signatures$" docs/agent-architecture.md` returns 1.
    - `grep -c "^### Method Signatures$" docs/agent-architecture.md` returns 1.
    - `grep -c "^### Runnable Example$" docs/agent-architecture.md` returns 1 (the new section's; existing `## Authoring Tools` has its own `### Runnable Example` so total file count is 2 — verify `grep -c "^### Runnable Example$" docs/agent-architecture.md` returns 2).
    - ASCII diagram present: `grep -c "Request ──▶ AgentQueryPipeline" docs/agent-architecture.md` returns 1.
    - ToolPlan field table present verbatim from utils/models.py: `grep -c "parallel_groups:   list\\[list\\[int\\]\\] = Field(default_factory=list)" docs/agent-architecture.md` returns 1.
    - plan_from_messages signature: `grep -c "async def plan_from_messages(" docs/agent-architecture.md` returns 1.
    - execute_plan_streaming signature: `grep -c "async def execute_plan_streaming(" docs/agent-architecture.md` returns 1.
    - Runnable snippet present: `grep -c "asyncio.run(main())" docs/agent-architecture.md` returns 1; `grep -c "AgentQueryPipeline()" docs/agent-architecture.md` returns 1; `grep -c "pipeline.run_streaming(req)" docs/agent-architecture.md` returns 1.
    - Cross-references present: `grep -c "\\[Authoring Tools\\](#authoring-tools)" docs/agent-architecture.md` returns 1; `grep -c "\\[Event Schema Reference\\](#event-schema-reference)" docs/agent-architecture.md` returns 1.
    - Status banner updated: `grep -c "Phase 19 (v1.4) — Concept + tool-authoring + SSE event-schema trilogy" docs/agent-architecture.md` returns 1.
    - Phase 17 + Phase 18 sections UNCHANGED (verify by line count diff): `wc -l docs/agent-architecture.md` returns ≥ 360 (was 245; expect ~130-150 lines added). `grep -c "@get_tool_registry().register" docs/agent-architecture.md` returns 2 (existing 1 in Authoring Tools + 1 in WebSearchTool example — unchanged).
    - Section line count: `awk '/^## Planner \\/ Executor Model$/,/^## Authoring Tools$/' docs/agent-architecture.md | wc -l` returns 120-160 (D-10 budget).
    - No real tenant IDs leaked: `grep -E "tenant_id=\"acme\"|tenant.*production" docs/agent-architecture.md` returns 0.
    - Commit message: `docs(19-04-T1): add ## Planner / Executor Model section to agent-architecture.md (Phase 19, AGENT-08, SC2)`.
  </acceptance_criteria>
  <done>New section inserted at top of docs file. Existing sections unchanged. All cross-references resolve. Status banner updated. File committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs file → public reader | Any tenant identifiers, JWT samples, or PII in code snippets become public when the repo is public/forked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Information Disclosure | Real tenant IDs in docs snippet | mitigate | Runnable snippet uses placeholder values: `tenant_id="demo-tenant"`, `user_id="demo-user"`, `session_id="demo-session"`. Verified via grep gate in acceptance_criteria (`grep -E "tenant_id=\\"acme\\"" returns 0`). |
| T-19-04-02 | Information Disclosure | JWT sample with embedded real signature | mitigate | Snippet does not include cURL or HTTP — pure Python in-process invocation. No JWT samples in this section. The cURL example (with `<JWT>` placeholder) is in plan 19-06's README, not here. |
| T-19-04-03 | Tampering | Future contributor adds real query/credentials to runnable example | accept | Plan 19-04 ships placeholders; this is a docs-maintenance discipline going forward, enforced by code-review on docs PRs. |
</threat_model>

<verification>
- `## Planner / Executor Model` section present at top of docs/agent-architecture.md.
- Section ordering correct: Planner / Executor Model → Authoring Tools → Event Schema Reference.
- All 5 subsections present (Concept implicit in 2-paragraph opener + Flow + Pydantic V2 Signatures + Method Signatures + Runnable Example).
- Runnable snippet imports real shipped types (AgentQueryPipeline, GenerationRequest) and uses placeholder identifiers.
- Cross-references to existing sections via GitHub auto-anchors (#authoring-tools, #event-schema-reference).
- ROADMAP SC2 closed: 3 docs sections × 1 runnable snippet each.
</verification>

<success_criteria>
- New `## Planner / Executor Model` section inserted between status banner and existing `## Authoring Tools`.
- 130-150 lines added per D-10 budget.
- Existing Phase 17 + Phase 18 docs sections preserved verbatim.
- Status banner updated to reflect Phase 19 trilogy completion.
- 1 atomic commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19-agent-first-docs-demo-release/19-04-SUMMARY.md`
</output>
