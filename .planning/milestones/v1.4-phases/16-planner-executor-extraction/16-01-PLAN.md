---
phase: 16-planner-executor-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/agent/__init__.py
  - services/agent/tool_executor.py
  - tests/unit/fixtures/agent_parity/single_step.json
  - tests/unit/fixtures/agent_parity/parallel_multi_step.json
  - tests/unit/fixtures/agent_parity/__init__.py
autonomous: true
requirements: [AGENT-09]
must_haves:
  truths:
    - "services/agent/__init__.py exists; package importable as `services.agent`"
    - "services/agent/tool_executor.py defines a module-level async free function `execute_tool_call(...)` whose body is a verbatim extract of `services/pipeline.py::AgentQueryPipeline._execute_tool_call` at the v1.3 baseline (commit a3a95f8) — no behavior change in this plan"
    - "execute_tool_call signature matches v1.3 self-method signature minus `self`; type hints preserved verbatim including `BaseException` (NOT `Exception`) per v1.3 D-01"
    - "services/pipeline.py::AgentQueryPipeline._execute_tool_call (line 846) is REPLACED with a one-line method that delegates to `services.agent.tool_executor.execute_tool_call(...)` — no other lines in pipeline.py change in this plan"
    - "services/pipeline.py::SwarmQueryPipeline._execute_tool_call (line 1107) is REPLACED with a one-line method that delegates to `services.agent.tool_executor.execute_tool_call(...)` — no other lines in pipeline.py change in this plan"
    - "tests/unit/fixtures/agent_parity/__init__.py is empty (package marker only) — mirrors tests/unit/fixtures/agentic_turn/ pattern"
    - "tests/unit/fixtures/agent_parity/single_step.json contains keys: input_messages (list), expected_tool_call_sequence (list of ToolCall dicts), expected_final_text (string), llm_canned_turns (list of AgenticTurn dicts) — schema documented in Wave 2 plan 16-02"
    - "tests/unit/fixtures/agent_parity/parallel_multi_step.json contains the same keys; expected_tool_call_sequence has length >= 2 (the parallel-fan-out fixture)"
    - "Both fixtures are MANUALLY constructed by reading v1.3 LOG_LEVEL=DEBUG output of `AgentQueryPipeline.run` for fixture inputs — captured in Plan 16-01 task notes; not generated"
    - "All v1.3 unit tests still pass: `tests/unit/test_agent_pipeline_refactor.py` and `tests/unit/test_swarm_pipeline.py` produce 0 regressions after the helper move"
    - "No new mypy errors in services/pipeline.py or services/agent/* relative to v1.3 baseline (commit a3a95f8)"
  artifacts:
    - path: "services/agent/__init__.py"
      provides: "package marker for new services/agent/ module"
      contains: ""
    - path: "services/agent/tool_executor.py"
      provides: "shared async free function execute_tool_call() consumed by both AgentQueryPipeline and SwarmQueryPipeline"
      contains: "async def execute_tool_call"
    - path: "tests/unit/fixtures/agent_parity/single_step.json"
      provides: "single-step parity fixture for Plan 16-02 RED tests"
      contains: "input_messages"
    - path: "tests/unit/fixtures/agent_parity/parallel_multi_step.json"
      provides: "multi-step parallel parity fixture for Plan 16-02 RED tests"
      contains: "expected_tool_call_sequence"
  key_links:
    - from: "services/pipeline.py::AgentQueryPipeline._execute_tool_call"
      to: "services.agent.tool_executor.execute_tool_call"
      via: "one-line delegating call (preserves method API for callers)"
      pattern: "from services\\.agent\\.tool_executor import execute_tool_call"
    - from: "services/pipeline.py::SwarmQueryPipeline._execute_tool_call"
      to: "services.agent.tool_executor.execute_tool_call"
      via: "one-line delegating call; eliminates v1.3 verbatim duplication"
      pattern: "from services\\.agent\\.tool_executor import execute_tool_call"
---

<objective>
Wave 1 is mechanical extraction. Create the new `services/agent/` package; move `_execute_tool_call` into `services/agent/tool_executor.py` as a module-level async free function; replace both v1.3 in-class methods (AgentQueryPipeline at line 846, SwarmQueryPipeline at line 1107) with one-line delegating calls. Capture two parity fixtures (single-step + parallel multi-step) by reading v1.3 baseline `LOG_LEVEL=DEBUG` output. NO new behavior; NO Planner / Executor; NO test changes besides adding the fixture files.

Purpose: Wave 1 ships a refactor with measurable parity (`grep -rn "def _execute_tool_call" services/` returns 2 matches before, 2 matches after — but both are now one-line delegates pointing at the single canonical helper). Wave 2 (16-02) consumes the fixtures to build TDD parity tests for the new Planner+Executor skeletons. Wave 3 (16-03) collapses the AgentQueryPipeline.run body into the orchestrator. The verbatim deletion of one-line delegates is in Wave 3, NOT here — this plan keeps both pipelines runnable after every commit.

Output: 5 files. `services/agent/__init__.py` (empty), `services/agent/tool_executor.py` (~80 lines verbatim from v1.3 lines 846-905), two-line edit at `services/pipeline.py:846` (delegate), two-line edit at `services/pipeline.py:1107` (delegate), two JSON fixture files, one empty fixture package marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<task id="16-01-T1" name="Create services/agent/ package and tool_executor.py">
<read_first>
- services/pipeline.py lines 581-905 (AgentQueryPipeline class incl. `_execute_tool_call` at line 846)
- services/pipeline.py lines 1100-1180 (SwarmQueryPipeline `_execute_tool_call` at line 1107) — verify byte-equivalence to AgentQueryPipeline copy via inspect.getsource normalized comparison documented in v1.3 PROJECT.md row 176
- services/__init__.py — confirm package layout convention
</read_first>

1. Create `services/agent/__init__.py` (empty file, package marker).
2. Create `services/agent/tool_executor.py`. Body:
   - Copy the entire `_execute_tool_call` method body from `services/pipeline.py:846-905` (or wherever it ends in the current v1.3 baseline).
   - Convert to a module-level async free function: drop `self`; add explicit type hints for every parameter the method previously bound from `self` (likely none — the helper is stateless per v1.3 D-01).
   - Imports at file top: ALL imports the function body needs (`asyncio`, `json`, structured logger, exception types, `BaseException` handling pattern, `ToolCall` from `utils.models`, etc). Pull each import from the v1.3 pipeline.py header verbatim — do NOT add new imports.
   - Module-level docstring: one line referencing AGENT-09 + the v1.3 source line range.
3. Run `ruff check services/agent/` — must be clean.
4. Run `mypy --strict services/agent/tool_executor.py` — must produce ZERO errors (the v1.3 method body was strict-clean per Phase 12 closure; if errors appear, you have copied wrong).

<acceptance_criteria>
- `services/agent/__init__.py` exists and is empty.
- `services/agent/tool_executor.py` contains exactly one async function: `execute_tool_call`.
- `python -c "from services.agent.tool_executor import execute_tool_call; import inspect; print(inspect.iscoroutinefunction(execute_tool_call))"` prints `True`.
- `ruff check services/agent/` exits 0.
- `mypy --strict services/agent/tool_executor.py` exits 0.
</acceptance_criteria>
</task>

<task id="16-01-T2" name="Wire AgentQueryPipeline._execute_tool_call to delegate">
<read_first>
- services/pipeline.py lines 846-905 (the method to replace)
- services/agent/tool_executor.py (just-written shared helper)
</read_first>

1. Replace the body of `AgentQueryPipeline._execute_tool_call` (line 846) with a single-line delegate:
   ```python
   return await execute_tool_call(...)
   ```
   passing through every argument the method received unchanged.
2. Add `from services.agent.tool_executor import execute_tool_call` to the imports at the top of `services/pipeline.py` (alphabetical order, near other `services.*` imports).
3. Run the v1.3 test that exercises this method: `pytest tests/unit/test_agent_pipeline_refactor.py -q`. MUST: 11 passed (matches Phase 11 baseline).

<acceptance_criteria>
- `grep -nE "^\s*async def _execute_tool_call" services/pipeline.py` shows exactly 2 lines (one in AgentQueryPipeline, one in SwarmQueryPipeline) — Wave 3 deletes the in-class methods entirely; this plan only rewires bodies.
- The AgentQueryPipeline body is exactly: `return await execute_tool_call(<args>)`. No other statements in the method body.
- `pytest tests/unit/test_agent_pipeline_refactor.py -q` exits 0 with 11 passed.
</acceptance_criteria>
</task>

<task id="16-01-T3" name="Wire SwarmQueryPipeline._execute_tool_call to delegate">
<read_first>
- services/pipeline.py lines 1107-~1170 (the second copy)
</read_first>

Same as T2 but for `SwarmQueryPipeline._execute_tool_call`. Body becomes one-line delegate. The `from services.agent.tool_executor import execute_tool_call` added in T2 already covers both classes — do NOT add a duplicate import.

<acceptance_criteria>
- The SwarmQueryPipeline body is exactly: `return await execute_tool_call(<args>)`. No other statements.
- `pytest tests/unit/test_swarm_pipeline.py -q` exits 0 (8 passed per v1.3 close; integration test gated by pytest.mark.integration stays at 1 deselected).
- Combined: `pytest tests/unit/test_agent_pipeline_refactor.py tests/unit/test_swarm_pipeline.py -q` exits 0.
</acceptance_criteria>
</task>

<task id="16-01-T4" name="Capture v1.3 baseline parity fixtures">
<read_first>
- 16-CONTEXT.md D-07, D-08 (fixture schema and minimum count)
- tests/unit/fixtures/agentic_turn/ — pattern to clone
</read_first>

1. Create `tests/unit/fixtures/agent_parity/__init__.py` (empty).
2. Run the v1.3 `AgentQueryPipeline` once with `LOG_LEVEL=DEBUG` against a designed single-step query (e.g., "What are the v1.0 hardening features?" if a doc index exists, otherwise a synthetic query that exercises one tool call). Capture from the structured logger output:
   - The exact `messages` list passed in (input_messages).
   - The `AgenticTurn` returned by `call_agentic_turn` (this becomes `llm_canned_turns[0]`).
   - The resulting `tool_calls` list (becomes `expected_tool_call_sequence`).
   - The final assistant turn text (becomes `expected_final_text`).
3. Write to `tests/unit/fixtures/agent_parity/single_step.json` with schema:
   ```json
   {
     "input_messages": [...],
     "llm_canned_turns": [...],
     "expected_tool_call_sequence": [...],
     "expected_final_text": "..."
   }
   ```
4. Repeat steps 2-3 for a designed multi-step parallel query (e.g., a query that exercises ≥2 concurrent tool calls per `agent_mode=True` parallelism). Save as `tests/unit/fixtures/agent_parity/parallel_multi_step.json`. `expected_tool_call_sequence` length MUST be ≥ 2.
5. Validate both JSON files parse cleanly: `python -c "import json; json.load(open('tests/unit/fixtures/agent_parity/single_step.json')); json.load(open('tests/unit/fixtures/agent_parity/parallel_multi_step.json'))"` exits 0.
6. NO test code is added in this task — fixtures only. Plan 16-02 builds the parity test that consumes them.

<acceptance_criteria>
- `tests/unit/fixtures/agent_parity/single_step.json` parses; has all 4 required keys; `expected_tool_call_sequence` length is exactly 1.
- `tests/unit/fixtures/agent_parity/parallel_multi_step.json` parses; has all 4 required keys; `expected_tool_call_sequence` length is >= 2.
- Both fixture queries are reproducible: a comment block at the top of each fixture (in a `_provenance` key) records the input query string, the v1.3 commit SHA used to capture, and a one-line note on how to re-capture.
</acceptance_criteria>
</task>

<verification>
- `grep -rn "def _execute_tool_call" services/` returns exactly 3 matches: 1 in `services/agent/tool_executor.py`, 2 one-line delegates in `services/pipeline.py`. (Wave 3 collapses to 1.)
- `pytest tests/unit/test_agent_pipeline_refactor.py tests/unit/test_swarm_pipeline.py -q` exits 0 with no regressions vs v1.3 baseline.
- `ruff check services/agent/` exits 0.
- `mypy --strict services/agent/tool_executor.py` exits 0.
- Both parity fixture JSON files parse cleanly.
- `git diff --stat HEAD~1` shows exactly the 5 files in `files_modified` plus pipeline.py edits — no other files touched.
</verification>
