---
phase: 16-planner-executor-extraction
plan: 02
type: tdd
wave: 2
depends_on: ['16-01']
files_modified:
  - services/agent/planner.py
  - services/agent/executor.py
  - services/agent/__init__.py
  - utils/models.py
  - tests/unit/test_agent_parity.py
  - tests/unit/test_planner.py
  - tests/unit/test_executor.py
autonomous: true
requirements: [AGENT-06, NLU-03]
must_haves:
  truths:
    - "utils/models.py defines `ToolPlan` Pydantic V2 frozen model with three fields: `steps: list[ToolCall]`, `parallel_groups: list[list[int]]`, `rationale: str` (CONTEXT.md D-01)"
    - "ToolPlan.model_config has `frozen=True` (matches utils/models.py::ToolCall convention)"
    - "ToolPlan validators reject: empty parallel_groups, indices in parallel_groups outside range(len(steps)), duplicate indices across groups, missing index across groups (every step index must appear in exactly one group)"
    - "services/agent/planner.py defines `class Planner` with `async def plan(self, req: GenerationRequest) -> ToolPlan` method; uses `BaseLLMClient.call_agentic_turn` (provider-neutral); does NOT call provider-specific code; module-level `get_planner()` singleton factory matches v1.2/v1.3 pattern"
    - "Planner system prompt (constant `_PLANNER_SYSTEM` at module top) instructs LLM to emit `rationale` in the same language as the user query (CONTEXT.md D-03)"
    - "services/agent/executor.py defines `class Executor` with `async def execute_plan(self, plan: ToolPlan, ...) -> list[ToolResult]` method; iterates `plan.parallel_groups` and dispatches each group via `asyncio.gather`; uses `BaseException` (not `Exception`) for gather isolation per v1.3 D-01"
    - "Executor.execute_plan calls `services.agent.tool_executor.execute_tool_call` for each ToolCall — does NOT contain its own tool dispatch logic"
    - "services/agent/__init__.py re-exports: `Planner`, `get_planner`, `Executor`, `get_executor`, `execute_tool_call` for clean import API"
    - "tests/unit/test_agent_parity.py loads both fixtures from tests/unit/fixtures/agent_parity/, monkeypatches `services.agent.planner.call_agentic_turn` and `services.agent.executor.call_agentic_turn` (consumer paths) to return llm_canned_turns from the fixture, runs Planner.plan + Executor.execute_plan, asserts the resulting ToolCall sequence equals expected_tool_call_sequence byte-for-byte (model_dump comparison)"
    - "tests/unit/test_agent_parity.py has at least 2 parametrized test functions: one over single_step.json, one over parallel_multi_step.json"
    - "tests/unit/test_planner.py covers: rejects malformed LLM output (raises specific exception type, NOT generic), respects max_iterations=5 cap (planner does NOT loop on its own — single LLM call returns one ToolPlan), monkeypatches call_agentic_turn at consumer path"
    - "tests/unit/test_executor.py covers: parallel_groups dispatch order, asyncio.gather isolation (one tool error does not crash siblings — uses BaseException scope), tool_call.id round-trip preserved through results, parallelism_factor field logged in audit per turn (matches v1.2 audit naming)"
    - "AgentQueryPipeline.run is UNCHANGED in this plan — Wave 3 (16-03) wires the new Planner+Executor in. Existing v1.3 tests still pass (no regression)."
    - "All new tests use `monkeypatch.setattr('services.agent.<mod>.<dep>', ...)` consumer-path mock pattern (v1.3 Phase 13/15 convention)"
  artifacts:
    - path: "utils/models.py"
      provides: "ToolPlan Pydantic V2 frozen model"
      contains: "class ToolPlan"
    - path: "services/agent/planner.py"
      provides: "Planner class + get_planner singleton; emits ToolPlan from a single LLM call"
      contains: "class Planner"
    - path: "services/agent/executor.py"
      provides: "Executor class + get_executor singleton; runs ToolPlan via asyncio.gather"
      contains: "class Executor"
    - path: "services/agent/__init__.py"
      provides: "package re-exports"
      contains: "from services.agent.planner import Planner"
    - path: "tests/unit/test_agent_parity.py"
      provides: "behavioral parity tests against v1.3 baseline fixtures"
      contains: "from tests.unit.fixtures.agent_parity"
    - path: "tests/unit/test_planner.py"
      provides: "Planner unit tests"
      contains: "def test_planner_"
    - path: "tests/unit/test_executor.py"
      provides: "Executor unit tests including parallel_groups dispatch and asyncio.gather isolation"
      contains: "def test_executor_"
  key_links:
    - from: "Planner.plan"
      to: "BaseLLMClient.call_agentic_turn"
      via: "provider-neutral interface (v1.2 Phase 11 abstraction)"
      pattern: "await self\\._llm\\.call_agentic_turn"
    - from: "Executor.execute_plan"
      to: "services.agent.tool_executor.execute_tool_call"
      via: "shared helper from Plan 16-01"
      pattern: "await execute_tool_call"
    - from: "Executor.execute_plan"
      to: "asyncio.gather with BaseException isolation"
      via: "v1.3 Phase 12 D-01 isolation pattern"
      pattern: "except BaseException"
    - from: "ToolPlan validator"
      to: "parallel_groups index coverage rule"
      via: "every step index appears in exactly one group; reject otherwise"
      pattern: "set\\(.*for group in.*parallel_groups"
---

<objective>
TDD plan. RED phase: write the parity tests + Planner + Executor unit tests against the still-empty `services/agent/planner.py` and `services/agent/executor.py` skeletons. GREEN phase: implement Planner and Executor minimally so all RED tests pass. REFACTOR phase: tighten internal helpers, ensure mypy --strict + ruff clean, no behavior change. By end of plan: `Planner.plan` produces a `ToolPlan`; `Executor.execute_plan` walks `parallel_groups` and gathers tool calls via the shared helper from Plan 16-01; both flow through `BaseLLMClient.call_agentic_turn` (provider-neutral); the parity fixtures from Plan 16-01 replay byte-for-byte through the new collaborators.

Purpose: Wave 2 builds the new collaborators in isolation — no callsite changes. AgentQueryPipeline.run still calls its existing v1.3 internal logic; Plan 16-03 (Wave 3) is the seam swap. Building Planner+Executor in isolation lets parity tests fail-loud BEFORE the seam swap; if the new collaborators don't replay the fixtures, the seam swap MUST NOT proceed.

Output: `utils/models.py` gains `ToolPlan` (~30 lines incl. validators). `services/agent/planner.py` (~120 lines: class, system prompt, singleton factory). `services/agent/executor.py` (~80 lines: class, parallel_groups dispatch, audit field threading). `services/agent/__init__.py` gains 5 re-exports. `tests/unit/test_agent_parity.py` (~120 lines: parametrized fixture loader). `tests/unit/test_planner.py` (~80 lines). `tests/unit/test_executor.py` (~120 lines: parallel dispatch, gather isolation, id round-trip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<task id="16-02-T1" name="RED: Define ToolPlan model + validators with failing tests">
<read_first>
- utils/models.py — match `ToolCall` convention exactly (frozen, validators, naming)
- 16-CONTEXT.md D-01, D-02 (ToolPlan field spec, parallel_groups canonical role)
</read_first>

1. Add `ToolPlan` to `utils/models.py` with the three fields and Pydantic V2 frozen config. INCLUDE the validators listed in must_haves.truths bullet 3.
2. Add `tests/unit/test_planner.py` with failing test cases that:
   - Construct a valid `ToolPlan` with one step in one group; assert `model_dump()` shape.
   - Construct an INVALID `ToolPlan` (empty parallel_groups, out-of-range index, duplicate index across groups, missing index) and assert `pydantic.ValidationError` raised — one parametrized test per validator branch.
   - Test the (yet-unwritten) `Planner.plan(req)` returns a `ToolPlan` instance — this test FAILS at this stage (RED) because Planner is not implemented.
3. Run `pytest tests/unit/test_planner.py -q`. Expected: validator tests PASS; Planner.plan tests FAIL with `ImportError` or `NotImplementedError`.

<acceptance_criteria>
- `ToolPlan` exists in `utils/models.py` with the three fields.
- ALL four validators (empty groups / out-of-range / duplicate / missing index) raise `pydantic.ValidationError` on the documented bad inputs.
- `pytest tests/unit/test_planner.py -q` shows the Planner-related tests in FAIL state (RED gate).
- `mypy --strict utils/models.py` — no NEW errors relative to v1.3 baseline.
</acceptance_criteria>
</task>

<task id="16-02-T2" name="RED: Write Executor parallel-dispatch tests against empty class">
<read_first>
- services/pipeline.py:846 (existing tool dispatch logic — pre-Plan-16-01 baseline; the new helper from Plan 16-01 is at services/agent/tool_executor.py)
- 16-CONTEXT.md D-12 (max_iterations stays in orchestrator, NOT Executor)
</read_first>

1. Add `tests/unit/test_executor.py` with the test cases listed in must_haves.truths bullet 11. Mock `services.agent.executor.call_agentic_turn` and `services.agent.executor.execute_tool_call` at consumer path.
2. Define a stub `Executor` class skeleton in `services/agent/executor.py`:
   ```python
   class Executor:
       async def execute_plan(self, plan: "ToolPlan", *args, **kwargs) -> list:
           raise NotImplementedError
   ```
3. Run `pytest tests/unit/test_executor.py -q`. Expected: all tests FAIL with `NotImplementedError` or stub-related error.

<acceptance_criteria>
- `tests/unit/test_executor.py` has at least 4 distinct test cases covering: parallel_groups dispatch order, asyncio.gather BaseException isolation, tool_call.id round-trip preservation, parallelism_factor audit field.
- All Executor tests are in FAIL state (RED gate).
- The Executor stub raises `NotImplementedError` — never returns garbage.
</acceptance_criteria>
</task>

<task id="16-02-T3" name="RED: Write parity tests against fixtures + empty Planner+Executor">
<read_first>
- tests/unit/fixtures/agent_parity/single_step.json (Plan 16-01 output)
- tests/unit/fixtures/agent_parity/parallel_multi_step.json
- 16-CONTEXT.md D-07, D-08, D-09 (parity proof contract)
</read_first>

1. Add `tests/unit/test_agent_parity.py`. Skeleton:
   ```python
   import json
   import pytest
   from pathlib import Path

   FIXTURES = Path(__file__).parent / "fixtures" / "agent_parity"

   @pytest.mark.parametrize("fixture_name", ["single_step.json", "parallel_multi_step.json"])
   @pytest.mark.asyncio
   async def test_agent_parity(fixture_name, monkeypatch):
       fix = json.loads((FIXTURES / fixture_name).read_text())
       # patch consumer-path call_agentic_turn to yield fix["llm_canned_turns"] in order
       # build Planner + Executor; run; assert ToolCall sequence matches expected_tool_call_sequence
       ...
   ```
2. Implement the patch + assertion logic. Use `model_dump()` comparison with `mode="json"` to compare ToolCall instances byte-for-byte.
3. Define stub `Planner` class skeleton in `services/agent/planner.py` (raises `NotImplementedError` from `plan()`).
4. Run `pytest tests/unit/test_agent_parity.py -q`. Expected: all tests FAIL.

<acceptance_criteria>
- `tests/unit/test_agent_parity.py` has at least 2 parametrized cases (one per fixture).
- All parity tests are in FAIL state with `NotImplementedError` or stub-related error (RED gate).
- `services/agent/planner.py` and `services/agent/executor.py` exist as stubs that raise `NotImplementedError` — no implementation logic in this task.
</acceptance_criteria>
</task>

<task id="16-02-T4" name="GREEN: Implement Planner.plan minimally to pass tests">
<read_first>
- services/generator/llm_client.py — `BaseLLMClient.call_agentic_turn` signature
- 16-CONTEXT.md D-03 (system prompt language rule)
</read_first>

1. Implement `Planner.plan(req)`:
   - Build messages: one system prompt (constant `_PLANNER_SYSTEM` at module top) + user message from `req.query`. System prompt instructs LLM to emit `ToolPlan` JSON with the documented schema and to write `rationale` in the user query's language.
   - Call `await self._llm.call_agentic_turn(messages, tools=[...])` — provider-neutral.
   - Parse the LLM response into `ToolPlan` via `ToolPlan.model_validate_json()`.
   - On parse failure: raise `PlannerOutputError` (custom exception class, narrow scope per project standard — NEVER bare except).
2. Add `_PLANNER_SYSTEM` constant + `PlannerOutputError` class + `get_planner()` singleton factory + `_planner_instance` module-level None.
3. Run `pytest tests/unit/test_planner.py tests/unit/test_agent_parity.py -q`. The Planner test that was RED should now be GREEN. Parity tests likely still RED (Executor not implemented yet — that's T5).

<acceptance_criteria>
- All Planner-specific tests in `test_planner.py` pass (GREEN).
- Parity tests still fail at the Executor step.
- `mypy --strict services/agent/planner.py` exits 0.
- `ruff check services/agent/planner.py` exits 0.
</acceptance_criteria>
</task>

<task id="16-02-T5" name="GREEN: Implement Executor.execute_plan minimally to pass tests">
<read_first>
- 16-CONTEXT.md D-04, D-05 (helper placement), D-12 (cap at orchestrator, not Executor)
- services/agent/tool_executor.py (Plan 16-01 output)
- services/audit/audit_service.py — `parallel_factor` audit field convention from v1.2
</read_first>

1. Implement `Executor.execute_plan(plan, ...)`:
   - For each `group` in `plan.parallel_groups`:
     - Build coroutines: `[execute_tool_call(plan.steps[i], ...) for i in group]`.
     - `results = await asyncio.gather(*coros, return_exceptions=False)` — preserve v1.3 BaseException scope by NOT catching here. Audit + outer cap is orchestrator (Wave 3) responsibility.
     - Append results in `tool_call.id` order to a flat `list[ToolResult]`.
   - Return the flat list. Length equals `sum(len(g) for g in plan.parallel_groups)` which equals `len(plan.steps)`.
   - Log `parallel_factor=len(group)` per group via the structured logger (matches v1.2 convention).
2. Add `get_executor()` singleton factory.
3. Run `pytest tests/unit/test_executor.py tests/unit/test_agent_parity.py -q`. Both should now be GREEN.

<acceptance_criteria>
- All Executor-specific tests pass.
- All parity tests pass (parallel_multi_step replays byte-for-byte through Planner + Executor; single_step replays similarly).
- `mypy --strict services/agent/executor.py` exits 0.
- `ruff check services/agent/executor.py` exits 0.
- `services/agent/__init__.py` re-exports `Planner`, `get_planner`, `Executor`, `get_executor`, `execute_tool_call`.
</acceptance_criteria>
</task>

<task id="16-02-T6" name="REFACTOR: Tighten internals, no behavior change">
<read_first>
- services/agent/planner.py + executor.py (just-written GREEN code)
</read_first>

1. Extract any duplicated message-building logic between Planner and AgentQueryPipeline.run into a shared helper if the duplication is meaningful (≥ 5 lines repeated verbatim).
2. Simplify validators in `ToolPlan` if multiple branches share logic.
3. Update `services/agent/__init__.py` re-exports to match the final public API.
4. Run the FULL unit suite: `pytest tests/unit -q`. MUST: 0 regressions vs v1.3 baseline (623 passed at v1.3 close + new tests added in this plan).

<acceptance_criteria>
- All tests still pass after refactor.
- No new mypy errors anywhere in `services/agent/` or `utils/models.py`.
- `ruff check services/agent/ utils/models.py` exits 0.
- `git diff --stat HEAD~5` (one diff per task commit) shows changes confined to `files_modified` + `__init__.py`.
</acceptance_criteria>
</task>

<verification>
- All RED tests authored in T1-T3 went GREEN by end of T5.
- `pytest tests/unit/test_agent_parity.py tests/unit/test_planner.py tests/unit/test_executor.py -q` exits 0.
- Full unit suite `pytest tests/unit -q` shows 0 regressions vs v1.3 baseline.
- `services/pipeline.py::AgentQueryPipeline.run` is BYTE-IDENTICAL to its state at the end of Plan 16-01 — Wave 3 is where the seam swap happens.
- `python -c "from services.agent import Planner, Executor, execute_tool_call; print('ok')"` prints `ok`.
- coverage on new modules ≥ 80% per file (diff-cover gate from v1.1 Phase 10 TEST-03 — REQUIRED on touched files).
</verification>
