---
phase: 23-background-extractor-schema-migration
plan: 05
type: execute
wave: 3
depends_on:
  - 23-02
  - 23-04
files_modified:
  - services/agent/extractor.py
  - services/pipeline.py
  - tests/unit/test_extractor_dispatch.py
  - tests/unit/test_agent_pipeline_extractor.py
  - tests/unit/test_swarm_pipeline_extractor.py
autonomous: true
requirements: [MEM-04]
tags: [asyncio-create-task, log_task_error, dispatch-wrapper, pipeline-wire-in, log-then-skip, swarm, agent-query]
must_haves:
  truths:
    - "`dispatch_extraction(user_turn, ai_turn, user_id, tenant_id) -> None` body filled (signature takes BOTH turns per eng-review A2): kill-switch FIRST → missing user_id → log-then-skip → missing tenant_id → log-then-skip → both present → `asyncio.create_task(_run_and_persist(...), name='extractor').add_done_callback(log_task_error)`"
    - "`AgentQueryPipeline._persist_turn` calls `dispatch_extraction(user_turn=user_turn, ai_turn=ai_turn, ...)` immediately after `save_turn`, reusing the exact `ConversationTurn` instances passed into `save_turn`"
    - "`SwarmQueryPipeline.run` post-`save_turn` block (~line 1619–1626) calls `dispatch_extraction(user_turn=user_turn, ai_turn=ai_turn, ...)` immediately after `save_turn`, reusing the exact `ConversationTurn` instances passed into `save_turn`"
    - "`QueryPipeline.run` (legacy non-agentic) is NOT wired (per CONTEXT D / RESEARCH §Pattern 5 recommendation)"
    - "Extractor exception inside the task callback is logged via `log_task_error` and does NOT raise into the pipeline's response path"
    - "When `settings.extractor_enabled = False`, `dispatch_extraction` returns immediately (kill-switch path) without firing any task"
  artifacts:
    - path: "services/agent/extractor.py"
      provides: "`dispatch_extraction` body filled (Plan 03 stubbed); adds `_run_and_persist` inner coroutine"
      contains: ["asyncio.create_task", "log_task_error", "operation=\"extractor_skipped\""]
    - path: "services/pipeline.py"
      provides: "Two wire-in sites — `AgentQueryPipeline._persist_turn` + `SwarmQueryPipeline.run`"
      contains: ["from services.agent.extractor import dispatch_extraction", "dispatch_extraction(user_turn=user_turn, ai_turn=ai_turn"]
    - path: "tests/unit/test_extractor_dispatch.py"
      provides: "Dispatch wrapper unit tests: log-then-skip, kill-switch, happy-path task creation, isolation"
      exports: ["test_dispatch_skips_on_missing_user_id", "test_dispatch_skips_on_missing_tenant_id", "test_dispatch_skips_when_disabled", "test_dispatch_fires_create_task", "test_dispatch_extraction_failure_isolated"]
    - path: "tests/unit/test_agent_pipeline_extractor.py"
      provides: "Pipeline-level wire-in test for `AgentQueryPipeline._persist_turn`"
      exports: ["test_persist_turn_dispatches_extractor"]
    - path: "tests/unit/test_swarm_pipeline_extractor.py"
      provides: "Pipeline-level wire-in test for `SwarmQueryPipeline.run`"
      exports: ["test_swarm_run_dispatches_extractor"]
  key_links:
    - from: "services/agent/extractor.py::dispatch_extraction"
      to: "utils.tasks.log_task_error"
      via: "task.add_done_callback(log_task_error)"
      pattern: "add_done_callback\\(log_task_error\\)"
    - from: "services/pipeline.py::AgentQueryPipeline._persist_turn"
      to: "services.agent.extractor.dispatch_extraction"
      via: "post-save_turn synchronous call"
      pattern: "dispatch_extraction\\(user_turn=user_turn, ai_turn="
    - from: "services/pipeline.py::SwarmQueryPipeline.run"
      to: "services.agent.extractor.dispatch_extraction"
      via: "post-save_turn synchronous call"
      pattern: "dispatch_extraction\\(user_turn=user_turn, ai_turn="
---

<objective>
Wire the extractor into the agent pipelines. Three deliverables: (1) fill the body of `dispatch_extraction` in `services/agent/extractor.py` (Plan 03 stubbed it) with the auth-precondition log-then-skip check, the kill-switch check, and the `asyncio.create_task` + `log_task_error` dispatch; (2) call `dispatch_extraction` from `AgentQueryPipeline._persist_turn` immediately after `save_turn` (single attach point covers both `run` and `run_streaming`); (3) call `dispatch_extraction` from `SwarmQueryPipeline.run`'s post-`save_turn` block. `QueryPipeline.run` (legacy non-agentic path) is intentionally NOT wired per CONTEXT D + RESEARCH §Pattern 5.

Purpose: This is the integration plan. Without wire-in, the extractor sits unused. Plan 05 ensures every agent-mode user turn dispatches the extractor in the background with full failure isolation.

Output: One existing file body-filled + one production file modified at two sites + three new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-background-extractor-schema-migration/23-CONTEXT.md
@.planning/phases/23-background-extractor-schema-migration/23-RESEARCH.md
@.planning/phases/23-background-extractor-schema-migration/23-PATTERNS.md
@.planning/phases/23-background-extractor-schema-migration/23-VALIDATION.md
@.planning/phases/23-background-extractor-schema-migration/23-02-SUMMARY.md
@.planning/phases/23-background-extractor-schema-migration/23-04-SUMMARY.md
@services/agent/extractor.py
@services/pipeline.py
@services/events/event_bus.py
@utils/tasks.py

<interfaces>
<!-- Wire-in sites verified against working tree 2026-05-15. -->

From services/pipeline.py:
- `AgentQueryPipeline._persist_turn` — async method starting line 915. Body persists user+assistant turn via `self._memory.save_turn(...)` at line 927, then calls `self._audit.log_query(...)` (lines 943–948 region). Both `AgentQueryPipeline.run` (line 1006) and `AgentQueryPipeline.run_streaming` (line 1172) call `_persist_turn` — single attach point covers both paths.
- `SwarmQueryPipeline.run` — single async method. Post-`save_turn` block at line 1619–1626. `user_id` + `tenant_id` already extracted earlier in the method scope. After the `save_turn` await, audit-log block follows around line 1633.
- `getattr(req, "user_id", "")` + `getattr(req, "tenant_id", "")` is the established pattern for extracting auth fields from `GenerationRequest`.
- `ConversationTurn` already imported at top of pipeline.py.

From services/events/event_bus.py (exact dispatch pattern source — lines 132–133, verified verbatim):
```
_dispatch_task = asyncio.create_task(_consume(), name="event-dispatch")
_dispatch_task.add_done_callback(log_task_error)
```

From utils/tasks.py (lines 14–34 — `log_task_error` contract):
- Signature: `def log_task_error(task: asyncio.Task) -> None`
- Handles `CancelledError` (returns silently) + `InvalidStateError` (returns silently)
- Never re-raises
- Used AS-IS — Plan 05 does NOT wrap with additional try/except

From services/agent/extractor.py (Plan 03 result — stub to fill):
- `def dispatch_extraction(user_turn: ConversationTurn, ai_turn: ConversationTurn, user_id: str | None, tenant_id: str | None) -> None` — empty body with stub docstring (signature carries BOTH turns per eng-review A2)

From config/settings.py (Plan 03 added):
- `extractor_enabled: bool = True` — kill-switch flag

Wire-in call shape (RESEARCH §Pattern 5, **amended per eng-review A2 2026-05-16**):
```
from services.agent.extractor import dispatch_extraction
user_turn = ConversationTurn(role="user", content=req.query)
ai_turn = ConversationTurn(role="assistant", content=answer, sources=[c.doc_id for c in all_chunks[:3]])
await self._memory.save_turn(..., user_turn=user_turn, ai_turn=ai_turn, ...)  # existing call
dispatch_extraction(user_turn=user_turn, ai_turn=ai_turn, user_id=user_id, tenant_id=tenant_id)  # NEW
```
- Pass BOTH `user_turn` and `ai_turn` so the extractor sees the user's preference (in `user_turn.content`) plus the assistant's reply (`ai_turn.content`) for context. The A2 design fix replaced the original ai_turn-only design (which silently extracted zero facts on terse/tool-call assistant replies).
- `user_id` / `tenant_id` come from `req` (already extracted at the wire-in site via the existing `getattr(req, "user_id", "")` line), NOT from either turn (Pitfall #6 — `ConversationTurn` has no user_id field).
- Reuse the EXACT `ConversationTurn` instances already constructed for the `save_turn(...)` call immediately above — avoid building two parallel objects.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — `test_extractor_dispatch.py` unit tests for `dispatch_extraction` body</name>
  <files>tests/unit/test_extractor_dispatch.py</files>
  <read_first>
    - services/agent/extractor.py (Plan 03 result — stub `dispatch_extraction`)
    - utils/tasks.py (`log_task_error` contract)
    - services/events/event_bus.py (lines 132–133 dispatch pattern)
    - tests/unit/test_extractor.py (mock fixture pattern, AgenticTurn helper)
    - .planning/phases/23-background-extractor-schema-migration/23-RESEARCH.md (§Pattern 4 — full dispatch wrapper body)
    - .planning/phases/23-background-extractor-schema-migration/23-PATTERNS.md (§`dispatch_extraction` analog)
  </read_first>
  <behavior>
    - `test_dispatch_skips_on_missing_user_id(caplog, monkeypatch)`: monkeypatch `services.agent.extractor.asyncio.create_task` with a MagicMock. Call `dispatch_extraction(user_turn=<dummy_user>, ai_turn=<dummy_ai>, user_id="", tenant_id="t1")`. Assert `asyncio.create_task` NOT called. Assert `caplog` captured a log record with `extra` containing `operation="extractor_skipped"` and `reason="missing_user_id"`. Repeat with `user_id=None`.
    - `test_dispatch_skips_on_missing_tenant_id(caplog, monkeypatch)`: same shape; `tenant_id=""`, then `tenant_id=None`. Assert log carries `reason="missing_tenant_id"`. Test that missing `user_id` is checked FIRST — call with both missing, assert only `missing_user_id` reason emitted (early-return order matters for diagnostic clarity).
    - `test_dispatch_skips_when_disabled(caplog, monkeypatch)`: monkeypatch `services.agent.extractor.settings.extractor_enabled` to `False`; patch `asyncio.create_task` with MagicMock. Call `dispatch_extraction(user_turn=<dummy_user>, ai_turn=<dummy_ai>, user_id="u1", tenant_id="t1")`. Assert `create_task` NOT called. Assert log carries `operation="extractor_skipped"` and `reason="disabled"` (or equivalent). (Note: kill-switch check ordering vs auth checks is a small design choice — recommend kill-switch FIRST since it's the cheapest path to skip.)
    - `test_dispatch_fires_create_task(monkeypatch)`: monkeypatch `services.agent.extractor.asyncio.create_task` with a MagicMock that returns a MagicMock task (which records `.add_done_callback` calls). Patch `services.agent.extractor.get_extractor` to return a MagicMock with `run = AsyncMock(return_value=[])`. Patch `services.agent.extractor.get_memory_service` to return a MagicMock with `_long.save_fact = AsyncMock()`. Patch settings to enabled+default. Call `dispatch_extraction(user_turn=<dummy_user>, ai_turn=<dummy_ai>, user_id="u1", tenant_id="t1")`. Assert `asyncio.create_task` called exactly once with `name="extractor"`. Assert the returned task's `.add_done_callback` was called exactly once with `log_task_error` (imported from `utils.tasks`).
    - `test_dispatch_extraction_failure_isolated(monkeypatch)`: this is an end-to-end-isolation test. Use real `asyncio.create_task` (no monkeypatch). Patch `get_extractor` to return a mock whose `run` is `AsyncMock(side_effect=RuntimeError("extractor down"))`. Call `dispatch_extraction(user_turn=<dummy_user>, ai_turn=<dummy_ai>, user_id="u1", tenant_id="t1")` synchronously (it returns None immediately). Then `await asyncio.sleep(0.05)` to let the background task complete. Assert no exception raised into the test scope. Assert `caplog` contains a log record from `log_task_error` reporting the failure.
    - `test_dispatch_run_and_persist_calls_save_fact(monkeypatch)`: patch `get_extractor().run` to return `[ExtractedFact(fact="user prefers React", category="stable_preferences", importance=0.8), ExtractedFact(fact="user works in healthcare", category="stable_preferences", importance=0.8)]`. Patch `get_memory_service()._long.save_fact = AsyncMock()`. Call `dispatch_extraction(user_turn=u, ai_turn=a, user_id="u1", tenant_id="t1")`. `await asyncio.sleep(0.05)`. Assert `save_fact` awaited exactly 2 times, each with the correct `user_id`, `tenant_id`, `fact`, and `importance` matching the extracted facts (positional + kwarg correctness).
    - `test_dispatch_zero_facts_skips_save_fact(monkeypatch)`: patch `get_extractor().run` to return `[]`. Patch `save_fact = AsyncMock()`. Call `dispatch_extraction(user_turn=u, ai_turn=a, user_id="u1", tenant_id="t1")`. `await asyncio.sleep(0.05)`. Assert `save_fact.await_count == 0`.

    All 7 tests MUST fail on the post-Plan-04 tree (the `dispatch_extraction` body is still the Plan 03 stub returning None unconditionally).
  </behavior>
  <action>
    Create `tests/unit/test_extractor_dispatch.py`. Use `from __future__ import annotations` + env-var setdefault.

    Imports: `import asyncio, logging, pytest`; `from unittest.mock import AsyncMock, MagicMock`; `from utils.models import ExtractedFact`; `from services.memory.memory_service import ConversationTurn`; `from services.agent.extractor import dispatch_extraction` (the symbol exists from Plan 03 — currently a stub).

    Define an autouse fixture `reset_extractor_singletons(monkeypatch)` that sets `services.agent.extractor._extractor = None` between tests and resets `services.agent.extractor.settings.extractor_enabled = True` to default.

    For `caplog` to capture loguru records, add this conftest snippet OR an inline fixture using `loguru.logger.add(<test-handler>)` — repo convention check: `grep -n 'caplog\|propagate\|loguru' tests/conftest.py` to find the existing pattern. If no existing pattern, use the standard loguru→stdlib propagation recipe:
    ```
    @pytest.fixture
    def caplog_loguru(caplog, monkeypatch):
        from loguru import logger
        handler_id = logger.add(lambda msg: caplog.handler.emit(logging.makeLogRecord({"msg": msg, "level": logging.INFO, "name": "loguru"})), level="INFO")
        yield caplog
        logger.remove(handler_id)
    ```
    Use this fixture in tests that need log assertions. If the conftest already exposes a loguru-caplog adapter, prefer that.

    For tests that need to inspect kwargs passed via loguru's structured-log extras (e.g., `operation="extractor_skipped"`, `reason="missing_user_id"`), patch `services.agent.extractor.logger` with a MagicMock and inspect `mock_logger.info.call_args_list` directly — this avoids loguru/caplog plumbing complexity. The pattern:
    ```
    mock_logger = MagicMock()
    monkeypatch.setattr("services.agent.extractor.logger", mock_logger)
    dispatch_extraction(user_turn=dummy_user_turn, ai_turn=dummy_ai_turn, user_id="", tenant_id="t1")
    assert mock_logger.info.call_count == 1
    call = mock_logger.info.call_args
    assert call.kwargs.get("operation") == "extractor_skipped"
    assert call.kwargs.get("reason") == "missing_user_id"
    ```
    Prefer this approach over caplog where possible — simpler, no fixture plumbing, direct kwarg assertion.

    DO NOT modify production code in this task. All 7 tests fail RED.

    Run: `uv run pytest tests/unit/test_extractor_dispatch.py --collect-only -q` — verify collection succeeds.

    Commit: `test(23-05): MEM-04 dispatch wrapper RED gates`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_extractor_dispatch.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_extractor_dispatch.py` exists with 7 named test functions.
    - `uv run pytest tests/unit/test_extractor_dispatch.py --collect-only -q` lists ≥ 7 items.
    - `uv run pytest tests/unit/test_extractor_dispatch.py::test_dispatch_fires_create_task -x` exits non-zero (RED — stub body returns None without firing create_task).
    - `grep -c 'monkeypatch.setattr("services.agent.extractor' tests/unit/test_extractor_dispatch.py` ≥ 3.
    - `grep -E '^from __future__ import annotations' tests/unit/test_extractor_dispatch.py` matches.
    - `grep -c 'extractor_skipped' tests/unit/test_extractor_dispatch.py` ≥ 2.
    - All plans 01–04 tests still GREEN: `uv run pytest tests/unit/test_memory_*.py tests/unit/test_extractor*.py -x -q --ignore=tests/unit/test_extractor_dispatch.py` exits 0.
  </acceptance_criteria>
  <done>7 RED gates committed for the dispatch wrapper body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — fill `dispatch_extraction` body in `services/agent/extractor.py`</name>
  <files>services/agent/extractor.py</files>
  <read_first>
    - services/agent/extractor.py (Plan 03 result — `dispatch_extraction` stub + `get_extractor` + `Extractor.run`)
    - services/events/event_bus.py (lines 124–133 — verbatim `asyncio.create_task` + `add_done_callback(log_task_error)` shape)
    - utils/tasks.py (`log_task_error` contract)
    - services/memory/memory_service.py (`get_memory_service` factory at line 395–401; `LongTermMemory.save_fact` Plan 02 result)
    - tests/unit/test_extractor_dispatch.py (RED gates from Task 1)
    - .planning/phases/23-background-extractor-schema-migration/23-RESEARCH.md (§Pattern 4 — full body)
  </read_first>
  <action>
    Fill the body of `dispatch_extraction` in `services/agent/extractor.py`. Replace the stub with the full implementation (per RESEARCH §Pattern 4).

    Required imports added at module top (some already present from Plan 03):
    - `import asyncio` (already present)
    - `from utils.tasks import log_task_error` (NEW — Plan 03 imported it but did not use it; verify with grep)

    Body shape:

    ```
    def dispatch_extraction(
        user_turn: ConversationTurn,
        ai_turn: ConversationTurn,
        user_id: str | None,
        tenant_id: str | None,
    ) -> None:
        """Phase 23 / MEM-04 — post-turn fire-and-forget extractor dispatch.

        Pattern source: services/events/event_bus.py:132-133 (verified). Wraps
        asyncio.create_task with log_task_error done-callback so any background
        failure surfaces to logs without affecting the user-facing turn.

        Signature takes BOTH the user_turn and ai_turn (per eng-review A2,
        2026-05-16) — user-preference facts live in `user_turn.content`; the
        assistant's reply is supporting context. The original ai_turn-only
        design extracted zero facts on terse assistant replies.

        Kill-switch: settings.extractor_enabled=False short-circuits.
        Auth precondition: missing user_id or tenant_id → log-then-skip per CONTEXT D.
        Multi-tenant isolation: empty-string fallback is REJECTED (would pollute
        the empty-tenant bucket across requests).
        """
        # Kill-switch check FIRST (cheapest skip path)
        if not settings.extractor_enabled:
            logger.info(
                "extractor skipped",
                operation="extractor_skipped",
                reason="disabled",
            )
            return
        # Auth precondition checks — order matters for diagnostic clarity:
        # missing user_id reported even if tenant_id also missing.
        if not user_id:
            logger.info(
                "extractor skipped",
                operation="extractor_skipped",
                reason="missing_user_id",
            )
            return
        if not tenant_id:
            logger.info(
                "extractor skipped",
                operation="extractor_skipped",
                reason="missing_tenant_id",
            )
            return

        async def _run_and_persist() -> None:
            extractor = get_extractor()
            facts = await extractor.run(user_turn, ai_turn)
            if not facts:
                return
            from services.memory.memory_service import get_memory_service
            mem = get_memory_service()
            for f in facts:
                # save_fact internally embeds + INSERTs; MemoryFactWriteError
                # bubbles into log_task_error (BaseException catch at callback).
                await mem._long.save_fact(
                    user_id=user_id,
                    tenant_id=tenant_id,
                    fact=f.fact,
                    source_doc="",
                    importance=f.importance,
                )

        task = asyncio.create_task(_run_and_persist(), name="extractor")
        task.add_done_callback(log_task_error)
    ```

    Key implementation notes:
    - Three skip paths emit `logger.info` (NOT `logger.error` — these are operational signals, not errors per CONTEXT D).
    - The lazy `from services.memory.memory_service import get_memory_service` import inside `_run_and_persist` avoids a top-of-module circular: `memory_service` may transitively import from `services.agent.*` via integration shims. Verify by running `python -c "from services.agent.extractor import dispatch_extraction"` after the edit.
    - `mem._long.save_fact(...)` — `MemoryService` exposes the `LongTermMemory` instance via the `_long` attribute (per `services/memory/memory_service.py:395-401` `MemoryService` class). Verify by `grep -n 'self._long' services/memory/memory_service.py`. If the accessor is named differently (e.g. `mem.long_term`), use the actual attribute name from the tree.
    - DO NOT wrap `_run_and_persist` with `try/except` (Pitfall #3 mitigation — `log_task_error` callback handles failure at the task boundary).
    - DO NOT pass cap-of-3 to the prompt; the truncation already happened in `Extractor._parse_and_truncate` (Plan 03).

    Run: `uv run pytest tests/unit/test_extractor_dispatch.py -x -q` — expect all 7 GREEN.
    Run: `uv run pytest tests/unit/test_extractor*.py -x -q` — expect all extractor tests GREEN (no regression on Plan 03 / Plan 04).
    Run: `uv run ruff check services/agent/extractor.py` — expect zero violations.

    Commit: `feat(23-05): MEM-04 dispatch_extraction body — kill-switch + log-then-skip + create_task + log_task_error`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_extractor_dispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_extractor_dispatch.py -x -q` exits 0 (all 7 GREEN).
    - `uv run pytest tests/unit/test_extractor*.py -x -q` exits 0 (no regression on plan 03 / 04 extractor tests).
    - `uv run pytest tests/unit/test_memory_*.py -x -q` exits 0 (no regression on plan 01 / 02).
    - `grep -c 'extractor_skipped' services/agent/extractor.py` ≥ 3 (three skip-path log calls).
    - `grep -c 'reason=\"missing_user_id\"' services/agent/extractor.py` ≥ 1.
    - `grep -c 'reason=\"missing_tenant_id\"' services/agent/extractor.py` ≥ 1.
    - `grep -c 'reason=\"disabled\"' services/agent/extractor.py` ≥ 1.
    - `grep -n 'asyncio.create_task' services/agent/extractor.py` matches one line inside dispatch_extraction.
    - `grep -n 'add_done_callback(log_task_error)' services/agent/extractor.py` matches one line.
    - `grep -n 'name=\"extractor\"' services/agent/extractor.py` matches one line.
    - `python -c "from services.agent.extractor import dispatch_extraction; assert callable(dispatch_extraction)"` exits 0 (import cycle check).
    - `uv run ruff check services/agent/extractor.py` exits 0.
  </acceptance_criteria>
  <done>MEM-04 dispatch wrapper body complete. Kill-switch, log-then-skip on missing IDs, and `asyncio.create_task` + `log_task_error` callback wiring all behave per spec. All Wave-0 stubs for the dispatch wrapper turn GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED + GREEN — wire `dispatch_extraction` into `AgentQueryPipeline._persist_turn` + `SwarmQueryPipeline.run`</name>
  <files>services/pipeline.py, tests/unit/test_agent_pipeline_extractor.py, tests/unit/test_swarm_pipeline_extractor.py</files>
  <read_first>
    - services/pipeline.py (lines 915–948 — `AgentQueryPipeline._persist_turn` body; lines 1616–1633 — `SwarmQueryPipeline.run` post-`save_turn` block)
    - services/agent/extractor.py (Task 2 result — filled `dispatch_extraction`)
    - tests/unit/test_extractor_dispatch.py (Task 2 GREEN gates)
    - .planning/phases/23-background-extractor-schema-migration/23-PATTERNS.md (§`AgentQueryPipeline._persist_turn` analog; §`SwarmQueryPipeline.run` analog)
    - .planning/phases/23-background-extractor-schema-migration/23-RESEARCH.md (§Pattern 5 — exact wire-in shape; §Pitfall #6, #7)
  </read_first>
  <behavior>
    For pipeline-level wire-in verification, mock `services.pipeline.dispatch_extraction` at the consumer path (the pipeline imports `dispatch_extraction` — patch where pipeline imports it from, NOT at extractor.py source).

    - `test_persist_turn_dispatches_extractor(monkeypatch)`: instantiate `AgentQueryPipeline` with all deps mocked. Patch `services.pipeline.dispatch_extraction` with a MagicMock. Call `await pipeline._persist_turn(req=<mocked GenerationRequest with user_id='u1', tenant_id='t1', query='I prefer React'>, answer="The answer", all_chunks=[<3 mocked RetrievedChunks with doc_id values 'd1','d2','d3'>], trace_id="tr1", t0=time.perf_counter(), parallelism_factors=[])`. Assert `dispatch_extraction` called exactly once. Inspect the call's kwargs: `user_turn` is a `ConversationTurn` with `role="user"`, `content="I prefer React"`. `ai_turn` is a `ConversationTurn` with `role="assistant"`, `content="The answer"`, `sources=["d1","d2","d3"]`. `user_id="u1"`, `tenant_id="t1"`.
    - `test_swarm_run_dispatches_extractor(monkeypatch)`: instantiate `SwarmQueryPipeline` with all deps mocked. Patch `services.pipeline.dispatch_extraction`. Build a minimal `req` that drives `SwarmQueryPipeline.run` through to the `save_turn` block. Run `await pipeline.run(req)`. Assert `dispatch_extraction` called once with `user_turn.role=="user"`, `user_turn.content==req.query`, `ai_turn.role=="assistant"`, `ai_turn.content==final_answer`, plus correct user_id/tenant_id.
      - **Implementation realism caveat:** `SwarmQueryPipeline.run` is a large method (~200+ lines) that invokes the planner, executor, synthesizer, peer fan-out, optional verifier, etc. Building a unit test for the FULL run path is a Plan 06 (integration) concern. For Plan 05's wire-in test, the goal is to verify the **call shape** at the attach point. Two approaches: (a) full-mock-and-run — patch every collaborator to short-circuit and reach the post-`save_turn` block; (b) inject directly — extract the post-save_turn block into a helper method (refactor) and test the helper. Recommendation: approach (a), with a fixture builder `_build_minimal_swarm_pipeline(monkeypatch)` that mocks planner/executor/synthesizer/verifier/audit/memory at consumer paths so `run(req)` completes through to the dispatch line. Document patches inline in the test for clarity.
      - If approach (a) proves too fragile (test setup > 50 LOC), pivot to a focused assertion using `inspect.getsource(SwarmQueryPipeline.run)` to confirm the `dispatch_extraction(turn=` literal appears in the source — a structural rather than behavioral check. Document this limitation in the test docstring; the integration test in Plan 06 will exercise the real path.
    - `test_query_pipeline_NOT_wired(monkeypatch)`: `inspect.getsource(QueryPipeline.run) ` must NOT contain `dispatch_extraction` — verifies legacy non-agentic path is intentionally skipped per CONTEXT D / RESEARCH §Pattern 5 recommendation.

    All three tests fail RED on the unmodified `services/pipeline.py` (no `dispatch_extraction` import or call site exists yet).
  </behavior>
  <action>
    **STEP A — write RED tests:**

    Create `tests/unit/test_agent_pipeline_extractor.py` and `tests/unit/test_swarm_pipeline_extractor.py`. Both follow the standard test conventions (env-var setdefault, `from __future__ import annotations`, AsyncMock at consumer path).

    For `test_persist_turn_dispatches_extractor`: build a minimal `AgentQueryPipeline` instance by mocking its `__init__` deps. The pattern from existing pipeline tests (grep for `tests/unit/test_pipeline*.py` if available) — typical shape is to instantiate with all deps as MagicMock + AsyncMock-stubbed methods. If pipeline `__init__` requires concrete dependencies that fight mocking, instantiate via `AgentQueryPipeline.__new__(AgentQueryPipeline)` and set required attributes directly (e.g., `pipeline._memory = MagicMock(save_turn=AsyncMock())`, `pipeline._audit = MagicMock(log_query=AsyncMock())`).

    For `test_query_pipeline_NOT_wired`: simple `inspect.getsource` + assertion. No mocking needed.

    For `test_swarm_run_dispatches_extractor`: try approach (a) first; if setup exceeds 50 LOC, fall back to `inspect.getsource(SwarmQueryPipeline.run)` containment check, document the fallback in test docstring.

    Run: `uv run pytest tests/unit/test_agent_pipeline_extractor.py tests/unit/test_swarm_pipeline_extractor.py --collect-only -q` — verify collection succeeds.
    Run: `uv run pytest tests/unit/test_agent_pipeline_extractor.py::test_persist_turn_dispatches_extractor -x` — expect RED.

    Commit: `test(23-05): MEM-04 pipeline wire-in RED gates`.

    **STEP B — write GREEN production edits to services/pipeline.py:**

    Edit 1 — `AgentQueryPipeline._persist_turn` (around line 915–948). Add the import at the top of the file in the existing import block (NOT lazy inside the method — pipeline.py already imports `services.agent.*` at top-level): `from services.agent.extractor import dispatch_extraction`. Verify no circular import via `grep -n 'from services.pipeline' services/agent/extractor.py` — should match zero (extractor does not back-import pipeline).

    **Refactor the existing `save_turn(...)` call** at line 927–935 to hoist the two `ConversationTurn` constructions into local variables, then pass those locals to BOTH `save_turn` and `dispatch_extraction`:
    ```
    user_turn = ConversationTurn(role="user", content=req.query)
    ai_turn = ConversationTurn(
        role="assistant",
        content=answer,
        sources=[c.doc_id for c in all_chunks[:3]],
    )
    await self._memory.save_turn(
        session_id=req.session_id, user_id=user_id, tenant_id=tenant_id,
        user_turn=user_turn,
        ai_turn=ai_turn,
        intent=None,
    )
    dispatch_extraction(
        user_turn=user_turn,
        ai_turn=ai_turn,
        user_id=user_id,
        tenant_id=tenant_id,
    )
    ```
    Notes:
    - `user_id`, `tenant_id` are already in local scope from the `getattr(req, "user_id", "")` line earlier in `_persist_turn` body. Re-use the existing variables, do NOT re-extract.
    - `ConversationTurn` is already imported at top of pipeline.py — `grep -n 'ConversationTurn' services/pipeline.py | head -3` confirms.
    - The hoisted `user_turn`/`ai_turn` locals are the SAME instances passed to both `save_turn` and `dispatch_extraction` — per A2, avoid building two parallel objects.
    - `dispatch_extraction` is synchronous and returns immediately; no `await` keyword. Placed BEFORE the `self._audit.log_query(...)` block.

    Edit 2 — `SwarmQueryPipeline.run` (around line 1619–1626). Refactor the existing `save_turn(...)` block at line 1619–1626 to hoist both `ConversationTurn` constructions into locals, then pass them to BOTH `save_turn` and `dispatch_extraction`. After the existing `await self._memory.save_turn(...)` block and BEFORE the audit-log call around line 1633, insert the dispatch call:
    ```
    user_turn = ConversationTurn(role="user", content=req.query)
    ai_turn = ConversationTurn(role="assistant", content=final_answer)
    await self._memory.save_turn(
        session_id=req.session_id, user_id=user_id, tenant_id=tenant_id,
        user_turn=user_turn,
        ai_turn=ai_turn,
        intent=None,
    )
    dispatch_extraction(
        user_turn=user_turn,
        ai_turn=ai_turn,
        user_id=user_id,
        tenant_id=tenant_id,
    )
    ```
    Notes:
    - `user_id`, `tenant_id` already in scope (extracted earlier in the method around line 1621–1622).
    - `final_answer` is the variable name used in the surrounding save_turn block (verify with `grep -n 'final_answer\|ai_turn' services/pipeline.py | head -10`).
    - In `SwarmQueryPipeline.run`, the existing `save_turn` passes `ai_turn=ConversationTurn(role="assistant", content=final_answer)` (no sources) per PATTERNS §SwarmQueryPipeline.run analog. Mirror — extractor receives the same assistant turn shape without sources.
    - As with Edit 1, the hoisted locals are passed to BOTH `save_turn` and `dispatch_extraction` to avoid parallel object construction.

    DO NOT wire `QueryPipeline.run` (legacy non-agentic path at lines 575 + 643). RESEARCH §Pattern 5 explicit: extractor is agent-tier; wiring into QueryPipeline blurs the boundary.

    Run: `uv run pytest tests/unit/test_agent_pipeline_extractor.py tests/unit/test_swarm_pipeline_extractor.py -x -q` — expect all GREEN.
    Run: `uv run pytest tests/unit/ -x -q --ignore=tests/integration/` — full unit-suite green, no regression on existing pipeline tests.
    Run: `uv run ruff check services/pipeline.py` — expect zero violations.
    Run: `uv run mypy --strict services/pipeline.py` — expect no new violations (or document the baseline).

    Commit: `feat(23-05): MEM-04 wire dispatch_extraction into AgentQueryPipeline._persist_turn + SwarmQueryPipeline.run post-save_turn`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_agent_pipeline_extractor.py tests/unit/test_swarm_pipeline_extractor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_agent_pipeline_extractor.py tests/unit/test_swarm_pipeline_extractor.py -x -q` exits 0 (all 3 GREEN).
    - `uv run pytest tests/unit/ -x -q --ignore=tests/integration/` exits 0 (no regression across the unit suite).
    - `grep -n 'from services.agent.extractor import dispatch_extraction' services/pipeline.py` matches exactly one line.
    - `grep -c 'dispatch_extraction(' services/pipeline.py` equals 2 (one call in AgentQueryPipeline, one in SwarmQueryPipeline).
    - `grep -c 'user_turn=user_turn, ai_turn=ai_turn' services/pipeline.py` equals 2 (both wire-in sites use the A2 kwarg form).
    - `inspect.getsource(QueryPipeline.run)` does NOT contain `dispatch_extraction` — verified by test_query_pipeline_NOT_wired.
    - `uv run ruff check services/pipeline.py` exits 0.
    - `python -c "from services.pipeline import AgentQueryPipeline, SwarmQueryPipeline, QueryPipeline; import inspect; assert 'dispatch_extraction' in inspect.getsource(AgentQueryPipeline._persist_turn); assert 'dispatch_extraction' in inspect.getsource(SwarmQueryPipeline.run); assert 'dispatch_extraction' not in inspect.getsource(QueryPipeline.run)"` exits 0.
  </acceptance_criteria>
  <done>MEM-04 fully wired. Both agent-tier pipelines dispatch the extractor post-save_turn. Legacy `QueryPipeline.run` intentionally skipped per CONTEXT decision. All extractor tests GREEN; broader unit suite still GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → dispatch_extraction | Synchronous handoff; auth fields (`user_id`, `tenant_id`) cross from the request context into the dispatch precondition check. |
| dispatch_extraction → asyncio.create_task | Fire-and-forget task boundary; failures within the task surface via `log_task_error` callback only, never propagate to the pipeline's response. |
| Task callback → log_task_error | Background-task done callback; `log_task_error` handles CancelledError + InvalidStateError silently, never re-raises (utils/tasks.py:25-26). |
| _run_and_persist → save_fact | `MemoryFactWriteError` from Plan 02 propagates to `log_task_error` callback; never reaches the user response. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-A1 | Tampering | Empty-string `tenant_id` fallback (CONTEXT D rejected option) | mitigate | Explicit `if not tenant_id: log-then-skip` check in dispatch wrapper. Test `test_dispatch_skips_on_missing_tenant_id` covers both `""` and `None`. |
| T-23-05-D1 | Denial | Extractor failure propagates into user response | mitigate | `asyncio.create_task` schedules `_run_and_persist` on the event loop without awaiting. Pipeline returns its response synchronously after the synchronous `dispatch_extraction` call. Test `test_dispatch_extraction_failure_isolated` verifies a raising extractor does NOT raise into the caller. |
| T-23-05-D2 | Denial / partial write | save_fact failure mid-iteration | mitigate | `_run_and_persist` iterates `for f in facts: await save_fact(...)`. If `save_fact` raises on fact #2 of 3, facts #1 is committed (atomic per Plan 02), the raise propagates to `log_task_error`, fact #3 is skipped. This is acceptable — partial-write at the **multi-fact** level is by design (each fact is its own row; Plan 02 zero-partial-write contract is at the **single-row** level). Log captures the failure for ops review. |
| T-23-05-R1 | Repudiation | Background failure not logged | mitigate | `log_task_error` callback fires on every task completion (success or failure), logs failures with full traceback. Three skip paths emit structured logs with `operation="extractor_skipped"`. |
| T-23-05-I1 | Info disclosure | Cross-tenant fact write via `user_id` confusion at pipeline layer | accept | `user_id` and `tenant_id` are extracted from `req` (JWT-resolved upstream by the controller). Plan 05 does not introduce new auth handling; existing JWT-resolution path is the trust source. Empty-string skip already mitigates the empty-tenant-pool risk. |
| T-23-05-E1 | Elevation | Kill-switch bypass via settings tampering | accept | `settings.extractor_enabled` is a `bool = True` Pydantic BaseSettings field; tampering requires env-var modification at process startup. Out of scope — same trust boundary as every other settings flag. |
| T-23-05-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages. |
</threat_model>

<verification>
- All Plan 05 task tests GREEN.
- All Plan 01–04 tests still GREEN (no regression).
- Pipeline-level wire-in adds exactly 2 call sites (`AgentQueryPipeline._persist_turn` + `SwarmQueryPipeline.run`); legacy `QueryPipeline.run` intentionally skipped (verified by `test_query_pipeline_NOT_wired`).
- `dispatch_extraction` import is at top of pipeline.py (no lazy/circular).
- ruff + (best-effort) mypy --strict clean on touched files.

Run gate: `uv run pytest tests/unit/test_extractor_dispatch.py tests/unit/test_agent_pipeline_extractor.py tests/unit/test_swarm_pipeline_extractor.py -x -q && uv run pytest tests/unit/ -x -q --ignore=tests/integration/ && uv run ruff check services/agent/extractor.py services/pipeline.py`
</verification>

<success_criteria>
- ROADMAP SC-4 partially satisfied: user turn with stable preference triggers `asyncio.create_task` background extraction (unit-test verified — Task 1 happy path); the within-2s row-appearance + p95 latency-delta claims are verified by Plan 06 integration test.
- ROADMAP SC-5 satisfied: extractor exception is isolated — Task 1 `test_dispatch_extraction_failure_isolated` proves the pipeline turn completes normally even when the extractor raises. `log_task_error` captures the failure structurally.
- MEM-04 structurally complete: dispatch wrapper body + both agent-tier wire-ins land in the tree.
- The complete extractor → save_fact → long_term_facts pipeline is now operational end-to-end at the unit level. Plan 06 verifies it end-to-end with real PG + an actual `asyncio.create_task` completion.
</success_criteria>

<output>
Create `.planning/phases/23-background-extractor-schema-migration/23-05-SUMMARY.md` when done.
</output>
