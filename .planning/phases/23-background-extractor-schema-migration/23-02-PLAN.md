---
phase: 23-background-extractor-schema-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - services/memory/memory_service.py
  - services/vectorizer/embedder.py
  - tests/unit/test_memory_save_fact.py
  - tests/unit/test_openai_embedder.py
autonomous: true
requirements: [MEM-02]
tags: [pgvector, embed-on-write, save_fact, typed-exception, asyncpg, narrow-exceptions, openai-dimensions-fix]
must_haves:
  truths:
    - "`save_fact(user_id, tenant_id, fact, source_doc, importance)` computes the embedding via `get_embedder().embed_one(fact)` before acquiring a DB connection"
    - "A successful call writes exactly one `long_term_facts` row with a non-NULL 1024-dim `embedding` column populated"
    - "Embedding-adapter failure raises `MemoryFactWriteError` with `__cause__` set; zero partial-write rows committed (DB INSERT never reached)"
    - "asyncpg.PostgresError during INSERT raises `MemoryFactWriteError` with `__cause__` set"
    - "Signature `(self, user_id, tenant_id, fact, source_doc='', importance=0.5)` is preserved — no caller-visible API change"
    - "`OpenAIEmbedder.embed_batch` passes `dimensions=settings.embedding_dim` to the OpenAI API call so `text-embedding-3-large` returns 1024-dim vectors matching the `VECTOR(1024)` column (closes RESEARCH §Pitfall 2 silent-failure mode)"
  artifacts:
    - path: "services/memory/memory_service.py"
      provides: "Rewritten `LongTermMemory.save_fact` (embed-on-write) — embedding param at $6 cast `::vector`"
      contains: ["get_embedder().embed_one", "embedding", "$6::vector", "MemoryFactWriteError"]
    - path: "services/vectorizer/embedder.py"
      provides: "`OpenAIEmbedder.embed_batch` gains `dimensions=settings.embedding_dim` kwarg on the `embeddings.create` call (Pitfall 2 fix)"
      contains: ["dimensions=settings.embedding_dim"]
    - path: "tests/unit/test_memory_save_fact.py"
      provides: "Unit tests covering happy path, embedder failure (no partial write), asyncpg failure"
      exports: ["test_save_fact_embeds", "test_embedder_failure_no_partial", "test_pg_failure_typed_error"]
    - path: "tests/unit/test_openai_embedder.py"
      provides: "Regression test verifying `OpenAIEmbedder.embed_batch` passes `dimensions=settings.embedding_dim` to the OpenAI API (Pitfall 2)"
      exports: ["test_openai_embedder_passes_dimensions_kwarg"]
  key_links:
    - from: "services/memory/memory_service.py::LongTermMemory.save_fact"
      to: "services.vectorizer.embedder.get_embedder().embed_one"
      via: "lazy import inside method"
      pattern: "get_embedder\\(\\)\\.embed_one"
    - from: "services/memory/memory_service.py::LongTermMemory.save_fact"
      to: "long_term_facts.embedding column"
      via: "INSERT ... $6::vector"
      pattern: "\\$6::vector"
---

<objective>
Rewrite `LongTermMemory.save_fact()` to embed the fact text internally via `services.vectorizer.embedder.get_embedder().embed_one()` BEFORE acquiring an asyncpg pool connection, then INSERT both `fact` and `embedding` in a single statement with explicit `$6::vector` cast. Embedder failure surfaces as `MemoryFactWriteError`; asyncpg failure surfaces as `MemoryFactWriteError`; zero partial-write rows on either failure mode. Signature unchanged (preserves existing callers, including the scripts referenced in CONTEXT and the Plan 05 dispatch wrapper).

Purpose: Closes the write half of the agent-writable memory contract. Without this, Plan 05's dispatch wrapper would write rows without embeddings, breaking Phase 24's recall path.

Output: One modified production method (≤25 lines net diff) + one new test file with three tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-background-extractor-schema-migration/23-CONTEXT.md
@.planning/phases/23-background-extractor-schema-migration/23-RESEARCH.md
@.planning/phases/23-background-extractor-schema-migration/23-PATTERNS.md
@.planning/phases/23-background-extractor-schema-migration/23-VALIDATION.md
@.planning/phases/23-background-extractor-schema-migration/23-01-SUMMARY.md
@services/memory/memory_service.py
@services/vectorizer/embedder.py
@services/vectorizer/vector_store.py

<interfaces>
<!-- Symbols Plan 02 must align with. Extracted from working tree on 2026-05-15. -->

From services/memory/memory_service.py (current `save_fact` at lines 255–269):
```
async def save_fact(self, user_id: str, tenant_id: str, fact: str, source_doc: str = "", importance: float = 0.5) -> None
```
Current body: try/`pool.acquire`/`INSERT INTO long_term_facts (user_id, tenant_id, fact, source_doc, importance) VALUES ($1,$2,$3,$4,$5)` / `except asyncpg.PostgresError` / `logger.error` (NO raise — silently swallowed). Plan 01 introduced `MemoryFactWriteError`.

From services/vectorizer/embedder.py:
- `def get_embedder() -> BaseEmbedder` (singleton factory at line 232–247)
- `async def embed_one(self, text: str) -> list[float]` (ABC at line 32; default impl delegates to `embed_batch([text])[0]`)
- Concrete embedder failure surfaces (from RESEARCH §Pattern 2):
  - `OllamaEmbedder.embed_batch` raises `RuntimeError` on httpx failure (embedder.py:68)
  - `OllamaEmbedder` httpx transport → `httpx.HTTPError`
  - `OpenAIEmbedder` → `httpx.HTTPError`
  - `HuggingFaceEmbedder` torch device load → `OSError`

From services/vectorizer/vector_store.py (`$N::vector` cast precedent at lines 264–276):
- `INSERT INTO {table} (..., embedding, ...) VALUES (..., $5::vector, ...)` — explicit cast required for the asyncpg parameter binding (pgvector codec registered in pool init from Plan 01)

From services/memory/memory_service.py (Plan 01 result):
- `class MemoryFactWriteError(Exception)` — defined near top of file, subclasses `Exception`, no extra fields
- `_get_pool` now passes `init=_init_conn` registering pgvector codec on every connection
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — `test_memory_save_fact.py` covering happy path + embed failure + asyncpg failure</name>
  <files>tests/unit/test_memory_save_fact.py</files>
  <read_first>
    - services/memory/memory_service.py (current `save_fact` at lines 255–269; Plan 01 result around `MemoryFactWriteError`)
    - services/vectorizer/embedder.py (lines 29–110 — `BaseEmbedder.embed_one`, concrete embedders' failure modes)
    - tests/unit/test_memory_service.py (env-var setdefault at module top + reset_memory_singleton autouse fixture)
    - tests/unit/test_verifier.py (AsyncMock + mock-at-consumer-path style)
    - .planning/phases/23-background-extractor-schema-migration/23-PATTERNS.md (§`tests/unit/test_save_fact_embed.py` — full test scaffold including fake_pool wiring)
    - .planning/phases/23-background-extractor-schema-migration/23-VALIDATION.md (per-task verification map for MEM-02)
  </read_first>
  <behavior>
    - Test 1 (`test_save_fact_embeds_one_row_with_1024_dim_embedding`): patch `services.memory.memory_service.get_embedder` to return a MagicMock whose `embed_one` is `AsyncMock(return_value=[0.1] * 1024)`; set `mem._pool` to a fake pool whose `acquire` async-context yields a `conn` with `execute = AsyncMock()`; call `await mem.save_fact(user_id="u1", tenant_id="t1", fact="user prefers React", importance=0.8)`. Assert `embed_one` awaited once with `"user prefers React"`. Assert `conn.execute` awaited exactly once; assert the SQL string in the call args contains `INSERT INTO long_term_facts` AND `embedding` AND `$6::vector`; assert the positional params are `("u1", "t1", "user prefers React", "", 0.8, [0.1]*1024)`.
    - Test 2 (`test_save_fact_embedder_failure_raises_typed_error_no_partial_write`): patch `get_embedder` to return a MagicMock whose `embed_one` is `AsyncMock(side_effect=RuntimeError("ollama down"))`; set fake `mem._pool` with `conn.execute = AsyncMock()`. Call `save_fact` inside `pytest.raises(MemoryFactWriteError) as exc_info`. Assert `conn.execute.await_count == 0` (zero partial-write — INSERT never reached). Assert `isinstance(exc_info.value.__cause__, RuntimeError)`. Repeat parametrize with `httpx.HTTPError("transport")` and `OSError("torch device")` — three sub-cases under the same test (use `pytest.mark.parametrize("exc_cls,exc_msg", [(RuntimeError, "..."), (httpx.HTTPError, "..."), (OSError, "...")])`).
    - Test 3 (`test_save_fact_pg_failure_raises_typed_error`): patch `get_embedder` for success; patch `conn.execute` with `AsyncMock(side_effect=asyncpg.PostgresError("dim mismatch"))`. Call `save_fact` inside `pytest.raises(MemoryFactWriteError)`. Assert `isinstance(exc_info.value.__cause__, asyncpg.PostgresError)`.
    - Test 4 (`test_save_fact_signature_unchanged`): inspect via `inspect.signature(LongTermMemory.save_fact)`; assert parameter names equal `("self", "user_id", "tenant_id", "fact", "source_doc", "importance")` in that order; assert defaults `source_doc=""`, `importance=0.5`.

    All four tests MUST fail on the post-Plan-01 tree (current `save_fact` body has no embed call, swallows errors silently). Failures expected: `AssertionError` (Test 1 — no `embed_one` call, no `$6::vector` in SQL); silent swallow (Test 2 and Test 3 — no exception raised, `pytest.raises` block fails).
  </behavior>
  <action>
    Create `tests/unit/test_memory_save_fact.py` with `from __future__ import annotations` + the env-var setdefault block at module top (mirror tests/unit/test_memory_service.py). Imports: `import asyncpg, httpx, pytest, inspect` + `from unittest.mock import AsyncMock, MagicMock`.

    For the fake pool fixture, define a module-scope helper `def _make_fake_pool(execute_mock: AsyncMock) -> MagicMock` returning a pool whose `acquire().__aenter__` resolves to `MagicMock(execute=execute_mock)` and `__aexit__` returns False. Reuse across all three "with pool" tests.

    Use `monkeypatch.setattr("services.memory.memory_service.get_embedder", lambda: <mock>)` — mock-at-consumer-path per v1.3 D-08.

    `pytest.raises(MemoryFactWriteError)` import path: `from services.memory.memory_service import LongTermMemory, MemoryFactWriteError`.

    Use `pytest.mark.asyncio` decorator on async tests; rely on `asyncio_mode=auto` from pytest.ini but explicit decorator improves readability.

    DO NOT touch production code in this task. Run: `uv run pytest tests/unit/test_memory_save_fact.py -x -q` — expect failures.

    Commit: `test(23-02): add save_fact RED gates — embed-on-write + no-partial-write + typed errors (MEM-02)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_memory_save_fact.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_memory_save_fact.py` exists with the four named test functions.
    - `uv run pytest tests/unit/test_memory_save_fact.py --collect-only -q` lists ≥ 4 + parametrize-expansion test items (collection succeeds).
    - `uv run pytest tests/unit/test_memory_save_fact.py::test_save_fact_embeds_one_row_with_1024_dim_embedding -x` exits non-zero (RED).
    - `grep -c 'monkeypatch.setattr("services.memory.memory_service' tests/unit/test_memory_save_fact.py` ≥ 2 (consumer-path discipline).
    - `grep -E '^from __future__ import annotations' tests/unit/test_memory_save_fact.py` matches.
    - `grep -c 'pytest.raises(MemoryFactWriteError)' tests/unit/test_memory_save_fact.py` ≥ 2.
  </acceptance_criteria>
  <done>Three RED gates + signature gate committed. All fail with the expected `AssertionError` / silent-swallow on the post-Plan-01 tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — rewrite `save_fact` to embed-on-write + typed-error contract</name>
  <files>services/memory/memory_service.py</files>
  <read_first>
    - services/memory/memory_service.py (current `save_fact` at lines 255–269; Plan 01 result: `MemoryFactWriteError` near top, register_vector pool init in `_get_pool`)
    - services/vectorizer/vector_store.py (lines 264–276 — `$N::vector` INSERT precedent including positional binding shape)
    - services/vectorizer/embedder.py (lines 32, 54–110, 232–247 — `embed_one` ABC, concrete failure modes, `get_embedder()` factory)
    - .planning/phases/23-background-extractor-schema-migration/23-RESEARCH.md (§Pattern 2 — full rewrite body including narrow-exception list rationale)
    - .planning/phases/23-background-extractor-schema-migration/23-PATTERNS.md (§`save_fact` analog 1+2+3)
    - tests/unit/test_memory_save_fact.py (RED gates from Task 1)
  </read_first>
  <action>
    Rewrite `LongTermMemory.save_fact` (currently `services/memory/memory_service.py:255-269`). Preserve the signature `async def save_fact(self, user_id: str, tenant_id: str, fact: str, source_doc: str = "", importance: float = 0.5) -> None`.

    Body order (mandatory — embed BEFORE connection acquire so we never hold a DB connection across an LLM-adjacent API call):

    Step 1 — Lazy-import inside the function body (NOT at module top — circular-import resilience per existing convention):
    - `from services.vectorizer.embedder import get_embedder`
    - `import httpx` (lazy — already an indirect dep of embedders but not currently imported by memory_service)

    Step 2 — Compute embedding with narrow-exception catch list:
    - `try: embedding: list[float] = await get_embedder().embed_one(fact)`
    - `except (httpx.HTTPError, RuntimeError, OSError) as exc:`
      - `logger.error("memory service failure", operation="save_fact_embed", exc_info=exc)`
      - `raise MemoryFactWriteError("embedding failed") from exc`
    - Narrow-exception list rationale (RESEARCH §Pattern 2 narrow-exception list source): `RuntimeError` covers `OllamaEmbedder.embed_batch` re-raise (embedder.py:68); `httpx.HTTPError` covers Ollama + OpenAI transport; `OSError` covers HuggingFace torch device. NO bare `except` per CLAUDE.md ERR-01.

    Step 3 — INSERT with explicit cast:
    - `try: pool = await self._get_pool(); async with pool.acquire() as conn:`
      - `await conn.execute("""INSERT INTO long_term_facts (user_id, tenant_id, fact, source_doc, importance, embedding) VALUES ($1,$2,$3,$4,$5,$6::vector)""", user_id, tenant_id, fact, source_doc, importance, embedding,)`
    - `except asyncpg.PostgresError as exc:`
      - `logger.error("memory service failure", operation="save_fact", exc_info=exc)`
      - `raise MemoryFactWriteError("persistence failed") from exc`

    The two try/except blocks are SEPARATE — Step 2's catch returns before Step 3 runs, satisfying "zero partial-write" (the test verifies `conn.execute.await_count == 0` on embedder failure).

    Use Python triple-quoted multiline SQL string for the INSERT to keep it readable. The `$6::vector` cast is mandatory because pgvector requires explicit casting when binding `list[float]` to a `vector` column even with `register_vector` codec installed (vector_store.py:264-276 precedent).

    Do NOT change `source_doc=""` default. Do NOT add new parameters. Do NOT change return type (`None`).

    Critical contract change vs current code: the current `save_fact` silently swallows `asyncpg.PostgresError` with only a log.error. Plan 02 RAISES `MemoryFactWriteError` after logging. Other callers in the tree (verify via `grep -rn "save_fact" --include='*.py'` before committing) — Phase 23 introduces only one new caller (Plan 05's `_run_and_persist`) which lives inside an `asyncio.create_task` whose `add_done_callback(log_task_error)` surfaces the raise as a structured log entry (Pitfall #3 mitigation). Existing callers that catch nothing will now see exceptions; document this in the commit message.

    Run: `uv run pytest tests/unit/test_memory_save_fact.py tests/unit/test_memory_schema.py tests/unit/test_memory_pool.py -x -q` — expect all GREEN. Run: `uv run ruff check services/memory/memory_service.py` — expect zero violations.

    Commit: `feat(23-02): MEM-02 save_fact embed-on-write rewrite — typed MemoryFactWriteError on embedder/asyncpg failure; zero partial writes`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_memory_save_fact.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_memory_save_fact.py -x -q` exits 0 (all 4 + parametrize tests GREEN).
    - `uv run pytest tests/unit/test_memory_schema.py tests/unit/test_memory_pool.py -x -q` exits 0 (Plan 01 tests still GREEN — no regression).
    - `grep -n 'get_embedder()\.embed_one' services/memory/memory_service.py` matches one line inside `save_fact`.
    - `grep -n '\$6::vector' services/memory/memory_service.py` matches exactly one line.
    - `grep -n 'raise MemoryFactWriteError' services/memory/memory_service.py` matches exactly two lines (embed-failure path + asyncpg-failure path).
    - `grep -n 'except (httpx.HTTPError, RuntimeError, OSError)' services/memory/memory_service.py` matches exactly one line.
    - `grep -v '^#' services/memory/memory_service.py | grep -c 'except Exception' | tr -d ' '` equals 0 (no broad `except Exception`).
    - `grep -v '^#' services/memory/memory_service.py | grep -c 'except:' | tr -d ' '` equals 0 (no bare except — ERR-01 enforced).
    - `uv run ruff check services/memory/memory_service.py` exits 0.
    - `python -c "import inspect; from services.memory.memory_service import LongTermMemory; sig = inspect.signature(LongTermMemory.save_fact); assert list(sig.parameters.keys()) == ['self','user_id','tenant_id','fact','source_doc','importance']"` exits 0.
  </acceptance_criteria>
  <done>MEM-02 GREEN. `save_fact` embeds via `embed_one` before INSERT, INSERTs with `$6::vector` cast, raises typed `MemoryFactWriteError` on both failure paths with `__cause__` chained. Zero partial-write rows on embedder failure. Signature preserved. All Wave-0 stubs for Plans 01+02 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A1 fix — pass `dimensions=settings.embedding_dim` to OpenAI embeddings.create (Pitfall 2 closure)</name>
  <files>services/vectorizer/embedder.py, tests/unit/test_openai_embedder.py</files>
  <read_first>
    - services/vectorizer/embedder.py (current `OpenAIEmbedder.embed_batch` at lines 84-93 — no `dimensions` kwarg)
    - .planning/phases/23-background-extractor-schema-migration/23-RESEARCH.md (§Pitfall 2 — full silent-failure analysis)
    - tests/unit/test_verifier.py (AsyncMock pattern for OpenAI client mocks)
  </read_first>
  <behavior>
    - `test_openai_embedder_passes_dimensions_kwarg`: monkeypatch `openai.AsyncOpenAI` so `OpenAIEmbedder.__init__` does not require a real API key. Patch the constructed client's `embeddings.create` with an `AsyncMock` whose return resembles the real response (`MagicMock(data=[MagicMock(embedding=[0.1]*1024)])`). Call `await OpenAIEmbedder().embed_batch(["fact"])`. Assert `embeddings.create` was awaited exactly once with kwargs containing `dimensions=settings.embedding_dim` (i.e. `1024`), `model=settings.embedding_model or "text-embedding-3-large"`, `input=["fact"]`, `encoding_format="float"`. Assert returned list length is 1 and inner vector length is 1024.
    - This test MUST fail on the pre-fix tree (the current `embed_batch` body has no `dimensions` kwarg).
  </behavior>
  <action>
    Patch `services/vectorizer/embedder.py` `OpenAIEmbedder.embed_batch` (currently lines 84-93):

    Add `dimensions=settings.embedding_dim,` to the `self._client.embeddings.create(...)` call so the API request becomes:

    ```python
    resp = await self._client.embeddings.create(
        model=self._model,
        input=texts,
        encoding_format="float",
        dimensions=settings.embedding_dim,
    )
    ```

    Rationale: `text-embedding-3-large` returns native 3072-dim unless the API call explicitly passes `dimensions`. The `long_term_facts.embedding` column from Plan 01 is `VECTOR(settings.embedding_dim)` = `VECTOR(1024)`. Without this kwarg, every `save_fact` call with `embedding_provider="openai"` raises pgvector dimension-mismatch inside the background task and is swallowed by `log_task_error` — silent prod-only failure invisible to the HuggingFace-default test suite. RESEARCH §Pitfall 2 + §ASSUMED row both flagged this for /plan-eng-review; PR-LEVEL DECISION (eng-review 2026-05-16): fix `OpenAIEmbedder` rather than constrain provider, because the same kwarg already benefits the chunks-table embed path and avoids gating Phase 23 on a provider-allowlist runtime guard.

    Also create `tests/unit/test_openai_embedder.py` with the regression test described above.

    DO NOT widen scope into other embedder methods or other providers. Single-line kwarg add + single regression test.

    Run: `uv run pytest tests/unit/test_openai_embedder.py -x -q && uv run ruff check services/vectorizer/embedder.py`.

    Commit: `fix(23-02): OpenAIEmbedder passes dimensions=settings.embedding_dim — closes Pitfall 2 silent-failure when embedding_provider=openai (A1)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_openai_embedder.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_openai_embedder.py::test_openai_embedder_passes_dimensions_kwarg -x` exits 0.
    - `grep -n 'dimensions=settings.embedding_dim' services/vectorizer/embedder.py` matches exactly one line inside the `OpenAIEmbedder.embed_batch` body.
    - `uv run ruff check services/vectorizer/embedder.py` exits 0.
    - No other embedder method modified (`grep -c 'dimensions=' services/vectorizer/embedder.py` == 1).
  </acceptance_criteria>
  <done>A1 closed. `OpenAIEmbedder.embed_batch` now requests 1024-dim vectors from OpenAI, matching the `long_term_facts.embedding` column schema. Silent-failure path for `embedding_provider="openai"` is eliminated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save_fact caller ↔ embedder | `fact` text passes from caller (extractor or test) into `embed_one`. fact text is user-originated (extracted from conversation) but is normalized by Pydantic `ExtractedFact.fact` validator (≤200 chars, non-empty) before reaching `save_fact` from the extractor path. |
| save_fact ↔ PostgreSQL | Parameterized `INSERT ... $1,$2,...,$6` — never f-string interpolation of user input |
| save_fact ↔ external embedding API | Network call via httpx (Ollama/OpenAI) or local torch (HuggingFace); failure modes enumerated in narrow exception list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-T1 | Tampering | SQL injection via `fact` | mitigate | Parameterized `$1,$2,$3,$4,$5,$6` positional binding. `fact` arrives as positional param — asyncpg never interpolates user input into the SQL string. Pre-existing pattern from current code; preserved verbatim. |
| T-23-02-D1 | Denial / partial write | Embedder failure mid-write | mitigate | Step 2 (embed) precedes Step 3 (INSERT) in separate try blocks. Embedder exception path returns via `raise MemoryFactWriteError` BEFORE `_get_pool` is called. Test 2 verifies `conn.execute.await_count == 0` on embedder failure. |
| T-23-02-D2 | Denial / partial write | asyncpg failure after embedding | mitigate | Single `conn.execute` call inserts all 6 columns atomically. No two-statement transaction; pg auto-commits or rolls back the whole INSERT. Test 3 verifies typed `MemoryFactWriteError` surfaces with original `asyncpg.PostgresError` as `__cause__`. |
| T-23-02-I1 | Info disclosure | `MemoryFactWriteError` message exposes embedding-API details | accept | Error message is two static strings (`"embedding failed"` / `"persistence failed"`). The `__cause__` chain carries provider-specific exception types but is only surfaced via `log_task_error` to internal logs, never to the user response (SC-5 isolation). |
| T-23-02-R1 | Repudiation | save_fact failure not auditable | mitigate | `logger.error("memory service failure", operation="save_fact_embed"|"save_fact", exc_info=exc)` emits structured log with operation label and full traceback before the typed raise. Matches existing `services/memory/memory_service.py:204,234,252,269,286,305` shape. |
| T-23-02-E1 | Elevation | Tenant-id confusion at write | accept | `tenant_id` is a positional INSERT param, not f-string. Plan 05 dispatch wrapper enforces non-empty `tenant_id` BEFORE calling `save_fact`, closing the empty-string-pollution gap. Plan 02 itself does not validate `tenant_id` (caller responsibility, matches current convention). |
| T-23-02-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages (RESEARCH §Package Legitimacy Audit "Not applicable"). |
</threat_model>

<verification>
- All three Task 1 RED gates flip to GREEN after Task 2.
- Plan 01's tests still pass (no regression on schema/pool layer).
- `save_fact` signature unchanged — existing callers (test fixtures, scripts) don't break at call-site.
- Narrow-exception list complete: `httpx.HTTPError + RuntimeError + OSError` cover all three concrete embedder failure paths.
- `MemoryFactWriteError.__cause__` preserved via `raise ... from exc` (debuggability).
- `grep` gates confirm no bare except, no broad `except Exception`.

Run gate: `uv run pytest tests/unit/test_memory_save_fact.py tests/unit/test_memory_schema.py tests/unit/test_memory_pool.py -x -q && uv run ruff check services/memory/memory_service.py`
</verification>

<success_criteria>
- ROADMAP SC-2 satisfied: `save_fact` writes one row with non-NULL 1024-dim embedding (Test 1); embedder failure → `MemoryFactWriteError` with zero partial-write rows (Test 2); asyncpg failure → `MemoryFactWriteError` (Test 3 — covers the verification-clause in SC-2 about `SELECT count(*)` before/after).
- MEM-02 requirement structurally complete.
- Plan 05 has a fault-tolerant write surface to consume.
</success_criteria>

<output>
Create `.planning/phases/23-background-extractor-schema-migration/23-02-SUMMARY.md` when done.
</output>
