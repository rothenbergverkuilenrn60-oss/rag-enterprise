---
phase: 19-agent-first-docs-demo-release
plan: 08
type: execute
wave: 6
depends_on: [19-01, 19-02, 19-03, 19-04, 19-05, 19-06, 19-07]
files_modified:
  - .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md
  - .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md
autonomous: false
requirements: [AGENT-08]
must_haves:
  truths:
    - "Tag annotation message drafted: 6-bullet headline (1 thesis + 4 phases + 1 milestone) per D-15"
    - "GitHub release notes drafted (~150-250 lines per D-13): full prose; links to all 4 phase SUMMARYs + design doc + CHANGELOG + asciinema cast URL"
    - "User-runnable command list provided for tag-cutting + push + GitHub release publish"
    - "Release ceremony NOT executed by Claude — autonomous: false; user runs the commands post-PR-merge"
    - "ROADMAP SC5 closed: v1.4 release tag-and-notes ready to cut from master after merge"
  artifacts:
    - path: ".planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md"
      provides: "GitHub release notes draft (full prose; ~150-250 lines)"
      contains: "v1.4 — Agent-first architecture inversion"
    - path: ".planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md"
      provides: "Tag-cutting command list + GitHub release publish flow (user runs these post-PR-merge)"
      contains: "git tag -a v1.4.0 master -m"
  key_links:
    - from: "release-notes-v1.4.md"
      to: "docs/v1.4-design.md + CHANGELOG.md + 4 phase SUMMARYs"
      via: "markdown links"
      pattern: "(v1.4-design|CHANGELOG|16-03-SUMMARY|17-03-SUMMARY|18-03-SUMMARY|19-08-SUMMARY)\\.md"
    - from: "release-tag-commands.md"
      to: "release-notes-v1.4.md"
      via: "gh release create --notes-file"
      pattern: "--notes-file.*release-notes-v1.4\\.md"
---

<objective>
Release ceremony preparation for v1.4. Three deliverables, ALL DRAFTING ONLY:

1. **Tag annotation message** — terse 6-bullet headline per D-15. Recorded in
   `release-notes-v1.4.md` so the user can copy-paste into the `git tag -a` command.
2. **GitHub release notes** — full prose per D-13 (~150-250 lines). Same file.
3. **User-runnable command list** — tag cut, push, GitHub release publish. Recorded in
   `release-tag-commands.md` so the user runs them locally post-PR-merge per D-12.

Plan is **autonomous: false** — Claude prepares the artifacts; user executes the
release ceremony after PR merge. v1.4.0 is permanent; the human-in-the-loop gate
prevents accidental tag-cutting on a not-yet-merged branch.

Purpose: lock SC5 ("v1.4 release tag created on `master` after merge; release notes
link to design doc + the four phase summaries").

Output: 2 markdown drafts under `.planning/phases/19-agent-first-docs-demo-release/`.
NO actual git tag, NO push, NO GitHub release published.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md
@.planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md
@.planning/phases/19-agent-first-docs-demo-release/19-01-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-02-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-03-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-04-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-05-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-06-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-07-SUMMARY.md
@CHANGELOG.md
@docs/v1.4-design.md
@README.md

<interfaces>
<!-- D-15 tag annotation shape: 6-line headline. -->

```
v1.4.0 — Agent-first architecture inversion

- Phase 16: Planner + Executor extraction (AGENT-06, AGENT-09, NLU-03)
- Phase 17: Tool abstraction + RetrieveTool (AGENT-07)
- Phase 18: SSE event stream (AGENT-04)
- Phase 19: Agent-first docs + demo + release (AGENT-08)
- Architectural inversion: agent runtime is the core; RAG is one tool the agent calls.
```

<!-- D-12 manual release flow. -->

1. PR opens from `gsd/v1.3-milestone` → `master`. PR body links the v1.4 milestone-design markdown + all 4 phase SUMMARYs.
2. Review + merge to `master`.
3. User locally:
   ```bash
   git fetch origin master
   git tag -a v1.4.0 origin/master -m "<tag-annotation>"
   git push origin v1.4.0
   gh release create v1.4.0 --notes-file .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md
   ```
4. After release publishes, user updates CHANGELOG.md compare-link footer placeholders (`<owner>/<repo>` → canonical URL).

<!-- Existing gh CLI release-create flow (verified via gh --help). -->

```bash
gh release create <tag> [<assets>...] [flags]
  -F, --notes-file <file>    Read release notes from file
  -t, --title <string>       Release title
  -p, --prerelease           Mark as a prerelease
  -d, --draft                Save as a draft
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md (tag annotation + full release notes)</name>
  <files>.planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md</files>
  <read_first>
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-12, D-13, D-15 (release flow + release-notes scope + tag annotation shape)
    - .planning/phases/19-agent-first-docs-demo-release/19-01-SUMMARY.md through 19-07-SUMMARY.md (verify they exist; pull headline outcomes from each)
    - .planning/phases/16-planner-executor-extraction/16-03-SUMMARY.md (Phase 16 release-note bullet content)
    - .planning/phases/17-tool-abstraction-retrievetool/17-03-SUMMARY.md (Phase 17 release-note bullet content)
    - .planning/phases/18-sse-planner-trace-event-stream/18-03-SUMMARY.md (Phase 18 release-note bullet content)
    - CHANGELOG.md (verify v1.4.0 entry exists; release notes mirror its structure)
    - docs/v1.4-design.md (verify it exists; release notes link to it)
    - .planning/phases/19-agent-first-docs-demo-release/.demo-cast-url (read if exists; release notes embed the asciinema URL — graceful fallback to docs/demo.cast OR docs/demo.gif if URL absent)
  </read_first>
  <action>
    Create `.planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md`.

    The file has TWO clearly-marked sections separated by a horizontal rule:

    **Section A — Tag annotation (terse, ≤ 8 lines).**

    Heading `## Tag annotation (for `git tag -a v1.4.0 -m "..."`)`. Followed by a fenced code block containing exactly this content (copy-paste-ready into the `-m` flag):

    ```
    v1.4.0 — Agent-first architecture inversion

    Phase 16: Planner + Executor extraction (AGENT-06, AGENT-09, NLU-03)
    Phase 17: Tool abstraction + RetrieveTool (AGENT-07)
    Phase 18: SSE event stream (AGENT-04)
    Phase 19: Agent-first docs + demo + release (AGENT-08)

    Architectural inversion: the agent runtime (Planner / Executor / Synthesizer)
    is the project's core; agentic RAG is one of the tools the agent calls.
    ```

    Note: D-15 specifies "5-bullet summary (one per phase 16-19 + the milestone thesis)". Final shape: 1 headline + 4 phase bullets + 1 thesis paragraph = 6 lines of content + 2 separator blank lines.

    **Section B — GitHub release notes (full prose, ~150-250 lines).**

    Horizontal rule (`---`), then heading `## GitHub release notes (full prose for `gh release create --notes-file`)`. Body content (write verbatim, target ~180 lines):

    ````markdown
    # v1.4 — Agent-First Architecture Inversion

    *Released 2026-05-09. Cut from `master` after the v1.4 milestone PR merge.*

    A Planner → Executor → Synthesizer agent. RAG is one of its tools.

    v1.4 inverts the architecture: where v1.0..v1.3 framed EnterpriseRAG as a
    RAG platform with an optional agentic mode, v1.4 makes the agent runtime
    the project's core. The Planner emits a `ToolPlan`. The Executor walks
    `parallel_groups` and dispatches concurrently with `BaseException`
    isolation. The Synthesizer composes the final answer. Hybrid retrieval
    (pgvector + BM25 + RRF + reranker) is now `RetrieveTool` — one of several
    tools the agent calls. The same registry that ships `RetrieveTool` is
    designed for MCP tool discovery to replace it later without changing
    callsites.

    Full architectural thesis: [docs/v1.4-design.md](https://github.com/<owner>/<repo>/blob/v1.4.0/docs/v1.4-design.md) (Approach A — incremental refactor, no framework lock-in).

    ## What changed

    ### Phase 16 — Planner + Executor extraction

    `services/pipeline.py::AgentQueryPipeline.run` rewritten as a thin
    orchestrator (43 lines) over `Planner` + `Executor` + `Synthesizer`. Each
    collaborator has a single-purpose Pydantic V2 frozen interface
    (`ToolPlan`, `ToolCall`). Query intent is encoded as `ToolPlan` shape —
    no separate `IntentRouter` class. Behavioral parity vs v1.3 baseline
    asserted before any new behavior landed.

    `_execute_tool_call` consolidated to `services/agent/tool_executor.py`
    and consumed by both `SwarmQueryPipeline` and the new `Executor` —
    `grep -rn "def _execute_tool_call" services/` returns 0 (the helper
    moved to `execute_tool_call` in `services/agent/`).

    Closes AGENT-06, AGENT-09, NLU-03.
    See [Phase 16 SUMMARY](https://github.com/<owner>/<repo>/blob/v1.4.0/.planning/phases/16-planner-executor-extraction/16-03-SUMMARY.md).

    ### Phase 17 — Tool abstraction + RetrieveTool

    `BaseTool` ABC + static `ToolRegistry` (`services/agent/tools/`).
    `RetrieveTool` wraps `QueryPipeline.run()` — v1.3 retrieval behavior
    preserved on existing test fixtures (no recall/rank regression).
    `RefinedRetrieveTool` shares `_retrieve_impl` with `RetrieveTool`.
    `WebSearchTool` placeholder registered but excluded from
    `AGENT_TOOL_ALLOWLIST` — proves pluggability with a non-RAG implementation.

    `Executor` dispatches strictly through the registry; no direct imports of
    tool classes by name in pipeline code. Tool authoring guide stub at
    [docs/agent-architecture.md#authoring-tools](https://github.com/<owner>/<repo>/blob/v1.4.0/docs/agent-architecture.md#authoring-tools) with one runnable example.

    Closes AGENT-07.
    See [Phase 17 SUMMARY](https://github.com/<owner>/<repo>/blob/v1.4.0/.planning/phases/17-tool-abstraction-retrievetool/17-03-SUMMARY.md).

    ### Phase 18 — SSE planner-trace event stream

    New endpoint `POST /api/v1/agent/v1/run/stream` emits a structured event
    stream as the agent runs:

    | Event | Emitted | Contains |
    |-------|---------|----------|
    | `planner.plan` | once per non-terminal `ToolPlan` | the full `ToolPlan` |
    | `tool.span.start` | once per dispatch (before await) | `span_id`, tool `name`, `args` |
    | `tool.span.end` | once per resolved dispatch | `latency_ms`, `chunk_count`, `is_error`, `content_preview` (200-char) |
    | `tool.span.error` | replaces end on `BaseException` | `error_type`, `error_message` (200-char) |
    | `executor.parallel` | once per group at group END | `fan_out`, `group_latency_ms` |
    | `synthesizer.final` | terminal | `answer`, `sources_count` |

    Latency assertion: agentic queries with N parallel tools complete in
    `max(tool_latency) + planner + synthesizer + small overhead`, NOT
    `sum(tool_latency)`. Verified by integration test
    `tests/unit/test_agent_sse.py::test_run_streaming_latency_bounded_by_max_not_sum_d14_sc4`
    (4 × 0.5 s tools complete in 450..700 ms wall-time).

    Closes AGENT-04.
    See [Phase 18 SUMMARY](https://github.com/<owner>/<repo>/blob/v1.4.0/.planning/phases/18-sse-planner-trace-event-stream/18-03-SUMMARY.md).

    ### Phase 19 — Agent-first docs + demo + release

    `docs/agent-architecture.md` now covers the Concept → Tool authoring →
    Wire format trilogy with a runnable code snippet in each section.

    `make demo-agent` runs the 4-tool parallel fan-out from a clean checkout
    in ~1.5 seconds. Stub LLM + fixture tools — no API keys required, no
    Docker stack required. The same code path as the SC4 latency test;
    the demo IS the integration test, dressed up with a real query string.

    README rewritten with agent-first framing; v1.3 technical content
    preserved under `## Platform features` (D-04 — no information lost,
    framing inverted). Asciinema cast embedded near the top showing the
    SSE event timeline with visible parallel fan-out.

    Closes AGENT-08.
    See [Phase 19 SUMMARY](https://github.com/<owner>/<repo>/blob/v1.4.0/.planning/phases/19-agent-first-docs-demo-release/19-08-SUMMARY.md).

    ## Demo

    [If `.planning/phases/19-agent-first-docs-demo-release/.demo-cast-url` exists, embed:]

    [![asciinema demo](https://asciinema.org/a/<id>.svg)](https://asciinema.org/a/<id>)

    [Else if `docs/demo.gif` exists, embed:]

    ![Phase 19 demo](https://github.com/<owner>/<repo>/raw/v1.4.0/docs/demo.gif)

    [Else, fall back to a link:]

    Replay locally: [`docs/demo.cast`](https://github.com/<owner>/<repo>/blob/v1.4.0/docs/demo.cast). Run `asciinema play docs/demo.cast`.

    From a clean checkout:
    ```bash
    git clone https://github.com/<owner>/<repo>.git && cd <repo>
    make demo-agent
    ```

    Exits 0 in ~1.5 s. Prints the SSE event stream to stdout. No API keys, no
    Docker stack required.

    ## Carried forward from v1.3

    - PostgreSQL + pgvector with HNSW + Row-Level Security (multi-tenant by construction)
    - Hybrid retrieval (dense + BM25 + RRF + reranker) — now inside `RetrieveTool`
    - Async ingest (`POST /ingest/async` + ARQ/Redis worker)
    - Image extraction via PyMuPDF (AGPL-3.0 — commercial deployments require a separate license)
    - JWT startup validation, per-route rate limiting, PII blocking, CORS lockdown
    - Combined unit + integration coverage with ≥ 70% floor (TEST-06) and ≥ 80% diff-cover gate (TEST-03)
    - Provider-neutral agentic layer (`BaseLLMClient.call_agentic_turn` — Anthropic, OpenAI, Azure, Ollama)

    ## Upgrade notes

    - **Endpoint surface:** `/query`, `/query/stream`, `/query/agent`, `/ingest`, `/ingest/async`, `/ingest/status/{task_id}` UNCHANGED. The new `/agent/v1/run/stream` endpoint is the canonical agent-streaming entrypoint going forward; the legacy `/query?agent_mode=true` path remains as a thin alias.
    - **Tool registration:** custom tools authored against the v1.4 `BaseTool` ABC will continue to work in v1.5+ when MCP plug-in discovery replaces the static class registry — the `BaseTool` interface is the stable contract.
    - **SSE event schema:** the 6-event surface is locked; future minor versions may add fields (additive) but will not rename or remove existing ones. Subscribe via `EventSource` (consumer snippet in [docs/agent-architecture.md#consuming-the-stream](https://github.com/<owner>/<repo>/blob/v1.4.0/docs/agent-architecture.md#consuming-the-stream)).

    ## Roadmap (next)

    Deferred to v1.5+:
    - Real `WebSearchTool` implementation (placeholder in v1.4)
    - `SQLTool`, `MCPTool`
    - `SwarmQueryPipeline.run_streaming` (Phase 18 D-11 deferral)
    - OpenTelemetry-style trace propagation (Phase 18 D-08 — currently 8-hex `trace_id`)
    - GitHub Actions auto-tag workflow

    See [.planning/ROADMAP.md](https://github.com/<owner>/<repo>/blob/v1.4.0/.planning/ROADMAP.md) for the full forward plan.

    ## Acknowledgements

    v1.0..v1.3 milestones: see [CHANGELOG.md](https://github.com/<owner>/<repo>/blob/v1.4.0/CHANGELOG.md).
    Per-milestone roadmaps archived under [.planning/milestones/](https://github.com/<owner>/<repo>/tree/v1.4.0/.planning/milestones/).

    ---

    Full diff: `v1.3.0...v1.4.0` ([compare](https://github.com/<owner>/<repo>/compare/v1.3.0...v1.4.0)).
    ````

    Notes:
    - `<owner>/<repo>` placeholders throughout — user substitutes the canonical repo URL when running `gh release create`. Document this in the command file (Task 2).
    - Demo embed has THREE conditional forms — pick exactly one based on which artifact plan 19-05 produced. Indicate the chosen form by leaving only ONE of the three "[If ...]" sections in the final file (delete the other two and the "[Else ...]" markers).
    - Total length target: 150-250 lines per D-13. Count after writing: `wc -l release-notes-v1.4.md` should return 160-260.
  </action>
  <verify>
    <automated>test -s .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md && grep -c "v1.4 — Agent-first architecture inversion\\|Phase 1[6-9]" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` exists.
    - Total length 100-300 lines (`wc -l` returns N in this range).
    - Tag annotation section present: `grep -c "## Tag annotation" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns 1.
    - Tag annotation contains the 6-line headline shape: `grep -cE "^v1\\.4\\.0 — Agent-first architecture inversion$" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns 1; `grep -cE "^Phase 1[6-9]: " .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns ≥ 4.
    - GitHub release notes section present: `grep -c "## GitHub release notes" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns 1.
    - All 4 phase SUMMARYs linked: `grep -cE "16-03-SUMMARY|17-03-SUMMARY|18-03-SUMMARY|19-08-SUMMARY" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns ≥ 4.
    - Design doc linked: `grep -c "v1.4-design.md" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns ≥ 1.
    - CHANGELOG linked: `grep -c "CHANGELOG.md" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns ≥ 1.
    - All 6 v1.4 requirement IDs cited: `grep -cE "AGENT-04|AGENT-06|AGENT-07|AGENT-08|AGENT-09|NLU-03" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns ≥ 6.
    - Demo embed: at least ONE of three forms present — `grep -cE "asciinema\\.org/a/|docs/demo\\.gif|docs/demo\\.cast" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns ≥ 1.
    - All four major Architectural Inversion bullets cited: `grep -cE "Planner|Executor|Synthesizer|ToolPlan|RetrieveTool" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns ≥ 5.
    - Compare-link present: `grep -cE "compare/v1\\.3\\.0\\.\\.\\.v1\\.4\\.0" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns 1.
    - No real credentials: `grep -cE "sk-[a-zA-Z0-9]{20,}|Bearer [A-Za-z0-9._\\-]{20,}" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` returns 0.
    - Commit message: `docs(19-08-T1): draft v1.4 release notes (tag annotation + GitHub release prose)`.
  </acceptance_criteria>
  <done>release-notes-v1.4.md committed with both sections (tag annotation + full release notes prose). Cross-links to all four phase SUMMARYs + design doc + CHANGELOG present.</done>
</task>

<task type="auto">
  <name>Task 2: Draft .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md (user-runnable command list)</name>
  <files>.planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md</files>
  <read_first>
    - .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md (verify it exists from Task 1)
    - CHANGELOG.md (compare-link footer placeholders to substitute)
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-12 (release flow steps 1-4)
  </read_first>
  <action>
    Create `.planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md`. Single document, ~50-80 lines, structured as a numbered runbook the user follows after PR merge:

    ````markdown
    # v1.4 release ceremony — user-runnable command list

    *Plan 19-08 prepares the artifacts (release notes, tag annotation). The user runs the commands below LOCALLY after the v1.4 milestone PR merges to `master`.*

    *Per CONTEXT.md D-12: no CI auto-tag in v1.4 — manual tag from master keeps the release ceremonial. The release-cutter reviews the merged code one more time before tagging.*

    ## Prerequisites

    - [ ] v1.4 milestone PR merged to `master` (Phase 16 + 17 + 18 + 19 all in)
    - [ ] Local clone has `master` checked out (or you'll fetch it)
    - [ ] `gh` CLI authenticated (`gh auth status` shows authenticated)
    - [ ] Canonical repo URL known (the GitHub `<owner>/<repo>` to substitute throughout)

    ## Step 1 — Substitute the repo URL placeholder

    Several artifacts use `<owner>/<repo>` placeholders. Identify your canonical repo URL (e.g. `acme-org/rag-enterprise`) and substitute throughout. Files to update:

    ```bash
    REPO="<owner>/<repo>"  # ← REPLACE with your canonical repo (e.g. "acme-org/rag-enterprise")

    # CHANGELOG.md compare-link footer:
    sed -i "s|<owner>/<repo>|${REPO}|g" CHANGELOG.md

    # release-notes-v1.4.md cross-links:
    sed -i "s|<owner>/<repo>|${REPO}|g" .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md
    ```

    Verify (must return 0):

    ```bash
    grep -c "<owner>/<repo>" CHANGELOG.md .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md
    ```

    Commit the substitution:

    ```bash
    git add CHANGELOG.md .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md
    git commit -m "docs: substitute canonical repo URL in CHANGELOG + v1.4 release notes"
    git push origin master
    ```

    ## Step 2 — Pull the latest master

    ```bash
    git checkout master
    git pull --ff-only origin master
    git log --oneline -5
    ```

    Confirm the v1.4 milestone PR's merge commit is `HEAD` (or near it). If not, STOP and figure out why — do not proceed.

    ## Step 3 — Cut the annotated tag

    Extract the tag annotation from `release-notes-v1.4.md` Section A (everything inside the fenced code block under `## Tag annotation`):

    ```bash
    TAG_MSG="$(awk '/^## Tag annotation/{flag=1; next} /^---$/{flag=0} flag && /^[^`]/' .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md | sed -n '/```/,/```/p' | sed '1d;$d')"
    echo "${TAG_MSG}" | head -10
    ```

    Verify the message looks correct (6 lines: 1 headline + 4 phase bullets + 1 thesis paragraph). Then tag:

    ```bash
    git tag -a v1.4.0 master -m "${TAG_MSG}"
    git tag -v v1.4.0 | head -5
    ```

    Verify the tag annotation message is exactly what `release-notes-v1.4.md` prescribes.

    ## Step 4 — Push the tag

    ```bash
    git push origin v1.4.0
    ```

    Verify on GitHub: `https://github.com/${REPO}/releases/tag/v1.4.0` should show the tag with the annotation visible.

    ## Step 5 — Publish the GitHub release

    Use the gh CLI with the prepared release notes file. The `--notes-file` flag reads the FULL prose from Section B of `release-notes-v1.4.md`:

    ```bash
    # Extract Section B (everything after the `---` separator):
    awk '/^## GitHub release notes/{flag=1; next} flag' .planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md > /tmp/v1.4.0-release-notes.md

    # Verify it looks right:
    head -10 /tmp/v1.4.0-release-notes.md
    wc -l /tmp/v1.4.0-release-notes.md   # expect 100..300

    # Publish:
    gh release create v1.4.0 \
        --title "v1.4.0 — Agent-First Architecture Inversion" \
        --notes-file /tmp/v1.4.0-release-notes.md \
        --verify-tag
    ```

    Optional: attach `docs/demo.cast` and/or `docs/demo.gif` as release assets:

    ```bash
    gh release upload v1.4.0 docs/demo.cast docs/demo.gif --clobber 2>/dev/null || true
    ```

    ## Step 6 — Verify the release publishes correctly

    - [ ] Visit `https://github.com/${REPO}/releases/tag/v1.4.0`
    - [ ] Release title: "v1.4.0 — Agent-First Architecture Inversion"
    - [ ] Body renders the markdown cleanly (headings, tables, code blocks)
    - [ ] All four phase SUMMARY links resolve (clicking them lands on the rendered SUMMARY at the v1.4.0 tag)
    - [ ] The asciinema embed (or gif) renders inline
    - [ ] Compare-link at the bottom resolves to a valid `compare/v1.3.0...v1.4.0` GitHub diff page

    ## Step 7 — Update STATE.md

    Mark v1.4 as shipped in `.planning/STATE.md`:

    ```bash
    # Edit STATE.md frontmatter: change `status: executing` to `status: shipped`
    # Update `last_updated` to today's date
    # Update `last_activity` to "2026-05-09 — v1.4 milestone shipped (tag v1.4.0)"
    ```

    Commit:

    ```bash
    git add .planning/STATE.md
    git commit -m "docs(state): mark v1.4 milestone shipped"
    git push origin master
    ```

    ## Rollback (if Step 6 reveals a problem)

    ```bash
    # Delete remote tag:
    git push --delete origin v1.4.0
    # Delete local tag:
    git tag -d v1.4.0
    # Delete GitHub release:
    gh release delete v1.4.0 --yes
    ```

    Then fix the underlying issue, force-push the docs fix on master, and re-run Steps 3-6.
    ````

    Notes:
    - The runbook is plain commands a human runs at a terminal — no Claude execution.
    - The `awk` extractors in Steps 3 and 5 parse `release-notes-v1.4.md` deterministically based on the section markers from Task 1.
    - Rollback section provided per release-best-practice; v1.4.0 is permanent if Step 6 passes, but the path is documented for safety.
  </action>
  <verify>
    <automated>test -s .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md && grep -c "git tag -a v1.4.0\\|gh release create v1.4.0" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` exists, 50-150 lines.
    - 7 numbered steps present: `grep -cE "^## Step [1-7] " .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns 7.
    - Tag-cut command present: `grep -c "git tag -a v1.4.0 master" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns 1.
    - Tag-push command present: `grep -c "git push origin v1.4.0" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns ≥ 1.
    - gh release create command present: `grep -c "gh release create v1.4.0" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns 1.
    - Release notes file referenced: `grep -c "release-notes-v1.4.md" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns ≥ 3.
    - Repo-URL placeholder substitution step present: `grep -c "<owner>/<repo>" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns ≥ 2 (substitution instruction + at least one example).
    - Rollback section present: `grep -c "## Rollback" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns 1; `grep -c "gh release delete v1.4.0" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns 1.
    - STATE.md update step present: `grep -c "STATE.md" .planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` returns ≥ 1.
    - No actual git tag was cut (verify on local repo): `git tag -l | grep -c "^v1\\.4\\.0$"` returns 0.
    - No actual gh release was published (verify on GitHub): `gh release list | grep -c "^v1\\.4\\.0\\b" 2>/dev/null` returns 0 (skip this check if `gh` not authenticated).
    - Commit message: `docs(19-08-T2): draft v1.4 release-tag commands runbook`.
  </acceptance_criteria>
  <done>release-tag-commands.md committed with 7-step runbook + rollback. NO actual tag was cut.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User runs the release ceremony post-PR-merge</name>
  <what-built>
    Plan 19-08 has prepared TWO drafts:
    1. `.planning/phases/19-agent-first-docs-demo-release/release-notes-v1.4.md` — tag annotation + GitHub release notes prose
    2. `.planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` — 7-step runbook for cutting the tag, pushing, and publishing the GitHub release

    The actual release ceremony (steps in `release-tag-commands.md`) is NOT executed by Claude — it's a human-in-the-loop gate per CONTEXT.md D-12 because v1.4.0 is permanent and the cutter wants to review the merged code one more time before tagging.
  </what-built>
  <how-to-verify>
    1. Open the v1.4 milestone PR (`gsd/v1.3-milestone` → `master`). Confirm:
       - All 4 phase commits are present (Phases 16/17/18/19)
       - CI is green
       - Reviewers have approved
       - There are NO outstanding review comments

    2. Merge the PR to `master`.

    3. Open `.planning/phases/19-agent-first-docs-demo-release/release-tag-commands.md` and follow Steps 1-7 verbatim. Each step has its own verification gate; do not skip.

    4. After Step 5 (release published), verify on GitHub:
       - The release page shows the body rendered cleanly (headings, tables, code blocks)
       - All 4 phase SUMMARY links resolve (clicking lands on the rendered SUMMARY at tag `v1.4.0`)
       - The asciinema embed (or gif) renders inline
       - The compare-link `compare/v1.3.0...v1.4.0` resolves to a valid GitHub diff page

    5. Type "released" once Steps 1-7 are complete and verified, OR describe any failure (e.g. "Step 5 failed: gh release create returned 422", "Step 6: phase 18 SUMMARY link 404") so plan 19-08 can iterate the artifacts.

    Decision branch — if the release publishes cleanly and all verification gates pass, type `released`. If any gate fails, describe the failure to trigger a fix-and-retry pass.
  </how-to-verify>
  <resume-signal>
    Type "released" once `gh release view v1.4.0` returns a published release with all cross-links resolving. Or describe specific issues to trigger a fix pass on the drafts (release-notes-v1.4.md or release-tag-commands.md).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tag annotation → public release | Tag message is permanent and public. Must not contain placeholders that look like real values, must not contain credentials. |
| GitHub release notes → public consumers | Cross-links must resolve at tag `v1.4.0`; any 404 erodes trust. |
| Manual ceremony → permanent tag | v1.4.0 is permanent; rollback exists but is observable. Human-in-the-loop gate prevents accidental tag-cut on a not-yet-merged branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08-01 | Tampering | Tag annotation cut from a non-master branch | mitigate | Step 2 of `release-tag-commands.md` explicitly checks `git log --oneline -5` and STOPs if HEAD is not at the v1.4 PR merge commit. Step 3 uses `git tag -a v1.4.0 master` (explicit `master` ref). |
| T-19-08-02 | Information Disclosure | `<owner>/<repo>` placeholder leaked into the published release | mitigate | Step 1 substitutes the placeholder with `sed -i` on both CHANGELOG.md and release-notes-v1.4.md. Verification grep gate `grep -c "<owner>/<repo>"` must return 0 before proceeding to Step 2. |
| T-19-08-03 | Tampering | release-notes-v1.4.md modified after tag is cut, drifting from the tag annotation | accept | The release notes are extracted from the file at Step 5 time; if the file changes between Step 3 and Step 5, the GitHub release body diverges from the tag message. Documented risk; mitigation is to run Steps 3-5 in one sitting. |
| T-19-08-04 | Denial of Service | gh release create failing due to network or auth issues | mitigate | `--verify-tag` flag added to `gh release create` to fail fast if the tag was not pushed correctly. Rollback section provides delete commands if the release publishes in a broken state. |
| T-19-08-05 | Elevation of Privilege | Claude executing the release ceremony autonomously | mitigate | Plan is `autonomous: false`; Tasks 1+2 only DRAFT the artifacts. Task 3 is `checkpoint:human-action`. Claude does NOT run `git tag`, `git push`, or `gh release create` — these are human-only actions per D-12 contract. |
| T-19-08-06 | Tampering | Forward-reference link `19-08-SUMMARY.md` not yet existing at tag-cut time | mitigate | Step 7 of the runbook implicitly creates the SUMMARY before the tag is cut (Step 3 happens after Step 7? — actually Step 7 is post-tag). Resolution: plan 19-08 outputs a SUMMARY file at the END of plan execution (per `<output>` section), so the SUMMARY exists by the time the user runs Step 3 of the runbook. Verified by acceptance_criteria for plan 19-08 itself. |
</threat_model>

<verification>
- 2 markdown drafts under `.planning/phases/19-agent-first-docs-demo-release/`.
- release-notes-v1.4.md: tag annotation (Section A) + full GitHub release notes prose (Section B).
- release-tag-commands.md: 7-step runbook + rollback.
- NO actual `git tag v1.4.0` cut. NO actual `gh release create` invoked.
- Task 3 is a human checkpoint — Claude does NOT execute the release ceremony.
</verification>

<success_criteria>
- v1.4 release notes drafted with all required cross-links (4 phase SUMMARYs + design doc + CHANGELOG + asciinema cast).
- Tag annotation drafted per D-15 (terse 6-line headline).
- Runbook drafted per D-12 (manual flow, no CI auto-tag).
- Plan is autonomous: false; user runs the ceremony after PR merge.
- 2 atomic commits.
</success_criteria>

<output>
After completion, create `.planning/phases/19-agent-first-docs-demo-release/19-08-SUMMARY.md`
</output>
