---
phase: 19-agent-first-docs-demo-release
plan: 06
type: execute
wave: 5
depends_on: [19-04, 19-05, 19-07]
files_modified:
  - README.md
autonomous: true
requirements: [AGENT-08]
must_haves:
  truths:
    - "README opens with the agent-first thesis: 'A Planner → Executor → Synthesizer agent. RAG is one of its tools.'"
    - "Section order matches D-02 exactly: Title+Thesis → Quick demo → Architecture → Tools the agent calls → Platform features → Observability → Quick start → Project status"
    - "Asciinema cast embedded near the top (`## Quick demo`) so peer engineers see fan-out at first scroll"
    - "All v1.3 technical content preserved under `## Platform features` (reorganized + reframed in agent-first terms — D-04)"
    - "Cross-links resolve: docs/agent-architecture.md#planner-executor-model + docs/v1.4-design.md + CHANGELOG.md + docs/demo.cast (or docs/demo.gif)"
    - "cURL example targets POST /api/v1/agent/v1/run/stream with --no-buffer flag and JWT placeholder"
  artifacts:
    - path: "README.md"
      provides: "Agent-first README closing ROADMAP SC1"
      contains: "A Planner → Executor → Synthesizer agent. RAG is one of its tools."
  key_links:
    - from: "README.md::## Quick demo"
      to: "docs/demo.cast or docs/demo.gif"
      via: "asciinema HTML embed OR markdown img embed"
      pattern: "(asciinema.org|demo\\.gif|demo\\.cast)"
    - from: "README.md::## Architecture"
      to: "docs/agent-architecture.md#planner-executor-model"
      via: "markdown link"
      pattern: "agent-architecture.md#planner-executor-model"
    - from: "README.md::## Project status"
      to: "docs/v1.4-design.md + CHANGELOG.md"
      via: "markdown links"
      pattern: "(v1.4-design\\.md|CHANGELOG\\.md)"
---

<objective>
Full rewrite of README.md per D-01..D-04. Lead with the agent-first thesis;
RAG appears under "Tools the agent calls." All v1.3 technical content
preserved under `## Platform features` (reorganized, reframed in agent-first terms).

Purpose: lock SC1 ("README 'What This Is' / 'Architecture' sections lead with
agent-first framing; agentic RAG appears under 'Tools the agent calls.'") and
contribute to SC4 (asciinema embedded in README, renders correctly on GitHub).

Output: Replaced README.md (~250-300 lines target — comparable to current 312).
Single atomic edit. Existing badges (if any) preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md
@.planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md
@.planning/phases/19-agent-first-docs-demo-release/19-04-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-05-SUMMARY.md
@.planning/phases/19-agent-first-docs-demo-release/19-07-SUMMARY.md
@README.md
@docs/agent-architecture.md
@docs/v1.4-design.md
@CHANGELOG.md

<interfaces>
<!-- Existing v1.3 README content blocks to PRESERVE under `## Platform features`. -->

From README.md lines 7-16 (Features bullets — preserve verbatim, regroup under `## Platform features`):
- Multi-tenant isolation (RLS)
- Hybrid retrieval (pgvector HNSW + BM25 + RRF + HyDE)
- 6-stage ingest pipeline
- 10-stage query pipeline
- Image extraction
- Async ingest
- Agentic RAG (Anthropic Tool Use loop)
- Security (JWT, rate-limit, PII)
- Observability (Prometheus, structured logging, audit, optional Langfuse)
- Streaming (SSE)

From README.md lines 36-65 (Architecture directory tree — preserve under `## Platform features` "Module layout" sub-bullet).

From README.md lines 67-165 (Quick Start — Docker + Local Dev — preserve under `## Quick start`).

From README.md lines 215-230 (Configuration table — preserve verbatim under `## Quick start` -> Configuration sub-section).

From README.md lines 232-246 (Testing block — preserve under `## Platform features` -> Testing sub-section).

From README.md lines 246-281 (Coverage block TEST-04 + TEST-06 — preserve under `## Platform features` -> Coverage sub-section).

From README.md line 312 (License + PyMuPDF AGPL note — preserve at bottom).

<!-- Plan 19-04 cross-reference target: docs/agent-architecture.md::## Planner / Executor Model -->

GitHub auto-anchor: `#planner-executor-model` (lowercase, hyphens replace spaces, slashes dropped).

<!-- Plan 19-05 cast/gif outputs available for embed. -->

Either `docs/demo.cast` (asciinema cast file) OR `docs/demo.gif` (rendered fallback) OR a public asciinema.org URL (recorded in `.planning/phases/19-agent-first-docs-demo-release/.demo-cast-url`).

<!-- Plan 19-07 outputs the README links to. -->

`CHANGELOG.md` at repo root.
`docs/v1.4-design.md` (copy of gstack milestone-design markdown).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full rewrite of README.md per D-02 section order with agent-first framing</name>
  <files>README.md</files>
  <read_first>
    - README.md (current v1.3 file — extract every technical detail; nothing in `## Features`/`## Architecture`/`## Quick Start`/`## Configuration`/`## Coverage`/`## Testing` may be lost; the FRAMING is replaced, the CONTENT is preserved + reorganized)
    - docs/agent-architecture.md (verify the `#planner-executor-model` anchor target exists from plan 19-04)
    - docs/v1.4-design.md (verify it exists — plan 19-07 ships this)
    - CHANGELOG.md (verify it exists — plan 19-07 ships this)
    - docs/demo.cast (verify it exists — plan 19-05 ships this)
    - .planning/phases/19-agent-first-docs-demo-release/.demo-cast-url (read this if it exists — contains asciinema.org URL for HTML embed; if absent, fall back to img embed of docs/demo.gif)
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-01..D-04 (thesis, section order, audience, preservation rules)
    - .planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md §1 (file 1 — README adaptation notes, preserve/invert mapping)
  </read_first>
  <action>
    REPLACE the entire content of `README.md`. Section structure (D-02 verbatim):

    **Section 1 — Title + thesis (replaces existing lines 1-3):**

    ```markdown
    # EnterpriseRAG

    A Planner → Executor → Synthesizer agent. RAG is one of its tools.

    Multi-tenant document understanding, parallel tool dispatch, structured event
    stream — built on FastAPI + PostgreSQL/pgvector. Provider-neutral across
    Anthropic, OpenAI, Azure, and Ollama.
    ```

    The 2-line elaboration after the thesis stays compact (≤ 4 lines) and avoids marketing language (D-03).

    **Section 2 — `## Quick demo`:**

    Read `.planning/phases/19-agent-first-docs-demo-release/.demo-cast-url` if it exists. If it does, use the HTML embed (D-07 preferred form):

    ```markdown
    ## Quick demo

    <a href="https://asciinema.org/a/<id-from-.demo-cast-url-file>" target="_blank">
      <img src="https://asciinema.org/a/<id>.svg" alt="Phase 19 demo: 4-way parallel tool fan-out via SSE event stream" width="720">
    </a>

    `make demo-agent` runs a 4-tool parallel `RetrieveTool` plan against fixture data
    (no API keys required). Each `tool.span.start` event fires near-simultaneously;
    each `tool.span.end` event fires ~500 ms later, bounded by `max(tool_latency)`,
    not the sum. The SSE event stream IS the architectural surface — what you see in
    the cast is what `POST /api/v1/agent/v1/run/stream` emits over the wire.
    ```

    If `.demo-cast-url` does NOT exist (no asciinema upload), use the static gif fallback:

    ```markdown
    ## Quick demo

    ![Phase 19 demo: 4-way parallel tool fan-out via SSE event stream](docs/demo.gif)

    `make demo-agent` runs a 4-tool parallel `RetrieveTool` plan against fixture data
    (no API keys required). Each `tool.span.start` event fires near-simultaneously;
    each `tool.span.end` event fires ~500 ms later, bounded by `max(tool_latency)`,
    not the sum. The SSE event stream IS the architectural surface — what you see in
    the gif is what `POST /api/v1/agent/v1/run/stream` emits over the wire.
    ```

    If neither `.demo-cast-url` nor `docs/demo.gif` exists, embed `docs/demo.cast` as a link with one paragraph explaining how to view it locally:

    ```markdown
    ## Quick demo

    `make demo-agent` runs a 4-tool parallel `RetrieveTool` plan against fixture data.
    Replay the recorded session: [`docs/demo.cast`](docs/demo.cast) (asciinema cast v2).

    Each `tool.span.start` event fires near-simultaneously; each `tool.span.end` event
    fires ~500 ms later, bounded by `max(tool_latency)`, not the sum.
    ```

    Pick exactly ONE of the three forms based on what plan 19-05 produced.

    **Section 3 — `## Architecture`:**

    ```markdown
    ## Architecture

    ```
    Request ──▶ Planner ──ToolPlan──▶ Executor ──results──▶ Synthesizer ──▶ Response
                                         │
                                  parallel dispatch
                                  (asyncio.as_completed,
                                   BaseException isolation)
    ```

    Three explicit collaborators behind a Pydantic V2 frozen contract. The
    Planner is stateless and provider-neutral (`BaseLLMClient.call_agentic_turn`).
    The Executor walks `ToolPlan.parallel_groups` via `asyncio.as_completed`. The
    Synthesizer is the LLM's terminal turn after results return.

    Full mental model + signatures + runnable example: [Planner / Executor Model](docs/agent-architecture.md#planner-executor-model).
    ```

    **Section 4 — `## Tools the agent calls`:**

    ```markdown
    ## Tools the agent calls

    Tools register via a static class registry (`services/agent/tools/registry.py`)
    and are dispatched provider-neutrally. The planner sees only tools listed in
    `AGENT_TOOL_ALLOWLIST` in `services/pipeline.py`.

    | Tool | Status | Implementation |
    |------|--------|----------------|
    | `RetrieveTool` | shipped (v1.4) | Hybrid pgvector + BM25 + RRF + reranker (Phase 17). Wraps `QueryPipeline.run()` — v1.3 retrieval behavior preserved. |
    | `RefinedRetrieveTool` | shipped (v1.4) | LLM-driven query refinement before retrieval (Phase 17). |
    | `WebSearchTool` | placeholder | Registered but excluded from `AGENT_TOOL_ALLOWLIST`. Real implementation deferred to v1.5+. |
    | `SQLTool` | planned | Tool-authoring example shipped in [docs/agent-architecture.md#authoring-tools](docs/agent-architecture.md#authoring-tools). |
    | `MCPTool` | planned | MCP plug-in discovery as a registry replacement — interface clean enough that callsites do not change. |

    Add a tool: see [Authoring Tools](docs/agent-architecture.md#authoring-tools).
    ```

    **Section 5 — `## Platform features`:**

    Reorganize the v1.3 technical content (D-04 — no information lost; framing inverted). Suggested sub-structure:

    ```markdown
    ## Platform features

    Each item below is a tool the agent calls OR a supporting service the agent depends on.

    ### Multi-tenant isolation
    PostgreSQL Row-Level Security; each tenant's data is strictly separated at the database level. Every `RetrieveTool` dispatch carries a `tenant_id` that the executor scopes via Postgres RLS — no cross-tenant leakage by construction.

    ### Hybrid retrieval (RetrieveTool internals)
    Dense vector search (pgvector HNSW, `ef_construction=200 m=16`) + BM25 sparse, fused with RRF; HyDE and multi-query expansion. The reranker runs cross-encoder over the top-K fused candidates.

    ### Document ingestion
    Six-stage pipeline: preprocess → extract → PII detect → chunk → vectorize → audit. Async variant returns a `task_id` immediately (`POST /ingest/async`); ARQ/Redis worker processes in background; poll status via `GET /ingest/status/{task_id}`.

    ### Image extraction
    PDF-embedded images extracted via PyMuPDF, captioned by LLM, stored as retrievable `chunk_type="image"` vector chunks. Note: PyMuPDF is licensed under AGPL-3.0 — commercial on-premise deployments require a separate license.

    ### Provider neutrality
    `BaseLLMClient.call_agentic_turn` (Phase 11) abstracts Anthropic Tool Use, OpenAI function-calling, and Ollama into one shape. The Planner consumes only this interface — no provider branches in pipeline code. `parallel_tool_calls=True` enabled on OpenAI; `disable_parallel_tool_use=False` on Anthropic.

    ### Security
    JWT startup validation, per-route rate limiting, PII blocking by default, CORS locked to explicit origins. v1.0 hardening + v1.1 multi-tenant RLS form the defense-in-depth baseline.

    ### Module layout
    ```
    controllers/api.py          HTTP layer (FastAPI routes, auth, rate-limit, SSE)
        │
    services/pipeline.py        IngestionPipeline / QueryPipeline / AgentQueryPipeline
        │
    services/
        agent/                  Planner / Executor / Synthesizer + tools/ registry (v1.4 core)
        preprocessor/           Stage 1 — clean, deduplicate, language detect
        extractor/              Stage 2 — PDF text + image extraction (PyMuPDF)
        doc_processor/          Stage 3/4 — PII detection, chunking strategies
        vectorizer/             Stage 5 — BGE-M3 embedding, pgvector upsert
        retriever/              Dense + BM25 + RRF fusion + reranker
        generator/              LLM client (Ollama / OpenAI / Anthropic / Azure)
        nlu/                    Filter extraction (intent classification by Planner — Phase 16)
        memory/                 Redis short-term + PostgreSQL long-term
        auth/                   OIDC/JWT validation
        audit/                  Buffered audit log
        rules/                  Business rules engine (ABC enforcement)
        knowledge/              Version control + quality validation
        ab_test/                A/B experiment service
        events/                 In-process or Kafka event bus

    utils/                      Shared: models, logger, cache, metrics, tasks
    config/settings.py          Pydantic V2 BaseSettings — all config via env vars
    ```

    **Stack:** PostgreSQL + pgvector (HNSW), ARQ + Redis, OIDC/JWT, FastAPI.

    ### Testing & coverage

    | Gate | Threshold | Scope |
    |------|-----------|-------|
    | Combined coverage | ≥ 70% (TEST-06, v1.3 Phase 15) | unit + integration |
    | Diff-coverage | ≥ 80% on changed lines (TEST-03) | per-PR |
    | RAGAS faithfulness | > 0.85 | 200 stratified QA pairs, judge model |
    | RAGAS answer relevancy | > 0.80 | same suite |

    Run locally: `make test`, `make coverage-combined`, `make eval`. CI enforces all four on PRs against `master`.
    ```

    **Section 6 — `## Observability`:**

    ```markdown
    ## Observability

    The agent runtime emits a structured SSE event stream on `POST /api/v1/agent/v1/run/stream` (Phase 18, AGENT-04). Six event types: `planner.plan`, `tool.span.start`, `tool.span.end`, `tool.span.error`, `executor.parallel`, `synthesizer.final`. Each carries a `trace_id`, monotonic `seq`, and `ts_ms`. Wire format + payload tables: [Event Schema Reference](docs/agent-architecture.md#event-schema-reference).

    Other observability: Prometheus metrics on `/metrics`; structured logging via `structlog`; optional Langfuse tracing; buffered audit log with flush.
    ```

    **Section 7 — `## Quick start`:**

    Preserve the existing Quick Start blocks from current README lines 67-198 verbatim, with these changes:
    - Section heading: `## Quick start` (not `## Quick Start — Docker`).
    - Add the `## Quick start` `### Try the demo first` sub-section at the very top of the section:
      ```markdown
      ### Try the demo first
      ```bash
      git clone <repo-url> && cd rag_enterprise
      make demo-agent
      ```
      Stub-LLM, fixture-only, no API keys needed. Exits 0 in ~1.5s and prints the SSE event stream to stdout.
      ```
    - Then `### Docker stack` (existing Docker Quick Start content).
    - Then `### Local dev` (existing Local Development content).
    - Add a `### cURL example` sub-section at the end of `## Quick start`:
      ```markdown
      ### cURL example

      Stream events from a real agent invocation:

      ```bash
      curl --no-buffer -X POST http://localhost:8000/api/v1/agent/v1/run/stream \
          -H "Authorization: Bearer <JWT>" \
          -H "Content-Type: application/json" \
          -d '{
              "query": "What is the annual leave policy?",
              "session_id": "user-123",
              "tenant_id": "demo-tenant",
              "user_id": "demo-user",
              "top_k": 5
          }'
      ```

      `<JWT>` is a token issued by your identity provider; see `services/auth/` for OIDC integration. Replace `demo-tenant` with the tenant ID your JWT scopes you to.
      ```
    - Preserve the `## Configuration` table (current README lines 215-230) as `### Configuration` under `## Quick start`.

    **Section 8 — `## Project status`:**

    ```markdown
    ## Project status

    **Current release:** v1.4 — Agent-First Architecture Inversion (Phases 16–19).

    - **Design doc:** [docs/v1.4-design.md](docs/v1.4-design.md) — the architectural-inversion thesis (Approach A: incremental refactor, no framework lock-in).
    - **Phase summaries:** [Planner + Executor Extraction](.planning/phases/16-planner-executor-extraction/16-03-SUMMARY.md) · [Tool Abstraction + RetrieveTool](.planning/phases/17-tool-abstraction-retrievetool/17-03-SUMMARY.md) · [SSE Event Stream](.planning/phases/18-sse-planner-trace-event-stream/18-03-SUMMARY.md) · Agent-First Docs + Demo + Release (this milestone).
    - **Changelog:** [CHANGELOG.md](CHANGELOG.md) (keep-a-changelog 1.1.0 format; v1.0 → v1.4 reverse-chronological).
    - **Roadmap:** [.planning/ROADMAP.md](.planning/ROADMAP.md).

    Prior milestones (archived): v1.0 Hardening · v1.1 Retrieval Depth & Frontend · v1.2 Agentic Layer + Swarm · v1.3 Fork Swarm, NLU & Quality. Per-milestone roadmaps live under `.planning/milestones/`.

    ## License

    See [SECURITY.md](SECURITY.md) for security policy. PyMuPDF (image extraction) is licensed under AGPL-3.0 — commercial on-premise deployments require a separate license.
    ```

    Preserve any existing badges (CI, license — none currently present in v1.3 README, but check via `grep -E '^\\[!\\[' README.md`).

    **Sanity rule** — after writing the new README, verify `grep -c "Multi-tenant\\|hybrid retrieval\\|6-stage\\|10-stage\\|Image extraction\\|Async ingest\\|JWT\\|RAGAS\\|coverage" README.md` returns ≥ 8 (all preserved technical content surfaces). If < 8, content was lost — restore from current README before committing.

    **Length target** — ≤ 320 lines (current is 312). Anchor: agent-first framing should ADD content (sections 1-4), not REPLACE the technical-content volume. Sections 5+7 carry the v1.3 technical preservation.
  </action>
  <verify>
    <automated>grep -c "A Planner → Executor → Synthesizer agent. RAG is one of its tools." README.md && grep -nE "^## (Quick demo|Architecture|Tools the agent calls|Platform features|Observability|Quick start|Project status)" README.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Thesis present: `grep -c "A Planner → Executor → Synthesizer agent. RAG is one of its tools." README.md` returns 1.
    - Section order verified: `grep -nE "^## " README.md` returns lines in this order — `## Quick demo` < `## Architecture` < `## Tools the agent calls` < `## Platform features` < `## Observability` < `## Quick start` < `## Project status` < `## License` (8 sections, monotonically increasing line numbers).
    - Quick demo embed: at least ONE of three forms present — `grep -cE "asciinema\\.org/a/[a-zA-Z0-9]+|docs/demo\\.gif|docs/demo\\.cast" README.md` returns ≥ 1.
    - Architecture diagram present: `grep -c "Request ──▶ Planner ──ToolPlan──▶ Executor" README.md` returns 1.
    - Tools table present with 5 rows: `grep -c "^| \\(RetrieveTool\\|RefinedRetrieveTool\\|WebSearchTool\\|SQLTool\\|MCPTool\\)" README.md` returns 5.
    - Cross-link to plan 19-04 docs section: `grep -c "agent-architecture.md#planner-executor-model" README.md` returns ≥ 1.
    - Cross-link to plan 19-04 event reference: `grep -c "agent-architecture.md#event-schema-reference" README.md` returns ≥ 1.
    - Cross-link to plan 19-04 authoring section: `grep -c "agent-architecture.md#authoring-tools" README.md` returns ≥ 2 (Tools section + Platform features OR docs cross-ref).
    - Cross-link to plan 19-07 design doc: `grep -c "docs/v1.4-design.md" README.md` returns ≥ 1.
    - Cross-link to plan 19-07 changelog: `grep -c "CHANGELOG.md" README.md` returns ≥ 1.
    - cURL example correct route: `grep -c "POST http://localhost:8000/api/v1/agent/v1/run/stream" README.md` returns 1.
    - cURL `--no-buffer` flag: `grep -c "curl --no-buffer" README.md` returns 1.
    - JWT placeholder (NOT real token): `grep -c "Bearer <JWT>" README.md` returns 1; `grep -cE "Bearer (sk-|ey[A-Za-z0-9])" README.md` returns 0.
    - Tenant placeholders: `grep -c "demo-tenant" README.md` returns ≥ 1; `grep -cE "tenant_id.*acme|tenant_id.*production" README.md` returns 0.
    - Make demo-agent referenced (SC4 + SC3 cross-anchor): `grep -c "make demo-agent" README.md` returns ≥ 2 (Quick demo intro + Try the demo first).
    - All v1.3 technical content preserved (ROADMAP D-04): `grep -cE "Multi-tenant|hybrid retrieval|6-stage|10-stage|Image extraction|Async ingest|JWT|RAGAS|coverage|RLS|HNSW|BM25|RRF|HyDE|reranker|PyMuPDF|Prometheus|Langfuse|audit" README.md` returns ≥ 14 (current README has these; all must persist).
    - Configuration table preserved: `grep -c "SECRET_KEY\\|APP_MODEL_DIR\\|LLM_PROVIDER\\|VECTOR_STORE\\|PG_DSN" README.md` returns ≥ 5.
    - Length: `wc -l README.md` returns 200 ≤ N ≤ 350 (preservation target).
    - License section preserved at bottom: `grep -n "PyMuPDF.*AGPL" README.md | tail -1 | awk -F: '{print $1}'` returns the last 10 lines of the file (last section).
    - mkdocs/markdown lint smoke (no broken links): `grep -oE "\\]\\([^)]+\\)" README.md | grep -E "(\\.md|\\.cast|\\.gif|\\.svg)" | wc -l` returns ≥ 5 (multiple internal references).
    - Commit message: `docs(19-06-T1): full README rewrite — agent-first framing (Phase 19, AGENT-08, SC1+SC4)`.
  </acceptance_criteria>
  <done>README.md replaced with agent-first framing per D-02. All v1.3 technical content preserved. Cross-links resolve. File committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → public reader | README is the project's first impression; any embedded credentials, real tenant IDs, or sensitive paths leak publicly upon push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06-01 | Information Disclosure | JWT samples with embedded real signatures | mitigate | cURL example uses `<JWT>` placeholder; grep gate `grep -cE "Bearer (sk-|ey[A-Za-z0-9])" README.md` returns 0 in acceptance_criteria. |
| T-19-06-02 | Information Disclosure | Real tenant IDs in cURL or code samples | mitigate | All cURL/snippet examples use `tenant_id="demo-tenant"`, `user_id="demo-user"`. Grep gate `grep -cE "tenant_id.*acme|tenant_id.*production" README.md` returns 0. |
| T-19-06-03 | Information Disclosure | API keys / secrets in env-var examples | mitigate | Quick start preserves the v1.3 pattern of `OPENAI_API_KEY=sk-...` placeholders (NOT real values). The current v1.3 README uses `sk-...` ellipsis form — preserve this. |
| T-19-06-04 | Tampering | Cross-links pointing to non-existent files | mitigate | All cross-link targets verified by acceptance_criteria grep gates: `docs/agent-architecture.md`, `docs/v1.4-design.md`, `CHANGELOG.md`, `docs/demo.cast`/`docs/demo.gif`, `.planning/phases/16-...`, `17-...`, `18-...` SUMMARY paths. Plan 19-06 depends_on 19-04 + 19-05 + 19-07 ensures these exist before this plan runs. |
</threat_model>

<verification>
- README.md replaced; opens with agent-first thesis verbatim per D-01.
- 8 sections in D-02 order; verified by ordered `grep -nE "^## "`.
- All v1.3 technical content preserved (RLS, HNSW, BM25, RRF, HyDE, reranker, PyMuPDF AGPL note, RAGAS gates, coverage tables, configuration table — verified by grep gate ≥ 14 keywords).
- Cross-links resolve to plan 19-04 (docs section anchors), plan 19-05 (cast/gif), plan 19-07 (CHANGELOG, design doc).
- No real credentials / tenant IDs leaked.
</verification>

<success_criteria>
- README.md leads with agent-first thesis; RAG appears under "Tools the agent calls" (SC1).
- Asciinema cast embedded near the top of README (SC4).
- All cross-links to plans 19-04, 19-05, 19-07 outputs resolve.
- v1.3 technical content preserved verbatim under `## Platform features` and `## Quick start`.
- 1 atomic commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19-agent-first-docs-demo-release/19-06-SUMMARY.md`
</output>
