---
phase: 19-agent-first-docs-demo-release
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/agent/_demo_stubs.py
  - tests/unit/test_demo_stubs.py
autonomous: true
requirements: [AGENT-08]
must_haves:
  truths:
    - "DemoStubPlanner returns a 4-step parallel ToolPlan + a terminal plan from plan_from_messages"
    - "make_fake_retrieve_tool produces a BaseTool subclass that returns a deterministic ToolResult after sleep_s seconds"
    - "build_demo_registry registers all passed BaseTool subclasses and returns a fresh ToolRegistry"
    - "Module is importable without pytest (runtime artifact, not test fixture)"
  artifacts:
    - path: "services/agent/_demo_stubs.py"
      provides: "DemoStubPlanner + make_fake_retrieve_tool + build_demo_registry + DEMO_QUERY constant + DEMO_PLANS factory"
      contains: "class DemoStubPlanner"
    - path: "tests/unit/test_demo_stubs.py"
      provides: "RED→GREEN tests for the three demo-stub primitives"
      contains: "def test_demo_stub_planner_returns_four_step_plan"
  key_links:
    - from: "services/agent/_demo_stubs.py"
      to: "services.agent.tools.base.BaseTool"
      via: "make_fake_retrieve_tool returns BaseTool subclass"
      pattern: "class.*\\(BaseTool\\)"
    - from: "services/agent/_demo_stubs.py"
      to: "services.agent.tools.registry.ToolRegistry"
      via: "build_demo_registry returns ToolRegistry instance"
      pattern: "ToolRegistry\\(\\)"
    - from: "services/agent/_demo_stubs.py"
      to: "utils.models.ToolPlan / ToolCall / ToolResult / ToolContext"
      via: "DemoStubPlanner returns ToolPlan; fake tool returns ToolResult"
      pattern: "ToolPlan\\(|ToolResult\\("
---

<objective>
Promote the Phase 18 SSE test fixtures (`_StubPlanner`, `_make_fake_tool`, `_stub_registry`) from
`tests/unit/test_agent_sse.py` lines 69-107 into a reusable runtime module
`services/agent/_demo_stubs.py` consumed by both the Phase 19 demo runner and the
Phase 19 demo integration test.

Purpose: single source of truth for the 4-tool 0.5s parallel-fan-out fixture (D-05).
Output: `services/agent/_demo_stubs.py` (~50-80 lines) + RED-then-GREEN unit tests.

This is a TDD plan. Test file written first (RED — failing import / failing assertions),
then implementation written to make tests pass (GREEN). REFACTOR optional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md
@.planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md
@.planning/phases/18-sse-planner-trace-event-stream/18-03-SUMMARY.md
@tests/unit/test_agent_sse.py
@services/agent/tools/base.py
@services/agent/tools/registry.py

<interfaces>
<!-- Key types the demo stubs implement against. Extracted from existing code. -->

From utils/models.py (ToolPlan / ToolCall / ToolResult / ToolContext):
```python
class ToolCall(BaseModel):
    model_config = ConfigDict(frozen=True)
    id:        str
    name:      str
    arguments: dict[str, Any] = Field(default_factory=dict)

class ToolPlan(BaseModel):
    model_config = ConfigDict(frozen=True)
    steps:             list[ToolCall]  = Field(default_factory=list)
    parallel_groups:   list[list[int]] = Field(default_factory=list)
    rationale:         str             = ""
    raw_assistant_msg: dict[str, Any]  = Field(default_factory=dict)
    stop_reason:       str             = "text_only"

class ToolResult(BaseModel):
    model_config = ConfigDict(frozen=True)
    content:  str
    chunks:   list[Any] = Field(default_factory=list)
    metadata: dict[str, Any] = Field(default_factory=dict)
```

From services/agent/tools/base.py (BaseTool ABC):
```python
class BaseTool(ABC):
    name:              ClassVar[str]
    description:       ClassVar[str]
    parameters_schema: ClassVar[dict[str, Any]]
    @abstractmethod
    async def run(self, args: dict[str, Any], ctx: ToolContext) -> ToolResult: ...
```

From services/agent/tools/registry.py (ToolRegistry):
```python
class ToolRegistry:
    def register(self, cls: type[BaseTool]) -> type[BaseTool]: ...
    def get(self, name: str) -> BaseTool: ...
    def list(self) -> list[str]: ...
    def schemas_for(self, provider: str, names: list[str] | None = None) -> list[dict[str, Any]]: ...
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing tests for the three demo-stub primitives</name>
  <files>tests/unit/test_demo_stubs.py</files>
  <read_first>
    - tests/unit/test_agent_sse.py lines 39-107 (the existing _StubPlanner / _make_fake_tool / _stub_registry fixtures — the demo stubs are these primitives promoted to a runtime module)
    - services/agent/tools/base.py (BaseTool ABC + ToolContext shape)
    - utils/models.py lines 244-315 (ToolCall, ToolPlan)
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-05, D-06 (demo query string + 4-tool fan-out shape)
    - .planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md §7 (file 7 — adaptation notes for _demo_stubs.py)
  </read_first>
  <behavior>
    - Test 1: `test_demo_stub_planner_returns_four_step_plan` — instantiate `DemoStubPlanner()`, call `await planner.plan_from_messages([], None, None)`, assert returned `ToolPlan.steps` length == 4, all step names == "search_knowledge_base", `parallel_groups == [[0, 1, 2, 3]]`, `stop_reason == "tool_use"`, `rationale` non-empty.
    - Test 2: `test_demo_stub_planner_second_call_returns_terminal_plan` — call `plan_from_messages` a second time on same instance; assert returned plan has empty `steps`, empty `parallel_groups`, `stop_reason == "text_only"`, `rationale` non-empty (the composed answer).
    - Test 3: `test_make_fake_retrieve_tool_returns_basetool_subclass` — call `make_fake_retrieve_tool(name="search_knowledge_base", sleep_s=0.0, content="ctx")`, assert return value is a class, `issubclass(cls, BaseTool)`, `cls.name == "search_knowledge_base"`, `cls.parameters_schema` is a dict with `"type": "object"`.
    - Test 4: `test_make_fake_retrieve_tool_run_returns_toolresult_with_metadata` — instantiate the class, call `await instance.run({"q": "x"}, ToolContext(...))`, assert returned `ToolResult.content == "ctx"`, `metadata["latency_ms"] == 0` for sleep_s=0, `metadata["chunk_count"] == 3`.
    - Test 5: `test_make_fake_retrieve_tool_respects_sleep_s` — instantiate with sleep_s=0.05, time the run; assert wall-clock > 0.04s and < 0.2s; assert `metadata["latency_ms"] == 50`.
    - Test 6: `test_build_demo_registry_registers_all_tools` — create a fake tool class, call `build_demo_registry(FakeCls)`, assert returned object is a `ToolRegistry`, `reg.get("search_knowledge_base")` returns the registered instance, `"search_knowledge_base" in reg.list()`.
    - Test 7: `test_demo_query_constant_matches_context_d05` — assert `_demo_stubs.DEMO_QUERY == "Across our compliance, finance, engineering, and HR knowledge bases, where do we mention 'data retention'?"` (verbatim per CONTEXT.md `<specifics>`).
  </behavior>
  <action>
    Create `tests/unit/test_demo_stubs.py`. Mirror the import + fixture style of `tests/unit/test_agent_sse.py` lines 1-37. Use `from services.agent._demo_stubs import DemoStubPlanner, make_fake_retrieve_tool, build_demo_registry, DEMO_QUERY`. ALL imports MUST fail at this stage (the module does not exist yet) — that is the RED state.

    Test bodies use `pytest.mark.asyncio` per existing convention. Construct `ToolContext` for Test 4/5 with `req=GenerationRequest(query="x", session_id="s", top_k=5, tenant_id="t", user_id="u")`, `tf={}`, `retriever=object()`, `llm=type("LLM", (), {"provider_name": "openai"})()`. Allow `arbitrary_types_allowed=True` is already on ToolContext per Phase 17 D-03.

    For Test 5 use `time.perf_counter()` deltas, NOT real-time SLA — bound is 0.04 < t < 0.2 to allow CI jitter.

    Run `conda run -n torch_env pytest tests/unit/test_demo_stubs.py -v` and confirm ALL 7 tests fail with `ModuleNotFoundError: No module named 'services.agent._demo_stubs'` (RED gate).
  </action>
  <verify>
    <automated>conda run -n torch_env pytest tests/unit/test_demo_stubs.py -v 2>&1 | grep -E "ModuleNotFoundError|FAILED|ERRORS" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_demo_stubs.py` exists.
    - `pytest tests/unit/test_demo_stubs.py` exits NON-ZERO with `ModuleNotFoundError: No module named 'services.agent._demo_stubs'` for ALL 7 tests (collection-stage error or per-test ImportError).
    - File contains exactly 7 `def test_` functions matching the names in `<behavior>`.
    - File imports `pytest`, `pytest.mark.asyncio` decorators present on all 7 tests.
    - File imports from `services.agent._demo_stubs` (the to-be-created module) AND from `services.agent.tools.base.BaseTool` (existing) AND from `utils.models.ToolPlan, ToolCall, ToolContext, ToolResult, GenerationRequest` (existing).
    - Commit message: `test(19-01-T1): RED — failing tests for services.agent._demo_stubs primitives`.
  </acceptance_criteria>
  <done>RED state confirmed: all 7 tests fail with ModuleNotFoundError on services.agent._demo_stubs. Test file committed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement services/agent/_demo_stubs.py to make tests pass</name>
  <files>services/agent/_demo_stubs.py</files>
  <read_first>
    - tests/unit/test_demo_stubs.py (the RED tests from Task 1 — implement to make them pass)
    - tests/unit/test_agent_sse.py lines 69-107 (verbatim source patterns: _StubPlanner, _make_fake_tool, _stub_registry)
    - services/agent/tools/base.py (BaseTool ABC; ClassVar requirements)
    - services/agent/tools/registry.py (ToolRegistry constructor + register signature)
    - utils/models.py lines 244-315 (ToolCall, ToolPlan; ConfigDict frozen)
    - .planning/phases/19-agent-first-docs-demo-release/19-CONTEXT.md D-05, D-06
    - .planning/phases/19-agent-first-docs-demo-release/19-PATTERNS.md §7
  </read_first>
  <action>
    Create `services/agent/_demo_stubs.py` (~80 lines, target ≤ 100). Module docstring begins with: `"""Runtime stubs for `make demo-agent` (Phase 19, AGENT-08).` followed by 1 paragraph explaining "Promoted from `tests/unit/test_agent_sse.py` Phase 18 fixtures so the demo runner and the demo integration test consume a single source of truth (CONTEXT.md D-05/D-06)."

    Required exports (module-level):

    1. `DEMO_QUERY: Final[str] = "Across our compliance, finance, engineering, and HR knowledge bases, where do we mention 'data retention'?"` — verbatim per CONTEXT.md `<specifics>`.

    2. `DEMO_KB_SHARDS: Final[tuple[str, ...]] = ("compliance", "finance", "engineering", "hr")` — 4 shard names matching the 4 tools.

    3. `class DemoStubPlanner:` — constructor takes no args; sets `self._call_count: int = 0`. Method `async def plan_from_messages(self, messages: list[dict[str, Any]] | None = None, tools: list[dict[str, Any]] | None = None, system: str | None = None) -> ToolPlan:`. First call returns the 4-tool parallel plan: 4 `ToolCall(id=f"c{i}", name="search_knowledge_base", arguments={"query": DEMO_QUERY, "kb_shard": DEMO_KB_SHARDS[i]})`, `parallel_groups=[[0, 1, 2, 3]]`, `rationale="Fan out across 4 KBs in parallel"`, `stop_reason="tool_use"`, `raw_assistant_msg={"role": "assistant", "content": "stub"}`. Second+ call returns terminal plan: `steps=[]`, `parallel_groups=[]`, `rationale=` the canned answer string `"Found references to 'data retention' across all 4 knowledge bases — see span results above."`, `stop_reason="text_only"`, `raw_assistant_msg={"role": "assistant", "content": rationale}`. Increment `self._call_count` each call.

    4. `def make_fake_retrieve_tool(name: str = "search_knowledge_base", sleep_s: float = 0.5, content: str = "[fixture chunk]") -> type[BaseTool]:` — body verbatim from `tests/unit/test_agent_sse.py::_make_fake_tool` lines 69-89, with three changes: (a) default `sleep_s=0.5` (matches D-05 latency); (b) `metadata={"latency_ms": int(sleep_s * 1000), "chunk_count": 3}` (NOT 0 — demo wants visible chunk counts in SSE events per D-06); (c) `_Fake.__name__ = f"FakeTool_{name}"` preserved.

    5. `def build_demo_registry(*tool_classes: type[BaseTool]) -> ToolRegistry:` — body verbatim from `tests/unit/test_agent_sse.py::_stub_registry` lines 92-96. Returns a fresh `ToolRegistry` with each class registered.

    Imports required: `from __future__ import annotations`; `import asyncio`; `from typing import Any, ClassVar, Final`; `from services.agent.tools.base import BaseTool`; `from services.agent.tools.registry import ToolRegistry`; `from utils.models import ToolCall, ToolContext, ToolPlan, ToolResult`.

    Module is importable without pytest. Run `conda run -n torch_env python -c "from services.agent import _demo_stubs; print(_demo_stubs.DEMO_QUERY)"` — must print the demo query.

    Then run `conda run -n torch_env pytest tests/unit/test_demo_stubs.py -v` — all 7 tests MUST pass (GREEN gate).
  </action>
  <verify>
    <automated>conda run -n torch_env pytest tests/unit/test_demo_stubs.py -v --asyncio-mode=auto 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `services/agent/_demo_stubs.py` exists, ≤ 100 lines (`wc -l services/agent/_demo_stubs.py | awk '{print $1}'` returns ≤ 100).
    - `pytest tests/unit/test_demo_stubs.py -v --asyncio-mode=auto` exits 0 with `7 passed`.
    - `grep -c "from services.agent._demo_stubs import" services/agent/_demo_stubs.py` returns 0 (no self-import).
    - `grep -E "^class DemoStubPlanner|^def make_fake_retrieve_tool|^def build_demo_registry|^DEMO_QUERY" services/agent/_demo_stubs.py` returns exactly 4 matches.
    - `grep -c "Across our compliance, finance, engineering, and HR knowledge bases" services/agent/_demo_stubs.py` returns 1 (DEMO_QUERY verbatim per CONTEXT.md `<specifics>`).
    - `conda run -n torch_env ruff check services/agent/_demo_stubs.py tests/unit/test_demo_stubs.py` — clean (0 errors).
    - `conda run -n torch_env mypy --strict services/agent/_demo_stubs.py 2>&1 | grep -E "error:" | wc -l` returns 0 new errors versus baseline.
    - Commit message: `feat(19-01-T2): GREEN — services/agent/_demo_stubs.py demo primitives (Phase 19, AGENT-08)`.
  </acceptance_criteria>
  <done>All 7 tests in tests/unit/test_demo_stubs.py pass. Module importable. ruff + mypy clean. Files committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stub-runtime → orchestrator | DemoStubPlanner returns hardcoded ToolPlan; never calls a real LLM. No external network egress from this module. |
| stub-runtime → ToolRegistry | build_demo_registry returns a fresh ToolRegistry instance — does NOT mutate the global singleton. The demo runner (plan 19-03) patches `get_tool_registry` at consumer paths to inject this fresh instance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Information Disclosure | DemoStubPlanner.plan_from_messages | accept | Returns hardcoded fixture data; no PII; no real tenant identifiers. DEMO_QUERY uses generic department names ("compliance/finance/engineering/hr"). |
| T-19-01-02 | Tampering | build_demo_registry returning a stale registry | mitigate | Returns a fresh ToolRegistry() per call; no global state mutation. Verified via test_build_demo_registry_registers_all_tools (no shared state across calls). |
| T-19-01-03 | Elevation of Privilege | Stub Planner bypassing real auth check | mitigate | This module is invoked ONLY from `make demo-agent` (plan 19-03) which scopes patches via `unittest.mock.patch` context managers. No global monkey-patching at import. The module being importable without side effects is verified by `python -c "from services.agent import _demo_stubs"` succeeding without raising. |
</threat_model>

<verification>
- All 7 unit tests pass under pytest --asyncio-mode=auto.
- ruff check on services/agent/_demo_stubs.py + tests/unit/test_demo_stubs.py: clean.
- mypy --strict on services/agent/_demo_stubs.py: 0 new errors vs baseline.
- Module importable in isolation: `python -c "from services.agent._demo_stubs import DemoStubPlanner; print(DemoStubPlanner)"` exits 0.
</verification>

<success_criteria>
- `services/agent/_demo_stubs.py` exists (~80 lines) with 4 required public exports (DEMO_QUERY, DemoStubPlanner, make_fake_retrieve_tool, build_demo_registry).
- All 7 RED tests pass after GREEN implementation.
- DEMO_QUERY string is byte-identical to the CONTEXT.md `<specifics>` verbatim string.
- Module loads without invoking real LLM, real DB, or real registry singleton mutation.
- 2 atomic commits: RED (test file only), GREEN (impl + makes tests pass).
</success_criteria>

<output>
After completion, create `.planning/phases/19-agent-first-docs-demo-release/19-01-SUMMARY.md`
</output>
