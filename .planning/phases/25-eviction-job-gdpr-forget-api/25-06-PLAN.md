---
phase: 25-eviction-job-gdpr-forget-api
plan: 06
type: execute
wave: 3
depends_on: [25-04, 25-05]
files_modified:
  - tests/integration/test_evict_long_term_facts_e2e.py
  - tests/integration/test_memory_forget_e2e.py
autonomous: true
requirements: [EVICT-01, EVICT-02, GDPR-01, GDPR-02, GDPR-03]
tags: [integration, pgvector, real-pg, e2e, eviction, gdpr-forget, audit-log-db]
review_amendments:
  - id: T4
    source: 25-ENG-REVIEW.md (Test review / Plan-check W2)
    summary: "Integration test seed helper inserts long_term_facts rows with dummy embedding=[0.0]*1024 instead of NULL. Column is nullable today (services/memory/memory_service.py:211 — ALTER ADD COLUMN without NOT NULL) but future-proof against schema tightening; matches Phase 23 MEM-02 zero-partial-write invariant."
must_haves:
  truths:
    - "Audit-mode run on 600-row + 100-row buckets: stdout JSON-line for 600-row bucket, zero deletes confirmed via SELECT count(*) (SC-1 audit half)"
    - "Enforce-mode run drops 600-row bucket to exactly 500; 100-row bucket untouched (SC-1 enforce half)"
    - "Tie-break correctness: cap=2, 3 rows (importance=0.2@T0, 0.2@T1, 0.8@T2) — T0 row deleted; T1 + T2 survive (SC-2)"
    - "`DELETE /api/v1/memory/forget?user_id=alice` with admin JWT returns 200 + deleted_row_count; re-call returns 0 (idempotent, SC-3)"
    - "Non-admin JWT for different user returns 403 (SC-3)"
    - "MEMORY_FORGET audit_log row retrievable with correct detail fields when `audit_db_enabled=True` (SC-4)"
    - "T4: All `_seed_facts` helpers in BOTH integration files seed `long_term_facts` rows with `embedding=[0.0] * 1024` (dummy 1024-dim zero vector) rather than NULL. Survives any future migration that tightens `embedding` to NOT NULL."
  artifacts:
    - path: "tests/integration/test_evict_long_term_facts_e2e.py"
      provides: "4 integration tests: audit-mode no-delete, enforce caps 600→500, 100-row untouched, tie-break correctness"
      exports:
        - test_audit_mode_no_deletes
        - test_enforce_mode_caps_bucket
        - test_enforce_mode_small_bucket_untouched
        - test_eviction_tiebreak_correctness
    - path: "tests/integration/test_memory_forget_e2e.py"
      provides: "4 integration tests: forget API e2e, idempotent re-call, 403 for other user, audit_log DB row content"
      exports:
        - test_forget_api_e2e_admin_200
        - test_forget_api_e2e_idempotent
        - test_forget_api_e2e_non_admin_403
        - test_forget_api_audit_log_row
  key_links:
    - from: "tests/integration/test_evict_long_term_facts_e2e.py"
      to: "scripts/evict_long_term_facts.py::main_async (Plan 25-05)"
      via: "direct asyncio.run(main_async(...)) call against real pgvector_pool"
      pattern: "main_async"
    - from: "tests/integration/test_memory_forget_e2e.py"
      to: "controllers/memory.py::forget_user_memory (Plan 25-04)"
      via: "FastAPI TestClient DELETE /api/v1/memory/forget against real PG pool"
      pattern: "forget_user_memory"
    - from: "tests/integration/test_memory_forget_e2e.py"
      to: "services/audit/audit_service.py (audit_log table)"
      via: "`monkeypatch.setattr(audit_mod.settings, 'audit_db_enabled', True)` + `await audit_service.flush()`"
      pattern: "audit_db_enabled"
---

<objective>
Write integration tests validating all 5 ROADMAP success criteria against a live PostgreSQL + pgvector instance (Wave 3 — depends on Plans 25-04 and 25-05 delivering the controller and CLI).

Two test files:
1. `tests/integration/test_evict_long_term_facts_e2e.py` — seeds `long_term_facts` rows; calls `main_async()` directly; asserts bucket counts and tie-break correctness. Covers SC-1 + SC-2.
2. `tests/integration/test_memory_forget_e2e.py` — seeds facts; calls FastAPI TestClient DELETE endpoint; asserts row deletion + idempotency + 403 auth + audit_log DB row. Covers SC-3 + SC-4.

Both files use the integration marker block from 25-PATTERNS.md Analog 9 (verbatim copy from `test_recall_tool_planner_pick.py`). Both skip if `PG_AVAILABLE` is False.

Critical: `audit_db_enabled` must be patched to `True` for SC-4 audit_log DB assertion (Pitfall 3). After forget call, `await audit_service.flush()` must be awaited before the `SELECT count(*)` assertion.

Output: two new integration test files. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-CONTEXT.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-RESEARCH.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-PATTERNS.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-VALIDATION.md
@tests/conftest.py
@tests/integration/test_recall_tool_planner_pick.py
@tests/integration/test_gdpr_forget_e2e.py

<interfaces>
<!-- Extracted from working tree 2026-05-16. -->

From tests/conftest.py (key integration fixtures per Analog 9):
- `PG_AVAILABLE` bool at line 33 — used in pytestmark skipif
- `pgvector_pool` — session-scoped asyncpg pool with register_vector codec (lines 85-93)
- `clean_long_term_facts` — TRUNCATE before each test (lines 166-177)

From tests/integration/test_recall_tool_planner_pick.py (integration marker block to copy verbatim):
```python
from __future__ import annotations
import os
os.environ.setdefault("APP_MODEL_DIR", "/tmp")
os.environ.setdefault("SECRET_KEY", "a-very-secure-key-for-testing-that-is-long-32c")
import pytest
from tests.conftest import PG_AVAILABLE
pytestmark = [
    pytest.mark.integration,
    pytest.mark.pgvector,
    pytest.mark.skipif(not PG_AVAILABLE, reason="PostgreSQL + pgvector not available"),
]
_USER_ID = "test-gdpr25-u"
_TENANT_ID = "test-gdpr25-t"
```

From services/audit/audit_service.py (Pitfall 3 mitigation pattern):
```python
import services.audit.audit_service as audit_mod
monkeypatch.setattr(audit_mod.settings, "audit_db_enabled", True)
# ... after forget call ...
await get_audit_service().flush()
# ... then assert SELECT count(*) FROM audit_log WHERE action='MEMORY_FORGET' ...
```

For eviction e2e seed: INSERT into long_term_facts with:
- (user_id, tenant_id, fact, importance, created_at, embedding) values using known timestamps for tie-break test
- **T4**: include `embedding = [0.0] * 1024` (dummy 1024-dim zero vector) for every seeded row. Eviction queries `importance + created_at` only, so the vector value is irrelevant — but seeding non-NULL embeddings future-proofs the suite against any subsequent migration that tightens the `embedding` column to NOT NULL. Plan-check W2 acceptable today (column is nullable per `services/memory/memory_service.py:211`) but T4 closes the latent fragility.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eviction e2e integration tests (SC-1, SC-2)</name>
  <files>tests/integration/test_evict_long_term_facts_e2e.py</files>
  <read_first>
    - tests/conftest.py (pgvector_pool fixture shape; clean_long_term_facts; PG_AVAILABLE; INSERT helper if present)
    - tests/integration/test_recall_tool_planner_pick.py (full marker block; test shape; any seed helper patterns)
    - scripts/evict_long_term_facts.py (main_async signature: `async def main_async(mode, batch_size, user_id) -> int`)
    - .planning/phases/25-eviction-job-gdpr-forget-api/25-RESEARCH.md (§Pre-tag Manual Verification SC-1 + SC-2; Validation Architecture rows; Pitfall 1 pool reuse in test setup)
    - .planning/phases/25-eviction-job-gdpr-forget-api/25-PATTERNS.md (Analog 9 integration marker block)
  </read_first>
  <action>
    Create `tests/integration/test_evict_long_term_facts_e2e.py`.

    Copy integration marker block verbatim from Analog 9, adjusting reason string:
    `reason="PostgreSQL + pgvector not available — skipping eviction e2e tests"`

    Constants:
    ```python
    _USER_BIG = "test-evict-u-big"     # 600-row bucket
    _USER_SMALL = "test-evict-u-small" # 100-row bucket
    _TENANT = "test-evict-t"
    _CAP = 500
    ```

    Helper `_seed_facts(pool, user_id, tenant_id, count, base_importance=0.5)`:
    - INSERT `count` rows into `long_term_facts` (user_id, tenant_id, fact, importance, created_at, embedding)
    - Use `datetime.now() + timedelta(seconds=i)` for distinct created_at (tie-break ordering)
    - **T4**: seed with `embedding=[0.0] * 1024` (dummy 1024-dim zero vector). Eviction reads only importance + created_at + id, but supplying a non-NULL embedding future-proofs against schema tightening to NOT NULL.
    - SQL: `INSERT INTO long_term_facts (user_id, tenant_id, fact, importance, created_at, embedding) VALUES ($1, $2, $3, $4, $5, $6)` with `$6 = [0.0] * 1024` passed as Python list (asyncpg + register_vector codec accepts list-of-float).

    Test 1 (`test_audit_mode_no_deletes`, async):
    - Seed 600 rows for `_USER_BIG`, 100 for `_USER_SMALL`, use `clean_long_term_facts`
    - Monkeypatch `settings.memory_facts_cap_per_user = _CAP`
    - `import io, sys; old_stdout = sys.stdout; sys.stdout = io.StringIO()`
    - Call `await main_async(mode="audit", batch_size=1000, user_id=None)`
    - Restore stdout; parse JSON-lines from captured output
    - Assert: at least one JSON-line with `user_id == _USER_BIG` and `over_cap_by == 100`
    - Assert: `SELECT count(*) FROM long_term_facts WHERE user_id=$1` = 600 (no deletes)
    - Assert: `SELECT count(*) FROM long_term_facts WHERE user_id=$1 AND tenant_id=$2` = 100 for small bucket (untouched)

    Test 2 (`test_enforce_mode_caps_bucket`, async):
    - Seed same as Test 1; monkeypatch cap = 500
    - Call `await main_async(mode="enforce", batch_size=1000, user_id=None)`
    - Assert: `SELECT count(*) FROM long_term_facts WHERE user_id=_USER_BIG` = 500
    - Assert: `SELECT count(*) FROM long_term_facts WHERE user_id=_USER_SMALL` = 100 (untouched)

    Test 3 (`test_enforce_mode_small_bucket_untouched`, async) — can be combined with Test 2 assertion, or kept separate for clarity.

    Test 4 (`test_eviction_tiebreak_correctness`, async):
    - Seed 3 rows in a test bucket with `MEMORY_FACTS_CAP_PER_USER=2`:
      - Row A: importance=0.2, created_at=T0 (oldest)
      - Row B: importance=0.2, created_at=T1 (middle)
      - Row C: importance=0.8, created_at=T2 (newest, highest importance)
    - Capture the IDs of A, B, C from INSERT RETURNING or SELECT after seed
    - Monkeypatch `settings.memory_facts_cap_per_user = 2`
    - Call `await main_async(mode="enforce", batch_size=1000, user_id=None)`
    - Assert: Row A (importance=0.2, oldest) is deleted — `SELECT count(*) WHERE id = A_id` = 0
    - Assert: Row B survives — `SELECT count(*) WHERE id = B_id` = 1
    - Assert: Row C survives — `SELECT count(*) WHERE id = C_id` = 1
    - Assert: Total count for bucket = 2 (exactly at cap)

    For Tests 2-4: monkeypatch `scripts.evict_long_term_facts.settings.memory_facts_cap_per_user` (consumer-path per SP-8).

    All tests: use `pgvector_pool` fixture for the SELECT assertions (direct pool query after CLI run); use `clean_long_term_facts` autouse.

    Run gate after creation: `uv run pytest tests/integration/test_evict_long_term_facts_e2e.py -m pgvector -x -q` (skips if no PG).
    Commit: `test(25-06): eviction e2e integration tests — SC-1 + SC-2 (EVICT-01, EVICT-02)`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_evict_long_term_facts_e2e.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/test_evict_long_term_facts_e2e.py` exists.
    - `uv run pytest tests/integration/test_evict_long_term_facts_e2e.py --collect-only -q` lists 4 test items.
    - `grep -n 'pytest.mark.pgvector' tests/integration/test_evict_long_term_facts_e2e.py` matches one line.
    - `grep -n 'pytest.mark.skipif.*PG_AVAILABLE' tests/integration/test_evict_long_term_facts_e2e.py` matches one line.
    - `grep -c 'ORDER BY importance ASC, created_at ASC\|tiebreak\|tie.break' tests/integration/test_evict_long_term_facts_e2e.py` ≥ 1 (SC-2 tie-break test present).
    - `grep -c 'mode="audit"' tests/integration/test_evict_long_term_facts_e2e.py` ≥ 1 (SC-1 audit half).
    - `grep -c 'mode="enforce"' tests/integration/test_evict_long_term_facts_e2e.py` ≥ 1 (SC-1 enforce half).
    - **T4**: `grep -cE '\[0\.0\]\s*\*\s*1024' tests/integration/test_evict_long_term_facts_e2e.py` ≥ 1 (dummy embedding vector in seed helper).
    - When PG available: `uv run pytest tests/integration/test_evict_long_term_facts_e2e.py -m pgvector -x -q` exits 0 (all 4 GREEN).
  </acceptance_criteria>
  <done>4 eviction integration tests written; collect passes; SC-1 + SC-2 covered; T4 dummy embedding in seed.</done>
</task>

<task type="auto">
  <name>Task 2: Forget API e2e integration tests (SC-3, SC-4)</name>
  <files>tests/integration/test_memory_forget_e2e.py</files>
  <read_first>
    - tests/conftest.py (pgvector_pool; clean_long_term_facts; PG_AVAILABLE)
    - tests/integration/test_recall_tool_planner_pick.py (marker block, test shape)
    - tests/unit/test_oidc_auth_dependency.py (JWT mock pattern for get_auth_service — integration tests must also mock auth to avoid needing a real OIDC server)
    - services/audit/audit_service.py (audit_db_enabled field; flush() method; AuditAction.MEMORY_FORGET string value)
    - controllers/memory.py (route: DELETE /api/v1/memory/forget?user_id=... with X-Confirm-Delete header)
    - .planning/phases/25-eviction-job-gdpr-forget-api/25-RESEARCH.md (Pitfall 3 — audit_db_enabled must be True for DB assertion; §Pre-tag Manual Verification SC-3 + SC-4)
    - .planning/phases/25-eviction-job-gdpr-forget-api/25-PATTERNS.md (Analog 9 integration marker; Test harness reuse map for integration auth mock)
  </read_first>
  <action>
    Create `tests/integration/test_memory_forget_e2e.py`.

    Integration marker block (verbatim from Analog 9, adjusted reason):
    `reason="PostgreSQL + pgvector not available — skipping GDPR forget e2e tests"`

    Constants:
    ```python
    _USER_ID = "test-gdpr25-alice"
    _OTHER_USER = "test-gdpr25-bob"
    _TENANT = "test-gdpr25-t"
    _ADMIN_JWT_ROLES = ["admin"]
    _USER_JWT_ROLES = []
    ```

    Auth mock setup (module-level fixture or per-test monkeypatch):
    ```python
    fake_admin = AuthenticatedUser(user_id="admin-user", tenant_id=_TENANT, roles=["admin"])
    fake_self = AuthenticatedUser(user_id=_USER_ID, tenant_id=_TENANT, roles=[])
    fake_other = AuthenticatedUser(user_id=_OTHER_USER, tenant_id=_TENANT, roles=[])
    mock_svc = MagicMock()
    mock_svc.verify_token = AsyncMock(return_value=fake_admin)  # override per test
    monkeypatch.setattr("services.auth.oidc_auth.get_auth_service", lambda: mock_svc)
    ```

    TestClient: `from main import app; client = TestClient(app)`

    Helper `_seed_facts(pool, user_id, tenant_id, count=5)`:
    - INSERT count rows into long_term_facts for the given user/tenant
    - **T4**: include `embedding=[0.0] * 1024` (dummy vector) on every INSERT to future-proof against schema tightening to NOT NULL. Forget endpoint doesn't read embeddings either, but mirroring the eviction-test seed shape keeps the helper consistent across files.

    Test 1 (`test_forget_api_e2e_admin_200`, async):
    - Seed 5 facts for `_USER_ID` / `_TENANT`
    - Set `mock_svc.verify_token = AsyncMock(return_value=fake_admin)`
    - `client.delete("/api/v1/memory/forget", params={"user_id": _USER_ID}, headers={"Authorization": "Bearer fake", "X-Confirm-Delete": "yes"})`
    - Assert: status 200; `response.json()["deleted_row_count"] == 5`
    - Assert: `SELECT count(*) FROM long_term_facts WHERE user_id=$1 AND tenant_id=$2` = 0 (rows gone)

    Test 2 (`test_forget_api_e2e_idempotent`, async):
    - Seed 5 facts; first DELETE → 200 with count=5
    - Second DELETE (same URL + headers) → 200 with `deleted_row_count=0`

    Test 3 (`test_forget_api_e2e_non_admin_403`, sync):
    - Set `mock_svc.verify_token = AsyncMock(return_value=fake_other)` (non-admin, different user_id)
    - DELETE with `user_id=_USER_ID` (different from jwt.user_id=_OTHER_USER) → 403

    Test 4 (`test_forget_api_audit_log_row`, async):
    - Pitfall 3 mitigation: `import services.audit.audit_service as audit_mod; monkeypatch.setattr(audit_mod.settings, "audit_db_enabled", True)`
    - Seed 3 facts for `_USER_ID`
    - Call forget with admin JWT → 200
    - `await get_audit_service().flush()` — force buffer flush before assertion
    - `SELECT * FROM audit_log WHERE action='MEMORY_FORGET' AND resource_id=$1` against pgvector_pool
    - Assert: 1 row returned
    - Assert: `row["detail"]["target_user_id"] == _USER_ID`
    - Assert: `row["detail"]["deleted_row_count"] == 3`
    - Assert: `row["detail"]["actor_user_id"]` is present
    - Assert: `row["detail"]["target_tenant_id"] == _TENANT`

    Run gate: `uv run pytest tests/integration/test_memory_forget_e2e.py -m pgvector -x -q` (skips if no PG).
    Commit: `test(25-06): GDPR forget API e2e integration tests — SC-3 + SC-4 (GDPR-01, GDPR-02, GDPR-03)`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_memory_forget_e2e.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/test_memory_forget_e2e.py` exists.
    - `uv run pytest tests/integration/test_memory_forget_e2e.py --collect-only -q` lists 4 test items.
    - `grep -n 'pytest.mark.pgvector' tests/integration/test_memory_forget_e2e.py` matches one line.
    - `grep -n 'X-Confirm-Delete' tests/integration/test_memory_forget_e2e.py | wc -l` ≥ 1 (confirm-delete header in request).
    - `grep -c 'audit_db_enabled.*True\|True.*audit_db_enabled' tests/integration/test_memory_forget_e2e.py` ≥ 1 (Pitfall 3 mitigation for SC-4).
    - `grep -c 'flush()' tests/integration/test_memory_forget_e2e.py` ≥ 1 (flush before audit assert).
    - `grep -c '"MEMORY_FORGET"\|action.*MEMORY_FORGET' tests/integration/test_memory_forget_e2e.py` ≥ 1 (SC-4 audit action check).
    - `grep -c '403' tests/integration/test_memory_forget_e2e.py` ≥ 1 (SC-3 non-admin test).
    - `grep -c 'deleted_row_count.*0\|0.*deleted_row_count' tests/integration/test_memory_forget_e2e.py` ≥ 1 (idempotent re-call test).
    - **T4**: `grep -cE '\[0\.0\]\s*\*\s*1024' tests/integration/test_memory_forget_e2e.py` ≥ 1 (dummy embedding vector in seed helper).
    - When PG available: `uv run pytest tests/integration/test_memory_forget_e2e.py -m pgvector -x -q` exits 0 (all 4 GREEN).
  </acceptance_criteria>
  <done>4 forget API integration tests written; collect passes; SC-3 + SC-4 covered including Pitfall 3 mitigation for audit_db_enabled; T4 dummy embedding in seed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test ↔ real PostgreSQL | Tests use conftest.py pgvector_pool — session-scoped; clean_long_term_facts prevents cross-test contamination |
| Integration test ↔ audit_log table | Tests patch audit_db_enabled=True; INSERT-only table; tests never UPDATE/DELETE audit rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-T1 | Tampering | Integration test seeds data that persists across tests | mitigate | `clean_long_term_facts` autouse fixture TRUNCATEs before each test; unique `_USER_ID` constants per test file avoid cross-file contamination |
| T-25-06-I1 | Information Disclosure | Integration tests log real user_id/tenant_id values | accept | Test constants are synthetic (not real user data); test DB is isolated |
| T-25-06-D1 | Denial | Integration test suite takes too long (600-row seed) | accept | 600 rows INSERT is fast; pgvector_pool is session-scoped (reused); acceptable for CI |
| T-25-06-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages; test-only files |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_evict_long_term_facts_e2e.py tests/integration/test_memory_forget_e2e.py --collect-only -q` lists 8 items total.
- When PG available: `uv run pytest tests/integration/ -m pgvector -x -q --tb=short` passes (all integration tests including Phase 24).
- SC-1, SC-2, SC-3, SC-4 all covered by assertions in these two files.

Run gate (unit baseline, always): `uv run pytest tests/unit/test_evict_long_term_facts.py tests/unit/test_memory_forget.py tests/unit/test_memory_controller.py -x -q`
Run gate (integration, PG required): `uv run pytest tests/integration/test_evict_long_term_facts_e2e.py tests/integration/test_memory_forget_e2e.py -m pgvector -x -q`
</verification>

<success_criteria>
- SC-1 (ROADMAP): audit-mode zero deletes + enforce drops 600→500 + 100-row untouched.
- SC-2 (ROADMAP): tie-break correctness with cap=2, 3 seeded rows.
- SC-3 (ROADMAP): admin JWT → 200 + deleted_row_count; idempotent re-call → 0; non-admin other user → 403.
- SC-4 (ROADMAP): MEMORY_FORGET audit_log row retrievable with correct detail fields.
- EVICT-01, EVICT-02, GDPR-01, GDPR-02, GDPR-03 all have integration-level validation.
- Phase gate ready: run `uv run pytest tests/integration/ -m pgvector -x -q` before `/gsd-verify-work 25`.
</success_criteria>

<output>
Create `.planning/phases/25-eviction-job-gdpr-forget-api/25-06-SUMMARY.md` when done.
</output>
