---
phase: 25-eviction-job-gdpr-forget-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.py
  - services/audit/audit_service.py
  - tests/unit/test_phase25_foundations.py
autonomous: true
requirements: [EVICT-01, EVICT-02, GDPR-03]
tags: [settings, memory-cap, audit-enum, MEMORY_FORGET, MEMORY_EVICT, wave1-prep]
review_amendments: []
must_haves:
  truths:
    - "`settings.memory_facts_cap_per_user: int = 500` is importable from `config.settings.settings` with default 500"
    - "`AuditAction.MEMORY_FORGET` and `AuditAction.MEMORY_EVICT` exist in `services/audit/audit_service.py` as str-enum values `'MEMORY_FORGET'` and `'MEMORY_EVICT'` respectively"
    - "Both new enum values are appended AFTER `TOKEN_VERIFIED` — existing 12 values are verbatim unchanged"
    - "The new settings field is readable via `MEMORY_FACTS_CAP_PER_USER` env override (Pydantic V2 BaseSettings auto-mapping)"
  artifacts:
    - path: "config/settings.py"
      provides: "New `memory_facts_cap_per_user: int = 500` field with comment block in the memory section"
      contains: ["memory_facts_cap_per_user: int = 500", "# Phase 25 / EVICT-01"]
    - path: "services/audit/audit_service.py"
      provides: "AuditAction extended with MEMORY_FORGET + MEMORY_EVICT at end of enum block"
      contains: ["MEMORY_FORGET     = \"MEMORY_FORGET\"", "MEMORY_EVICT      = \"MEMORY_EVICT\"", "# Phase 25"]
    - path: "tests/unit/test_phase25_foundations.py"
      provides: "4 unit tests: settings field presence + default, enum presence × 2, enum value strings"
      exports:
        - test_memory_facts_cap_per_user_default
        - test_memory_facts_cap_per_user_is_int
        - test_audit_action_memory_forget_exists
        - test_audit_action_memory_evict_exists
  key_links:
    - from: "config/settings.py::Settings.memory_facts_cap_per_user"
      to: "scripts/evict_long_term_facts.py (Plan 25-05 consumer)"
      via: "`cap = settings.memory_facts_cap_per_user` at top of `main_async()`"
      pattern: "memory_facts_cap_per_user"
    - from: "services/audit/audit_service.py::AuditAction"
      to: "scripts/evict_long_term_facts.py + controllers/memory.py (Plans 25-04, 25-05)"
      via: "`AuditAction.MEMORY_FORGET` / `AuditAction.MEMORY_EVICT` in `AuditEvent(action=...)`"
      pattern: "MEMORY_FORGET|MEMORY_EVICT"
---

<objective>
Land the two independent Wave-1 foundations for Phase 25:

1. `config/settings.py` — add `memory_facts_cap_per_user: int = 500` (new field, env override `MEMORY_FACTS_CAP_PER_USER`). Consumed by the eviction CLI (Plan 25-05) and optionally by save_fact pre-check in v1.7+.

2. `services/audit/audit_service.py::AuditAction` — append `MEMORY_FORGET` and `MEMORY_EVICT` at end of the existing 12-value enum block (per D-2.1 + Pitfall 5: append-only to preserve existing string values in DB).

Purpose: unblocks Plan 25-04 (forget controller references `AuditAction.MEMORY_FORGET`) and Plan 25-05 (eviction CLI references `AuditAction.MEMORY_EVICT` + `settings.memory_facts_cap_per_user`). These are pure additions — no existing code modified beyond the two targeted insertion points.

Output: two modified files + one new unit test file (4 RED→GREEN tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-CONTEXT.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-RESEARCH.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-PATTERNS.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-VALIDATION.md
@config/settings.py
@services/audit/audit_service.py

<interfaces>
<!-- Extracted from working tree 2026-05-16. -->

From config/settings.py (Phase 23 extractor precedent — exact shape to mirror):
```python
# Extractor sub-agent (Phase 23, MEM-03) ──────────────────────────────────
# extractor_enabled gates dispatch_extraction at the pipeline boundary
extractor_enabled:  bool                                = True
```
Place `memory_facts_cap_per_user` in the memory section near `audit_db_enabled`, after Phase 23/24 extractor fields.

From services/audit/audit_service.py (existing AuditAction — verbatim skeleton per 25-PATTERNS Analog 5):
```python
class AuditAction(str, Enum):
    QUERY             = "QUERY"
    INGEST            = "INGEST"
    DELETE_DOC        = "DELETE_DOC"
    LOGIN             = "LOGIN"
    LOGOUT            = "LOGOUT"
    PERMISSION_DENIED = "PERMISSION_DENIED"
    RATE_LIMITED      = "RATE_LIMITED"
    PII_DETECTED      = "PII_DETECTED"
    RULE_BLOCKED      = "RULE_BLOCKED"
    FEEDBACK          = "FEEDBACK"
    KB_UPDATE         = "KB_UPDATE"
    TOKEN_VERIFIED    = "TOKEN_VERIFIED"
```
Append AFTER `TOKEN_VERIFIED`. Do NOT reorder existing members.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — settings cap field + AuditAction enum presence tests</name>
  <files>tests/unit/test_phase25_foundations.py</files>
  <read_first>
    - config/settings.py (extractor block at ~line 296-302 — precedent for new field placement)
    - services/audit/audit_service.py (AuditAction enum block — identify TOKEN_VERIFIED as last member, verify 12-count)
    - tests/unit/test_memory_save_fact.py (lines 25-28 — env-var setdefault pattern to copy verbatim)
    - .planning/phases/25-eviction-job-gdpr-forget-api/25-PATTERNS.md (Analog 5 — AuditAction; Analog 6 — settings field)
  </read_first>
  <behavior>
    - Test 1 (`test_memory_facts_cap_per_user_default`): import settings; assert `settings.memory_facts_cap_per_user == 500`. Fails with `AttributeError` until Task 2 adds the field. RED.
    - Test 2 (`test_memory_facts_cap_per_user_is_int`): introspect `Settings.model_fields["memory_facts_cap_per_user"]`; assert `field.annotation is int` and `field.default == 500`. RED.
    - Test 3 (`test_audit_action_memory_forget_exists`): `from services.audit.audit_service import AuditAction; assert AuditAction.MEMORY_FORGET.value == "MEMORY_FORGET"`. Fails with `AttributeError` until Task 2 extends the enum. RED.
    - Test 4 (`test_audit_action_memory_evict_exists`): `assert AuditAction.MEMORY_EVICT.value == "MEMORY_EVICT"`. RED.
  </behavior>
  <action>
    Create `tests/unit/test_phase25_foundations.py`.

    File header (verbatim env-var setdefault before any service import):
    - `os.environ.setdefault("APP_MODEL_DIR", "/tmp")`
    - `os.environ.setdefault("SECRET_KEY", "a-very-secure-key-for-testing-that-is-long-32c")`

    Start with `from __future__ import annotations` (repo convention — SP-1).

    Keep all `config.settings` and `services.audit.audit_service` imports INSIDE test function bodies (not at module top) so pytest collection succeeds in RED state without import errors.

    Do NOT test eviction logic or controller behavior here — this plan only gates the two field/enum additions.

    Run collection gate: `uv run pytest tests/unit/test_phase25_foundations.py --collect-only -q` — must show 4 items.
    Run RED gate: `uv run pytest tests/unit/test_phase25_foundations.py -x -q` — expect 4 failures.
    Commit: `test(25-01): RED gates for settings cap field + AuditAction enum (EVICT-01, GDPR-03 D-2.1)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_phase25_foundations.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_phase25_foundations.py` exists.
    - `uv run pytest tests/unit/test_phase25_foundations.py --collect-only -q` lists exactly 4 test items.
    - `uv run pytest tests/unit/test_phase25_foundations.py -x -q` exits non-zero (RED).
    - `grep -E '^os.environ.setdefault' tests/unit/test_phase25_foundations.py | wc -l` ≥ 2.
    - `grep -E '^from __future__ import annotations' tests/unit/test_phase25_foundations.py` matches one line.
    - `grep -v '^#' tests/unit/test_phase25_foundations.py | grep -c 'importlib'` equals 0 (no reload tests here).
  </acceptance_criteria>
  <done>4 RED gates committed. Collection succeeds; all 4 tests fail confirming production changes not yet landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add settings field + extend AuditAction enum</name>
  <files>config/settings.py, services/audit/audit_service.py</files>
  <read_first>
    - config/settings.py (full file — find memory/audit section for insertion point; locate exact line after Phase 23/24 extractor block)
    - services/audit/audit_service.py (AuditAction enum block — find TOKEN_VERIFIED line; confirm 12-member count before adding)
    - .planning/phases/25-eviction-job-gdpr-forget-api/25-PATTERNS.md (Analog 5 verbatim skeleton for AuditAction; Analog 6 settings pattern)
    - .planning/phases/25-eviction-job-gdpr-forget-api/25-RESEARCH.md (§E3 AuditAction verbatim skeleton; Pitfall 5 — append-only invariant)
    - tests/unit/test_phase25_foundations.py (the 4 RED gates from Task 1)
  </read_first>
  <action>
    Edit `config/settings.py`:
    Find the memory-related section (near `audit_db_enabled` or after Phase 23/24 extractor fields). Insert immediately after the Phase 24 recall_tool_enabled field or in the memory sub-section:

    ```
    # Phase 25 / EVICT-01 — per-user long_term_facts row cap ─────────────────
    # Consumed by scripts/evict_long_term_facts.py and (v1.7+) save_fact.
    # Override via env MEMORY_FACTS_CAP_PER_USER=<int>.
    memory_facts_cap_per_user: int = 500
    ```

    Verbatim from 25-PATTERNS.md Analog 6. Do NOT add a `Field(...)` wrapper — plain type annotation with default is the Pydantic V2 BaseSettings convention used throughout this file.

    Edit `services/audit/audit_service.py`:
    Locate the `AuditAction` enum class. Find the `TOKEN_VERIFIED = "TOKEN_VERIFIED"` line (last existing member). Append immediately after it:

    ```python
        # Phase 25 — D-2.1 — GDPR forget API + eviction job
        MEMORY_FORGET     = "MEMORY_FORGET"   # forget_user() API call
        MEMORY_EVICT      = "MEMORY_EVICT"    # eviction sweep (one row per bucket)
    ```

    Verbatim from 25-PATTERNS.md Analog 5 and 25-RESEARCH.md §E3. Do NOT reorder or rename existing members — Pitfall 5 invariant. Do NOT insert between existing members.

    After both edits, run:
    - `uv run pytest tests/unit/test_phase25_foundations.py -x -q` — expect 4 GREEN.
    - `uv run ruff check config/settings.py services/audit/audit_service.py` — expect zero violations.
    - `uv run mypy --strict services/audit/audit_service.py` — expect zero new violations (enum extension is append-only).

    Commit: `feat(25-01): add memory_facts_cap_per_user setting + MEMORY_FORGET/MEMORY_EVICT enum values (D-2.1, EVICT-01)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_phase25_foundations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_phase25_foundations.py -x -q` exits 0 (4 tests GREEN).
    - `python -c "from config.settings import settings; assert settings.memory_facts_cap_per_user == 500"` exits 0.
    - `python -c "from services.audit.audit_service import AuditAction; assert AuditAction.MEMORY_FORGET.value == 'MEMORY_FORGET'; assert AuditAction.MEMORY_EVICT.value == 'MEMORY_EVICT'"` exits 0.
    - `grep -n 'memory_facts_cap_per_user: int = 500' config/settings.py` matches exactly one line.
    - `grep -n 'MEMORY_FORGET' services/audit/audit_service.py | grep -v '^#'` matches exactly one line containing `= "MEMORY_FORGET"`.
    - `grep -n 'MEMORY_EVICT' services/audit/audit_service.py | grep -v '^#'` matches exactly one line containing `= "MEMORY_EVICT"`.
    - `grep -n 'TOKEN_VERIFIED' services/audit/audit_service.py | head -1` shows line number LESS THAN the MEMORY_FORGET line (append-after-TOKEN_VERIFIED invariant).
    - `uv run ruff check config/settings.py services/audit/audit_service.py` exits 0.
  </acceptance_criteria>
  <done>Settings field importable with default 500; both AuditAction enum values accessible; 4 RED gates GREEN; ruff clean; no existing enum members disturbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env var → Pydantic settings | `MEMORY_FACTS_CAP_PER_USER` parsed as int; malformed value raises Pydantic ValidationError at startup |
| DB column `action` ↔ AuditAction enum | Enum string values are persisted to DB; append-only extension ensures existing rows decode correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-S1 | Spoofing | `MEMORY_FACTS_CAP_PER_USER` env var injected by unprivileged process | accept | Cap only affects eviction policy, not auth; operator owns deploy config |
| T-25-01-T1 | Tampering | Existing AuditAction enum values renamed or reordered | mitigate | Pitfall 5: tests assert exact string values (`== "MEMORY_FORGET"`); append-only edit; existing member values unchanged by this plan |
| T-25-01-I1 | Information Disclosure | AuditAction enum names readable in OpenAPI schema | accept | Enum names are intentionally public; existing values already exposed |
| T-25-01-D1 | Denial | `memory_facts_cap_per_user = 0` set via env collapses cap to zero, deleting all rows | accept | Operator config; documented in EVICT-03 runbook; default 500 is sane; v1.7+ can add min-value validator |
| T-25-01-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages (RESEARCH §Package Legitimacy Audit "Not applicable") |
</threat_model>

<verification>
- 4 RED tests from Task 1 flip GREEN in Task 2.
- `python -c "from config.settings import settings; assert settings.memory_facts_cap_per_user == 500"` exits 0.
- `python -c "from services.audit.audit_service import AuditAction; assert len([a for a in AuditAction]) == 14"` exits 0 (12 existing + 2 new).
- `uv run ruff check config/settings.py services/audit/audit_service.py` exits 0.

Run gate: `uv run pytest tests/unit/test_phase25_foundations.py -x -q && uv run ruff check config/settings.py services/audit/audit_service.py`
</verification>

<success_criteria>
- EVICT-01 structural prerequisite: `settings.memory_facts_cap_per_user` available for eviction CLI (Plan 25-05).
- EVICT-02 structural prerequisite: `AuditAction.MEMORY_EVICT` available for eviction CLI audit rows.
- GDPR-03 structural prerequisite: `AuditAction.MEMORY_FORGET` available for forget controller audit rows.
- D-2.1 honored: TWO new enum values added, 12 existing values unchanged.
- Pitfall 5 mitigated: values appended after TOKEN_VERIFIED, string values match enum names exactly.
</success_criteria>

<output>
Create `.planning/phases/25-eviction-job-gdpr-forget-api/25-01-SUMMARY.md` when done.
</output>
