---
phase: 25-eviction-job-gdpr-forget-api
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [EVICT-03]
tags: [requirements-accounting, evict-03-unmark, d-4.1, traceability]
review_amendments: []
must_haves:
  truths:
    - "EVICT-03 in `.planning/REQUIREMENTS.md` is marked `[ ]` (open) — not `[x]`"
    - "The un-mark is accompanied by a NOTE comment explaining why (Plan 24-06 partial delivery, Phase 25 re-mark at verifier close)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "EVICT-03 line shows `- [ ] **EVICT-03**:` with NOTE annotation intact"
      contains: ["- [ ] **EVICT-03**", "NOTE (2026-05-16, per 25-CONTEXT D-4.1)"]
  key_links:
    - from: ".planning/REQUIREMENTS.md::EVICT-03"
      to: ".planning/phases/25-eviction-job-gdpr-forget-api/25-07-PLAN.md"
      via: "re-mark `[x]` at phase verifier close after docs extension + coverage gate pass"
      pattern: "EVICT-03"
---

<objective>
Un-mark `EVICT-03` from `[x]` to `[ ]` in `.planning/REQUIREMENTS.md` per Decision D-4.1.

Plan 24-06 delivered ONLY the Backfill companion section (49 LOC) to `docs/memory-eviction.md`. The CronJob YAML, audit→enforce operator workflow, cap-tuning guidance, and forget-API curl example were NEVER delivered. The `[x]` mark on EVICT-03 was premature.

This is a single-line edit for honest accounting. It is Wave 1 (parallel to Plans 25-01 and 25-02) because it touches a disjoint file.

`EVICT-03` will be re-marked `[x]` at Phase 25 verifier close — after Plan 25-07 delivers the docs extension and the per-module coverage gate passes.

Purpose: surface the accounting gap in git history before any Phase 25 code lands. Commit message: `docs(25): un-mark EVICT-03 — Plan 24-06 delivered Backfill section only; CronJob + audit-workflow + forget-curl deferred to Phase 25`.

Output: one line edit in REQUIREMENTS.md (the `[x]` → `[ ]` flip). The NOTE annotation already present from context preparation is preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/25-eviction-job-gdpr-forget-api/25-CONTEXT.md

<interfaces>
<!-- From .planning/REQUIREMENTS.md (EVICT section, lines ~52-53): -->
Current line to edit:
```
- [x] **EVICT-03**: `docs/memory-eviction.md` ...
```
Target state (un-marked + NOTE preserved):
```
- [ ] **EVICT-03**: `docs/memory-eviction.md` ... **NOTE (2026-05-16, per 25-CONTEXT D-4.1):** Un-marked...
```
Note: the NOTE annotation may already be present from the CONTEXT preparation session. Read the file to confirm current state before editing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify + un-mark EVICT-03 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (EVICT section — find EVICT-03 line, confirm its current marked/unmarked state; read surrounding NOTE annotation if present)
    - .planning/phases/25-eviction-job-gdpr-forget-api/25-CONTEXT.md (D-4.1 decision text)
  </read_first>
  <action>
    Read `.planning/REQUIREMENTS.md`. Find the `EVICT-03` line in the Eviction section.

    Case A — already `[ ]` with NOTE: file is already correct (CONTEXT session may have done this). Verify the NOTE text is present. If correct, no edit needed. Record in SUMMARY that the state was pre-applied.

    Case B — still `[x]` without NOTE: change `[x]` to `[ ]` and append the NOTE inline:
    `**NOTE (2026-05-16, per 25-CONTEXT D-4.1):** Un-marked from [x] to [ ]. Plan 24-06 delivered ONLY the Backfill section (49 LOC); CronJob YAML + audit→enforce workflow + forget-API curl are deferred to Phase 25. Re-mark [x] at Phase 25 verifier close.`

    Case C — `[ ]` already but NOTE missing: add the NOTE text inline after the existing EVICT-03 description.

    After the edit (or confirmation), run:
    - `grep -n 'EVICT-03' .planning/REQUIREMENTS.md` — verify the line contains `[ ]` (not `[x]`).
    - `grep -A 2 'EVICT-03' .planning/REQUIREMENTS.md` — verify NOTE text is present.

    Commit: `docs(25): un-mark EVICT-03 — Plan 24-06 delivered Backfill section only; CronJob + audit-workflow + forget-curl deferred to Phase 25`.

    Do NOT edit any other requirement lines. Do NOT edit ROADMAP.md here (that is Plan 25-07's responsibility). Touch ONLY `.planning/REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>grep -n 'EVICT-03' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'EVICT-03' .planning/REQUIREMENTS.md | grep -c '\[ \]'` equals 1 (un-marked).
    - `grep 'EVICT-03' .planning/REQUIREMENTS.md | grep -c '\[x\]'` equals 0 (not marked complete).
    - `grep -A 5 'EVICT-03' .planning/REQUIREMENTS.md | grep -c 'D-4.1\|Phase 25\|verifier'` ≥ 1 (NOTE present).
    - No other requirement lines modified (EVICT-01, EVICT-02, GDPR-01, GDPR-02, GDPR-03 all still `[ ]`; MEM-01..MEM-10 status unchanged).
  </acceptance_criteria>
  <done>EVICT-03 is `[ ]` with NOTE in REQUIREMENTS.md. Commit records the honest accounting correction. Plan 25-07 will re-mark `[x]` at phase close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md ↔ orchestrator tools | Planner/checker reads requirement markers; incorrect `[x]` would report false closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-T1 | Tampering | Accidental edit of other requirement lines during this single-line fix | mitigate | acceptance_criteria confirms surrounding requirements unchanged; surgical Edit (not full rewrite) |
| T-25-03-I1 | Information Disclosure | n/a | n/a | doc-only change |
| T-25-03-SC | Tampering | npm/pip/cargo installs | n/a | No code changes |
</threat_model>

<verification>
- `grep 'EVICT-03' .planning/REQUIREMENTS.md | grep -c '\[ \]'` equals 1.
- `grep 'EVICT-03' .planning/REQUIREMENTS.md | grep -c '\[x\]'` equals 0.

Run gate: `grep 'EVICT-03' .planning/REQUIREMENTS.md`
</verification>

<success_criteria>
- D-4.1 honored: EVICT-03 is `[ ]` before Phase 25 code execution begins.
- Git history captures the honest accounting correction with a clear commit message.
- Plan 25-07 can re-mark `[x]` after docs + coverage gate pass.
</success_criteria>

<output>
Create `.planning/phases/25-eviction-job-gdpr-forget-api/25-03-SUMMARY.md` when done.
</output>
