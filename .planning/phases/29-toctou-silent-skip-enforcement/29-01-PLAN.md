---
plan_id: 29-01
phase: 29
phase_name: TOCTOU + Silent-Skip Enforcement
plan: 1
wave: 2
type: tdd
depends_on: [29-00]
requirements: [SK-01]
files_modified:
  - services/memory/memory_service.py
  - tests/unit/memory/test_save_facts_batch_dedupe.py
  - tests/unit/test_memory_save_fact.py
autonomous: true
must_haves:
  truths:
    - "When `_bulk_near_duplicate_check` returns `dup_zero_idxs = {1, 3}` for a 5-item batch, only indices {0, 2, 4} are present in `rows_to_insert` passed to `conn.executemany`."
    - "`MEMORY_NEAR_DUPLICATE_SKIPPED` audit row is still emitted per duplicate index (same shape as v1.7 D-09 audit-mode)."
    - "`SaveFactsResult.saved_count` equals the count of non-duplicate rows actually INSERTed (== 3 in the example above); `skipped_near_duplicates` equals `len(dup_zero_idxs)` (== 2)."
    - "Audit-write failure on a `MEMORY_NEAR_DUPLICATE_SKIPPED` emit does NOT block the skip-INSERT — `executemany` still receives the non-dup rows."
    - "`save_fact` (D-12 wrapper) inherits silent-skip via delegation: a duplicate-text single-fact save returns `saved_count=0`, `skipped_near_duplicates=1`, and emits NO `MemoryFactWriteError`."
  artifacts:
    - path: "services/memory/memory_service.py"
      provides: "INSERT-side filter that excludes duplicates from `rows_to_insert`"
      contains: "if local_i not in dup_zero_idxs"
    - path: "tests/unit/memory/test_save_facts_batch_dedupe.py"
      provides: "v1.7 pin test renamed to silent-skip enforcement assertion shape"
      contains: "test_dedupe_in_batch_fires_audit_AND_executemany_inserts_non_dup_rows_only"
  key_links:
    - from: "services/memory/memory_service.py::save_facts (Step 5)"
      to: "filtered `rows_to_insert` (silent-skip enforcement)"
      via: "list comprehension that excludes `local_i in dup_zero_idxs`"
      pattern: "not in dup_zero_idxs"
    - from: "services/memory/memory_service.py::save_facts (Step 4)"
      to: "audit emit (preserved)"
      via: "`_fire_near_duplicate_audit` still called per dup; failure non-fatal"
      pattern: "_fire_near_duplicate_audit"
---

<objective>
Promote v1.7 D-09 audit-mode-only near-duplicate handling to silent-skip
enforcement in `LongTermMemory.save_facts`: filter `dup_zero_idxs` out of
`rows_to_insert` BEFORE `conn.executemany`. Preserve the audit emit and the
audit-write-failure-non-fatal discipline.

Purpose: SK-01 acceptance — duplicates do NOT INSERT, only the audit row
remains. `save_fact` (D-12 wrapper) inherits via delegation. Safe to ship NOW
because Plan 29-00 closed the TOCTOU window — two concurrent writers no longer
race past the precheck.

Output: production code on `services/memory/memory_service.py::save_facts`
+ rewrite-in-place of v1.7 pin test in `tests/unit/memory/test_save_facts_batch_dedupe.py`
(rename + flip assertion) + adjustment of `save_fact` wrapper tests in
`tests/unit/test_memory_save_fact.py` for the v1.8 contract change.

Per `29-CONTEXT.md` D-SK-01: audit row still fires; only the INSERT side
changes. Per Open Risks #3: `save_fact` wrapper tests at
`tests/unit/test_memory_save_fact.py` may need adjustment (expected
`saved_count=0` for duplicate-text single saves vs v1.7's `saved_count=1`).
Per Open Risks #4: audit-write failure must NOT block skip-INSERT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-toctou-silent-skip-enforcement/29-CONTEXT.md
@.planning/phases/29-toctou-silent-skip-enforcement/29-00-PLAN.md
@.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-04-SUMMARY.md
@.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-VERIFICATION.md
@services/memory/memory_service.py
@tests/unit/memory/test_save_facts_batch_dedupe.py
@tests/unit/test_memory_save_fact.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from `services/memory/memory_service.py`. -->

From services/memory/memory_service.py:566-717 (`save_facts` POST-29-00):
  - `dup_zero_idxs: set[int]` populated by `_bulk_near_duplicate_check` (Step 3a)
  - `indexed: list[tuple[int, ExtractedFact, list[float]]]` — original-index, fact, embedding
  - Step 4: audit emit loop iterates `for local_i in dup_zero_idxs: _fire_near_duplicate_audit(... indexed[local_i][1].fact ...)`
  - Step 5 (CURRENT): `rows_to_insert = [(user_id, tenant_id, f.fact, source_doc, f.importance, e) for _, f, e in indexed]` — UNFILTERED
  - Step 5 (POST-29-01): filter `enumerate(indexed)` by `local_i not in dup_zero_idxs`

From services/memory/memory_service.py:480-514 (`_fire_near_duplicate_audit`):
  - signature: `async def _fire_near_duplicate_audit(user_id, tenant_id, fact, dist) -> None`
  - emits `AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED` with `result=AuditResult.SKIPPED`
  - `except Exception` swallow (v1.6 GDPR T1 — audit failure non-fatal)

From services/memory/memory_service.py:96-130 (`SaveFactsResult` dataclass):
  - fields: `saved_count: int`, `skipped_near_duplicates: int`, `skipped_embed_failures: int`
  - frozen dataclass

From services/memory/memory_service.py:719-759 (`save_fact` D-12 wrapper):
  - delegates to `save_facts([extracted], ...)`
  - raises `MemoryFactWriteError` ONLY when `saved_count == 0 AND skipped_embed_failures > 0`
  - silent-skip case: `saved_count == 0 AND skipped_near_duplicates == 1 AND skipped_embed_failures == 0` → returns normally (no raise)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (RED): Flip v1.7 pin test to silent-skip assertion shape</name>
  <files>tests/unit/memory/test_save_facts_batch_dedupe.py</files>
  <read_first>
    - tests/unit/memory/test_save_facts_batch_dedupe.py (entire file — v1.7 pin test plus fail-OPEN test)
    - services/memory/memory_service.py:566-717 (`save_facts` body)
    - .planning/phases/29-toctou-silent-skip-enforcement/29-CONTEXT.md (D-SK-01 + Open Risks #4)
    - .planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-VERIFICATION.md (C3 D-09 pin evidence)
  </read_first>
  <behavior>
    - Rename `test_dedupe_in_batch_fires_audit_AND_executemany_inserts_all_rows` → `test_dedupe_in_batch_fires_audit_AND_executemany_inserts_non_dup_rows_only`.
    - Update docstring: replace "D-09 audit-mode-only" wording with "SK-01 silent-skip enforcement (v1.8 promotion of D-09 audit-mode)".
    - Flip CRITICAL assertion: `len(rows passed to executemany) == 3` (NOT 5) — duplicates at indices 1 and 3 are filtered out; only original indices {0, 2, 4} remain.
    - Add assertion on row content: extract `fact` field (position 2 in the tuple per `(user_id, tenant_id, fact, source_doc, importance, embedding)`) → `{row[2] for row in rows} == {"dup-test fact 0", "dup-test fact 2", "dup-test fact 4"}`.
    - Update `result.saved_count == 3` (was 5).
    - Keep `result.skipped_near_duplicates == 2` (semantics unchanged — count of skipped).
    - Keep `result.skipped_embed_failures == 0`.
    - Keep all 2 audit emits (`audit.log.await_count == 2`) — audit emit semantics preserved.
    - Keep audit fact-texts coming from original indices {1, 3} — `seen_facts == {"dup-test fact 1", "dup-test fact 3"}`.
    - ADD a third test: `test_audit_write_failure_does_not_block_skip_insert` — `_fire_near_duplicate_audit` raises an exception (monkeypatch `get_audit_service` to a mock whose `.log` raises); assert `conn.executemany` still receives the 3 non-dup rows and `result.saved_count == 3` (v1.6 T1 carry-forward; Open Risks #4).
    - **ADD a fourth test (plan-review T3): `test_all_duplicates_short_circuits_executemany`** — `_bulk_near_duplicate_check_raw` (or whichever helper save_facts now calls) returns `dup_zero_idxs = {0, 1, 2, 3, 4}` for a 5-fact batch. Assert: `conn.executemany.call_count == 0` (INSERT skipped entirely — no empty-list passed to executemany), `result.saved_count == 0`, `result.skipped_near_duplicates == 5`, `result.skipped_embed_failures == 0`. Audit emit count == 5 (one per dup, fire-and-forget). Pins the `if not rows_to_insert:` short-circuit branch added in Task 1.
    - Keep `test_bulk_dedupe_fail_open_on_postgres_error` UNCHANGED (no duplicates flagged → all 5 rows inserted → assertion shape unchanged).
    - These tests RED-fail until the production-code change in Task 1 lands.
  </behavior>
  <action>
    Edit `tests/unit/memory/test_save_facts_batch_dedupe.py` in place.
    Rename the test function (use `git mv`-friendly in-file rename; no file rename).
    Update assertions per `<behavior>`.
    Append the new `test_audit_write_failure_does_not_block_skip_insert` test using the existing `_make_fake_pool` + `_patch_embedder_batch` helpers; monkeypatch `services.memory.memory_service.LongTermMemory._fire_near_duplicate_audit` (or the underlying `get_audit_service`) so the call raises `RuntimeError("audit DB down")` per dup. Assert `executemany` call_count == 1, len(rows) == 3, `result.saved_count == 3`, `result.skipped_near_duplicates == 2`.
    Do NOT touch production code in this task.
    Commit message: `test(29-01): RED — flip v1.7 audit-mode pin to SK-01 silent-skip enforcement`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/memory/test_save_facts_batch_dedupe.py -v 2>&1 | tee /tmp/29-01-red.log; grep -E "FAILED|PASSED" /tmp/29-01-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_dedupe_in_batch_fires_audit_AND_executemany_inserts_non_dup_rows_only" tests/unit/memory/test_save_facts_batch_dedupe.py` == 1.
    - `grep -c "test_dedupe_in_batch_fires_audit_AND_executemany_inserts_all_rows" tests/unit/memory/test_save_facts_batch_dedupe.py` == 0 (old name removed).
    - `grep -c "test_audit_write_failure_does_not_block_skip_insert" tests/unit/memory/test_save_facts_batch_dedupe.py` == 1 (new test added).
    - `grep -c "test_all_duplicates_short_circuits_executemany" tests/unit/memory/test_save_facts_batch_dedupe.py` == 1 (plan-review T3 added).
    - Renamed test fails: assertion error on `len(rows) == 3` (actual 5) or `result.saved_count == 3` (actual 5).
    - `test_audit_write_failure_does_not_block_skip_insert` fails for the same reason.
    - `test_all_duplicates_short_circuits_executemany` fails because pre-Task-1 production code calls `executemany` with all 5 rows (no short-circuit).
    - `test_bulk_dedupe_fail_open_on_postgres_error` STILL PASSES (no semantic change).
    - No production-code change in this commit.
    - mypy + ruff clean on edited test file.
  </acceptance_criteria>
  <done>
    RED commit pushed; renamed test + new audit-failure test fail; fail-open test still passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (GREEN): Silent-skip filter in `save_facts` Step 5</name>
  <files>services/memory/memory_service.py</files>
  <read_first>
    - services/memory/memory_service.py:566-717 (`save_facts` body — POST-29-00, with advisory lock in place)
    - services/memory/memory_service.py:480-514 (`_fire_near_duplicate_audit` — non-fatal contract)
    - services/memory/memory_service.py:96-130 (`SaveFactsResult` dataclass)
    - services/memory/memory_service.py:719-759 (`save_fact` D-12 wrapper — verify silent-skip path does NOT trigger `MemoryFactWriteError`)
    - tests/unit/memory/test_save_facts_batch_dedupe.py (RED from Task 0)
    - .planning/phases/29-toctou-silent-skip-enforcement/29-CONTEXT.md (D-SK-01 + Open Risks #3, #4)
  </read_first>
  <behavior>
    - In `save_facts` Step 5 (`rows_to_insert` construction at line ~694), filter out entries whose local index is in `dup_zero_idxs`.
    - Local index = the position within `indexed` list (0..len(indexed)-1) — this is the same coordinate space `_bulk_near_duplicate_check` returns via `(idx - 1) AS zero_idx` from `unnest($1::text[]) WITH ORDINALITY` where `$1 = valid_embeddings` (passed in `indexed` order).
    - `rows_to_insert = [(user_id, tenant_id, f.fact, source_doc, f.importance, e) for local_i, (_, f, e) in enumerate(indexed) if local_i not in dup_zero_idxs]`.
    - `SaveFactsResult.saved_count` becomes `len(rows_to_insert)` (already is — definition unchanged; value is now smaller for dup batches).
    - `skipped_near_duplicates = len(dup_zero_idxs)` (unchanged).
    - If filtered `rows_to_insert` is empty (entire batch was duplicates), SKIP the `conn.executemany` call entirely — do NOT pass empty list to executemany (asyncpg behavior on empty executemany is implementation-defined; explicit skip is safer + reduces 1 RTT).
    - Audit emit loop UNCHANGED — still fires `_fire_near_duplicate_audit` per dup index BEFORE the INSERT filter happens (audit emit order: dup audit emits first, then INSERT of non-dups).
    - Audit-write failure handling UNCHANGED — `_fire_near_duplicate_audit` already swallows exceptions internally (line ~513-514); confirm path still works (Open Risks #4 / Task 0 test 3 covers this).
    - `save_fact` D-12 wrapper unchanged in code — but its observable contract changes: a duplicate-text single save now returns `saved_count=0, skipped_near_duplicates=1, skipped_embed_failures=0` (was `saved_count=1, skipped_near_duplicates=1`). The wrapper's `MemoryFactWriteError` raise condition (`saved_count == 0 AND skipped_embed_failures > 0`) does NOT trigger for silent-skip — wrapper returns normally. Verify by reading lines 757-759.
    - Update `save_facts` docstring "D-09 audit-mode-only (C3)" paragraph: rewrite to state "SK-01 v1.8 silent-skip enforcement: duplicates filtered from rows_to_insert; audit row still emitted for ops visibility." Update the "Wire shape" block: "K× audit_log emit" is unchanged in count; `1× executemany INSERT` only runs if `rows_to_insert` non-empty.
    - Update `_fire_near_duplicate_audit` docstring: remove "INSERT in save_fact still runs (audit-mode-before-enforce per v1.6 Phase 25 EVICT-02). v1.8 promotes to actual silent-skip." sentence → replace with "v1.8 SK-01 enforcement live: caller `save_facts` filters duplicate from rows_to_insert; this audit emit is now the sole DB-visible artifact of a skipped duplicate."
    - Update `AuditResult.SKIPPED` inline comment in `_fire_near_duplicate_audit` (line ~507): change "semantic intent — INSERT NOT skipped in v1.7" → "SK-01 v1.8 — INSERT IS skipped by caller".
    - Optional: update `SaveFactsResult.skipped_near_duplicates` field-doc in dataclass docstring (lines 96-130) to drop the "*semantic* intent — v1.7 still INSERTs duplicates" caveat.
  </behavior>
  <action>
    Edit `services/memory/memory_service.py::save_facts` Step 5 region.
    Use the list comprehension form in `<behavior>`.
    Add `if not rows_to_insert: return SaveFactsResult(saved_count=0, skipped_near_duplicates=len(dup_zero_idxs), skipped_embed_failures=embed_failures)` short-circuit BEFORE the `conn.executemany` try block.
    Update three docstring sites per `<behavior>`.
    Do NOT modify `_bulk_near_duplicate_check` (unchanged).
    Do NOT modify `_fire_near_duplicate_audit` body (only docstring + inline comment).
    Do NOT modify `save_fact` wrapper code (only run its tests to verify inherited behavior).
    No new imports.
    Commit message: `feat(29-01): GREEN — SK-01 silent-skip enforcement filters dups from save_facts INSERT`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/memory/test_save_facts_batch_dedupe.py tests/unit/memory/test_save_facts_batch.py tests/unit/memory/test_save_facts_embed_batch_fallback.py tests/unit/test_memory_save_fact.py -v 2>&1 | tee /tmp/29-01-green.log; grep -E "passed|failed" /tmp/29-01-green.log | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "not in dup_zero_idxs" services/memory/memory_service.py` ≥ 1.
    - `grep -n "if not rows_to_insert" services/memory/memory_service.py` shows the empty-batch short-circuit.
    - Renamed test `test_dedupe_in_batch_fires_audit_AND_executemany_inserts_non_dup_rows_only` PASSES.
    - New `test_audit_write_failure_does_not_block_skip_insert` PASSES.
    - `test_bulk_dedupe_fail_open_on_postgres_error` PASSES (no dups flagged → all 5 rows inserted; assertion unchanged).
    - All tests in `tests/unit/memory/test_save_facts_batch.py` + `test_save_facts_embed_batch_fallback.py` PASS (no SK-01 semantics change applies to non-dup batch tests).
    - `tests/unit/test_memory_save_fact.py` — if any test asserted `saved_count==1` for a duplicate-text input, update it to `saved_count==0` AND `skipped_near_duplicates==1` per Open Risks #3 (Plan 29-01 explicitly allows wrapper-test edits; document deltas in commit body).
    - TOC-01 integration test (Plan 29-00) STILL PASSES with SK-01 layered on top (`uv run pytest tests/integration/memory/test_save_facts_toctou.py -v -m uses_postgres` green).
    - `uv run mypy --strict services/memory/memory_service.py` — no new errors vs baseline.
    - `uv run ruff check services/memory/memory_service.py` clean.
    - No bare `except` introduced.
    - INSERT-ONLY `audit_log` invariant preserved.
  </acceptance_criteria>
  <done>
    GREEN commit pushed; renamed pin test + new audit-failure test pass; wrapper tests adjusted as needed per Open Risks #3; TOC-01 integration test still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (REFACTOR, optional): docstring + observability polish</name>
  <files>services/memory/memory_service.py</files>
  <read_first>
    - services/memory/memory_service.py (post-Task-1 state)
  </read_first>
  <behavior>
    - Cosmetic-only: harmonize `save_facts` + `_fire_near_duplicate_audit` + `SaveFactsResult` dataclass docstrings to remove ALL "audit-mode-only / v1.7 semantic intent" residual wording (any sites Task 1 missed).
    - Optional: add `logger.debug` line `"silent-skip: filtered {n} near-dup rows from save_facts batch user={} tenant={}"` after the filter step.
    - NO behavior change.
    - Skip entirely if Task 1 already left a clean state.
  </behavior>
  <action>
    Edit only docstrings + optional debug log. No control-flow change.
    Commit message: `refactor(29-01): SK-01 docstring sweep — remove residual audit-mode wording`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/memory/ tests/unit/test_memory_save_fact.py tests/integration/memory/test_save_facts_toctou.py -v -m "uses_postgres or not uses_postgres" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 acceptance still holds.
    - `grep -n "audit-mode-only\|v1.7 still INSERT\|v1.7 metric-only" services/memory/memory_service.py` returns no hits in `save_facts` / `_fire_near_duplicate_audit` / `SaveFactsResult` docstrings.
    - mypy --strict + ruff clean.
  </acceptance_criteria>
  <done>
    REFACTOR commit pushed (or task skipped — documented in SUMMARY); test suite still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → save_facts | untrusted fact text; near-duplicate-skip suppresses INSERT silently |
| save_facts → audit_log | trusted internal write; audit emit is the sole external signal of a skipped INSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Repudiation / Observability | silent INSERT suppression hides legitimate writes from caller-visible side effects | mitigate | `MEMORY_NEAR_DUPLICATE_SKIPPED` audit row still emitted per dup; ops dashboards retain dup-rate signal (D-09 audit emit preserved per D-SK-01). `SaveFactsResult.skipped_near_duplicates` exposes count to caller. |
| T-29-01-02 | Integrity (false-positive) | `_is_near_duplicate` flags a legitimate distinct fact as dup because cosine distance < threshold | accept | Cosine threshold unchanged from v1.7 audit-mode (already in production via D-09); threshold tuning is a future v1.9 question, not in v1.8 scope. Audit emit makes false-positives recoverable from logs. |
| T-29-01-03 | Availability | audit-write failure blocks the skip-INSERT path | mitigate | `_fire_near_duplicate_audit` already swallows `Exception` (line ~513-514, v1.6 GDPR T1 Pattern D). Task 0 adds a regression test (`test_audit_write_failure_does_not_block_skip_insert`) pinning this. |
| T-29-01-04 | Integrity (TOCTOU) | concurrent writer races past precheck and INSERTs dup, defeating silent-skip | mitigate | Plan 29-00 (advisory_lock) closes the race — SK-01 enforcement is only safe with 29-00 already shipped (depends_on: [29-00] gates this in execute-phase wave ordering). |
| T-29-01-SC | Tampering / supply-chain | npm/pip/cargo installs | accept | No package installs introduced. |

Severity: medium. Silent suppression is an observability-sensitive operation; mitigation depends on the audit-emit channel staying healthy (covered by v1.6 T1 + audit-emit non-fatal contract). ASVS L1.
</threat_model>

<verification>
- Renamed pin test passes (silent-skip enforcement).
- New audit-failure regression test passes (Open Risks #4).
- Fail-open test unchanged + passes.
- `save_fact` wrapper tests in `tests/unit/test_memory_save_fact.py` adjusted as needed per Open Risks #3 — silent-skip case (duplicate-text single save) returns `saved_count=0` without raising.
- TOC-01 integration test from Plan 29-00 still green under SK-01 layered on top.
- mypy --strict + ruff clean.
- diff-cover ≥ 80% on changed lines; --fail-under=70 global floor preserved.
- INSERT-ONLY `audit_log` invariant preserved.
- No new mypy --strict violations beyond Phase 28 baseline.
</verification>

<success_criteria>
1. `save_facts` filters duplicates from `rows_to_insert`; `conn.executemany` receives only non-duplicate rows.
2. `MEMORY_NEAR_DUPLICATE_SKIPPED` audit row still emitted per duplicate (D-09 audit emit preserved).
3. v1.7 pin test renamed `..._inserts_all_rows` → `..._inserts_non_dup_rows_only` with flipped assertion shape.
4. New `test_audit_write_failure_does_not_block_skip_insert` covers Open Risks #4.
5. `save_fact` wrapper silent-skip case (`saved_count=0, skipped_near_duplicates=1`) does NOT raise `MemoryFactWriteError` (wrapper raise condition is unchanged: only `embed_failures > 0` triggers the raise).
6. RED → GREEN [→ REFACTOR] commit cadence visible.
</success_criteria>

<output>
Create `.planning/phases/29-toctou-silent-skip-enforcement/29-01-SUMMARY.md` when done. Include:
- Wrapper-test edits applied (or "none required") per Open Risks #3.
- Audit-emit-failure test passing output excerpt.
- TOC-01 + SK-01 layered integration test output (Plan 29-00's test still green).
- Confirmation that mypy --strict baseline did not regress (record numeric baseline pre/post if helpful).
</output>
