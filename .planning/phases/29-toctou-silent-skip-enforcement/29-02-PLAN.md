---
plan_id: 29-02
phase: 29
phase_name: TOCTOU + Silent-Skip Enforcement
plan: 2
wave: 2
type: execute
depends_on: [29-00, 29-01]
requirements: [TEST-INFRA-02]
files_modified:
  - tests/unit/memory/test_save_fact_precheck.py
  - tests/unit/memory/test_save_fact_precheck_failure.py
autonomous: true
must_haves:
  truths:
    - "Precheck unit tests assert against the C1 bulk-SELECT SQL shape: `unnest($1::text[]) WITH ORDINALITY` plus `vec_txt::vector` cast (not the legacy per-fact `_is_near_duplicate` fetchrow shape)."
    - "The `nearest_distance=None` / empty-table branch is covered with an explicit unit test asserting the no-existing-rows path returns `dup_zero_idxs == set()` and `executemany` runs with all rows."
    - "Per-file LOC delta ≤ +150 (req acceptance bound)."
    - "Zero production-code edits land from this plan alone."
    - "Mocks live at consumer path `services.memory.memory_service.<dep>` (carry-forward v1.3 D-mock convention)."
  artifacts:
    - path: "tests/unit/memory/test_save_fact_precheck.py"
      provides: "Bulk-SELECT mock shape coverage for `_bulk_near_duplicate_check`"
      contains: "unnest"
    - path: "tests/unit/memory/test_save_fact_precheck_failure.py"
      provides: "nearest_distance=None / empty-table + failure-path coverage"
      contains: "nearest_distance"
  key_links:
    - from: "tests/unit/memory/test_save_fact_precheck.py"
      to: "services.memory.memory_service._bulk_near_duplicate_check"
      via: "mocked `conn.fetch` return value matches `[{'zero_idx': N}, ...]` shape"
      pattern: "zero_idx"
    - from: "tests/unit/memory/test_save_fact_precheck_failure.py"
      to: "empty-table branch (nearest_distance=None)"
      via: "mocked `conn.fetch` returns `[]`"
      pattern: "nearest_distance"
---

<objective>
Rewrite the `save_facts` precheck unit tests to exercise the C1 bulk-SELECT
code path (the SQL actually executed in production since Phase 27/TD-05),
covering the `nearest_distance=None` / empty-table branch explicitly.

Purpose: v1.7 ships per-fact precheck tests that exercise `_is_near_duplicate`
(the singular `fetchrow` SELECT) — but production routes through
`_bulk_near_duplicate_check` (C1 SQL: `unnest($1::text[]) WITH ORDINALITY`
plus `vec_txt::vector` cast). The legacy tests no longer reflect the live
code path. TEST-INFRA-02 acceptance: tests assert against the bulk-SELECT
SQL shape and cover the `None` branch.

Output: rewrite-in-place of two existing test files (no new files, no
production code). Per-file LOC delta ≤ +150 (req acceptance bound enforced
by line-count assertion in the acceptance check).

Per `29-CONTEXT.md` D-TEST-INFRA-02 (rewrite-in-place locked): primary
target is `tests/unit/memory/test_save_facts_batch_dedupe.py`, but that
file is owned by Plan 29-01's SK-01 rename. This plan focuses on the
remaining precheck files — `test_save_fact_precheck.py` and
`test_save_fact_precheck_failure.py` — which currently test the legacy
singular `_is_near_duplicate` shape and must be rewritten to assert the
C1 bulk-SELECT shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-toctou-silent-skip-enforcement/29-CONTEXT.md
@.planning/phases/29-toctou-silent-skip-enforcement/29-00-PLAN.md
@.planning/phases/29-toctou-silent-skip-enforcement/29-01-PLAN.md
@.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-04-SUMMARY.md
@services/memory/memory_service.py
@tests/unit/memory/test_save_fact_precheck.py
@tests/unit/memory/test_save_fact_precheck_failure.py
@tests/unit/memory/test_save_facts_batch_dedupe.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from `services/memory/memory_service.py`. -->

From services/memory/memory_service.py:516-564 (`_bulk_near_duplicate_check`):
  signature: async def _bulk_near_duplicate_check(self, conn, *, user_id, tenant_id, embeddings, threshold) -> set[int]
  SQL (CRITICAL — assertion target):
    SELECT (idx - 1) AS zero_idx
    FROM unnest($1::text[]) WITH ORDINALITY AS t(vec_txt, idx)
    WHERE EXISTS (
        SELECT 1 FROM long_term_facts
        WHERE user_id = $2
          AND tenant_id = $3
          AND embedding <=> vec_txt::vector < $4
    )
  GUC discipline: `SET LOCAL hnsw.iterative_scan = 'strict_order'` + `SET LOCAL hnsw.ef_search = <N>`
  Vec encoding: `embeddings` arg → `vec_literals = ["[v1,v2,...]", ...]` (pgvector text literals)
  Return: `set[int]` of zero-based indices flagged dup
  Empty-table branch: when no rows match the EXISTS clause for any candidate, `fetch` returns `[]` → `_bulk_near_duplicate_check` returns `set()` → all rows pass to executemany.

From services/memory/memory_service.py:435-478 (`_is_near_duplicate` — LEGACY, no longer on save_facts hot path):
  signature: async def _is_near_duplicate(self, conn, *, user_id, tenant_id, embedding, threshold) -> tuple[bool, float | None]
  SQL: SELECT ... ORDER BY embedding <=> $3::vector LIMIT 1
  Returns (False, None) on empty table.
  Still referenced as the "nearest_distance=None" branch shape — but production save_facts uses bulk-check; the legacy test files test this singular shape. v1.8 rewrites them to bulk shape.

From tests/unit/memory/test_save_facts_batch_dedupe.py (referenced — NOT modified here):
  Helpers `_make_fake_pool`, `_patch_embedder_batch`, `_patch_audit`, `_make_long` already exist and use the bulk-SELECT mock shape. Reuse the same helper patterns (do NOT import from that file — keep test files independent; copy-paste the helpers or factor into `tests/unit/memory/conftest.py` if it cleans the LOC budget).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Rewrite `test_save_fact_precheck.py` to bulk-SELECT shape</name>
  <files>tests/unit/memory/test_save_fact_precheck.py</files>
  <read_first>
    - tests/unit/memory/test_save_fact_precheck.py (entire file — record current LOC count BEFORE edit)
    - tests/unit/memory/test_save_facts_batch_dedupe.py (helper-pattern reference: `_make_fake_pool`, `_patch_embedder_batch`, `_make_long`, `_AcquireCtx`)
    - services/memory/memory_service.py:516-564 (`_bulk_near_duplicate_check` — assertion target)
    - services/memory/memory_service.py:566-717 (`save_facts` body — call-site context)
    - .planning/phases/29-toctou-silent-skip-enforcement/29-CONTEXT.md (D-TEST-INFRA-02 + LOC bound)
  </read_first>
  <action>
    Pre-edit: `wc -l tests/unit/memory/test_save_fact_precheck.py` → record as `LOC_BEFORE`.
    Drop all legacy assertions that mock the singular `_is_near_duplicate` fetchrow shape.
    Replace with bulk-SELECT mock shape per `_bulk_near_duplicate_check` contract:
      - `conn.fetch` (not `conn.fetchrow`) is the call to mock.
      - Return value must match shape `[{"zero_idx": int}, ...]` per the SQL `SELECT (idx - 1) AS zero_idx`.
      - Embedder mock returns `embed_batch` results (use `_patch_embedder_batch` pattern from `test_save_facts_batch_dedupe.py`).
    Cover at minimum the following cases (each as a distinct `@pytest.mark.asyncio` test function):
      (a) Bulk precheck returns `[]` → empty dup set → `executemany` receives all rows → `result.skipped_near_duplicates == 0`.
      (b) Bulk precheck returns `[{"zero_idx": 0}]` for a single-fact batch → `result.skipped_near_duplicates == 1`, `result.saved_count == 0`; under SK-01 enforcement (Plan 29-01 has shipped by Wave 2 ordering after the plan-review depends_on update) — `executemany.call_count == 0` (empty rows_to_insert short-circuit). Audit emit count == 1.
      (c) Bulk precheck returns multi-index dups for a multi-fact batch — verify the `vec_literals` argument shape passed as `$1`: each entry is a `str` starting with `[` and ending with `]`, containing comma-separated floats.
    Add explicit assertion on the SQL shape — extract the SQL string from `conn.fetch.call_args.args[0]` (or `kwargs["query"]`) and assert it contains BOTH `unnest($1::text[]) WITH ORDINALITY` AND `vec_txt::vector`. This is the C1 SQL-shape pin per req acceptance.
    Reuse helpers from `test_save_facts_batch_dedupe.py` by copy-paste (helpers are small; copy keeps test files independent and avoids cross-file import coupling — per v1.7 convention). Alternative: create `tests/unit/memory/conftest.py` housing `_make_fake_pool` / `_AcquireCtx` / `_make_long` / `_patch_embedder_batch` / `_patch_audit` / `_make_facts` as pytest fixtures or module-level helpers, then import. CHOOSE one route based on LOC budget — conftest is preferred IF it brings the per-file delta further under +150.
    Use `services.memory.memory_service.<dep>` consumer-path patching for embedder and audit-service mocks (carry-forward v1.3 D-mock).
    Post-edit: `wc -l tests/unit/memory/test_save_fact_precheck.py` → `LOC_AFTER`. Assert `(LOC_AFTER - LOC_BEFORE) <= 150`.
    NO production-code change. NO imports from `services/memory/memory_service.py` other than `LongTermMemory`, `AuditAction`, `ExtractedFact`.
    Wave Notes: this plan depends_on `[29-00, 29-01]` (updated per plan-review Q1 — Wave 2 now serializes 29-01 → 29-02 to eliminate the case (b) conditional-assertion logic). Case (b) assertion is unconditional SK-01 silent-skip shape: `result.saved_count == 0`, `result.skipped_near_duplicates == 1`, `conn.executemany.call_count == 0`. No need to read live `save_facts` Step 5 at execute time — Plan 29-01 GREEN has shipped by the time this plan starts.
    Commit message: `test(29-02): rewrite test_save_fact_precheck.py to C1 bulk-SELECT shape (TEST-INFRA-02)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/memory/test_save_fact_precheck.py -v 2>&1 | tee /tmp/29-02-task0.log; grep -E "passed|failed" /tmp/29-02-task0.log | tail -5; wc -l tests/unit/memory/test_save_fact_precheck.py</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/memory/test_save_fact_precheck.py` exists post-edit.
    - All tests in the file PASS under `uv run pytest`.
    - `grep -c "unnest" tests/unit/memory/test_save_fact_precheck.py` ≥ 1 (SQL shape assertion present).
    - `grep -c "WITH ORDINALITY" tests/unit/memory/test_save_fact_precheck.py` ≥ 1.
    - `grep -c "vec_txt::vector" tests/unit/memory/test_save_fact_precheck.py` ≥ 1.
    - `grep -c "zero_idx" tests/unit/memory/test_save_fact_precheck.py` ≥ 1 (mock return shape).
    - `grep -c "fetchrow" tests/unit/memory/test_save_fact_precheck.py` == 0 (legacy singular shape removed).
    - LOC delta (`LOC_AFTER - LOC_BEFORE`) ≤ 150. Capture both numbers in commit body.
    - No edits to any file under `services/` (run `git diff --name-only services/ | wc -l` → must be 0 for this commit).
    - mypy --strict + ruff clean on the edited test file.
  </acceptance_criteria>
  <done>
    Commit pushed; test file passes; per-file LOC delta documented in commit body.
  </done>
</task>

<task type="auto">
  <name>Task 1: Rewrite `test_save_fact_precheck_failure.py` — failure paths + `nearest_distance=None` branch</name>
  <files>tests/unit/memory/test_save_fact_precheck_failure.py</files>
  <read_first>
    - tests/unit/memory/test_save_fact_precheck_failure.py (entire file — record `LOC_BEFORE`)
    - tests/unit/memory/test_save_fact_precheck.py (post-Task-0 — helper pattern + bulk-SELECT mock shape)
    - services/memory/memory_service.py:516-564 (`_bulk_near_duplicate_check` — fail-OPEN contract on `asyncpg.PostgresError`)
    - services/memory/memory_service.py:566-717 (`save_facts` Step 3 try/except block — fail-open routing)
    - .planning/phases/29-toctou-silent-skip-enforcement/29-CONTEXT.md (acceptance: `nearest_distance=None` branch covered explicitly)
  </read_first>
  <action>
    Pre-edit: `wc -l tests/unit/memory/test_save_fact_precheck_failure.py` → `LOC_BEFORE`.
    Drop legacy `_is_near_duplicate` failure-path mocks (singular `fetchrow` raising patterns).
    Replace with `_bulk_near_duplicate_check` failure-path coverage using the same helpers as Task 0:
      (i)  `conn.fetch` raises `asyncpg.PostgresError("conn lost")` → fail-OPEN: warning logged, `dup_zero_idxs == set()`, `executemany` still runs with all rows, no raise.
      (ii) `conn.fetch` raises `asyncpg.InterfaceError("pool closed")` → same fail-OPEN behavior (covered by `except (asyncpg.PostgresError, asyncpg.InterfaceError)` in save_facts Step 3).
      (iii) `conn.fetch` returns `[]` for a 3-fact batch (empty table — no existing rows match EXISTS clause for any candidate) → explicit assertion: `dup_zero_idxs == set()`, `executemany` called with 3 rows, `result.skipped_near_duplicates == 0`. This is the `nearest_distance=None` / empty-table branch per req acceptance.
      (iv) Embed-batch failure cases (existing in file pre-edit) — keep or simplify; do NOT remove embed-failure coverage if it's the only place that pins it (cross-check with `test_save_facts_embed_batch_fallback.py` to avoid duplication; if duplication exists, prune from this file to stay under LOC bound).
    Add SQL-shape assertion in case (iii): extract the SQL string from `conn.fetch.call_args.args[0]` and assert `unnest($1::text[]) WITH ORDINALITY` present.
    Add explicit comment annotating case (iii) as "TEST-INFRA-02 `nearest_distance=None` branch coverage" (per req acceptance).
    Post-edit: `wc -l tests/unit/memory/test_save_fact_precheck_failure.py` → `LOC_AFTER`. Assert delta ≤ +150.
    NO production-code change.
    Commit message: `test(29-02): rewrite test_save_fact_precheck_failure.py to C1 bulk-SELECT + nearest_distance=None coverage`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/memory/test_save_fact_precheck_failure.py -v 2>&1 | tee /tmp/29-02-task1.log; grep -E "passed|failed" /tmp/29-02-task1.log | tail -5; wc -l tests/unit/memory/test_save_fact_precheck_failure.py</automated>
  </verify>
  <acceptance_criteria>
    - File passes `uv run pytest`.
    - `grep -c "asyncpg.PostgresError\|asyncpg.InterfaceError" tests/unit/memory/test_save_fact_precheck_failure.py` ≥ 2 (both narrow-exception types covered).
    - `grep -c "nearest_distance" tests/unit/memory/test_save_fact_precheck_failure.py` ≥ 1 (explicit branch annotation per req).
    - `grep -c "unnest" tests/unit/memory/test_save_fact_precheck_failure.py` ≥ 1.
    - `grep -c "fetchrow" tests/unit/memory/test_save_fact_precheck_failure.py` == 0 (legacy shape removed).
    - LOC delta (`LOC_AFTER - LOC_BEFORE`) ≤ 150. Document in commit body.
    - No edits to any file under `services/` for this commit (`git diff --name-only services/ | wc -l` == 0).
    - Combined: full `uv run pytest tests/unit/memory/ -v` passes (Task 0 + Task 1 + pre-existing tests + SK-01 from Plan 29-01).
    - mypy --strict + ruff clean.
  </acceptance_criteria>
  <done>
    Commit pushed; both rewritten files pass; cumulative LOC delta across both files documented in 29-02-SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Test-only plan; no production trust boundaries touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Repudiation / Test gap | legacy precheck tests pinned the wrong (singular) SQL shape → live bulk path was under-tested | mitigate | Rewrite tests to assert C1 bulk-SELECT shape (`unnest($1::text[]) WITH ORDINALITY` + `vec_txt::vector` cast); add explicit empty-table / `nearest_distance=None` coverage. |
| T-29-02-02 | Integrity (test infra) | LOC bloat masks real coverage drop in rewrite | mitigate | Per-file LOC delta ≤ +150 enforced via `wc -l` assertion in commit body and acceptance criteria. |
| T-29-02-03 | Tampering | unintended production-code edit slipped into a "test-only" plan | mitigate | Acceptance criterion: `git diff --name-only services/ | wc -l == 0` for both commits. |
| T-29-02-SC | Tampering / supply-chain | npm/pip/cargo installs | accept | No package installs introduced; test-only file edits. |

Severity: low — test-only plan, no production threat surface. ASVS L1.
</threat_model>

<verification>
- Both rewritten test files pass under `uv run pytest`.
- Full `uv run pytest tests/unit/memory/ -v` passes (no regression on SK-01-flipped tests in Plan 29-01).
- SQL-shape assertions present in both files (`unnest`, `WITH ORDINALITY`, `vec_txt::vector`).
- `nearest_distance=None` / empty-table branch explicitly covered with annotation.
- LOC delta per file ≤ +150.
- Zero `services/` edits (`git diff --name-only services/` empty across both commits).
- mypy --strict + ruff clean on both files.
- diff-cover ≥ 80% on touched test files.
- Combined coverage `--fail-under=70` global floor not regressed.
</verification>

<success_criteria>
1. `tests/unit/memory/test_save_fact_precheck.py` and `tests/unit/memory/test_save_fact_precheck_failure.py` rewritten in place to assert the C1 bulk-SELECT SQL shape.
2. `nearest_distance=None` / empty-table branch explicitly covered with a dedicated test function.
3. Per-file LOC delta ≤ +150 (documented in each commit body).
4. Zero production-code edits in either commit.
5. Full `uv run pytest tests/unit/memory/` passes post both tasks.
</success_criteria>

<output>
Create `.planning/phases/29-toctou-silent-skip-enforcement/29-02-SUMMARY.md` when done. Include:
- Per-file LOC delta numbers (`LOC_BEFORE` + `LOC_AFTER` for both files).
- Confirmation of zero `services/` edits.
- Confirmation that case (b) asserted the unconditional SK-01 silent-skip shape (plan-review Q1 — depends_on now [29-00, 29-01]).
- Whether a `tests/unit/memory/conftest.py` helper extraction was performed (if any).
</output>
