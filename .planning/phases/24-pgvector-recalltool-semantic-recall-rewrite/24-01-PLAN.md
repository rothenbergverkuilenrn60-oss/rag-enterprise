---
phase: 24-pgvector-recalltool-semantic-recall-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.py
  - services/agent/tools/recall.py
  - tests/unit/test_settings_recall_kill_switch.py
autonomous: true
requirements: [MEM-08, MEM-09]
tags: [settings, kill-switch, recall_tool_enabled, recall-tool-stub, classvars, base-tool, registry, conditional-import]
must_haves:
  truths:
    - "`settings.recall_tool_enabled: bool = True` field is importable from `config.settings.settings` with default True (mirrors Phase 23 `extractor_enabled` precedent at config/settings.py:302)"
    - "`services/agent/tools/recall.py` exists as a module-importable file defining `class RecallTool(BaseTool)` with three required ClassVars (`name`, `description`, `parameters_schema`) — stub body returns the empty-marker `ToolResult` until Plan 03 fills it"
    - "`RecallTool.name == 'recall_memory'` and `RecallTool.description` matches the ROADMAP D-C4 verbatim string"
    - "`RecallTool.parameters_schema == {'type':'object','properties':{'query':{'type':'string'}},'required':['query']}` — MEM-08 literal"
    - "Settings field default True; field is a plain `bool` (Pydantic V2 BaseSettings inference) — no env-var name override needed (default `RECALL_TOOL_ENABLED` mapping from Pydantic settings precedent)"
  artifacts:
    - path: "config/settings.py"
      provides: "New `recall_tool_enabled: bool = True` field appended directly after the Phase 23 extractor block (after line 302); preserves all existing fields verbatim"
      contains: ["recall_tool_enabled: bool = True", "# Recall tool (Phase 24, MEM-09 / D-B4)"]
    - path: "services/agent/tools/recall.py"
      provides: "RecallTool stub — three ClassVars + empty `run()` that returns the empty marker so Plan 03 can fill the body without re-declaring the class"
      contains: ["class RecallTool(BaseTool)", "name: ClassVar[str] = \"recall_memory\"", "parameters_schema: ClassVar[dict[str, Any]] = _RECALL_PARAMETERS_SCHEMA", "_RECALL_PARAMETERS_SCHEMA", "No matching facts found."]
    - path: "tests/unit/test_settings_recall_kill_switch.py"
      provides: "Settings field presence + default + type unit tests (full conditional-import behavior tested in Plan 04 after registration lands)"
      exports: ["test_recall_tool_enabled_default_true", "test_recall_tool_enabled_is_bool", "test_recall_tool_classvars_present"]
  key_links:
    - from: "config/settings.py::Settings"
      to: "services/agent/tools/__init__.py (Plan 04 consumer)"
      via: "module-level conditional import `if settings.recall_tool_enabled:`"
      pattern: "recall_tool_enabled"
    - from: "services/agent/tools/recall.py::RecallTool"
      to: "services.agent.tools.base.BaseTool"
      via: "subclass + ClassVar declarations"
      pattern: "class RecallTool\\(BaseTool\\)"
---

<objective>
Land the two **independent foundations** for Phase 24 in a single Wave-1 plan:
(1) the `settings.recall_tool_enabled: bool = True` kill-switch field (per D-B4); and
(2) a stub `services/agent/tools/recall.py` defining `RecallTool` class with the three required ClassVars (`name`, `description`, `parameters_schema`) but a placeholder `run()` body. The stub MUST NOT yet be registered via `@get_tool_registry().register` and MUST NOT yet be imported from `services/agent/tools/__init__.py` — that wiring lands in Plan 04 after Plan 03 fills the run body.

Purpose: unblocks Plan 03 (which writes the real `run()` body and adds the registration decorator) and Plan 04 (which adds the conditional import + allowlist edit) by getting the type surface in place. Splitting registration off the stub lets the test suite for the kill-switch register the tool fresh in Plan 04 without contaminating Plan 01's unit-test scope. The class-var enforcement in `BaseTool.__init_subclass__` (services/agent/tools/base.py:40-49) means the ClassVars MUST be present at class-body time — that requirement is satisfied here.

Output: one modified config file + one new stub module + one new unit test file (settings + ClassVar shape).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-CONTEXT.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-VALIDATION.md
@config/settings.py
@services/agent/tools/base.py
@services/agent/tools/web_search.py
@services/agent/extractor.py

<interfaces>
<!-- Symbols Plan 01 must align with. Extracted from working tree on 2026-05-16. -->

From config/settings.py (current Phase 23 extractor precedent at lines 296-302):
```
extractor_enabled:  bool                                = True
extractor_model:    str | None                          = None
extractor_provider: Literal["openai", "anthropic"] | None = None
```
- Append IMMEDIATELY after `extractor_provider` (line 302), BEFORE the next existing section header.
- Pydantic V2 BaseSettings — env-var override resolved by field name `RECALL_TOOL_ENABLED` automatically (no `Field(alias=...)` needed).

From services/agent/tools/base.py (BaseTool ABC enforces three ClassVars):
- `BaseTool.__init_subclass__` raises `TypeError` if any of `name`, `description`, `parameters_schema` missing at class body time (lines 40-49).
- `async def run(self, args: dict[str, Any], ctx: ToolContext) -> ToolResult` abstract signature (lines 53-58).
- `_build_error_result(self, exc, latency_ms) -> ToolResult` helper exists but Plan 03 will NOT use it (D-C3 forbids leaking exception text — see PATTERNS analog 3 anti-pattern callout).

From services/agent/tools/web_search.py (BaseTool subclass shape mirror; ~307 LOC reference):
- Module imports: `from __future__ import annotations`, `from typing import Any, ClassVar`, `from services.agent.tools.base import BaseTool`, `from services.agent.tools.registry import get_tool_registry`, `from utils.models import ToolContext, ToolResult`.
- `@get_tool_registry().register` decorator applied to the class — Plan 01 OMITS this decorator (registration deferred to Plan 03 when the real body lands).
- `_*_PARAMETERS_SCHEMA: dict[str, Any]` module-level constant convention.

From utils/models.py:
- `class ToolResult(BaseModel)` (lines 386-407, frozen) — fields: `content: str`, `metadata: dict[str, Any] = {}`, `is_error: bool = False`, `chunks: list[RetrievedChunk] = []`.
- `class ToolContext(BaseModel)` (lines 370-385, frozen) — `req: GenerationRequest`, `tf: dict`, `retriever: Any`, `llm: Any`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — settings field + RecallTool stub presence tests</name>
  <files>tests/unit/test_settings_recall_kill_switch.py</files>
  <read_first>
    - config/settings.py (Phase 23 `extractor_enabled` block at lines 296-302 — exact precedent shape)
    - services/agent/tools/base.py (BaseTool ABC + `__init_subclass__` ClassVar enforcement at lines 40-49)
    - services/agent/tools/web_search.py (lines 1-50 for the import + ClassVar declaration shape)
    - tests/unit/test_memory_save_fact.py (env-var setdefault + `reset_memory_singleton` autouse fixture pattern at lines 25-47)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 6 — settings; analog 12 — kill-switch tests; only the PRESENCE tests run in Plan 01, the importlib.reload registration tests are deferred to Plan 04)
  </read_first>
  <behavior>
    - Test 1 (`test_recall_tool_enabled_default_true`): `from config.settings import settings; assert settings.recall_tool_enabled is True`. Asserts the field exists and defaults True per D-B4.
    - Test 2 (`test_recall_tool_enabled_is_bool`): `from config.settings import Settings; field = Settings.model_fields["recall_tool_enabled"]; assert field.annotation is bool; assert field.default is True`. Asserts type and default via Pydantic V2 model_fields introspection (mirrors verifier/extractor field-level checks elsewhere in the suite).
    - Test 3 (`test_recall_tool_classvars_present`): `from services.agent.tools.recall import RecallTool; assert RecallTool.name == "recall_memory"; assert RecallTool.parameters_schema == {"type":"object","properties":{"query":{"type":"string"}},"required":["query"]}; assert "Recall durable facts" in RecallTool.description; assert "Skip when conversation pivots" in RecallTool.description`. Asserts the three required ClassVars match D-C4 verbatim + MEM-08 literal schema.
    - All three tests MUST fail on the unmodified tree with `AttributeError` (settings field missing) and `ImportError` / `ModuleNotFoundError` (recall.py missing). RED gate.
  </behavior>
  <action>
    Create `tests/unit/test_settings_recall_kill_switch.py`. Place env-var setdefault at module top BEFORE any `services.*` or `config.*` import (verbatim from `tests/unit/test_memory_save_fact.py:25-28`):

    - `os.environ.setdefault("APP_MODEL_DIR", "/tmp")`
    - `os.environ.setdefault("SECRET_KEY", "a-very-secure-key-for-testing-that-is-long-32c")`

    Use `from __future__ import annotations` at the top (repo convention — every existing .py in scope uses it per PATTERNS §Shared Patterns).

    The three tests use direct `from config.settings import settings`/`Settings` + `from services.agent.tools.recall import RecallTool` imports inside the test bodies (not at module top) so collection succeeds even when the module is still missing — pytest will report `ImportError` during the test run, which is the RED signal we want.

    Do NOT add `importlib.reload` or `get_tool_registry().list()` assertions in this file — those are Plan 04 tests (after registration wiring lands). Plan 01 only verifies the field+ClassVar presence layer; behavioral kill-switch tests are deferred per the analog-12 split.

    Do NOT add a `services.agent.tools.recall` import at the module top — keeping all `services.*` references inside test bodies prevents collection-time failure that would mask the RED→GREEN transition signal.

    Do NOT add `is_error=False` assertions — Plan 03 owns the run-body unit test surface; Plan 01 only proves the class object is importable with correct ClassVars.

    Run: `uv run pytest tests/unit/test_settings_recall_kill_switch.py --collect-only -q` — collection succeeds (no module-top import of recall). Then `uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q` — expect 3 failures (RED). Commit: `test(24-01): settings field + RecallTool ClassVar RED gates (MEM-08, MEM-09 D-B4)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings_recall_kill_switch.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_settings_recall_kill_switch.py` exists with the three test functions named above.
    - `uv run pytest tests/unit/test_settings_recall_kill_switch.py --collect-only -q` lists exactly 3 test items (collection succeeds — imports inside test bodies, not at module top).
    - `uv run pytest tests/unit/test_settings_recall_kill_switch.py::test_recall_tool_enabled_default_true -x` returns non-zero (RED — settings field not yet added).
    - `grep -E '^os.environ.setdefault' tests/unit/test_settings_recall_kill_switch.py | wc -l` ≥ 2 (env-var pattern present per shared-patterns rule).
    - `grep -E '^from __future__ import annotations' tests/unit/test_settings_recall_kill_switch.py` matches one line.
    - `grep -v '^#' tests/unit/test_settings_recall_kill_switch.py | grep -c 'importlib.reload'` equals 0 (registration tests deferred to Plan 04).
  </acceptance_criteria>
  <done>Three RED gates committed. Test file exists, collects cleanly, fails with `AttributeError`/`ImportError` confirming neither production change has landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — append `recall_tool_enabled` field + create RecallTool stub module</name>
  <files>config/settings.py, services/agent/tools/recall.py</files>
  <read_first>
    - config/settings.py (lines 296-310 — exact insertion point after the Phase 23 extractor block at line 302; identify the next existing section comment header so the new field lands BETWEEN them without disturbing surrounding fields)
    - services/agent/tools/base.py (lines 1-80 — `BaseTool` ABC + `__init_subclass__` enforcement; understand which three ClassVars are mandatory so the stub passes type-init)
    - services/agent/tools/web_search.py (lines 30-60 — module imports + module-level `_*_PARAMETERS_SCHEMA` constant pattern)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 3 verbatim skeleton — Plan 01 lands ONLY the class definition + ClassVars + stub `run()` body; Plan 03 will replace the stub body)
    - tests/unit/test_settings_recall_kill_switch.py (the three RED gates from Task 1)
  </read_first>
  <action>
    Edit `config/settings.py`:

    1. Find the existing Phase 23 extractor block (lines ~296-302). Immediately AFTER the `extractor_provider: Literal["openai", "anthropic"] | None = None` line and BEFORE the next existing section header (likely `# ═══ Swarm ═══` near line 306), append a new block:

       ```
       # Recall tool (Phase 24, MEM-09 / D-B4) ───────────────────────────────────
       # recall_tool_enabled=False kill-switch (default True — always-on).
       # Gates REGISTRATION (not allowlist membership) at
       # services/agent/tools/__init__.py: when False, the conditional
       # `if settings.recall_tool_enabled: from ... import RecallTool` skips the
       # decorator → registry lookup returns absent → planner-LLM never sees the
       # tool schema (D-B4 / Pattern 3). AGENT_TOOL_ALLOWLIST stays length 4
       # regardless of toggle.
       recall_tool_enabled: bool = True
       ```

       Verbatim per PATTERNS.md analog 6 §After block. Do NOT add `recall_model` / `recall_provider` siblings (RecallTool calls embedder, not LLM — anti-pattern §6 callout).

    Create `services/agent/tools/recall.py` (NEW — ~50 LOC stub):

    1. Header: `"""RecallTool — pgvector cosine-similarity recall via LongTermMemory (Phase 24, MEM-08).` ... brief docstring noting Plan 01 lands the stub; Plan 03 fills the run body; Plan 04 wires registration.

    2. `from __future__ import annotations` (mandatory per PATTERNS shared-patterns rule).

    3. Imports: `from typing import Any, ClassVar`; `from services.agent.tools.base import BaseTool`; `from utils.models import ToolContext, ToolResult`. DO NOT import `get_tool_registry` or apply `@get_tool_registry().register` decorator — registration is deferred to Plan 03 (which adds the decorator atop the class) AFTER it implements the real run body. DO NOT import `get_memory_service`, `httpx`, `asyncpg`, `loguru.logger` — those are Plan 03's surface.

    4. Module-level constants per PATTERNS analog 3:
       - `_RECALL_PARAMETERS_SCHEMA: dict[str, Any] = {"type": "object", "properties": {"query": {"type": "string"}}, "required": ["query"]}` — MEM-08 verbatim literal.
       - `_EMPTY_MARKER = "No matching facts found."` — D-C2 single source of truth (Plan 03 will reuse this constant).
       - DO NOT define `_RECALL_RUNTIME_ERRORS` or `_ERROR_MARKER` yet — Plan 03 owns those (they need `asyncpg` + `httpx` imports which we are deferring).

    5. Class definition WITHOUT the `@get_tool_registry().register` decorator:

       ```
       class RecallTool(BaseTool):
           """recall_memory — pgvector cosine recall over long_term_facts.

           Plan 01 stub: ClassVars only. Plan 03 fills the run body.
           Plan 04 wires registration via conditional import in
           services/agent/tools/__init__.py.
           """

           name: ClassVar[str] = "recall_memory"
           description: ClassVar[str] = (
               "Recall durable facts the agent has previously learned about this user. "
               "Call when the query references prior context, preferences, or recurring "
               "topics. Skip when conversation pivots to a new topic."
           )
           parameters_schema: ClassVar[dict[str, Any]] = _RECALL_PARAMETERS_SCHEMA

           async def run(
               self,
               args: dict[str, Any],
               ctx: ToolContext,
           ) -> ToolResult:
               # Plan 01 stub — Plan 03 replaces this body with the real recall
               # path. Returning the empty marker (NOT is_error) means the type
               # surface is exercised end-to-end without depending on Plan 02's
               # get_relevant_facts rewrite.
               return ToolResult(content=_EMPTY_MARKER, metadata={"stub": True})
       ```

    6. Description string MUST match D-C4 verbatim word-for-word. Use the multi-line `(... )` concatenation form to keep lines under ruff line-length while preserving the single logical string.

    Run: `uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q` — expect all 3 tests GREEN.
    Run: `uv run ruff check config/settings.py services/agent/tools/recall.py` — expect zero violations.
    Run: `uv run mypy --strict services/agent/tools/recall.py` — expect zero new violations (BaseTool ClassVars satisfy `__init_subclass__`; the stub body returns a valid `ToolResult`).

    Commit: `feat(24-01): add recall_tool_enabled settings field + RecallTool stub (MEM-08, MEM-09 D-B4)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q` exits 0 (3 tests GREEN).
    - `python -c "from config.settings import settings; assert settings.recall_tool_enabled is True"` exits 0.
    - `python -c "from services.agent.tools.recall import RecallTool; from services.agent.tools.base import BaseTool; assert issubclass(RecallTool, BaseTool)"` exits 0.
    - `grep -n 'recall_tool_enabled: bool = True' config/settings.py` matches exactly one line.
    - `grep -v '^#' config/settings.py | grep -c 'recall_model\|recall_provider'` equals 0 (no sibling fields per analog-6 anti-pattern).
    - `grep -n 'name: ClassVar\[str\] = "recall_memory"' services/agent/tools/recall.py` matches exactly one line.
    - `grep -n '_RECALL_PARAMETERS_SCHEMA: dict\[str, Any\]' services/agent/tools/recall.py` matches exactly one line.
    - `grep -v '^#' services/agent/tools/recall.py | grep -c '@get_tool_registry'` equals 0 (registration deferred to Plan 03).
    - `grep -v '^#' services/agent/tools/recall.py | grep -c '_RECALL_RUNTIME_ERRORS\|_ERROR_MARKER\|get_memory_service\|asyncpg\|httpx'` equals 0 (Plan 03 surface — out of scope here).
    - `wc -l services/agent/tools/recall.py | awk '{print $1}'` is between 30 and 80 (stub bounded).
    - `uv run ruff check config/settings.py services/agent/tools/recall.py` exits 0.
  </acceptance_criteria>
  <done>Settings field present + RecallTool stub class importable with three correct ClassVars + Task 1 RED gates flipped GREEN; ruff clean; no Plan 03 surface leaked; commit pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pydantic BaseSettings ↔ env vars | `recall_tool_enabled` resolved via standard Pydantic env-var binding; bool coercion happens inside Pydantic, not at the consumer |
| Module-import surface ↔ tool registry | RecallTool class object created at import time but NOT registered in Plan 01 — defers attack surface to Plan 04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-S1 | Spoofing | recall_tool_enabled env-var spoofed at deploy | accept | Operator owns deploy config; bool field default-True; same trust shape as `extractor_enabled` precedent (Phase 23 accepted) |
| T-24-01-T1 | Tampering | `parameters_schema` mutated post-import | mitigate | `ClassVar` + Pydantic V2 frozen ToolResult downstream; class-body literal is the single source of truth; tests assert exact dict equality (Task 1 Test 3) |
| T-24-01-I1 | Information Disclosure | description string leaks internal terms | accept | D-C4 wording locked verbatim from ROADMAP; reviewed at /office-hours 2026-05-15 |
| T-24-01-D1 | Denial | RecallTool stub `run()` always returns empty marker, masking absence of real implementation | accept | Plan 03 replaces the body; the stub is intentionally non-functional and the kill-switch (Plan 04) gates registration so it cannot reach the planner |
| T-24-01-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages introduced (RESEARCH §Package Legitimacy Audit "Not applicable") |
</threat_model>

<verification>
- Three Task 1 RED gates flip to GREEN under Task 2.
- `config/settings.py` ruff clean; field placement does not reflow surrounding code (line-count delta ≤ 12 added lines).
- `services/agent/tools/recall.py` ruff + mypy --strict clean.
- `BaseTool.__init_subclass__` enforcement passes (would raise `TypeError` at class-body time if any of three ClassVars missing).
- RecallTool is NOT yet in `get_tool_registry().list()` (registration deferred to Plan 03 + 04) — verifiable: `python -c "from services.agent.tools.registry import get_tool_registry; import services.agent.tools.recall; assert 'recall_memory' not in get_tool_registry().list()"` succeeds.

Run gate: `uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q && uv run ruff check config/settings.py services/agent/tools/recall.py`
</verification>

<success_criteria>
- ROADMAP SC-2 partially seeded: `AGENT_TOOL_ALLOWLIST` is NOT yet length 4 here (Plan 04 ships that edit), but the `RecallTool` class is class-loaded so Plan 03 can decorate it.
- MEM-08 structural prerequisites: class exists with three correct ClassVars; D-C4 description verbatim; MEM-08 `parameters_schema` literal verbatim.
- MEM-09 structural prerequisite: kill-switch field exists for Plan 04's conditional import to consume.
- All Plan 01 Wave-0 stubs (3 tests) GREEN.
</success_criteria>

<output>
Create `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-01-SUMMARY.md` when done.
</output>
