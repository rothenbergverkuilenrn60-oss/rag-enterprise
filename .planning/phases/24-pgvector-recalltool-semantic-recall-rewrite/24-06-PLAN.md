---
phase: 24-pgvector-recalltool-semantic-recall-rewrite
plan: 06
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - scripts/backfill_fact_embeddings.py
  - docs/memory-eviction.md
  - tests/unit/test_backfill_fact_embeddings.py
autonomous: true
requirements: [MEM-07]
tags: [backfill, cli, idempotent, chunked-commit, asyncpg, register-vector, embed-batch, dry-run, txn-rollback, cost-docs]
must_haves:
  truths:
    - "`scripts/backfill_fact_embeddings.py` is an async CLI invokable via `uv run python scripts/backfill_fact_embeddings.py [--dry-run] [--batch-size 100] [--resume-from-id <uuid>]` per PATTERNS analog 7 verbatim skeleton"
    - "Idempotent: `WHERE embedding IS NULL` cursor — re-running the script after a successful pass selects 0 rows and exits 0 with `total_done=0` (Test idempotent_second_run)"
    - "Resumable: `--resume-from-id <uuid>` appends `AND id > $1` to the SELECT cursor; the prior batch's already-embedded rows are not re-fetched"
    - "Chunked-commit: each batch of `--batch-size` (default 100) rows is wrapped in `async with conn.transaction():` — Pitfall 7 atomicity. UPDATE loop inside the txn either commits ALL or NONE."
    - "Whole-batch rollback on mid-batch failure → exit code 1; subsequent re-run skips the 0 covered rows from the failed batch via the IS NULL cursor (D-D3)"
    - "Pool reuse: instantiates `LongTermMemory()` then awaits `_get_pool()` — inherits Phase 23 `register_vector` codec init callback (Pitfall 1 mitigation). Does NOT construct a standalone asyncpg pool."
    - "Dry-run: `--dry-run` prints `Would embed N facts (~M tokens, ~$X)` per D-D4 cost formula, makes ZERO API calls, ZERO DB writes, exits 0"
    - "Cost docs in `docs/memory-eviction.md` companion section: cost formula by provider; --dry-run usage; rate-limit fallback; ~30-50 lines per D-D4"
  artifacts:
    - path: "scripts/backfill_fact_embeddings.py"
      provides: "Standalone async CLI per D-D1; argparse + asyncio.run shape mirrors scripts/ingest_batch.py; chunked-commit cursor loop per PATTERNS analog 7"
      contains: ["argparse", "asyncio.run", "WHERE embedding IS NULL", "async with conn.transaction()", "UPDATE long_term_facts SET embedding=$1::vector", "LongTermMemory()", "get_embedder().embed_batch"]
    - path: "docs/memory-eviction.md"
      provides: "NEW companion doc per D-D4 — cost formula by provider, backfill operational usage, failure modes, recurring-backfill stance; ~30-50 lines"
      contains: ["Cost Formula", "Backfill — Run Once", "Failure Modes", "uv run python scripts/backfill_fact_embeddings.py"]
    - path: "tests/unit/test_backfill_fact_embeddings.py"
      provides: "9 unit tests: dry-run-no-api-calls, dry-run-cost-format, happy-path-batch-commit, idempotent-second-run, batch-rollback-on-failure, embedder-failure-exit-1, resume-from-id, batch-size-respected, pool-reuse-from-long-term-memory"
      exports: ["test_dry_run_no_api_calls", "test_dry_run_cost_estimate_format", "test_happy_path_batch_commit", "test_idempotent_second_run", "test_batch_rollback_on_failure", "test_embedder_failure_exit_1", "test_resume_from_id_uses_cursor_filter", "test_batch_size_parameter_respected", "test_reuses_long_term_memory_pool"]
  key_links:
    - from: "scripts/backfill_fact_embeddings.py::backfill"
      to: "services.memory.memory_service.LongTermMemory._get_pool"
      via: "mem = LongTermMemory(); pool = await mem._get_pool()"
      pattern: "LongTermMemory\\(\\)\\._get_pool"
    - from: "scripts/backfill_fact_embeddings.py::backfill"
      to: "services.vectorizer.embedder.BatchedEmbedder.embed_batch"
      via: "vectors = await embedder.embed_batch(texts)"
      pattern: "embedder\\.embed_batch"
    - from: "scripts/backfill_fact_embeddings.py::backfill"
      to: "long_term_facts.embedding column"
      via: "UPDATE long_term_facts SET embedding=$1::vector WHERE id=$2 inside txn"
      pattern: "UPDATE long_term_facts SET embedding"
---

<objective>
Ship the MEM-07 backfill script + cost documentation. The script embeds pre-Phase-23 `long_term_facts` rows whose `embedding IS NULL`, idempotently and resumably, with whole-batch txn rollback on failure (D-D3). The docs/memory-eviction.md companion section (D-D4) documents the cost formula by provider, the dry-run usage, and the failure-mode operator playbook.

Purpose: closes the deploy story for Phase 24. Phase 23 made `save_fact` embed-on-write, so all NEW facts have embeddings; but rows created before Phase 23 deployment have `embedding IS NULL` and would not surface in semantic recall. This script is run ONCE post-deploy to backfill those legacy rows. After backfill completes, the script is archived (no recurring CronJob in v1.6 per D-D1).

Output: 1 new CLI script (~180 LOC) + 1 new docs file (~50 LOC) + 1 new unit test file (~250 LOC). No production-code changes outside the script + docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-CONTEXT.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-VALIDATION.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-02-SUMMARY.md
@scripts/ingest_batch.py
@services/memory/memory_service.py
@services/vectorizer/embedder.py

<interfaces>
<!-- Symbols Plan 06 must align with. Extracted from working tree on 2026-05-16. -->

From scripts/ingest_batch.py (sole existing async CLI — argparse + asyncio.run shape source):
- `#!/usr/bin/env python` shebang + module docstring + `from __future__ import annotations`.
- `import argparse, asyncio, sys, time` + `from pathlib import Path`.
- `sys.path.insert(0, str(Path(__file__).parent.parent))` — project-root injection.
- `from loguru import logger` + `from utils.logger import setup_logger`.
- `if __name__ == "__main__": asyncio.run(main())` — standard CLI invocation.
- argparse args: `--dir`, `--recursive`, `--dry-run`, `--concurrency` — Plan 06 swaps to `--dry-run`, `--batch-size`, `--resume-from-id` per D-D1.

From services/memory/memory_service.py (Phase 23 + Plan 02 result):
- `LongTermMemory()` constructor (line ~123) — fresh instance per script invocation; per-instance pool isolation.
- `_get_pool()` at line ~146-160 — returns `asyncpg.Pool` with `init=_init_conn` registering `register_vector` codec (Pitfall 1 mitigation — script REUSES this rather than constructing standalone pool per PATTERNS §Don't Hand-Roll table).

From services/vectorizer/embedder.py:
- `get_embedder()` at line ~239 returns `BatchedEmbedder`.
- `BatchedEmbedder.embed_batch(texts: list[str]) -> list[list[float]]` at line ~142 — OpenAI 2048/call; HF/Ollama CPU/GPU bound; tenacity already wrapped on OpenAI 5xx/429 (embedder.py:84 `stop_after_attempt(3)`).
- Concrete embedder failure surfaces for narrow-exception catch in backfill: `RuntimeError` (Ollama re-raise) + `OSError` (HF torch); `httpx.HTTPError` handled inside tenacity (not re-raised).

From PATTERNS analog 7 §Verbatim skeleton:
- Full script body provided (~180 LOC). The skeleton uses an outer try/except `(RuntimeError, OSError)` around `embed_batch`, then an inner try/except around the UPDATE-loop txn. Plan 06 follows this two-block structure; the outer block tightens the exception list to exclude the bare `Exception` shown in RESEARCH §Pattern 4 line 399 (RESEARCH explicitly flags this for the planner to narrow — Plan 06 narrows to `(RuntimeError, OSError)`).
- The UPDATE statement: `UPDATE long_term_facts SET embedding=$1::vector WHERE id=$2` — uses `$1::vector` cast (pgvector codec registered via pool init).
- Tracker: `total_done` count + per-batch loguru info line.

From PATTERNS analog 11 (test file):
- 9 unit tests using fake-pool harness (same `_AcquireCtx` + `_make_fake_pool` pattern from test_memory_save_fact, extended for `fetch` + `fetchrow`).
- Loguru → caplog propagation requires `conftest.py` configuration; alternative is `logger.add(capture_handler)` per-test.

For Test 4 (`test_idempotent_second_run`) and Test 5 (`test_batch_rollback_on_failure`), the fake-pool harness must simulate state changes across two passes (first pass populates a "shadow" row state; second pass returns 0 rows from the cursor). Simplest: maintain a list of remaining row IDs in test scope and have `fetch.side_effect` pop from it.

From docs/ (verified — no existing memory-eviction.md):
- docs/ directory has `DOCKER_DEPLOY.md`, `agent-architecture.md`, `v1.4-design.md`, `demo.cast`. NO `memory-eviction.md` — Plan 06 CREATES this file new.
- Layout: ops doc tone per `docs/DOCKER_DEPLOY.md` (short paragraphs + `bash` code blocks + tables + numbered procedures).

From PATTERNS analog 8 §Required sections:
- `## Cost Formula (per provider)` table (4 rows: OpenAI -large, OpenAI -small, HuggingFace, Ollama).
- `## Backfill — Run Once` code block with 3 invocation examples.
- `## Failure Modes` table (3 rows: embedder-failure, txn-failure, long-runtime-no-progress).
- `## Recurring Backfill` paragraph stating not-needed-in-steady-state.

Total ~30-50 lines per D-D4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 9 unit tests for backfill CLI behavior (mock-at-consumer-path, fake pool)</name>
  <files>tests/unit/test_backfill_fact_embeddings.py</files>
  <read_first>
    - scripts/ingest_batch.py (argparse + asyncio.run shape — see line 11-30 + 116; argparse args definition pattern)
    - services/memory/memory_service.py (lines 146-160 — `_get_pool` register_vector callback; line 123 — `__init__`)
    - services/vectorizer/embedder.py (lines 142-150 — BatchedEmbedder.embed_batch signature)
    - tests/unit/test_memory_save_fact.py (Phase 23 sibling — fake-pool harness lines 25-90; reset_memory_singleton autouse pattern)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 11 §Required tests table + §Swap list vs test_memory_save_fact; analog 11 anti-pattern callouts)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md (Pitfall 7 — backfill atomicity; §Validation Architecture rows MEM-07)
  </read_first>
  <behavior>
    - Test 1 (`test_dry_run_no_api_calls`): patch `scripts.backfill_fact_embeddings.get_embedder` to a MagicMock; patch `scripts.backfill_fact_embeddings.LongTermMemory._get_pool` via instance attribute set to a fake pool whose `fetchrow` returns `{"n": 42}` and `fetch` is unused. Call `await backfill(batch_size=100, dry_run=True, resume_from_id=None)`. Assert: return value `== 0`; embedder MagicMock `embed_batch.await_count == 0` AND `embed_one.await_count == 0`; fake pool's `execute.await_count == 0`; fake pool's `fetch.await_count == 0` (only fetchrow for count). Loguru log contains substring `"Would embed 42 facts"`.
    - Test 2 (`test_dry_run_cost_estimate_format`): same as Test 1; capture loguru logs (use `caplog` with loguru's `logger.add(caplog.handler, level="INFO")` setup OR a custom captured-log fixture); assert log output contains all of: `"Would embed"`, `"42"`, `"tokens"`, `"$"`. Format-substring matches (NOT exact text — analog 11 anti-pattern callout).
    - Test 3 (`test_happy_path_batch_commit`): seed fake pool `fetch` to return 100 rows `[{"id": uuid, "fact": f"f{i}"} for i in range(100)]` on first call, then `[]` on second call (cursor exhausted). Embedder `embed_batch.return_value = [[0.1]*1024] * 100`. Spy `conn.transaction` enter count = 1 (single batch txn). Spy `conn.execute.await_count == 100` (one UPDATE per row, inside the txn). `backfill()` returns 0; `total_done == 100`.
    - Test 4 (`test_idempotent_second_run`): same setup as Test 3 but after the first run, mutate the fake pool's row state so `fetchrow` for `COUNT(*)` returns `{"n": 0}` and `fetch` returns `[]`. Call `backfill()` a second time; assert return 0, no new embed_batch calls, no UPDATE executes.
    - Test 5 (`test_batch_rollback_on_failure`): seed 100 rows. Embedder succeeds. Stub `conn.execute` to raise `asyncpg.PostgresError` on the 47th call. Assert: `backfill()` returns 1; the txn enter count is 1 (txn rolled back on exit-with-exception per `async with` semantics); the fake-pool's "would-have-been-persisted" UPDATE count is bounded — what the test asserts is that the failure SHORT-CIRCUITS the outer batch loop AND returns 1. Loguru log contains `"txn UPDATE failed, rollback complete"`.
    - Test 6 (`test_embedder_failure_exit_1`): seed 100 rows; `embed_batch.side_effect = RuntimeError("ollama down")`. Assert: return 1; `conn.execute.await_count == 0` (no UPDATE attempted). Loguru log contains `"embedder failed"`.
    - Test 7 (`test_resume_from_id_uses_cursor_filter`): call `backfill(batch_size=10, dry_run=True, resume_from_id="abc-uuid")`. Spy `pool.fetchrow.call_args` (the COUNT query) — should NOT include the resume filter (count remaining ignores filter). For the cursor SELECT (in a NON-dry-run setup), spy `conn.fetch.call_args.args` and assert the SQL contains `"AND id > $1"` AND positional arg `"abc-uuid"`. Use two sub-tests OR a single combined test with `dry_run=False` happy-path setup.
    - Test 8 (`test_batch_size_parameter_respected`): call `backfill(batch_size=50, dry_run=False, resume_from_id=None)` with happy-path seed. Spy `conn.fetch.call_args.args[0]` (the LIMIT positional in the SELECT) — assert equals 50.
    - Test 9 (`test_reuses_long_term_memory_pool`): assert `scripts.backfill_fact_embeddings.LongTermMemory` is the imported class (NOT a fresh `asyncpg.create_pool(...)` call elsewhere in the script). Pitfall 1 mitigation: script must reuse the codec-registering pool. Verify via `grep`-like: `from services.memory.memory_service import LongTermMemory` import is present; `asyncpg.create_pool` is NOT imported or called inside the script body.

    All 9 tests MUST fail on the unmodified tree because the script does not exist — `ImportError` at test body. RED gate.
  </behavior>
  <action>
    Create `tests/unit/test_backfill_fact_embeddings.py`. Env-var setdefault top + `from __future__ import annotations` (shared patterns).

    Imports inside test bodies (deferred to avoid collection failure when the script does not yet exist): `import scripts.backfill_fact_embeddings as bf_mod` + `from scripts.backfill_fact_embeddings import backfill` — defer to test body, OR wrap the module-top import in a `try/except ImportError: bf_mod = None` block + `@pytest.mark.skipif(bf_mod is None, reason="...")` skip decorators on each test for the RED phase. Recommended approach for the RED gate: put imports inside test bodies so tests collection succeeds and each test fails individually with `ModuleNotFoundError` at body-execution time.

    Use the fake-pool harness from PATTERNS analog 9 §Reusable harness, extended for Plan 06 per analog 11 §Swap list:
    - `_make_fake_pool(execute_mock, fetch_mock, fetchrow_mock)` — three AsyncMocks.
    - `conn.transaction = MagicMock(return_value=_AcquireCtx(conn))` for Pitfall 7 atomicity check.
    - `pool.acquire = MagicMock(return_value=_AcquireCtx(conn))`.
    - `pool.fetchrow = fetchrow_mock` — for the `_count_remaining` cheap pre-flight count.

    For Tests 4 (idempotent) and 5 (rollback), use stateful side_effects:
    - Test 4: `fetchrow.side_effect = [{"n": 100}, {"n": 0}]` (first call returns 100, second 0); `fetch.side_effect = [<100 rows>, [], []]` (cursor exhausts after first batch).
    - Test 5: track `execute` call number; `def execute_side_effect(*a, **k): nonlocal call_n; call_n += 1; if call_n == 47: raise asyncpg.PostgresError("simulated"); return None`.

    Patch the embedder via `monkeypatch.setattr("scripts.backfill_fact_embeddings.get_embedder", lambda: fake_embedder)` — consumer-path discipline. Plan 06's script imports `get_embedder` at module top per PATTERNS analog 7; the patch target matches that import path.

    For the `LongTermMemory` pool reuse, patch the instance attribute: `monkeypatch.setattr(bf_mod.LongTermMemory, "_get_pool", AsyncMock(return_value=fake_pool))` — patches the class method; the script instantiates fresh each run.

    Run: `uv run pytest tests/unit/test_backfill_fact_embeddings.py --collect-only -q` — expect 9 items collected (deferred imports allow collection success). Run with `-x -q` — expect 9 failures (RED — script does not exist). Commit: `test(24-06): RED gates for backfill_fact_embeddings CLI (MEM-07)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_backfill_fact_embeddings.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_backfill_fact_embeddings.py` exists with 9 test functions.
    - `uv run pytest tests/unit/test_backfill_fact_embeddings.py --collect-only -q` lists ≥ 9 items (deferred imports — collection succeeds).
    - `uv run pytest tests/unit/test_backfill_fact_embeddings.py::test_dry_run_no_api_calls -x` returns non-zero (RED — script does not exist).
    - `grep -c 'monkeypatch.setattr("scripts.backfill_fact_embeddings' tests/unit/test_backfill_fact_embeddings.py` ≥ 1 (consumer-path discipline).
    - `grep -c 'conn.transaction' tests/unit/test_backfill_fact_embeddings.py` ≥ 1 (Pitfall 7 atomicity check landed).
    - `grep -E '^from __future__ import annotations' tests/unit/test_backfill_fact_embeddings.py` matches one line.
    - `grep -c 'asyncpg.create_pool' tests/unit/test_backfill_fact_embeddings.py` equals 0 (Pitfall 1 — test verifies the script DOES NOT call this; the assertion is in Test 9 via import-side-effect inspection).
  </acceptance_criteria>
  <done>9 RED gates committed. Tests collect cleanly; failures are `ModuleNotFoundError` confirming script absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — create backfill CLI script + docs/memory-eviction.md companion section</name>
  <files>scripts/backfill_fact_embeddings.py, docs/memory-eviction.md</files>
  <read_first>
    - scripts/ingest_batch.py (full file — argparse + asyncio.run shape source verbatim)
    - services/memory/memory_service.py (lines 146-160 — `_get_pool` callback; Phase 23 register_vector init)
    - services/vectorizer/embedder.py (lines 142-150 — embed_batch signature + tenacity wrap precedent)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 7 §Verbatim skeleton — drop-in ~180 LOC script body; analog 7 §Swap list; analog 7 §Anti-pattern callouts; analog 8 §Required sections — drop-in docs ~30-50 LOC)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md (Code Examples §backfill skeleton; Pitfalls 1 + 7)
    - tests/unit/test_backfill_fact_embeddings.py (the 9 RED gates from Task 1)
  </read_first>
  <action>
    Create `scripts/backfill_fact_embeddings.py` per PATTERNS analog 7 §Verbatim skeleton. Concrete decisions:

    1. Shebang + module docstring per analog 7 first 15 lines. Module docstring references Phase 24 / MEM-07, the D-D1/D-D3/D-D4 decisions, and the usage examples.

    2. Imports:
       - `from __future__ import annotations`
       - `import argparse, asyncio, sys`
       - `from pathlib import Path`
       - `sys.path.insert(0, str(Path(__file__).parent.parent))` — project-root injection (mirror ingest_batch.py:18-19)
       - `from loguru import logger`
       - `from services.memory.memory_service import LongTermMemory` (Pitfall 1 — pool reuse)
       - `from services.vectorizer.embedder import get_embedder`
       - `from utils.logger import setup_logger`
       - DO NOT import `asyncpg` directly (Pitfall 1 anti-pattern callout — script must NOT construct its own pool); the script uses asyncpg INSIDE `LongTermMemory._get_pool` which already has the codec init.

    3. `async def _count_remaining(pool) -> int:` helper per analog 7 §Verbatim skeleton — cheap pre-flight COUNT for `--dry-run` + final progress report.

    4. `async def backfill(batch_size: int, dry_run: bool, resume_from_id: str | None) -> int:` — full body per analog 7 verbatim. Returns 0 on success, 1 on failure.

    5. Critical contract checkpoints (each maps to a Task 1 RED gate):
       - `mem = LongTermMemory(); pool = await mem._get_pool()` — pool reuse → Test 9.
       - `if dry_run: ... return 0` BEFORE the cursor loop → Test 1, Test 2.
       - Cursor SELECT inside `while True:` loop; resume filter applied via conditional SQL branch (NOT string interpolation — use parameter binding `args.append(resume_from_id)`) → Test 7.
       - `LIMIT $N` parameter respects `batch_size` → Test 8.
       - `texts = [r["fact"] for r in rows]` then `vectors = await embedder.embed_batch(texts)` inside `try: ... except (RuntimeError, OSError) as exc: ... return 1` → Test 6. Narrow exception list per Phase 23 save_fact precedent (memory_service.py:305) — httpx handled by provider tenacity.
       - UPDATE loop wrapped in `async with pool.acquire() as conn: async with conn.transaction():` → Test 3 + Test 5 (Pitfall 7).
       - UPDATE statement: `UPDATE long_term_facts SET embedding=$1::vector WHERE id=$2` (single-row UPDATE per row in the loop; v1.7+ batch optimization is deferred per STATE.md TODO).
       - Whole-batch rollback on mid-batch txn failure: catch `Exception` (with `# noqa: BLE001` per analog 7 line 707 — txn rollback contract requires catching ALL exceptions because asyncpg can raise from the `__aexit__` of the transaction context as well as from inner statements; narrow this AFTER landing if mypy/ruff complain), log + return 1 → Test 5.
       - Loguru log line `f"backfilled batch: count={len(rows)} total={total_done} remaining≈{remaining - total_done}"` per analog 7.

    6. `def main() -> None:` per analog 7 — `argparse` with three flags (`--dry-run`, `--batch-size` int default 100, `--resume-from-id` str default None); `asyncio.run(backfill(...))`; `sys.exit(exit_code)`.

    7. `if __name__ == "__main__": main()` at the bottom.

    8. Anti-patterns to AVOID (analog 7 callouts):
       - DO NOT construct standalone `asyncpg.create_pool(...)` (Pitfall 1).
       - DO NOT drop the txn wrap around the UPDATE loop (Pitfall 7).
       - DO NOT add `--qps` flag (D-D2 deferred to v1.7+).
       - DO NOT add a checkpoint file (D-D1).
       - DO NOT raise `MemoryFactWriteError` (operator-run; exit code is the signal).
       - DO NOT call `save_fact` per row (would INSERT; we need UPDATE).
       - DO NOT use `conda run -n torch_env` in docs (use `uv run python` per CLAUDE.md §1.6).

    Create `docs/memory-eviction.md` per PATTERNS analog 8 §Required sections. Concrete structure:

    ```
    # Memory Eviction & Backfill (Phase 24)

    This document covers operational concerns for the v1.6 `long_term_facts`
    pgvector store. Phase 24 ships the backfill section; Phase 25 will extend
    with the eviction section.

    ## Cost Formula (per provider)

    | Provider                              | Per 1M facts          | Per fact (~40 tokens) |
    | --- | --- | --- |
    | OpenAI `text-embedding-3-large` (1024-dim) | $0.13/1M tokens × 40M = ~$5.20 | ~$5.2 µ |
    | OpenAI `text-embedding-3-small` (1024-dim) | $0.02/1M tokens × 40M = ~$0.80 | ~$0.8 µ |
    | HuggingFace local (BGE-M3, CPU/GPU)        | $0 marginal | $0 |
    | Ollama local (any)                          | $0 marginal | $0 |

    ## Backfill — Run Once

    \`\`\`bash
    # 1. Dry-run cost estimate (no API calls)
    uv run python scripts/backfill_fact_embeddings.py --dry-run

    # 2. Real run (100 rows per txn; tenacity 3-retry on 5xx)
    uv run python scripts/backfill_fact_embeddings.py --batch-size 100

    # 3. Resume after a partial failure (UUID from prior log line)
    uv run python scripts/backfill_fact_embeddings.py --resume-from-id <uuid>
    \`\`\`

    ## Failure Modes

    | Symptom                                                | Cause                          | Recovery                                                  |
    | --- | --- | --- |
    | Exit 1 with `embedder failed`                            | Provider transient (429 / 5xx) | Re-run; `WHERE embedding IS NULL` cursor skips done rows  |
    | Exit 1 with `txn UPDATE failed, rollback complete`       | pgvector codec or dim mismatch | Verify `settings.embedding_dim` matches column; re-run    |
    | Long runtime + no progress                               | Rate limit not honored         | Wait, or split batches (`--batch-size 50`)                |

    ## Recurring Backfill

    Not needed in steady state. Phase 23 `save_fact` embeds-on-write — only
    pre-Phase-24 rows have `embedding IS NULL`. If a future schema migration
    adds new fact rows without embeddings, re-run the same script.
    ```

    Bound total docs at 30-50 lines per D-D4. DO NOT add CronJob / systemd / k8s YAML (D-D1 / analog 8 anti-pattern).
    DO NOT document the eviction section here (analog 8 anti-pattern — Phase 25 owns that).

    Run: `uv run pytest tests/unit/test_backfill_fact_embeddings.py -x -q` — expect all 9 tests GREEN.
    Run: `uv run pytest tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q` — Plan 02 + 03 regression check.
    Run: `uv run ruff check scripts/backfill_fact_embeddings.py` — zero violations (or minimal: the `# noqa: BLE001` around the txn-rollback catch is the one expected `noqa`).
    Run: `uv run mypy --strict scripts/backfill_fact_embeddings.py` — zero new violations (or pre-existing baseline only).

    Commit: `feat(24-06): MEM-07 backfill_fact_embeddings.py + docs/memory-eviction.md cost section`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_backfill_fact_embeddings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_backfill_fact_embeddings.py -x -q` exits 0 (9 tests GREEN).
    - `uv run pytest tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q` exits 0 (Plans 02 + 03 still GREEN).
    - `scripts/backfill_fact_embeddings.py` exists; `wc -l` between 100 and 250 (analog 7 target ~180).
    - `docs/memory-eviction.md` exists; `wc -l` between 25 and 80 (D-D4 target 30-50).
    - `grep -n 'WHERE embedding IS NULL' scripts/backfill_fact_embeddings.py | wc -l` ≥ 1.
    - `grep -n 'async with conn.transaction()' scripts/backfill_fact_embeddings.py` matches exactly one line (Pitfall 7).
    - `grep -n 'UPDATE long_term_facts SET embedding=\$1::vector' scripts/backfill_fact_embeddings.py` matches exactly one line.
    - `grep -n 'LongTermMemory()' scripts/backfill_fact_embeddings.py` matches exactly one line (Pitfall 1 pool reuse).
    - `grep -v '^#' scripts/backfill_fact_embeddings.py | grep -c 'asyncpg.create_pool'` equals 0 (Pitfall 1 — no standalone pool).
    - `grep -v '^#' scripts/backfill_fact_embeddings.py | grep -c '\\-\\-qps'` equals 0 (D-D2 — deferred).
    - `grep -v '^#' scripts/backfill_fact_embeddings.py | grep -c 'MemoryFactWriteError'` equals 0 (operator-run; exit code is the signal).
    - `grep -v '^#' scripts/backfill_fact_embeddings.py | grep -c 'save_fact'` equals 0 (UPDATE not INSERT).
    - `grep -n 'uv run python' docs/memory-eviction.md | wc -l` ≥ 3 (3 invocation examples).
    - `grep -v '^#' docs/memory-eviction.md | grep -c 'CronJob\|systemd\|cron'` equals 0 (analog 8 anti-pattern — D-D1 deferred).
    - `grep -v '^#' docs/memory-eviction.md | grep -c 'conda run\|conda env'` equals 0 (CLAUDE.md §1.6).
    - `uv run ruff check scripts/backfill_fact_embeddings.py` exits 0.
  </acceptance_criteria>
  <done>MEM-07 GREEN. Backfill CLI idempotent + resumable + rollback-atomic; cost docs published; 9 RED gates flip GREEN; Plans 02 + 03 regression preserved; no anti-patterns; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator ↔ CLI | `--resume-from-id` accepts arbitrary string; bound via asyncpg `$1` parameter (no SQL injection surface) |
| Script ↔ embedder | `BatchedEmbedder.embed_batch(texts)` — texts come from `long_term_facts.fact` column (stored facts written via Phase 23 save_fact); no user input at backfill time |
| asyncpg pool ↔ PostgreSQL | Pool reused from `LongTermMemory._get_pool()` — inherits `register_vector` codec from Phase 23 |
| Docs file ↔ git tree | `docs/memory-eviction.md` published as repo artifact; no secrets; cost figures are public OpenAI pricing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06-T1 | Tampering | `--resume-from-id` SQL injection | mitigate | Bound as `$1` parameter — parameterized; never interpolated into SQL string |
| T-24-06-T2 | Tampering | `--batch-size` triggers OOM via huge value | accept | Operator-controlled flag; default 100; LIMIT bounded by OpenAI 2048/batch max anyway |
| T-24-06-I1 | Information Disclosure | Loguru log lines include fact content | mitigate | Log lines include `count=`, `total=`, `remaining≈` — NEVER `texts` or `vec` values; assert in Test 2 + 3 |
| T-24-06-I2 | Information Disclosure | Standalone pool leaks DSN to logs | n/a | Pool reused from `LongTermMemory` — no new DSN log surface |
| T-24-06-D1 | Denial | Embedder 429 exhausts quota | accept | Tenacity already wraps `embed_batch` (embedder.py:84 stop_after_attempt(3)); operator can pause via Ctrl+C and resume via `--resume-from-id` |
| T-24-06-D2 | Denial | Mid-batch failure leaves DB in inconsistent state | mitigate | Whole-batch txn rollback (Pitfall 7 + Test 5); idempotent re-run via WHERE IS NULL cursor |
| T-24-06-R1 | Repudiation | No audit log of who ran backfill or when | accept | Operator-run; outside of v1.0 Phase 2 audit-log scope; ops journal owns this |
| T-24-06-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages (PATTERNS analog 7 — every import already in tree) |
</threat_model>

<verification>
- 9 Task 1 RED gates flip GREEN under Task 2.
- Script + docs files exist; bounded LOC.
- Plans 02 + 03 unit suites remain GREEN.
- No standalone asyncpg.create_pool (Pitfall 1); no missing txn wrap (Pitfall 7).
- `docs/memory-eviction.md` parses as Markdown; 3 invocation examples present; no CronJob YAML (D-D1).

Run gate: `uv run pytest tests/unit/test_backfill_fact_embeddings.py tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q && uv run ruff check scripts/backfill_fact_embeddings.py`
</verification>

<success_criteria>
- ROADMAP SC-4 complete: backfill idempotent (second run = 0 API calls); resumable; chunked 100/txn.
- MEM-07 requirement complete: script + cost docs + idempotency tested.
- Plan 06 Wave-0 stubs (9 tests) GREEN.
- D-D1/2/3/4 all honored: standalone CLI / embed_batch + no --qps / whole-batch rollback / cost docs in companion section.
</success_criteria>

<output>
Create `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-06-SUMMARY.md` when done.
</output>
