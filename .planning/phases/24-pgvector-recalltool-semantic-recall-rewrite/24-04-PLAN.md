---
phase: 24-pgvector-recalltool-semantic-recall-rewrite
plan: 04
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-03
files_modified:
  - services/agent/tools/__init__.py
  - services/pipeline.py
  - tests/unit/test_settings_recall_kill_switch.py
  - tests/integration/test_recall_tool_planner_pick.py
autonomous: true
requirements: [MEM-09]
tags: [agent-tool-allowlist, conditional-registration, kill-switch, planner-pick, importlib-reload, integration-test, pgvector-marker]
must_haves:
  truths:
    - "`services/agent/tools/__init__.py` adds `from config.settings import settings` at the top and wraps `from services.agent.tools.recall import RecallTool` in `if settings.recall_tool_enabled:` (per D-B4 Pattern 3)"
    - "`AGENT_TOOL_ALLOWLIST` at `services/pipeline.py:744` grows from 3 entries to 4 by appending `'recall_memory'` as the 4th element (insertion order preserved); list stays length 4 REGARDLESS of `settings.recall_tool_enabled` toggle (D-B4 wording)"
    - "When `settings.recall_tool_enabled=True`: `get_tool_registry().list()` contains `'recall_memory'`; `registry.schemas_for('anthropic', names=AGENT_TOOL_ALLOWLIST)` returns 4 schemas"
    - "When `settings.recall_tool_enabled=False` (via reload): `get_tool_registry().list()` does NOT contain `'recall_memory'`; `registry.get('recall_memory')` raises `KeyError`; `registry.schemas_for(..., names=AGENT_TOOL_ALLOWLIST)` returns 3 schemas (filtered by registered names per registry.py:78)"
    - "Integration test asserts the planner LLM picks `recall_memory` for a preference-referencing query AND skips it for an unrelated query (real planner with `AGENT_TOOL_ALLOWLIST` including `recall_memory`)"
  artifacts:
    - path: "services/agent/tools/__init__.py"
      provides: "Conditional import of RecallTool; preserves existing RetrieveTool/RefinedRetrieveTool/WebSearchTool unconditional imports; `__all__` UNCHANGED (RecallTool deliberately NOT exported — consumers go through registry per analog-4 swap-list row 3)"
      contains: ["from config.settings import settings", "if settings.recall_tool_enabled:", "from services.agent.tools.recall import RecallTool"]
    - path: "services/pipeline.py"
      provides: "`AGENT_TOOL_ALLOWLIST` literal grows 3→4 at line 744 with `'recall_memory'` appended; comment updated to reference Phase 24 / MEM-09 + the D-B4 registration-not-allowlist gating note"
      contains: ["recall_memory", "AGENT_TOOL_ALLOWLIST"]
    - path: "tests/unit/test_settings_recall_kill_switch.py"
      provides: "EXTENDED with 5 importlib.reload-based kill-switch tests added to the file created in Plan 01 (which only had the 3 presence tests); single test file now covers both presence + behavioral kill-switch"
      exports: ["test_enabled_registers_recall_memory", "test_disabled_skips_recall_memory", "test_disabled_registry_lookup_raises_keyerror", "test_allowlist_length_constant_regardless_of_toggle", "test_schemas_for_omits_when_disabled"]
    - path: "tests/integration/test_recall_tool_planner_pick.py"
      provides: "Planner-picks integration tests (real PG + real planner OR mocked planner with deterministic prompt) — 3 tests covering preference-pick, unrelated-skip, seeded-fact-recall"
      exports: ["test_planner_picks_recall_for_preference_query", "test_planner_skips_recall_for_unrelated_query", "test_recall_tool_returns_seeded_fact"]
  key_links:
    - from: "services/agent/tools/__init__.py"
      to: "services.agent.tools.recall.RecallTool"
      via: "conditional import under `if settings.recall_tool_enabled:`"
      pattern: "if settings\\.recall_tool_enabled:"
    - from: "services/pipeline.py::AGENT_TOOL_ALLOWLIST"
      to: "services.agent.tools.registry.ToolRegistry.schemas_for"
      via: "names=AGENT_TOOL_ALLOWLIST filter in planner schema-building path at pipeline.py:984, 1075, 1321"
      pattern: "names=AGENT_TOOL_ALLOWLIST"
---

<objective>
Wire RecallTool into the planner's schema-visibility path. Two production edits land here:
(1) `services/agent/tools/__init__.py` adds `from config.settings import settings` + a conditional `if settings.recall_tool_enabled: from services.agent.tools.recall import RecallTool` (per PATTERNS analog 4 §After — Plan 04 owns this single-line guard);
(2) `services/pipeline.py:744` appends `'recall_memory'` as the 4th element of `AGENT_TOOL_ALLOWLIST` (per PATTERNS analog 5).

Plus the test surface: extend the existing `tests/unit/test_settings_recall_kill_switch.py` (created in Plan 01) with 5 importlib.reload-based tests covering the toggle behavior, AND add a new `tests/integration/test_recall_tool_planner_pick.py` integration test asserting the planner picks `recall_memory` for preference queries and skips it for unrelated queries.

Purpose: this is the moment when `recall_memory` becomes planner-visible. Before this plan, the tool is registered (Plan 03) but the planner cannot see it because `AGENT_TOOL_ALLOWLIST` is still length 3. After this plan, the planner's `schemas_for(..., names=AGENT_TOOL_ALLOWLIST)` returns the 4th tool schema and the LLM can pick it.

Output: 2 production edits + 1 test file extension + 1 new integration test file. No new test files are created in tests/unit/ (we extend the existing one to keep the kill-switch test surface in a single location per RESEARCH §Validation Architecture Wave 0 layout).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-CONTEXT.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-VALIDATION.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-01-SUMMARY.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-03-SUMMARY.md
@services/agent/tools/__init__.py
@services/agent/tools/registry.py
@services/pipeline.py
@tests/conftest.py
@tests/integration/test_pgvector_filtered_recall.py

<interfaces>
<!-- Symbols Plan 04 must align with. Extracted from working tree on 2026-05-16. -->

From services/agent/tools/__init__.py (current 30-line file — Plan 01 left UNCHANGED):
```
from services.agent.tools.base import BaseTool
from services.agent.tools.registry import ToolRegistry, get_tool_registry

# Side-effect imports trigger @get_tool_registry().register at package load.
from services.agent.tools.retrieve import (  # noqa: F401
    RefinedRetrieveTool,
    RetrieveTool,
    retrieve_impl,
)
from services.agent.tools.web_search import WebSearchTool  # noqa: F401

__all__ = [
    "BaseTool",
    "ToolRegistry",
    "get_tool_registry",
    "RetrieveTool",
    "RefinedRetrieveTool",
    "WebSearchTool",
    "retrieve_impl",
]
```
Plan 04 adds:
- `from config.settings import settings` as the FIRST module-level import (before BaseTool import).
- The conditional `if settings.recall_tool_enabled:` block AFTER `WebSearchTool` import.
- `__all__` UNCHANGED — RecallTool deliberately NOT exported per PATTERNS analog 4 swap-list row 3 (avoid surprising `ImportError` when toggle is False).

From services/pipeline.py:744 (current literal):
```
AGENT_TOOL_ALLOWLIST: list[str] = ["search_knowledge_base", "refine_search", "web_search"]
```
Plan 04 replaces with the analog-5 §After form (4-element list + updated comment block).

From services/agent/tools/registry.py:
- `ToolRegistry.schemas_for(provider, names) -> list[dict]` at line ~78 — filters by registered names; if a name in `names` is not registered, it is silently dropped from the output. This is THE mechanism that makes the kill-switch work: when `recall_memory` is not registered, `schemas_for(..., names=AGENT_TOOL_ALLOWLIST)` returns 3 schemas instead of 4.
- `ToolRegistry.list() -> list[str]` returns registered names.
- `ToolRegistry.get(name) -> type[BaseTool]` raises `KeyError("Tool {name!r} not found")` on missing.
- Module-level singleton: `_registry: ToolRegistry | None = None`; `get_tool_registry()` lazy-instantiates.

From services/pipeline.py:
- `AGENT_TOOL_ALLOWLIST` referenced at lines 984, 1075, 1321 (three call sites — search confirms). Plan 04 only edits the literal at 744.

From tests/conftest.py:
- `PG_AVAILABLE` constant + `pgvector_pool` fixture (alias for `pg_pool` at lines 85-93) — REUSED by Plan 04 integration test.

From tests/integration/test_pgvector_filtered_recall.py:22-29:
- Marker convention: `pytestmark = [pytest.mark.integration, pytest.mark.pgvector, pytest.mark.skipif(not PG_AVAILABLE, reason="...")]` — Plan 04 integration test mirrors verbatim.

From .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md analog 12 §Skeleton:
- The 5 importlib.reload tests use `_reset_registry_and_reimport(monkeypatch, enabled)` helper that resets `_registry` to None, sets `settings.recall_tool_enabled`, and reloads `services.agent.tools` + child modules. This is the load-bearing test mechanism for the kill-switch behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extend kill-switch tests with 5 reload tests + create planner-pick integration tests</name>
  <files>tests/unit/test_settings_recall_kill_switch.py, tests/integration/test_recall_tool_planner_pick.py</files>
  <read_first>
    - tests/unit/test_settings_recall_kill_switch.py (Plan 01 result — 3 presence tests; Plan 04 EXTENDS, NOT REPLACES)
    - services/agent/tools/__init__.py (current 30-line shape — analog-4 §Before block; verify it has NOT changed since Plan 01)
    - services/agent/tools/registry.py (full file — `_registry` singleton + `register` decorator + `schemas_for` filter + `list/get` accessors)
    - services/agent/tools/retrieve.py + web_search.py (the side-effect import pattern Plan 04 mirrors)
    - tests/integration/test_pgvector_filtered_recall.py (lines 1-30 — marker + skipif idiom)
    - tests/integration/test_pgvector_recall.py (full file — `PG_AVAILABLE`, asyncpg.Pool fixture access pattern)
    - tests/conftest.py (lines 80-100 for `pgvector_pool` fixture; line ~20-30 for `PG_AVAILABLE`)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 12 §Skeleton verbatim for the 5 reload tests; analog 13 for integration shape)
  </read_first>
  <behavior>
    A. EXTEND `tests/unit/test_settings_recall_kill_switch.py` with 5 new tests AFTER the existing 3 Plan 01 presence tests. Use the `_reset_registry_and_reimport(monkeypatch, enabled: bool)` helper per PATTERNS analog 12 §Skeleton:

    - Test 4 (`test_enabled_registers_recall_memory`): `_reset_registry_and_reimport(monkeypatch, enabled=True)`; assert `"recall_memory" in reg.list()`. Sanity: tool registers under True toggle.
    - Test 5 (`test_disabled_skips_recall_memory`): `enabled=False`; assert `"recall_memory" not in reg.list()`. Load-bearing kill-switch test.
    - Test 6 (`test_disabled_registry_lookup_raises_keyerror`): `enabled=False`; `with pytest.raises(KeyError): reg.get("recall_memory")`.
    - Test 7 (`test_allowlist_length_constant_regardless_of_toggle`): `from services.pipeline import AGENT_TOOL_ALLOWLIST; assert len(AGENT_TOOL_ALLOWLIST) == 4; assert "recall_memory" in AGENT_TOOL_ALLOWLIST`. D-B4 wording — list literal stays length 4 regardless of toggle. (Test doesn't actually toggle — just asserts the post-Plan-04 length).
    - Test 8 (`test_schemas_for_omits_when_disabled`): `enabled=False`; `schemas = reg.schemas_for("anthropic", names=AGENT_TOOL_ALLOWLIST); assert len(schemas) == 3; names = [s["name"] for s in schemas]; assert "recall_memory" not in names`. This is the actual mechanism by which the planner stops seeing the tool.

    All 5 NEW tests must fail on Plan 03's tree because `services/agent/tools/__init__.py` does NOT yet conditionally import `recall.py` — the registry will NOT contain `recall_memory` even when the toggle is True (Test 4 fails). RED gate.

    B. CREATE `tests/integration/test_recall_tool_planner_pick.py` with 3 tests per PATTERNS analog 13:

    - Marker block (verbatim from analog 13 §Marker convention): env-var setdefault top + `pytestmark = [pytest.mark.integration, pytest.mark.pgvector, pytest.mark.skipif(not PG_AVAILABLE, reason="PostgreSQL + pgvector not available — skipping")]`. Tests SKIP gracefully on CI hosts without PG (matching Phase 23 Plan 06 behavior).
    - Test 1 (`test_planner_picks_recall_for_preference_query`): seed `long_term_facts` with a `"user prefers React"` fact (via real `LongTermMemory().save_fact(...)`). Call the real `Planner.plan_from_messages(...)` (or via `AgentQueryPipeline._build_initial_messages` + a planner call) with query `"based on what you've learned about me, what frontend framework should I use?"`. Assert at least one `ToolCall` in the resulting `ToolPlan` has `name == "recall_memory"`. Use a low-temperature planner (`temperature=0.0`) to reduce flakiness; if `EMBEDDING_PROVIDER == "huggingface"` and model files not present, the test SHOULD skip — same pattern as `test_pgvector_filtered_recall`.
    - Test 2 (`test_planner_skips_recall_for_unrelated_query`): query `"what is 2+2?"` (no preference reference). Assert NO `ToolCall.name == "recall_memory"` appears.
    - Test 3 (`test_recall_tool_returns_seeded_fact`): after seed, instantiate `RecallTool()` directly + call `await tool.run(args={"query":"frontend framework"}, ctx=...)` with proper `ToolContext`. Assert `result.content` contains the substring `"React"` AND `result.is_error is False`.
    - Cleanup: scoped `DELETE FROM long_term_facts WHERE user_id='test-plan04-u' AND tenant_id='test-plan04-t'` in a teardown — NEVER drop the table (analog 13 anti-pattern).

    On the unmodified Plan 03 tree, the planner integration test fails because `AGENT_TOOL_ALLOWLIST` is still length 3 — the planner LLM cannot pick a tool that is not in its tool-schemas list. RED gate.
  </behavior>
  <action>
    EXTEND `tests/unit/test_settings_recall_kill_switch.py`:

    1. Add `import importlib` and `import pytest` at the top (after the env-var setdefault block). Keep `from config.settings import settings` import at the top (Plan 01 already has it).

    2. Add the helper function `_reset_registry_and_reimport(monkeypatch, enabled: bool)` per PATTERNS analog 12 §Skeleton verbatim. The helper:
       - imports `services.agent.tools.registry as reg_mod`
       - `monkeypatch.setattr(reg_mod, "_registry", None, raising=False)` — reset singleton
       - `monkeypatch.setattr(settings, "recall_tool_enabled", enabled, raising=False)` — flip toggle
       - imports `services.agent.tools as tools_mod` then `importlib.reload(tools_mod)` — re-runs the `if settings.recall_tool_enabled: from ...` line
       - reloads sibling modules so RetrieveTool / WebSearchTool re-register fresh on the reset registry: `import services.agent.tools.retrieve as r1; importlib.reload(r1); import services.agent.tools.web_search as r2; importlib.reload(r2)`
       - returns `tools_mod.get_tool_registry()`

    3. Add the 5 test functions per <behavior> A. Each uses the helper to set toggle state then queries the registry. Tests 4-8 are independent (no shared state via monkeypatch undo).

    4. DO NOT modify or remove the 3 Plan 01 presence tests. Append.

    5. DO NOT manually clear `sys.modules["services.agent.tools.recall"]` — analog 12 anti-pattern; reload is sufficient.

    CREATE `tests/integration/test_recall_tool_planner_pick.py`:

    1. Env-var setdefault block + `pytestmark` marker per analog 13 §Marker convention.

    2. Import `from tests.conftest import PG_AVAILABLE` + `from services.memory.memory_service import LongTermMemory, get_memory_service` + `from services.agent.tools.recall import RecallTool` + `from utils.models import GenerationRequest, ToolContext` + `from services.pipeline import AGENT_TOOL_ALLOWLIST`.

    3. Use the existing `pgvector_pool` fixture from `tests/conftest.py:85-93` for connection access. The seed step uses `await mem._long.save_fact(user_id="test-plan04-u", tenant_id="test-plan04-t", fact="user prefers React", source_doc="test", importance=0.8)` directly — Phase 23 save_fact embeds-on-write so embedding lands automatically.

    4. For the planner-pick tests, the implementation may follow either path (planner discretion at execute time, but RED-gate authors should mock the planner if real-LLM availability is uncertain at the test seam):
       - Option A (real planner): construct `Planner` per existing pipeline.py wiring and call `plan_from_messages` with a stub message list. Higher fidelity, requires API keys at test time.
       - Option B (mocked planner): patch `BaseLLMClient.call_agentic_turn` to return a deterministic `ToolPlan` JSON for the preference query AND a different one for the unrelated query. Higher determinism, lower fidelity.

       Recommend Option B for CI-stable RED→GREEN gating; flag Test 1 with `pytest.mark.real_llm` if Option A is chosen so it can be skipped via `pytest -m 'not real_llm'`.

    5. For Test 3 (RecallTool seeded-fact recall), build `ToolContext(req=GenerationRequest(query="frontend framework", user_id="test-plan04-u", tenant_id="test-plan04-t"), tf={}, retriever=object(), llm=object())`. The downstream `get_memory_service()._long.get_relevant_facts(...)` runs against real PG via the fixture pool — no mocks needed.

    6. Teardown: `await pool.execute("DELETE FROM long_term_facts WHERE user_id='test-plan04-u' AND tenant_id='test-plan04-t'")` in a fixture finalizer or `try/finally` per test.

    Run: `uv run pytest tests/unit/test_settings_recall_kill_switch.py --collect-only -q` — expect 8 items (3 Plan 01 + 5 new). Run unit: `uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q` — expect 5 failures (Plan 01 presence tests already GREEN). Run integration collection: `uv run pytest tests/integration/test_recall_tool_planner_pick.py --collect-only -q` — expect 3 items. Integration full run depends on PG availability — on CI without PG, tests SKIP (`pytest -m pgvector --collect-only` enumerates without running).

    Commit: `test(24-04): RED gates for conditional registration + planner-pick integration (MEM-09)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings_recall_kill_switch.py tests/integration/test_recall_tool_planner_pick.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_settings_recall_kill_switch.py` now contains 8 test functions (3 Plan 01 + 5 added here).
    - `tests/integration/test_recall_tool_planner_pick.py` exists with 3 test functions and the `pytestmark` marker block.
    - `uv run pytest tests/unit/test_settings_recall_kill_switch.py --collect-only -q` lists 8 items.
    - `uv run pytest tests/integration/test_recall_tool_planner_pick.py --collect-only -q` lists 3 items.
    - `uv run pytest tests/unit/test_settings_recall_kill_switch.py::test_enabled_registers_recall_memory -x` returns non-zero (RED — `__init__.py` does NOT yet conditionally import recall.py).
    - `uv run pytest tests/unit/test_settings_recall_kill_switch.py::test_allowlist_length_constant_regardless_of_toggle -x` returns non-zero (RED — allowlist still length 3 in pipeline.py).
    - `grep -c 'importlib.reload' tests/unit/test_settings_recall_kill_switch.py` ≥ 3 (helper uses reload on tools_mod + retrieve + web_search).
    - `grep -c 'pytest.mark.pgvector' tests/integration/test_recall_tool_planner_pick.py` ≥ 1 (marker present).
    - `grep -c 'DROP TABLE' tests/integration/test_recall_tool_planner_pick.py` equals 0 (scoped DELETE only — analog 13 anti-pattern).
  </acceptance_criteria>
  <done>5 unit-test RED gates appended; 3 integration-test RED gates created (skip-gated on PG availability). Plan 01 tests still GREEN. Test files collect cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — conditional import in `__init__.py` + `AGENT_TOOL_ALLOWLIST` 3→4 edit</name>
  <files>services/agent/tools/__init__.py, services/pipeline.py</files>
  <read_first>
    - services/agent/tools/__init__.py (current 30-line file; understand the existing `RetrieveTool` / `WebSearchTool` unconditional-import shape Plan 04 mirrors)
    - services/pipeline.py (line 744 `AGENT_TOOL_ALLOWLIST` literal + surrounding comments; lines 984/1075/1321 — three consumer call sites that use `names=AGENT_TOOL_ALLOWLIST` — VERIFY these are NOT touched by Plan 04)
    - config/settings.py (verify `recall_tool_enabled` field landed in Plan 01)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 4 §After block — verbatim `__init__.py` edit; analog 5 §After block — verbatim allowlist edit)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md (Pattern 3 — conditional registration shape; Code Examples §Allowlist edit)
    - tests/unit/test_settings_recall_kill_switch.py + tests/integration/test_recall_tool_planner_pick.py (the RED gates from Task 1)
  </read_first>
  <action>
    Edit `services/agent/tools/__init__.py`:

    1. ADD as the FIRST module-level import (above the existing `from services.agent.tools.base import BaseTool` line):
       ```
       from config.settings import settings
       ```

    2. AFTER the existing `from services.agent.tools.web_search import WebSearchTool  # noqa: F401` line and BEFORE the `__all__` declaration, add:
       ```
       # Phase 24 / D-B4 / Pattern 3 — kill-switch via conditional registration.
       # When False, the decorator never runs → registry.get("recall_memory")
       # raises KeyError → planner-LLM never sees the tool schema.
       if settings.recall_tool_enabled:
           from services.agent.tools.recall import RecallTool  # noqa: F401
       ```

    3. LEAVE `__all__` UNCHANGED — RecallTool deliberately NOT exported per analog 4 swap-list row 3 (consumers go through `get_tool_registry().get("recall_memory")` — avoid surprising `ImportError` when toggle is False).

    Edit `services/pipeline.py:744`:

    1. Replace the current literal:
       ```
       AGENT_TOOL_ALLOWLIST: list[str] = ["search_knowledge_base", "refine_search", "web_search"]
       ```
       with the analog 5 §After block:
       ```
       # Explicit allowlist of tool names exposed to the planner LLM (AGENT-07).
       # Phase 20: web_search joins with real Tavily impl.
       # Phase 24 / MEM-09: recall_memory joins with pgvector cosine-recall impl
       # (services/agent/tools/recall.py). settings.recall_tool_enabled gates
       # REGISTRATION (not allowlist membership) — when False the registry lookup
       # at registry.py:78 silently drops "recall_memory" from schemas_for(...).
       AGENT_TOOL_ALLOWLIST: list[str] = [
           "search_knowledge_base",
           "refine_search",
           "web_search",
           "recall_memory",
       ]
       ```

    2. DO NOT rename the constant (referenced at lines 984, 1075, 1321 — Plan 04 leaves those call sites untouched per analog 5 anti-pattern callout 2).
    3. DO NOT add a runtime `if settings.recall_tool_enabled:` filter around the allowlist (analog 5 anti-pattern callout 1 — D-B4 spec says list stays length 4 regardless of toggle).

    Run: `uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q` — expect all 8 tests GREEN.
    Run: `uv run pytest tests/unit/test_recall_tool.py tests/unit/test_memory_recall_semantic.py -x -q` — Plan 02 + 03 regression check.
    Run: `uv run pytest tests/integration/test_recall_tool_planner_pick.py -m pgvector -x -q` — integration tests GREEN if PG available; SKIP gracefully on CI hosts without PG (Phase 23 Plan 06 precedent for skip-gracefully behavior).
    Run: `uv run ruff check services/agent/tools/__init__.py services/pipeline.py` — zero new violations.
    Run: `uv run mypy --strict services/agent/tools/__init__.py services/pipeline.py` — zero new violations.

    Commit: `feat(24-04): MEM-09 wire RecallTool — conditional registration + allowlist 3→4`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_settings_recall_kill_switch.py -x -q` exits 0 (all 8 tests GREEN — 3 Plan 01 + 5 Plan 04).
    - `uv run pytest tests/unit/test_recall_tool.py tests/unit/test_memory_recall_semantic.py -x -q` exits 0 (Plans 02 + 03 still GREEN).
    - `uv run pytest tests/integration/test_recall_tool_planner_pick.py --collect-only -q` lists 3 items (skip-gating preserved).
    - `python -c "from services.pipeline import AGENT_TOOL_ALLOWLIST; assert AGENT_TOOL_ALLOWLIST == ['search_knowledge_base','refine_search','web_search','recall_memory']"` exits 0.
    - `grep -n 'from config.settings import settings' services/agent/tools/__init__.py` matches exactly one line.
    - `grep -n 'if settings.recall_tool_enabled:' services/agent/tools/__init__.py` matches exactly one line.
    - `grep -n 'from services.agent.tools.recall import RecallTool' services/agent/tools/__init__.py` matches exactly one line.
    - `grep -v '^#' services/agent/tools/__init__.py | grep -c '"RecallTool"'` equals 0 (NOT in `__all__` per analog 4 swap-list row 3).
    - `grep -n '"recall_memory"' services/pipeline.py | wc -l` ≥ 1 (allowlist edit landed).
    - `grep -E 'AGENT_TOOL_ALLOWLIST.*\[' services/pipeline.py | wc -l` ≥ 1 (multi-line list literal at 744).
    - `python -c "import services.agent.tools; from services.agent.tools.registry import get_tool_registry; assert 'recall_memory' in get_tool_registry().list()"` exits 0 (registration via __init__ side-effect).
    - `uv run ruff check services/agent/tools/__init__.py services/pipeline.py` exits 0.
  </acceptance_criteria>
  <done>MEM-09 GREEN. Conditional registration wired; allowlist grows 3→4; 8 unit tests + 3 integration tests collect-clean; ruff clean; Plans 02 + 03 regression preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings module ↔ `__init__.py` import path | `settings.recall_tool_enabled` evaluated at module-import time; toggle change requires process restart (acceptable for ops emergency rollback per RESEARCH §specifics) |
| `AGENT_TOOL_ALLOWLIST` ↔ planner schemas | `registry.schemas_for(provider, names=ALLOWLIST)` at registry.py:78 — list literal is static; registration is the dynamic surface |
| Planner LLM ↔ ToolPlan output | Planner-LLM may hallucinate `recall_memory` even when not in schemas; `Executor` defends via `registry.get(name)` `KeyError` (existing Phase 17 behavior) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-S1 | Spoofing | `recall_tool_enabled` env-var spoofed at deploy | accept | Operator owns deploy config; default True; `extractor_enabled` precedent (Phase 23) accepted same shape |
| T-24-04-T1 | Tampering | `AGENT_TOOL_ALLOWLIST` mutated at runtime | accept | List is module-level constant; mutation requires source-code change; tested by Test 7 length-assertion |
| T-24-04-T2 | Tampering | Duplicate registration of `recall_memory` via secondary import path | mitigate | Single-path import enforced by `__init__.py` guard; PATTERNS analog 3 + Pitfall 4 + Test 1 (Plan 03) all assert exactly-one registration; no other module in the tree imports `services.agent.tools.recall` (verified) |
| T-24-04-I1 | Information Disclosure | RecallTool schema visible to planner reveals memory feature exists | accept | Tool description is the same string an end-user would see in product docs; no implementation detail leaks |
| T-24-04-D1 | Denial | Planner picks `recall_memory` then RecallTool errors → user turn degraded | mitigate | Plan 03's best-effort isolation (D-C3) — `is_error=True ToolResult` flows back to synthesizer; user turn completes with degraded context |
| T-24-04-D2 | Denial | Toggle change at runtime not honored until restart | accept | D-B4 explicitly accepts module-import-time evaluation; ops doc notes restart requirement |
| T-24-04-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages (PATTERNS analog 4 + 5 — only edits to existing files) |
</threat_model>

<verification>
- 5 Task 1 unit RED gates flip GREEN under Task 2.
- 3 Task 1 integration RED gates GREEN when PG available (SKIP otherwise — acceptable per Phase 23 precedent).
- Plans 02 + 03 unit suites remain GREEN.
- `RecallTool` registered exactly once after `import services.agent.tools` (Pitfall 4 verified).
- Both modified files ruff + mypy --strict clean.

Run gate: `uv run pytest tests/unit/test_settings_recall_kill_switch.py tests/unit/test_recall_tool.py tests/unit/test_memory_recall_semantic.py -x -q && uv run ruff check services/agent/tools/__init__.py services/pipeline.py`
</verification>

<success_criteria>
- ROADMAP SC-2 complete: `AGENT_TOOL_ALLOWLIST length == 4`; planner integration test asserts pick-on-preference + skip-on-unrelated.
- MEM-09 requirement complete: `recall_memory` in allowlist; registered via decorator-import side-effect; planner integration coverage in place.
- Plan 04 Wave-0 stubs (5 unit + 3 integration) GREEN (integration may SKIP on CI without PG — acceptable per analog 13).
- Kill-switch behavior auditable: toggle False → registry lookup fails → schemas_for omits → planner cannot pick.
</success_criteria>

<output>
Create `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-04-SUMMARY.md` when done.
</output>
