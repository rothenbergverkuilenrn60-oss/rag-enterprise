---
phase: 24-pgvector-recalltool-semantic-recall-rewrite
plan: 05
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - tests/integration/test_pipeline_load_context_audit.py
# T8 (eng-review 2026-05-16) removed `services/memory/memory_service.py` from
# this plan — Plan 02 Task 4 (T1) owns the body change + docstring + ASCII
# diagram. Plan 05 is now integration-test-only.
autonomous: true
requirements: [MEM-10]
tags: [load-context-audit, semantic-shift, removal-regression, integration-test, pgvector-marker]
review_amendments:
  - T8 (eng-review 2026-05-16 / Decision-9): MEM-10 audit RESHAPED. Original popularity-vs-semantic token-delta JSON was methodologically moot (both paths used LIMIT 5 + same fact column → near-zero delta by construction). Post Decision-1, `load_context` no longer fetches `long_term_facts` at all, so the comparison subject doesn't exist. New audit shape: REMOVAL regression at all 4 pipeline call sites — assert `mem_ctx.long_term_facts == []` (always, regardless of seed). Token-delta JSON artifact DROPPED.
  - The Plan 02 `load_context` docstring already absorbs the ASCII diagram (T10) and the semantic-shift acknowledgement, so Plan 05's docstring edit shrinks to a small cross-reference pointing at Plan 02 for the body change.
must_haves:
  truths:
    - "`MemoryService.load_context` body was edited by Plan 02 Task 4 (T1 / Decision-1) — `long_term_facts` no longer in the asyncio.gather call. Plan 05 verifies the post-removal behavior via integration test; does NOT re-edit the docstring (already done in Plan 02 with the ASCII diagram)."
    - "At each of the 4 `load_context` call sites (services/pipeline.py:429, 608, 971, 1062), `mem_ctx.long_term_facts == []` regardless of seeded fact count — verified by integration test seeded with 12 rows (semantic recall would have returned non-empty under the old behavior; post-Decision-1 the field is ALWAYS empty)."
    - "Verify the typed-field shape is preserved: `isinstance(mem_ctx.long_term_facts, list)` (Pydantic V2 field still present, just always `[]`)."
    - "Optional end-to-end response-token measurement against v1.5 baseline — measures actual LLM response tokens (not fact-text estimates) for a fixture query set. Observational only, written to `24-MEM10-AUDIT.json` with `{mean_response_tokens, p95_response_tokens, sample_size, methodology, version: 2}`. If end-to-end measurement is impractical in the test environment, skip the JSON write and document in SUMMARY.md as pre-tag manual verification."
    - "Existing v1.0–v1.5 integration suite still passes (no new failures introduced by the load_context behavior change)."
  artifacts:
    - path: "services/memory/memory_service.py"
      provides: "Plan 05 makes NO further edits to this file — Plan 02 Task 4 (T1 / T10) already landed the body change + ASCII diagram + semantic-shift docstring. If a cross-reference comment to Plan 05's audit file would help, ADD a single line in the load_context docstring: `See tests/integration/test_pipeline_load_context_audit.py for the MEM-10 removal regression gate.` But this is optional — the diagram already mentions the audit path."
      contains: []
    - path: "tests/integration/test_pipeline_load_context_audit.py"
      provides: "3 integration tests (was 4; token-delta JSON writer dropped per T8): 4-call-site removal regression (parametrized over 4 lines), typed-list-shape preserved, v1.5 baseline-suite green-check. Optional 4th test for end-to-end response-token measurement (skipped in default CI; pre-tag verification)."
      exports: ["test_load_context_long_term_facts_empty_all_four_callsites", "test_load_context_long_term_facts_typed_list_preserved", "test_no_v1_5_regression"]
  key_links:
    - from: "services/memory/memory_service.py::MemoryService.load_context"
      to: "(none — long_term_facts call site removed by Plan 02 Task 4 / T1)"
      via: "post-removal: load_context only gathers _short + _long.get_user_profile"
      pattern: "long_term_facts=\\[\\]"
    - from: "tests/integration/test_pipeline_load_context_audit.py"
      to: "services/memory/memory_service.py::MemoryService.load_context"
      via: "real PG fixture seeded with 12 rows; assert mem_ctx.long_term_facts == [] regardless"
      pattern: "long_term_facts == \\[\\]"
---

<objective>
Close the MEM-10 audit — RESHAPED by eng-review 2026-05-16 (T8 / Decision-9) from a token-delta observational artifact (methodologically moot) to a **4-call-site REMOVAL regression** that asserts `mem_ctx.long_term_facts == []` at every consumer post-Decision-1.

Surfaces:
(1) NO further edit to `MemoryService.load_context` — Plan 02 Task 4 (T1 / T10) already landed the body change (gather no longer fetches long_term_facts) + ASCII diagram + docstring. Plan 05 may add an optional one-line cross-reference to this file in the docstring.
(2) Create `tests/integration/test_pipeline_load_context_audit.py` with 3 tests: 4-call-site removal regression (parametrized over the 4 lines), typed-list-shape preserved (`isinstance(mem_ctx.long_term_facts, list)` even when always `[]`), v1.5 baseline-suite green-check.

Purpose: MEM-10 audit is the proof-of-no-regression for Decision-1. The integration test verifies the removal landed everywhere consumers expect it AND that the typed-field contract (`MemoryContext.long_term_facts: list[str]`) is preserved (consumers reading the field still get a list, not AttributeError). The dropped popularity-vs-semantic token-delta comparison would have measured the wrong variable anyway — both paths used LIMIT 5 + same column, so the fact-text token estimate was identical by construction.

Optional 4th test (deferred): end-to-end LLM response-token measurement against v1.5 baseline. Requires running the full pipeline with a real LLM; pre-tag manual verification per Phase 23 SUMMARY.md precedent.

Output: 1 new integration test file (~180 LOC, was ~250) + 0 production file edits (was 1 docstring edit). No `24-MEM10-AUDIT.json` artifact in default flow. No new production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-CONTEXT.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-VALIDATION.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-02-SUMMARY.md
@services/memory/memory_service.py
@services/pipeline.py
@tests/conftest.py
@tests/integration/test_pgvector_filtered_recall.py
@tests/integration/test_extractor_e2e.py

<interfaces>
<!-- Symbols Plan 05 must align with. Extracted from working tree on 2026-05-16. -->

From services/memory/memory_service.py (current `load_context` at lines ~395-417):
```
async def load_context(
    self,
    session_id: str,
    user_id:    str,
    tenant_id:  str,
    query:      str,
) -> MemoryContext:
    """加载当前请求所需的全部记忆上下文。"""
    short_term, long_term_facts, user_profile = await asyncio.gather(
        self._short.get_history(session_id),
        self._long.get_relevant_facts(user_id, tenant_id, query),
        self._long.get_user_profile(user_id, tenant_id),
        return_exceptions=True,
    )
    return MemoryContext(
        session_id=session_id,
        user_id=user_id,
        tenant_id=tenant_id,
        short_term=short_term if isinstance(short_term, list) else [],
        long_term_facts=long_term_facts if isinstance(long_term_facts, list) else [],
        user_profile=user_profile if isinstance(user_profile, UserProfile) else None,
    )
```
- Method body: UNCHANGED in Plan 05 — only docstring extended per PATTERNS analog 2.
- The `isinstance(long_term_facts, list)` guard at line ~415 is the safety net: `asyncio.gather(return_exceptions=True)` may surface a `BaseException`; the guard converts to `[]`. PRESERVE this guard verbatim.

From services/pipeline.py (4 load_context call sites verified by grep):
- Line 429 — `mem_ctx = await self._memory.load_context(...)` inside `QueryPipeline.run` (legacy non-agentic path).
- Line 608 — same call inside another path (verify against working tree).
- Line 971 — inside `AgentQueryPipeline.run`.
- Line 1062 — inside `AgentQueryPipeline.run_streaming` (also `SwarmQueryPipeline.run` adjacent).

CONTEXT D-B3 + RESEARCH §Pitfall 6 say the audit is a LENGTH-ONLY regression assertion — NOT a snapshot of the popularity-baseline content. The semantic shift changes WHICH facts return; it MUST NOT change HOW MANY facts return.

From utils/models.py:
- `class MemoryContext` — frozen Pydantic V2 model with `long_term_facts: list[str]` field; length-cap not enforced at the model level (relies on `get_relevant_facts` LIMIT $4).

From tests/conftest.py:
- `PG_AVAILABLE` constant + `pgvector_pool` fixture (alias for `pg_pool`).
- Phase 23 added: `extractor_llm_mock`, `embedder_or_mock`, `clean_long_term_facts` fixtures — REUSABLE in Plan 05 for seeding + cleanup.

From tests/integration/test_extractor_e2e.py (Phase 23 — closest analog for end-to-end-with-real-PG pattern):
- pytestmark marker block; PG-skip-gracefully shape; seed-via-real-save_fact pattern.

For the token-delta measurement, use a simple character-count → estimated-token approximation: `est_tokens = sum(len(fact) // 4 for fact in facts)` — same heuristic used in Plan 06's backfill --dry-run formula per PATTERNS analog 7 §D-D4 cost-formula row. For the baseline (popularity-ranked content), the test re-issues the same recall against a temporary popularity-ranked SELECT (inline SQL in the test file) on the same seed data so the comparison is apples-to-apples. The test does NOT call a v1.5 git-checkout — instead it issues a popularity-baseline SELECT inline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 4 integration tests for MEM-10 audit</name>
  <files>tests/integration/test_pipeline_load_context_audit.py</files>
  <read_first>
    - services/memory/memory_service.py (lines 395-417 — `load_context` body + the `isinstance` guards)
    - services/pipeline.py (4 call sites at lines 429, 608, 971, 1062 — confirm exact call shape so the test can drive each path)
    - tests/integration/test_extractor_e2e.py (Phase 23 — full end-to-end pattern with real PG seed + cleanup)
    - tests/integration/test_pgvector_filtered_recall.py (lines 1-30 — marker + skipif idiom)
    - tests/conftest.py (lines 60-120 — `PG_AVAILABLE`, `pgvector_pool`, `clean_long_term_facts`, `embedder_or_mock` fixtures)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 5 — MEM-10 audit shape; analog 13 — integration mechanics; §No Analog Found row 4 — audit artifact write idiom)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md (Pitfall 3 + 6; §Validation Architecture MEM-10 rows at lines 805-806)
  </read_first>
  <behavior>
    - Test 1 (`test_load_context_long_term_facts_empty_all_four_callsites`, parametrized over the 4 call sites): seed `long_term_facts` with 12 rows for `(user_id="test-mem10-u", tenant_id="test-mem10-t")` via real `save_fact` (Phase 23 embed-on-write covers the embedding). Call `await mem.load_context(session_id="s", user_id=..., tenant_id=..., query="frontend")`. Assert `mem_ctx.long_term_facts == []` (T8 / Decision-1 — the field is ALWAYS empty regardless of seed). Parametrize over `["pipeline.py:429 (QueryPipeline.run)", "pipeline.py:608 (QueryPipeline.run_streaming)", "pipeline.py:971 (AgentQueryPipeline.run)", "pipeline.py:1062 (AgentQueryPipeline.run_streaming)"]` — the underlying `load_context` call is identical, parametrize for documentation traceability so each row maps 1:1 to a pipeline.py line consumed in the contract.
    - Test 2 (`test_load_context_long_term_facts_typed_list_preserved`): seed 5 facts. Call `load_context`. Assert `isinstance(mem_ctx.long_term_facts, list)` (NOT None, NOT missing attribute) — Pydantic V2 field still present and typed even though always empty. Asserts the typed-shape contract that downstream consumers depend on.
    - Test 3 (`test_no_v1_5_regression`): instantiate `QueryPipeline()` + run a recorded request shape; assert valid `GenerationResponse`. Equivalent to v1.5 baseline test_pipeline pattern. Catches any consumer that was reading `mem_ctx.long_term_facts` expecting non-empty (would now fail or silently render less). If a v1.5 test fails, document the regression in SUMMARY.md and either (a) revise the v1.5 test to match new behavior (correct path — the semantic shift is intentional) or (b) escalate.
    - **Removed (per T8)**: previous Test 2 (`test_load_context_returns_list_of_str_no_prefix`) — moot because the list is always empty.
    - **Removed (per T8)**: previous Test 3 (`test_writes_token_delta_artifact`) — methodology was broken (same-LIMIT same-column comparison → near-zero delta) AND now meaningless (popularity-baseline path doesn't exist anymore).

    On the Plan 04 tree (with Plan 02 Task 4 GREEN — `load_context` body modified), all 3 tests PASS immediately. The RED→GREEN gate for THIS plan is post-amendment-only: tests pass after Plan 02 Task 4 (T1) lands the body change. If executing Plan 05 BEFORE Plan 02 Task 4 in the wave, tests fail — that's the RED.
  </behavior>
  <action>
    Create `tests/integration/test_pipeline_load_context_audit.py`. Env-var setdefault top + `from __future__ import annotations` + `pytestmark = [pytest.mark.integration, pytest.mark.pgvector, pytest.mark.skipif(not PG_AVAILABLE, reason="...")]` per analog 13 §Marker convention.

    Imports: `import json`; `from pathlib import Path`; `import pytest`; `from tests.conftest import PG_AVAILABLE`; `from services.memory.memory_service import LongTermMemory, get_memory_service`; `from utils.models import MemoryContext`.

    Use the existing `pgvector_pool` fixture for direct PG access (popularity-baseline SELECT in Test 3).
    Use `clean_long_term_facts` fixture (Phase 23 — `tests/conftest.py` fixture) for teardown OR define a local `try/finally` `DELETE FROM long_term_facts WHERE user_id LIKE 'test-mem10-%'`.

    For the seed-fact set in Test 3 (30 facts with varied lengths), generate synthetically: short facts (`"X likes Y"` ~10 chars), medium (`"User prefers React over Vue for frontend development"` ~50 chars), long (~150 chars). Distribute 10/10/10 across importance buckets {0.2/0.5/0.8} to exercise tie-break.

    For the 10-query fixture list in Test 3, pick queries that span diverse domains (frontend, database, lifestyle, work) so the semantic-recall path returns DIFFERENT facts per query. The baseline popularity-rank returns the SAME 5 facts regardless of query (popularity is query-independent) — that asymmetry is what the audit measures.

    For the artifact path, use `Path(__file__).resolve().parents[2] / ".planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-MEM10-AUDIT.json"` — relative to the test file's location (tests/integration/). Verify parent dir exists before write (Phase 24 dir created by /gsd-plan-phase already).

    Do NOT assert any specific token delta value — analog 5 anti-pattern + D-B3 ban gating threshold assertions.
    Do NOT use a snapshot fixture for the baseline content — analog 5 anti-pattern + Pattern 5; only LENGTH is asserted.
    Do NOT drop the `long_term_facts` table — analog 13 anti-pattern; scoped DELETE only.

    Run: `uv run pytest tests/integration/test_pipeline_load_context_audit.py --collect-only -q` — expect 4 items (Test 1 parametrize counts as 4 effective items if you parametrize, otherwise 4 functions). Run: `uv run pytest tests/integration/test_pipeline_load_context_audit.py -m pgvector -x -q` — RED expected primarily on Test 3 (artifact file does not exist yet at first run, but that test should PASS on first run because it CREATES the artifact; the RED signal is in production-side regression if Plan 02's rewrite broke length contract). On a CI host without PG, the test SKIPs gracefully.

    Commit: `test(24-05): RED gates for MEM-10 4-call-site audit + token-delta artifact`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_pipeline_load_context_audit.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/test_pipeline_load_context_audit.py` exists with 4 test functions (Test 1 may be parametrized — count as 4 effective items).
    - `uv run pytest tests/integration/test_pipeline_load_context_audit.py --collect-only -q` lists ≥ 4 items.
    - `grep -c 'pytest.mark.pgvector' tests/integration/test_pipeline_load_context_audit.py` ≥ 1.
    - `grep -c '24-MEM10-AUDIT.json' tests/integration/test_pipeline_load_context_audit.py` ≥ 1.
    - `grep -c 'DROP TABLE' tests/integration/test_pipeline_load_context_audit.py` equals 0 (scoped DELETE only).
    - `grep -c 'assert.*delta.*<' tests/integration/test_pipeline_load_context_audit.py` equals 0 (no threshold gating per D-B3).
    - `grep -c 'snapshot\|exact_match' tests/integration/test_pipeline_load_context_audit.py` equals 0 (length-only per analog 5).
    - `grep -E '^from __future__ import annotations' tests/integration/test_pipeline_load_context_audit.py` matches one line.
  </acceptance_criteria>
  <done>4 integration RED gates committed. Test file collects cleanly; SKIPs gracefully on CI without PG.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (T8 / Decision-9 reshape): GREEN — verify post-Plan-02 removal landed at all 4 sites</name>
  <files>tests/integration/test_pipeline_load_context_audit.py (verify only)</files>
  <read_first>
    - services/memory/memory_service.py — confirm Plan 02 Task 4 landed `long_term_facts=[]` in `load_context` body
    - tests/integration/test_pipeline_load_context_audit.py (Task 1 result — the 3 tests this task should turn GREEN)
  </read_first>
  <action>
    Plan 05 now ships ONLY the integration test file (Task 1). Task 2 reduces to a verification step — confirm the 3 RED gates from Task 1 flip GREEN after Plan 02 Task 4 lands.

    1. Run: `uv run pytest tests/integration/test_pipeline_load_context_audit.py -m pgvector -x -q` — expect 3 GREEN where PG available; SKIP gracefully otherwise (Phase 23 precedent).
    2. Run: `uv run pytest tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q` — Plans 02 + 03 regression check.
    3. Optional: if the executing environment supports a real pipeline end-to-end run, measure mean / p95 response tokens against a v1.5 baseline (e.g., 10 fixture queries × current branch vs git-revert-checkout of v1.5 tag). Write to `24-MEM10-AUDIT.json` with version=2 schema `{mean_response_tokens, p95_response_tokens, sample_size, methodology, version: 2}`. SKIP this step in default execution; document in SUMMARY.md as a pre-tag manual verification (matches Phase 23 pattern).
    4. NO production code edits. NO docstring edits (Plan 02 owns).
    5. If a v1.5 baseline integration test fails (any test previously asserting non-empty `mem_ctx.long_term_facts`), document in SUMMARY.md with root cause + revision note. The semantic shift is intentional — revise the v1.5 test if needed.

    Commit (only if SUMMARY.md updated or audit JSON written): `chore(24-05): record MEM-10 audit verification + v1.5 baseline notes`. Otherwise no commit — Plan 05 is verification-only.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_pipeline_load_context_audit.py --collect-only -q && uv run pytest tests/unit/test_memory_recall_semantic.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q` exits 0 (Plans 02 + 03 still GREEN; including the 2 new tests in test_memory_recall_semantic.py that verify the load_context drop).
    - `uv run pytest tests/integration/test_pipeline_load_context_audit.py -m pgvector --collect-only -q` lists 3 items (parametrize Test 1 expands to 4 effective items).
    - If PG available locally: `uv run pytest tests/integration/test_pipeline_load_context_audit.py -m pgvector -x -q` exits 0.
    - `grep -c '24-MEM10-AUDIT.json' tests/integration/test_pipeline_load_context_audit.py` equals 0 (T8 — JSON artifact dropped from default flow).
    - `grep -c 'token_delta\|baseline_mean\|popularity_baseline' tests/integration/test_pipeline_load_context_audit.py` equals 0 (T8 — old methodology fully removed).
    - `grep -c 'long_term_facts == \[\]\|long_term_facts =='  tests/integration/test_pipeline_load_context_audit.py` ≥ 1 (T8 — new assertion shape).
    - SUMMARY.md (Plan 05) documents whether optional response-token audit was performed; if not, pre-tag manual verification clause present.
  </acceptance_criteria>
  <done>T8 GREEN. MEM-10 audit reshaped to 4-site removal regression; old token-delta methodology removed; Plans 02 + 03 regression preserved; SUMMARY.md notes audit verification status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `load_context` ↔ 4 pipeline call sites | List-shape contract is the implicit invariant; semantic shift changes WHICH facts return, NOT the shape; length-only regression gate is the load-bearing defense |
| Audit artifact ↔ `.planning/` tree | JSON written to planning dir at test time; observational only; no security surface (no secrets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-T1 | Tampering | Pre-formatted bullet strings leak into `MemoryContext.long_term_facts` | mitigate | Pitfall 3 + Test 2 (bare-string-no-prefix regression); RecallTool's bullet formatting lives ONLY in `ToolResult.content`, never in `MemoryContext` |
| T-24-05-I1 | Information Disclosure | Audit artifact contains user-attributed fact content | mitigate | Test 3 uses synthetic seed data (`"X likes Y"` placeholder facts under test user/tenant IDs `test-mem10-*`); no production data |
| T-24-05-D1 | Denial | Length contract regression breaks `_build_initial_messages` at 4 call sites | mitigate | Test 1 parametrized over all 4 sites; Pitfall 6 mitigation; default `limit=5` enforced by Plan 02's pgvector LIMIT |
| T-24-05-D2 | Denial | Token-delta exceeds prompt-budget at production scale | accept | Observational artifact per D-B3 — operators monitor; v1.7+ remediation paths documented in CONTEXT (B1 option 2: shrink `load_context` K; B2 option 2: dedup at merge) |
| T-24-05-R1 | Repudiation | Audit not produced on CI without PG | accept | Pre-tag manual verification clause per Phase 23 SUMMARY.md precedent |
| T-24-05-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages (docstring + integration test only) |
</threat_model>

<verification>
- 4 integration tests run (or SKIP gracefully on CI without PG).
- `load_context` body untouched; only docstring extended.
- Token-delta audit artifact produced when PG available; pre-tag manual verification otherwise.
- Plans 02 + 03 unit suites remain GREEN.
- v1.5 baseline integration tests (whatever the Task 4 chosen set) remain GREEN.

Run gate: `uv run pytest tests/integration/test_pipeline_load_context_audit.py -m pgvector --collect-only -q && uv run pytest tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q`
</verification>

<success_criteria>
- ROADMAP SC-5 complete: 4-site length regression + token-delta artifact + no new v1.0-v1.5 test failures.
- MEM-10 requirement complete: semantic-shift documented in code (docstring) + audit gate (integration test) + ops-visible artifact (JSON in planning dir).
- D-B3 observational-only constraint respected: no gating threshold on token delta.
</success_criteria>

<output>
Create `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-05-SUMMARY.md` when done.
</output>
