---
phase: 24-pgvector-recalltool-semantic-recall-rewrite
plan: 05
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - services/memory/memory_service.py
  - tests/integration/test_pipeline_load_context_audit.py
autonomous: true
requirements: [MEM-10]
tags: [load-context-audit, semantic-shift, length-regression, token-delta-artifact, prompt-budget, integration-test, pgvector-marker, observational]
must_haves:
  truths:
    - "`MemoryService.load_context` docstring is extended with a Phase 24 / MEM-10 note acknowledging the `long_term_facts` semantic shift; method body is UNCHANGED (the rewrite lives entirely inside `_long.get_relevant_facts` per Plan 02 / PATTERNS analog 2)"
    - "At each of the 4 `load_context` call sites (services/pipeline.py:429, 608, 971, 1062), `len(mem_ctx.long_term_facts) <= settings.recall_top_k_default` (default 5) is verified by integration test"
    - "Each element of `mem_ctx.long_term_facts` is a bare `str` with no `- ` or `* ` prefix (Pitfall 3 regression)"
    - "A token-delta audit artifact is written to `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-MEM10-AUDIT.json` containing `{mean_tokens, p95_tokens, baseline_mean, baseline_p95, delta_mean, delta_p95, sample_size, methodology}` — observational only (NOT a gating test per D-B3)"
    - "Existing v1.0–v1.5 integration suite still passes (no new failures introduced by the semantic shift)"
  artifacts:
    - path: "services/memory/memory_service.py"
      provides: "Docstring-only edit on `load_context` (lines ~395-417) — no executable change; semantic-shift acknowledgement + pointer to MEM-10 audit gate"
      contains: ["Phase 24 / MEM-10", "long_term_facts", "load_context"]
    - path: "tests/integration/test_pipeline_load_context_audit.py"
      provides: "4 integration tests: 4-call-site length regression (parametrized), bare-str-no-prefix regression, token-delta artifact writer, v1.5 baseline-suite green-check"
      exports: ["test_load_context_facts_length_le_5_all_four_callsites", "test_load_context_returns_list_of_str_no_prefix", "test_writes_token_delta_artifact", "test_no_v1_5_regression"]
  key_links:
    - from: "services/memory/memory_service.py::MemoryService.load_context"
      to: "services/memory/memory_service.py::LongTermMemory.get_relevant_facts"
      via: "asyncio.gather(return_exceptions=True) await"
      pattern: "_long\\.get_relevant_facts"
    - from: "tests/integration/test_pipeline_load_context_audit.py"
      to: ".planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-MEM10-AUDIT.json"
      via: "Path(...).write_text(json.dumps(metrics))"
      pattern: "24-MEM10-AUDIT\\.json"
---

<objective>
Close the MEM-10 semantic-shift audit. Two surfaces:
(1) extend the docstring of `MemoryService.load_context` at `services/memory/memory_service.py` (lines ~395-417) with a Phase 24 / MEM-10 note acknowledging the `long_term_facts` semantic shift from popularity to query-relevance (method body UNCHANGED per PATTERNS analog 2 — the rewrite lives entirely inside `_long.get_relevant_facts`);
(2) create `tests/integration/test_pipeline_load_context_audit.py` with 4 tests: length-only regression at the 4 pipeline call sites (D-B3 — assertion-style, NOT a snapshot), bare-string-no-prefix regression (Pitfall 3), token-delta artifact writer (observational per D-B3 — produces `.planning/phases/24-.../24-MEM10-AUDIT.json` recording mean/p95 token deltas vs popularity baseline), and a v1.5 baseline-suite green-check.

Purpose: MEM-10 audit is the proof-of-no-regression for the semantic shift. The integration test asserts the LIST-SHAPE contract is preserved at every consumer, AND records the prompt-budget impact for ops visibility. Per D-B3, the token-delta artifact is observational (NOT gating) — a 30% delta does NOT fail the build; it just gets recorded. This intentionally avoids guessing a token-budget threshold without real eval data.

Output: 1 production docstring edit + 1 new integration test file (~250 LOC) + 1 JSON audit artifact produced at test runtime. No new production code; no test files in `tests/unit/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-CONTEXT.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-VALIDATION.md
@.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-02-SUMMARY.md
@services/memory/memory_service.py
@services/pipeline.py
@tests/conftest.py
@tests/integration/test_pgvector_filtered_recall.py
@tests/integration/test_extractor_e2e.py

<interfaces>
<!-- Symbols Plan 05 must align with. Extracted from working tree on 2026-05-16. -->

From services/memory/memory_service.py (current `load_context` at lines ~395-417):
```
async def load_context(
    self,
    session_id: str,
    user_id:    str,
    tenant_id:  str,
    query:      str,
) -> MemoryContext:
    """加载当前请求所需的全部记忆上下文。"""
    short_term, long_term_facts, user_profile = await asyncio.gather(
        self._short.get_history(session_id),
        self._long.get_relevant_facts(user_id, tenant_id, query),
        self._long.get_user_profile(user_id, tenant_id),
        return_exceptions=True,
    )
    return MemoryContext(
        session_id=session_id,
        user_id=user_id,
        tenant_id=tenant_id,
        short_term=short_term if isinstance(short_term, list) else [],
        long_term_facts=long_term_facts if isinstance(long_term_facts, list) else [],
        user_profile=user_profile if isinstance(user_profile, UserProfile) else None,
    )
```
- Method body: UNCHANGED in Plan 05 — only docstring extended per PATTERNS analog 2.
- The `isinstance(long_term_facts, list)` guard at line ~415 is the safety net: `asyncio.gather(return_exceptions=True)` may surface a `BaseException`; the guard converts to `[]`. PRESERVE this guard verbatim.

From services/pipeline.py (4 load_context call sites verified by grep):
- Line 429 — `mem_ctx = await self._memory.load_context(...)` inside `QueryPipeline.run` (legacy non-agentic path).
- Line 608 — same call inside another path (verify against working tree).
- Line 971 — inside `AgentQueryPipeline.run`.
- Line 1062 — inside `AgentQueryPipeline.run_streaming` (also `SwarmQueryPipeline.run` adjacent).

CONTEXT D-B3 + RESEARCH §Pitfall 6 say the audit is a LENGTH-ONLY regression assertion — NOT a snapshot of the popularity-baseline content. The semantic shift changes WHICH facts return; it MUST NOT change HOW MANY facts return.

From utils/models.py:
- `class MemoryContext` — frozen Pydantic V2 model with `long_term_facts: list[str]` field; length-cap not enforced at the model level (relies on `get_relevant_facts` LIMIT $4).

From tests/conftest.py:
- `PG_AVAILABLE` constant + `pgvector_pool` fixture (alias for `pg_pool`).
- Phase 23 added: `extractor_llm_mock`, `embedder_or_mock`, `clean_long_term_facts` fixtures — REUSABLE in Plan 05 for seeding + cleanup.

From tests/integration/test_extractor_e2e.py (Phase 23 — closest analog for end-to-end-with-real-PG pattern):
- pytestmark marker block; PG-skip-gracefully shape; seed-via-real-save_fact pattern.

For the token-delta measurement, use a simple character-count → estimated-token approximation: `est_tokens = sum(len(fact) // 4 for fact in facts)` — same heuristic used in Plan 06's backfill --dry-run formula per PATTERNS analog 7 §D-D4 cost-formula row. For the baseline (popularity-ranked content), the test re-issues the same recall against a temporary popularity-ranked SELECT (inline SQL in the test file) on the same seed data so the comparison is apples-to-apples. The test does NOT call a v1.5 git-checkout — instead it issues a popularity-baseline SELECT inline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 4 integration tests for MEM-10 audit</name>
  <files>tests/integration/test_pipeline_load_context_audit.py</files>
  <read_first>
    - services/memory/memory_service.py (lines 395-417 — `load_context` body + the `isinstance` guards)
    - services/pipeline.py (4 call sites at lines 429, 608, 971, 1062 — confirm exact call shape so the test can drive each path)
    - tests/integration/test_extractor_e2e.py (Phase 23 — full end-to-end pattern with real PG seed + cleanup)
    - tests/integration/test_pgvector_filtered_recall.py (lines 1-30 — marker + skipif idiom)
    - tests/conftest.py (lines 60-120 — `PG_AVAILABLE`, `pgvector_pool`, `clean_long_term_facts`, `embedder_or_mock` fixtures)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 5 — MEM-10 audit shape; analog 13 — integration mechanics; §No Analog Found row 4 — audit artifact write idiom)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-RESEARCH.md (Pitfall 3 + 6; §Validation Architecture MEM-10 rows at lines 805-806)
  </read_first>
  <behavior>
    - Test 1 (`test_load_context_facts_length_le_5_all_four_callsites`, parametrized over the 4 call sites): seed `long_term_facts` with 12 rows for `(user_id="test-mem10-u", tenant_id="test-mem10-t")` via real `save_fact` (Phase 23 embed-on-write covers the embedding). Call `await mem.load_context(session_id="s", user_id=..., tenant_id=..., query="frontend")` directly (NOT through pipeline.py — we are testing the contract at the seam consumed by the 4 sites). Assert `isinstance(mem_ctx.long_term_facts, list)`; `len(mem_ctx.long_term_facts) <= 5` (D-A4 default); `len(mem_ctx.long_term_facts) >= 0` (legitimate empty also OK). Parametrize over `["QueryPipeline.run", "QueryPipeline.run_streaming", "AgentQueryPipeline.run", "AgentQueryPipeline.run_streaming"]` — though the actual `load_context` call is identical at all 4 sites, parametrize for documentation traceability (each row maps 1:1 to a `services/pipeline.py` line).
    - Test 2 (`test_load_context_returns_list_of_str_no_prefix`): seed 5 facts. Call `load_context`. Assert every element of `mem_ctx.long_term_facts` `isinstance(x, str)` AND `not x.startswith("- ")` AND `not x.startswith("* ")` AND `not x.startswith("• ")` — Pitfall 3 regression. The bullet formatting belongs ONLY to RecallTool.run's `ToolResult.content`, NOT to the `MemoryContext.long_term_facts` shape.
    - Test 3 (`test_writes_token_delta_artifact`): seed 30 facts with varied lengths (some short, some long). For each of `N=10` queries (predefined fixture list of realistic queries: `["what frontend do I prefer?", "what database do I use?", "what's my favorite language?", ...]`), measure: (a) semantic-recall path tokens — call `mem.load_context(query=q)` then `sum(len(f) // 4 for f in mem_ctx.long_term_facts)`; (b) popularity-baseline tokens — issue the same WHERE + ORDER BY importance DESC, created_at DESC LIMIT 5 directly via `pool.fetch(...)` then `sum(len(r["fact"]) // 4 for r in rows)`. Aggregate mean + p95 across the 10 queries. Write the result to `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-MEM10-AUDIT.json` via `Path(...).write_text(json.dumps({"mean_tokens": ..., "p95_tokens": ..., "baseline_mean": ..., "baseline_p95": ..., "delta_mean": ..., "delta_p95": ..., "sample_size": 10, "methodology": "char-count // 4 estimate over 10 fixed queries; semantic_recall vs popularity_baseline on same seed"}, indent=2))`. NO assertion on the delta value itself — observational per D-B3.
    - Test 4 (`test_no_v1_5_regression`): collect+run a small known v1.5 integration test (`tests/integration/test_extractor_e2e.py` OR `tests/integration/test_pgvector_recall.py`) inline via `pytest.main([...])` or by importing + invoking a test function directly. Simpler approach: assert importing `services.pipeline` + instantiating `QueryPipeline()` + calling a recorded request shape produces a valid `GenerationResponse` — equivalent to the v1.5 baseline test_pipeline pattern. The point is to fail-fast if the semantic shift broke any upstream assumption.

    On the Plan 04 tree, Test 1 + 2 may already PASS (because Plan 02 preserved the `list[str]` shape). Test 3 will FAIL on first run because the audit artifact does not yet exist. Test 4's behavior depends on the chosen v1.5 baseline test — if any pre-existing test in the suite was relying on popularity ordering, Test 4 catches it. RED gate is mixed: Test 3 is the unambiguous RED.
  </behavior>
  <action>
    Create `tests/integration/test_pipeline_load_context_audit.py`. Env-var setdefault top + `from __future__ import annotations` + `pytestmark = [pytest.mark.integration, pytest.mark.pgvector, pytest.mark.skipif(not PG_AVAILABLE, reason="...")]` per analog 13 §Marker convention.

    Imports: `import json`; `from pathlib import Path`; `import pytest`; `from tests.conftest import PG_AVAILABLE`; `from services.memory.memory_service import LongTermMemory, get_memory_service`; `from utils.models import MemoryContext`.

    Use the existing `pgvector_pool` fixture for direct PG access (popularity-baseline SELECT in Test 3).
    Use `clean_long_term_facts` fixture (Phase 23 — `tests/conftest.py` fixture) for teardown OR define a local `try/finally` `DELETE FROM long_term_facts WHERE user_id LIKE 'test-mem10-%'`.

    For the seed-fact set in Test 3 (30 facts with varied lengths), generate synthetically: short facts (`"X likes Y"` ~10 chars), medium (`"User prefers React over Vue for frontend development"` ~50 chars), long (~150 chars). Distribute 10/10/10 across importance buckets {0.2/0.5/0.8} to exercise tie-break.

    For the 10-query fixture list in Test 3, pick queries that span diverse domains (frontend, database, lifestyle, work) so the semantic-recall path returns DIFFERENT facts per query. The baseline popularity-rank returns the SAME 5 facts regardless of query (popularity is query-independent) — that asymmetry is what the audit measures.

    For the artifact path, use `Path(__file__).resolve().parents[2] / ".planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-MEM10-AUDIT.json"` — relative to the test file's location (tests/integration/). Verify parent dir exists before write (Phase 24 dir created by /gsd-plan-phase already).

    Do NOT assert any specific token delta value — analog 5 anti-pattern + D-B3 ban gating threshold assertions.
    Do NOT use a snapshot fixture for the baseline content — analog 5 anti-pattern + Pattern 5; only LENGTH is asserted.
    Do NOT drop the `long_term_facts` table — analog 13 anti-pattern; scoped DELETE only.

    Run: `uv run pytest tests/integration/test_pipeline_load_context_audit.py --collect-only -q` — expect 4 items (Test 1 parametrize counts as 4 effective items if you parametrize, otherwise 4 functions). Run: `uv run pytest tests/integration/test_pipeline_load_context_audit.py -m pgvector -x -q` — RED expected primarily on Test 3 (artifact file does not exist yet at first run, but that test should PASS on first run because it CREATES the artifact; the RED signal is in production-side regression if Plan 02's rewrite broke length contract). On a CI host without PG, the test SKIPs gracefully.

    Commit: `test(24-05): RED gates for MEM-10 4-call-site audit + token-delta artifact`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_pipeline_load_context_audit.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/test_pipeline_load_context_audit.py` exists with 4 test functions (Test 1 may be parametrized — count as 4 effective items).
    - `uv run pytest tests/integration/test_pipeline_load_context_audit.py --collect-only -q` lists ≥ 4 items.
    - `grep -c 'pytest.mark.pgvector' tests/integration/test_pipeline_load_context_audit.py` ≥ 1.
    - `grep -c '24-MEM10-AUDIT.json' tests/integration/test_pipeline_load_context_audit.py` ≥ 1.
    - `grep -c 'DROP TABLE' tests/integration/test_pipeline_load_context_audit.py` equals 0 (scoped DELETE only).
    - `grep -c 'assert.*delta.*<' tests/integration/test_pipeline_load_context_audit.py` equals 0 (no threshold gating per D-B3).
    - `grep -c 'snapshot\|exact_match' tests/integration/test_pipeline_load_context_audit.py` equals 0 (length-only per analog 5).
    - `grep -E '^from __future__ import annotations' tests/integration/test_pipeline_load_context_audit.py` matches one line.
  </acceptance_criteria>
  <done>4 integration RED gates committed. Test file collects cleanly; SKIPs gracefully on CI without PG.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extend `load_context` docstring + run audit to GREEN + commit artifact</name>
  <files>services/memory/memory_service.py</files>
  <read_first>
    - services/memory/memory_service.py (lines 395-417 — `load_context` body; ensure no executable changes)
    - .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-PATTERNS.md (analog 2 §After block — docstring verbatim; analog 2 §Swap "Docstring addition only — no executable change")
    - tests/integration/test_pipeline_load_context_audit.py (Task 1 result — the 4 tests this task should turn GREEN)
  </read_first>
  <action>
    Edit `services/memory/memory_service.py` at the `load_context` method (lines ~395-417):

    1. REPLACE the existing docstring (currently `"""加载当前请求所需的全部记忆上下文。"""`) with the PATTERNS analog 2 §After version:
       ```
       """加载当前请求所需的全部记忆上下文。

       Phase 24 / MEM-10 — ``long_term_facts`` semantics flip from popularity
       to query-relevance (handled inside ``_long.get_relevant_facts``). The
       LIST SHAPE (``list[str]``, length ≤ 5) is PRESERVED — pipeline call
       sites at services/pipeline.py:429,608,971,1062 see no length regression.
       See ``tests/integration/test_pipeline_load_context_audit.py`` for the
       MEM-10 length-only regression gate + token-delta artifact.
       """
       ```

    2. DO NOT touch the method body. DO NOT touch the `asyncio.gather(return_exceptions=True)` call. DO NOT change the `isinstance(long_term_facts, list)` guard at line ~415 (analog 2 anti-pattern callout — `asyncio.gather(return_exceptions=True)` may surface `BaseException` and the guard is the defense).

    3. Run the integration test (if PG available) to write the audit artifact:
       `uv run pytest tests/integration/test_pipeline_load_context_audit.py -m pgvector -x -q`
       - Tests 1, 2, 4 should already PASS (Plan 02's rewrite preserved length and bare-string shape).
       - Test 3 produces `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-MEM10-AUDIT.json`.

    4. On a CI host without PG, the tests SKIP gracefully — the audit artifact will NOT be produced in that environment. Pre-tag check (mirror Phase 23 STATE.md guidance): operator runs the integration test once locally against PG to commit the artifact alongside the docstring edit.

    5. Stage AND commit the audit artifact file: `git add .planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-MEM10-AUDIT.json services/memory/memory_service.py tests/integration/test_pipeline_load_context_audit.py`. The audit file becomes part of the project record (D-B3 observational artifact lives in the planning dir, not in the code tree).

    Run: `uv run pytest tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q` — Plans 02 + 03 regression check.
    Run: `uv run ruff check services/memory/memory_service.py` — zero new violations.

    Commit: `feat(24-05): MEM-10 load_context docstring + token-delta audit artifact`.

    If the integration test cannot run against PG in the executing environment, fall back: docstring-only commit; mark the artifact-write step in the SUMMARY.md as a pre-tag manual verification (matching the Phase 23 SUMMARY.md "Pre-tag check" pattern at STATE.md line 91).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_pipeline_load_context_audit.py --collect-only -q && uv run pytest tests/unit/test_memory_recall_semantic.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Phase 24 / MEM-10' services/memory/memory_service.py` matches exactly one line (docstring landed).
    - `grep -n 'long_term_facts' services/memory/memory_service.py | wc -l` ≥ 3 (existing references + new docstring mention).
    - `grep -v '^#' services/memory/memory_service.py | grep -c 'def load_context'` equals 1 (no duplicate; method preserved).
    - `python -c "import inspect; from services.memory.memory_service import MemoryService; assert 'Phase 24 / MEM-10' in inspect.getdoc(MemoryService.load_context)"` exits 0.
    - `python -c "import inspect; src = inspect.getsource(__import__('services.memory.memory_service', fromlist=['MemoryService']).MemoryService.load_context); assert 'asyncio.gather' in src and 'return_exceptions=True' in src"` exits 0 (body preserved).
    - `uv run pytest tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q` exits 0 (Plans 02 + 03 still GREEN).
    - If PG available: `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-MEM10-AUDIT.json` exists AND parses as JSON AND contains keys `mean_tokens`, `p95_tokens`, `baseline_mean`, `baseline_p95`, `delta_mean`, `delta_p95`, `sample_size`, `methodology`.
    - If PG unavailable: SUMMARY.md notes the artifact write is a pre-tag manual verification.
    - `uv run ruff check services/memory/memory_service.py` exits 0.
  </acceptance_criteria>
  <done>MEM-10 GREEN (or GREEN + manual-pre-tag for the artifact write if PG unavailable in the executing environment). Docstring acknowledgement landed; integration suite verifies length contract; token-delta audit produced for ops visibility; Plans 02 + 03 regression preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `load_context` ↔ 4 pipeline call sites | List-shape contract is the implicit invariant; semantic shift changes WHICH facts return, NOT the shape; length-only regression gate is the load-bearing defense |
| Audit artifact ↔ `.planning/` tree | JSON written to planning dir at test time; observational only; no security surface (no secrets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-T1 | Tampering | Pre-formatted bullet strings leak into `MemoryContext.long_term_facts` | mitigate | Pitfall 3 + Test 2 (bare-string-no-prefix regression); RecallTool's bullet formatting lives ONLY in `ToolResult.content`, never in `MemoryContext` |
| T-24-05-I1 | Information Disclosure | Audit artifact contains user-attributed fact content | mitigate | Test 3 uses synthetic seed data (`"X likes Y"` placeholder facts under test user/tenant IDs `test-mem10-*`); no production data |
| T-24-05-D1 | Denial | Length contract regression breaks `_build_initial_messages` at 4 call sites | mitigate | Test 1 parametrized over all 4 sites; Pitfall 6 mitigation; default `limit=5` enforced by Plan 02's pgvector LIMIT |
| T-24-05-D2 | Denial | Token-delta exceeds prompt-budget at production scale | accept | Observational artifact per D-B3 — operators monitor; v1.7+ remediation paths documented in CONTEXT (B1 option 2: shrink `load_context` K; B2 option 2: dedup at merge) |
| T-24-05-R1 | Repudiation | Audit not produced on CI without PG | accept | Pre-tag manual verification clause per Phase 23 SUMMARY.md precedent |
| T-24-05-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages (docstring + integration test only) |
</threat_model>

<verification>
- 4 integration tests run (or SKIP gracefully on CI without PG).
- `load_context` body untouched; only docstring extended.
- Token-delta audit artifact produced when PG available; pre-tag manual verification otherwise.
- Plans 02 + 03 unit suites remain GREEN.
- v1.5 baseline integration tests (whatever the Task 4 chosen set) remain GREEN.

Run gate: `uv run pytest tests/integration/test_pipeline_load_context_audit.py -m pgvector --collect-only -q && uv run pytest tests/unit/test_memory_recall_semantic.py tests/unit/test_recall_tool.py -x -q`
</verification>

<success_criteria>
- ROADMAP SC-5 complete: 4-site length regression + token-delta artifact + no new v1.0-v1.5 test failures.
- MEM-10 requirement complete: semantic-shift documented in code (docstring) + audit gate (integration test) + ops-visible artifact (JSON in planning dir).
- D-B3 observational-only constraint respected: no gating threshold on token delta.
</success_criteria>

<output>
Create `.planning/phases/24-pgvector-recalltool-semantic-recall-rewrite/24-05-SUMMARY.md` when done.
</output>
