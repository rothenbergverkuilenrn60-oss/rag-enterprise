---
phase: 20-websearchtool-real-implementation-tavily
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.py
  - requirements.txt
  - .env.docker
autonomous: true
requirements: [AGENT-10]
tags: [config, tavily, settings, dependency-pin, env-placeholder]

must_haves:
  truths:
    - "`Settings` exposes `tavily_api_key`, `tavily_search_depth`, `tavily_max_results` with documented defaults"
    - "`tavily-python>=0.7.24,<0.8` is a tracked dependency"
    - "`.env.docker` carries a `TAVILY_API_KEY=${TAVILY_API_KEY:-}` placeholder line (never a real key)"
  artifacts:
    - path: "config/settings.py"
      provides: "3 new Pydantic V2 BaseSettings fields adjacent to anthropic_api_key"
      contains: "tavily_api_key"
    - path: "requirements.txt"
      provides: "Tavily SDK pin"
      contains: "tavily-python>=0.7.24,<0.8"
    - path: ".env.docker"
      provides: "Empty TAVILY_API_KEY placeholder using ${TAVILY_API_KEY:-} substitution"
      contains: "TAVILY_API_KEY=${TAVILY_API_KEY:-}"
  key_links:
    - from: "config/settings.py"
      to: "services/agent/tools/web_search.py (Plan 20-02)"
      via: "module-level `from config.settings import settings` import"
      pattern: "tavily_api_key"
    - from: ".env.docker"
      to: "Settings env-var binding (case-insensitive name match)"
      via: "Pydantic V2 BaseSettings auto-load"
      pattern: "TAVILY_API_KEY"
---

<objective>
Land the configuration foundation Plan 20-02 depends on: three new Pydantic V2 settings fields adjacent to `anthropic_api_key`, the `tavily-python` SDK pin in `requirements.txt`, and the empty-by-default `TAVILY_API_KEY` placeholder in `.env.docker`. No code paths consume these yet — that wiring is Plan 20-02.

Purpose: per CONTEXT D-06, `WebSearchTool` reads settings via module-level `settings = get_settings()` import; those fields must exist before the tool body is rewritten. Per CONTEXT D-07, `ToolContext` is NOT extended (would ripple to every tool). Per P-16, `${TAVILY_API_KEY:-}` substitution prevents real keys from being committed to `.env.docker`.

Output: 3 new settings fields (5 lines), 1 dependency line in `requirements.txt`, 3 lines in `.env.docker` (section header + placeholder + 2 optional knobs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-websearchtool-real-implementation-tavily/20-CONTEXT.md
@.planning/phases/20-websearchtool-real-implementation-tavily/20-PATTERNS.md
@CLAUDE.md
@config/settings.py
@requirements.txt
@.env.docker

<interfaces>
<!-- Existing settings adjacency. Extracted from config/settings.py:268-275. -->
<!-- The new tavily fields go directly after anthropic_model. -->

From config/settings.py (current shape, lines 268-275):
```python
ollama_base_url:    str   = "http://localhost:11434"
ollama_model:       str   = "qwen2.5:14b"
ollama_model_path:  Path  = MODEL_DIR / "ollama_model"
openai_api_key:     str   = ""
openai_base_url:    str   = ""
openai_model:       str   = "gpt-4o"
anthropic_api_key:  str   = ""
anthropic_model:    str   = "claude-sonnet-4-6"
```

Settings class config (lines ~50-55):
```python
extra="ignore",
case_sensitive=False,
```
→ `TAVILY_API_KEY` env var binds to `tavily_api_key` field automatically; no `Field(alias=)` needed.

From requirements.txt (current shape, lines 50-51):
```
openai==1.59.6               # OpenAI Embedding / Chat
anthropic==0.43.0            # Anthropic Claude
```

From .env.docker (current shape, lines 25-27):
```
# Anthropic (optional)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 Tavily settings fields to config/settings.py</name>
  <files>config/settings.py</files>
  <read_first>
    - config/settings.py (lines 268-280 — see existing openai_api_key / anthropic_api_key shape; need to verify exact insertion line)
    - .planning/phases/20-websearchtool-real-implementation-tavily/20-CONTEXT.md (D-06 — three field names, types, defaults)
    - .planning/phases/20-websearchtool-real-implementation-tavily/20-PATTERNS.md ("config/settings.py" section — exact code block to insert)
  </read_first>
  <action>
Add three new Pydantic V2 `BaseSettings` fields directly after the `anthropic_model:` line (currently `config/settings.py:275`). Match the existing alignment style (column-aligned `:` and `=`). Field definitions per CONTEXT D-06:

```python
# Tavily web search (Phase 20, AGENT-10)
tavily_api_key:        str = ""
tavily_search_depth:   str = "basic"   # SDK accepts: basic | fast | advanced | ultra-fast
tavily_max_results:    int = 5
```

Constraints:
- Do NOT introduce a `TavilySettings` sub-model (single-Settings convention preserved per D-06).
- Do NOT add a `field_validator` — empty `tavily_api_key` is observable behavior (D-03), not a startup error.
- Do NOT touch `extra="ignore"` / `case_sensitive=False` (already correct for `TAVILY_API_KEY` env binding).
- Do NOT add any `Field(alias=...)` — case-insensitive match handles `TAVILY_API_KEY` ↔ `tavily_api_key` automatically.
- Insert location: after `anthropic_model:    str   = "claude-sonnet-4-6"` (current line 275), BEFORE the `# Azure OpenAI` comment block.
  </action>
  <verify>
    <automated>python -c "from config.settings import settings; assert hasattr(settings, 'tavily_api_key'), 'tavily_api_key missing'; assert hasattr(settings, 'tavily_search_depth'), 'tavily_search_depth missing'; assert hasattr(settings, 'tavily_max_results'), 'tavily_max_results missing'; assert settings.tavily_api_key == '', f'expected empty default, got {settings.tavily_api_key!r}'; assert settings.tavily_search_depth == 'basic'; assert settings.tavily_max_results == 5; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tavily_api_key:" config/settings.py` returns exactly 1 match.
    - `grep -n "tavily_search_depth:" config/settings.py` returns exactly 1 match.
    - `grep -n "tavily_max_results:" config/settings.py` returns exactly 1 match.
    - `grep -c "tavily_api_key" config/settings.py` returns `1` (no duplicates).
    - The Python verify command above prints `OK`.
    - `mypy --strict config/settings.py` reports zero NEW errors compared to baseline (296 errors per CONTEXT code_context — Phase 20 contributes 0 net new).
    - `ruff check config/settings.py` passes (zero warnings on the touched lines).
  </acceptance_criteria>
  <done>3 new fields present in `config/settings.py` adjacent to `anthropic_model`; defaults match D-06 (`""`, `"basic"`, `5`); `from config.settings import settings` succeeds and the new attributes are reachable.</done>
</task>

<task type="auto">
  <name>Task 2: Pin tavily-python in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (lines 50-51 — see openai/anthropic adjacent pin style)
    - .planning/phases/20-websearchtool-real-implementation-tavily/20-CONTEXT.md ("Tavily client lifecycle" — version range source: STACK.md `tavily-python>=0.7.24,<0.8`)
    - .planning/phases/20-websearchtool-real-implementation-tavily/20-PATTERNS.md ("requirements.txt" section)
  </read_first>
  <action>
Append a Tavily section header + pin line directly AFTER the `anthropic==0.43.0` line (current line 51). Use the existing inline-comment style:

```
# ── Web search ────────────────────────────────────────────────────────────────
tavily-python>=0.7.24,<0.8   # Tavily AsyncTavilyClient (Phase 20, AGENT-10)
```

Constraints:
- Pin range is `>=0.7.24,<0.8` — sourced from CONTEXT canonical_refs → `.planning/research/STACK.md`. Do NOT broaden to `>=0.7` or pin to a single exact version (`==0.7.24` would block patch upgrades).
- Place between `anthropic==0.43.0` (line 51) and the next section header. Do NOT scatter the pin elsewhere in the file.
- Do NOT install the package as part of this task — installation is the executor's responsibility per repo install workflow; the pin is the artifact.
- Do NOT remove the `tenacity==9.0.0` pin (line 21 — already present, used by Plan 20-02 inner helper).
  </action>
  <verify>
    <automated>grep -c "^tavily-python>=0.7.24,<0.8" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^tavily-python>=0.7.24,<0.8" requirements.txt` returns `1`.
    - `grep -c "^tavily" requirements.txt` returns `1` (single Tavily pin, no duplicates).
    - The pin line appears AFTER the `anthropic==0.43.0` line (verify with `awk '/^anthropic==/{anth=NR} /^tavily-python/{tav=NR} END{exit !(tav>anth)}' requirements.txt && echo OK`).
    - File still ends with a single trailing newline (no missing-newline diff noise).
  </acceptance_criteria>
  <done>`tavily-python>=0.7.24,<0.8` pin present immediately after `anthropic==0.43.0`; section header matches existing comment style (`# ── ... ─────`).</done>
</task>

<task type="auto">
  <name>Task 3: Add TAVILY_API_KEY placeholder to .env.docker</name>
  <files>.env.docker</files>
  <read_first>
    - .env.docker (lines 25-27 — see ANTHROPIC_API_KEY empty-placeholder pattern)
    - .planning/phases/20-websearchtool-real-implementation-tavily/20-CONTEXT.md (D-15 redaction rule + P-16 prevention — `${TAVILY_API_KEY:-}` substitution)
    - .planning/phases/20-websearchtool-real-implementation-tavily/20-PATTERNS.md (".env.docker" section — exact append shape)
  </read_first>
  <action>
Append three lines AFTER the `ANTHROPIC_MODEL=claude-sonnet-4-6` line (current line 27). Use a section header to mirror the existing layout:

```
# ── Web search: Tavily ────────────────────────────────────────────────────────
TAVILY_API_KEY=${TAVILY_API_KEY:-}
TAVILY_SEARCH_DEPTH=basic
TAVILY_MAX_RESULTS=5
```

Constraints:
- The literal value of `TAVILY_API_KEY` MUST be `${TAVILY_API_KEY:-}` (empty default substitution). NEVER a real key. NEVER `tvly-...`. Per P-16 + CONTEXT D-15 the placeholder protects against accidental commits.
- `TAVILY_SEARCH_DEPTH=basic` and `TAVILY_MAX_RESULTS=5` reflect the Settings defaults from Task 1 — no behavioral effect when unset, present for documentation.
- Do NOT write any actual Tavily key (verified by SC5 grep gate in Plan 20-05).
- Insertion point: after `ANTHROPIC_MODEL=claude-sonnet-4-6` (line 27), BEFORE the `# ── Embedding` block.
  </action>
  <verify>
    <automated>grep -c '^TAVILY_API_KEY=\${TAVILY_API_KEY:-}$' .env.docker</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^TAVILY_API_KEY=\${TAVILY_API_KEY:-}$' .env.docker` returns `1` (literal `${TAVILY_API_KEY:-}` substitution, exact match).
    - `grep -c '^TAVILY_SEARCH_DEPTH=basic$' .env.docker` returns `1`.
    - `grep -c '^TAVILY_MAX_RESULTS=5$' .env.docker` returns `1`.
    - `grep -c "tvly-" .env.docker` returns `0` (no real key prefix).
    - `grep -nE 'TAVILY_API_KEY=.+[^}]$' .env.docker` returns ZERO matches (defends against any future paste of a real key).
  </acceptance_criteria>
  <done>3 new lines + section header present after `ANTHROPIC_MODEL`; `TAVILY_API_KEY` value is the literal substitution placeholder; no `tvly-` prefix anywhere in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → Git history | `.env.docker` and `requirements.txt` are tracked; any committed value persists in history |
| Env var → Settings → Tool runtime | `TAVILY_API_KEY` enters the process via Pydantic V2 BaseSettings env loader; the value reaches `services/agent/tools/web_search.py` (Plan 20-02) and the Tavily SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | I (Information disclosure) | `.env.docker` real-key paste | mitigate | `${TAVILY_API_KEY:-}` substitution as the only form ever committed; Plan 20-05 adds pre-commit `tvly-` grep hook + secret-redaction smoke test enforcing absence in tracked files |
| T-20-02 | I | `requirements.txt` package-name typosquat (e.g. `tavily-pythn`) | mitigate | Pin uses the exact PyPI name `tavily-python` per RESEARCH STACK.md; pin range `>=0.7.24,<0.8` chosen from a vendor-published version that matches the documented `AsyncTavilyClient` surface |
| T-20-03 | T (Tampering) | Settings field default override | accept | Empty `tavily_api_key=""` triggers the `tavily_disabled` short-circuit (Plan 20-02) — no privileged behavior reachable when the key is missing |
| T-20-04 | D (Denial of service) | `tavily_max_results=5` upper bound on Tavily request size | accept | Default of 5 matches Tavily SDK's own default; future per-tenant cap is out of scope per CONTEXT deferred (v1.6+) |
</threat_model>

<verification>
- `python -c "from config.settings import settings; print(settings.tavily_api_key, settings.tavily_search_depth, settings.tavily_max_results)"` exits 0 and prints `  basic 5`.
- `grep -c "tavily-python>=0.7.24,<0.8" requirements.txt` returns `1`.
- `grep -c "^TAVILY_API_KEY=" .env.docker` returns `1` and the value is the literal substitution `${TAVILY_API_KEY:-}`.
- `grep -rn "tvly-" .env.docker requirements.txt config/settings.py` returns zero results.
- `ruff check config/settings.py` and `mypy --strict config/settings.py` show zero new errors vs baseline.
</verification>

<success_criteria>
- AGENT-10 contribution (settings shape): 3 fields landed with the exact names, types, and defaults specified by CONTEXT D-06; foundation for Plan 20-02 in place.
- Zero net-new code paths consume the settings yet — Plan 20-02 wires them. This plan is foundation-only.
- Zero secrets committed: `${TAVILY_API_KEY:-}` substitution + the `tvly-` grep across the three touched files returns zero matches.
- Behavioral parity: nothing else in `Settings` / `requirements.txt` / `.env.docker` changes; Phase 19 and earlier behavior is byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/20-websearchtool-real-implementation-tavily/20-01-SUMMARY.md` documenting:
- The 3 fields added (with exact line numbers in `config/settings.py`).
- The pin line + section header added in `requirements.txt`.
- The 3 lines added to `.env.docker` (placeholder + 2 default knobs).
- Confirmation of the `tvly-` grep returning zero results in tracked files.
- Hand-off note: Plan 20-02 imports `from config.settings import settings` at module top of `services/agent/tools/web_search.py` and reads `settings.tavily_api_key` / `settings.tavily_search_depth` / `settings.tavily_max_results` inside `_tavily_search` and the `tavily_disabled` short-circuit branch of `run()`.
</output>
