---
plan_id: 30-03
phase: 30
plan: 03
wave: 3
type: execute
depends_on: [30-00, 30-01, 30-02]
requirements: [MYPY-01]
files_modified:
  - config/settings.py
  - (repo-wide silenced sites — enumerated at Task 1, cap = 25)
  - deferred-items.md (overflow capture)
autonomous: true
must_haves:
  truths:
    - "`uv run mypy --strict config/settings.py` returns 'Success: no issues found in 1 source file'."
    - "Repo-wide `uv run mypy --strict` sweep: NET error count reduces — Phase 30 either fixes or silences up to 25 violations against the Phase 29 baseline of 40 pre-existing errors."
    - "Every silence uses `# type: ignore[error-code]` + single-line `# why:` comment. Bare `# type: ignore` (no error code) is NOT introduced anywhere."
    - "Sweep overflow (violations beyond cap=25) captured in `deferred-items.md` for v1.9 — count + per-file breakdown."
    - "Phase 30 introduces ZERO new mypy --strict violations outside MYPY-01 silenced sites (compared against Phase 29 baseline of 40 errors)."
  artifacts:
    - path: "config/settings.py"
      provides: "`embedding_ensemble` annotated as `list[dict[str, Any]]` (line 154 — named MYPY-01 site)"
      contains: "embedding_ensemble: list[dict[str, Any]]"
    - path: "deferred-items.md"
      provides: "v1.9 candidate list — overflow mypy violations beyond cap=25"
      contains: "MYPY-01 overflow"
  key_links:
    - from: "config/settings.py:154"
      to: "Pydantic V2 Field default + Any type"
      via: "parametric annotation `list[dict[str, Any]] = []`"
      pattern: "embedding_ensemble:\\s*list\\[dict\\[str, Any\\]\\]"
    - from: "any silenced site"
      to: "rationale"
      via: "single-line `# why:` comment immediately after `# type: ignore[error-code]`"
      pattern: "# type: ignore\\[[a-z-]+\\].*# why:"
---

<objective>
Run mypy --strict cleanup as the LAST plan in Phase 30, after 30-00 / 30-01 / 30-02 have landed their test-fixture changes (so the sweep catches any new violations they introduced). Fix the named site `config/settings.py:154` (`embedding_ensemble: list[dict] = []` → `list[dict[str, Any]] = []`). Then run a bounded repo-wide sweep — cap = 25 violations fixed OR silenced. Silences MUST use `# type: ignore[error-code]` + `# why:` (bare ignore prohibited). Overflow beyond cap → `deferred-items.md` for v1.9.

Purpose: REQUIREMENTS.md MYPY-01 acceptance plus CONTEXT.md §MYPY-01 bounded scope. Phase 29 verification recorded mypy --strict baseline = 40 pre-existing errors as of 2026-05-17. Phase 30 must measure NET reduction (Phase-29-baseline − fixed-or-silenced) AND not introduce new violations.

Output: `config/settings.py:154` fixed; up to 25 additional sites fixed or silenced with disciplined rationale; overflow captured; mypy --strict sweep summary in 30-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-test-infra-mypy-hardening/30-CONTEXT.md
@.planning/phases/29-toctou-silent-skip-enforcement/29-VERIFICATION.md
@config/settings.py
@CLAUDE.md
@Claude.md

<interfaces>
<!-- MYPY-01 silencing convention (locked by CONTEXT.md §MYPY-01 silencing convention) -->

Required silence format (every silenced site MUST match this regex):
```
# type: ignore[<specific-error-code>]  # why: <single-line rationale>
```

NOT allowed:
- `# type: ignore` (no error code — too broad, hides future regressions)
- `# type: ignore[error-code]` without `# why:` (rationale missing)

Examples (from CONTEXT.md):
```
result = some_untyped_lib.call()  # type: ignore[no-any-return]  # why: third-party lib lacks stubs as of 2026-05
```

Fix preference order (per CONTEXT.md MYPY-01 scope decision):
1. Add the missing parametric annotation (cheapest, e.g., `list[dict]` → `list[dict[str, Any]]`).
2. Tighten with Pydantic V2 sub-model if upstream consumers permit.
3. Silence with disciplined `# type: ignore[error-code] # why:` only if (1) and (2) infeasible.

Phase 29 baseline (per `.planning/phases/29-toctou-silent-skip-enforcement/29-VERIFICATION.md`):
- mypy --strict repo-wide: 40 pre-existing errors as of 2026-05-17.

Phase 30 success metric (locked):
- After this plan: `errors_after ≤ 40 − min(25, fixable_count)`. Plan caps fixes at 25; overflow deferred.
- ZERO new errors introduced by Phase 30 (compared against Phase 29 baseline).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 — Establish baseline + fix named site `config/settings.py:154`</name>
  <read_first>
    - config/settings.py (lines 140-170 — context around `embedding_ensemble`)
    - .planning/phases/29-toctou-silent-skip-enforcement/29-VERIFICATION.md (mypy baseline)
    - .planning/phases/30-test-infra-mypy-hardening/30-CONTEXT.md §MYPY-01
  </read_first>
  <action>
    1. Capture pre-Plan-30-03 baseline: `uv run mypy --strict . 2>&1 | tee /tmp/30-03-pre.log | tail -3`. Extract error count → `BASELINE_ERRORS`. Confirm `BASELINE_ERRORS` is approximately 40 (per Phase 29 verification). If materially different (< 30 or > 60), halt and surface — Plans 30-00 / 30-01 / 30-02 may have introduced new errors that must be regressed first.
    2. Confirm `config/settings.py:154` violation in `/tmp/30-03-pre.log`. The line is `embedding_ensemble: list[dict] = []`. Expected mypy message: missing type parameters or `Missing type parameters for generic type "dict"`.
    3. Apply named-site fix:
       - Line 154 in `config/settings.py`: replace `embedding_ensemble: list[dict] = []` with `embedding_ensemble: list[dict[str, Any]] = []`.
       - Ensure `Any` is imported. Read top of `config/settings.py` — if `from typing import Any` is absent, add it (alphabetize with existing typing imports).
       - Update the inline Chinese comment to match the new type: the existing comment block lines 150-152 documents the multi-model shape `[{"provider": "ollama", "weight": 0.6}, ...]` — this still matches `dict[str, Any]`; do not rewrite the comment unless the type changed semantically.
    4. Run named-site check: `uv run mypy --strict config/settings.py 2>&1 | tail -3`. **Required output: "Success: no issues found in 1 source file"** (per ROADMAP SC-4 + REQUIREMENTS.md MYPY-01 acceptance).
    5. If `mypy --strict config/settings.py` surfaces OTHER violations in the same file (not at line 154), include those in the cap=25 budget — fix them now if cheap (≤ 2 mins per fix), defer to Task 1 sweep otherwise.
    6. Re-run repo-wide sweep: `uv run mypy --strict . 2>&1 | tee /tmp/30-03-post-task0.log | tail -3`. `BASELINE_ERRORS - errors_after_task0` must be ≥ 1 (the named site is fixed). Halt if NEW errors surfaced (i.e., `errors_after_task0 > BASELINE_ERRORS - 1`) — root-cause before proceeding.
    7. Commit named-site fix: `chore(30-03): fix MYPY-01 named site config/settings.py:154 (list[dict] → list[dict[str, Any]])`.
  </action>
  <verify>
    <automated>uv run mypy --strict config/settings.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mypy --strict config/settings.py` returns `Success: no issues found in 1 source file`.
    - `from typing import Any` import present in `config/settings.py` (added if missing).
    - Pre-Plan baseline captured in `/tmp/30-03-pre.log` — approximately matches Phase 29 figure of 40.
    - Post-Task-0 sweep shows NET reduction of at least 1 (the named site).
    - Zero new errors introduced compared to baseline.
    - Commit message follows `chore(30-03):` convention.
  </acceptance_criteria>
  <done>Named site fixed; named-file mypy clean; baseline + post-Task-0 deltas captured.</done>
</task>

<task type="auto">
  <name>Task 1 — Bounded repo-wide sweep (cap = 25 violations fixed OR silenced)</name>
  <read_first>
    - /tmp/30-03-post-task0.log (mypy violations remaining after Task 0)
    - .planning/phases/30-test-infra-mypy-hardening/30-CONTEXT.md §MYPY-01 silencing convention
  </read_first>
  <action>
    1. Parse `/tmp/30-03-post-task0.log` into a list of `(file, line, error_code, message)` tuples. Group by file to minimize context-switching during fixes.
    2. Prioritize FIXING over silencing (CONTEXT.md fix-preference order). Apply step-by-step, max 25 fixes-or-silences total:
       - **Cheap fix (preferred):** missing parametric annotations like `list[dict]` → `list[dict[str, Any]]`, `dict[str, list]` → `dict[str, list[Any]]`, `tuple` → `tuple[X, Y]`. Use `Any` only when a more specific type is not deterministic from caller usage.
       - **Pydantic V2 sub-model tightening:** if a `list[dict[str, Any]]` actually carries a known shape, define a Pydantic sub-model. Allowed only if the upstream caller surface clearly supports it; otherwise fall back to `Any`.
       - **Disciplined silence (last resort):** `# type: ignore[<specific-error-code>]  # why: <reason>` immediately after the offending line. Reasons that justify a silence (non-exhaustive):
         - Third-party lib lacks stubs as of 2026-05 (e.g., `paddleocr`, `paddlepaddle`).
         - Dynamic Pydantic V2 model construct that mypy can't trace.
         - Intentional `Any`-typed return from a `**kwargs` passthrough.
       - **NEVER use bare `# type: ignore`** (no error code) — CONTEXT.md prohibits this.
    3. After every batch of ~5 fixes-or-silences, re-run `uv run mypy --strict . 2>&1 | tail -3` to confirm error count is decreasing (or at worst static — never increasing).
    4. **Cap gate.** When count of fixes-or-silences applied this task reaches **25**, STOP. Do not exceed. Record what remains in `/tmp/30-03-overflow.txt` (preserve the `(file, line, error_code, message)` shape).
    5. **Disciplined-silence audit gate.** After all 25 changes applied, run:
       ```
       git diff | grep -E "# type: ignore" | grep -v "# why:" | wc -l
       ```
       **Expected: 0** (every silence must have a `# why:` rationale on the same line). If non-zero, the offending line(s) violate convention — go back and add `# why:` to each.
       Also run (plan-review T1 — regex catches EOL bare-ignores too):
       ```
       git diff | grep -E "# type: ignore($|[^\\[])" | wc -l
       ```
       **Expected: 0** (bare ignore without error code prohibited per CONTEXT.md — covers both `# type: ignore<eol>` AND `# type: ignore <space-or-other>` cases). If non-zero, replace with specific error code from mypy output.
    6. Final repo sweep: `uv run mypy --strict . 2>&1 | tee /tmp/30-03-post-task1.log | tail -3`. Compute NET reduction: `BASELINE_ERRORS - errors_now`. Document in scratch notes for SUMMARY.
    7. Production-behavior guard: for any silence applied inside `services/`, `controllers/`, `utils/`, verify no behavioral change — `git diff <file>` should show ONLY annotation/comment additions, never code-flow changes. If a behavioral change snuck in, halt and revert that hunk.
  </action>
  <verify>
    <automated>uv run mypy --strict . 2>&amp;1 | tail -3 &amp;&amp; git diff | grep -E "# type: ignore" | grep -cv "# why:" &amp;&amp; ! git diff | grep -E "# type: ignore(\$|[^\\[])"</automated>
  </verify>
  <acceptance_criteria>
    - Number of fixes-or-silences applied this task ≤ **25**.
    - Every silenced site uses `# type: ignore[<specific-error-code>]  # why: <reason>` (verified via diff grep — both gates return 0 bad-pattern matches).
    - Repo-wide `uv run mypy --strict .` error count is strictly less than `BASELINE_ERRORS` (NET reduction confirmed).
    - Zero new errors introduced.
    - Per-silence rationale is meaningful (not "ignored", not "mypy is wrong" — must cite specific upstream constraint).
    - Behavioral hunks (code-flow changes) NOT introduced under `services/`, `controllers/`, `utils/` — diff there shows ONLY annotation/comment additions.
  </acceptance_criteria>
  <done>Sweep applied within cap; silences disciplined; NET mypy reduction confirmed; no new violations.</done>
</task>

<task type="auto">
  <name>Task 2 — Overflow capture in `deferred-items.md` + diff-cover/coverage gates + final summary inputs</name>
  <read_first>
    - /tmp/30-03-overflow.txt (residual violations from Task 1)
    - /tmp/30-03-post-task1.log (final mypy state)
    - deferred-items.md (if exists; create if not — repo root)
  </read_first>
  <action>
    1. Check for `deferred-items.md`:
       - `ls deferred-items.md 2>&1`. If absent, create it with a 2-line header documenting purpose ("v1.8+ deferred items captured during phase execution; format: H2 per item, bullet per concrete site").
       - If present, append; do not overwrite existing entries.
    2. Append overflow section. Format:
       ```
       ## MYPY-01 overflow (deferred to v1.9)

       Phase 30 cap = 25 violations fixed/silenced. Residual count: <N>. Captured 2026-05-17.

       ### Files
       - `<file>:<line>` — `<error-code>` — `<message>`
       - ...
       ```
       Include EVERY line from `/tmp/30-03-overflow.txt`. Do not summarize; v1.9 planner needs the full surface.
    3. Diff-cover on touched production-code files (silenced sites only — fixes that added annotations are typically annotation-only and may not register coverage delta):
       `uv run diff-cover coverage.xml --compare-branch=master --fail-under=80`.
       If gate fails on `config/settings.py` (the named site fix) due to annotation-only changes not registering coverage data, document "diff-cover N/A — annotation-only change" in commit body. Same for any other file with annotation-only diffs.
    4. Combined coverage floor: `uv run coverage report --fail-under=70`. Must not regress vs Phase 29 baseline. Halt if it does.
    5. INSERT-ONLY `audit_log` invariant guard: `grep -rn "audit_log" services/audit*.py services/memory/memory_service.py | grep -E "UPDATE|DELETE" | grep -v "REVOKE\\|test_\\|comment\\|#"`. **Expected: 0 matches.** (Plan 30-03 should not touch audit_log; this is a defense-in-depth assertion.)
    6. Phase 29 carry-forward guard: `grep -n "_bulk_near_duplicate_check_raw" services/memory/memory_service.py`. **Expected: ≥ 1 match** (the helper still exists with the post-29-00 name). If `_bulk_near_duplicate_check` (no suffix) appears in any diff under `services/memory/`, halt and revert — CONTEXT.md "Carry-Forward Decisions" row requires the `_raw` suffix.
    7. Final full-suite confidence run (since 30-03 runs LAST in Phase 30 — confirm 30-00 + 30-01 + 30-02 still green):
       `uv run pytest tests/unit/ tests/integration/ -m 'not benchmark' 2>&1 | tail -10`. Must exit 0 (PG required for integration suite — if PG unavailable, document scope of confirmation).
    8. Pull together Task 0 + Task 1 + Task 2 data points for SUMMARY:
       - `BASELINE_ERRORS` (from Task 0).
       - `errors_after_task0`, `errors_after_task1` (NET reduction trail).
       - Fix-vs-silence ratio (e.g., "18 fixed / 7 silenced").
       - Overflow count and `deferred-items.md` append diff.
       - Confirmation of zero new violations.
       - Confirmation that bare `# type: ignore` and missing `# why:` gates returned 0.
  </action>
  <verify>
    <automated>uv run mypy --strict config/settings.py &amp;&amp; uv run coverage report --fail-under=70 &amp;&amp; ! git diff | grep -E "# type: ignore(\$|[^\\[])"</automated>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` exists at repo root with MYPY-01 overflow section appended.
    - Every residual violation from `/tmp/30-03-overflow.txt` listed with file:line + error-code + message.
    - `uv run mypy --strict config/settings.py` still passes (Task 0 result preserved).
    - Repo-wide NET reduction in mypy --strict error count confirmed (≥ Task 1 figure).
    - Combined coverage `--fail-under=70` preserved.
    - INSERT-ONLY `audit_log` invariant preserved (defense-in-depth grep clean).
    - `_bulk_near_duplicate_check_raw` carry-forward preserved (grep finds the `_raw`-suffixed name in `services/memory/memory_service.py`).
    - Full test suite (`tests/unit/` + `tests/integration/`) exits 0 — Phase 30 plans 30-00 / 30-01 / 30-02 still green (PG-availability noted if integration suite scope reduced).
    - No bare `# type: ignore` introduced; every silence has `# why:`.
  </acceptance_criteria>
  <done>Overflow captured; gates clean; SUMMARY inputs gathered; full Phase 30 still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static-type-check ↔ production code | mypy --strict silences can hide real type bugs if rationale is missing or bare; cap-bounded sweep + mandatory `# why:` constrains drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Repudiation (silent type drift) | `# type: ignore` accumulation | mitigate | Mandatory `# type: ignore[error-code]` + single-line `# why:` rationale per CONTEXT.md §MYPY-01; Task 1 audit-gate enforces both via diff grep (bare ignore + missing why both must return 0). |
| T-30-03-02 | Tampering (scope creep) | repo-wide sweep | mitigate | Hard cap = 25 violations per Phase 30; overflow captured in `deferred-items.md` for v1.9. Prevents unbounded refactor consuming Phase 30. |
| T-30-03-03 | Tampering (production-behavior change masquerading as type fix) | `services/`, `controllers/`, `utils/` diff under silence application | mitigate | Task 1 step 7 requires `git diff <file>` to show ONLY annotation/comment additions; behavioral hunks halt and revert. |
| T-30-03-SC | Tampering (supply chain) | n/a | accept | MYPY-01 adds no new package installs. Stub packages (e.g., `types-foo`) — if needed — are out of scope; Phase 30 silences with `# why: stubs unavailable` instead. |
</threat_model>

<verification>
- `uv run mypy --strict config/settings.py` returns `Success: no issues found in 1 source file`.
- Repo-wide `uv run mypy --strict .` shows NET error-count reduction vs Phase 29 baseline of 40 — Phase 30 either fixes or silences up to 25 violations.
- Zero new mypy --strict violations introduced by Phase 30 outside MYPY-01 silenced sites.
- Every silenced site matches the regex `# type: ignore\[[a-z-]+\].*# why:` (no bare ignore, no missing why).
- Overflow captured in `deferred-items.md` for v1.9.
- diff-cover ≥ 80% on touched files (or "N/A annotation-only" documented).
- Combined coverage `--fail-under=70` preserved.
- No bare `except` introduced.
- INSERT-ONLY `audit_log` invariant preserved (Plan 30-03 does not touch `audit_log`).
- `_bulk_near_duplicate_check_raw` carry-forward preserved (Plan 30-03 does not touch `services/memory/memory_service.py`; if a silence is needed there, uses the `_raw`-suffixed name).
- Full Phase 30 test suite still green (30-00 + 30-01 + 30-02 unaffected).
</verification>

<success_criteria>
ROADMAP Phase 30 SC-4 satisfied: `uv run mypy --strict config/settings.py` returns Success. Full-repo `uv run mypy --strict` scan: up to 25 surfaced violations fixed or silenced with `# type: ignore[error-code]` + `# why:` comment; remainder captured in `deferred-items.md`.
</success_criteria>

<output>
Create `.planning/phases/30-test-infra-mypy-hardening/30-03-SUMMARY.md` when done, recording:
- `BASELINE_ERRORS` (Task 0 capture) and `errors_after_task1` (final figure).
- NET reduction in mypy --strict error count.
- Fix-vs-silence ratio (e.g., "X fixed / Y silenced of 25 budget").
- Per-silence table: `| file:line | error-code | why-rationale |`.
- Overflow count and pointer to `deferred-items.md` section appended.
- Confirmation that disciplined-silence audit gates (bare ignore + missing why) returned 0 violations.
- Confirmation that production-behavior guard caught no code-flow changes in silenced hunks.
- Confirmation that full Phase 30 suite (30-00 + 30-01 + 30-02 + 30-03) ends green.
- Note any PG-availability scoping that affected integration suite confirmation in Task 2 step 7.
</output>
