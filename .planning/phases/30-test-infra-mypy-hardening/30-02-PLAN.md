---
plan_id: 30-02
phase: 30
plan: 02
wave: 2
type: tdd
depends_on: []
requirements: [TEST-INFRA-01]
files_modified:
  - tests/integration/test_extractor_e2e.py
  - tests/conftest.py
autonomous: true
must_haves:
  truths:
    - "`uv run pytest tests/integration/test_extractor_e2e.py -v` passes on a clean checkout — no `FileNotFoundError: Path /tmp/embedding_models/bge-m3 not found`."
    - "Fix uses Option (c) per CONTEXT.md §TEST-INFRA-01: mock `services.vectorizer.embedder.HuggingFaceEmbedder.__init__` directly at conftest scope before `AgentQueryPipeline` construction. No CI pre-download of bge-m3 (~1.3GB) introduced."
    - "Fixture is local to `tests/integration/` scope (nearest conftest = `tests/conftest.py` — confirmed pre-edit). Unit tests are not affected."
    - "Zero edits under `services/`, `controllers/`, `utils/` — test-only fix per TEST-INFRA-01 acceptance."
  artifacts:
    - path: "tests/conftest.py"
      provides: "Embedder __init__ mock fixture (autouse OR explicit, decided at execute time) gating `tests/integration/test_extractor_e2e.py`"
      contains: "HuggingFaceEmbedder"
    - path: "tests/integration/test_extractor_e2e.py"
      provides: "Test body using mocked embedder; no model download triggered"
  key_links:
    - from: "tests/conftest.py (nearest to extractor_e2e)"
      to: "services.vectorizer.embedder.HuggingFaceEmbedder"
      via: "patch at `__init__` to bypass FileNotFoundError; mock-at-consumer convention (v1.3 D-mock)"
      pattern: "monkeypatch\\.setattr\\(.*HuggingFaceEmbedder.*__init__|patch\\(.*HuggingFaceEmbedder.*__init__"
    - from: "tests/integration/test_extractor_e2e.py"
      to: "AgentQueryPipeline construction"
      via: "embedder mock applies before pipeline instantiation"
      pattern: "AgentQueryPipeline"
---

<objective>
Fix `tests/integration/test_extractor_e2e.py` by mocking `services.vectorizer.embedder.HuggingFaceEmbedder.__init__` at the nearest conftest scope (`tests/conftest.py` — `tests/integration/conftest.py` does NOT exist pre-edit). Option (c) per CONTEXT.md §TEST-INFRA-01 — most surgical: prevents `FileNotFoundError` at `AgentQueryPipeline.__init__` construction time without reordering existing fixture topology, no CI dep, no 1.3GB bge-m3 model download. ~20 LOC mock fixture.

Purpose: `embedder_or_mock` fixture currently reaches the consumer AFTER `AgentQueryPipeline.__init__` calls `get_embedder()` → `HuggingFaceEmbedder.__init__` raises `FileNotFoundError: Path /tmp/embedding_models/bge-m3 not found` before the test body runs. Pre-existing bug (would fail on v1.6 master tip in same env); not v1.7-caused.

Output: One conftest fixture; `tests/integration/test_extractor_e2e.py` passes on clean checkout with no bge-m3 download.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-test-infra-mypy-hardening/30-CONTEXT.md
@services/vectorizer/embedder.py
@tests/integration/test_extractor_e2e.py
@tests/conftest.py
@CLAUDE.md
@Claude.md

<interfaces>
<!-- Patch target: services.vectorizer.embedder.HuggingFaceEmbedder.__init__ -->
<!-- Per CONTEXT.md, this is "Option (c)" — mock at consumer path before AgentQueryPipeline construction. -->

Two implementation shapes acceptable (executor picks one based on existing tests/conftest.py structure):

(A) monkeypatch fixture, scoped to `tests/integration/`:
```
@pytest.fixture(autouse=False)
def mock_huggingface_embedder_init(monkeypatch):
    """TEST-INFRA-01: bypass bge-m3 model load (FileNotFoundError on clean checkout).
    Option (c) per 30-CONTEXT.md — mock __init__ directly, no model download."""
    from services.vectorizer import embedder as _embedder_mod
    def _noop_init(self, *args, **kwargs):
        # minimal attribute init required by downstream callers
        self.model_name = kwargs.get("model_name", "mock-bge-m3")
        self.dim = kwargs.get("dim", 1024)
    monkeypatch.setattr(_embedder_mod.HuggingFaceEmbedder, "__init__", _noop_init)
    yield
```

(B) unittest.mock.patch at module scope (more surgical to single test file):
```
# at top of test_extractor_e2e.py OR a tests/integration/conftest.py created for this purpose
@pytest.fixture(autouse=True)
def _mock_embedder_init():
    with patch.object(HuggingFaceEmbedder, "__init__", lambda self, *a, **kw: None):
        yield
```

Decision rule:
- If `tests/conftest.py` already has integration-only fixtures gated by markers, add (A) to `tests/conftest.py`.
- If `tests/conftest.py` is mostly unit-scoped, create `tests/integration/conftest.py` with (B).
- Executor picks at Task 1; record choice in 30-02-SUMMARY.md.

Note: executor MUST inspect the actual `HuggingFaceEmbedder.__init__` signature in `services/vectorizer/embedder.py` and ensure the mock sets any attributes that downstream callers (e.g., `embed(text)` callers) rely on. CONTEXT.md says "~20 LOC test fixture; bounded LOC delta" — don't over-engineer the mock.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 — RED: reproduce FileNotFoundError + verify failure shape</name>
  <read_first>
    - tests/integration/test_extractor_e2e.py (full file)
    - services/vectorizer/embedder.py (focus on `HuggingFaceEmbedder.__init__`)
    - tests/conftest.py (existing fixtures; check for embedder-related patches that fire too late)
  </read_first>
  <behavior>
    - Test 1 (current failure shape): `uv run pytest tests/integration/test_extractor_e2e.py -v 2>&1` produces `FileNotFoundError: Path /tmp/embedding_models/bge-m3 not found` (or equivalent missing-model error) at `AgentQueryPipeline.__init__` time, BEFORE test body executes.
    - Test 2 (consumer path identity): `grep -n "HuggingFaceEmbedder\\|get_embedder" services/vectorizer/embedder.py services/pipeline.py` confirms `HuggingFaceEmbedder.__init__` is what raises (vs an indirect factory like `get_embedder()` that we'd need to mock instead).
  </behavior>
  <action>
    1. Read `services/vectorizer/embedder.py` to capture:
       - The exact line in `HuggingFaceEmbedder.__init__` that raises (likely a `Path(...).exists()` check or `from_pretrained(local_dir)` call).
       - The required `__init__` parameter shape (positional + keyword).
       - What attributes downstream code on the instance reads (`self.model_name`, `self.dim`, `self._model`, etc.) — these must be set by the mock or the test will trip elsewhere.
    2. Read `tests/integration/test_extractor_e2e.py` to find:
       - Where `AgentQueryPipeline(...)` is instantiated (fixture setup vs test body).
       - Whether an existing `embedder_or_mock` fixture is referenced — confirm it fires AFTER pipeline construction (per CONTEXT.md root cause).
    3. Reproduce the failure: `uv run pytest tests/integration/test_extractor_e2e.py -v 2>&1 | tail -30`. Confirm error matches `FileNotFoundError: Path /tmp/embedding_models/bge-m3 not found` (or document the actual error if it has drifted from CONTEXT.md description).
    4. RED gate: failure reproduced (non-zero exit, FileNotFoundError surfaced). Capture failure traceback in scratch notes.
    5. Decide conftest landing site:
       - Inspect `tests/conftest.py`. If it already has integration-marker-gated fixtures, plan to add the mock there.
       - Else, plan to create `tests/integration/conftest.py` (does not exist pre-edit per host inspection).
    6. Decide mock shape (A vs B from `<interfaces>`) based on conftest decision. Record both decisions for Task 1.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_extractor_e2e.py 2>&amp;1 | grep -E "FileNotFoundError|bge-m3"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-fix failure reproduced — `FileNotFoundError` (or model-load error) on `tests/integration/test_extractor_e2e.py`.
    - `HuggingFaceEmbedder.__init__` raise site identified with exact line number.
    - Required mock-attribute set documented (which `self.*` attributes downstream code reads).
    - Conftest landing site decided (existing `tests/conftest.py` OR new `tests/integration/conftest.py`).
    - Mock fixture shape (A monkeypatch vs B `unittest.mock.patch`) decided.
  </acceptance_criteria>
  <done>RED state confirmed; root cause + raise site identified; mock landing decisions made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 — GREEN: implement embedder __init__ mock fixture + extractor_e2e passes</name>
  <read_first>
    - The conftest file decided in Task 0 (`tests/conftest.py` OR newly-created `tests/integration/conftest.py`)
    - tests/integration/test_extractor_e2e.py
    - services/vectorizer/embedder.py (re-confirm `__init__` shape)
  </read_first>
  <behavior>
    - `uv run pytest tests/integration/test_extractor_e2e.py -v` exits 0.
    - Mock fixture fires BEFORE `AgentQueryPipeline.__init__` (verified by removing the mock and watching the failure return — but that's a manual sanity check, not a perm test).
    - No bge-m3 model download triggered (`ls /tmp/embedding_models/` shows no change between pre-fix and post-fix runs).
    - Zero edits under `services/`, `controllers/`, `utils/` — `git diff --name-only services/ controllers/ utils/` empty.
  </behavior>
  <action>
    1. Apply the mock fixture per Task 0's decision:
       - **If landing in `tests/conftest.py`:** add fixture at end of file with `autouse=False` and marker-gate the autouse via a marker decorator on `test_extractor_e2e.py`, OR scope the fixture to only the integration test path. Verify it does NOT pollute unit tests.
       - **If creating `tests/integration/conftest.py`:** new file containing only the embedder mock fixture + a 2-line docstring referencing TEST-INFRA-01. `autouse=True` is acceptable here since the conftest is integration-scoped.
    2. Set ONLY the minimal attributes downstream code reads (per Task 0 inspection). Common candidates:
       - `self.model_name = kwargs.get("model_name", "mock-bge-m3")`
       - `self.dim = kwargs.get("dim", 1024)` (or whatever the real `__init__` defaults are)
       - If `self._model` or similar is read by `embed()` or `embed_async()`, set it to a `MagicMock()` returning zero-vectors of the right dim.
    3. If `embed(text)` / `embed_async(text)` is called by extractor_e2e and reads `self._model.encode(...)`, add a method mock OR mock the `embed` method too:
       ```
       monkeypatch.setattr(_embedder_mod.HuggingFaceEmbedder, "embed", lambda self, text: [0.0] * self.dim)
       ```
       Only add this if step 2's minimal attrs are insufficient and the test still fails.
    4. Run `uv run pytest tests/integration/test_extractor_e2e.py -v 2>&1 | tail -20`. **Expected: exit 0.**
    5. Run wider integration probe to confirm no collateral damage: `uv run pytest tests/integration/ -v 2>&1 | tail -10`. If new failures surface in OTHER integration tests caused by the mock (e.g., autouse fixture leaks into a test that needs the real embedder), narrow the fixture scope — do NOT widen by leaving regressions.
    6. Run unit suite probe: `uv run pytest tests/unit/ -m 'not benchmark' 2>&1 | tail -5`. Must still exit 0 (confirms fixture didn't leak into unit scope).
    7. Production-code untouched gate: `git diff --name-only services/ controllers/ utils/`. **Expected: empty.**
    8. LOC check: `git diff --stat tests/conftest.py tests/integration/test_extractor_e2e.py tests/integration/conftest.py 2>/dev/null`. Mock fixture should be ~20 LOC per CONTEXT.md. If significantly larger, simplify.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_extractor_e2e.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_extractor_e2e.py -v` exits 0.
    - No `FileNotFoundError: Path /tmp/embedding_models/bge-m3` in test output.
    - No bge-m3 model directory created during the test run (`ls /tmp/embedding_models/bge-m3 2>&1` shows "No such file" both before AND after the test run).
    - Mock fixture is in `tests/conftest.py` (scoped) OR `tests/integration/conftest.py` (autouse, integration-scoped) — choice documented in commit body.
    - Wider integration suite has zero new failures vs pre-fix baseline (excluding the now-passing extractor_e2e).
    - Unit suite (`tests/unit/`) unaffected (mock didn't leak into unit scope).
    - `git diff --name-only services/ controllers/ utils/` empty.
    - Mock fixture body ≤ ~30 LOC (CONTEXT.md target ~20; allow modest overrun for downstream-attr coverage).
  </acceptance_criteria>
  <done>extractor_e2e passes; integration suite no new regressions; unit suite unaffected; production code clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 — REFACTOR: scope tightening + diff-cover gate + document fix path</name>
  <read_first>
    - The conftest file modified in Task 1
    - tests/integration/test_extractor_e2e.py (post-fix)
  </read_first>
  <behavior>
    - Mock fixture is narrowly scoped — fires ONLY for `test_extractor_e2e.py` (or any other test that explicitly opts in via the marker).
    - diff-cover on touched files ≥ 80% (or "no coverage data needed" for test files — capture which applies).
    - Combined coverage `--fail-under=70` preserved.
  </behavior>
  <action>
    1. Audit scope of the fixture. If `autouse=True` was set at `tests/integration/conftest.py`, confirm no other integration test is *worsened* by the mock (e.g., a test that wanted the real embedder is now silently using the mock). If so:
       - Narrow to `autouse=False` and explicit fixture-request OR
       - Gate via marker (`pytest.mark.mock_embedder` applied to extractor_e2e only).
    2. Re-run the full integration suite if scope tightened: `uv run pytest tests/integration/ -v 2>&1 | tail -10`. Compare against Task 1's run — same pass/skip counts expected.
    3. Diff-cover: `uv run diff-cover coverage.xml --compare-branch=master --fail-under=80`. Capture result; if test files lack coverage data (typical), document "diff-cover N/A — test infra only" in commit body.
    4. Combined coverage floor: `uv run coverage report --fail-under=70`. Must not regress vs Phase 29 baseline.
    5. Document fix path in SUMMARY skeleton:
       - Which option chosen (c — direct `__init__` mock).
       - Why NOT (a) or (b) — CONTEXT.md rationale (option a = autouse side-effect risk; option b = 1.3GB CI download).
       - Conftest landing site (which file).
       - Fixture scope (autouse vs explicit, integration-only).
       - Minimal attribute set the mock provides.
    6. Final production-code gate: `git diff --name-only services/ controllers/ utils/` empty.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_extractor_e2e.py -v &amp;&amp; uv run coverage report --fail-under=70</automated>
  </verify>
  <acceptance_criteria>
    - Fixture scope narrowed if Task 1 surfaced any over-application (or rationale documented if `autouse=True` is the right call).
    - `uv run pytest tests/integration/test_extractor_e2e.py -v` exits 0.
    - Wider integration suite no new failures vs Task 1 baseline.
    - diff-cover gate decision documented (≥80% OR "N/A test infra").
    - Combined coverage `--fail-under=70` preserved.
    - `git diff --name-only services/ controllers/ utils/` empty.
    - Fix path documented in 30-02-SUMMARY skeleton notes (option c chosen, why a/b rejected, scope, minimal-attr set).
  </acceptance_criteria>
  <done>Fixture scope tight, gates clean, fix-path docs ready for SUMMARY write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-fixture ↔ `services.vectorizer.embedder.HuggingFaceEmbedder` | Test fixture bypasses real model load via `__init__` patch; if the mock omits attributes downstream code reads, the test breaks in a worse way (AttributeError instead of FileNotFoundError). Test-only — no production trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Repudiation (latent test failure) | `tests/integration/test_extractor_e2e.py` | mitigate | Mock `HuggingFaceEmbedder.__init__` directly at conftest scope; bounded ~20 LOC fixture. Avoids 1.3GB CI download (option b) and avoids autouse side-effect risk (option a). |
| T-30-02-02 | Tampering (cross-test contamination) | tests/conftest.py OR tests/integration/conftest.py | mitigate | Fixture scope reviewed in Task 2; narrowed to integration-only or marker-gated. Unit suite probe confirms no leak. |
| T-30-02-SC | Tampering (supply chain) | n/a | accept | TEST-INFRA-01 introduces no new package installs. Avoids pulling bge-m3 (option b). |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_extractor_e2e.py -v` exits 0.
- No `FileNotFoundError: Path /tmp/embedding_models/bge-m3` in any test output.
- No bge-m3 model directory created during test execution.
- diff-cover ≥ 80% on touched files (or "N/A test infra" documented).
- Combined coverage `--fail-under=70` preserved.
- No bare `except` introduced.
- INSERT-ONLY `audit_log` invariant preserved (Plan 30-02 does not touch `audit_log`).
- `_bulk_near_duplicate_check_raw` carry-forward preserved (Plan 30-02 does not touch `services/memory/memory_service.py`).
- `git diff --name-only services/ controllers/ utils/` empty.
- Wider integration suite (and unit suite) have no new failures introduced by the mock.
</verification>

<success_criteria>
ROADMAP Phase 30 SC-3 satisfied: `uv run pytest tests/integration/test_extractor_e2e.py -v` passes on clean checkout. Fix path documented in plan SUMMARY: option (c) — direct mock of `HuggingFaceEmbedder.__init__`.
</success_criteria>

<output>
Create `.planning/phases/30-test-infra-mypy-hardening/30-02-SUMMARY.md` when done, recording:
- Fix path: option (c) — direct `HuggingFaceEmbedder.__init__` mock (NOT (a) earlier patch order, NOT (b) CI pre-download).
- Conftest landing site: which file (`tests/conftest.py` vs newly-created `tests/integration/conftest.py`).
- Fixture scope: autouse vs explicit; integration-only.
- Minimal attribute set the mock provides on the instance.
- Pre-fix failure traceback (exact error line) from Task 0.
- Post-fix test result.
- diff-cover and combined coverage figures.
- Confirmation that wider integration suite and unit suite have no new regressions.
</output>
