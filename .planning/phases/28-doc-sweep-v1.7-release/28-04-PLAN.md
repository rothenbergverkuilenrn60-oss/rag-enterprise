---
plan_id: 28-04
phase: 28
plan: 4
type: execute
wave: 2
depends_on: [28-00, 28-01, 28-02, 28-03]
requirements: [DOC-01]
files_modified:
  - .planning/milestones/v1.7-ROADMAP.md
  - .planning/milestones/v1.7-REQUIREMENTS.md
  - .planning/ROADMAP.md
  - MILESTONES.md
  - .planning/STATE.md
autonomous: true
notes: |
  Implements D-05: fold v1.7 milestone archive INTO Phase 28 as the final sequential plan. 4 sub-tasks:
  1. Snapshot ROADMAP/REQ → .planning/milestones/v1.7-{ROADMAP,REQUIREMENTS}.md
  2. Move phase 26/27/28 dirs → .planning/milestones/v1.7-phases/
  3. Collapse ROADMAP v1.7 section into <details> + update progress table
  4. Create + backfill MILESTONES.md (v1.0..v1.7 entries per Claude's discretion)
  Sequential after 28-00/01/02/03 because:
    - 28-04 MOVES phase 26/27/28 directories — any in-flight Wave 1 plan would orphan
    - 28-04 references the SUMMARY.md links in the just-shipped CHANGELOG / release-notes / RUNBOOK / REQUIREMENTS-v1.8 → those files must exist first
  Post-condition sanity checks per project TDD-discipline note: ls .planning/milestones/v1.7-phases/ returns 3 dirs; MILESTONES.md has ≥ 7 v1.* entries.

must_haves:
  truths:
    - "v1.7 milestone is archived: snapshot files exist; phase dirs moved; ROADMAP collapsed; MILESTONES.md backfilled."
    - "ROADMAP.md progress table shows Phase 28 as Complete ✓."
    - "Future readers can find the full v1.7 history at .planning/milestones/v1.7-ROADMAP.md (snapshot) AND .planning/milestones/v1.7-phases/{26,27,28}/ (raw artifacts) AND MILESTONES.md (one-line summary)."
    - "STATE.md is updated to reflect v1.7 ship; points to /gsd-new-milestone for v1.8 open."
    - "No in-flight Wave 1 artifact orphaned: 28-{00,01,02,03}-SUMMARY.md files (when written by executor) live alongside this plan's SUMMARY.md and get archived together."
  artifacts:
    - path: .planning/milestones/v1.7-ROADMAP.md
      provides: "Snapshot of ROADMAP.md v1.7 section at time of archive"
      contains:
        - "v1.7 Memory Tech-Debt Burn-Down"
        - "Phase 26"
        - "Phase 27"
        - "Phase 28"
    - path: .planning/milestones/v1.7-REQUIREMENTS.md
      provides: "Snapshot of REQUIREMENTS.md (v1.7 active requirements) at time of archive"
      contains:
        - "TD-01"
        - "TD-07"
        - "DOC-01"
    - path: .planning/milestones/v1.7-phases/26-memory-infra-hygiene/
      provides: "Moved Phase 26 dir"
      contains: ["26-01-SUMMARY.md", "26-04-SUMMARY.md"]
    - path: .planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/
      provides: "Moved Phase 27 dir"
      contains: ["27-01-SUMMARY.md", "27-04-SUMMARY.md", "27-VERIFICATION.md", "27-BENCHMARK.md", "deferred-items.md"]
    - path: .planning/milestones/v1.7-phases/28-doc-sweep-v1.7-release/
      provides: "Moved Phase 28 dir (self-archive)"
      contains: ["28-CONTEXT.md", "28-00-PLAN.md", "28-04-PLAN.md"]
    - path: MILESTONES.md
      provides: "New repo-root file; backfilled v1.0..v1.7"
      min_lines: 30
      contains:
        - "v1.0"
        - "v1.1"
        - "v1.2"
        - "v1.3"
        - "v1.4"
        - "v1.5"
        - "v1.6"
        - "v1.7"
    - path: .planning/ROADMAP.md
      provides: "v1.7 section collapsed into <details>; progress table updated"
      contains:
        - "<details>"
        - "v1.7 Memory Tech-Debt Burn-Down"
        - "SHIPPED 2026-05-17"
        - "28. Doc Sweep + v1.7 Release"
        - "Complete ✓"
    - path: .planning/STATE.md
      provides: "Updated to v1.7 shipped; pointer to /gsd-new-milestone for v1.8"
      contains:
        - "v1.7"
        - "shipped"
  key_links:
    - from: MILESTONES.md
      to: .planning/milestones/v1.7-ROADMAP.md
      via: relative markdown link
      pattern: "milestones/v1.7-ROADMAP\\.md"
    - from: .planning/ROADMAP.md (collapsed v1.7 details)
      to: .planning/milestones/v1.7-ROADMAP.md
      via: link inside <summary>
      pattern: "milestones/v1.7-ROADMAP\\.md"
---

<objective>
Archive the v1.7 milestone: snapshot ROADMAP + REQUIREMENTS to `.planning/milestones/v1.7-*`; move Phase 26/27/28 directories to `.planning/milestones/v1.7-phases/`; collapse ROADMAP v1.7 section into a `<details>` block; create `MILESTONES.md` at repo root and backfill v1.0..v1.7 entries; update STATE.md to v1.7-shipped.

Purpose: Close Phase 28 and the v1.7 milestone. Free `.planning/phases/` for v1.8 work. Establish `MILESTONES.md` as the single-row-per-milestone navigation surface for future readers.

Output: 2 new snapshot files + 3 moved directories + 1 new `MILESTONES.md` + 2 edited files (ROADMAP, STATE). Zero production code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-doc-sweep-v1.7-release/28-CONTEXT.md
@.planning/milestones/v1.6-ROADMAP.md
@.planning/milestones/v1.6-REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot ROADMAP + REQUIREMENTS to .planning/milestones/v1.7-*</name>
  <files>.planning/milestones/v1.7-ROADMAP.md, .planning/milestones/v1.7-REQUIREMENTS.md</files>
  <action>
**Snapshot 1: `.planning/milestones/v1.7-ROADMAP.md`** — copy the v1.7 section of `.planning/ROADMAP.md` (currently lines 12–55 per Read) into a new standalone file. Header pattern matches `.planning/milestones/v1.6-ROADMAP.md`:

```
# v1.7 Memory Tech-Debt Burn-Down

**Shipped:** 2026-05-17
**Phases:** 26, 27, 28
**Status:** ✅ COMPLETE

---

## v1.7 Memory Tech-Debt Burn-Down (Phases 26–28) — SHIPPED 2026-05-17

**Milestone goal:** Knock out all 7 deferred items surfaced at v1.6 ship — production-clean the memory subsystem before adding more features. Pure refactor + reliability; no new user-facing capabilities.

**Carry-forward gates:** `diff-cover ≥ 80%` on touched files; combined coverage `--fail-under=70`; INSERT-ONLY `audit_log` invariant; audit-mode-before-enforce for any new destructive default; audit-write failure must NOT block destructive action.

### Phase 26: Memory Infra Hygiene
[copy verbatim from ROADMAP.md lines ~20-30]

### Phase 27: Test Isolation + Memory Reliability
[copy verbatim from ROADMAP.md lines ~32-43]

### Phase 28: Doc Sweep + v1.7 Release
[copy verbatim from ROADMAP.md lines ~45-55]
```

Use `Read` to load the current `.planning/ROADMAP.md` v1.7 section (lines 12-55) before writing. Snapshot is verbatim — do NOT edit content, do NOT rewrite. The point is preservation: this file is what `.planning/milestones/v1.7-ROADMAP.md` links to from MILESTONES.md and the collapsed `<details>` block in ROADMAP.md.

**Snapshot 2: `.planning/milestones/v1.7-REQUIREMENTS.md`** — copy current `.planning/REQUIREMENTS.md` verbatim. This file is small (78 lines per Read). The full file is the snapshot — header line "EnterpriseRAG — v1.7 Memory Tech-Debt Burn-Down Requirements" already identifies it as v1.7-scoped. Add nothing; remove nothing. Use Read to load, then Write to the new path.

Verification embedded in this task:
- Both new files exist
- v1.7-ROADMAP.md mentions all 3 phases by name
- v1.7-REQUIREMENTS.md mentions all 8 REQ-IDs (TD-01..07, DOC-01)
- Both files mention "shipped 2026-05-17"
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.7-ROADMAP.md && test -f .planning/milestones/v1.7-REQUIREMENTS.md && grep -q 'Phase 26' .planning/milestones/v1.7-ROADMAP.md && grep -q 'Phase 27' .planning/milestones/v1.7-ROADMAP.md && grep -q 'Phase 28' .planning/milestones/v1.7-ROADMAP.md && grep -q 'TD-01' .planning/milestones/v1.7-REQUIREMENTS.md && grep -q 'TD-07' .planning/milestones/v1.7-REQUIREMENTS.md && grep -q 'DOC-01' .planning/milestones/v1.7-REQUIREMENTS.md && grep -q '2026-05-17' .planning/milestones/v1.7-ROADMAP.md</automated>
  </verify>
  <done>Both snapshot files exist with all 3 phases / 8 REQ-IDs and the shipped-date present.</done>
</task>

<task type="auto">
  <name>Task 2: Move phase directories — git mv 26-* + 27-* + 28-* → .planning/milestones/v1.7-phases/</name>
  <files>.planning/phases/26-memory-infra-hygiene/, .planning/phases/27-test-isolation-memory-reliability/, .planning/phases/28-doc-sweep-v1.7-release/</files>
  <action>
Use `git mv` (NOT plain `mv`) to preserve git history per the v1.6 archive pattern. Three directories move:

```bash
mkdir -p .planning/milestones/v1.7-phases
git mv .planning/phases/26-memory-infra-hygiene .planning/milestones/v1.7-phases/26-memory-infra-hygiene
git mv .planning/phases/27-test-isolation-memory-reliability .planning/milestones/v1.7-phases/27-test-isolation-memory-reliability
git mv .planning/phases/28-doc-sweep-v1.7-release .planning/milestones/v1.7-phases/28-doc-sweep-v1.7-release
```

**Critical sequencing notes:**
- This task moves the CURRENT phase 28 directory — including the PLAN.md file the executor is running from and any in-flight SUMMARY.md files from plans 28-00/01/02/03. Per CONTEXT D-05 sub-task 2, this is intended: the move happens after all sibling plans commit their SUMMARYs (Wave 1 dependency: 28-04 depends_on=[28-00, 28-01, 28-02, 28-03]).
- After move, this plan's own PLAN.md path is at `.planning/milestones/v1.7-phases/28-doc-sweep-v1.7-release/28-04-PLAN.md`. The executor should be tolerant of its own working file moving — finish the move, then continue with subsequent tasks reading from the NEW path.
- Use `git mv` so the commit shows as a rename (preserves blame/history). `git status` after each `git mv` should show `R` (rename) status for the moved directory.

Post-move sanity (project TDD-discipline note from planning context):
```bash
ls .planning/milestones/v1.7-phases/ | wc -l   # expect 3
ls .planning/milestones/v1.7-phases/26-memory-infra-hygiene/*-SUMMARY.md | wc -l   # expect 5
ls .planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/*-SUMMARY.md | wc -l   # expect 5
ls .planning/phases/ 2>/dev/null | grep -c -E '^(26|27|28)-'   # expect 0
```

Constraints:
- Do NOT use `rsync` or `cp -r` followed by `rm -rf` — these break git history. Use `git mv` exclusively.
- Do NOT move `.planning/milestones/` itself; only the three phase subdirectories under `.planning/phases/`.
- Do NOT move `.planning/REQUIREMENTS.md` or `.planning/REQUIREMENTS-v1.8.md` — those stay at planning root (REQUIREMENTS.md is rewritten at next milestone open by gsd-new-milestone; REQUIREMENTS-v1.8.md becomes the active file).
  </action>
  <verify>
    <automated>test -d .planning/milestones/v1.7-phases/26-memory-infra-hygiene && test -d .planning/milestones/v1.7-phases/27-test-isolation-memory-reliability && test -d .planning/milestones/v1.7-phases/28-doc-sweep-v1.7-release && test ! -d .planning/phases/26-memory-infra-hygiene && test ! -d .planning/phases/27-test-isolation-memory-reliability && test ! -d .planning/phases/28-doc-sweep-v1.7-release && test -f .planning/milestones/v1.7-phases/26-memory-infra-hygiene/26-01-SUMMARY.md && test -f .planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-VERIFICATION.md && test -f .planning/milestones/v1.7-phases/28-doc-sweep-v1.7-release/28-CONTEXT.md</automated>
  </verify>
  <done>All 3 phase dirs moved via git mv; old paths no longer exist; SUMMARY/VERIFICATION/CONTEXT files present at new paths.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse ROADMAP v1.7 section into <details> + update progress table</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit `.planning/ROADMAP.md`:

**A. Update Milestones list (line 12)** — change:
```
- 🚧 **v1.7 Memory Tech-Debt Burn-Down** — Phases 26–28 (in planning, opened 2026-05-17)
```
to:
```
- ✅ **v1.7 Memory Tech-Debt Burn-Down** — Phases 26–28 (shipped 2026-05-17) — [archive](milestones/v1.7-ROADMAP.md)
```

**B. Collapse v1.7 expanded section (current lines 14-55)** — wrap in `<details>` matching the v1.6 + v1.5 pattern (existing examples in same file at lines 107-115 and 117-170):

```markdown
<details>
<summary>✅ v1.7 Memory Tech-Debt Burn-Down (Phases 26–28) — SHIPPED 2026-05-17</summary>

- [x] Phase 26: Memory Infra Hygiene (5/5 plans) — completed 2026-05-17
- [x] Phase 27: Test Isolation + Memory Reliability (5/5 plans) — completed 2026-05-17
- [x] Phase 28: Doc Sweep + v1.7 Release (5/5 plans) — completed 2026-05-17

See [milestones/v1.7-ROADMAP.md](milestones/v1.7-ROADMAP.md) for full phase details.

</details>
```

The expanded "Milestone goal / Carry-forward gates / Phase 26..28 details" block (current lines 14-55) is REMOVED — it's preserved in the snapshot file (milestones/v1.7-ROADMAP.md) per Task 1.

**C. Update Progress table (currently at lines 254-284)** — change the row for Phase 28:
```
| 28. Doc Sweep + v1.7 Release | v1.7 | 0/? | Planning | — |
```
to:
```
| 28. Doc Sweep + v1.7 Release | v1.7 | 5/5 | Complete ✓ | 2026-05-17 |
```

Constraints:
- Preserve all OTHER milestone `<details>` blocks (v1.0, v1.1, v1.2, v1.3, v1.5, v1.6, v1.4 — note: file has them in non-chronological order per existing structure; do NOT reorder).
- Preserve the `## Progress` table for Phases 1-27 (only update the Phase 28 row).
- Preserve the file's TOP-OF-FILE `# Roadmap — EnterpriseRAG` + `## Milestones` heading structure.
- Use `Edit` with sufficient context anchors. The lines 14-55 deletion is a large block — use a multi-line search-string anchored on `## v1.7 Memory Tech-Debt Burn-Down (Phases 26–28) — IN PLANNING` (line 14) through the end of Phase 28 success criterion 4 (line 55).
  </action>
  <verify>
    <automated>grep -q '✅ \*\*v1.7 Memory Tech-Debt Burn-Down\*\*' .planning/ROADMAP.md && grep -q 'SHIPPED 2026-05-17' .planning/ROADMAP.md && grep -q '<summary>✅ v1.7 Memory Tech-Debt Burn-Down' .planning/ROADMAP.md && grep -q 'milestones/v1.7-ROADMAP\.md' .planning/ROADMAP.md && grep -q -E '^\| 28\. Doc Sweep \+ v1\.7 Release \| v1\.7 \| 5/5 \| Complete' .planning/ROADMAP.md && ! grep -q '🚧 \*\*v1.7' .planning/ROADMAP.md && ! grep -q 'v1.7 Memory Tech-Debt Burn-Down (Phases 26–28) — IN PLANNING' .planning/ROADMAP.md</automated>
  </verify>
  <done>v1.7 milestone marker shows ✅ shipped; v1.7 section collapsed into <details>; progress table updated for Phase 28; other milestone blocks preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Create MILESTONES.md at repo root + backfill v1.0..v1.7 entries</name>
  <files>MILESTONES.md</files>
  <action>
Create new file `MILESTONES.md` at repo root. Per Claude's-discretion default in CONTEXT (MILESTONES.md backfill scope): backfill v1.0 → v1.7 entries. **Per ENG-REVIEW D2: summaries MUST be extracted from each `.planning/milestones/v{X}-ROADMAP.md` source-of-truth doc, NOT planner-written prose.** Per ENG-REVIEW D1: this file is the stable navigation hub that CHANGELOG.md + docs/release-notes-v1.7.md link to (decouples public docs from `.planning/` directory restructuring).

**Step A — extract per-milestone metadata (one bash loop):**
```bash
for v in 1.0 1.1 1.2 1.3 1.4 1.5 1.6; do
  ROADMAP=".planning/milestones/v${v}-ROADMAP.md"
  REQ=".planning/milestones/v${v}-REQUIREMENTS.md"
  test -f "$ROADMAP" -a -f "$REQ" || { echo "MISSING: v${v}"; exit 1; }
  # Goal line (drop leading **Milestone goal:** prefix)
  GOAL=$(grep -m1 -E '^\*\*Milestone goal:\*\*' "$ROADMAP" | sed -E 's/^\*\*Milestone goal:\*\*\s*//')
  # Requirement count (sum of checked + unchecked top-level items)
  REQ_COUNT=$(grep -c -E '^- \[[ x]\] \*\*[A-Z]+-[0-9]+' "$REQ")
  # Phase range (read from the milestone marker line in ROADMAP.md top-of-file)
  PHASES=$(grep -m1 -E "\*\*v${v} " .planning/ROADMAP.md | sed -E 's/.*Phases? ([0-9]+[–-]?[0-9]*).*/\1/')
  # Shipped date (same source line)
  SHIPPED=$(grep -m1 -E "\*\*v${v} " .planning/ROADMAP.md | sed -E 's/.*shipped ([0-9-]+).*/\1/')
  echo "v${v}|${SHIPPED}|${PHASES}|${REQ_COUNT}|${GOAL}"
done
# v1.7 row uses the same loop pattern but reads from the v1.7 snapshot created in Task 1 of THIS plan.
```

Each line is a `|`-separated tuple. Format each into the Shipped table below, truncating GOAL to a single sentence if it spans multiple. If a milestone's `**Milestone goal:**` line is missing from its archived ROADMAP, fall back to the first paragraph of the milestone overview section (anchored on the H2/H3 milestone heading); record the fallback choice in 28-04-SUMMARY.md.

**Step B — write MILESTONES.md with table + per-milestone H3 anchor sections:**

```
# EnterpriseRAG — Milestones

Single-row-per-milestone navigation surface. Each row links to the full milestone roadmap snapshot under `.planning/milestones/`. Section anchors below the table provide stable deep-links for CHANGELOG.md + release-notes-v{X}.md references — these resurvive `.planning/` directory restructuring.

## Shipped

| Version | Shipped | Phases | Requirements | Anchor |
|---------|---------|--------|--------------|--------|
| [v1.0 Hardening](.planning/milestones/v1.0-ROADMAP.md) | {SHIPPED} | {PHASES} | {REQ_COUNT} | [details](#v10) |
| [v1.1 Retrieval Depth & Frontend](.planning/milestones/v1.1-ROADMAP.md) | {SHIPPED} | {PHASES} | {REQ_COUNT} | [details](#v11) |
| [v1.2 Agentic Layer + Swarm](.planning/milestones/v1.2-ROADMAP.md) | {SHIPPED} | {PHASES} | {REQ_COUNT} | [details](#v12) |
| [v1.3 Fork Swarm, NLU & Quality](.planning/milestones/v1.3-ROADMAP.md) | {SHIPPED} | {PHASES} | {REQ_COUNT} | [details](#v13) |
| [v1.4 Agent-First Architecture Inversion](.planning/milestones/v1.4-ROADMAP.md) | {SHIPPED} | {PHASES} | {REQ_COUNT} | [details](#v14) |
| [v1.5 Web Search + Multi-Agent Debate + Coverage Lift](.planning/milestones/v1.5-ROADMAP.md) | {SHIPPED} | {PHASES} | {REQ_COUNT} | [details](#v15) |
| [v1.6 Memory Tool — Agent-Authored Long-Term Facts](.planning/milestones/v1.6-ROADMAP.md) | {SHIPPED} | {PHASES} | {REQ_COUNT} | [details](#v16) |
| [v1.7 Memory Tech-Debt Burn-Down](.planning/milestones/v1.7-ROADMAP.md) | 2026-05-17 | 26–28 | 8 | [details](#v17) |

## In Planning

(none — run `/gsd-new-milestone` to open v1.8.)

---

### v1.0
{GOAL extracted from .planning/milestones/v1.0-ROADMAP.md}
[Roadmap snapshot](.planning/milestones/v1.0-ROADMAP.md) · [Requirements snapshot](.planning/milestones/v1.0-REQUIREMENTS.md) · [Phase artifacts](.planning/milestones/v1.0-phases/)

### v1.1
{GOAL extracted from .planning/milestones/v1.1-ROADMAP.md}
[Roadmap snapshot](.planning/milestones/v1.1-ROADMAP.md) · [Requirements snapshot](.planning/milestones/v1.1-REQUIREMENTS.md) · [Phase artifacts](.planning/milestones/v1.1-phases/)

### v1.2
{GOAL extracted from .planning/milestones/v1.2-ROADMAP.md}
[Roadmap snapshot](.planning/milestones/v1.2-ROADMAP.md) · [Requirements snapshot](.planning/milestones/v1.2-REQUIREMENTS.md) · [Phase artifacts](.planning/milestones/v1.2-phases/)

### v1.3
{GOAL extracted from .planning/milestones/v1.3-ROADMAP.md}
[Roadmap snapshot](.planning/milestones/v1.3-ROADMAP.md) · [Requirements snapshot](.planning/milestones/v1.3-REQUIREMENTS.md) · [Phase artifacts](.planning/milestones/v1.3-phases/)

### v1.4
{GOAL extracted from .planning/milestones/v1.4-ROADMAP.md}
[Roadmap snapshot](.planning/milestones/v1.4-ROADMAP.md) · [Requirements snapshot](.planning/milestones/v1.4-REQUIREMENTS.md) · [Phase artifacts](.planning/milestones/v1.4-phases/)

### v1.5
{GOAL extracted from .planning/milestones/v1.5-ROADMAP.md}
[Roadmap snapshot](.planning/milestones/v1.5-ROADMAP.md) · [Requirements snapshot](.planning/milestones/v1.5-REQUIREMENTS.md) · [Phase artifacts](.planning/milestones/v1.5-phases/)

### v1.6
{GOAL extracted from .planning/milestones/v1.6-ROADMAP.md}
[Roadmap snapshot](.planning/milestones/v1.6-ROADMAP.md) · [Requirements snapshot](.planning/milestones/v1.6-REQUIREMENTS.md) · [Phase artifacts](.planning/milestones/v1.6-phases/)

### v1.7
{GOAL extracted from .planning/milestones/v1.7-ROADMAP.md — snapshot written by Task 1 of this plan}

**TD-01..07 + DOC-01** — Refactor + reliability milestone. Pure tech-debt burn-down; zero new user-facing capabilities.
- Phase 26 (TD-01, TD-03, TD-07): audit_log self-bootstrap; asyncpg helper centralization; bge-m3 vanilla HF cache
- Phase 27 (TD-02, TD-04, TD-05, TD-06): create_app() factory; near-dup audit-mode (D-09 — INSERT still runs, v1.8 promotes to silent-skip); save_facts batch; redis_mock fixture
- Phase 28 (DOC-01): docs sweep + release artifacts + v1.8 scaffold + milestone archive

[Roadmap snapshot](.planning/milestones/v1.7-ROADMAP.md) · [Requirements snapshot](.planning/milestones/v1.7-REQUIREMENTS.md) · [Phase artifacts](.planning/milestones/v1.7-phases/) · [Release notes](docs/release-notes-v1.7.md) · [Tag ceremony](.planning/milestones/v1.7-release-tag.md)
```

Constraints:
- Repo root path is `MILESTONES.md` (NOT `docs/MILESTONES.md`, NOT `.planning/MILESTONES.md`).
- v1.7 row is the last/newest "Shipped" entry.
- Per-milestone H3 anchors (`### v10`, `### v11`, ... `### v17`) MUST exist and resolve via GitHub auto-anchor (`#v10`, `#v11`, ..., `#v17` — GitHub strips dots from heading IDs; verify gate enforces presence).
- **ENG-REVIEW D2 binding constraint:** `<fill from vN-REQUIREMENTS>` placeholders MUST NOT appear in final output (gate `! grep -q '<fill'`). All metadata extracted via Step A bash loop.
- Length target: 80–130 lines (table + 8 anchor sections + footer).
  </action>
  <verify>
    <automated>test -f MILESTONES.md && grep -q -E '^\| \[v1\.0' MILESTONES.md && grep -q -E '^\| \[v1\.1' MILESTONES.md && grep -q -E '^\| \[v1\.2' MILESTONES.md && grep -q -E '^\| \[v1\.3' MILESTONES.md && grep -q -E '^\| \[v1\.4' MILESTONES.md && grep -q -E '^\| \[v1\.5' MILESTONES.md && grep -q -E '^\| \[v1\.6' MILESTONES.md && grep -q -E '^\| \[v1\.7' MILESTONES.md && grep -q 'milestones/v1.7-ROADMAP.md' MILESTONES.md && grep -q 'In Planning' MILESTONES.md && test $(grep -c '^| \[v1\.' MILESTONES.md) -eq 8 && ! grep -q '<fill' MILESTONES.md && ! grep -q '{GOAL' MILESTONES.md && ! grep -q '{SHIPPED' MILESTONES.md && ! grep -q '{PHASES' MILESTONES.md && ! grep -q '{REQ_COUNT' MILESTONES.md && grep -q '^### v10' MILESTONES.md && grep -q '^### v17' MILESTONES.md</automated>
  </verify>
  <done>MILESTONES.md exists at repo root with exactly 8 v1.* entries (v1.0..v1.7); all link to .planning/milestones/v{X}-ROADMAP.md; In Planning section present (empty placeholder).</done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md — mark v1.7 shipped; point to /gsd-new-milestone for v1.8</name>
  <files>.planning/STATE.md</files>
  <action>
Edit `.planning/STATE.md` frontmatter + body:

**Frontmatter changes:**
- `status:` change to `v1.7 shipped — Phase 28 complete; v1.8 not yet opened`
- `stopped_at:` change to `Phase 28 28-04-SUMMARY committed; v1.7 archived to .planning/milestones/v1.7-*; CHANGELOG + release-notes + RUNBOOK + REQUIREMENTS-v1.8 scaffold all committed; ready for /gsd-new-milestone for v1.8 open`
- `last_updated:` change to today's date (2026-05-17 per env)
- `last_activity:` replace with a 1-sentence summary of Phase 28 ship (5 plans across 2 waves, doc sweep + release artifacts + v1.7 archive)
- `progress.total_phases:` change to 3 (Phase 26 + 27 + 28; was 3 — confirm unchanged)
- `progress.completed_phases:` change to 3 (was 2)
- `progress.total_plans:` change to 15 (was 10) — Phase 28 adds 5 plans
- `progress.completed_plans:` change to 15 (was 10) — Phase 28 adds 5 plans
- `progress.percent:` change to 100 (was 67)

**Body changes:**
- `## Current Position` section: change Phase to "v1.8 (not yet opened — run /gsd-new-milestone)"; Plan to "n/a"; Status to "v1.7 milestone shipped 2026-05-17"; Last activity 1-sentence Phase 28 summary.
- `## Phase Overview` table: update Phase 28 row from "Planning" to "✅ Shipped 2026-05-17 (5 plans, doc sweep + v1.7 release artifacts)".
- `## Session Continuity` block at bottom: replace with v1.7-shipped state. Point next action to `/gsd-new-milestone` (NOT `/gsd-discuss-phase 29` — milestone open precedes phase planning).

Constraints:
- Preserve `gsd_state_version`, `milestone`, `milestone_name`, `## Project Reference`, `## Accumulated Context` sections (carry-forward decisions + resolved blockers + open blockers + todos), `### Promoted Into v1.7 Active Scope`.
- The `## Accumulated Context` section's "Open Blockers Carried Into v1.7" subsection — rename to "Open Blockers Carried Into v1.8" since v1.7 just shipped and we're handing off. Content stays "None." since v1.7 didn't open any new blockers.
- Add new entry to "Todos (carry-forward, NOT v1.7-scoped)" list — already has +14 event-loop singleton leaks entry. Promote SK-01, TOC-01, OAI-01, EVT-01, MYPY-01, TEST-INFRA-01, TEST-INFRA-02 references in the todos? Actually those are NOW in .planning/REQUIREMENTS-v1.8.md so the todos list can keep its existing forward-references intact (no edit needed) — just confirm the list still mentions them via the v1.8+ tag.
  </action>
  <verify>
    <automated>grep -q 'v1.7 shipped' .planning/STATE.md && grep -q -E 'completed_phases:\s*3' .planning/STATE.md && grep -q -E 'completed_plans:\s*15' .planning/STATE.md && grep -q -E 'percent:\s*100' .planning/STATE.md && grep -q '/gsd-new-milestone' .planning/STATE.md && grep -q -E '\| 28 \|.*Shipped 2026-05-17' .planning/STATE.md && grep -q '2026-05-17' .planning/STATE.md</automated>
  </verify>
  <done>STATE.md frontmatter + body updated; progress at 100%; phase overview shows Phase 28 shipped; session continuity points to /gsd-new-milestone for v1.8 open.</done>
</task>

<task type="auto">
  <name>Task 6: Final sanity post-conditions (project TDD-discipline check)</name>
  <files>(read-only; no files modified)</files>
  <action>
Per the planning-context note "plan 28-04 (archive) MUST include a sanity post-condition check", run these gates AFTER Tasks 1-5 complete. If any fails, the executor must investigate and fix (likely a Task 2 git mv that missed a sibling SUMMARY.md, or a Task 3 ROADMAP edit that mis-counted).

Gate 1 — phase directories moved:
```bash
ls .planning/milestones/v1.7-phases/26-* 2>/dev/null | head -1
ls .planning/milestones/v1.7-phases/27-* 2>/dev/null | head -1
ls .planning/milestones/v1.7-phases/28-* 2>/dev/null | head -1
# Each should return at least one path; old paths should not exist
test ! -d .planning/phases/26-memory-infra-hygiene
test ! -d .planning/phases/27-test-isolation-memory-reliability
test ! -d .planning/phases/28-doc-sweep-v1.7-release
```

Gate 2 — MILESTONES.md backfill complete:
```bash
grep -c '^| \[v1\.' MILESTONES.md   # expect exactly 8
```

Gate 3 — ROADMAP collapsed correctly:
```bash
grep -c '^<details>' .planning/ROADMAP.md   # expect ≥ 8 (v1.0..v1.7 all collapsed)
grep -q 'milestones/v1.7-ROADMAP\.md' .planning/ROADMAP.md
```

Gate 4 — Snapshot files exist and are non-empty:
```bash
test -s .planning/milestones/v1.7-ROADMAP.md
test -s .planning/milestones/v1.7-REQUIREMENTS.md
```

Gate 5 — STATE.md reflects ship + frontmatter counters consistent:
```bash
grep -q 'v1.7 shipped' .planning/STATE.md
grep -q '/gsd-new-milestone' .planning/STATE.md
grep -q -E '^\s*total_plans:\s*15' .planning/STATE.md
grep -q -E '^\s*completed_plans:\s*15' .planning/STATE.md
grep -q -E '^\s*percent:\s*100' .planning/STATE.md
```

Gate 6 — No production code touched in any of Tasks 1-5:
```bash
# After all tasks, `git diff --name-only HEAD~N..HEAD` (where N = number of commits made by this plan) should show only:
#   .planning/milestones/v1.7-{ROADMAP,REQUIREMENTS}.md (new)
#   .planning/milestones/v1.7-phases/... (renamed from .planning/phases/...)
#   .planning/ROADMAP.md (edited)
#   .planning/STATE.md (edited)
#   MILESTONES.md (new)
# Specifically zero files under services/, controllers/, utils/, config/, or tests/.
git diff --name-only HEAD~5..HEAD | grep -E '^(services|controllers|utils|config|tests)/' | wc -l   # expect 0
```

Gate 7 — Markdown link integrity across v1.7 doc surface (ENG-REVIEW D3):

```bash
# Collect every relative-path link target from the v1.7 doc surface (CHANGELOG,
# release-notes, RUNBOOK, MILESTONES, REQUIREMENTS-v1.8, v1.7 archive snapshots
# + their .planning/milestones/v1.7-phases/ children). Skip code fences. Verify
# each target resolves on disk. Fragment links (#anchor) are stripped before
# the existence test — anchors are validated by GitHub markdown auto-anchor at
# render time, not here.

set -euo pipefail
LINK_CHECK_FAILS=$(python3 - <<'PY'
import os, re, sys
from pathlib import Path

SURFACE_GLOBS = [
    "CHANGELOG.md", "MILESTONES.md", "README.md", "ARCHITECTURE.md",
    "docs/RUNBOOK.md", "docs/release-notes-v1.7.md", "docs/memory-eviction.md",
    ".planning/REQUIREMENTS-v1.8.md",
    ".planning/milestones/v1.7-ROADMAP.md",
    ".planning/milestones/v1.7-REQUIREMENTS.md",
    ".planning/milestones/v1.7-release-tag.md",
]
# Also scan every SUMMARY.md inside .planning/milestones/v1.7-phases/ (in-archive cross-links).
for path in Path(".planning/milestones/v1.7-phases").rglob("*-SUMMARY.md"):
    SURFACE_GLOBS.append(str(path))

LINK_RX = re.compile(r"\[(?P<text>[^\]]+)\]\((?P<href>[^)\s]+)(?:\s+\"[^\"]*\")?\)")
FENCE_RX = re.compile(r"^```")

fails = []
for fn in SURFACE_GLOBS:
    if not os.path.isfile(fn):
        continue
    src_dir = os.path.dirname(fn) or "."
    in_fence = False
    with open(fn, "r", encoding="utf-8") as f:
        for lineno, line in enumerate(f, 1):
            if FENCE_RX.match(line):
                in_fence = not in_fence
                continue
            if in_fence:
                continue
            for m in LINK_RX.finditer(line):
                href = m.group("href")
                # Skip absolute URLs, mailto, anchors-only
                if href.startswith(("http://", "https://", "mailto:", "#")):
                    continue
                # Strip fragment for existence check
                target = href.split("#", 1)[0]
                if not target:
                    continue
                # Resolve relative to the source file's directory
                resolved = os.path.normpath(os.path.join(src_dir, target))
                if not os.path.exists(resolved):
                    fails.append(f"{fn}:{lineno}: broken link → {href} (resolved: {resolved})")

if fails:
    print("\n".join(fails), file=sys.stderr)
    sys.exit(1)
sys.exit(0)
PY
echo $?)
test "$LINK_CHECK_FAILS" = "0"
```

If any gate fails: investigate root cause; fix; re-run the failing gate. Do NOT proceed to SUMMARY without all gates green.
  </action>
  <verify>
    <automated>test -d .planning/milestones/v1.7-phases/26-memory-infra-hygiene && test -d .planning/milestones/v1.7-phases/27-test-isolation-memory-reliability && test -d .planning/milestones/v1.7-phases/28-doc-sweep-v1.7-release && test ! -d .planning/phases/26-memory-infra-hygiene && test ! -d .planning/phases/27-test-isolation-memory-reliability && test ! -d .planning/phases/28-doc-sweep-v1.7-release && [ "$(grep -c '^| \[v1\.' MILESTONES.md)" = "8" ] && grep -q 'milestones/v1.7-ROADMAP\.md' .planning/ROADMAP.md && test -s .planning/milestones/v1.7-ROADMAP.md && test -s .planning/milestones/v1.7-REQUIREMENTS.md && grep -q 'v1.7 shipped' .planning/STATE.md && grep -q '/gsd-new-milestone' .planning/STATE.md</automated>
  </verify>
  <done>All 7 gates pass (Gate 7 = ENG-REVIEW D3 link-integrity check); v1.7 milestone fully archived; STATE.md reflects ship; future readers can find everything from MILESTONES.md → .planning/milestones/v1.7-*; CHANGELOG + release-notes links all resolve on disk.</done>
</task>

</tasks>

<verification>
- All 6 task gates above pass.
- File ownership: this plan is the ONLY Wave 2 plan; depends_on=[28-00, 28-01, 28-02, 28-03] guarantees all Wave 1 SUMMARYs are committed before any of this plan's tasks start.
- Move operations use `git mv` exclusively (preserves history; allows future blame to trace TD-01 → 26-04-SUMMARY.md → 26-04-PLAN.md → CHANGELOG.md context).
- After this plan ships, `.planning/phases/` is empty (no v1.7 phase dirs remain); v1.8 work will create fresh dirs there.
- Zero production code touched across all 6 tasks — Gate 6 enforces this absolutely.
</verification>

<success_criteria>
- D-05 sub-task 1: Snapshot files exist at `.planning/milestones/v1.7-{ROADMAP,REQUIREMENTS}.md`.
- D-05 sub-task 2: Phase 26/27/28 dirs moved to `.planning/milestones/v1.7-phases/`.
- D-05 sub-task 3: ROADMAP v1.7 section collapsed into `<details>` + progress table updated (Phase 28 → Complete ✓).
- D-05 sub-task 4: `MILESTONES.md` created at repo root with v1.0..v1.7 backfill (8 entries).
- STATE.md reflects v1.7 ship; points to `/gsd-new-milestone` for v1.8.
- Plan 28-04 ran AFTER 28-00/01/02/03 committed (enforced by depends_on; sanity-checked by Gate 1 finding all expected SUMMARY.md siblings at the moved Phase 28 path).
</success_criteria>

<output>
Create `.planning/milestones/v1.7-phases/28-doc-sweep-v1.7-release/28-04-SUMMARY.md` (note: at post-move path) recording:
1. Snapshot files created (paths + line counts).
2. `git mv` commands run (verify git status shows R for each).
3. ROADMAP edits applied (before/after line counts; <details> count).
4. MILESTONES.md final content (8-entry table + footer).
5. STATE.md fields updated.
6. All 6 sanity gates → confirmation PASSED.
7. v1.7 milestone CLOSE statement: "v1.7 Memory Tech-Debt Burn-Down — SHIPPED 2026-05-17 — 3 phases / 15 plans / 8 requirements / 0 carry-forward blockers."
</output>
