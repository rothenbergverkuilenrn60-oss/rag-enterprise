---
plan_id: 28-01
phase: 28
plan: 1
type: execute
wave: 1
depends_on: []
requirements: [DOC-01]
files_modified:
  - README.md
  - ARCHITECTURE.md
  - docs/memory-eviction.md
  - CHANGELOG.md
autonomous: true
notes: |
  Implements ROADMAP SC-1 (README + ARCHITECTURE refresh), SC-2 (docs/memory-eviction.md partial refresh), SC-3 (CHANGELOG v1.7 entry).
  Surgical patches only per Claude's-discretion default in CONTEXT — touch only paragraphs that mention v1.7-changed code (TD-01/03/07/02/04/05/06). NO full structural review.
  CHANGELOG entry uses strict keep-a-changelog format (Added / Changed / Fixed) per Claude's-discretion default; final paragraph audit-mode call-out per CONTEXT carry-forward row 1 + Claude's-discretion table row 1.
  Forward reference to `.planning/REQUIREMENTS-v1.8.md` (SK-01) — file created in parallel plan 28-03 within same Wave 1; OK because no PR boundary forces ordering inside the wave (project ships as single milestone, not per-plan).

must_haves:
  truths:
    - "Reviewer reading README can identify post-v1.7 audit_log auto-create behavior without grep (no manual DDL step lingering in docs)."
    - "Reviewer reading README does NOT find stale references to qdrant as the default vector store (qdrant→pgvector was pre-v1.7 but stale wording in §Docker stack is in-scope for v1.7 surgical sweep if it touches a TD-* path)."
    - "Reviewer reading ARCHITECTURE.md §5.2.11 (services/memory) sees the post-v1.7 batch path (save_facts batch + near-dup precheck audit-mode) and the asyncpg_helper consumption."
    - "Reviewer reading ARCHITECTURE.md §5.2.13 (services/audit) sees the auto-create lazy-pool path (no manual DDL)."
    - "CHANGELOG.md [Unreleased] section is filled with a complete v1.7 entry (Added / Changed / Fixed + audit-mode → v1.8 SK-01 call-out paragraph)."
    - "docs/memory-eviction.md mentions the new near-duplicate audit metric where contextually appropriate, untouched where v1.6 wording is still accurate."
  artifacts:
    - path: README.md
      provides: "Surgical refresh — patches only paragraphs referencing v1.7-changed paths"
      preserves: "Existing structure, agent-first framing from v1.4"
    - path: ARCHITECTURE.md
      provides: "Surgical refresh — §5.2.11 memory + §5.2.13 audit subsections updated for TD-01/03/04/05/07"
      preserves: "Existing 486-line structure; sections 1-4 + §5.1 + non-memory/audit §5.2.* untouched"
    - path: docs/memory-eviction.md
      provides: "Partial refresh — add note re audit-mode-only near-dup guard (D-09) where eviction/near-duplicate context exists; do NOT add new top-level section"
      preserves: "v1.6 cron + cap-tuning + audit→enforce workflow content untouched"
    - path: CHANGELOG.md
      provides: "[Unreleased] → [1.7.0] - 2026-05-17 entry with Added/Changed/Fixed + audit-mode call-out paragraph + new compare-link footer"
      contains: "TD-01, TD-02, TD-03, TD-04, TD-05, TD-06, TD-07"
  key_links:
    - from: CHANGELOG.md
      to: .planning/REQUIREMENTS-v1.8.md
      via: relative markdown link to SK-01 anchor
      pattern: "SK-01"
    - from: ARCHITECTURE.md
      to: utils/asyncpg_helper.py
      via: filename mention
      pattern: "asyncpg_helper"
---

<objective>
Surgically refresh `README.md`, `ARCHITECTURE.md`, and `docs/memory-eviction.md` for v1.7 changes (TD-01 audit_log auto-create, TD-03 asyncpg_helper centralization, TD-04 near-dup audit-mode guard, TD-05 save_facts batch path, TD-07 bge-m3 HF cache layout, TD-02 create_app factory mention in test docs, TD-06 redis_mock fixture mention). Add a complete v1.7 CHANGELOG entry under [Unreleased] in keep-a-changelog format.

Purpose: Close ROADMAP SC-1 (no stale manual `audit_log` DDL / `?ssl=disable` strip / bge-m3 symlink refs in README + ARCHITECTURE + runbook), SC-2 (memory-eviction.md partial refresh), SC-3 (CHANGELOG v1.7 with item-per-TD + audit-mode→v1.8 SK-01 call-out).

Output: 4 modified doc files. Zero production code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-doc-sweep-v1.7-release/28-CONTEXT.md
@.planning/phases/26-memory-infra-hygiene/26-01-SUMMARY.md
@.planning/phases/26-memory-infra-hygiene/26-04-SUMMARY.md
@.planning/phases/26-memory-infra-hygiene/26-05-SUMMARY.md
@.planning/phases/27-test-isolation-memory-reliability/27-VERIFICATION.md
@.planning/phases/27-test-isolation-memory-reliability/27-03-SUMMARY.md
@.planning/phases/27-test-isolation-memory-reliability/27-04-SUMMARY.md
@README.md
@ARCHITECTURE.md
@docs/memory-eviction.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md surgical patches for v1.7-touched paragraphs</name>
  <files>README.md</files>
  <action>
Edit `README.md` SURGICALLY — touch only paragraphs referencing v1.7-changed code paths. Do NOT rewrite sections, do NOT change agent-first framing from v1.4, do NOT touch unrelated stale content (e.g. qdrant references at line 202 are NOT v1.7-touched; leave per Claude's-discretion default in CONTEXT — "touch only paragraphs that mention v1.7-changed code paths").

Specific edits:

1. **§Module layout (~line 91-110)** — line that reads `memory/                 Redis short-term + PostgreSQL long-term`: amend the trailing comment to mention v1.7 batch path + near-dup guard, e.g. `memory/                 Redis short-term + PostgreSQL long-term (v1.7: save_facts batch + near-duplicate audit guard)`. No new line, just the parenthetical.

2. **§Project status (~line 289-298)** — update `**Current release:** v1.4 — Agent-First Architecture Inversion` to mention v1.7 as the current release-in-flight, e.g. `**Current release:** v1.7 — Memory Tech-Debt Burn-Down (Phases 26–28). Prior milestones: v1.4 Agent-First Architecture Inversion (Phases 16–19), v1.5 Web Search + Multi-Agent Debate + Coverage Lift (Phases 20–22), v1.6 Memory Tool — Agent-Authored Long-Term Facts (Phases 23–25).` Adjust the "Phase summaries:" line to add links to Phase 26 + Phase 27 SUMMARYs (use the last-plan SUMMARY of each, i.e. 26-05-SUMMARY.md and 27-04-SUMMARY.md). Update the "Prior milestones (archived)" line to extend the chain through v1.6.

3. **§Local dev (~line 226-251)** — replace the `conda activate torch_env` + `pip install -r requirements.txt` block (lines ~245-250) with `uv` equivalents per project standard (CLAUDE.md §Python Package Management): `uv venv` + `uv sync` + `uv run uvicorn main:app --reload --port 8000`. The PG_DSN line at ~241 is fine. Add one line below the dev block: `Detailed runbook: [docs/RUNBOOK.md](docs/RUNBOOK.md).`

4. **§Configuration table (~line 274-287)** — no edits required UNLESS a v1.7-changed env var was added. Phase 27 added `memory_near_duplicate_threshold` (config/settings.py:503 per 27-VERIFICATION.md SC-3 evidence). Add one row to the table: `| `MEMORY_NEAR_DUPLICATE_THRESHOLD` | No | Cosine distance threshold for save_fact near-duplicate audit (default `0.05`, v1.7 audit-mode-only — see CHANGELOG v1.7 + SK-01). |`

Constraints:
- Do NOT touch §Quick demo / §Architecture / §Tools the agent calls / §Platform features subsections beyond Module layout.
- Do NOT change the qdrant line at ~202 (Docker stack section was last touched pre-v1.7; not in scope).
- Do NOT rewrite or restructure §Quick start / §Docker stack / §cURL example.
- Use `Edit` tool with sufficient context strings to disambiguate each patch.
- Preserve markdown table alignment in §Configuration.
  </action>
  <verify>
    <automated>grep -q 'save_facts batch' README.md && grep -q 'Phase 26' README.md && grep -q 'Phase 27' README.md && grep -q 'uv venv' README.md && grep -q 'uv sync' README.md && grep -q 'docs/RUNBOOK.md' README.md && grep -q 'MEMORY_NEAR_DUPLICATE_THRESHOLD' README.md && ! grep -q 'conda activate torch_env' README.md && ! grep -q 'pip install -r requirements.txt' README.md</automated>
  </verify>
  <done>README.md has the 4 surgical patches applied; conda/pip lines replaced with uv; no unrelated content changed.</done>
</task>

<task type="auto">
  <name>Task 2: ARCHITECTURE.md surgical patches for §5.2.11 memory + §5.2.13 audit</name>
  <files>ARCHITECTURE.md</files>
  <action>
Edit `ARCHITECTURE.md` SURGICALLY. The file is 486 lines and bilingual (Chinese tables). Touch only the two subsections whose code path changed in v1.7:

1. **§5.2.11 `services/memory/` — 对话记忆 (line ~262-266)** — the existing single-row table at line 266 only describes load_context/save_turn. Append rows for v1.7 deltas:
   - `| `memory_service.py::LongTermMemory.save_facts` | v1.7 批量写入路径 — 1× embed_batch + 1× bulk dedupe SELECT + 1× executemany；近重复触发 `MEMORY_NEAR_DUPLICATE_SKIPPED` 审计行后 INSERT 仍执行（D-09 审计模式，v1.8 → 静默跳过 SK-01）。 |`
   - `| `memory_service.py::LongTermMemory._is_near_duplicate` | v1.7 余弦距离预检（`<=> $vec < 0.05`，由 `memory_near_duplicate_threshold` 设置控制）。 |`
   - `| `memory_service.py::LongTermMemory._get_pool` / `close` | v1.7 通过 `utils/asyncpg_helper.prepare_dsn(...)` 统一处理 `?ssl=disable` URL 参数（TD-03 集中化）。 |`

2. **§5.2.13 `services/audit/` — 审计 (line ~274-278)** — append rows for v1.7 deltas:
   - `| `audit/audit_service.py::AuditService._create_tables` | v1.7 冷启动 `audit_log` 自动创建（DDL + REVOKE UPDATE/DELETE 保留 INSERT-ONLY 不变式）；首个 `_get_pool` 调用触发，不需手工 DDL（TD-01）。 |`
   - `| `audit/audit_service.py::AuditService._get_pool` / `close` | v1.7 单例 asyncpg pool；通过 `utils/asyncpg_helper.prepare_dsn(...)` 处理 DSN（TD-03）。 |`
   - `| `audit/audit_service.py::AuditAction.MEMORY_NEAR_DUPLICATE_SKIPPED` | v1.7 新增审计动作枚举（near-duplicate 预检命中后写入；INSERT 仍执行，审计模式优先于强制执行）。 |`

3. **§3 顶层架构 / §3.2 查询流 (~line 94-134)** — if `_persist_turn → memory_service + audit_service` appears (it does at line 126), append a parenthetical: `（v1.7：memory_service 走批量 save_facts，audit_service pool 单例自启）`. Single-line surgical patch only.

Constraints:
- Do NOT translate the file to English. Match existing zh-CN tone.
- Do NOT touch sections 1, 2, 4, 5.1, or any §5.2.* other than memory + audit.
- Do NOT touch the directory tree at §2.
- Use `Edit` tool with table-row anchors (the existing single-row content) for disambiguation.
- Preserve table column count (2 cols: 文件 | 作用).
  </action>
  <verify>
    <automated>grep -q 'save_facts' ARCHITECTURE.md && grep -q '_is_near_duplicate' ARCHITECTURE.md && grep -q 'asyncpg_helper' ARCHITECTURE.md && grep -q 'audit_log 自动创建' ARCHITECTURE.md && grep -q 'MEMORY_NEAR_DUPLICATE_SKIPPED' ARCHITECTURE.md && grep -q 'INSERT-ONLY' ARCHITECTURE.md && grep -c '## ' ARCHITECTURE.md | awk '$1 >= 5 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>ARCHITECTURE.md §5.2.11 + §5.2.13 + §3.2 patched with v1.7 deltas; structure (heading count) preserved; no untouched section accidentally renumbered.</done>
</task>

<task type="auto">
  <name>Task 3: docs/memory-eviction.md partial refresh for v1.7 deltas</name>
  <files>docs/memory-eviction.md</files>
  <action>
Edit `docs/memory-eviction.md` PARTIALLY — only update sections that v1.7 changes touched per Claude's-discretion default in CONTEXT. The doc is 178 lines and was last refreshed in v1.6 Phase 25 (eviction job + GDPR forget).

v1.7 touched two adjacent concerns:
- TD-04 / TD-05 near-duplicate audit-mode guard (`save_fact` / `save_facts` precheck emits `MEMORY_NEAR_DUPLICATE_SKIPPED` audit row, INSERT still runs in v1.7 per D-09; v1.8 will promote to silent skip via SK-01).
- TD-05 batch path (`save_facts` collapses N round-trips to 1 embed + 1 PG round-trip).

Edits:
1. If a section discusses near-duplicate / dedupe / cosine guard: add a paragraph noting v1.7 audit-mode (`MEMORY_NEAR_DUPLICATE_SKIPPED` audit row emitted, INSERT still runs) and forward-pointer to v1.8 SK-01 silent-skip enforcement.
2. If a section discusses batch ingestion / write throughput / extractor latency: add a sentence noting v1.7 `save_facts` batch path collapses to 1× embed_batch + 1× executemany (per 27-VERIFICATION.md SC-4).
3. If NEITHER concern is mentioned in the existing doc (it may not be — memory-eviction is primarily about capacity-cap eviction, not save-path dedupe), add ONE new H2 section at end-of-file: `## v1.7 deltas` with two short subsections — `### Near-duplicate guard (audit-mode)` and `### Batch save path` — each 2-3 sentences linking to CHANGELOG v1.7 entry + 27-04-SUMMARY.md and 27-03-SUMMARY.md respectively.

Constraints:
- Do NOT rewrite the v1.6 cron + cap-tuning + audit→enforce workflow content.
- Do NOT change the k8s CronJob YAML example or any internal anchor.
- Use `grep -n -E "near.?dup|dedupe|cosine|batch|save_facts"` BEFORE editing to detect whether the appended-section path or the inline-paragraph path applies. Path chosen MUST be recorded in 28-01-SUMMARY.md.
- Final length: ≤ 220 lines (current 178 + at most ~40 lines of additions).
  </action>
  <verify>
    <automated>test $(wc -l < docs/memory-eviction.md) -le 220 && grep -q -E 'save_facts|MEMORY_NEAR_DUPLICATE_SKIPPED|near-?duplicate.*audit' docs/memory-eviction.md && grep -q -E 'v1\.7|SK-01' docs/memory-eviction.md && grep -q 'CronJob' docs/memory-eviction.md</automated>
  </verify>
  <done>docs/memory-eviction.md mentions v1.7 near-dup audit-mode + batch path; v1.6 eviction content intact (CronJob YAML preserved); ≤ 220 lines total.</done>
</task>

<task type="auto">
  <name>Task 4: CHANGELOG.md v1.7 entry (Added / Changed / Fixed + audit-mode call-out)</name>
  <files>CHANGELOG.md</files>
  <action>
**ENG-REVIEW D1 binding constraint — link target rule:** all per-TD references in the CHANGELOG body MUST link to the MILESTONES.md anchor `MILESTONES.md#v17` (NOT to per-Phase SUMMARY paths). Stable anchor decouples CHANGELOG from `.planning/` directory restructuring (avoids the post-archive link-drift window that 28-04 introduces).

Per-TD SUMMARY links still need to be VERIFIED (used internally by 28-04's archive logic and the v1.7 anchor section in MILESTONES.md). Pre-write mapping verification (mandatory first step):
```bash
for td in TD-01 TD-02 TD-03 TD-04 TD-05 TD-06 TD-07; do
  echo "$td:"; grep -l "$td" .planning/phases/2[67]-*/[0-9]*-SUMMARY.md 2>/dev/null | head -2
done
```
Expected mapping: TD-01→26-04, TD-02→27-01, TD-03→26-01, TD-04→27-03, TD-05→27-04, TD-06→27-02, TD-07→26-02. If any grep returns a different plan number, propagate the correction to MILESTONES.md `### v17` section content (28-04 Task 4) — NOT to this CHANGELOG body (which only links to `MILESTONES.md#v17` per D1). Record the verified mapping in 28-01-SUMMARY.md.

Add a complete v1.7.0 entry to `CHANGELOG.md` under the existing `## [Unreleased]` line. Use strict keep-a-changelog format per Claude's-discretion default in CONTEXT: `### Added`, `### Changed`, `### Fixed`.

Replace the `## [Unreleased]` heading at line 8 with:
```
## [Unreleased]

## [1.7.0] - 2026-05-17

Memory Tech-Debt Burn-Down. Production-cleans the memory subsystem (TD-01..TD-07) before adding more features. Zero new user-facing capabilities; pure refactor + reliability.
```

Then under `## [1.7.0] - 2026-05-17`, populate:

**### Added** — one bullet per net-new artifact. Per ENG-REVIEW D1: each bullet links to the MILESTONES.md v1.7 anchor (`[v1.7](MILESTONES.md#v17)`) for full per-phase detail + SUMMARY pointers. CHANGELOG narrative carries the technical detail (what changed + why it matters); MILESTONES.md owns the per-Phase navigation.
- `**TD-01** — `services/audit/audit_service.py` self-bootstraps the `audit_log` PostgreSQL table on first call via `_get_pool` → `_create_tables`. INSERT-ONLY invariant (`REVOKE UPDATE, DELETE ON audit_log FROM PUBLIC`) preserved. Cold-start fresh PG no longer requires manual DDL. See [v1.7 milestone detail](MILESTONES.md#v17).`
- `**TD-03** — `utils/asyncpg_helper.prepare_dsn(dsn)` centralizes asyncpg URL `?ssl=disable` strip (asyncpg URL parser misreads the literal). Both `services/memory/memory_service.py` and `services/audit/audit_service.py` import the helper; per-module inline copies removed. See [v1.7 milestone detail](MILESTONES.md#v17).`
- `**TD-07** — bge-m3 model loader resolves vanilla HuggingFace cache layout `{MODEL_DIR}/BAAI/bge-m3/` natively. Legacy `{MODEL_DIR}/embedding_models/bge-m3/` path still supported (backwards-compat). See [v1.7 milestone detail](MILESTONES.md#v17).`
- `**TD-02** — `tests/factories/app.py::create_app()` factory + `_configure_app(app)` extraction in `main.py`. Per-test isolated FastAPI app construction; parallel cross-contamination test green. 34-entry singleton inventory + completeness lint. See [v1.7 milestone detail](MILESTONES.md#v17).`
- `**TD-06** — `redis_mock` fixture in `tests/conftest.py` (fakeredis-backed) + `pytest_collection_modifyitems` hook auto-applies to `@pytest.mark.uses_redis` tests. `ShortTermMemory._get_client` now delegates to `utils.cache.get_redis` (single mock target). Unit suite runs without live Redis. See [v1.7 milestone detail](MILESTONES.md#v17).`
- `**TD-04** — `LongTermMemory._is_near_duplicate(text, threshold=0.05)` cosine precheck before save. Near-duplicate hits emit `MEMORY_NEAR_DUPLICATE_SKIPPED` audit row (new `AuditAction` enum value). New `MEMORY_NEAR_DUPLICATE_THRESHOLD` setting. See [v1.7 milestone detail](MILESTONES.md#v17).`
- `**TD-05** — `LongTermMemory.save_facts(list[ExtractedFact])` batch path: 1× `embed_batch` + 1× bulk dedupe SELECT (C1 `unnest($1::text[]) WITH ORDINALITY` + `vec_txt::vector` cast — sidesteps pgvector.asyncpg codec hijack) + 1× `executemany`. `save_fact` retained as thin D-12 wrapper. ExtractorAgent dispatch (D-17) migrated. Benchmark: p50 25.31ms → 5.51ms (speedup 19.80ms with MagicMock embedder; ~123ms expected with real bge-m3). See [v1.7 milestone detail](MILESTONES.md#v17).`
- `**DOC-01** — `docs/RUNBOOK.md` (new); `docs/release-notes-v1.7.md` (new); `.planning/REQUIREMENTS-v1.8.md` (new scaffold with 7 pre-seeded backlog items: SK-01, TOC-01, OAI-01, EVT-01, MYPY-01, TEST-INFRA-01, TEST-INFRA-02). README + ARCHITECTURE + memory-eviction.md surgically refreshed.`

**### Changed** — one bullet per behavior shift:
- `**Memory write path** — `LongTermMemory.save_fact` is now a thin wrapper that delegates to `save_facts([...])`. Signature unchanged; embed-failure raise contract preserved. ExtractorAgent (`services/agent/extractor.py`) calls `save_facts(...)` once per turn instead of the per-fact for-loop.`
- `**Test conventions** — Redis-dependent unit tests must apply `@pytest.mark.uses_redis` (auto-mocked via `redis_mock` fixture). Audit + memory integration suites can be migrated to `app_factory` fixture for per-test isolated app construction (existing tests left in place per Phase 27 D-05).`

**### Fixed** — one bullet per latent bug closed:
- `**`audit_log` cold-start manual-DDL footgun** — fresh PG cluster no longer requires operator to run `audit_log` DDL by hand before first audit-write succeeds (TD-01).`
- `**asyncpg `?ssl=disable` URL literal misread** — duplicate per-module strip helpers consolidated to `utils/asyncpg_helper.prepare_dsn` (TD-03; per A1 also handles short-form `postgres+asyncpg://` → `postgres://` scheme normalization).`
- `**bge-m3 fresh-machine setup** — vanilla HuggingFace cache layout resolves without symlink (TD-07).`

**Final paragraph (after Fixed):**
> Near-duplicate guard is **audit-mode** in v1.7 (`MEMORY_NEAR_DUPLICATE_SKIPPED` audit row emitted on hit; INSERT still runs). v1.8 will promote to silent-skip with TOCTOU mitigation — see [SK-01](.planning/REQUIREMENTS-v1.8.md) + [TOC-01](.planning/REQUIREMENTS-v1.8.md). This audit-mode-before-enforce discipline follows v1.6 EVICT-02 precedent.

**Compare-link footer** — extend the existing footer block at end-of-file. Current footer has `[Unreleased]: ...compare/v1.4.0...HEAD` (line 88 per Read at offset 80). Update to:
```
[Unreleased]: https://github.com/rothenbergverkuilenrn60-oss/rag-enterprise/compare/v1.7.0...HEAD
[1.7.0]:      https://github.com/rothenbergverkuilenrn60-oss/rag-enterprise/compare/v1.6.0...v1.7.0
[1.6.0]:      https://github.com/rothenbergverkuilenrn60-oss/rag-enterprise/compare/v1.4.0...v1.6.0
```
(The `[1.6.0]` link needs adding — the existing footer skips it. Pre-existing CHANGELOG `[1.5.0]` gap is explicitly NOT in scope per CONTEXT out-of-scope row 6 — do NOT add a [1.5.0] link or entry.)

Constraints:
- Keep TD-* bullet ordering: TD-01, TD-03, TD-07 (Phase 26), then TD-02, TD-06, TD-04, TD-05 (Phase 27), then DOC-01 (Phase 28).
- **ENG-REVIEW D1 binding constraint:** all per-TD links use `MILESTONES.md#v17` anchors. ZERO direct links to `.planning/milestones/v1.7-phases/...` paths in the CHANGELOG. SK-01 / TOC-01 references in the audit-mode call-out link to `.planning/REQUIREMENTS-v1.8.md` (single, stable file).
- INSERT-ONLY invariant phrasing must appear verbatim in the TD-01 bullet (do not soften to "INSERT-only").
- Do NOT modify the existing [1.6.0] / [1.4.0] / [1.3.0] / [1.2.0] / [1.1.0] / [1.0.0] entries.
- Preserve the Keep a Changelog 1.1.0 header banner at top of file (lines 1-6).
  </action>
  <verify>
    <automated>grep -q '## \[1.7.0\] - 2026-05-17' CHANGELOG.md && grep -q '### Added' CHANGELOG.md && grep -q '### Changed' CHANGELOG.md && grep -q '### Fixed' CHANGELOG.md && grep -q 'TD-01' CHANGELOG.md && grep -q 'TD-02' CHANGELOG.md && grep -q 'TD-03' CHANGELOG.md && grep -q 'TD-04' CHANGELOG.md && grep -q 'TD-05' CHANGELOG.md && grep -q 'TD-06' CHANGELOG.md && grep -q 'TD-07' CHANGELOG.md && grep -q 'DOC-01' CHANGELOG.md && grep -q 'INSERT-ONLY' CHANGELOG.md && grep -q 'audit-mode' CHANGELOG.md && grep -q 'SK-01' CHANGELOG.md && grep -q 'TOC-01' CHANGELOG.md && grep -q 'compare/v1.6.0...v1.7.0' CHANGELOG.md && grep -q 'compare/v1.7.0...HEAD' CHANGELOG.md && ! grep -q 'compare/v1.4.0...HEAD' CHANGELOG.md && grep -q 'MILESTONES.md#v17' CHANGELOG.md && ! grep -qE '\.planning/milestones/v1\.7-phases/' CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md has complete [1.7.0] entry with all 7 TD-IDs + DOC-01 in Added; behavior shifts in Changed; latent bugs in Fixed; audit-mode → SK-01/TOC-01 call-out paragraph; compare-link footer extended to v1.7.0; pre-existing [1.5.0] gap untouched; existing entries preserved.</done>
</task>

</tasks>

<verification>
- All 4 files modified; verify gates per task above pass.
- File ownership exclusive within Wave 1: README.md / ARCHITECTURE.md / docs/memory-eviction.md / CHANGELOG.md are NOT touched by plans 28-00, 28-02, 28-03 (per their `files_modified` frontmatter). Parallel-safe.
- Spot-check (not automated): `git diff README.md ARCHITECTURE.md` shows only the targeted patches; no whitespace churn, no unrelated section rewrites.
- No production code touched: `git diff --name-only HEAD` shows only README.md / ARCHITECTURE.md / docs/memory-eviction.md / CHANGELOG.md.
</verification>

<success_criteria>
- ROADMAP SC-1: README + ARCHITECTURE reference all v1.7 changes; no stale manual `audit_log` DDL / per-module `?ssl=disable` strip / bge-m3 symlink references.
- ROADMAP SC-2: `docs/memory-eviction.md` partial refresh (v1.7 deltas mentioned where contextually appropriate; v1.6 content untouched).
- ROADMAP SC-3: `CHANGELOG.md` v1.7 entry in keep-a-changelog format with one item per TD-* requirement + audit-mode → v1.8 SK-01 call-out paragraph.
</success_criteria>

<output>
Create `.planning/phases/28-doc-sweep-v1.7-release/28-01-SUMMARY.md` recording:
1. Exact patches applied to each of the 4 files (line range or anchor + before/after summary).
2. Which path was taken for `docs/memory-eviction.md` (inline-paragraph vs new-section).
3. CHANGELOG SUMMARY-link path decision (post-archive `.planning/milestones/v1.7-phases/` confirmed).
4. Any deviations from this plan with rationale.
</output>
