---
plan_id: 28-02
phase: 28
plan: 2
type: execute
wave: 1
depends_on: []
requirements: [DOC-01]
files_modified:
  - docs/release-notes-v1.7.md
  - .planning/milestones/v1.7-release-tag.md
autonomous: true
notes: |
  Implements D-07: split release docs by audience.
  - docs/release-notes-v1.7.md = public/ops-focused 5-section template (Highlights / Shipped Items / Ops Impact / Upgrade Notes / Breaking Changes)
  - .planning/milestones/v1.7-release-tag.md = planning-internal annotated tag ceremony + checklist
  Closes ROADMAP SC-4.
  Pattern source: .planning/milestones/v1.4-phases/19-agent-first-docs-demo-release/release-notes-v1.4.md + release-tag-commands.md (replicate structure, NOT v1.4 content).

must_haves:
  truths:
    - "An ops reader scanning release-notes-v1.7.md learns: zero new user-facing capabilities; zero upgrade steps required; zero breaking changes — and what changed under the hood (TD-01..07)."
    - "A release-cutter following v1.7-release-tag.md can cut + push the v1.7.0 annotated tag, publish the GitHub release, and roll back if needed."
    - "Every TD-* requirement appears in §Shipped Items of release-notes-v1.7.md with a SUMMARY.md link."
    - "The audit-mode-before-enforce discipline (v1.6 EVICT-02 → v1.7 D-09 → v1.8 SK-01) is explicit in §Ops Impact so future readers can trace the path."
  artifacts:
    - path: docs/release-notes-v1.7.md
      provides: "Public release notes — 5-section template per D-07"
      min_lines: 80
      contains:
        - "## Highlights"
        - "## Shipped Items"
        - "## Ops Impact"
        - "## Upgrade Notes"
        - "## Breaking Changes"
        - "TD-01"
        - "TD-07"
        - "DOC-01"
        - "MEMORY_NEAR_DUPLICATE_SKIPPED"
        - "SK-01"
    - path: .planning/milestones/v1.7-release-tag.md
      provides: "Annotated tag ceremony + pre-tag checklist + rollback"
      min_lines: 60
      contains:
        - "git tag -a v1.7.0"
        - "gh release create v1.7.0"
        - "Rollback"
        - "CHANGELOG"
        - "RUNBOOK"
        - "release-notes-v1.7"
  key_links:
    - from: docs/release-notes-v1.7.md
      to: .planning/REQUIREMENTS-v1.8.md
      via: SK-01 anchor
      pattern: "SK-01"
    - from: .planning/milestones/v1.7-release-tag.md
      to: docs/release-notes-v1.7.md
      via: relative path
      pattern: "docs/release-notes-v1.7\\.md"
    - from: .planning/milestones/v1.7-release-tag.md
      to: CHANGELOG.md
      via: pre-tag checklist item
      pattern: "CHANGELOG"
---

<objective>
Draft the v1.7 release artifacts: public release notes (`docs/release-notes-v1.7.md`) and planning-internal tag ceremony (`.planning/milestones/v1.7-release-tag.md`). Split by audience per D-07; both files created in this phase.

Purpose: Close ROADMAP SC-4 (release notes drafted with shipped-items list + ops impact + upgrade notes; tag-commands updated for v1.7 tag ceremony).

Output: 2 new files. Zero production code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-doc-sweep-v1.7-release/28-CONTEXT.md
@.planning/phases/26-memory-infra-hygiene/26-01-SUMMARY.md
@.planning/phases/26-memory-infra-hygiene/26-04-SUMMARY.md
@.planning/phases/26-memory-infra-hygiene/26-05-SUMMARY.md
@.planning/phases/27-test-isolation-memory-reliability/27-VERIFICATION.md
@.planning/phases/27-test-isolation-memory-reliability/27-04-SUMMARY.md
@.planning/phases/27-test-isolation-memory-reliability/27-BENCHMARK.md
@.planning/milestones/v1.4-phases/19-agent-first-docs-demo-release/release-notes-v1.4.md
@.planning/milestones/v1.4-phases/19-agent-first-docs-demo-release/release-tag-commands.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft docs/release-notes-v1.7.md (5-section template per D-07)</name>
  <files>docs/release-notes-v1.7.md</files>
  <action>
**Pre-write SUMMARY-link verification (mandatory first step):** before writing TD-NN ↔ Phase-XX-YY SUMMARY links below, run the same grep loop as 28-01 Task 4:
```bash
for td in TD-01 TD-02 TD-03 TD-04 TD-05 TD-06 TD-07; do
  echo "$td:"; grep -l "$td" .planning/phases/2[67]-*/[0-9]*-SUMMARY.md 2>/dev/null | head -2
done
```
Plan body assumes: TD-01→26-04, TD-02→27-01, TD-03→26-01, TD-04→27-03, TD-05→27-04, TD-06→27-02, TD-07→26-02. If any TD's grep returns a different plan number, update both this plan's Shipped Items links AND plan 28-01 Task 4 CHANGELOG links to match. Both plans MUST agree.

Create `docs/release-notes-v1.7.md` from scratch. Audience = public + ops (mixed per D-07). Use the locked 5-section template from CONTEXT D-07:

Top of file: H1 `# v1.7.0 Release Notes — Memory Tech-Debt Burn-Down` + 2-line preamble: shipped date (2026-05-17), phase range (26–28), summary statement "Pure refactor + reliability — zero new user-facing capabilities."

**1. ## Highlights (2-3 sentences):**
> v1.7 is a refactor + reliability milestone. The memory subsystem (write path, schema bootstrap, asyncpg DSN handling, model-cache layout, test isolation) is now production-clean. Zero new user-facing capabilities; zero upgrade steps required.

**2. ## Shipped Items (grouped by TD-ID, link to relevant SUMMARY.md):**

Phase 26 — Memory Infra Hygiene:
- **TD-01** — `audit_log` PostgreSQL table auto-creates on first call into `services/audit/audit_service.py` (cold-start no longer requires manual DDL). INSERT-ONLY invariant (`REVOKE UPDATE, DELETE ON audit_log FROM PUBLIC`) preserved. [Phase 26-04 SUMMARY](../.planning/milestones/v1.7-phases/26-memory-infra-hygiene/26-04-SUMMARY.md).
- **TD-03** — `utils/asyncpg_helper.prepare_dsn(dsn)` centralizes the `?ssl=disable` URL-param strip (asyncpg URL parser misreads the literal). Both memory + audit services consume the helper. [Phase 26-01 SUMMARY](../.planning/milestones/v1.7-phases/26-memory-infra-hygiene/26-01-SUMMARY.md).
- **TD-07** — bge-m3 loads from vanilla HuggingFace cache `{MODEL_DIR}/BAAI/bge-m3/`. Legacy `{MODEL_DIR}/embedding_models/bge-m3/` path still resolved (backwards-compat). Symlink workaround removed from documented setup. [Phase 26-02 SUMMARY](../.planning/milestones/v1.7-phases/26-memory-infra-hygiene/26-02-SUMMARY.md).

Phase 27 — Test Isolation + Memory Reliability:
- **TD-02** — `tests/factories/app.py::create_app()` factory + `main._configure_app(app)` extraction. Per-test isolated FastAPI app construction; parallel cross-contamination test green. 34-entry singleton inventory + completeness lint. [Phase 27-01 SUMMARY](../.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-01-SUMMARY.md).
- **TD-06** — `redis_mock` fixture (fakeredis-backed) auto-applied via `@pytest.mark.uses_redis` marker. `ShortTermMemory._get_client` delegates to `utils.cache.get_redis` (single mock target). Unit suite runs without live Redis. [Phase 27-02 SUMMARY](../.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-02-SUMMARY.md) + [27-02-DIAGNOSTIC](../.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-02-DIAGNOSTIC.md).
- **TD-04** — `LongTermMemory._is_near_duplicate` cosine precheck (`<embedding> <=> $vec < 0.05`); near-duplicate hits emit `MEMORY_NEAR_DUPLICATE_SKIPPED` audit row. New `AuditAction` enum value; new `MEMORY_NEAR_DUPLICATE_THRESHOLD` setting. **Audit-mode-only in v1.7** per D-09 — the INSERT still runs. [Phase 27-03 SUMMARY](../.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-03-SUMMARY.md).
- **TD-05** — `LongTermMemory.save_facts(list[ExtractedFact])` batch path: 1× `embed_batch` + 1× bulk dedupe SELECT (C1 `unnest($1::text[]) WITH ORDINALITY` + `vec_txt::vector` cast) + 1× `executemany`. `save_fact` retained as D-12 wrapper. ExtractorAgent migrated. Benchmark: p50 25.31ms → 5.51ms with MagicMock embedder (speedup 19.80ms; ~123ms expected on real bge-m3). [Phase 27-04 SUMMARY](../.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-04-SUMMARY.md) + [27-BENCHMARK](../.planning/milestones/v1.7-phases/27-test-isolation-memory-reliability/27-BENCHMARK.md).

Phase 28 — Doc Sweep + v1.7 Release:
- **DOC-01** — `docs/RUNBOOK.md` (new operational reference); this release notes file; `.planning/REQUIREMENTS-v1.8.md` scaffold (7 backlog items pre-seeded); README + ARCHITECTURE + memory-eviction.md surgically refreshed; v1.7 archive + MILESTONES.md backfill.

**3. ## Ops Impact:**
What changes in production:
- `audit_log` table self-bootstraps on first audit-write — fresh PG cluster operators no longer run manual DDL (TD-01).
- asyncpg DSN `?ssl=disable` strip is now centralized in `utils/asyncpg_helper.prepare_dsn` — operators do not need to per-module-handle this; new modules that open their own asyncpg pools should import the helper (TD-03).
- bge-m3 loads from the vanilla HuggingFace cache (`{MODEL_DIR}/BAAI/bge-m3/`) — symlink workaround unnecessary. Pre-download via `huggingface-cli download BAAI/bge-m3 --local-dir $MODEL_DIR/BAAI/bge-m3` or rely on first-use auto-download (TD-07).
- `redis_mock` fixture in `tests/conftest.py` — unit suite no longer requires a live Redis. Integration tests that genuinely need Redis stay un-mocked (TD-06).
- **Near-duplicate save audit metric** — `save_fact` / `save_facts` emit `MEMORY_NEAR_DUPLICATE_SKIPPED` audit rows when an embedding is within cosine distance `MEMORY_NEAR_DUPLICATE_THRESHOLD` (default 0.05) of an existing fact. **Critical**: in v1.7 this is **audit-mode only** — the INSERT still runs (per v1.6 EVICT-02 audit-mode-before-enforce discipline applied to v1.7 D-09). v1.8 will promote this to silent-skip with TOCTOU mitigation — see [SK-01](../.planning/REQUIREMENTS-v1.8.md) + [TOC-01](../.planning/REQUIREMENTS-v1.8.md). Operators tuning the threshold should observe the audit-row volume before assuming v1.8 silent-skip will drop the same fraction.

**4. ## Upgrade Notes:**
**None required.** v1.7 is a zero-prod-behavior-change milestone. Existing deployments upgrade by pulling the v1.7.0 tag; no database migration, no config change, no API change. Optional: rebuild your test base image with `fakeredis` available if your CI runs the unit suite without live Redis (per `pyproject.toml` dev deps).

**5. ## Breaking Changes:**
**None.** Public API surface unchanged (`/api/v1/query`, `/query/stream`, `/agent/v1/run/stream`, `/ingest`, `/ingest/async`, `/ingest/status/{task_id}`, `/memory/forget` all behave identically to v1.6.0). `LongTermMemory.save_fact` signature unchanged (now a thin wrapper around `save_facts([...])`; embed-failure raise contract preserved).

Footer:
> Full diff: [v1.6.0...v1.7.0](https://github.com/rothenbergverkuilenrn60-oss/rag-enterprise/compare/v1.6.0...v1.7.0).
> Changelog: [CHANGELOG.md](../CHANGELOG.md).
> Tag ceremony: [.planning/milestones/v1.7-release-tag.md](../.planning/milestones/v1.7-release-tag.md) (planning-internal).

Constraints:
- Use SUMMARY links with **post-archive** path `.planning/milestones/v1.7-phases/...` (file ships with milestone shipment; archive plan 28-04 moves dirs in same milestone).
- The path prefix from `docs/release-notes-v1.7.md` to `.planning/...` is `../.planning/...` (two-level up wrong — `docs/` is one level deep, `.planning/` is sibling of `docs/`). Verify: from `docs/release-notes-v1.7.md`, relative path to `.planning/` is `../.planning/`. Confirm with `realpath` if uncertain.
- Audit-mode wording: use the exact phrase "the INSERT still runs" to mirror 27-VERIFICATION.md SC-3 wording (ROADMAP SC-3 originally said "save is skipped" — explicitly overridden per the verification report).
- Do NOT invent breaking changes; v1.7 has none — state explicitly.
- Length target: 100–180 lines.
  </action>
  <verify>
    <automated>test -f docs/release-notes-v1.7.md && grep -q '^## Highlights' docs/release-notes-v1.7.md && grep -q '^## Shipped Items' docs/release-notes-v1.7.md && grep -q '^## Ops Impact' docs/release-notes-v1.7.md && grep -q '^## Upgrade Notes' docs/release-notes-v1.7.md && grep -q '^## Breaking Changes' docs/release-notes-v1.7.md && grep -q 'TD-01' docs/release-notes-v1.7.md && grep -q 'TD-02' docs/release-notes-v1.7.md && grep -q 'TD-03' docs/release-notes-v1.7.md && grep -q 'TD-04' docs/release-notes-v1.7.md && grep -q 'TD-05' docs/release-notes-v1.7.md && grep -q 'TD-06' docs/release-notes-v1.7.md && grep -q 'TD-07' docs/release-notes-v1.7.md && grep -q 'DOC-01' docs/release-notes-v1.7.md && grep -q 'MEMORY_NEAR_DUPLICATE_SKIPPED' docs/release-notes-v1.7.md && grep -q 'INSERT still runs' docs/release-notes-v1.7.md && grep -q 'SK-01' docs/release-notes-v1.7.md && grep -q 'v1.6.0\.\.\.v1.7.0' docs/release-notes-v1.7.md</automated>
  </verify>
  <done>docs/release-notes-v1.7.md exists with all 5 H2 sections; all 7 TD-IDs + DOC-01 in Shipped Items; audit-mode "INSERT still runs" wording present; SK-01/TOC-01 forward refs present; compare-link footer present.</done>
</task>

<task type="auto">
  <name>Task 2: Draft .planning/milestones/v1.7-release-tag.md (annotated tag ceremony + checklist)</name>
  <files>.planning/milestones/v1.7-release-tag.md</files>
  <action>
Create `.planning/milestones/v1.7-release-tag.md` from scratch. Audience = release-cutter (planning-internal). Mirror the structure of `.planning/milestones/v1.4-phases/19-agent-first-docs-demo-release/release-tag-commands.md` but use v1.7 specifics.

Top of file: H1 `# v1.7 Release Ceremony — Annotated Tag + GitHub Release` + 2-line preamble: this file is planning-internal; the user runs commands locally after the v1.7 milestone PR merges to master.

**## Pre-tag checklist** (checkbox list — operator marks each as they verify):
- [ ] v1.7 milestone PR merged to `master` (Phases 26 + 27 + 28 all in)
- [ ] `CHANGELOG.md` has `## [1.7.0] - 2026-05-17` entry with TD-01..TD-07 + DOC-01 bullets + audit-mode → SK-01 call-out (verify: `grep -q 'compare/v1.6.0...v1.7.0' CHANGELOG.md`)
- [ ] `README.md` reflects v1.7 in §Project status + §Local dev uses uv (verify: `grep -q 'Phase 27' README.md && grep -q 'uv venv' README.md`)
- [ ] `ARCHITECTURE.md` §5.2.11 + §5.2.13 patched (verify: `grep -q 'save_facts' ARCHITECTURE.md && grep -q 'audit_log 自动创建' ARCHITECTURE.md`)
- [ ] `docs/RUNBOOK.md` exists with 3 sections (Local dev / Ops procedures / Troubleshooting)
- [ ] `docs/release-notes-v1.7.md` exists with 5 sections (Highlights / Shipped Items / Ops Impact / Upgrade Notes / Breaking Changes)
- [ ] `.planning/REQUIREMENTS-v1.8.md` scaffold exists with 7 pre-seeded items (SK-01, TOC-01, OAI-01, EVT-01, MYPY-01, TEST-INFRA-01, TEST-INFRA-02)
- [ ] Phase 26/27/28 directories archived to `.planning/milestones/v1.7-phases/` (verify: `ls .planning/milestones/v1.7-phases/26-* 27-* 28-* | wc -l` returns 3)
- [ ] `.planning/milestones/v1.7-ROADMAP.md` + `.planning/milestones/v1.7-REQUIREMENTS.md` snapshots exist
- [ ] `MILESTONES.md` at repo root exists with v1.7 entry (and v1.0..v1.6 backfill per Claude's discretion)
- [ ] `gh auth status` reports authenticated; canonical repo URL known (e.g. `rothenbergverkuilenrn60-oss/rag-enterprise`)
- [ ] Local clone has `master` checked out and up-to-date (`git pull --ff-only origin master`)

**## Step 1 — Verify HEAD is the merge commit**
```bash
git checkout master
git pull --ff-only origin master
git log --oneline -5
```
Confirm the v1.7 milestone PR's merge commit is `HEAD` (or close to it). If not, STOP and figure out why.

**## Step 2 — Extract tag annotation from release-notes-v1.7.md**
The tag annotation is the §Highlights paragraph plus a 4-line phase summary. Construct inline (no awk extraction needed — annotation is small):
```bash
TAG_MSG="v1.7.0 — Memory Tech-Debt Burn-Down

Phase 26: Memory Infra Hygiene (TD-01, TD-03, TD-07)
Phase 27: Test Isolation + Memory Reliability (TD-02, TD-04, TD-05, TD-06)
Phase 28: Doc Sweep + v1.7 Release (DOC-01)

Pure refactor + reliability milestone. Zero new user-facing capabilities;
production-cleans the memory subsystem before adding more features."
echo "$TAG_MSG" | head -10
```

**## Step 3 — Cut + push the annotated tag**
```bash
git tag -a v1.7.0 master -m "$TAG_MSG"
git tag -v v1.7.0 | head -5
git push origin v1.7.0
```
Verify on GitHub: `https://github.com/<owner>/<repo>/releases/tag/v1.7.0` shows the tag with annotation.

**## Step 4 — Publish the GitHub release**
```bash
gh release create v1.7.0 \
    --title "v1.7.0 — Memory Tech-Debt Burn-Down" \
    --notes-file docs/release-notes-v1.7.md \
    --verify-tag
```
Confirms tag exists, attaches the public release notes as the release body.

**## Step 5 — Post-publish verification**
- [ ] Visit `https://github.com/<owner>/<repo>/releases/tag/v1.7.0`
- [ ] Body renders cleanly (headings, tables, code blocks, internal links to SUMMARY.md files resolve at the v1.7.0 tag)
- [ ] Compare-link `v1.6.0...v1.7.0` at bottom resolves to valid GitHub diff
- [ ] CHANGELOG.md footer line `[1.7.0]: ...compare/v1.6.0...v1.7.0` works on GitHub

**## Step 6 — Update STATE.md to shipped**
```bash
# Edit .planning/STATE.md frontmatter:
#   status: shipped (or "Phase 28 shipped; v1.7 milestone complete")
#   last_updated: 2026-05-17
#   last_activity: "2026-05-17 — v1.7 milestone shipped (tag v1.7.0)"
git add .planning/STATE.md
git commit -m "docs(state): mark v1.7 milestone shipped"
git push origin master
```

**## Rollback (if Step 5 reveals a problem)**
```bash
# Delete remote tag:
git push --delete origin v1.7.0
# Delete local tag:
git tag -d v1.7.0
# Delete GitHub release:
gh release delete v1.7.0 --yes
```
Then fix the underlying issue on `master` and re-run Steps 2–5.

Constraints:
- File path is `.planning/milestones/v1.7-release-tag.md` (NOT under `phases/`) — it's a milestone-level artifact, separate from phase planning.
- Mirror the v1.4 release-tag-commands.md structure but DO NOT use `<owner>/<repo>` substitution complexity unless needed — the canonical repo URL `rothenbergverkuilenrn60-oss/rag-enterprise` is already known and used in CHANGELOG footer.
- Reference `docs/release-notes-v1.7.md` for the GitHub release body via `--notes-file`.
- Include a Rollback section per the v1.4 pattern.
- Length target: 80–150 lines.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.7-release-tag.md && grep -q '^## Pre-tag checklist' .planning/milestones/v1.7-release-tag.md && grep -q 'git tag -a v1.7.0' .planning/milestones/v1.7-release-tag.md && grep -q 'gh release create v1.7.0' .planning/milestones/v1.7-release-tag.md && grep -q 'docs/release-notes-v1.7.md' .planning/milestones/v1.7-release-tag.md && grep -q -i 'rollback' .planning/milestones/v1.7-release-tag.md && grep -q 'CHANGELOG' .planning/milestones/v1.7-release-tag.md && grep -q 'RUNBOOK' .planning/milestones/v1.7-release-tag.md && grep -q 'MILESTONES.md' .planning/milestones/v1.7-release-tag.md</automated>
  </verify>
  <done>.planning/milestones/v1.7-release-tag.md exists with pre-tag checklist (covering CHANGELOG, README, ARCHITECTURE, RUNBOOK, release-notes, REQUIREMENTS-v1.8, archive dirs, snapshots, MILESTONES.md, gh auth) + 6 ceremony steps + rollback section.</done>
</task>

</tasks>

<verification>
- Both files created at correct paths.
- File ownership exclusive within Wave 1: no overlap with plans 28-00 / 28-01 / 28-03 `files_modified`.
- No production code touched: `git diff --name-only HEAD` shows only docs/release-notes-v1.7.md + .planning/milestones/v1.7-release-tag.md.
</verification>

<success_criteria>
- ROADMAP SC-4: release notes drafted with shipped-items list + ops impact + upgrade notes; tag-commands updated for v1.7 tag ceremony.
- Every TD-01..TD-07 + DOC-01 findable in release-notes-v1.7.md §Shipped Items (verifier requirement per 28-CONTEXT.md §"Notes for downstream agents").
</success_criteria>

<output>
Create `.planning/phases/28-doc-sweep-v1.7-release/28-02-SUMMARY.md` recording:
1. Both file paths + line counts.
2. SUMMARY-link path strategy (post-archive `../.planning/milestones/v1.7-phases/...` confirmed).
3. Any deviations from the 5-section template or ceremony structure.
</output>
