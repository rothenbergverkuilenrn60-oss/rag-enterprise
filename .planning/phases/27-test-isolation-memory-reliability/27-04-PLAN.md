---
plan_id: 27-04-save-facts-batch
phase: 27
plan: 04
type: execute
wave: 2
depends_on: [27-00-test-infra-prep, 27-03-save-fact-cosine-precheck]
requirements: [TD-05, TD-02]
files_modified:
  - services/memory/memory_service.py
  - services/agent/extractor.py
  - tests/unit/memory/test_save_facts_batch.py
  - tests/unit/memory/test_save_facts_batch_dedupe.py
  - tests/unit/memory/test_save_facts_embed_batch_fallback.py
  - tests/integration/memory/__init__.py
  - tests/integration/memory/test_memory_suite_factory_migrated.py
  - tests/benchmark/__init__.py
  - tests/benchmark/test_extractor_latency.py
autonomous: true
notes: |
  Wave 2 SC-4 + SC-5 + memory-side SC-1. Builds on 27-03 (cosine precheck) + 27-00 (test infra) + 27-01 (create_app/app_factory).
  C1 SQL CORRECTION: bulk dedupe SQL MUST use `unnest($1::text[]) WITH ORDINALITY AS t(vec_txt, idx)` + inline `vec_txt::vector` cast (NOT `unnest($1::vector[])`). The pgvector.asyncpg register_vector codec hijacks `vector[]` parameter binding — empirically validated against live PG (RESEARCH §10 lines 281-304). Pass `$1` as `list[str]` of vector literals: `'[' + ','.join(str(x) for x in vec) + ']'`.
  C2 EMBED FAIL-FAST CORRECTION: All 3 embedders (Ollama/OpenAI/HuggingFace) RAISE on first failed text — they do NOT return per-item None. save_facts MUST wrap `embed_batch` in try/except `(httpx.HTTPError, RuntimeError, OSError)`; on failure, fall back to `asyncio.gather(*[embed_one(t) for t in texts], return_exceptions=True)`, then treat per-item BaseException as None. Test `test_save_facts_embed_batch_fallback.py` asserts this fallback path runs.
  C3 D-09 AUDIT-MODE INHERITED: batch path honors the same audit-mode-only contract — bulk dedupe identifies duplicates, audit rows fire for each, but the INSERT (executemany) inserts ALL rows including the duplicates. v1.7 = metric only; v1.8 = enforcement.
  - D-12 save_fact wrapper migration: save_fact becomes `await self.save_facts([ExtractedFact(...)])`. Wrapper coerces raw `fact: str` + raw `importance: float` into `ExtractedFact(fact=fact, category="recurring_topics", importance=round_to_literal(importance))` per RESEARCH §Theme 4 "Caveat for D-12 wrapper" lines 752-757. Round importance to nearest of {0.2, 0.5, 0.8}.
  - SC-4 "exactly 1 PG round-trip" — interpret generously as "1 embed + O(1) PG RTTs not O(N)". Actual shape: 1× embed_batch + 1× bulk dedupe SELECT + 1× executemany INSERT + K× audit_log (where K = duplicate count) = 3 + K RTT. Document in SUMMARY per RESEARCH §"Mock-counting test for 1-RTT contract" lines 928-951.
  - SC-5 latency benchmark: relative measurement (RESEARCH §"Latency baseline measurement" lines 917-926). Two timed loops on the SAME branch: (A) 5× sequential save_fact via the D-12 wrapper path looped, (B) 1× save_facts([5 facts]). Assert `median(A) - median(B) >= 80ms` tolerant floor (RESEARCH calculates expected ~123ms speedup; 80ms gives 30% variance margin).
  - Memory integration suite migration is here (not 27-01) because it depends on save_facts existing.

must_haves:
  truths:
    - "LongTermMemory.save_facts(facts: list[ExtractedFact], *, user_id, tenant_id, source_doc) -> SaveFactsResult exists and ingests N facts in 1 embed_batch + 1 bulk dedupe + 1 executemany"
    - "SaveFactsResult dataclass carries saved_count, skipped_near_duplicates, skipped_embed_failures fields"
    - "save_fact (singular) becomes a thin wrapper that delegates to save_facts via D-12; importance rounded to {0.2, 0.5, 0.8} literal"
    - "Bulk dedupe SQL uses unnest($1::text[]) WITH ORDINALITY + inline ::vector cast (C1 — NOT $1::vector[])"
    - "embed_batch failure falls back to asyncio.gather(*embed_one, return_exceptions=True); per-item exceptions count as skipped_embed_failures (C2)"
    - "Batch dedupe honors D-09 audit-mode-only: duplicates emit audit rows AND are inserted (NOT skipped) — C3 preservation in batch path"
    - "ExtractorAgent._run_and_persist replaces the for-loop save_fact calls with single save_facts() call"
    - "Memory integration suite has >=1 NEW test exercising create_app() + save_facts end-to-end via real PG (SC-1 memory-side coverage)"
    - "SC-5 latency benchmark captures baseline_p50 vs new_p50 and asserts speedup_ms >= 80ms"
  artifacts:
    - path: services/memory/memory_service.py
      provides: "SaveFactsResult dataclass + LongTermMemory.save_facts + LongTermMemory._bulk_near_duplicate_check + save_fact wrapper delegating to save_facts"
      contains: "SaveFactsResult"
    - path: services/agent/extractor.py
      provides: "_run_and_persist dispatch via save_facts (replaces for-loop save_fact)"
      contains: "await mem._long.save_facts"
    - path: tests/unit/memory/test_save_facts_batch.py
      provides: "SC-4 mock-counting: 1 embed_batch + 1 bulk dedupe + 1 executemany for N=5"
    - path: tests/unit/memory/test_save_facts_batch_dedupe.py
      provides: "near-dup-in-batch: audit rows fired, executemany still inserts ALL 5 (D-09)"
    - path: tests/unit/memory/test_save_facts_embed_batch_fallback.py
      provides: "embed_batch raises -> fallback to gather(embed_one, return_exceptions=True); skipped_embed_failures counter accurate"
    - path: tests/integration/memory/test_memory_suite_factory_migrated.py
      provides: "Memory-side SC-1 coverage: create_app() + real PG + save_facts end-to-end"
    - path: tests/benchmark/test_extractor_latency.py
      provides: "SC-5 relative latency benchmark; records baseline vs new in summary"
  key_links:
    - from: services/agent/extractor.py::_run_and_persist
      to: services/memory/memory_service.py::LongTermMemory.save_facts
      via: single batch call replacing for-loop
      pattern: "await mem._long.save_facts"
    - from: services/memory/memory_service.py::LongTermMemory.save_facts
      to: services/memory/memory_service.py::_bulk_near_duplicate_check
      via: direct call inside acquire()
      pattern: "_bulk_near_duplicate_check"
    - from: services/memory/memory_service.py::LongTermMemory.save_facts
      to: pgvector unnest($1::text[]) WITH ORDINALITY pattern (C1)
      via: bulk dedupe SQL string
      pattern: "unnest\\(\\$1::text\\[\\]\\) WITH ORDINALITY"
---

<objective>
Land SC-4 + SC-5 + memory-side SC-1 of Phase 27: `LongTermMemory.save_facts(list[ExtractedFact])` batch path with `1× embed_batch + 1× bulk dedupe + 1× executemany` per turn; `ExtractorAgent._run_and_persist` migrated to call it; near-duplicate audit rows fired inside the batch per D-09 audit-mode-only (insert still runs); embed_batch fail-fast handled via gather fallback (C2); pgvector bulk SQL uses text[] cast pattern (C1); SC-5 relative latency benchmark captures the speedup; memory integration suite gains >=1 new test via create_app() (memory-side SC-1).

Purpose: ExtractorAgent currently issues 3N PG RTTs per turn (embed + precheck + insert × N facts). This plan collapses that to 3 + K RTT (1 embed + 1 bulk dedupe + 1 executemany + K audit writes for K duplicates). With N=5 typical: 15 RTT → ~3-8 RTT depending on dedupe hit rate. The three corrections (C1/C2/C3) thread through every task — SQL, fallback, and audit semantics are concrete.

Output: services/memory/memory_service.py adds SaveFactsResult + save_facts + _bulk_near_duplicate_check + save_fact-wrapper refactor; services/agent/extractor.py:260 inline migration; 3 new unit test files + 1 integration test + 1 benchmark.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-isolation-memory-reliability/27-CONTEXT.md
@.planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md
@.planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md
@.planning/phases/27-test-isolation-memory-reliability/27-VALIDATION.md
@.planning/phases/27-test-isolation-memory-reliability/27-03-PLAN.md
@services/memory/memory_service.py
@services/agent/extractor.py
@services/vectorizer/embedder.py
@utils/models.py
@tests/conftest.py
@tests/unit/test_memory_save_fact.py
@CLAUDE.md
</context>

<interfaces>
Key existing interfaces:

From services/memory/memory_service.py (post-27-03):
- `LongTermMemory._is_near_duplicate(conn, *, user_id, tenant_id, embedding, threshold) -> (bool, float | None)` — singular precheck.
- `LongTermMemory.save_fact(user_id, tenant_id, fact, source_doc="", importance=0.5) -> None` — singular save with precheck (D-09 audit-mode-only).
- `LongTermMemory._fire_near_duplicate_audit(user_id, tenant_id, fact, dist)` module-level helper.
- INSERT SQL (verbatim, lines 389-393): `INSERT INTO long_term_facts (user_id, tenant_id, fact, source_doc, importance, embedding) VALUES ($1,$2,$3,$4,$5,$6::vector)` — reused by executemany.
- `_get_pool` lazy-init + pgvector.asyncpg.register_vector at line 163 (which is the codec that breaks `unnest($1::vector[])` per C1).

From services/vectorizer/embedder.py:
- `BaseEmbedder.embed_batch(texts: list[str]) -> list[list[float]]` (line 29) abstract.
- `BaseEmbedder.embed_one(text: str) -> list[float]` (line 32-34) delegates to `embed_batch([text])`.
- `OllamaEmbedder.embed_batch` (line 61-70): raises RuntimeError on first failed text — does NOT return per-item None (C2).
- `OpenAIEmbedder.embed_batch` (line 85-99): single API call; raises on error.
- `HuggingFaceEmbedder.embed_batch` (line 115-126): single torch call; raises on error.
- `get_embedder()` accessor (line 236).

From services/agent/extractor.py:
- `_run_and_persist` dispatch site at lines 247-266 (RESEARCH §5; PATTERNS.md §"extractor.py::_run_and_persist — D-17 inline migration" lines 135-153).
- Current loop:
  - `mem = get_memory_service()`
  - `for f in facts: await mem._long.save_fact(user_id=user_id, tenant_id=tenant_id, fact=f.fact, source_doc="", importance=f.importance)`
- Wrapped in try/except MemoryFactWriteError -> log_task_error callback (line 269).
- `dispatch_extraction` signature unchanged.

From utils/models.py:
- `ExtractedFact` Pydantic V2 frozen model: `fact: str`, `category: Literal[...]`, `importance: Literal[0.2, 0.5, 0.8]`.
- `MemoryFactWriteError` exception.

From tests/conftest.py (extended in 27-00):
- `app_factory`, `pg_pool`, `clean_long_term_facts` (lines 178-195) — integration fixture set.

From config/settings.py (extended in 27-03):
- `memory_near_duplicate_threshold: float = Field(default=0.05, ge=0.0, le=1.0)`.
- `pgvector_ef_search_filtered: int` (existing).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SaveFactsResult + LongTermMemory.save_facts batch + _bulk_near_duplicate_check (C1 SQL) + embed_batch fallback (C2) + D-12 save_fact wrapper</name>
  <files>services/memory/memory_service.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §10 (lines 281-304, C1 empirically validated SQL — `unnest($1::text[]) WITH ORDINALITY` + inline `vec_txt::vector` cast) + §Theme 4 "Exact signature" (lines 707-756) + "Bulk dedupe SQL (corrected — empirically validated)" (lines 758-786) + "Full save_facts implementation sketch" (lines 788-864) + §9 (lines 266-280, C2 embed_batch fail-fast reality + recommended fallback) + "Caveat for D-12 wrapper" (lines 752-757, importance rounding)
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"LongTermMemory.save_facts (new) — TD-05 batch" (lines 82-112, bulk dedupe SQL + INSERT SQL + SaveFactsResult placement) + §"Adapter Pattern (Embedder)" (lines 398-410, all 3 embedders raise on failure — C2 verification)
    - services/memory/memory_service.py post-27-03 state (full LongTermMemory class incl. _is_near_duplicate from 27-03)
    - services/vectorizer/embedder.py lines 27-130 (BaseEmbedder ABC + 3 impls — verify the raise-on-first-failure behavior firsthand)
    - utils/models.py lines 675-723 (ExtractedFact model — importance Literal[0.2, 0.5, 0.8] enforcement)
  </read_first>
  <behavior>
    - Behavior 1: `SaveFactsResult(saved_count: int, skipped_near_duplicates: int, skipped_embed_failures: int)` is a frozen dataclass at module top (alongside ConversationTurn/UserProfile).
    - Behavior 2: `save_facts([])` returns `SaveFactsResult(0, 0, 0)` immediately.
    - Behavior 3: Happy path with N facts, no duplicates, no embed failures: 1× embed_batch call returns N embeddings; 1× bulk dedupe SELECT returns empty set; 1× executemany inserts N rows; SaveFactsResult(N, 0, 0).
    - Behavior 4: embed_batch raises RuntimeError: fallback to `asyncio.gather(*[embed_one(t) for t in texts], return_exceptions=True)`; per-item exceptions count as None and contribute to skipped_embed_failures.
    - Behavior 5: Bulk dedupe SQL — empirically-validated C1 pattern: `unnest($1::text[]) WITH ORDINALITY AS t(vec_txt, idx)` with `vec_txt::vector` cast inside EXISTS. Pass `vec_literals: list[str]` where each literal is `'[0.1,0.2,...]'` format.
    - Behavior 6: Duplicates inside batch — audit rows fire for each duplicate (via existing `_fire_near_duplicate_audit`), BUT executemany inserts ALL N rows including duplicates (D-09 audit-mode-only — C3).
    - Behavior 7: Bulk dedupe SELECT failure (asyncpg.PostgresError): fail-OPEN — log warning, treat dup set as empty, proceed with executemany of all N rows.
    - Behavior 8: `save_fact(user_id, tenant_id, fact: str, source_doc="", importance: float = 0.5)` delegates to `save_facts([ExtractedFact(fact=fact, category="recurring_topics", importance=_round_to_literal(importance))], user_id=user_id, tenant_id=tenant_id, source_doc=source_doc)`. `_round_to_literal(0.5)` returns 0.5; `_round_to_literal(0.3)` returns 0.2; `_round_to_literal(0.7)` returns 0.8. Map: x < 0.35 -> 0.2; 0.35 <= x < 0.65 -> 0.5; x >= 0.65 -> 0.8.
    - Behavior 9: save_fact wrapper raises MemoryFactWriteError when save_facts result has saved_count == 0 AND skipped_embed_failures > 0 (preserves embed-failure-raise contract from pre-27-03 save_fact).
  </behavior>
  <action>
    Edit `services/memory/memory_service.py`:

    1. Add `SaveFactsResult` dataclass at module top alongside ConversationTurn/UserProfile (PATTERNS.md line 105 placement):
       - `from dataclasses import dataclass`
       - `@dataclass(frozen=True)`
       - `class SaveFactsResult:`
       - Fields: `saved_count: int`, `skipped_near_duplicates: int`, `skipped_embed_failures: int`.

    2. Add private helper `_round_importance_to_literal(value: float) -> Literal[0.2, 0.5, 0.8]` (module-level or static method). Mapping per Behavior 8 above. Returns the bucket the value falls in.

    3. Add `LongTermMemory._bulk_near_duplicate_check` method per RESEARCH §"Bulk dedupe SQL (corrected — empirically validated)" lines 758-786:
       - Signature: `async def _bulk_near_duplicate_check(self, conn, *, user_id: str, tenant_id: str, embeddings: list[list[float]], threshold: float) -> set[int]`.
       - Build `vec_literals = ['[' + ','.join(str(x) for x in v) + ']' for v in embeddings]`.
       - `async with conn.transaction():`
       - Lazy `from config.settings import settings`.
       - `ef = int(getattr(settings, "pgvector_ef_search_filtered", 200))`.
       - `await conn.execute("SET LOCAL hnsw.iterative_scan = 'strict_order'")`.
       - `await conn.execute(f"SET LOCAL hnsw.ef_search = {ef}")`.
       - `rows = await conn.fetch("""SELECT (idx - 1) AS zero_idx FROM unnest($1::text[]) WITH ORDINALITY AS t(vec_txt, idx) WHERE EXISTS (SELECT 1 FROM long_term_facts WHERE user_id = $2 AND tenant_id = $3 AND embedding <=> vec_txt::vector < $4)""", vec_literals, user_id, tenant_id, threshold)`.
       - Return `{row["zero_idx"] for row in rows}`.
       - C1 enforcement: SQL string MUST contain `unnest($1::text[]) WITH ORDINALITY` (NOT `$1::vector[]`); the per-row cast happens via `vec_txt::vector` inside EXISTS. Do NOT change this — empirically validated against live PG.

    4. Add `LongTermMemory.save_facts` method per RESEARCH §"Full save_facts implementation sketch" lines 788-864:
       - Signature: `async def save_facts(self, facts: list[ExtractedFact], *, user_id: str, tenant_id: str, source_doc: str = "") -> SaveFactsResult`.
       - Early return: `if not facts: return SaveFactsResult(0, 0, 0)`.
       - Step 1 — embed (with C2 fallback):
         - Lazy `from services.vectorizer.embedder import get_embedder; import httpx; import asyncio`.
         - `embedder = get_embedder()`.
         - `embed_failures = 0`.
         - `try: embeddings = await embedder.embed_batch([f.fact for f in facts])` — happy path: list of N vectors.
         - `except (httpx.HTTPError, RuntimeError, OSError) as exc:` — C2 fallback (per RESEARCH §9 + §Theme 4 fallback):
           - `logger.warning("embed_batch failed; falling back per-item: {}", exc)`.
           - `per_item = await asyncio.gather(*[embedder.embed_one(f.fact) for f in facts], return_exceptions=True)`.
           - `embeddings = []`.
           - For each `r` in `per_item`: if `isinstance(r, BaseException)`: append None, increment embed_failures; else: append r.
       - Step 2 — filter embed-failed entries: `indexed = [(i, f, e) for i, (f, e) in enumerate(zip(facts, embeddings)) if e is not None]`. If empty, return `SaveFactsResult(0, 0, embed_failures)`.
       - Step 3 — acquire pool + bulk dedupe check:
         - `pool = await self._get_pool(); async with pool.acquire() as conn:`
         - `valid_embeddings = [e for _, _, e in indexed]`.
         - `try: dup_zero_idxs = await self._bulk_near_duplicate_check(conn, user_id=user_id, tenant_id=tenant_id, embeddings=valid_embeddings, threshold=settings.memory_near_duplicate_threshold)` (note: also need lazy `from config.settings import settings` here).
         - `except asyncpg.PostgresError as exc: logger.warning("bulk dedupe check failed (fail-open): {}", exc); dup_zero_idxs = set()` — fail-OPEN.
       - Step 4 — fire audit rows for duplicates (best-effort, parallel):
         - `audit_tasks = [_fire_near_duplicate_audit(user_id, tenant_id, indexed[local_i][1].fact, None) for local_i in dup_zero_idxs]`.
         - If audit_tasks: `await asyncio.gather(*audit_tasks, return_exceptions=True)`. Note: dist=None on the bulk path because the bulk SELECT doesn't return per-row distance (could be added with a second pass if ops demand it; out of scope per RESEARCH §"Full save_facts" line 840 comment).
       - Step 5 — executemany INSERT (C3 — D-09 audit-mode: INSERT ALL rows including duplicates):
         - `rows_to_insert = [(user_id, tenant_id, f.fact, source_doc, f.importance, e) for _, f, e in indexed]`.
         - `try: await conn.executemany("""INSERT INTO long_term_facts (user_id, tenant_id, fact, source_doc, importance, embedding) VALUES ($1,$2,$3,$4,$5,$6::vector)""", rows_to_insert)` — preserve INSERT SQL verbatim from existing save_fact.
         - `except asyncpg.PostgresError as exc: raise MemoryFactWriteError("batch persistence failed") from exc`.
       - Return `SaveFactsResult(saved_count=len(rows_to_insert), skipped_near_duplicates=len(dup_zero_idxs), skipped_embed_failures=embed_failures)`.

    5. Refactor existing `save_fact` (singular) per D-12 + RESEARCH §"Exact signature" lines 739-749:
       - Signature unchanged: `async def save_fact(self, user_id, tenant_id, fact, source_doc="", importance=0.5) -> None`.
       - Body: build `ExtractedFact(fact=fact, category="recurring_topics", importance=_round_importance_to_literal(importance))`.
       - `result = await self.save_facts([extracted_fact], user_id=user_id, tenant_id=tenant_id, source_doc=source_doc)`.
       - `if result.saved_count == 0 and result.skipped_embed_failures > 0: raise MemoryFactWriteError("embedding failed")` — preserves pre-27-03 embed-failure contract.
       - Existing precheck logic from 27-03 (`_is_near_duplicate` + `_fire_near_duplicate_audit`) now happens INSIDE save_facts via the bulk dedupe path. The singular `_is_near_duplicate` method is retained (now unused by save_fact, but kept for direct callers + as the analog reference).

    Per CLAUDE.md: Pydantic V2 (ExtractedFact already V2), no bare except outside Pattern D audit boundary, narrow exception tuples, asyncpg-async (no blocking I/O), mypy --strict on new helpers, structured logging via loguru, tenacity unchanged (embedders own their own retry).

    DO NOT modify ExtractorAgent in this task — Task 3 owns the dispatch migration.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from services.memory.memory_service import LongTermMemory, SaveFactsResult; assert hasattr(LongTermMemory, 'save_facts') and hasattr(LongTermMemory, '_bulk_near_duplicate_check'); sig = inspect.signature(LongTermMemory.save_facts); assert 'facts' in sig.parameters and 'user_id' in sig.parameters; r = SaveFactsResult(0, 0, 0); print(f'SaveFactsResult fields: {r.saved_count}, {r.skipped_near_duplicates}, {r.skipped_embed_failures}'); print('batch wiring: ok')" && grep -v '^#' services/memory/memory_service.py | grep -c "unnest(\\\$1::text\\[\\]) WITH ORDINALITY" && uv run ruff check services/memory/memory_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' services/memory/memory_service.py | grep -c "class SaveFactsResult"` returns 1.
    - `grep -v '^#' services/memory/memory_service.py | grep -c "async def save_facts"` returns 1.
    - `grep -v '^#' services/memory/memory_service.py | grep -c "async def _bulk_near_duplicate_check"` returns 1.
    - **C1 enforcement (CRITICAL):** `grep -v '^#' services/memory/memory_service.py | grep -c "unnest(\\\$1::text\\[\\]) WITH ORDINALITY"` returns >= 1. AND `grep -v '^#' services/memory/memory_service.py | grep -c "unnest(\\\$1::vector\\[\\])"` returns 0 (the broken pattern MUST NOT be present).
    - **C1 enforcement (cast):** `grep -v '^#' services/memory/memory_service.py | grep -c "vec_txt::vector"` returns >= 1.
    - **C2 enforcement:** `grep -v '^#' services/memory/memory_service.py | grep -c "return_exceptions=True"` returns >= 1 (the gather fallback is present).
    - **C2 enforcement:** `grep -v '^#' services/memory/memory_service.py | grep -A 3 "embed_batch failed" | grep -c "embed_one\|gather"` returns >= 1.
    - **C3 enforcement:** `grep -v '^#' services/memory/memory_service.py | grep -B 5 "conn.executemany" | grep -c "indexed\|rows_to_insert"` shows executemany is called with all rows (NOT filtered by dup_zero_idxs).
    - D-12 wrapper: `grep -v '^#' services/memory/memory_service.py | grep -B 2 -A 8 "async def save_fact" | grep -c "await self.save_facts"` returns >= 1.
    - Regression: `uv run pytest tests/unit/test_memory_save_fact.py tests/unit/memory/test_save_fact_precheck.py tests/unit/memory/test_save_fact_precheck_failure.py -x --timeout 30` exit 0 — existing + 27-03 tests still green.
    - `uv run ruff check services/memory/memory_service.py` passes.
    - `uv run mypy --strict services/memory/memory_service.py` preserves baseline.
  </acceptance_criteria>
  <done>SaveFactsResult dataclass + save_facts batch method + _bulk_near_duplicate_check (C1 text[] cast pattern) implemented; C2 embed_batch fail-fast handled via gather fallback; C3 D-09 audit-mode preserved (audit rows fire for duplicates AND executemany inserts all rows); save_fact wrapper delegates per D-12 with importance rounding; existing tests + 27-03 tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for save_facts batch (SC-4 mock-counting + dedupe-in-batch + embed_batch fallback C2)</name>
  <files>tests/unit/memory/test_save_facts_batch.py, tests/unit/memory/test_save_facts_batch_dedupe.py, tests/unit/memory/test_save_facts_embed_batch_fallback.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §"Mock-based unit test for SC-4" (lines 866-915, mock pool + spy on embedder + assertions) + §9 (lines 266-280, C2 fail-fast — fallback test target) + §Theme 4 "Mock-counting test for 1-RTT contract" (lines 928-951, 3-RTT shape acceptance)
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"tests/unit/memory/test_save_facts_batch.py — SC-4 mock counting" (lines 292-296, mock helpers + executemany AsyncMock) + §"tests/unit/memory/test_save_facts_batch_dedupe.py" (lines 297-303, D-09 audit-mode-in-batch test pattern) + §"tests/unit/memory/test_save_facts_embed_batch_fallback.py" (lines 305-307, parametrize embed_batch raise + per-item gather assertion)
    - tests/unit/test_memory_save_fact.py (helper class verbatim reuse — `_AcquireCtx`, `_make_fake_pool`, `_make_long`; extend conn mock with `executemany = AsyncMock()`)
    - tests/unit/memory/test_save_fact_precheck.py (created in 27-03 — sibling test file conventions: Pattern A env-var, Pattern B autouse singleton-reset, Pattern C dual-path embedder mock)
  </read_first>
  <behavior>
    Test file 1 (`test_save_facts_batch.py` — SC-4 mock counting):
    - Test 1 `test_save_facts_5_emits_exactly_1_embed_batch_and_1_executemany`:
      - Build 5 ExtractedFact instances.
      - Mock embedder: `embed_spy = AsyncMock(return_value=[[0.1]*1024]*5)`. Dual-path patch.
      - Mock pool/conn: extend `_make_fake_pool` so `conn.fetchrow=AsyncMock(...)`, `conn.fetch=AsyncMock(return_value=[])` (empty dup set), `conn.executemany=AsyncMock()`, `conn.execute=AsyncMock(...)`, `conn.transaction` returns an async context manager.
      - Mock audit service `MagicMock(log=AsyncMock())` patched at consumer path.
      - Call `await ltm.save_facts(facts, user_id="u1", tenant_id="t1")`.
      - Assert `embed_spy.call_count == 1` AND `embed_spy.call_args.args[0] == [f.fact for f in facts]` (single batch call with all 5 strings).
      - Assert `conn.executemany.call_count == 1` (single insert call) AND the SQL string contains `INSERT INTO long_term_facts`.
      - Assert `conn.fetch.call_count >= 1` (the bulk dedupe SELECT — note: conn.fetch is the bulk dedupe call, conn.fetchrow was the singular precheck).
      - Assert result `SaveFactsResult(saved_count=5, skipped_near_duplicates=0, skipped_embed_failures=0)`.
    - Test 2 `test_save_facts_empty_list_returns_zeros`:
      - `result = await ltm.save_facts([], user_id="u1", tenant_id="t1")`.
      - Assert `result == SaveFactsResult(0, 0, 0)`.
      - Embedder NOT called (`embed_spy.call_count == 0`).
    - Test 3 `test_save_facts_bulk_dedupe_sql_uses_text_array_pattern_c1`:
      - Spy on `conn.fetch` call_args.
      - Call save_facts with 3 facts.
      - Inspect `conn.fetch.call_args.args[0]` (the SQL string) — assert it contains `unnest($1::text[]) WITH ORDINALITY` AND `vec_txt::vector`. NOT `unnest($1::vector[])`.
      - Assert `conn.fetch.call_args.args[1]` is a list of strings (vector literals — C1 binding pattern).

    Test file 2 (`test_save_facts_batch_dedupe.py` — C3 D-09 in-batch audit-mode):
    - Test 1 `test_dedupe_in_batch_fires_audit_AND_executemany_inserts_all_rows`:
      - Build 5 ExtractedFact instances.
      - Mock `conn.fetch.return_value = [{"zero_idx": 1}, {"zero_idx": 3}]` (indices 1 and 3 are duplicates).
      - Mock audit log `audit_mock = MagicMock(log=AsyncMock())`. Patch via PATTERNS.md line 300-302.
      - Mock executemany.
      - Call save_facts.
      - Assertions (CRITICAL — D-09 audit-mode):
        - `audit_mock.log.await_count == 2` (one audit row per duplicate index).
        - `conn.executemany.call_count == 1`.
        - `conn.executemany.call_args.args[1]` (the rows list) has length 5 (ALL 5 rows inserted — duplicates NOT filtered out per D-09).
        - `result.saved_count == 5` AND `result.skipped_near_duplicates == 2`.
        - The name "skipped" is semantic intent (v1.8 will skip); v1.7 saved_count is full N because INSERT runs for all.
    - Test 2 `test_bulk_dedupe_fail_open_on_postgres_error`:
      - `conn.fetch.side_effect = asyncpg.PostgresError("conn lost")`.
      - Call save_facts; assert no raise, logger.warning called, executemany still called with all rows, result.skipped_near_duplicates == 0 (treated as no-dup on fail-open).

    Test file 3 (`test_save_facts_embed_batch_fallback.py` — C2 fail-fast handling):
    - Test 1 `test_embed_batch_raises_falls_back_to_per_item_gather`:
      - `embed_batch_spy = AsyncMock(side_effect=RuntimeError("Embedding failed for text[0]"))`.
      - `embed_one_spy = AsyncMock(return_value=[0.1]*1024)`.
      - Mock embedder with both methods spied.
      - Call save_facts with 5 facts.
      - Assertions:
        - `embed_batch_spy.call_count == 1` (the batch call was attempted).
        - `embed_one_spy.call_count == 5` (fell back to N parallel embed_one calls).
        - `result.saved_count == 5` AND `result.skipped_embed_failures == 0`.
        - executemany called once with all 5 rows.
    - Test 2 `test_embed_batch_fallback_with_one_per_item_failure`:
      - `embed_batch_spy.side_effect = RuntimeError`.
      - `embed_one_spy.side_effect = [vec, vec, RuntimeError("text[2] failed"), vec, vec]` (item 2 fails).
      - Note: per RESEARCH §9 the gather wraps each embed_one with return_exceptions; per-item exceptions surface as BaseException.
      - Call save_facts.
      - Assert `result.saved_count == 4` AND `result.skipped_embed_failures == 1`.
      - executemany called with 4 rows (the surviving embeddings).
    - Test 3 `test_embed_batch_fallback_logged`:
      - With embed_batch raising, assert `caplog` captures a warning message containing "embed_batch failed; falling back per-item" (or similar — match the format string used in Task 1).
    - Parametrize the embed_batch exception class: `[RuntimeError, httpx.HTTPError, OSError]` per RESEARCH §9 narrow exception tuple.
  </behavior>
  <action>
    Create `tests/unit/memory/test_save_facts_batch.py`:
    - Pattern A env-var setdefault.
    - Pattern B autouse singleton-reset for `_memory_service`.
    - Pattern C dual-path embedder patch.
    - Helper classes from `test_memory_save_fact.py:50-80` (reused). EXTEND `_make_fake_pool` to include `executemany=AsyncMock()` and `fetch=AsyncMock(return_value=[])` and `transaction` returning an async context manager.
    - Audit mock pattern per PATTERNS.md line 300-302: `monkeypatch.setattr("services.audit.audit_service.get_audit_service", lambda: mock_audit)`. Note: depending on the import path in memory_service.py, may also need `monkeypatch.setattr("services.memory.memory_service.get_audit_service", lambda: mock_audit, raising=False)` (dual-path safety belt).
    - 3 behavior tests above.

    Create `tests/unit/memory/test_save_facts_batch_dedupe.py`:
    - Same setup pattern.
    - 2 behavior tests above.

    Create `tests/unit/memory/test_save_facts_embed_batch_fallback.py`:
    - Same setup pattern.
    - 3 behavior tests above.
    - For Test 3 `test_embed_batch_fallback_logged`: use `caplog` fixture per pytest's loguru integration OR `monkeypatch.setattr` on the logger module — match whatever pattern the codebase already uses (grep for `caplog` in tests/unit/ to find precedent). If no precedent: use `monkeypatch.setattr("loguru.logger.warning", spy)`.

    Per CLAUDE.md: mypy --strict on new test files (mock spies may need # type: ignore comments), ruff clean, no bare except, narrow exception types.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/memory/test_save_facts_batch.py tests/unit/memory/test_save_facts_batch_dedupe.py tests/unit/memory/test_save_facts_embed_batch_fallback.py -x -v --timeout 30 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All 3 new test files pass: `uv run pytest tests/unit/memory/test_save_facts_batch.py tests/unit/memory/test_save_facts_batch_dedupe.py tests/unit/memory/test_save_facts_embed_batch_fallback.py -x` exit 0.
    - SC-4 mock-count assertion present: `grep -c "embed_spy.call_count.*==.*1\|embed_batch_spy.call_count.*==.*1" tests/unit/memory/test_save_facts_batch.py` >= 1.
    - SC-4 executemany assertion present: `grep -c "conn.executemany.call_count.*==.*1\|executemany_spy.call_count.*==.*1" tests/unit/memory/test_save_facts_batch.py` >= 1.
    - **C1 SQL assertion in tests:** `grep -c "unnest(\\\$1::text\\[\\]) WITH ORDINALITY\|vec_txt::vector" tests/unit/memory/test_save_facts_batch.py` >= 1.
    - **C3 D-09 in-batch assertion:** `grep -B 2 -A 5 "test_dedupe_in_batch_fires_audit_AND_executemany_inserts_all_rows" tests/unit/memory/test_save_facts_batch_dedupe.py | grep -c "executemany.*length.*5\|rows.*5\|len(.*) == 5"` >= 1.
    - **C2 fallback assertion present:** `grep -c "embed_one_spy.call_count.*==.*5\|embed_one.call_count == 5" tests/unit/memory/test_save_facts_embed_batch_fallback.py` >= 1.
    - Regression: `uv run pytest tests/unit/test_memory_save_fact.py tests/unit/memory/test_save_fact_precheck.py tests/unit/memory/test_save_fact_precheck_failure.py -x --timeout 30` exit 0.
    - `uv run ruff check tests/unit/memory/` passes.
  </acceptance_criteria>
  <done>3 new unit test files cover SC-4 mock-counting (1 embed_batch + 1 executemany), C1 SQL pattern presence, C3 D-09 in-batch audit-mode (all rows inserted), and C2 fail-fast fallback (gather + per-item exceptions); all pass; existing tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExtractorAgent dispatch migration (D-17) + integration test for memory suite factory migration (SC-1 memory-side) + SC-5 latency benchmark</name>
  <files>services/agent/extractor.py, tests/integration/memory/__init__.py, tests/integration/memory/test_memory_suite_factory_migrated.py, tests/benchmark/__init__.py, tests/benchmark/test_extractor_latency.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §5 (lines 195-207, ExtractorAgent dispatch site lines 255-266) + §Theme 4 D-17 (lines 46, inline migration target) + "Latency baseline measurement" (lines 917-926, SC-5 relative-measurement strategy)
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"extractor.py::_run_and_persist — D-17 inline migration" (lines 135-153, exact replacement) + §"tests/integration/memory/test_memory_suite_factory_migrated.py" (lines 326-330) + §"tests/benchmark/test_extractor_latency.py — SC-5" (lines 332-345, timing-loop + relative-measurement pattern)
    - services/agent/extractor.py lines 220-280 (full `_run_and_persist` function — the for-loop save_fact at 255-266 is the migration target; preserve MemoryFactWriteError -> log_task_error callback at line 269)
    - tests/integration/test_pgvector_filtered_recall.py (Pattern E pytestmark skip-gate analog) + tests/integration/test_lifespan_shutdown_closes_pools.py (reset-before-each analog)
    - tests/integration/test_recall_latency.py lines 17-103 (timing-loop pattern for SC-5 — p50/p95 capture)
    - tests/conftest.py — confirm `pg_pool` + `clean_long_term_facts` fixtures (referenced at PATTERNS.md line 328)
  </read_first>
  <behavior>
    Extractor migration (D-17):
    - `_run_and_persist` no longer loops over facts calling save_fact. Single `await mem._long.save_facts(facts, user_id=user_id, tenant_id=tenant_id, source_doc="")` call.
    - MemoryFactWriteError handling preserved — single try/except around the batch call.
    - `dispatch_extraction` signature unchanged.
    - Existing extractor unit tests still green.

    Integration test (memory suite factory migration — SC-1 memory-side):
    - Uses `app_factory` + `pg_pool` + `clean_long_term_facts`.
    - Constructs 5 ExtractedFact instances.
    - Calls `await LongTermMemory().save_facts(facts, user_id="u1", tenant_id="t1")` against real PG.
    - Asserts `SELECT COUNT(*) FROM long_term_facts WHERE user_id='u1' AND tenant_id='t1'` returns 5.
    - Asserts SaveFactsResult counts match.
    - Skip-gated on `pg_available`.

    Benchmark (SC-5):
    - Two timing loops on the SAME branch:
      - Loop A: 5× sequential `save_fact` via the wrapper (which delegates to save_facts(1)) — measures the singular-per-fact path.
      - Loop B: 1× `save_facts([5 facts])` — measures the batch path.
    - Trials: 10 iterations each, capture `p50` and `p95` per loop.
    - Compute `speedup_ms = median(A) - median(B)`.
    - Assert `speedup_ms >= 80ms` (RESEARCH §"Latency baseline measurement" gives expected ~123ms; 80ms tolerant floor for variance).
    - Record `baseline_p50`, `baseline_p95`, `new_p50`, `new_p95`, `speedup_ms` in SUMMARY (the benchmark writes to a file under `.planning/phases/27-test-isolation-memory-reliability/`).
    - Marker `@pytest.mark.benchmark` (file-level pytestmark).
  </behavior>
  <action>
    1. Edit `services/agent/extractor.py` `_run_and_persist` per PATTERNS.md §"extractor.py::_run_and_persist — D-17 inline migration" lines 135-153:
       - Locate the for-loop at lines 255-266:
         - Current:
           - `mem = get_memory_service()`
           - `for f in facts: await mem._long.save_fact(user_id=user_id, tenant_id=tenant_id, fact=f.fact, source_doc="", importance=f.importance)`
       - Replace with single call:
         - `mem = get_memory_service()`
         - `await mem._long.save_facts(facts, user_id=user_id, tenant_id=tenant_id, source_doc="")`
       - Preserve: lazy `get_memory_service` import context; MemoryFactWriteError -> `log_task_error` callback path at line 269 (the try/except shape around the call — verify the existing block wraps the new single call in the same way).
       - DO NOT change `dispatch_extraction` signature.

    2. Create `tests/integration/memory/__init__.py` (empty marker).

    3. Create `tests/integration/memory/test_memory_suite_factory_migrated.py` per PATTERNS.md §"tests/integration/memory/test_memory_suite_factory_migrated.py":
       - Pattern A env-var setdefault at module top.
       - Pattern E pytestmark skip-gate: `pytestmark = [pytest.mark.integration, pytest.mark.skipif(not PG_AVAILABLE, reason="needs live PostgreSQL"), pytest.mark.pgvector]`. Import `PG_AVAILABLE` from `tests.conftest`.
       - Test 1 `test_save_facts_5_via_factory_real_pg_round_trip`:
         - Use fixtures: `app_factory`, `pg_pool`, `clean_long_term_facts`.
         - `app = app_factory()` (resets singletons; obtains fresh memory service).
         - Build `facts = [ExtractedFact(fact=f"fact {i}", category="recurring_topics", importance=0.5) for i in range(5)]`.
         - `from services.memory.memory_service import get_memory_service; ltm = get_memory_service()._long`.
         - `result = await ltm.save_facts(facts, user_id="u1_test27", tenant_id="t1_test27", source_doc="phase27-test")`.
         - Acquire `pg_pool` conn, `SELECT COUNT(*) FROM long_term_facts WHERE user_id='u1_test27' AND tenant_id='t1_test27'` → assert 5.
         - Assert `result == SaveFactsResult(5, 0, 0)` (assuming no pre-existing facts for that test user — clean_long_term_facts fixture ensures this).
       - Test 2 `test_save_facts_with_near_duplicate_emits_audit_and_still_inserts_real_pg` (C3 in-batch audit-mode at integration layer):
         - Insert 1 fact directly (`await ltm.save_facts([ExtractedFact(fact="hello world", ...)], ...)`).
         - Then call `save_facts` with a fact whose embedding will be cosine-close (use the same fact text; embeddings will be identical → distance 0 < 0.05 threshold).
         - Query audit_log: assert >= 1 row with `action = 'MEMORY_NEAR_DUPLICATE_SKIPPED'` for the test (user_id, tenant_id) pair (use pg_pool to query directly OR use `services.audit.audit_service.AuditService` API).
         - Query long_term_facts: assert COUNT == 2 (BOTH inserted — D-09 audit-mode-only).
         - Note: this test requires audit_db_enabled=True. Set `monkeypatch.setattr("services.audit.audit_service.settings.audit_db_enabled", True, raising=False)`.
       - Skip-gate also handles missing-embedder gracefully (use try/except ImportError on `get_embedder()` if model not loaded; pytest.skip with reason).

    4. Create `tests/benchmark/__init__.py` (empty marker).

    5. Create `tests/benchmark/test_extractor_latency.py` per PATTERNS.md §"tests/benchmark/test_extractor_latency.py — SC-5":
       - Pattern A env-var setdefault at module top.
       - Pattern E pytestmark skip-gate + `pytest.mark.benchmark`.
       - Import `time.perf_counter`, `statistics`.
       - Module constant `_TRIALS = 10`, `_N = 5`.
       - Test 1 `test_save_facts_batch_beats_singular_loop_by_at_least_80ms`:
         - Use `app_factory`, `pg_pool`, `clean_long_term_facts`.
         - Build 5 facts per RESEARCH §"Latency baseline measurement" pattern.
         - Loop A (baseline — singular per-fact):
           - `baseline_timings_ms: list[float] = []`.
           - For trial in range(_TRIALS):
             - Clean facts table for this test's user_id.
             - `t0 = time.perf_counter()`.
             - `for f in facts: await ltm.save_fact(user_id=u, tenant_id=t, fact=f.fact, importance=f.importance)` — explicitly loop save_fact to mirror pre-D-17 ExtractorAgent shape.
             - `baseline_timings_ms.append((time.perf_counter() - t0) * 1000)`.
         - Loop B (new — batch):
           - `new_timings_ms: list[float] = []`.
           - For trial in range(_TRIALS):
             - Clean facts table.
             - `t0 = time.perf_counter()`.
             - `await ltm.save_facts(facts, user_id=u, tenant_id=t)`.
             - `new_timings_ms.append((time.perf_counter() - t0) * 1000)`.
         - Compute `baseline_p50 = statistics.median(baseline_timings_ms)`, `baseline_p95 = sorted(...)[int(0.95*_TRIALS)]`, same for new.
         - `speedup_ms = baseline_p50 - new_p50`.
         - Append a row to `.planning/phases/27-test-isolation-memory-reliability/27-BENCHMARK.md` with timestamp + the 4 numbers + speedup (use Path().write_text with append-or-create semantics; OK to overwrite if multiple runs — record latest).
         - Assert `speedup_ms >= 80.0` per RESEARCH §"Latency baseline measurement" tolerant floor.
       - **SC-5 gating policy (plan-checker resolution):** the assertion stays hard (`pytest.fail` on regress) so a green local run is a real signal — but the default CI gate is `pytest -m "not benchmark"`, so a noisy machine never red-fails the phase. Phase 27 acceptance is "benchmark recorded in 27-BENCHMARK.md with the 4 numbers", NOT "speedup_ms ≥ 80 on every machine". The verifier reads 27-BENCHMARK.md and accepts if numbers are present + speedup is positive; the 80ms floor is a local-dev tripwire, not a CI gate. Document this stance in SUMMARY (do not handwave — quote the policy verbatim).

    Per CLAUDE.md: no blocking I/O in async (perf_counter is sync but called outside await; OK), no bare except, narrow exception types, mypy --strict on new files, structured logging via loguru.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_extractor*.py -x --timeout 60 -q 2>&1 | tail -10 && grep -v '^#' services/agent/extractor.py | grep -c "save_facts\|save_fact" && uv run pytest tests/integration/memory/test_memory_suite_factory_migrated.py -x --timeout 60 -v 2>&1 | tail -20 && uv run pytest tests/benchmark/test_extractor_latency.py -x --timeout 300 -v -m benchmark 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' services/agent/extractor.py | grep -c "await mem._long.save_facts"` returns >= 1 (batch call present).
    - `grep -v '^#' services/agent/extractor.py | grep -c "for f in facts:.*await.*save_fact\|for .* in facts:.*save_fact"` returns 0 (the for-loop is gone).
    - `uv run pytest tests/unit/test_extractor*.py -x --timeout 60` exit 0 (extractor unit tests still pass — D-17 migration preserves semantics).
    - `uv run pytest tests/integration/memory/test_memory_suite_factory_migrated.py -x --timeout 60` — both tests pass when PG + embedder available; skip cleanly otherwise. Test 2 asserts: audit_log row with MEMORY_NEAR_DUPLICATE_SKIPPED present AND long_term_facts COUNT == 2 (both inserted — D-09 at integration layer).
    - `grep -v '^#' tests/integration/memory/test_memory_suite_factory_migrated.py | grep -c "app_factory"` >= 2 (both new tests go through the factory — SC-1 memory-side).
    - `uv run pytest tests/benchmark/test_extractor_latency.py -m benchmark -x --timeout 300` — benchmark test runs and asserts speedup; if it fails the speedup floor on a noisy machine, the SUMMARY records the actual numbers and the executor flags for review (not auto-failing the phase per RESEARCH Open Question 4).
    - Benchmark file written: `.planning/phases/27-test-isolation-memory-reliability/27-BENCHMARK.md` exists with the 4 numbers + speedup_ms.
    - `uv run ruff check services/agent/extractor.py tests/integration/memory/ tests/benchmark/` passes.
    - Regression: `uv run pytest tests/unit/memory/ -x --timeout 30` exit 0 (all 27-03 + 27-04 unit tests pass).
    - Sanity: existing memory integration tests still pass — `uv run pytest tests/integration/test_pgvector_filtered_recall.py tests/integration/test_pgvector_recall.py tests/integration/test_long_term_facts_schema.py -x --timeout 60` exit 0 (no regression on Phase 24 suites; CONTEXT D-05 says they coexist with new factory tests).
  </acceptance_criteria>
  <done>ExtractorAgent for-loop replaced by single save_facts call; memory integration suite gets 2 new factory-migrated tests proving SC-1 memory-side + C3 D-09 at integration layer (audit row + INSERT both); SC-5 relative-latency benchmark captures baseline vs batch p50/p95 and writes to 27-BENCHMARK.md; speedup_ms ≥ 80ms floor asserted; existing extractor + memory tests untouched and still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExtractorAgent -> LongTermMemory batch | LLM-extracted facts (potentially attacker-influenced via prompt) cross into PG via parameterized executemany. Truncation + Pydantic validation preserved. |
| save_facts vector_literal strings -> PG | Vector literals constructed client-side from embedder output (model-generated floats, NOT user-supplied). Parameterized binding via asyncpg text[]. |
| bulk dedupe SELECT -> RLS | Per-(user_id, tenant_id) filter preserves tenant isolation under HNSW + GUC discipline. |
| Benchmark -> .planning artifact | Latency numbers written to 27-BENCHMARK.md — no secrets; CI-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Tampering | SQL injection via vector_literal string | mitigate | RESEARCH §Security Domain confirms: embeddings come from `embedder` (model-generated floats), NEVER from user-supplied input. Parameterized text[] binding via asyncpg — no string concat into SQL body. |
| T-27-04-02 | Information Disclosure | Cross-tenant leakage via bulk dedupe SELECT | mitigate | EXISTS subquery filters on `user_id = $2 AND tenant_id = $3` BEFORE applying `embedding <=> vec_txt::vector < $4`. HNSW + SET LOCAL GUC ensures exact top-K under the filter (v1.1 Phase 8 + v1.6 Phase 24 carry-forward). |
| T-27-04-03 | Tampering | Audit-log poisoning via bulk-batch fact contents | mitigate | `fact[:200]` truncation per Phase 25 RULE_BLOCKED convention in `_fire_near_duplicate_audit`; preserved unchanged. |
| T-27-04-04 | Denial of Service | Bulk dedupe slow path (large N) | accept | RESEARCH §10 fast-enough for N<=20 (typical ExtractorAgent batch size); temp table alternative deferred to v1.8 if N grows. |
| T-27-04-05 | Tampering | Embed-batch failure swallows error silently | mitigate | C2 fallback paths log loguru.warning for both `embed_batch` failure AND per-item embed_one failures; `skipped_embed_failures` counter surfaces the dropped count in `SaveFactsResult` (caller observable). |
| T-27-04-06 | Repudiation | Audit-mode metric not enforced (D-09 deferred) | accept | v1.7 is metric-only by design (CONTEXT D-09 + ROADMAP audit-mode-before-enforce). v1.8 promotes to silent-skip. Documented in 27-SUMMARY + STATE.md v1.8 follow-ups. |
</threat_model>

<verification>
- `uv run pytest tests/unit/memory/test_save_facts_batch.py tests/unit/memory/test_save_facts_batch_dedupe.py tests/unit/memory/test_save_facts_embed_batch_fallback.py -x --timeout 30 -v` — all green.
- `uv run pytest tests/unit/test_extractor*.py -x --timeout 60` — existing extractor tests still green (D-17 preserves semantics).
- `uv run pytest tests/integration/memory/test_memory_suite_factory_migrated.py -x --timeout 60` — both tests pass when PG + embedder available.
- `uv run pytest tests/benchmark/test_extractor_latency.py -m benchmark -x --timeout 300` — benchmark records numbers + asserts speedup floor.
- `uv run pytest tests/integration/test_pgvector_filtered_recall.py tests/integration/test_pgvector_recall.py tests/integration/test_long_term_facts_schema.py -x --timeout 60` — no regression on existing memory integration suite.
- **C1 enforcement:** `grep -c "unnest(\\\$1::text\\[\\]) WITH ORDINALITY" services/memory/memory_service.py` returns >= 1 AND `grep -c "unnest(\\\$1::vector\\[\\])" services/memory/memory_service.py` returns 0.
- **C2 enforcement:** `grep -c "return_exceptions=True" services/memory/memory_service.py` returns >= 1 AND `grep -B 2 -A 5 "embed_batch failed" services/memory/memory_service.py | grep -c "embed_one\|gather"` returns >= 1.
- **C3 enforcement:** `grep -A 5 "executemany" services/memory/memory_service.py | grep -c "rows_to_insert\|indexed"` returns >= 1 (executemany inserts ALL rows, not filtered).
- `uv run ruff check services/memory/memory_service.py services/agent/extractor.py tests/unit/memory/ tests/integration/memory/ tests/benchmark/` passes.
- 27-BENCHMARK.md exists with the SC-5 numbers.
</verification>

<success_criteria>
SC-4 of Phase 27:
- `LongTermMemory.save_facts(list[ExtractedFact])` batch API exists; 5-fact turn issues exactly 1× embed_batch + 1× bulk dedupe + 1× executemany (mock-counted in test_save_facts_batch.py).
- C1 bulk dedupe SQL uses `unnest($1::text[]) WITH ORDINALITY` + `vec_txt::vector` cast (NOT broken `$1::vector[]`).
- C2 embed_batch fail-fast handled via `asyncio.gather(*embed_one, return_exceptions=True)` fallback; per-item failures contribute to `skipped_embed_failures`.
- C3 D-09 audit-mode preserved inside batch: audit rows fire for duplicates, executemany inserts ALL rows including duplicates.
- D-12 `save_fact` (singular) becomes thin wrapper delegating to save_facts; importance rounded to {0.2, 0.5, 0.8} Literal bucket.
- D-17 ExtractorAgent inline migration: `_run_and_persist` for-loop replaced with single batch call; MemoryFactWriteError handling preserved.

SC-5 of Phase 27:
- Relative latency benchmark records baseline (5× sequential save_fact) vs new (1× save_facts([5])) p50/p95 in 27-BENCHMARK.md.
- Speedup_ms >= 80ms tolerant floor asserted (RESEARCH expectation: ~123ms).
- Benchmark marked `pytest.mark.benchmark`; not in default CI gate (per RESEARCH Open Q4 — observational not hard-fail).

SC-1 of Phase 27 (memory-side coverage):
- Memory integration suite has >=2 new tests using `app_factory`: end-to-end save_facts round-trip + D-09 in-batch audit-mode-at-PG-layer.
- Existing memory integration tests (test_pgvector_filtered_recall, test_pgvector_recall, test_long_term_facts_schema) still green.
</success_criteria>

<output>
Create `.planning/phases/27-test-isolation-memory-reliability/27-04-SUMMARY.md` capturing:
- LOC delta on services/memory/memory_service.py (additions for SaveFactsResult, save_facts, _bulk_near_duplicate_check, save_fact wrapper refactor).
- LOC delta on services/agent/extractor.py (for-loop → single batch call).
- C1 SQL excerpt (the `unnest($1::text[]) WITH ORDINALITY` clause) with confirmation it diverges from the broken `$1::vector[]` form.
- C2 fallback path excerpt (try/except embed_batch → gather embed_one).
- C3 D-09 confirmation: executemany row count == full N regardless of duplicate count.
- SC-4 mock-count results from test_save_facts_batch.py (the actual asserted numbers).
- SC-5 benchmark results: baseline_p50, baseline_p95, new_p50, new_p95, speedup_ms — copied from 27-BENCHMARK.md.
- Memory-side SC-1 test files added.
- v1.8 follow-up reminders (silent-skip enforcement, per-tenant threshold override, distance-in-bulk-audit-detail).
</output>
