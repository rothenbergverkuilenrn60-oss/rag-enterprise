---
plan_id: 27-00-test-infra-prep
phase: 27
plan: 00
type: execute
wave: 0
depends_on: []
requirements: [TD-02, TD-06]
files_modified:
  - tests/factories/__init__.py
  - tests/factories/app.py
  - tests/conftest.py
  - tests/unit/test_redis_mock_fixture.py
  - tests/unit/test_app_factory.py
autonomous: true
notes: |
  Wave 0 = test infrastructure scaffolding only. No production source touches.
  - tests/factories/app.py is greenfield (no analog); authoritative shape: RESEARCH §Theme 1 lines 308-388.
  - redis_mock uses fakeredis.aioredis.FakeRedis (NOT MagicMock(spec=...) — overrides CONTEXT D-20 per RESEARCH §Theme 2).
  - redis_mock patches BOTH utils.cache.get_redis AND redis.asyncio.from_url (latter because ShortTermMemory._get_client bypasses get_redis pre-bonus-refactor in 27-02).
  - _SINGLETON_INVENTORY: ~32 entries from RESEARCH §1 (38 - 4 skipped non-services: _tiktoken_enc, _anthropic_rate_limit_cls, _anthropic_overload_cls, _sem).
  - v1.8 follow-up: D-22 diagnostic (Redis-ConnectionError vs openai-SDK-drift failure attribution) tracked in 27-02 SC-2.

must_haves:
  truths:
    - "tests/factories/app.py exports create_app() and _SINGLETON_INVENTORY"
    - "tests/conftest.py exposes redis_mock, app_factory, isolated_app, isolated_client fixtures"
    - "uses_redis pytest marker is registered and auto-applies redis_mock via pytest_collection_modifyitems"
    - "redis_mock yields a fakeredis.aioredis.FakeRedis instance with GET/SET/list/sorted-set/pipeline support"
  artifacts:
    - path: tests/factories/__init__.py
      provides: "Package marker"
    - path: tests/factories/app.py
      provides: "create_app() factory + _SINGLETON_INVENTORY + _reset_singletons()"
      contains: "_SINGLETON_INVENTORY"
    - path: tests/conftest.py
      provides: "redis_mock fixture, app_factory fixture, pytest_configure markers, pytest_collection_modifyitems hook"
    - path: tests/unit/test_redis_mock_fixture.py
      provides: "Self-test that redis_mock covers GET/SET/RPUSH/LRANGE/ZADD/EXPIRE/pipeline"
    - path: tests/unit/test_app_factory.py
      provides: "Asserts create_app() returns isolated FastAPI instance; dependency_overrides applied"
  key_links:
    - from: tests/conftest.py::redis_mock
      to: utils.cache.get_redis
      via: monkeypatch.setattr at consumer path
      pattern: "monkeypatch.setattr\\(.utils.cache.get_redis"
    - from: tests/conftest.py::redis_mock
      to: redis.asyncio.from_url
      via: monkeypatch.setattr (ShortTermMemory bypass — RESEARCH §6 Pitfall 6)
      pattern: "monkeypatch.setattr\\(.redis.asyncio.from_url"
    - from: tests/factories/app.py::create_app
      to: main._configure_app + main.lifespan
      via: lazy import inside function body
      pattern: "from main import _configure_app, lifespan"
---

<objective>
Scaffold test infrastructure required by Wave 1 plans (27-01 create_app factory migration, 27-02 redis-mock rollout). No production source touches. New tests in this plan are self-tests of the scaffolding itself — they prove the fixtures work before downstream plans depend on them.

Purpose: Wave 1 plans (27-01, 27-02) consume `app_factory` / `redis_mock` / `isolated_client` from tests/conftest.py and `create_app()` from tests/factories/app.py. Building those first lets Wave 1 run cleanly in parallel.

Output: tests/factories/ package + extended tests/conftest.py + 2 self-test files. Zero production code modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-isolation-memory-reliability/27-CONTEXT.md
@.planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md
@.planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md
@.planning/phases/27-test-isolation-memory-reliability/27-VALIDATION.md
@tests/conftest.py
@utils/cache.py
@CLAUDE.md
</context>

<interfaces>
Key existing interfaces the executor will use:

From utils/cache.py:
- `async def get_redis() -> redis.asyncio.Redis` — singleton accessor; cached at module-level `_redis_client`. Lazy-imported by 5 services.

From tests/conftest.py (existing — extend, do not replace):
- `pg_pool` fixture (function-scoped, PG_AVAILABLE skip-guard) — analog for `redis_mock` placement.
- `pg_store` fixture (function-scoped; resets `_store_instance = None`) — exact shape mirror.
- Constants `PG_AVAILABLE` (module-scoped check; do NOT duplicate).

From main.py:
- `lifespan` async context manager at line 47 — referenced lazily inside create_app(); NOT imported at tests/factories/app.py module top.
- `_configure_app` does NOT exist yet — 27-01 creates it. This plan's create_app() must reference it via lazy import; the import will fail until 27-01 completes. Acceptable because 27-00 + 27-01 are in different waves (Wave 0 → Wave 1).

From RESEARCH §Theme 1 lines 308-388 (authoritative create_app shape):
- `_SINGLETON_INVENTORY: tuple[tuple[str, str], ...]` — ~32 (module_path, attr_name) pairs.
- `_reset_singletons() -> None` — importlib.import_module + setattr(mod, attr, None) with hasattr guard.
- `create_app(*, dependency_overrides=None) -> FastAPI` — calls _reset_singletons + FastAPI(lifespan=lifespan) + _configure_app(app) + applies dependency_overrides.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build tests/factories/ package with create_app() factory and singleton inventory</name>
  <files>tests/factories/__init__.py, tests/factories/app.py, tests/unit/test_app_factory.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §1 (lines 91-138, full 38-entry singleton inventory) + §Theme 1 (lines 308-453, authoritative create_app shape, app_factory fixture, parallel-contamination test pattern)
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"tests/factories/app.py (new — RESEARCH §Theme 1 lines 308-388 is authoritative)" + §"tests/unit/test_app_factory.py" + §"Pattern A — Env-var setdefault at module top"
    - tests/conftest.py (full file — pg_store fixture at lines 65-73 is the placement + shape analog)
    - main.py lines 1-260 (current FastAPI construction; _configure_app does NOT exist yet — create_app must lazy-import it and tolerate ImportError gracefully NO, must fail loudly: per 27-01 plan dependency, _configure_app is the prerequisite. This plan's test_app_factory.py uses pytest.importorskip to soft-skip until 27-01 lands.)
  </read_first>
  <behavior>
    - Test 1: `from tests.factories.app import create_app, _SINGLETON_INVENTORY, _reset_singletons` succeeds (module loads).
    - Test 2: `len(_SINGLETON_INVENTORY) >= 32` — covers all service singletons per RESEARCH §1.
    - Test 3: `_reset_singletons()` is idempotent — setting `services.agent.executor._executor_instance = object()` then calling `_reset_singletons()` resets it to None.
    - Test 4 (gated): two calls to `create_app()` return distinct FastAPI instances (`app_a is not app_b`). Gate with `pytest.importorskip` on `main._configure_app` so this test passes once 27-01 lands; it is skipped until then.
    - Test 5 (gated, same gate as Test 4): `create_app(dependency_overrides={dep_fn: stub})` applies the overrides to `app.dependency_overrides`.
  </behavior>
  <action>
    Create `tests/factories/__init__.py` (empty marker file — follows `tests/integration/__init__.py` precedent).

    Create `tests/factories/app.py` with the verbatim shape from RESEARCH §Theme 1 lines 308-388:
    1. Module docstring: "Per-test FastAPI factory. Implements TD-02 D-01 brute-force singleton reset + isolated app construction. _SINGLETON_INVENTORY is enumerated in RESEARCH §1 and lint-tested by tests/unit/test_singleton_inventory_complete.py (created in plan 27-01)."
    2. `_SINGLETON_INVENTORY: tuple[tuple[str, str], ...]` containing all 32 (module_path, attr_name) pairs from RESEARCH §1 — EXCLUDE the 4 non-services (`_tiktoken_enc`, `_anthropic_rate_limit_cls`, `_anthropic_overload_cls`, `_sem`). Include `_ner_pipeline` (services.nlu.nlu_service), `_reranker` (services.retriever.retriever), `_store_instance` (services.vectorizer.vector_store), `_embedder_instance` (services.vectorizer.embedder), `_knowledge_service`, `_version_service`, `_summary_indexer` (knowledge/*), `_generator`, `_llm_instance` (generator/*), `_ingest_pipeline`/`_query_pipeline`/`_agent_pipeline`/`_swarm_pipeline` (services.pipeline), `_tenant_service`, `_rules_engine`, `_event_bus`, `_pii_detector`, `_ab_service` (the 17 additions per RESEARCH §1 plan-time action).
    3. `_reset_singletons() -> None` — iterate inventory, `importlib.import_module(module_path)`, `if hasattr(mod, attr): setattr(mod, attr, None)`. Idempotent.
    4. `def create_app(*, dependency_overrides: dict[Callable[..., Any], Callable[..., Any]] | None = None) -> FastAPI`:
       - Call `_reset_singletons()` first.
       - Lazy import `from main import _configure_app, lifespan` INSIDE the function body (NOT at module top — main imports many singletons at module load).
       - `app = FastAPI(lifespan=lifespan)`
       - `_configure_app(app)`
       - If `dependency_overrides`: `app.dependency_overrides.update(dependency_overrides)`
       - Return app.
    5. Type hints under mypy --strict (project standard from CLAUDE.md). Use `from __future__ import annotations`. `Callable` from `collections.abc`.

    Create `tests/unit/test_app_factory.py`:
    - Pattern A env-var setdefault at module top (APP_MODEL_DIR, SECRET_KEY) per PATTERNS.md.
    - Test functions per behavior list above. Tests 4-5 use `pytest.importorskip("main", reason="needs _configure_app from 27-01")` after attempting `from main import _configure_app` — the `getattr(main, '_configure_app', None) is None` case raises pytest.skip.
    - No marker for unit tests except `pytest.mark.asyncio` where async.

    DO NOT touch main.py — 27-01 owns the _configure_app extraction. This task's gated tests will fail-skip until 27-01 ships.

    Per CLAUDE.md: Pydantic V2 (not used here — pure dataclass-free factory), mypy --strict on new modules (`tests/factories/app.py`), no bare except, structured logging not needed (test infra).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_app_factory.py -x -v 2>&1 | tail -30 && uv run python -c "from tests.factories.app import create_app, _SINGLETON_INVENTORY, _reset_singletons; print(f'inventory size: {len(_SINGLETON_INVENTORY)}'); assert len(_SINGLETON_INVENTORY) >= 32, f'expected >=32, got {len(_SINGLETON_INVENTORY)}'; _reset_singletons(); print('reset ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from tests.factories.app import _SINGLETON_INVENTORY; assert len(_SINGLETON_INVENTORY) >= 32"` exits 0.
    - `uv run pytest tests/unit/test_app_factory.py::test_inventory_size_and_reset_idempotent -x` passes (non-gated).
    - `uv run pytest tests/unit/test_app_factory.py -x` reports the two gated tests as SKIPPED until 27-01 ships (not as failures).
    - `grep -c "_executor_instance\|_memory_service\|_audit_service\|_extractor\|_planner_instance" tests/factories/app.py` returns >= 5 (sanity check that key singletons are listed).
    - `grep -c "_tiktoken_enc\|_anthropic_rate_limit_cls\|_anthropic_overload_cls\|_sem" tests/factories/app.py` returns 0 (the 4 non-services MUST NOT be in inventory).
    - `uv run ruff check tests/factories/app.py` passes.
    - `uv run mypy --strict tests/factories/app.py` passes.
  </acceptance_criteria>
  <done>tests/factories/ package exists, _SINGLETON_INVENTORY has >=32 entries excluding the 4 known non-services, _reset_singletons is idempotent, gated tests skip cleanly pending 27-01, ruff + mypy --strict clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/conftest.py with redis_mock + app_factory + isolated_app + isolated_client fixtures + uses_redis marker</name>
  <files>tests/conftest.py, tests/unit/test_redis_mock_fixture.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §Theme 2 (lines 514-610, fakeredis fixture shape + marker hook + ShortTermMemory bypass note) + §Pitfall 6 (lines 1083-1089, ShortTermMemory direct-redis-import gotcha)
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"tests/conftest.py — extend" (lines 197-221, full required additions list)
    - tests/conftest.py (existing — pg_pool placement + pg_store fixture shape at lines 36-79; do not duplicate PG_AVAILABLE)
    - utils/cache.py (current get_redis + _redis_client module-level singleton at lines 19-37)
    - services/memory/memory_service.py lines 87-108 (ShortTermMemory._get_client direct redis.asyncio.from_url call — Pitfall 6 source; this fixture must patch BOTH paths)
  </read_first>
  <behavior>
    - Test 1: pytest run reports `uses_redis` marker is registered (no `PytestUnknownMarkWarning`).
    - Test 2: A test decorated with `@pytest.mark.uses_redis` receives the `redis_mock` fixture automatically (via `pytest_collection_modifyitems` hook), even if not explicitly requested by name.
    - Test 3: `redis_mock` GET/SET round-trip works: `await redis_mock.set("k", "v"); assert await redis_mock.get("k") == "v"`.
    - Test 4: `redis_mock` RPUSH/LRANGE works: `await redis_mock.rpush("list", "a", "b"); assert await redis_mock.lrange("list", 0, -1) == ["a", "b"]`.
    - Test 5: `redis_mock` ZADD/ZCOUNT (sorted set, used by main.py rate-limiter): `await redis_mock.zadd("rate", {"req1": 1.0}); assert await redis_mock.zcount("rate", 0, 10) == 1`.
    - Test 6: `redis_mock` EXPIRE + pipeline works (await pipeline().execute()).
    - Test 7: `utils.cache.get_redis()` returns the redis_mock instance when the fixture is active.
    - Test 8: `redis.asyncio.from_url(...)` also returns the redis_mock instance (covers ShortTermMemory Pitfall 6).
    - Test 9: app_factory fixture yields a callable; calling it invokes create_app() and is skipped if _configure_app missing (pytest.importorskip gate).
  </behavior>
  <action>
    Edit `tests/conftest.py` (append, do not replace existing content). Required additions (RESEARCH §Theme 2 + PATTERNS.md §"tests/conftest.py — extend"):

    1. `pytest_configure(config)` function — register two markers via `config.addinivalue_line`:
       - `"uses_redis: test exercises Redis path; redis_mock fixture auto-applied"`
       - `"benchmark: long-running latency benchmark; opt-in via -m benchmark"`

    2. `pytest_collection_modifyitems(config, items)` function — iterate items; for any `item` where `"uses_redis" in item.keywords and "redis_mock" not in item.fixturenames`, append `"redis_mock"` to `item.fixturenames`. This auto-applies the fixture per D-18.

    3. `redis_mock` function-scoped async fixture:
       - `import fakeredis.aioredis; fake = fakeredis.aioredis.FakeRedis(decode_responses=True)` (NOT `MagicMock(spec=...)` — overrides CONTEXT D-20 per RESEARCH §Theme 2 lines 516-520).
       - Define `async def _get_redis_stub(): return fake`.
       - `monkeypatch.setattr("utils.cache.get_redis", _get_redis_stub)` — primary consumer-path patch.
       - `import utils.cache as cache_mod; monkeypatch.setattr(cache_mod, "_redis_client", None, raising=False)` — reset cached singleton.
       - Define `async def _from_url_stub(*args, **kwargs): return fake`.
       - `monkeypatch.setattr("redis.asyncio.from_url", _from_url_stub)` — covers ShortTermMemory bypass (Pitfall 6). Bonus delegate in 27-02 will eventually make this redundant, but keep it for safety belt + integration with the 8 unmigrated tests.
       - `yield fake`
       - Teardown: `try: await fake.aclose(); except Exception as exc: logger.debug(...)` — narrow exception OK here (test infra cleanup; project rule allows it for cleanup paths per CLAUDE.md "no bare except" interpretation — use `except Exception` is technically bare-ish; prefer `except (RuntimeError, OSError, AttributeError)`).

    4. `app_factory` function-scoped async fixture:
       - Lazy import `from tests.factories.app import create_app, _reset_singletons` inside the fixture body.
       - `created: list[FastAPI] = []`
       - Define `def _factory(**kwargs): app = create_app(**kwargs); created.append(app); return app`
       - `yield _factory`
       - Teardown: `_reset_singletons()` once more for cleanliness.

    5. `isolated_app` function-scoped async fixture:
       - `return app_factory()` — pre-built app for tests that don't need overrides.

    6. `isolated_client` function-scoped async fixture:
       - `from httpx import ASGITransport, AsyncClient`
       - `async with AsyncClient(transport=ASGITransport(app=isolated_app), base_url="http://test") as c: yield c`
       - Skips lifespan execution (Pitfall 4 — fine for SC-1 cross-contamination test which asserts at singleton-reset level).

    Create `tests/unit/test_redis_mock_fixture.py`:
    - Pattern A env-var setdefault at module top.
    - All tests use `@pytest.mark.uses_redis` + `@pytest.mark.asyncio`.
    - Cover the 9 behaviors above.
    - Analog: PATTERNS.md cites `test_memory_service.py:18-86` for fakeredis self-test patterns.

    Per CLAUDE.md compliance: no bare except (use narrow exception tuple in teardown), structured logging (use loguru `logger.debug` if importing it; otherwise stdlib `logging.getLogger(__name__).debug`), no blocking I/O in async (fakeredis async is non-blocking).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_redis_mock_fixture.py -x -v 2>&1 | tail -40 && uv run pytest --collect-only -q tests/unit/test_redis_mock_fixture.py 2>&1 | grep -c "uses_redis" || true</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_redis_mock_fixture.py -x` — all 9 behavior tests pass.
    - `uv run pytest --markers 2>&1 | grep -q "uses_redis"` — marker registered.
    - `uv run pytest tests/unit/test_redis_mock_fixture.py 2>&1 | grep -c "PytestUnknownMarkWarning"` returns 0 (no unknown-marker warning).
    - `grep -v '^#' tests/conftest.py | grep -c "fakeredis.aioredis.FakeRedis"` returns >= 1 (NOT MagicMock — sanity check on D-20 override).
    - `grep -v '^#' tests/conftest.py | grep -c "monkeypatch.setattr(.redis.asyncio.from_url"` returns >= 1 (Pitfall 6 patch present).
    - `grep -v '^#' tests/conftest.py | grep -c "pytest_collection_modifyitems"` returns >= 1.
    - `uv run ruff check tests/conftest.py tests/unit/test_redis_mock_fixture.py` passes.
    - `uv run pytest tests/unit -x --timeout 30 -q 2>&1 | tail -5` — full unit suite still collects + runs (no fixture conflict regressions). Pre-existing failures may remain (TD-06 closes those in 27-02).
  </acceptance_criteria>
  <done>redis_mock fixture lives in tests/conftest.py backed by fakeredis.aioredis.FakeRedis; uses_redis + benchmark markers registered; pytest_collection_modifyitems auto-applies redis_mock to marked tests; app_factory/isolated_app/isolated_client fixtures present; 9 self-tests pass; no regressions on existing unit suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → pytest fixture state | Tests share Python process; fixture leakage cross-contaminates. TD-02 brute-force reset is the mitigation; this plan ships the scaffolding (no consumers yet — Wave 1 onwards). |
| Mocked Redis ↔ production Redis | redis_mock fixture must NOT leak to integration tests that legitimately need real Redis (CONTEXT D-21). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-00-01 | Tampering | tests/conftest.py redis_mock | mitigate | Function-scoped (NOT autouse globally); marker-opt-in via `@pytest.mark.uses_redis` + `pytest_collection_modifyitems` per D-18. Integration tests without the marker run against real Redis. |
| T-27-00-02 | Tampering | tests/factories/app.py _SINGLETON_INVENTORY drift | mitigate (deferred to 27-01) | 27-01 ships `tests/unit/test_singleton_inventory_complete.py` lint test (D-03) that fails CI when new `_X = None` patterns appear under `services/` without an inventory entry. |
| T-27-00-03 | Information Disclosure | Test process env vars (APP_MODEL_DIR, SECRET_KEY) | accept | Test-only env values; never written to logs or audit. Pattern A setdefault uses generic test strings. |
| T-27-00-04 | Denial of Service | fakeredis state buildup across tests | mitigate | Function-scoped fixture + `await fake.aclose()` teardown ensures per-test isolation; no cross-test state. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_app_factory.py tests/unit/test_redis_mock_fixture.py -x -v` — both new test files pass.
- `uv run pytest tests/unit -x --timeout 30 -q` — full unit suite still runs (pre-existing failures may persist; this plan does not change failure counts).
- `uv run python -c "from tests.factories.app import _SINGLETON_INVENTORY; assert len(_SINGLETON_INVENTORY) >= 32"` exits 0.
- `uv run ruff check tests/factories/ tests/conftest.py tests/unit/test_app_factory.py tests/unit/test_redis_mock_fixture.py` passes.
- `uv run mypy --strict tests/factories/app.py` passes.
</verification>

<success_criteria>
- tests/factories/ package created with create_app() + _SINGLETON_INVENTORY (>=32 entries) + _reset_singletons() helper.
- tests/conftest.py extended with redis_mock (fakeredis-backed), app_factory, isolated_app, isolated_client fixtures + uses_redis/benchmark marker registration + pytest_collection_modifyitems hook.
- 2 self-test files (test_app_factory.py, test_redis_mock_fixture.py) pass.
- Zero production-source touches (services/, controllers/, main.py untouched).
- mypy --strict + ruff clean on all new files.
</success_criteria>

<output>
Create `.planning/phases/27-test-isolation-memory-reliability/27-00-SUMMARY.md` capturing: files created, _SINGLETON_INVENTORY final size + diff from CONTEXT D-02's 15 entries, fakeredis version pin verification, any singletons discovered by plan-time grep that were NOT in RESEARCH §1.
</output>
