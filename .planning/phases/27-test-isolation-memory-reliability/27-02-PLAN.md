---
plan_id: 27-02-redis-mock-rollout
phase: 27
plan: 02
type: execute
wave: 1
depends_on: [27-00-test-infra-prep]
requirements: [TD-06]
files_modified:
  - services/memory/memory_service.py
  - tests/unit/test_short_term_memory_get_redis.py
  - tests/unit/test_redis_mock_baseline_diagnostic.py
  - .planning/phases/27-test-isolation-memory-reliability/27-02-DIAGNOSTIC.md
autonomous: true
notes: |
  Wave 1 owns SC-2: redis_mock fixture rollout + ShortTermMemory bypass refactor + D-22 diagnostic.
  - The "32 PR #9 unit failures" (openai SDK signature drift) are NOT closed by this plan. D-22 diagnostic step measures the actual TD-06 impact on Redis-ConnectionError failures separately. Both kinds of failures may coexist on master; v1.8+ todo tracks openai-SDK cleanup.
  - ShortTermMemory._get_client refactor IS in scope as a bonus delivery (CONTEXT Claude's Discretion + RESEARCH §6 — 3-line surgical change, enables single-target mocking).
  - Marker-rollout to existing failing unit-test files (test_agent_pipeline_refactor.py, test_agent_sse.py, test_feedback_ab_forward.py, test_pipeline_coverage.py) is the marker-application sub-task.
  - This plan does NOT touch tests/conftest.py or tests/factories/ — those are 27-00 territory. It consumes redis_mock from 27-00 and proves it closes Redis-ConnectionError failures.

must_haves:
  truths:
    - "ShortTermMemory._get_client delegates to utils.cache.get_redis (no direct redis.asyncio.from_url import in ShortTermMemory)"
    - "Adding @pytest.mark.uses_redis to a unit test that previously failed with redis.exceptions.ConnectionError makes it pass"
    - "Pre/post diagnostic captures the Redis-ConnectionError failure count change (D-22)"
    - "Integration tests without the marker still hit real Redis (no force-mock leakage to integration suite)"
  artifacts:
    - path: services/memory/memory_service.py
      provides: "ShortTermMemory._get_client refactored to delegate to utils.cache.get_redis"
      contains: "from utils.cache import get_redis"
    - path: tests/unit/test_short_term_memory_get_redis.py
      provides: "Asserts ShortTermMemory uses get_redis() — receives redis_mock via standard fixture"
    - path: tests/unit/test_redis_mock_baseline_diagnostic.py
      provides: "D-22 diagnostic — counts redis.exceptions.ConnectionError vs APIError-missing-request failures pre/post marker rollout"
  key_links:
    - from: services/memory/memory_service.py::ShortTermMemory._get_client
      to: utils.cache.get_redis
      via: lazy import inside method body
      pattern: "from utils.cache import get_redis"
    - from: existing failing unit tests (test_agent_pipeline_refactor.py, test_agent_sse.py, test_feedback_ab_forward.py, test_pipeline_coverage.py)
      to: tests/conftest.py::redis_mock
      via: @pytest.mark.uses_redis marker + pytest_collection_modifyitems auto-application
      pattern: "@pytest.mark.uses_redis"
---

<objective>
Land SC-2 of Phase 27: Roll out `redis_mock` fixture across the unit test suite to close Redis-baseline failures. Refactor `ShortTermMemory._get_client` to delegate to `utils.cache.get_redis` (single-target mocking enabler — bonus delivery per CONTEXT Claude's Discretion). Capture the D-22 diagnostic so the TD-06 impact is measured separately from the openai SDK drift failures.

Purpose: 27-00 shipped the `redis_mock` fixture; this plan wires it to every unit test that needs it via `@pytest.mark.uses_redis` markers + closes the only remaining redis-bypass (ShortTermMemory). The 32-PR-#9 openai-SDK-drift failures persist unchanged (separate v1.8+ todo per STATE.md).

Output: services/memory/memory_service.py 3-line refactor + marker-application across 4-6 existing unit test files + 2 new test files (ShortTermMemory get_redis self-test + D-22 diagnostic).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-isolation-memory-reliability/27-CONTEXT.md
@.planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md
@.planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md
@.planning/phases/27-test-isolation-memory-reliability/27-VALIDATION.md
@.planning/phases/27-test-isolation-memory-reliability/27-00-PLAN.md
@services/memory/memory_service.py
@utils/cache.py
@tests/conftest.py
@CLAUDE.md
</context>

<interfaces>
Key existing interfaces:

From utils/cache.py:
- `async def get_redis() -> redis.asyncio.Redis` at line 19 — singleton accessor; caches via module-level `_redis_client`. Mocked in 27-00's redis_mock fixture.

From tests/conftest.py (extended in 27-00):
- `redis_mock` async fixture (function-scoped, marker-opt-in) — yields `fakeredis.aioredis.FakeRedis` instance.
- `uses_redis` marker auto-applies the fixture via `pytest_collection_modifyitems` hook.
- `redis_mock` also patches `redis.asyncio.from_url` as a Pitfall 6 safety belt (still works pre-refactor in this plan, becomes redundant post-refactor).

From services/memory/memory_service.py (current — to refactor):
- `ShortTermMemory._get_client` at lines 87-108: imports `from redis.asyncio import from_url` lazily inside method body and calls `from_url(settings.redis_url, ...)`. Bypasses `utils.cache.get_redis` (the only such bypass per RESEARCH §6).

From v1.6 Phase 24 SUMMARY (referenced in RESEARCH §Theme 2 D-22 + lines 596-597):
- Files known to fail with `redis.exceptions.ConnectionError` when Redis is down: `test_agent_pipeline_refactor.py`, `test_agent_sse.py`, `test_feedback_ab_forward.py`, `test_pipeline_coverage.py`. The "32 PR #9 failures" are a DIFFERENT set (openai SDK signature drift).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor ShortTermMemory._get_client to delegate to utils.cache.get_redis (bonus delivery per Claude's Discretion)</name>
  <files>services/memory/memory_service.py, tests/unit/test_short_term_memory_get_redis.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §6 (lines 209-224, full audit of who uses get_redis vs who bypasses) + §"ShortTermMemory direct-import refactor (D-19 follow-on)" (lines 598-610, exact 3-line replacement) + §Pitfall 6 (lines 1083-1089)
    - .planning/phases/27-test-isolation-memory-reliability/27-PATTERNS.md §"ShortTermMemory._get_client — TD-06 bonus delegate" (lines 115-130)
    - services/memory/memory_service.py lines 80-180 (current ShortTermMemory class — _get_client at 87-108, ShortTermMemory constructor + state shape)
    - utils/cache.py lines 1-137 (get_redis return shape, module-level _redis_client singleton, settings.redis_url resolution)
    - tests/conftest.py (redis_mock fixture from 27-00 — yields fakeredis.aioredis.FakeRedis)
  </read_first>
  <behavior>
    - Test 1: `ShortTermMemory()._get_client()` returns the redis_mock instance when `@pytest.mark.uses_redis` is active and `utils.cache.get_redis` is mocked.
    - Test 2: After refactor, `services.memory.memory_service` module no longer contains `from redis.asyncio import from_url` (or any top-level direct `redis.asyncio` import within ShortTermMemory's method bodies — only `from utils.cache import get_redis`).
    - Test 3 (regression): `ShortTermMemory.add_user_message`/`get_user_history` (the methods that use `_get_client()`) still work end-to-end against the fakeredis backing (round-trip RPUSH + LRANGE).
  </behavior>
  <action>
    Edit `services/memory/memory_service.py` ShortTermMemory class (lines ~80-180):

    1. Replace `_get_client` method body verbatim per PATTERNS.md §"ShortTermMemory._get_client" (3-line replacement):
       - OLD body (lines 87-108): `if self._client is None: from redis.asyncio import from_url; self._client = await from_url(settings.redis_url, decode_responses=True, ...); return self._client`
       - NEW body: `if self._client is None: from utils.cache import get_redis; self._client = await get_redis(); return self._client`
       - Preserve method signature, return type, async/await semantics, `self._client` caching attribute.
    2. Remove any now-unused module-level `from redis.asyncio import from_url` import IF AND ONLY IF nothing else in the file uses it. Verify via grep: `grep -c "from_url\|redis.asyncio" services/memory/memory_service.py` — should drop by 1.
    3. Remove `settings.redis_url` direct read from ShortTermMemory if it was only used for this call (`get_redis()` reads it internally via `utils.cache`). Preserve `from config.settings import settings` if used elsewhere in the file.

    Per CLAUDE.md: no bare except (preserve any existing narrow exception tuples in the surrounding code), no blocking I/O in async (get_redis is async — verified), structured logging unchanged, mypy --strict compatibility preserved.

    Create `tests/unit/test_short_term_memory_get_redis.py`:
    - Pattern A env-var setdefault at module top.
    - Pattern B autouse singleton-reset fixture for `_memory_service` (mirrors `test_memory_save_fact.py:43-47`).
    - All tests `@pytest.mark.uses_redis` + `@pytest.mark.asyncio`.
    - Test 1 `test_get_client_returns_redis_mock_via_get_redis`:
      - The redis_mock fixture has already patched `utils.cache.get_redis` → fakeredis.
      - Construct ShortTermMemory directly (`stm = ShortTermMemory()`).
      - `client = await stm._get_client()` — assert `client is redis_mock` (literally the same fake instance, since `get_redis` returns the fake).
    - Test 2 `test_get_client_caches_after_first_call`:
      - Two `await stm._get_client()` calls — assert returned client is the same instance both times (singleton cache works).
    - Test 3 `test_add_message_round_trip_via_fake`:
      - `stm = ShortTermMemory()`. `await stm.add_user_message(user_id="u1", message="hello")` (or whatever the actual ShortTermMemory write API is — verify exact signature in services/memory/memory_service.py).
      - `await stm.get_user_history(user_id="u1", limit=10)` — assert list contains the written message.
      - Uses fakeredis lists under the hood. Proves end-to-end refactor doesn't break behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_short_term_memory_get_redis.py -x -v 2>&1 | tail -20 && grep -c "from redis.asyncio import from_url\|redis.asyncio.from_url" services/memory/memory_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' services/memory/memory_service.py | grep -c "from redis.asyncio import from_url"` returns 0.
    - `grep -v '^#' services/memory/memory_service.py | grep -c "from utils.cache import get_redis"` returns >= 1 (lazy import inside _get_client).
    - `uv run pytest tests/unit/test_short_term_memory_get_redis.py -x` — all 3 tests pass.
    - Regression check: `uv run pytest tests/unit/test_memory_service.py -x --timeout 30` — exit 0 (existing ShortTermMemory unit tests still green).
    - `uv run ruff check services/memory/memory_service.py tests/unit/test_short_term_memory_get_redis.py` passes.
    - `uv run mypy --strict services/memory/memory_service.py` — preserves the baseline pass/fail status; do not regress.
  </acceptance_criteria>
  <done>ShortTermMemory._get_client delegates to utils.cache.get_redis; only redis-bypass in services/ closed; round-trip test via redis_mock proves no behavioral regression; existing memory service tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-22 diagnostic + uses_redis marker rollout to existing failing unit tests</name>
  <files>tests/unit/test_redis_mock_baseline_diagnostic.py, tests/unit/test_agent_pipeline_refactor.py, tests/unit/test_agent_sse.py, tests/unit/test_feedback_ab_forward.py, tests/unit/test_pipeline_coverage.py</files>
  <read_first>
    - .planning/phases/27-test-isolation-memory-reliability/27-RESEARCH.md §Theme 2 "Diagnostic plan for D-22 caveat" (lines 587-596, exact 4-step protocol) + lines 230-238 (file list with their `from main import app` counts)
    - .planning/phases/27-test-isolation-memory-reliability/27-VALIDATION.md §"Manual-Only Verifications" (SC-2 row — comparison against historical baseline)
    - tests/unit/test_agent_pipeline_refactor.py (head — to confirm whether it currently has any redis markers; understand what kind of tests these are — most likely they import main / consume Redis indirectly via fixtures)
    - tests/unit/test_agent_sse.py (head — same; the v1.6 Phase 24 SUMMARY lists this as Redis-baseline-failure)
    - tests/unit/test_feedback_ab_forward.py (head — feedback service uses Redis via utils.cache)
    - tests/unit/test_pipeline_coverage.py (head — pipeline.py uses get_redis for rate limiting + ingest queue)
  </read_first>
  <behavior>
    - Test 1 (diagnostic, the new file): runs a controlled experiment — collect (1) full failure list of `tests/unit/` BEFORE marker application (already known: a mix of redis.exceptions.ConnectionError + APIError-missing-request); (2) verifies that the four target files contain `@pytest.mark.uses_redis` after rollout; (3) re-runs `tests/unit/` and counts Redis-ConnectionError-mode failures — must drop to 0 (per SC-2 acceptance from VALIDATION.md). openai SDK failures persist unchanged.
    - The 4 existing files: marker added at file-level (or class-level if class-shaped). They are not rewritten — only the marker is added. If a file is already structured per-test and only some tests touch Redis, marker is applied at file level for safety (auto-applies redis_mock to all tests in the file; harmless for tests that don't touch Redis).
  </behavior>
  <action>
    Step A — D-22 diagnostic baseline capture (RESEARCH §Theme 2 lines 587-596):

    **CQ1 from /plan-eng-review:** diagnostic written to committed phase file (NOT /tmp) so SC-2 evidence is reviewable in PR diff and reproducible across hosts. Target: `.planning/phases/27-test-isolation-memory-reliability/27-02-DIAGNOSTIC.md`.

    1. Before any marker changes, run `uv run pytest tests/unit/ --co -q 2>&1 | tail -5` to confirm collection works.
    2. Run `uv run pytest tests/unit/ --timeout 30 -q 2>&1 > /tmp/27-02-pre-rollout.log; echo "exit=$?"` (capture full failure log to /tmp ephemeral — only the parsed counts + per-test summary land in the committed file).
    3. Grep the log for failure attribution:
       - `grep -c "redis.exceptions.ConnectionError\|ConnectionError: Error 111\|Cannot connect to Redis" /tmp/27-02-pre-rollout.log` → record as `pre_redis_failures`.
       - `grep -c "APIError.__init__() missing.*'request'" /tmp/27-02-pre-rollout.log` → record as `pre_openai_failures`.
    4. Create `.planning/phases/27-test-isolation-memory-reliability/27-02-DIAGNOSTIC.md` with this structure:
       ```
       # Phase 27 / 27-02 — Redis-Mock Rollout Diagnostic

       **Captured:** YYYY-MM-DD HH:MM:SS UTC
       **Host:** $(hostname)
       **Python:** $(uv run python --version)

       ## Pre-rollout baseline
       - pre_redis_failures: N
       - pre_openai_failures: N

       ## Failing tests (Redis-mode, captured pre-rollout)
       | Test | Failure mode | File |
       |------|--------------|------|
       | test_X | redis.exceptions.ConnectionError | tests/unit/test_agent_sse.py |
       | ... | | |

       ## Post-rollout
       - post_redis_failures: 0 (target)
       - post_openai_failures: N (unchanged — orthogonal v1.8 todo)

       ## Delta
       - Redis-mode failures closed: pre - post = N
       ```
       Populate the table by parsing `/tmp/27-02-pre-rollout.log` for lines matching `FAILED tests/unit/.* - redis.exceptions.ConnectionError|FAILED.*ConnectionError: Error 111`. Use awk/grep, not hand-curation.

    Step B — Apply `@pytest.mark.uses_redis` marker at file level to each of the 4 target files. File-level marker syntax:
    ```python
    import pytest
    pytestmark = pytest.mark.uses_redis  # auto-applies redis_mock via 27-00 hook
    ```
    (If a file already has `pytestmark = [...]` list form, append `pytest.mark.uses_redis` to the list. If a file has per-test `@pytest.mark.X` decorators only, set `pytestmark = pytest.mark.uses_redis` at module top.)

    Files to mark:
    - `tests/unit/test_agent_pipeline_refactor.py`
    - `tests/unit/test_agent_sse.py`
    - `tests/unit/test_feedback_ab_forward.py`
    - `tests/unit/test_pipeline_coverage.py`

    Step C — Re-run + verify Redis-ConnectionError drops to 0:
    1. `uv run pytest tests/unit/ --timeout 30 -q 2>&1 > /tmp/27-02-post-rollout.log; echo "exit=$?"`.
    2. `grep -c "redis.exceptions.ConnectionError\|ConnectionError: Error 111\|Cannot connect to Redis" /tmp/27-02-post-rollout.log` → MUST be 0 (or strictly less than pre_redis_failures; record the delta).
    3. `grep -c "APIError.__init__() missing.*'request'" /tmp/27-02-post-rollout.log` → should be approximately unchanged (openai SDK drift is orthogonal).
    4. Update `.planning/phases/27-test-isolation-memory-reliability/27-02-DIAGNOSTIC.md` Post-rollout + Delta sections with the actual numbers from /tmp/27-02-post-rollout.log. Commit the diagnostic file along with the source changes.

    Step D — Create `tests/unit/test_redis_mock_baseline_diagnostic.py`:
    - Pattern A env-var setdefault.
    - `@pytest.mark.uses_redis` at file level.
    - Test 1 `test_marker_applied_to_known_failing_files`:
      - For each of the 4 files above: `Path("tests/unit/test_X.py").read_text()`. Assert the file contains `uses_redis` (regex `uses_redis` appears at least once).
    - Test 2 `test_no_pre_existing_redis_connection_error_in_marked_files`:
      - Spawn a subprocess: `subprocess.run(["uv", "run", "pytest", *the_4_files, "--timeout", "30", "-q", "--no-header"], capture_output=True, timeout=300)`.
      - Assert `"redis.exceptions.ConnectionError" not in result.stdout + result.stderr` AND `"ConnectionError: Error 111" not in result.stdout + result.stderr`.
      - Note: this test passes IFF the marker rollout successfully closed the Redis-bypass failures. It does NOT assert the openai-SDK drift failures are gone (those remain — separate todo).

    Per CLAUDE.md: no bare except (subprocess interaction uses narrow exception types — `subprocess.TimeoutExpired`, `OSError`), structured logging via loguru where needed.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_redis_mock_baseline_diagnostic.py -x -v --timeout 300 2>&1 | tail -20 && grep -l "uses_redis" tests/unit/test_agent_pipeline_refactor.py tests/unit/test_agent_sse.py tests/unit/test_feedback_ab_forward.py tests/unit/test_pipeline_coverage.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "uses_redis" tests/unit/test_agent_pipeline_refactor.py tests/unit/test_agent_sse.py tests/unit/test_feedback_ab_forward.py tests/unit/test_pipeline_coverage.py` returns all 4 file paths.
    - `uv run pytest tests/unit/test_redis_mock_baseline_diagnostic.py::test_marker_applied_to_known_failing_files -x` passes.
    - `uv run pytest tests/unit/test_redis_mock_baseline_diagnostic.py::test_no_pre_existing_redis_connection_error_in_marked_files -x --timeout 300` passes (Redis-ConnectionError mode failures reach 0 in the 4 marked files).
    - `uv run pytest tests/unit/ --timeout 30 -q 2>&1 | grep -c "redis.exceptions.ConnectionError\|ConnectionError: Error 111"` returns 0 across the whole unit suite.
    - openai SDK drift failures may persist (`uv run pytest tests/unit/ --timeout 30 -q 2>&1 | grep -c "APIError.__init__()"` returns a non-zero number that MUST be documented in the SUMMARY.md as the pre-existing v1.8+ todo, NOT a new regression).
    - Integration suite NOT affected: `uv run pytest tests/integration/ --co -q 2>&1 | tail -5` collects cleanly; no force-mock leakage into integration.
    - `uv run ruff check tests/unit/test_redis_mock_baseline_diagnostic.py` passes.
  </acceptance_criteria>
  <done>4 known-failing unit test files marked with @pytest.mark.uses_redis at file level; diagnostic test asserts ConnectionError failure count goes to 0 in those files; openai SDK drift count documented as orthogonal in the SUMMARY but unchanged; integration suite isolation preserved (no marker leak).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit test → mocked Redis | `redis_mock` provides fakeredis-backed state; unit tests must NOT accidentally hit real Redis. |
| Integration test → real Redis | Integration tests rely on real `localhost:6379`. The marker pattern keeps them un-mocked (D-21). |
| ShortTermMemory refactor → existing memory tests | Behavioral parity required; existing unit tests in test_memory_service.py act as regression gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | ShortTermMemory._get_client refactor | mitigate | Behavioral parity test via fakeredis round-trip (RPUSH/LRANGE) + existing tests/unit/test_memory_service.py regression. |
| T-27-02-02 | Information Disclosure | Force-mock leak to integration suite | mitigate | Marker-opt-in per D-21; integration tests omit `@pytest.mark.uses_redis` → no auto-application; real Redis hit. |
| T-27-02-03 | Repudiation | D-22 diagnostic record drift | accept | The diagnostic file pins the specific failure pattern (`redis.exceptions.ConnectionError`); if the SDK changes the error string, the diagnostic must be updated — tracked in SUMMARY as known maintenance touchpoint. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_short_term_memory_get_redis.py -x -v` — all 3 tests pass.
- `uv run pytest tests/unit/test_redis_mock_baseline_diagnostic.py -x -v --timeout 300` — both tests pass.
- `grep -c "from redis.asyncio import from_url" services/memory/memory_service.py` returns 0.
- `grep -l "uses_redis" tests/unit/test_agent_pipeline_refactor.py tests/unit/test_agent_sse.py tests/unit/test_feedback_ab_forward.py tests/unit/test_pipeline_coverage.py | wc -l` returns 4.
- `uv run pytest tests/unit/ --timeout 30 -q 2>&1 | grep -c "redis.exceptions.ConnectionError"` returns 0.
- `uv run pytest tests/unit/test_memory_service.py -x --timeout 30` — exit 0 (regression check).
- `uv run pytest tests/integration/ --co -q` — collects without errors (no force-mock leakage).
- `uv run ruff check services/memory/memory_service.py tests/unit/test_short_term_memory_get_redis.py tests/unit/test_redis_mock_baseline_diagnostic.py` passes.
</verification>

<success_criteria>
SC-2 of Phase 27:
- `redis_mock` fixture (from 27-00) is consumed by every unit test that fails with Redis-ConnectionError pre-rollout.
- The 4 known-failing unit test files (test_agent_pipeline_refactor.py, test_agent_sse.py, test_feedback_ab_forward.py, test_pipeline_coverage.py) carry `@pytest.mark.uses_redis` at file level.
- Redis-ConnectionError failure count in `tests/unit/` reaches 0.
- ShortTermMemory._get_client delegates to utils.cache.get_redis (bonus: only Redis bypass closed).
- Integration tests bypass the mock automatically (D-21 — no marker → no auto-fixture).
- D-22 caveat documented: openai SDK drift failures persist as separate v1.8+ todo (already in STATE.md).
- SC-2 acceptance "32 → 0 baseline failures" is verified via the diagnostic against the Redis-ConnectionError mode only; openai-SDK drift is orthogonal.
</success_criteria>

<output>
Create `.planning/phases/27-test-isolation-memory-reliability/27-02-SUMMARY.md` capturing: pre/post diagnostic counts (redis_connection_failures_before, redis_connection_failures_after, openai_sdk_failures_before, openai_sdk_failures_after), exact list of files marked with `uses_redis`, any unmarked tests that turned out to also need the marker, ShortTermMemory LOC delta, regression test results.
</output>
