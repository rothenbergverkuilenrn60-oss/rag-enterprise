---
phase: 22-per-module-70-coverage-lift
plan: 06
type: execute
wave: 3
depends_on: [22-01, 22-02, 22-03, 22-04, 22-05]
files_modified:
  - .github/workflows/ci.yml
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: []
tags: [coverage, ci, finalize, milestone-close]

must_haves:
  truths:
    - "All 5 per-module gates flipped from warning-only to hard-fail (D-08): CI step now exits non-zero when any module is <70%"
    - "Run-All-Then-Fail semantics preserved (D-02): all 5 gates execute before job fails — accumulated-status pattern, NOT fail-fast"
    - "README.md Coverage section updated to mention per-module ≥70% floor on 5 v1.5-locked modules (in addition to global ≥70%)"
    - "REQUIREMENTS.md Traceability table TEST-08..12 columns filled with actual plan IDs (22-01..22-05)"
    - "STATE.md Open Q#5 flipped from `open` to `resolved per Phase 22 D-05 (whole-file ≥70%, no per-class breakdown)`"
    - "STATE.md TEST-08..12 marked complete + new Carry-Forward Decisions entry added: `Per-module ≥70% floor on the 5 v1.5-locked modules (Phase 22 D-01..D-08)`"
    - "`make coverage-per-module` exits 0 locally as final pre-flight on combined `.coverage` data"
    - "NO production code changes (CF-01) — `git diff --stat services/ utils/ config/` shows 0 lines"
    - "ci.yml YAML still valid (`python -c 'import yaml; yaml.safe_load(open(...))'` exits 0)"
    - "Zero literal `2026-05-XX` placeholders left in STATE.md (W4): all date stamps substituted via `$(date +%Y-%m-%d)` shell expansion at execution time"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "5 hard-fail per-module gates with run-all-then-fail semantics in coverage-combine job"
      contains: "coverage report --include=services/pipeline.py --fail-under=70"
    - path: README.md
      provides: "Coverage section copy mentioning per-module floor"
      contains: "per-module"
    - path: .planning/REQUIREMENTS.md
      provides: "Traceability table with TEST-08..12 plan IDs filled"
      contains: "22-01"
    - path: .planning/STATE.md
      provides: "Open Q#5 resolved + TEST-08..12 complete + new Carry-Forward Decisions entry"
      contains: "resolved per Phase 22 D-05"
  key_links:
    - from: .github/workflows/ci.yml `coverage-combine` job per-module step
      to: combined `.coverage` data file
      via: "`coverage report --include=services/<mod>.py --fail-under=70` (5 invocations) + accumulated-status final assertion that exits non-zero on any miss"
      pattern: "if \\[ \"\\$\\{?STATUS\\[.+\\]\\}\" -ne 0 \\]"
    - from: .planning/STATE.md Open Questions section
      to: D-05 resolution
      via: "Q#5 entry text update from `open` to `resolved per Phase 22 D-05`"
      pattern: "resolved per Phase 22 D-05"
---

<objective>
Close Phase 22: flip the 5 warning-only per-module CI gates installed in 22-00 to hard-fail (D-08); update README Coverage section copy; mark TEST-08..12 complete in REQUIREMENTS.md Traceability; resolve STATE.md Open Q#5 per D-05 + add new Carry-Forward Decisions entry; run `make coverage-per-module` locally as final pre-flight green-check.

Purpose: Single locking point — no per-plan ci.yml conflicts. By Wave 3 all 5 module test files have landed and verified ≥70% individually; this plan locks the gate so future regressions are caught.
Output: ci.yml hard-fail flip + README/REQUIREMENTS/STATE doc updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md
@.planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md
@.planning/phases/22-per-module-70-coverage-lift/22-00-SUMMARY.md
@.planning/phases/22-per-module-70-coverage-lift/22-01-SUMMARY.md
@.planning/phases/22-per-module-70-coverage-lift/22-02-SUMMARY.md
@.planning/phases/22-per-module-70-coverage-lift/22-03-SUMMARY.md
@.planning/phases/22-per-module-70-coverage-lift/22-04-SUMMARY.md
@.planning/phases/22-per-module-70-coverage-lift/22-05-SUMMARY.md
@.github/workflows/ci.yml
@README.md

<interfaces>
<!-- The Phase 22 staged-flip mechanism (D-08) -->

22-00 installed the per-module gate step in ci.yml `coverage-combine` job with these warning-only wrappers:
- `set +e` at top of `run:` block (allows commands to fail without exiting the step)
- `STATUS[$MOD]=$?` collecting exit codes per module
- `echo "::warning ..."` instead of `exit 1` on individual failures
- `exit 0` at the end of the script (forces step to pass regardless of accumulated statuses)

This plan flips:
- Keep `set +e` + `STATUS` accumulation (D-02 run-all-then-fail semantics)
- Change `echo "::warning ..."` → `echo "::error ..."` (signals to GitHub Actions UI)
- Change final `exit 0` → final assertion that exits non-zero if any STATUS is non-zero
- Remove the "warning-only" comment line; replace with "hard-fail per Phase 22 D-08"

Existing STATE.md Open Q#5 text (lines ~73-74 of STATE.md):
> 5. **Coverage lift scope drift.** When `pipeline.py` covers AgentQueryPipeline + SwarmQueryPipeline + QueryPipeline (5500+ lines), 70% may force test bloat. Per-class breakdown vs whole-file? Decide in coverage-lift phase plan.

Replacement text (W4 fix — date substituted at execution time, NOT a literal string):
> 5. **Coverage lift scope drift.** ✓ resolved per Phase 22 D-05 (whole-file ≥70%, no per-class breakdown; pipeline.py treated as single denominator). Plans 22-00..22-06 shipped <today's date — generate via `date +%Y-%m-%d`>.

Existing STATE.md Carry-Forward Decisions table — add new row:
> | Per-module ≥70% floor on 5 v1.5-locked modules | v1.5 Phase 22 D-01..D-08 | CI fails on regression in services/pipeline.py / generator/llm_client.py / vectorizer/vector_store.py / retriever/retriever.py / extractor/extractor.py |

Existing REQUIREMENTS.md Traceability rows for TEST-08..12 (lines 88-92) currently say `tbd (assigned during /gsd-plan-phase 22)`. Replace `tbd` with the actual plan ID:
- TEST-08 → `22-01`
- TEST-09 → `22-02`
- TEST-10 → `22-03`
- TEST-11 → `22-04`
- TEST-12 → `22-05`

README.md Coverage section (Phase 15 referenced lines 241-273) — add a new sentence after the existing "global ≥70% combined floor" mention noting:
> Phase 22 (v1.5) added a per-module ≥70% line-coverage floor on five high-traffic modules: `services/pipeline.py`, `services/generator/llm_client.py`, `services/vectorizer/vector_store.py`, `services/retriever/retriever.py`, `services/extractor/extractor.py`. CI fails the `coverage-combine` job if any of these regress below 70%.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip 5 ci.yml per-module gates from warning-only to hard-fail</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file; pay attention to the per-module gate step installed in plan 22-00 — locate via `grep -n 'Phase-22 per-module coverage floor'`)
    - .planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md (D-08 staged flip; D-02 run-all-then-fail semantics; CF-05 topology lock — only this step changes)
  </read_first>
  <action>
1. Locate the existing Phase-22 per-module gate step in `.github/workflows/ci.yml` (added in 22-00; identified by `name: Phase-22 per-module coverage floor (warning-only at 22-00; flips to hard-fail at 22-06)`).

2. Replace the entire step `run:` body with the hard-fail version. The new step should be:

```yaml
      - name: Phase-22 per-module coverage floor (hard-fail per D-08 milestone close)
        # D-01: 5 separate `coverage report --include=...` invocations against combined data
        # D-02: run-all-then-fail — collect statuses, exit non-zero AFTER all 5 have run
        # D-04: gates run on combined `.coverage` (downstream of `coverage combine` step above)
        # D-08: HARD-FAIL — replaces warning-only mode from plan 22-00
        run: |
          set +e
          declare -A STATUS
          for MOD in \
            services/pipeline.py \
            services/generator/llm_client.py \
            services/vectorizer/vector_store.py \
            services/retriever/retriever.py \
            services/extractor/extractor.py; do
            echo "::group::Per-module gate: $MOD"
            coverage report --include="$MOD" --fail-under=70
            STATUS[$MOD]=$?
            if [ "${STATUS[$MOD]}" -ne 0 ]; then
              echo "::error file=$MOD::Per-module coverage <70% (Phase 22 D-08 hard-fail gate)"
            else
              echo "PASS: $MOD ≥70%"
            fi
            echo "::endgroup::"
          done
          # D-02 run-all-then-fail: accumulate, then assert
          FAILED=0
          for MOD in "${!STATUS[@]}"; do
            if [ "${STATUS[$MOD]}" -ne 0 ]; then
              FAILED=1
              echo "FAIL: $MOD did not meet ≥70% per-module floor"
            fi
          done
          if [ "$FAILED" -ne 0 ]; then
            echo ""
            echo "Phase 22 per-module gate failed. See module-by-module output above."
            exit 1
          fi
          echo "All 5 Phase-22 modules ≥70% — gate PASS"
```

3. Verify:
   - The step's `name:` field includes `hard-fail` (replaces `warning-only`)
   - The step contains `exit 1` (D-08 hard-fail)
   - The step still uses `set +e` + `STATUS` accumulation (D-02 run-all-then-fail)
   - The 5 module paths are unchanged (CF-05 list locked)
   - No upstream-job edits (CF-05 topology lock)

4. Do NOT modify any other step in ci.yml. Do NOT modify `coverage report --fail-under=70` (the global floor at line ~173-174).
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -q 'hard-fail per D-08 milestone close' .github/workflows/ci.yml && [ "$(grep -c 'warning-only' .github/workflows/ci.yml)" -eq 0 ] && grep -q 'exit 1' .github/workflows/ci.yml && grep -c 'coverage report --include=services/' .github/workflows/ci.yml | awk '{exit ($1 < 5)}' && grep -q 'STATUS\[\$MOD\]=\$?' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
    - Step name contains `hard-fail per D-08 milestone close`
    - Zero occurrences of `warning-only` in `.github/workflows/ci.yml` (D-08 wrappers fully removed)
    - Step contains `exit 1` (hard-fail trigger)
    - Step contains `STATUS[$MOD]=$?` (D-02 run-all-then-fail accumulation preserved)
    - All 5 module paths still present (`grep -c 'coverage report --include=services/' ≥ 5`)
    - `git diff --stat services/ utils/ config/` shows 0 files changed (CF-01)
    - `git diff .github/workflows/ci.yml` shows changes ONLY in the Phase-22 step block (no upstream-job edits)
  </acceptance_criteria>
  <done>5 per-module gates now exit non-zero on any module <70% with run-all-then-fail semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md Traceability + STATE.md Open Q#5 + Carry-Forward Decisions + TEST-08..12 marks</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 88-92 — TEST-08..12 traceability rows)
    - .planning/STATE.md (lines 36-40 — Phase Overview row for Phase 22; lines 43-63 — Carry-Forward Decisions table; lines 65-78 — Open Questions section, Q#5 specifically)
    - .planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md (D-05 resolution text; D-01..D-08 carry-forward summary text)
  </read_first>
  <action>
**W4 fix:** Before any STATE.md edits below, capture today's date into a shell variable for verbatim substitution. Do NOT type a literal `2026-05-XX` anywhere — every date string in STATE.md must be the resolved output of `date +%Y-%m-%d` at execution time:

```bash
TODAY=$(date +%Y-%m-%d)
echo "Substituting TODAY=$TODAY into STATE.md edits"
```

Use `$TODAY` (or the resolved string, e.g., `2026-05-10`) in every replacement below. NEVER copy the placeholder `2026-05-XX` verbatim.

1. **Edit `.planning/REQUIREMENTS.md`** — Traceability table (lines 88-92):
   - Replace `| TEST-08  | 22 — Per-Module 70% Coverage Lift | tbd (assigned during \`/gsd-plan-phase 22\`) |` with `| TEST-08  | 22 — Per-Module 70% Coverage Lift | 22-01 |`
   - Replace `| TEST-09  | 22 — Per-Module 70% Coverage Lift | tbd |` with `| TEST-09  | 22 — Per-Module 70% Coverage Lift | 22-02 |`
   - Replace `| TEST-10  | 22 — Per-Module 70% Coverage Lift | tbd |` with `| TEST-10  | 22 — Per-Module 70% Coverage Lift | 22-03 |`
   - Replace `| TEST-11  | 22 — Per-Module 70% Coverage Lift | tbd |` with `| TEST-11  | 22 — Per-Module 70% Coverage Lift | 22-04 |`
   - Replace `| TEST-12  | 22 — Per-Module 70% Coverage Lift | tbd |` with `| TEST-12  | 22 — Per-Module 70% Coverage Lift | 22-05 |`
   - Also: change `[ ] **TEST-08**` to `[x] **TEST-08**` (and same for TEST-09..12) in the requirement-list section above the traceability table (lines 36-44)

2. **Edit `.planning/STATE.md`** — Phase Overview row for Phase 22 (line 39):
   - Replace `| 22 | Per-Module 70% Coverage Lift | TEST-08, TEST-09, TEST-10, TEST-11, TEST-12 | Planning |` with `| 22 | Per-Module 70% Coverage Lift | TEST-08, TEST-09, TEST-10, TEST-11, TEST-12 | Complete ✓ |`

3. **Edit `.planning/STATE.md`** — Open Questions section (Q#5 around line 73):
   - Replace the existing Q#5 text (current text starts with `5. **Coverage lift scope drift.** When pipeline.py covers...`) with the line below, where `$TODAY` resolves to the verbatim output of `date +%Y-%m-%d` (W4: NEVER write `2026-05-XX`):
     ```
     5. **Coverage lift scope drift.** ✓ resolved per Phase 22 D-05 (whole-file ≥70%, no per-class breakdown; pipeline.py treated as single denominator). Plans 22-00..22-06 shipped $TODAY. CI gate hard-fail flip in 22-06.
     ```

4. **Edit `.planning/STATE.md`** — Carry-Forward Decisions table (lines 43-63):
   - Append a new row after the existing v1.4 entries (after the row about `SSE event schema in docs/agent-architecture.md`):
     ```
     | Per-module ≥70% floor on 5 v1.5-locked modules | v1.5 Phase 22 D-01..D-08 | CI `coverage-combine` job fails on regression in any of services/pipeline.py / generator/llm_client.py / vectorizer/vector_store.py / retriever/retriever.py / extractor/extractor.py |
     ```

5. **Edit `.planning/STATE.md`** — `last_activity` field in frontmatter (line 8) and `Last activity:` field in body (line 31) and `Session Continuity → Last updated` (line 93). All three use the SAME resolved-`$TODAY` string (W4: NEVER literal `2026-05-XX`):
   - Update each field to: `$TODAY — Phase 22 complete: per-module ≥70% gate hard-fail flip + TEST-08..12 closed`
   - Concretely: shell-substitute `$(date +%Y-%m-%d)` first; then paste the resolved literal date (e.g. `2026-05-10`) into the file. Do NOT leave the `$TODAY` token unresolved in the file content.

6. Use the `Read` tool first to confirm exact current line content, then `Edit` tool for each replacement. Do NOT rewrite the entire files.

7. **W4 final self-check:** After all edits, verify `! grep -q '2026-05-XX' .planning/STATE.md` succeeds (no literal placeholder leaked through). If grep finds the literal string, fix the offending line and re-check.
  </action>
  <verify>
    <automated>grep -q '| TEST-08  | 22 — Per-Module 70% Coverage Lift | 22-01 |' .planning/REQUIREMENTS.md && grep -q '| TEST-09  | 22 — Per-Module 70% Coverage Lift | 22-02 |' .planning/REQUIREMENTS.md && grep -q '| TEST-10  | 22 — Per-Module 70% Coverage Lift | 22-03 |' .planning/REQUIREMENTS.md && grep -q '| TEST-11  | 22 — Per-Module 70% Coverage Lift | 22-04 |' .planning/REQUIREMENTS.md && grep -q '| TEST-12  | 22 — Per-Module 70% Coverage Lift | 22-05 |' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*TEST-08\*\*' .planning/REQUIREMENTS.md && grep -q 'resolved per Phase 22 D-05' .planning/STATE.md && grep -q 'Per-module ≥70% floor on 5 v1.5-locked modules' .planning/STATE.md && [ "$(grep -c '| 22 | Per-Module 70% Coverage Lift |.*Complete ✓ |' .planning/STATE.md)" -ge 1 ] && ! grep -q '2026-05-XX' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md Traceability table: all 5 TEST-08..12 rows have plan IDs (22-01..22-05) replacing `tbd`
    - REQUIREMENTS.md requirement list: TEST-08..12 marked `[x]` (complete)
    - STATE.md Phase 22 row marked `Complete ✓`
    - STATE.md Q#5 contains `resolved per Phase 22 D-05` (D-05 reference is mandatory for traceability)
    - STATE.md Carry-Forward Decisions table contains new row `Per-module ≥70% floor on 5 v1.5-locked modules`
    - **W4 fix:** `! grep -q '2026-05-XX' .planning/STATE.md` succeeds — zero literal `2026-05-XX` placeholders left in STATE.md (all date stamps must be substituted via `$(date +%Y-%m-%d)` shell expansion at execution time, not copied verbatim from the plan)
    - `git diff --stat services/ utils/ config/` shows 0 files changed (CF-01)
  </acceptance_criteria>
  <done>STATE + REQUIREMENTS reflect Phase 22 close; D-05 resolution captured for future planners; W4 verified no literal `2026-05-XX` leaked into STATE.md.</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md Coverage section + run final make coverage-per-module pre-flight</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file; locate Coverage section — Phase 15 referenced lines 241-273; verify exact line range by reading the file)
    - .planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md (D-08 + README copy update mention in `<code_context>` Integration Points)
  </read_first>
  <action>
1. Use `Read` tool first to locate the Coverage section in README.md. Look for headings like `## Coverage`, `## Testing & Coverage`, or `### Coverage Gate`. Identify the existing sentence about the global ≥70% combined floor (Phase 15 wording).

2. Use `Edit` tool to insert a new sentence (or short paragraph) immediately after the existing global-floor mention. Suggested text:
   ```
   **Per-module floor (Phase 22, v1.5):** Five high-traffic modules carry a per-module ≥70% line-coverage gate in addition to the global ≥70% combined floor: `services/pipeline.py`, `services/generator/llm_client.py`, `services/vectorizer/vector_store.py`, `services/retriever/retriever.py`, `services/extractor/extractor.py`. CI fails the `coverage-combine` job if any of these regress below 70%. Run `make coverage-per-module` locally to mirror the CI check.
   ```

3. Do NOT rewrite the rest of the README. The Coverage section content from Phase 15 stays intact.

4. Run final pre-flight locally:
   ```bash
   coverage erase
   COVERAGE_FILE=.coverage.unit pytest tests/unit/ --asyncio-mode=auto --timeout=30 --cov=services --cov=utils --cov-report= -q
   COVERAGE_FILE=.coverage.integration pytest tests/integration/ --asyncio-mode=auto --timeout=60 --cov=services --cov=utils --cov-append --cov-report= -q || true
   coverage combine --keep .coverage.unit .coverage.integration
   make coverage-per-module
   ```
   Assert the make target exits 0 (all 5 modules ≥70% by Wave 3 since 22-01..22-05 each shipped their gate-pass). If it fails, the failing module's plan (22-01..22-05) regressed in some prior commit — investigate before flipping.

5. Final CF-01 self-check: `git diff --stat services/ utils/ config/` shows 0 files changed.
  </action>
  <verify>
    <automated>grep -q 'Per-module floor (Phase 22, v1.5)' README.md && grep -q 'services/pipeline.py.*services/generator/llm_client.py' README.md && grep -q 'make coverage-per-module' README.md && make coverage-per-module && ! git diff --name-only services/ | grep -q '\.py$'</automated>
  </verify>
  <acceptance_criteria>
    - README.md Coverage section contains `Per-module floor (Phase 22, v1.5)` mention
    - README.md mentions all 5 module paths (verify via grep for at least pipeline.py + llm_client.py to confirm the list lands)
    - README.md mentions `make coverage-per-module` for local mirror
    - `make coverage-per-module` exits 0 (all 5 modules ≥70% on combined data)
    - `git diff --stat services/ utils/ config/` shows 0 files changed (CF-01)
    - `git diff README.md` shows additions only in Coverage section, no edits elsewhere
  </acceptance_criteria>
  <done>README documents the new floor; local pre-flight green; Phase 22 milestone close ready for verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo | Hard-fail flip changes the gate's exit semantics; no new write paths to secrets or external systems |
| README documentation | Public-facing copy update; no secrets, no executable instructions changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | Tampering | ci.yml `coverage-combine` job | accept | Workflow file is in repo and reviewed via PR; only the Phase-22 step changes; CF-05 upstream topology unchanged |
| T-22-06-02 | Denial of Service | hard-fail flip blocking CI | mitigate | Plan only flips AFTER 22-01..22-05 each verified their module ≥70% individually; Task 3 runs `make coverage-per-module` locally as final pre-flight before commit |
| T-22-06-03 | Information Disclosure | README.md update | accept | Copy mentions module paths only; no secrets, no internal-only info revealed |
</threat_model>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
- `make coverage-per-module` exits 0 (all 5 modules ≥70% on combined data)
- `git diff --stat services/ utils/ config/` shows 0 files changed (CF-01)
- All 4 doc updates landed: ci.yml hard-fail flip, README Coverage section, REQUIREMENTS Traceability, STATE Q#5 + Carry-Forward + Phase 22 row
- Zero `warning-only` mentions in ci.yml (D-08 wrappers fully removed)
- TEST-08..12 marked `[x]` in REQUIREMENTS.md
- W4: `! grep -q '2026-05-XX' .planning/STATE.md` succeeds (zero literal placeholders)
</verification>

<success_criteria>
1. ci.yml: 5 per-module gates flipped to hard-fail with run-all-then-fail (D-02 + D-08)
2. README.md: Coverage section documents per-module floor (5 modules listed) + `make coverage-per-module` pointer
3. REQUIREMENTS.md: TEST-08..12 marked complete; Traceability columns filled with 22-01..22-05
4. STATE.md: Open Q#5 resolved per D-05; Phase 22 row Complete ✓; new Carry-Forward Decisions entry added; last_activity refreshed
5. `make coverage-per-module` green locally as final pre-flight
6. CF-01 + CF-05 honored: zero production code changes; CI topology preserved
7. W4 honored: zero literal `2026-05-XX` placeholders in STATE.md (all date stamps shell-substituted via `$(date +%Y-%m-%d)`)
</success_criteria>

<output>
After completion, create `.planning/phases/22-per-module-70-coverage-lift/22-06-SUMMARY.md` summarizing: (a) ci.yml step diff (warning→hard-fail), (b) `make coverage-per-module` final output (per-module Cover% per module), (c) doc updates landed, (d) Phase 22 milestone-close confirmation, (e) Q#5 resolution text + new Carry-Forward Decisions entry text, (f) confirmation that no literal `2026-05-XX` leaked into STATE.md (W4). This SUMMARY closes Phase 22 and unblocks `/gsd-verify-work 22`.
</output>
</output>
