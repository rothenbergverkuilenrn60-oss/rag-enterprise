---
phase: 22-per-module-70-coverage-lift
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - Makefile
  - .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md
autonomous: true
requirements: []
tags: [coverage, ci, infrastructure]

must_haves:
  truths:
    - "NO production-code changes (CF-01) — `git diff --stat services/ utils/ config/` shows 0 lines"
    - "ci.yml YAML still parses (`python -c 'import yaml; yaml.safe_load(open(...))'` exits 0)"
    - "5 per-module `coverage report --include=...` invocations live in `coverage-combine` job, all wrapped warning-only (D-08)"
    - "Makefile target `coverage-per-module` mirrors the 5 CI invocations and runs HARD locally (exit non-zero on first miss)"
    - "22-BASELINE.md committed under .planning/phases/22-per-module-70-coverage-lift/ with current per-module Cover% + Missing line ranges"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "5 warning-only per-module gate steps in `coverage-combine` job"
      contains: "coverage report --include=services/pipeline.py --fail-under=70"
    - path: Makefile
      provides: "`coverage-per-module` target mirroring CI gates locally (hard-fail)"
      contains: "coverage-per-module:"
    - path: .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md
      provides: "snapshot of pre-Phase-22 per-module coverage + Missing line ranges per module"
      min_lines: 30
  key_links:
    - from: .github/workflows/ci.yml `coverage-combine` job
      to: combined `.coverage` data file (downstream of `coverage combine`)
      via: "`coverage report --include=services/<mod>.py --fail-under=70` (5 invocations)"
      pattern: "coverage report --include=services/.+\\.py --fail-under=70"
    - from: Makefile `coverage-per-module` target
      to: same combined `.coverage` data file
      via: "shared invocation pattern w/ CI"
      pattern: "coverage-per-module:"
---

<objective>
Install warning-only per-module CI gates + local Makefile mirror + baseline measurement so plans 22-01..22-05 have a concrete "stmts to add" budget and gate-mechanism integration is verified before any test work begins. Single locking point for ci.yml topology changes.

Purpose: D-01..D-04 enforcement mechanism + D-08 staged gate-flip strategy. Warning-only at 22-00 so CI stays green during the milestone window; hard-fail flip happens in 22-06.
Output: ci.yml gates, Makefile target, BASELINE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md
@.github/workflows/ci.yml
@Makefile
@pyproject.toml

<interfaces>
<!-- Existing ci.yml `coverage-combine` job structure (lines 128-200, Phase 15 D-02 locked).
     Phase 22 ADDS 5 gate steps after the existing "TEST-06 — Floor gate (combined coverage >= 70%)"
     step (line ~173-174). Does NOT modify upstream jobs (unit-tests, integration-tests). -->

The existing ci.yml `coverage-combine` job (Phase 15 D-02) already runs:
1. `coverage combine --keep .coverage.unit .coverage.integration`
2. `coverage report` (TEST-04: visible per-file table)
3. `coverage report --fail-under=70` (TEST-06: global floor)
4. `coverage xml -o coverage.xml`
5. `diff-cover coverage.xml --compare-branch=v1.0 --fail-under=80`

Phase 22 inserts the 5 new per-module gates AFTER step 3 (global floor) and BEFORE step 4 (xml).
This way they share the combined `.coverage` data and run in the same job.

Existing Makefile already has `coverage-combined` target (lines 123-137) that mirrors the global ci.yml gate.
Phase 22 adds NEW target `coverage-per-module` that runs the 5 per-module gates against the same combined data.

Per-module modules (D-01 list, locked):
- services/pipeline.py
- services/generator/llm_client.py
- services/vectorizer/vector_store.py
- services/retriever/retriever.py
- services/extractor/extractor.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run baseline measurement and write 22-BASELINE.md</name>
  <files>.planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md</files>
  <read_first>
    - .planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md (D-01..D-08, baseline table at top of `<domain>` block)
    - Makefile (existing `coverage-combined` target at lines 123-137)
    - pyproject.toml `[tool.coverage.run]` block (CF-04 locked config: parallel=false, branch=false, source=["services","utils"])
  </read_first>
  <action>
1. Run combined-coverage measurement locally to produce a fresh `.coverage` data file:
   ```bash
   coverage erase
   COVERAGE_FILE=.coverage.unit pytest tests/unit/ \
     --asyncio-mode=auto --timeout=30 \
     --cov=services --cov=utils --cov-report= -q
   COVERAGE_FILE=.coverage.integration pytest tests/integration/ \
     --asyncio-mode=auto --timeout=60 \
     --cov=services --cov=utils --cov-append --cov-report= -q || true
   coverage combine --keep .coverage.unit .coverage.integration
   ```
2. For each of the 5 Phase-22 modules, capture the per-module report:
   ```bash
   for MOD in services/pipeline.py services/generator/llm_client.py \
              services/vectorizer/vector_store.py services/retriever/retriever.py \
              services/extractor/extractor.py; do
     echo "### $MOD"; echo
     coverage report --include="$MOD" --show-missing
     echo
   done > /tmp/22-baseline-raw.txt
   ```
3. Write `.planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md` with:
   - Frontmatter: `measured_at`, `coverage_version` (from `coverage --version`), `python_version`
   - Section "## Per-Module Baseline" with a Markdown table: `| Module | Stmts | Miss | Cover% | Gap to 70% (stmts) |`
   - Section "## Missing Line Ranges" with one subsection per module (`### services/pipeline.py` etc.) containing the verbatim "Missing" column from `coverage report --show-missing` output (use fenced code blocks)
   - Section "## Source-of-Truth Note" stating: "Combined `.coverage` file (unit + integration); CF-08 single source of truth. Plans 22-01..22-05 use these line ranges as the Wave-2 backfill budget per D-06 measure-then-add."
4. Use the `Write` tool — NOT `Bash(cat << EOF)` — to create the file.
  </action>
  <verify>
    <automated>test -s .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md && grep -c '^### services/' .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md | grep -qE '^[5-9]$|^[1-9][0-9]+$' && grep -q 'services/pipeline.py' .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md && grep -q 'services/generator/llm_client.py' .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md && grep -q 'services/vectorizer/vector_store.py' .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md && grep -q 'services/retriever/retriever.py' .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md && grep -q 'services/extractor/extractor.py' .planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md` exists with non-zero size
    - Contains all 5 module names verbatim (`services/pipeline.py`, `services/generator/llm_client.py`, `services/vectorizer/vector_store.py`, `services/retriever/retriever.py`, `services/extractor/extractor.py`)
    - Contains per-module Stmts/Miss/Cover columns
    - Contains "## Missing Line Ranges" section with one `### services/<mod>.py` subsection per module
    - `git diff --stat services/ utils/ config/` shows 0 files changed (CF-01 verification)
  </acceptance_criteria>
  <done>22-BASELINE.md committed; planners 22-01..22-05 can reference Missing line ranges as Wave-2 backfill budget.</done>
</task>

<task type="auto">
  <name>Task 2: Add 5 warning-only per-module gate steps to ci.yml `coverage-combine` job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file; pay attention to lines 128-200 — `coverage-combine` job)
    - .planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md (D-01: 5 separate `coverage report --include=...` invocations; D-02: run-all-then-fail; D-08: warning-only at 22-00)
  </read_first>
  <action>
Insert the following step into `.github/workflows/ci.yml` `coverage-combine` job, AFTER the existing "TEST-06 — Floor gate (combined coverage >= 70%)" step (line ~173-174) and BEFORE the existing "Generate coverage.xml for diff-cover" step (line ~176-177):

```yaml
      - name: Phase-22 per-module coverage floor (warning-only at 22-00; flips to hard-fail at 22-06)
        # D-01: 5 separate `coverage report --include=...` invocations against combined data
        # D-02: run-all-then-fail — accumulate statuses, do NOT exit non-zero yet (gates flip in plan 22-06)
        # D-08: warning-only during 22-00..22-05 milestone window so CI stays green
        # D-04: gates run on combined `.coverage` (downstream of `coverage combine` step above)
        run: |
          set +e
          declare -A STATUS
          for MOD in \
            services/pipeline.py \
            services/generator/llm_client.py \
            services/vectorizer/vector_store.py \
            services/retriever/retriever.py \
            services/extractor/extractor.py; do
            echo "::group::Per-module gate: $MOD"
            coverage report --include="$MOD" --show-missing --fail-under=70
            STATUS[$MOD]=$?
            if [ "${STATUS[$MOD]}" -ne 0 ]; then
              echo "::warning file=$MOD::Per-module coverage <70% (warning-only until plan 22-06 flips to hard-fail)"
            else
              echo "PASS: $MOD ≥70%"
            fi
            echo "::endgroup::"
          done
          # Phase 22 D-08: do NOT exit non-zero yet. Plan 22-06 removes this comment + adds: exit $(( $(IFS=+; echo "$((${STATUS[*]:-0}))") > 0 ? 1 : 0 ))
          echo "Per-module gate summary (warning-only):"
          for MOD in "${!STATUS[@]}"; do
            echo "  $MOD: exit=${STATUS[$MOD]}"
          done
          exit 0
```

Indentation: match the surrounding 6-space indent of other steps in `coverage-combine` job (look at adjacent `- name: ...` entries to confirm).

Do NOT modify any other step in ci.yml. Do NOT modify `unit-tests`, `integration-tests`, `lint-and-type-check`, `security-scan`, `docker-build`, `publish-image`, `deploy-staging`, `eval-gate` jobs (CF-05 topology lock).
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -c 'coverage report --include=services/pipeline.py' .github/workflows/ci.yml | grep -qE '^[1-9]$' && grep -c 'coverage report --include=services/generator/llm_client.py' .github/workflows/ci.yml | grep -qE '^[1-9]$' && grep -c 'coverage report --include=services/vectorizer/vector_store.py' .github/workflows/ci.yml | grep -qE '^[1-9]$' && grep -c 'coverage report --include=services/retriever/retriever.py' .github/workflows/ci.yml | grep -qE '^[1-9]$' && grep -c 'coverage report --include=services/extractor/extractor.py' .github/workflows/ci.yml | grep -qE '^[1-9]$' && grep -q 'Phase-22 per-module coverage floor' .github/workflows/ci.yml && grep -q 'warning-only' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (YAML still valid)
    - All 5 module paths appear in the new step (one `coverage report --include=services/<mod>.py` per module)
    - The step contains the literal string `warning-only` (D-08)
    - The step contains `exit 0` at the end (D-08: do NOT fail CI yet)
    - `git diff --stat services/ utils/ config/` shows 0 files changed (CF-01)
    - `git diff .github/workflows/ci.yml` shows changes ONLY in `coverage-combine` job (no upstream-job edits)
    - The new step lives BETWEEN the existing "TEST-06 — Floor gate" step and "Generate coverage.xml for diff-cover" step (verify by `grep -n` ordering)
  </acceptance_criteria>
  <done>5 warning-only per-module gates installed in ci.yml; CI stays green; ready for 22-01..22-05 to land tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add `coverage-per-module` Makefile target (hard-fail local mirror)</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (full file; pay attention to lines 87-137 — `test`, `test-unit`, `coverage-diff`, `coverage-combined` targets for style reference)
    - .planning/phases/22-per-module-70-coverage-lift/22-CONTEXT.md (D-03: Makefile target mirrors CI gates; runs HARD locally so devs get real pre-flight feedback)
  </read_first>
  <action>
Append the following target to `Makefile` immediately after the existing `coverage-combined:` target (which currently ends at line 137 with `conda run -n torch_env coverage report --fail-under=70`).

User machine is uv-only (CLAUDE.md §1.6: `which conda` returns not-found; the existing `coverage-combined` target's `conda run` invocation is non-functional on this machine). To make the new local pre-flight ACTUALLY runnable per D-03, the recipe MUST use `uv run coverage` (NOT bare `coverage`, NOT `conda run`). Bare `coverage` only works inside an activated venv and breaks D-03's "devs run the same check locally" intent.

Recipe to append (uv-runnable, hard-fail, with a clear "run pytest first" precondition message — note the user must populate `.coverage` via `uv run pytest tests/unit/ ...` before invoking this target):

```make
.PHONY: coverage-per-module
coverage-per-module: ## Phase 22: enforce per-module ≥70% on the 5 v1.5-locked modules (run `uv run pytest tests/unit/ --cov=services --cov=utils` first to populate .coverage)
	@echo ">> Phase 22: per-module ≥70% gate (mirrors ci.yml coverage-combine job)"
	@test -f .coverage || { echo "ERROR: .coverage not found. Run: uv run pytest tests/unit/ --cov=services --cov=utils"; exit 1; }
	@FAILED=""; \
	for MOD in services/pipeline.py services/generator/llm_client.py services/vectorizer/vector_store.py services/retriever/retriever.py services/extractor/extractor.py; do \
		echo ""; echo "=== $$MOD ==="; \
		uv run coverage report --include="$$MOD" --show-missing --fail-under=70 || FAILED="$$FAILED $$MOD"; \
	done; \
	echo ""; \
	if [ -n "$$FAILED" ]; then \
		echo "FAIL: per-module floor missed for:$$FAILED"; \
		exit 1; \
	fi; \
	echo "PASS: all 5 Phase-22 modules ≥70%"
```

Also add `coverage-per-module` to the `.PHONY` line at the top (line 7) — although the recipe declares its own `.PHONY: coverage-per-module` directly above (defense-in-depth), keep the top-of-file `.PHONY` list canonical for readability:

Existing line 7 (current state):
```make
.PHONY: help build up down logs shell ingest eval clean coverage-diff coverage-combined demo-agent demo-agent-record
```

Replace with:
```make
.PHONY: help build up down logs shell ingest eval clean coverage-diff coverage-combined coverage-per-module demo-agent demo-agent-record
```

Out of scope for this plan: do NOT touch the existing `coverage-combined` target. (A single-line swap of its `conda run -n torch_env` to `uv run` would also be acceptable scope creep, but keep this PR tight; track as a deferred item — file a follow-up note in 22-06-SUMMARY if desired.)
  </action>
  <verify>
    <automated>grep -q '^coverage-per-module:' Makefile && grep -q 'uv run coverage' Makefile && grep -q 'services/pipeline.py' Makefile && grep -q 'services/generator/llm_client.py' Makefile && grep -q 'services/vectorizer/vector_store.py' Makefile && grep -q 'services/retriever/retriever.py' Makefile && grep -q 'services/extractor/extractor.py' Makefile && grep -q 'coverage-per-module' <(head -10 Makefile) && make -n coverage-per-module >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^coverage-per-module:' Makefile` succeeds
    - `grep -q 'uv run coverage' Makefile` succeeds (W1 fix: recipe must use `uv run coverage`, NOT bare `coverage` and NOT `conda run`)
    - All 5 Phase-22 module paths appear in the new target
    - `coverage-per-module` listed in the `.PHONY` declaration on line 7
    - `make -n coverage-per-module` (dry-run) prints the recipe without errors
    - Existing `coverage-combined`, `test`, `test-unit`, `coverage-diff` targets UNCHANGED (verify by `git diff Makefile` showing only additions, no deletions in lines 87-137 except .PHONY line)
    - `git diff --stat services/ utils/ config/` shows 0 files changed (CF-01)
  </acceptance_criteria>
  <done>Local pre-flight `make coverage-per-module` available via `uv run coverage`; mirrors CI gates with hard-fail semantics for dev feedback loop on the user's uv-only machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo | CI workflow reads/writes `.coverage*` artifacts; no new credentials added |
| Local `make coverage-per-module` → developer shell | reads `.coverage` only; no network, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-00-01 | Tampering | ci.yml `coverage-combine` job | accept | Workflow file is in repo and reviewed via PR; no new write paths to secrets or external systems |
| T-22-00-02 | Information Disclosure | `.coverage` data file | accept | Coverage data contains line numbers and module paths only; no PII, no secrets, already published as artifact in existing workflow (CF-05 unchanged) |
| T-22-00-03 | Denial of Service | per-module gate steps | mitigate | `set +e` + `exit 0` at end of step ensures CI step ALWAYS completes (D-08 warning-only); cannot accidentally block other downstream jobs |
</threat_model>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
- `make -n coverage-per-module` prints recipe without errors
- `.planning/phases/22-per-module-70-coverage-lift/22-BASELINE.md` exists and contains all 5 module names + Missing line ranges
- `git diff --stat services/ utils/ config/` shows 0 files (CF-01: NO production code changes)
- All 5 module paths appear in BOTH ci.yml gate step AND Makefile target (D-03 mirroring)
</verification>

<success_criteria>
1. ci.yml has 5 warning-only per-module gates in `coverage-combine` job (D-01, D-08)
2. Makefile has `coverage-per-module` hard-fail target mirroring CI gates (D-03)
3. 22-BASELINE.md captures pre-Phase-22 per-module Cover% + Missing line ranges (D-06 prep for measure-then-add)
4. Zero production-code changes (CF-01)
5. CI stays green (warning-only D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/22-per-module-70-coverage-lift/22-00-SUMMARY.md` summarizing: (a) baseline numbers per module, (b) ci.yml step location + warning-only confirmation, (c) Makefile target invocation, (d) note that 22-01..22-05 reference 22-BASELINE.md for Wave-2 backfill budget.
</output>
</output>
