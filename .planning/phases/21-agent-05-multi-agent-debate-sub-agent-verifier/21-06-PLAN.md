---
phase: 21-agent-05-multi-agent-debate-sub-agent-verifier
plan: 06
type: execute
wave: 4
depends_on: [02, 05]
files_modified:
  - docs/agent-architecture.md
requirements: [AGENT-15]
tags: [docs, event-schema, verifier-events, sse, backward-compat, sc4, agent-15]

must_haves:
  truths:
    - "`docs/agent-architecture.md` Event Schema Reference section gains 3 new subsections — `### verifier.start`, `### verifier.complete`, `### verifier.disagreement` — each with: SSE event_type, frozen Pydantic schema (per-event field table mirroring lines 297-307 / 314-321 / 335-341), an example JSON payload, when-emitted prose, ordering guarantees relative to existing events"
    - "A `### Debate Mode` subsection introduced before the 3 new event subsections explains: (a) opt-in `req.debate=True` requires `req.swarm_mode=True` (D-10 422 at Pydantic boundary); (b) latency contract `total ≤ max(peer) + verifier + small_overhead` (SC2/CF-06; verifier sequential after `asyncio.gather`); (c) `synthesizer.final` remains terminal in all paths (CF-07)"
    - "Backward-compat note immediately precedes the 3 new event subsections: `> The three event types below are emitted ONLY when req.debate=True. Non-debate flows are unchanged — the existing six event types remain the complete event surface for debate=False requests. synthesizer.final remains the terminal event in all paths (CF-07).`"
    - "Per-event field tables match the Pydantic schemas frozen in Plan 02's `utils/models.py` additions exactly (no field name drift, no type drift, no required/optional drift). Drift is structurally checked by a doc-vs-code grep verification step in Task 1's verify command"
    - "The `### Consuming the Stream` JavaScript example at line 376 is extended with 3 new `addEventListener` lines for the new event types (mirrors the existing 6 lines verbatim in shape) — keeps the consumer example honest and complete"
    - "NO code changes; doc-only — `wc -l docs/agent-architecture.md` grows by ~80-100 lines; no other file in the repo is touched"
  artifacts:
    - path: "docs/agent-architecture.md"
      provides: "3 new event subsections + 1 new Debate Mode subsection + 1 backward-compat note + 3 new addEventListener lines in the Consuming the Stream example"
      contains: "### verifier.start"
  key_links:
    - from: "docs/agent-architecture.md ### verifier.start (NEW)"
      to: "utils/models.py::VerifierStartEvent (Plan 21-02)"
      via: "frozen schema parity"
      pattern: "peer_count.*integer.*yes"
    - from: "docs/agent-architecture.md ### verifier.complete (NEW)"
      to: "utils/models.py::VerifierCompleteEvent (Plan 21-02)"
      via: "frozen schema parity"
      pattern: "evidence_chunk_count.*integer.*yes"
    - from: "docs/agent-architecture.md ### verifier.disagreement (NEW)"
      to: "utils/models.py::VerifierDisagreementEvent (Plan 21-02)"
      via: "frozen schema parity + 3-value reason Literal"
      pattern: "peers_diverge.*forced_no_evidence.*verifier_failed"
    - from: "docs/agent-architecture.md ### Debate Mode (NEW)"
      to: "services/pipeline.py::SwarmQueryPipeline.run debate hop (Plan 21-05)"
      via: "documents wire behavior of the run_streaming generator"
      pattern: "max\\(peer\\).*verifier"
---

<objective>
Land the SC4 acceptance criterion: extend `docs/agent-architecture.md` Event Schema Reference with 3 new event subsections + a Debate Mode introduction + a backward-compat note + JavaScript consumer extension. This is the final wave of Phase 21; depends on Plan 02 (the 3 event Pydantic schemas) for field-table parity and Plan 05 (the run_streaming wire emission) so the doc claims are wire-truthful, not forward-looking.

Purpose: per CONTEXT SC4, AGENT-15 explicitly requires "`docs/agent-architecture.md` Event Schema Reference extended with three new subsections + example payloads". The doc is the contract that downstream consumers (frontend EventSource handlers, ops dashboards, backend monitoring) read to integrate. Per CONTEXT specifics "Three `reason` values exhaust DisagreementEvent triggers (D-08): adding a fourth requires a Literal bump and a doc update", the doc IS the closed-set contract.

Output: ~80-100 LOC append/insertion in `docs/agent-architecture.md` (3 event subsections + 1 Debate Mode prose subsection + 1 backward-compat note callout + 3 new lines in the existing Consuming-the-Stream JS example). Zero code changes. Zero file additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-CONTEXT.md
@.planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-RESEARCH.md
@.planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-PATTERNS.md
@.planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-VALIDATION.md
@.planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-02-SUMMARY.md
@.planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-05-SUMMARY.md
@CLAUDE.md
@docs/agent-architecture.md
@utils/models.py

<interfaces>
<!-- The 3 new event schemas (frozen by Plan 02) — these are the CONTRACT the doc must mirror. -->

From utils/models.py (Plan 21-02 outputs — these are what the doc tables describe):
```python
class VerifierStartEvent(AgentEvent):
    """Emitted ONCE before Verifier.verify() awaits (D-09)."""
    event_type: ClassVar[str] = "verifier.start"
    model_config = ConfigDict(frozen=True)
    peer_count: int
    model:      str

class VerifierCompleteEvent(AgentEvent):
    """Emitted ONCE after Verifier.verify() returns successfully (D-09)."""
    event_type: ClassVar[str] = "verifier.complete"
    model_config = ConfigDict(frozen=True)
    verdict:              Literal["agree", "disagree"]
    evidence_chunk_count: int
    latency_ms:           int

class VerifierDisagreementEvent(AgentEvent):
    """Emitted on the three disagree paths (D-08).
    summary truncated to 200 chars at the emitter.
    error_type populated only when reason='verifier_failed'.
    """
    event_type: ClassVar[str] = "verifier.disagreement"
    model_config = ConfigDict(frozen=True)
    reason:             Literal["peers_diverge", "forced_no_evidence", "verifier_failed"]
    summary:            str
    evidence_chunk_ids: list[str]
    peer_count:         int
    error_type:         str | None = None
```

Common AgentEvent base fields (already documented at docs/agent-architecture.md:254-258 — DO NOT re-document, the new subsections inherit by reference):
- `trace_id: str` (8-hex)
- `seq: int` (monotonic counter)
- `ts_ms: int` (Unix epoch ms)

Existing Event Schema Reference structure (docs/agent-architecture.md):
- Line 245: `## Event Schema Reference` heading
- Lines 247-268: section preamble + common-field table + `event_type` discriminator note + redaction policy
- Line 270: `### planner.plan` (FIRST event subsection)
- Line 292: `### tool.span.start`
- Line 309: `### tool.span.end`
- Line 329: `### tool.span.error`
- Line 349: `### executor.parallel`
- Line 362: `### synthesizer.final` (LAST existing event subsection; ends ~line 373)
- Line 375: `### Consuming the Stream` (JS example with 6 addEventListener lines at 381-386)
- Line 389+: trailing prose about EventSource POST limitations

Layout to mirror — the `tool.span.start/end/error` triple at lines 292-347 is the canonical template. Each event subsection has:
1. `### <event_type>` heading
2. 1-3 paragraph "when emitted" prose
3. Field table: `| Field | Type | Required | Description |` (3-column markdown)
4. "Example payload:" prose + a fenced ```json block

The new 3 verifier event subsections SLOT between the existing `### synthesizer.final` (ends ~line 373) and `### Consuming the Stream` (line 375). The Debate Mode subsection precedes them (acts as a header for the entire 3-event group).

Existing JavaScript example (docs/agent-architecture.md:380-387) — pattern to extend:
```javascript
const es = new EventSource('/api/v1/agent/v1/run/stream'), J = (e) => JSON.parse(e.data);
es.addEventListener('planner.plan',      (e) => { const v = J(e); console.log('plan:', v.plan.parallel_groups, v.plan.rationale); });
es.addEventListener('tool.span.start',   (e) => { const v = J(e); console.log(`start ${v.name} span=${v.span_id}`, v.args); });
es.addEventListener('tool.span.end',     (e) => { const v = J(e); console.log(`end ${v.span_id} ${v.latency_ms}ms ${v.chunk_count} chunks`); });
es.addEventListener('tool.span.error',   (e) => { const v = J(e); console.warn(`err ${v.span_id} ${v.error_type}: ${v.error_message}`); });
es.addEventListener('executor.parallel', (e) => { const v = J(e); console.log(`group fan_out=${v.fan_out} ${v.group_latency_ms}ms`); });
es.addEventListener('synthesizer.final', (e) => { const v = J(e); console.log('final:', v.answer); es.close(); });
```

The 3 new addEventListener lines for verifier events go BEFORE `synthesizer.final` (since synthesizer.final closes the stream and is terminal).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Debate Mode subsection + 3 new event subsections + extend JS consumer example in docs/agent-architecture.md</name>
  <files>docs/agent-architecture.md</files>
  <read_first>
    - docs/agent-architecture.md (full file — 390 lines; specifically: lines 245-268 Event Schema Reference preamble + common fields table; lines 270-291 `### planner.plan` for prose+table+JSON shape reference; lines 292-308 `### tool.span.start` (analog for `### verifier.start` — same start-event shape); lines 309-327 `### tool.span.end` (analog for `### verifier.complete` — same complete-event shape); lines 329-347 `### tool.span.error` (analog for `### verifier.disagreement` — same Literal-discriminated error-event shape with optional `error_type`); lines 362-373 `### synthesizer.final` (insertion point: AFTER this section, BEFORE `### Consuming the Stream` at line 375); lines 375-388 `### Consuming the Stream` (JS example to extend with 3 new addEventListener lines))
    - utils/models.py (Plan 21-02 additions — `VerifierStartEvent`, `VerifierCompleteEvent`, `VerifierDisagreementEvent` schemas; field names, types, optional/required must match the doc tables verbatim)
    - .planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-CONTEXT.md (D-08 wire fields, D-09 sibling shapes, D-10 Pydantic-boundary 422, CF-06 latency contract, CF-07 terminal-event invariant, SC4 doc extension requirement)
    - .planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-RESEARCH.md (Standard plan `exec-2` — paste-ready instructions; Pitfall P-08 language note; mention three `reason` values exhaust the closed set)
    - .planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-PATTERNS.md ("docs/agent-architecture.md — 3 new event subsections" section — paste-ready Markdown analog from `tool.span.end` triple)
    - .planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-05-SUMMARY.md (when written by Plan 05 — confirms `run_streaming` is wire-truthful so the doc can claim "Emitted when `req.debate=True`" without forward-looking caveat; if SUMMARY not yet written at the moment this task runs, fall back to Plan 05's `<success_criteria>` for the same claim)
  </read_first>
  <action>
Make ONE structural edit to `docs/agent-architecture.md`: insert new content between the end of the existing `### synthesizer.final` subsection (currently ends at line 373 — the `Example payload: ...` line and the blank line after it) AND the start of `### Consuming the Stream` (line 375). The insertion is ~80-100 LOC of pure markdown.

**Insertion content (paste verbatim with adaptations described inline):**

```markdown

### Debate Mode

The three event types below — `verifier.start`, `verifier.complete`,
`verifier.disagreement` — are emitted ONLY when the request opts in via
`req.debate=True`. They extend the v1.4 event surface; non-debate flows
remain on the existing six event types unchanged.

**Opt-in contract (D-10):** `req.debate=True` requires `req.swarm_mode=True`.
The `GenerationRequest` Pydantic V2 model raises `ValidationError` at the
FastAPI body-parsing layer (HTTP 422) when only `debate` is set; the verifier
sub-agent runs after the peer fan-out, so debate-without-swarm is structurally
meaningless.

**Latency contract (SC2 / CF-06):** the verifier executes sequentially AFTER
`asyncio.gather` over the peer sub-agents (NOT in parallel with them). The
end-to-end latency upper bound is `max(peer_latency) + verifier_latency +
small_overhead` — never the sum, never `N × verifier_latency`. There is exactly
one verifier call per debate request regardless of peer count `N`.

**Ordering guarantees:** within a debate run, the event sequence is:

1. (debate=False path) — six existing events as documented above; terminal
   `synthesizer.final`.
2. (debate=True, agree) — `verifier.start` → `verifier.complete` → terminal
   `synthesizer.final`.
3. (debate=True, disagree honest) — `verifier.start` →
   `verifier.disagreement` (`reason="peers_diverge"`) → `verifier.complete`
   (with `verdict="disagree"`) → terminal `synthesizer.final`.
4. (debate=True, disagree forced — verifier returned `agree` with empty
   `evidence_chunk_ids`, overridden inside `Verifier.verify()` per CF-04) —
   `verifier.start` → `verifier.disagreement` (`reason="forced_no_evidence"`)
   → `verifier.complete` (with `verdict="disagree"`) → terminal
   `synthesizer.final`.
5. (debate=True, verifier failed — uncaught `BaseException`) —
   `verifier.start` → `verifier.disagreement` (`reason="verifier_failed"`,
   `error_type` populated) → terminal `synthesizer.final` (no `verifier.complete`
   on the failure path; the synthesizer falls through to the standard consensus
   path so the user still gets an answer per D-06 graceful degrade).

**Terminal event invariant (CF-07):** `synthesizer.final` remains the
terminal frame in ALL paths above — the verifier events are intermediate.

**Wire route:** all three event types serialize via the existing
`emit_sse_frame` (`event: <event_type>\ndata: <model_dump_json>\n\n`); no
serializer change. They reach the wire through `SwarmQueryPipeline.run_streaming`
and the existing `POST /api/v1/agent/v1/run/stream` route, which dispatches to
the swarm pipeline when `req.swarm_mode=True` (a 1-line ternary at
`controllers/api.py:274` — `pipeline = get_swarm_pipeline() if req.swarm_mode
else get_agent_pipeline()`). Non-swarm requests continue to route to the agent
pipeline unchanged (CF-08 backward compat).

> **Backward compatibility:** The three event types below are additive.
> Non-debate flows (`debate=False`) emit the v1.4 event set unchanged.
> The existing six event types remain the complete event surface for
> `debate=False` requests. `synthesizer.final` remains the terminal event
> in all paths (CF-07).

### verifier.start

Emitted ONCE per debate run, BEFORE `Verifier.verify()` awaits.
The `peer_count` field reflects the count of sub-agents whose results
are being verified (excludes peers that raised `BaseException`); `model`
is the resolved verifier LLM identifier (per `Settings.verifier_model`
or `Settings.active_model` fallback per D-05).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `peer_count` | integer | yes | Number of successful peer sub-agents whose results are passed to the verifier (failed peers excluded). |
| `model` | string | yes | Verifier LLM identifier — e.g. `"gpt-4o"`, `"claude-sonnet-4-6"`. Resolved from `Settings.verifier_model` (override) or `Settings.active_model` (default). |

Example payload:

```json
{"peer_count": 3, "model": "claude-sonnet-4-6"}
```

### verifier.complete

Emitted ONCE per debate run AFTER `Verifier.verify()` returns successfully.
NOT emitted on the verifier-failed path (where `verifier.disagreement`
with `reason="verifier_failed"` is the only verifier-tier event).
The `verdict` field carries the final discriminator AFTER the CF-04
forced-disagree override has been applied inside `Verifier.verify()`
(so an `agree`-with-empty-evidence verdict surfaces here as `disagree`).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `verdict` | string (`"agree"` \| `"disagree"`) | yes | Final discriminator after CF-04 forced-disagree override. |
| `evidence_chunk_count` | integer | yes | Length of `VerifierVerdict.evidence_chunk_ids` — count of evidence chunks the verifier cited. |
| `latency_ms` | integer | yes | Wall-clock ms for `Verifier.verify()` (the LLM call + parse). |

Example payload:

```json
{"verdict": "agree", "evidence_chunk_count": 3, "latency_ms": 412}
```

### verifier.disagreement

Emitted on three distinct paths (D-08):

- `reason="peers_diverge"` — verifier returned `disagree` AND cited at
  least one evidence chunk; the disagreement reflects honest divergence.
- `reason="forced_no_evidence"` — verifier originally returned `agree`
  but cited zero evidence chunks; CF-04 forced the verdict to `disagree`
  inside `Verifier.verify()`.
- `reason="verifier_failed"` — `Verifier.verify()` raised
  `BaseException`; the pipeline catches per D-06, logs the full traceback
  server-side, emits this event with `error_type` populated, and falls
  through to the standard consensus path so the user still gets an answer.

The `reason` field is a closed Literal set in v1.5; adding a fourth value
requires a Literal bump in `utils/models.py` AND an update to this doc.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `reason` | string (`"peers_diverge"` \| `"forced_no_evidence"` \| `"verifier_failed"`) | yes | Closed Literal set (D-08). |
| `summary` | string | yes | First 200 characters of `VerifierVerdict.reasoning` (or `str(exc)[:200]` on the `verifier_failed` path). Full text logged server-side only — mirrors `tool.span.error.error_message` truncation contract. |
| `evidence_chunk_ids` | array of string | yes | Chunk IDs the verifier cited; empty list on the `forced_no_evidence` and `verifier_failed` paths. |
| `peer_count` | integer | yes | Number of successful peer sub-agents (matches the prior `verifier.start.peer_count`). |
| `error_type` | string | no | Populated ONLY when `reason="verifier_failed"`; carries `type(exc).__name__` (e.g. `"RuntimeError"`, `"TimeoutError"`). `null` on the other two paths. |

Example payload (peers_diverge):

```json
{"reason": "peers_diverge", "summary": "Peer 0 cites chunk c1 saying X; peer 1 cites chunk c2 saying not-X.",
 "evidence_chunk_ids": ["c1", "c2"], "peer_count": 3, "error_type": null}
```

Example payload (forced_no_evidence):

```json
{"reason": "forced_no_evidence", "summary": "Verifier emitted agree with empty evidence_chunk_ids; CF-04 override.",
 "evidence_chunk_ids": [], "peer_count": 3, "error_type": null}
```

Example payload (verifier_failed):

```json
{"reason": "verifier_failed", "summary": "anthropic.APIError: connection timed out after 30s",
 "evidence_chunk_ids": [], "peer_count": 3, "error_type": "APIError"}
```

```

After inserting the above content (between the end of `### synthesizer.final` at ~line 373 and the start of `### Consuming the Stream` at line 375), make a SECOND focused edit: extend the existing JavaScript example in the `### Consuming the Stream` section. Insert 3 new lines (BEFORE the `synthesizer.final` line, since synthesizer.final closes the stream):

```javascript
es.addEventListener('verifier.start',         (e) => { const v = J(e); console.log(`verifier start: peer_count=${v.peer_count} model=${v.model}`); });
es.addEventListener('verifier.complete',      (e) => { const v = J(e); console.log(`verifier complete: ${v.verdict} (${v.evidence_chunk_count} chunks, ${v.latency_ms}ms)`); });
es.addEventListener('verifier.disagreement',  (e) => { const v = J(e); console.warn(`verifier disagreement: ${v.reason}${v.error_type ? ' [' + v.error_type + ']' : ''} — ${v.summary}`); });
```

Insert these 3 lines between the existing `executor.parallel` line and the existing `synthesizer.final` line in the JS code block (currently lines 385-386 in `docs/agent-architecture.md`).

After both edits, validate that the inserted content matches the Pydantic schemas. Run a structural drift check (see `<verify>` block). The doc must mention every Pydantic field name verbatim; the closed Literal sets (`reason`, `verdict`) must list all values.

Constraints:
- Do NOT modify ANY other section of `docs/agent-architecture.md` — preserve the preamble (lines 245-268), the existing 6 event subsections (lines 270-373), and the prose AFTER the JS example (lines 388+).
- Do NOT change the existing 6 `addEventListener` lines in the JS example — only INSERT 3 new lines between `executor.parallel` and `synthesizer.final`.
- Do NOT touch the `event_type` discriminator note at lines 260-263 (it already covers all subclasses including the 3 new ones via "concrete AgentEvent subclasses").
- Do NOT add a NEW common-fields table for the 3 new events — they inherit `trace_id` / `seq` / `ts_ms` from `AgentEvent` (covered at lines 254-258); the per-event tables list event-specific fields ONLY (matches the existing 6-event convention).
- Do NOT update line 251 ("`AgentEvent` base + 6 concrete subclasses") to "9 concrete subclasses" — actually, DO update it: change "6 concrete subclasses" → "9 concrete subclasses (3 verifier subclasses are debate-mode-only)" so the preamble stays accurate. This is a 1-line edit at line 251.
- Do NOT introduce English/Chinese mixing in the prose — the existing doc is English-only; keep that.
- The Pitfall P-08 language note (Chinese banner for English queries on the disagree path) is OUT of scope for this doc — it's an answer-text concern, not an event-schema concern; do not mention it in the event-schema section.
- Do NOT add a code-quoted reference to `_DISAGREE_BANNER_TEMPLATE` (Plan 04 internal); doc is the wire contract, not the implementation surface.
- Do NOT add a TOC entry update — the existing doc has no top-level TOC; the headings are self-navigating.
- Use exactly the heading style `### verifier.start` / `### verifier.complete` / `### verifier.disagreement` / `### Debate Mode` (mirrors the existing `### tool.span.*` heading style at lines 292/309/329; H3-level under the H2 `## Event Schema Reference`).
- Each Example payload JSON block must be valid JSON (no trailing comments, no trailing commas) so that a downstream `jq .` validation would parse it.
- The backward-compat note uses Markdown blockquote syntax `> ` (mirrors the existing redaction-policy note at line 265 which uses bold inline; the blockquote treatment is intentional here for visual separation since this note is the contract-anchor for AGENT-15 acceptance — not a regression to be subtle about).
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/project_pytorch/project/rag_enterprise && \
      grep -c "^### verifier.start$" docs/agent-architecture.md && \
      grep -c "^### verifier.complete$" docs/agent-architecture.md && \
      grep -c "^### verifier.disagreement$" docs/agent-architecture.md && \
      grep -c "^### Debate Mode$" docs/agent-architecture.md && \
      grep -c "peers_diverge.*forced_no_evidence.*verifier_failed\|forced_no_evidence.*peers_diverge\|verifier_failed.*peers_diverge" docs/agent-architecture.md && \
      grep -c "es.addEventListener('verifier" docs/agent-architecture.md && \
      grep -c "9 concrete subclasses\|three verifier subclasses\|3 verifier subclasses" docs/agent-architecture.md && \
      grep -c "max(peer_latency) + verifier_latency\|max(peer).*verifier" docs/agent-architecture.md && \
      python -c "
import re
doc = open('docs/agent-architecture.md').read()
models = open('utils/models.py').read()
# Drift check: every Pydantic field on the 3 verifier events must appear in the doc.
fields = {
    'VerifierStartEvent':        ['peer_count', 'model'],
    'VerifierCompleteEvent':     ['verdict', 'evidence_chunk_count', 'latency_ms'],
    'VerifierDisagreementEvent': ['reason', 'summary', 'evidence_chunk_ids', 'peer_count', 'error_type'],
}
missing = []
for cls, fs in fields.items():
    for f in fs:
        # field name must appear in the doc somewhere in the new sections
        if f not in doc:
            missing.append(f'{cls}.{f}')
assert not missing, f'doc missing fields: {missing}'
# Closed Literal sets:
assert all(v in doc for v in ['peers_diverge', 'forced_no_evidence', 'verifier_failed']), 'reason Literal incomplete'
assert '\"agree\"' in doc and '\"disagree\"' in doc, 'verdict Literal missing'
# Backward-compat blockquote:
assert '> **Backward compatibility:**' in doc, 'backward-compat note missing'
print('DOC-CODE-PARITY: OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### verifier.start$" docs/agent-architecture.md` returns `1`.
    - `grep -c "^### verifier.complete$" docs/agent-architecture.md` returns `1`.
    - `grep -c "^### verifier.disagreement$" docs/agent-architecture.md` returns `1`.
    - `grep -c "^### Debate Mode$" docs/agent-architecture.md` returns `1`.
    - The 3 new headings appear AFTER `### synthesizer.final` and BEFORE `### Consuming the Stream` — verified by `grep -nE "^### " docs/agent-architecture.md` showing the headings in this order: planner.plan, tool.span.start, tool.span.end, tool.span.error, executor.parallel, synthesizer.final, Debate Mode, verifier.start, verifier.complete, verifier.disagreement, Consuming the Stream.
    - `grep -c "es.addEventListener('verifier" docs/agent-architecture.md` returns `3` (one for each new event type in the JS example).
    - The 3 new `addEventListener` lines appear in the JS example BEFORE the `synthesizer.final` line — verified by `grep -nE "addEventListener\\('(verifier|synthesizer)" docs/agent-architecture.md` ordering.
    - All 3 `reason` Literal values present: `peers_diverge`, `forced_no_evidence`, `verifier_failed` (`grep -c` for each returns ≥ 1).
    - Backward-compat blockquote present: `grep -c "> \\*\\*Backward compatibility:\\*\\*" docs/agent-architecture.md` returns `1`.
    - Latency contract documented: `grep -c "max(peer.*+.*verifier\\|max(peer_latency) + verifier_latency" docs/agent-architecture.md` returns ≥ 1.
    - D-10 documented (debate requires swarm_mode): `grep -c "swarm_mode=True\\|requires.*swarm" docs/agent-architecture.md | head -1` returns ≥ 1 in the new section.
    - CF-07 terminal-event invariant restated in the new section: `grep -c "synthesizer.final remains the terminal" docs/agent-architecture.md` returns ≥ 1.
    - The Python doc-vs-code drift check in the `<verify>` block prints `DOC-CODE-PARITY: OK` (every Pydantic field name on the 3 new events appears in the doc).
    - `wc -l docs/agent-architecture.md` shows growth of 80-120 lines vs pre-edit baseline (per the action's expected scope).
    - All inserted JSON example blocks parse as valid JSON: `python -c "import json, re; doc = open('docs/agent-architecture.md').read(); blocks = re.findall(r'\`\`\`json\\n(.*?)\\n\`\`\`', doc, re.DOTALL); [json.loads(b) for b in blocks]; print(f'parsed {len(blocks)} JSON blocks OK')"` exits 0 (NOTE: pre-existing JSON blocks must also still parse — this is a regression check that the insertions did not corrupt prior blocks).
    - The preamble line at docs/agent-architecture.md:251 reads "9 concrete subclasses" (or equivalent phrasing acknowledging the 3 new subclasses) — verified by `grep -c "9 concrete subclasses\\|three verifier subclasses\\|3 verifier subclasses\\|six concrete subclasses + three verifier" docs/agent-architecture.md` returns ≥ 1.
    - `git diff --stat docs/agent-architecture.md` shows ONLY additions and the 1-line preamble edit (no surprise deletions).
    - No other file in the repo is touched: `git status --short` shows ONLY `docs/agent-architecture.md` as modified.
  </acceptance_criteria>
  <done>~80-100 LOC inserted in `docs/agent-architecture.md` (Debate Mode subsection + 3 event subsections + backward-compat blockquote + 3 new addEventListener lines in the JS example + 1-line preamble update at line 251); doc-vs-code drift check passes; commit message: `docs(21-06): document verifier.{start,complete,disagreement} events + Debate Mode (SC4 / AGENT-15)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documented event schema → downstream consumer (frontend EventSource handler, ops dashboard) | the doc IS the wire contract; consumers code against the documented schema, not against `utils/models.py` directly |
| Documented Literal sets (`reason`, `verdict`) → consumer enum-like switches | a missing Literal value in the doc would lead consumers to skip the unhandled branch silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-22 | I (Information disclosure) | The example payloads contain plausible PII (peer answers, evidence) — risk of consumer copy-pasting an example into a customer-facing demo | accept | All example payloads use synthetic strings (`"Peer 0 cites chunk c1 saying X..."`); no real PII. The redaction-policy note at lines 265-268 already covers the broader `summary` truncation contract |
| T-21-23 | T (Tampering) | Doc drifts from `utils/models.py` schema over time — a future field rename in models leaves the doc lying | mitigate | Task 1 verify command runs a Python doc-vs-code parity check on every commit; CI can wire the same check into a doc-drift gate (out of scope for this task; suggested for Phase 22 docs-drift CI lift) |
| T-21-24 | R (Repudiation) | Consumers integrate against the documented `reason="verifier_failed"` path but a future Literal expansion (e.g. `"verifier_timeout"`) lands without doc update | mitigate | The action explicitly documents that `reason` is a closed Literal set in v1.5 and that a fourth value requires a Literal bump in code AND a doc update — the contract is bilateral |
| T-21-25 | I | The verifier-failed example payload echoes a realistic provider exception name (`anthropic.APIError`) — could mislead consumers into specific exception handling | accept | The example is illustrative; consumers should switch on `error_type` value (string), not parse it as a class reference. The doc text states `type(exc).__name__` so consumers understand it's a runtime string, not a Python type |
</threat_model>

<verification>
- All `grep -c` checks in `<acceptance_criteria>` return their expected counts.
- Python drift check prints `DOC-CODE-PARITY: OK` (every field on the 3 new events appears in the doc).
- Python JSON-parse check parses ALL fenced ```json blocks in the doc without error.
- `wc -l docs/agent-architecture.md` shows +80-120 lines vs baseline.
- `git diff --stat docs/agent-architecture.md` confirms only additions and the 1-line preamble edit; no surprise deletions.
- `git status --short` shows ONLY `docs/agent-architecture.md` as modified.
- No code change: `pytest -x` exits 0 on the full unit suite (this plan touches no code).
</verification>

<success_criteria>
- AGENT-15 (SC4) contribution: docs/agent-architecture.md Event Schema Reference extended with 3 new subsections + example payloads + backward-compat note as required by the AGENT-15 acceptance text.
- Debate Mode subsection ties the doc to the wire contract: opt-in (D-10), latency (CF-06), terminal-event invariant (CF-07), and the wire route (existing `/api/v1/agent/v1/run/stream` via `SwarmQueryPipeline.run_streaming` from Plan 05).
- Backward-compat blockquote makes the additivity contract explicit and machine-greppable for future Phase 22 doc-drift CI.
- JS consumer example extended with 3 new addEventListener lines so frontend integrators have a copy-paste starting point that mirrors the existing 6-event pattern.
- Doc-vs-code drift gate (Task 1's Python verify) catches any future field rename in `utils/models.py` that breaks the doc; runs in seconds and is suitable for CI promotion (Phase 22 docs-drift lift).
- Per CONTEXT specifics: the `reason` closed Literal set is restated in prose with the "adding a fourth requires a Literal bump and a doc update" contract — bilateral wire-contract anchor.
- Phase 21 SC4 acceptance: ROADMAP §"Phase 21" SC4 line satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/21-agent-05-multi-agent-debate-sub-agent-verifier/21-06-SUMMARY.md` documenting:
- The 5 distinct insertions to `docs/agent-architecture.md` (with line ranges in the post-edit file): (a) line 251 preamble update; (b) `### Debate Mode` subsection at line ~375; (c) `### verifier.start` subsection; (d) `### verifier.complete` subsection; (e) `### verifier.disagreement` subsection; (f) 3 new `addEventListener` lines in the JS example.
- The doc-vs-code parity check pass (`DOC-CODE-PARITY: OK` output).
- The before/after `wc -l` count for `docs/agent-architecture.md` (baseline 390 → expected ~470-490).
- Phase 21 closing notes: all 5 success criteria addressed across 6 plans (SC1: Plan 03; SC2: Plan 05 integration test; SC3: Plan 02 + Plan 05; SC4: Plan 06; SC5: Plan 05 byte-identity test). Phase 21 ready for `/gsd-verify-work 21`.
- Hand-off note for Phase 22 (TEST-08 coverage lift): the new lines in `services/pipeline.py` from Plan 05 are diff-covered ≥ 70% by Plan 05's tests; whole-file `pipeline.py` coverage lift is Phase 22's job per CONTEXT deferred section.
- Hand-off note for v1.6+ (per CONTEXT deferred-list): `proposed_answer_preview` on `verifier.complete` (kept off the wire in v1.5 to avoid PII echo / frame bloat); UI banner for divergence (text-only banner is the v1.5 surface per D-03); `Synthesizer` class extraction (D-04 keeps composition inline); per-call model selection (`Settings.verifier_model` activation — only `verifier_provider` is wired in v1.5 per Pitfall P-09 / Assumption A3); true streaming inside `SwarmQueryPipeline.run_streaming` (Plan 05 batch-emits events at end-of-run via `_run_with_state`; v1.6+ can refactor to interleaved emission if ops dashboards demand mid-run progress). NOTE: swarm-route dispatch was promoted from "deferred" to "shipped in Plan 05" via the BLOCKER 2 fix from plan-checker iteration 1; the route now dispatches by `req.swarm_mode` (`controllers/api.py:274`).
</output>
</content>
</invoke>