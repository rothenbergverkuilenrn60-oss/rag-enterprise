---
phase: 21-agent-05-multi-agent-debate-sub-agent-verifier
verified: 2026-05-10T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 21: AGENT-05 Multi-Agent Debate / Sub-Agent Verifier — Verification Report

**Phase Goal (verbatim):** Introduce a single-pass verifier sub-agent that runs after `SwarmQueryPipeline`'s `asyncio.gather` peer fan-out when `req.debate=True`. Verifier reads N peer answers + their cited evidence chunks and emits a structured `VerifierVerdict` (agree / disagree). On disagreement, the synthesizer composes a final response that surfaces the divergence and the evidence-supported answer. Three new SSE event types extend the v1.4 schema; `synthesizer.final` remains terminal. Latency stays bounded by `max(peer) + verifier`, not `sum`.
**Verified:** 2026-05-10
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth (SC)                                                                                                                                                                                                                                                       | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SC1 | `services/agent/verifier.py::Verifier` class implemented; `verify(peer_results, evidence, user_query) → VerifierVerdict` async; uses `BaseLLMClient.call_agentic_turn` text-only (`tools=[]`, CF-03); system prompt forbids inventing facts; agree+empty → forced disagree (CF-04). | ✓ VERIFIED | `services/agent/verifier.py:89 class Verifier`; `verify(...)` is `async def` returning `VerifierVerdict` (line 112). `await self._llm.call_agentic_turn(... tools=[], ... parallel_tool_calls=False)` at line 132-138. System prompt `_VERIFIER_SYSTEM` line 55-86 contains "不得编造 evidence 中未出现的事实" (rule 2) — forbids invention. CF-04 forced-disagree at line 147-148: `if verdict.verdict == "agree" and not verdict.evidence_chunk_ids: verdict = verdict.model_copy(update={"verdict": "disagree"})`. Tests `test_verify_calls_text_only_with_tools_empty`, `test_system_prompt_forbids_invention`, `test_verify_forced_disagree_on_empty_evidence` all PASS. |
| SC2 | `GenerationRequest.debate: bool = False` opt-in field added; `SwarmQueryPipeline.run()` appends verifier hop AFTER `asyncio.gather` when `debate=True`; existing swarm behavior unchanged when `debate=False`; integration test asserts `total ≤ max(peer) + verifier + small_overhead`, NOT sum, NOT N×verifier. | ✓ VERIFIED | `utils/models.py:216 debate: bool = False` field on `GenerationRequest` with comment `AGENT-14 — opt-in verifier hop after peer fan-out (CF-02)`. Cross-field validator `_check_debate_requires_swarm` lines 227-234 raises ValueError when `debate=True` and `swarm_mode=False` (D-10). `services/pipeline.py:1314 await asyncio.gather(*sub_coros, ...)`; verifier hop gated `if req.debate:` at line 1340; `verdict = await self._verifier.verify(...)` at line 1359. Integration test `test_swarm_debate_latency_bounded_by_max_peer_plus_verifier` (3 peers × 0.3s + verifier 0.2s) asserts `450 < elapsed_ms < 700` (max+verifier=500 vs sum=1100) — PASSES. |
| SC3 | Three new SSE event types (`VerifierStartEvent`, `VerifierCompleteEvent`, `VerifierDisagreementEvent`) as Pydantic V2 frozen subclasses of `AgentEvent`; events emit through existing `/api/v1/agent/v1/run/stream` route; wire format unchanged; `synthesizer.final` remains terminal. | ✓ VERIFIED | All 3 classes at `utils/models.py:672-709`, each declares `event_type: ClassVar[str]` and `model_config = ConfigDict(frozen=True)`, inherits `AgentEvent`. `controllers/api.py:280-284` ternary `pipeline = get_swarm_pipeline() if req.swarm_mode else get_agent_pipeline()` (BLOCKER 2 fix shipped). `_sse()` async loop at line 286-292 emits all `pipeline.run_streaming(req)` events through `event: <type>\ndata: <json>\n\n` format unchanged. `services/pipeline.py:1463-1469` always emits terminal `SynthesizerFinalEvent`. Tests `test_swarm_debate_events_reach_route` and `test_synthesizer_final_terminal_in_all_debate_paths` PASS. |
| SC4 | `docs/agent-architecture.md` Event Schema Reference extended with three new subsections + example payloads; backward-compat note documents that debate-mode events are additive and non-debate flows unchanged. | ✓ VERIFIED | `### verifier.start` line 433, `### verifier.complete` line 452, `### verifier.disagreement` line 473. Backward-compat blockquote line 428 `> Non-debate flows (debate=False) emit the v1.4 event set unchanged.` Each subsection includes JSON example payloads (per 21-06-SUMMARY DOC-CODE-PARITY check; all 9 fenced JSON blocks parse). Preamble updated line 251-252 from "6 concrete subclasses" to "9 concrete subclasses; 3 verifier subclasses are debate-mode-only — see `### Debate Mode` below". |
| SC5 | v1.3 invariants intact: PostgreSQL RLS isolates tenants; audit log records verifier sub-agent calls with same fields as v1.3 swarm; combined coverage stays ≥ 70%; no production code changes when `debate=False`. | ✓ VERIFIED | Audit metadata namespace `agent_05` populated at `services/pipeline.py:1342-1346` (verifier_used / verifier_failed / forced_disagree / verifier_latency_ms / verifier_model / evidence_chunk_count); appended to audit detail at line 1454 ONLY when `req.debate` (`**({"agent_05": audit_agent_05} if req.debate else {})`). Test `test_debate_false_byte_identical_to_v13_swarm` (line 568) asserts: verifier never invoked, `_llm.chat.await_count == 2` (decompose+synth, no verifier hop), no `agent_05` key in audit detail — PASSES. RLS inherited (no Phase 21 production-code changes to `services/retriever/` or PG layer). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                        | Expected                                                                                                  | Status     | Details                                                                                                                                                                                                                                                                            |
| ----------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `services/agent/verifier.py`                    | NEW — `Verifier` class with `verify()` text-only, CF-04 forced-disagree, P-09 provider-override, defensive chunk_id filter | ✓ VERIFIED | 198 lines; class lines 89-197; `_VERIFIER_SYSTEM` prompt (55-86); `verify()` (112-149) text-only `tools=[]`; `_resolve_llm()` (100-110) P-09 branch for openai/anthropic; `_parse()` (169-197) defensive filter drops chunk_ids not in supplied evidence (190-196). 16 unit tests PASS. |
| `services/pipeline.py` (SwarmQueryPipeline)     | EXTEND — `_synthesize` accepts `verifier_verdict` kwarg; `_run_with_state` runs verifier hop, audit, events; `run_streaming` yields all events | ✓ VERIFIED | `_synthesize` signature line 1181-1187 takes `verifier_verdict: VerifierVerdict | None = None`; D-04 short-circuit `_format_disagree` at line 1206-1217. `_run_with_state` (1268-1480) owns the verifier hop in locals (W5/W8 fix verified — 0 instance buffers). `run_streaming` (1534-1570) yields events. Verifier instantiated once per pipeline at `__init__` (line 1040 `self._verifier = Verifier()`). |
| `utils/models.py`                               | EXTEND — `VerifierVerdict` + 3 events + `GenerationRequest.debate` + D-10 validator                       | ✓ VERIFIED | `VerifierVerdict` (654-669) frozen, all 5 D-01 fields. `VerifierStartEvent` (672-678), `VerifierCompleteEvent` (681-692), `VerifierDisagreementEvent` (695-709) all frozen with `ClassVar[str] event_type`. `debate: bool = False` (216) + `_check_debate_requires_swarm` validator (227-234). |
| `controllers/api.py`                            | EXTEND — route dispatches to swarm pipeline when `req.swarm_mode=True` (BLOCKER 2)                        | ✓ VERIFIED | Line 280-284 ternary `pipeline = get_swarm_pipeline() if req.swarm_mode else get_agent_pipeline()`. `pipeline.run_streaming(req)` consumed at line 288. SSE wire format unchanged at line 292.                                                                                       |
| `config/settings.py`                            | EXTEND — `verifier_model: str | None = None` + `verifier_provider: Literal["openai","anthropic"] | None = None` (D-05) | ✓ VERIFIED | Lines 289-294 with explanatory comment that `verifier_provider` overrides peer provider; `verifier_model` reserved (per-call override not wired in v1.5 per RESEARCH §A3). Test `test_verifier_settings_default_none` PASSES.                                                          |
| `docs/agent-architecture.md`                    | EXTEND — `### Debate Mode` + 3 event subsections + backward-compat blockquote (SC4)                       | ✓ VERIFIED | `### Debate Mode` block 378-431; `### verifier.start` 433; `### verifier.complete` 452; `### verifier.disagreement` 473. Backward-compat blockquote line 428. All 9 fenced JSON blocks parse (per 21-06-SUMMARY DOC-CODE-PARITY claim).                                                |
| `tests/unit/test_verifier.py`                   | NEW — 16 tests covering happy/forced/parse/raise/latency/text-only/single-call/provider-override          | ✓ VERIFIED | 16 tests; ALL PASS. Covers B-01..B-09 of 21-VALIDATION branch enumeration.                                                                                                                                                                                                          |
| `tests/unit/test_phase21_models.py`             | NEW — 13 tests covering VerifierVerdict construct/literal/frozen/copy + 3 event shapes + 4 GenerationRequest debate validator scenarios | ✓ VERIFIED | 13 tests; ALL PASS. Covers B-30 + B-31.                                                                                                                                                                                                                                              |
| `tests/unit/test_swarm_pipeline.py`             | EXTEND — debate hop tests (byte-identity / agree / disagree / forced / failed / audit superset / dedup / terminal-in-all-paths) | ✓ VERIFIED | 21 tests in file (8 pre-Phase-21 + 13 new Phase-21); ALL PASS. Covers B-16..B-22.                                                                                                                                                                                                    |
| `tests/unit/test_agent_stream_route.py`         | EXTEND — `test_swarm_debate_events_reach_route` (SC3 wire-truth)                                          | ✓ VERIFIED | Test at line 254-330; PASSES. Asserts 3 verifier event types reach SSE wire when `swarm_mode=True + debate=True`.                                                                                                                                                                  |
| `tests/integration/test_swarm_debate_e2e.py`    | NEW — SC2/CF-06 latency assertion (3 peers × 0.3s + verifier 0.2s → 450<elapsed<700)                      | ✓ VERIFIED | 1 integration test (line 89-152); PASSES (`pytest -m integration`). Mock-driven, NO `OPENAI_API_KEY` required.                                                                                                                                                                       |
| `tests/unit/test_settings.py`                   | NEW — assert `verifier_model is None` and `verifier_provider is None`                                     | ✓ VERIFIED | 1 test (line 10-20); PASSES.                                                                                                                                                                                                                                                         |

### Key Link Verification

| From                                                    | To                                                                                  | Via                                                                                              | Status     | Details                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `controllers/api.py:280` route `/agent/v1/run/stream`   | `SwarmQueryPipeline.run_streaming`                                                  | Ternary `pipeline = get_swarm_pipeline() if req.swarm_mode else get_agent_pipeline()` then `pipeline.run_streaming(req)` | ✓ WIRED    | `test_swarm_debate_events_reach_route` (test_agent_stream_route.py:254) verifies the 3 verifier event types + terminal SynthesizerFinalEvent reach the SSE response body when `req.swarm_mode=True + req.debate=True`. PASSES.                                                          |
| `SwarmQueryPipeline._run_with_state`                    | `Verifier.verify`                                                                   | `await self._verifier.verify(peer_results=successful, evidence=deduped_evidence, user_query=req.query)` (line 1359-1363) | ✓ WIRED    | Wired only when `req.debate` (line 1340). Single call (no N×). Uses dedup-before-verifier (line 1348). `_run_sub_agent` peer results filtered by `not isinstance(res, BaseException)` (line 1349) per CF-09.                                                                          |
| `SwarmQueryPipeline._synthesize`                        | `Verifier.proposed_answer` (via `verifier_verdict.proposed_answer`)                | D-04 short-circuit `if verifier_verdict is not None and verifier_verdict.verdict == "disagree": return self._format_disagree(verifier_verdict, sub_results)` (line 1206-1217) | ✓ WIRED    | Zero LLM calls on disagree path — `verifier.proposed_answer` IS user-visible answer. `test_synthesize_disagree_uses_proposed_answer_no_llm` (test_swarm_pipeline.py:441) PASSES.                                                                                                       |
| Verifier hop                                            | Audit log                                                                           | `audit_agent_05` dict appended to `AuditEvent.detail` ONLY when `req.debate` (line 1454)         | ✓ WIRED    | `test_debate_true_audit_detail_superset_with_agent_05` (test_swarm_pipeline.py:743) PASSES; `test_debate_false_byte_identical_to_v13_swarm` confirms NO `agent_05` key when debate=False.                                                                                              |
| `Verifier.__init__`                                     | `BaseLLMClient` instance                                                            | `_resolve_llm()` returns `AnthropicLLMClient()` / `OpenAILLMClient()` / `get_llm_client()` based on `settings.verifier_provider` | ✓ WIRED    | P-09 bypass of `get_llm_client()` singleton when verifier_provider is set. `test_resolve_llm_anthropic_branch` and `test_resolve_llm_openai_branch` (test_verifier.py:316, 329) PASS.                                                                                                  |
| `GenerationRequest`                                     | Cross-field validation                                                              | `@model_validator(mode="after") _check_debate_requires_swarm` at line 227-234                    | ✓ WIRED    | Raises `ValueError("debate=True requires swarm_mode=True...")`. `test_debate_requires_swarm_mode` (test_phase21_models.py:196) PASSES.                                                                                                                                                |
| 3 verifier events                                       | SSE wire serialization                                                              | Existing `_sse()` loop + `model_dump_json()` + `event_type` ClassVar (controllers/api.py:292)   | ✓ WIRED    | No new serializer; events flow through existing `event: <type>\ndata: <json>\n\n` format. ClassVar excluded from `model_dump()` per Pydantic V2 default. `test_verifier_start_event_classvar_excluded_from_json` PASSES.                                                                |

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable           | Source                                                                                                  | Produces Real Data | Status      |
| --------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------- | ------------------ | ----------- |
| `Verifier.verify` returns         | `VerifierVerdict`       | `self._llm.call_agentic_turn(...)` → JSON parsed via `_parse()` → defensive chunk-id filter → CF-04 override | YES                | ✓ FLOWING   |
| `_run_with_state` `verdict` local | `VerifierVerdict | None` | `await self._verifier.verify(peer_results=successful, evidence=deduped_evidence, user_query=req.query)` (1359) | YES                | ✓ FLOWING   |
| `_synthesize` final answer         | `final_answer: str`     | Either `_format_disagree(verdict, ...)` (D-04) OR `await self._llm.chat(...)` (consensus) — chosen by verdict | YES                | ✓ FLOWING   |
| `_run_with_state` events list     | `events: list[AgentEvent]` | `events.append(VerifierStartEvent(...))` (1351), `VerifierCompleteEvent(...)` (1402), conditional `VerifierDisagreementEvent(...)` (1367 / 1393), terminal `SynthesizerFinalEvent(...)` (1463) | YES                | ✓ FLOWING   |
| Audit `detail['agent_05']`        | `audit_agent_05: dict`  | Populated inside `if req.debate:` block; merged into `AuditEvent.detail` only when `req.debate`         | YES (gated)        | ✓ FLOWING   |
| Route SSE stream                  | `evt.model_dump_json()` | `pipeline.run_streaming(req)` (controllers/api.py:288) — for swarm pipeline yields verifier events     | YES                | ✓ FLOWING   |

### Behavioral Spot-Checks

| Behavior                                                                | Command                                                                                              | Result                | Status |
| ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------- | ------ |
| All 16 verifier unit tests PASS                                          | `uv run pytest tests/unit/test_verifier.py -q`                                                        | 16 passed             | ✓ PASS |
| 13 phase 21 model tests PASS (VerifierVerdict + 3 events + debate validator) | `uv run pytest tests/unit/test_phase21_models.py -q`                                                  | 13 passed             | ✓ PASS |
| 21 swarm pipeline tests PASS (8 pre + 13 phase-21 debate)                 | `uv run pytest tests/unit/test_swarm_pipeline.py -q`                                                  | 21 passed             | ✓ PASS |
| 8 stream route tests PASS (incl. SC3 wire-truth `test_swarm_debate_events_reach_route`) | `uv run pytest tests/unit/test_agent_stream_route.py -q`                                              | 8 passed              | ✓ PASS |
| settings smoke test PASS                                                  | `uv run pytest tests/unit/test_settings.py -q`                                                        | 1 passed              | ✓ PASS |
| SC2/CF-06 latency contract integration test PASS                          | `uv run pytest tests/integration/test_swarm_debate_e2e.py -m integration -q`                          | 1 passed              | ✓ PASS |
| Combined Phase 21 sweep                                                   | All six files via `pytest ... -m "not integration or integration"`                                    | 66 passed             | ✓ PASS |
| ruff clean across modified production files                                | `uv run ruff check services/agent/verifier.py services/pipeline.py utils/models.py controllers/api.py config/settings.py` | All checks passed!    | ✓ PASS |
| W5/W8 fix — no per-call instance buffers on `SwarmQueryPipeline`          | `grep -cE "self\._last_(verifier_events\|response\|trace_id)" services/pipeline.py`                  | 0                     | ✓ PASS |
| BLOCKER 2 fix — route ternary dispatches to swarm pipeline                | `grep -nE "pipeline.*get_swarm_pipeline\(\) if req.swarm_mode" controllers/api.py`                   | line 280-284 present  | ✓ PASS |
| BLOCKER 3 fix — verifier.py uses TYPE_CHECKING guard for `_SubAgentResult` (no top-level runtime import) | `grep -cE "^from services.pipeline import" services/agent/verifier.py`                                | 0                     | ✓ PASS |
| Doc subsections present (SC4)                                             | `grep -nE "^### verifier\." docs/agent-architecture.md`                                              | 3 subsections (433/452/473) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s)                              | Description                                                                                          | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                                       |
| ----------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AGENT-05    | 21-01 (settings) + 21-02 (models) + 21-03 (Verifier) + 21-04 (synth) + 21-05 (pipeline+route+audit) | Verifier sub-agent + system prompt forbids invention + CF-04 forced-disagree + text-only `call_agentic_turn` | ✓ SATISFIED | Maps to SC1. `services/agent/verifier.py::Verifier` (line 89), `_VERIFIER_SYSTEM` prompt rule 2 ("不得编造"), CF-04 override at line 147-148, text-only `tools=[]` at line 134. 16 unit tests PASS.                                                                                                                                                                              |
| AGENT-14    | 21-02 (debate field + validator) + 21-04 (synth divergence) + 21-05 (pipeline hop)              | `req.debate` opt-in + verifier hop after `asyncio.gather` + latency contract + `debate=False` byte-identical | ✓ SATISFIED | Maps to SC2 + SC5. `utils/models.py:216` debate field; D-10 validator (227-234); `services/pipeline.py:1340 if req.debate:` gates verifier hop; integration test asserts `450 < elapsed_ms < 700` (max+verifier vs sum); `test_debate_false_byte_identical_to_v13_swarm` PASSES.                                                                                              |
| AGENT-15    | 21-02 (3 event schemas) + 21-05 (wire emission + route dispatch) + 21-06 (doc extension)        | 3 SSE event subclasses, emit through existing route, terminal `synthesizer.final`, doc extension     | ✓ SATISFIED | Maps to SC3 + SC4. `utils/models.py:672-709` 3 frozen Pydantic V2 subclasses; `controllers/api.py:280` route ternary; `services/pipeline.py:1463` always emits `SynthesizerFinalEvent` last; `docs/agent-architecture.md:433/452/473` 3 new subsections. `test_swarm_debate_events_reach_route` + `test_synthesizer_final_terminal_in_all_debate_paths` PASS. |

**Coverage:** 3/3 AGENT-05/14/15 requirements SATISFIED. No orphaned requirements (REQUIREMENTS.md:85-87 lists exactly these 3 for Phase 21; all 3 mapped to plans 21-01..21-06).

### CONTEXT Decisions Fidelity (D-01..D-11 + CF-01..CF-10)

| ID    | Decision                                                                                                       | Status     | Evidence                                                                                                                                                                                                                                                              |
| ----- | -------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01  | `VerifierVerdict` frozen Pydantic V2 with 5 fields                                                              | ✓ HONORED  | `utils/models.py:654-669` exactly 5 fields: `verdict / evidence_chunk_ids / reasoning / proposed_answer / latency_ms`; `model_config = ConfigDict(frozen=True)`.                                                                                                       |
| D-02  | `proposed_answer` always populated (str, both verdicts)                                                          | ✓ HONORED  | Field is `str` (not `str | None`) at line 668. Test `test_verify_proposed_answer_always_populated` PASSES.                                                                                                                                                            |
| D-03  | Synthesizer divergence note (Chinese banner template)                                                           | ✓ HONORED  | `_DISAGREE_BANNER_TEMPLATE` formatted at `services/pipeline.py:1261-1265`. `_format_disagree` returns `f"{verdict.proposed_answer}\n\n{banner}"` (line 1266). Test `test_format_disagree_exact_template_substitution` PASSES.                                       |
| D-04  | Composition lives in `_synthesize` with optional `verifier_verdict` kwarg; no new `Synthesizer` class            | ✓ HONORED  | `_synthesize` signature line 1186 `verifier_verdict: VerifierVerdict | None = None`. `_format_disagree(verdict, sub_results)` private static helper line 1243-1266.                                                                                                  |
| D-05  | Settings.verifier_model + verifier_provider; default = peer model                                                | ✓ HONORED  | `config/settings.py:293-294` both default to None; `Verifier._resolve_llm()` (line 100-110) reuses `get_llm_client()` when both None.                                                                                                                                  |
| D-06  | Degrade-with-signal on Verifier failure: log + emit `verifier_failed` event + audit metadata + verdict=None     | ✓ HONORED  | `services/pipeline.py:1364-1378` catches `BaseException` (NOT Exception, per ERR-01), logs traceback, sets `audit_agent_05["verifier_failed"] = True`, emits `VerifierDisagreementEvent(reason="verifier_failed", error_type=...)`. Verdict stays None → `_synthesize` falls through to consensus path. |
| D-07  | No additional tenacity wrapper at Verifier class level                                                           | ✓ HONORED  | `services/agent/verifier.py:14-16` docstring explicitly notes "NO tenacity wrapper at this class level". No `@retry` decorator.                                                                                                                                       |
| D-08  | `VerifierDisagreementEvent` wire fields: reason / summary / evidence_chunk_ids / peer_count / error_type         | ✓ HONORED  | `utils/models.py:695-709` exact field list, `Literal["peers_diverge","forced_no_evidence","verifier_failed"]`. `error_type: str | None = None`.                                                                                                                       |
| D-09  | `VerifierStartEvent` (peer_count, model) and `VerifierCompleteEvent` (verdict, evidence_chunk_count, latency_ms) | ✓ HONORED  | `utils/models.py:672-678` and 681-692. NO `proposed_answer_preview` field per D-09 (kept off the wire for PII).                                                                                                                                                       |
| D-10  | 422 at Pydantic boundary — debate=True + swarm_mode=False → ValueError                                          | ✓ HONORED  | `utils/models.py:227-234` `_check_debate_requires_swarm` validator. Test `test_debate_requires_swarm_mode` PASSES.                                                                                                                                                    |
| D-11  | Forced-disagree two-channel signal: `VerifierDisagreementEvent(reason="forced_no_evidence")` + `forced_disagree=true` audit metadata | ✓ HONORED  | `services/pipeline.py:1387-1392` discriminator: `reason = "forced_no_evidence" if not verdict.evidence_chunk_ids else "peers_diverge"`; `audit_agent_05["forced_disagree"] = (reason == "forced_no_evidence")`. Test `test_debate_true_forced_disagree_emits_forced_no_evidence` PASSES. |
| CF-01 | Single sub-agent verifier role (STATE Q#3)                                                                       | ✓ HONORED  | One Verifier class; one verify() call per request (test_verify_makes_single_llm_call PASSES).                                                                                                                                                                          |
| CF-02 | Opt-in `req.debate=True` (STATE Q#4)                                                                             | ✓ HONORED  | `debate: bool = False` default; verifier hop gated `if req.debate:`.                                                                                                                                                                                                  |
| CF-03 | Text-only `call_agentic_turn` (no tools) — SC1                                                                  | ✓ HONORED  | `services/agent/verifier.py:134 tools=[]`. Test `test_verify_calls_text_only_with_tools_empty` PASSES.                                                                                                                                                                |
| CF-04 | Forced-disagree on agree+empty                                                                                   | ✓ HONORED  | `services/agent/verifier.py:147-148` override INSIDE Verifier (Pitfall P-02). Test `test_verify_forced_disagree_on_empty_evidence` PASSES.                                                                                                                            |
| CF-05 | 3 frozen Pydantic V2 event subclasses                                                                            | ✓ HONORED  | All 3 declare `model_config = ConfigDict(frozen=True)` and inherit AgentEvent. Test `test_verifier_start_event_shape` etc. PASS.                                                                                                                                       |
| CF-06 | Latency `total ≤ max(peer) + verifier + small_overhead`                                                         | ✓ HONORED  | Integration test asserts `450 < elapsed_ms < 700` (3 peers × 0.3s = 0.9s sum vs max=0.3 + verifier=0.2 = 0.5s).                                                                                                                                                       |
| CF-07 | `synthesizer.final` terminal in ALL paths                                                                        | ✓ HONORED  | `services/pipeline.py:1463-1469` always appends terminal `SynthesizerFinalEvent`. Test `test_synthesizer_final_terminal_in_all_debate_paths` PASSES.                                                                                                                  |
| CF-08 | `debate=False` no production code change                                                                          | ✓ HONORED  | `if req.debate:` gate at line 1340 + `**({"agent_05": ...} if req.debate else {})` at line 1454. Test `test_debate_false_byte_identical_to_v13_swarm` PASSES.                                                                                                          |
| CF-09 | `BaseException` (not `Exception`); explicit `parallel_tool_calls=False`                                          | ✓ HONORED  | `services/pipeline.py:1364 except BaseException as exc`; `services/agent/verifier.py:137 parallel_tool_calls=False` explicit (CF-09).                                                                                                                                |
| CF-10 | RLS isolates tenants; audit log fields parity; coverage ≥ 70%                                                    | ✓ HONORED  | No Phase 21 changes to retriever / PG / RLS layer — invariant inherited. Audit `AuditEvent.detail` reuses v1.3 `swarm_n / per_agent_*` fields + adds `agent_05` namespace ONLY when debate=True (D-11 metadata-only, no enum migration).                              |

### Nyquist B-01..B-32 Branch Coverage

All branches enumerated in 21-VALIDATION.md and 21-RESEARCH.md "Coverage Branch Enumeration" map to a passing test:

- **B-01..B-09 (Verifier internals):** test_verifier.py 16 tests covering happy/forced/honest-disagree/parses/raises/defensive-filter/latency/text-only/single-call/provider-overrides — all PASS.
- **B-16 (debate=False byte-identity):** `test_debate_false_byte_identical_to_v13_swarm` PASSES.
- **B-17 (verifier hop runs after gather):** `test_debate_true_happy_agree_emits_start_and_complete` PASSES (also covers single-verifier-call structural assertion).
- **B-18 (peers_diverge reason):** `test_debate_true_disagree_emits_peers_diverge` PASSES.
- **B-19 (forced_no_evidence reason):** `test_debate_true_forced_disagree_emits_forced_no_evidence` PASSES.
- **B-20 (verifier_failed reason — degrade path):** `test_debate_true_verifier_raises_emits_verifier_failed_and_degrades` PASSES.
- **B-21 (latency contract):** integration test `test_swarm_debate_latency_bounded_by_max_peer_plus_verifier` PASSES with `450 < elapsed_ms < 700`.
- **B-22 (audit superset):** `test_debate_true_audit_detail_superset_with_agent_05` PASSES.
- **B-30 (3 events frozen + SSE format):** test_phase21_models.py 6 event tests + test_swarm_debate_events_reach_route PASS.
- **B-31 (D-10 cross-field validator):** `test_debate_requires_swarm_mode` PASSES.

**Conclusion:** Nyquist condition satisfied — every branch sampled at rate strictly above its regression rate.

### Anti-Patterns Found

NONE. ruff clean across all 5 modified production files. No TODO/FIXME/PLACEHOLDER strings introduced; no bare `except`; no hardcoded empty data flowing to user-visible output (`audit_agent_05` dict initialized empty but populated in same block before use).

### Human Verification Required

NONE. All 5 ROADMAP success criteria, all 3 requirement IDs, all 11 CONTEXT D-* decisions, all 10 CF-* carry-forwards, and all enumerated Nyquist branches are programmatically verifiable AND verified PASSING in this report.

The integration test (`test_swarm_debate_e2e.py`) substitutes synthetic `asyncio.sleep` for real LLM provider latency (mock at consumer path), which is the validated v1.5 idiom for SC2 latency assertions per 21-VALIDATION.md (`# Wave-0 latency-contract idiom mirrored from tests/unit/test_agent_sse.py:247-250`). No real-LLM smoke needed for verification PASSED status — the latency contract is structural (asyncio.gather concurrency + sequential await on verifier), not a real-provider variance concern at this gate.

### Gaps Summary

NO GAPS. Phase 21 goal achieved.

- Verifier sub-agent shipped (`services/agent/verifier.py`, 198 LOC, 16 unit tests).
- Single-pass verifier hop wired in `SwarmQueryPipeline._run_with_state` after `asyncio.gather`, gated on `req.debate=True`.
- 3 SSE event types implemented as frozen Pydantic V2 subclasses, emit through existing `/api/v1/agent/v1/run/stream` route via the BLOCKER-2 ternary fix at `controllers/api.py:280`.
- D-04 divergence composition `_format_disagree` short-circuits LLM call on disagree path — verifier's `proposed_answer` is the user-visible answer per D-01/D-02.
- Latency contract `total ≤ max(peer) + verifier + small_overhead` proven by integration test.
- SC5 byte-identity for `debate=False` proven by `test_debate_false_byte_identical_to_v13_swarm` + audit-detail-key absence assertion.
- W5/W8 per-call state isolation verified (zero `self._last_*` instance buffers); BLOCKER-3 TYPE_CHECKING guard verified (zero runtime `from services.pipeline import` in verifier.py).
- Documentation (SC4) extends `docs/agent-architecture.md` with `### Debate Mode` block + 3 event subsections + JSON examples + locked backward-compat blockquote; DOC-CODE-PARITY check passes.

---

_Verified: 2026-05-10_
_Verifier: Claude (gsd-verifier)_
