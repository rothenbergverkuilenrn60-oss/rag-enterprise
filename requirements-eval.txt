# =============================================================================
# requirements-eval.txt
# RAGAS 评测专用依赖 — 独立文件，评测环境单独安装，不污染生产镜像
# 安装：pip install -r requirements-eval.txt
# =============================================================================

# ── RAGAS 评测框架 ─────────────────────────────────────────────────────────────
ragas==0.2.6                   # RAG 自动评测框架（faithfulness / answer_relevancy / context_precision）
datasets==3.2.0                # HuggingFace Datasets（构建评测数据集）

# ── LangChain（RAGAS 内部依赖）───────────────────────────────────────────────
langchain==0.3.14
langchain-community==0.3.14
langchain-openai==0.2.14

# ── LLM 评判模型客户端（judge model）────────────────────────────────────────
openai==1.59.6                 # GPT-4o 作为 RAGAS judge model
anthropic==0.43.0              # Claude 作为备选 judge model

# ── 数据科学 ──────────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1

# ── 报告生成 ──────────────────────────────────────────────────────────────────
jinja2==3.1.5                  # HTML 报告模板渲染
openpyxl==3.1.5               # Excel 报告导出

# ── 结构化输出 ────────────────────────────────────────────────────────────────
pydantic==2.10.4
pydantic-settings==2.7.0

# ── 日志 ──────────────────────────────────────────────────────────────────────
loguru==0.7.3

# ── HTTP 客户端 ───────────────────────────────────────────────────────────────
httpx==0.28.1

# ── 异步 ──────────────────────────────────────────────────────────────────────
anyio==4.7.0

# ── 测试集成 ──────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio>=1.3.0
